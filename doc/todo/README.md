# The todo directory

One file per piece of owed work. `doc/HANDOVER.md` says what the project *is*; these say what
it still owes, and they hold the detail that used to make that file long — the evidence, the
clause reading, the measurement and the cost of each item live with the item.

## Sorting

The number prefix **is** the priority, and `ls` sorts by it:

| band | means |
|---|---|
| `00`–`09` | **standing** — take some of this every round, whatever else is on |
| `10`–`19` | **defects**: wrong pixels or wrong output, with a diagnosis and usually a fix already argued |
| `20`–`29` | **owed features with corpus demand** — a real document asks and we refuse |
| `30`–`39` | **capability** — things the program cannot do at all, mostly hosts and interfaces |
| `40`–`49` | **performance**, each already measured and priced |
| `50`–`59` | **blocked** on a dependency, a decision or an infrastructure this program does not have |
| `_`-prefixed | not a todo: shared background several items refer to |

Within a band the number is a rough order, not a queue. `CLAUDE.md`'s two tracks still decide:
take from the *demand-driven* side (10–29, what the corpus and oracle name) **and** the
*spec-driven* side (00–09, what the ledger and the standard name) in every round.

## What a file holds

A header block — status, priority, corpus witnesses, the clauses, the code — then the argument.
A todo file is a place to *think*: what the clause determines, what has been measured, what the
fix would cost, and what has to be settled before it can be taken. When an item is done the file
is **deleted** and its argument lives on in an ADR, which is where a decision belongs.

## The index

| | item | corpus |
|---|---|---|
| [00](00-ambiguous-bucket.md) | Empty the oracle's ambiguous bucket, page by page | 5 names |
| [01](01-ledger-partial-rows.md) | Read the ledger's 252 `partial` rows against the code — and the **twelve** sweeps, the last of which reads quotation marks and has now found **five** populations nothing else checks (ADRs 0249, 0254, 0255) | — |
| [02](02-every-round.md) | **What every round does** — the gates, the sweeps, the binaries | — |
| [03](03-more-corpora.md) | **Three corpora added and the fetcher built** (ADR 0258): `pdf20examples`, `pdf-differences` and a sparse `pdfbox` under `doc/corpora/`, plus `tools/safedocs`, which takes a chunk of SafeDocs' 7 933 gigabyte archives through one byte range and refuses a plan over 32 MiB. **One of its two instruments is built** (ADR 0259): PDFBox's own frozen `*.pdf.txt` beside 40 of its PDFs, a second and undriftable reference for `text_extraction.rs`, whose first run found §9.10.2's permission declined for every `Identity-H` composite font. **The four-hundred-and-twenty-fifth took 79 archives, 1896 documents, 2.68 GiB — 5.4% of the owner's 50 GB** (ADR 0261) and found two things: the archives are **hash buckets rather than crawl neighbourhoods**, so this file's stratification rule is withdrawn and a round may go deep; and 67 of the sample's 86 reports are §11.4.7's page-group blending space, 3.5% of the web against 0.7% of the 974. Its own fix was §7.10.4's k — four shadings refused whole, and a `/Functions` naming its own object that overflowed the stack of a shipped binary, which is this project's **first crasher** and cost the promotion budget nothing. **The four-hundred-and-twenty-eighth closed the fuzzing item** (ADR 0264): `fuzz/fuzz_targets/page.rs` puts a whole document through `pdf_model::interpret`, which `nm` says no other target's binary even *contains* — twelve of the thirteen have no interpreter in them and the thirteenth calls it on a page with no `/Resources` — seeded by `fuzz/seed_page.py` from all 1944 + 108 + 974 documents on disk, **28 535 edges against the best of the thirteen at 6483**, and verified against the crasher itself at the pre-fix commit. Both of the reasons the item had been left open were false: `cargo-fuzz` was installed the whole time. **The four-hundred-and-thirtieth took four *whole* archives** — `0100 + 2000k`, 4000 documents for four directory reads where the stride paid 79 for 1896, **5.04 GiB, taking the running total to 7.72 GiB and 15.4% of the 50 GB** (ADR 0266). It is the first look at the web with §11.4.7's conversion built, and the residue is the result: **70 incomplete of 4000 (1.75%) against 86 of 1896 (4.5%)**, with §11.4.7's own population 67 → 24. Two defects out of it, both with clauses: §7.4.8's component count was being read off Adobe's APP14 marker rather than off the frame — `zune-jpeg` maps transform 0 to `CMYK` provisionally and defers the correction to a three-component `RGB`, so a three-component frame was asked for four channels and **21 images over four documents were lost whole** — and §7.3.8.1's empty stream, where the argument the item was waiting for turned out to be §7.3.8.2's: a truncation states a `/Length` the bytes do not support and an empty stream does not, which is the only thing that tells them apart. **Neither moves the 974.** The ranking it produced: the largest named residue is a **four-component `ICCBased` page group at 14 of 4000** where `doc/todo/23` had one witness, and the third-largest is a population nobody had named — **0.2% of the web reaches `MAX_TILES` or `MAX_OPERATIONS`**, stable across two samples, which is a finding about the budget and not a reason to raise one. What is left is *taking* a chunk a round, a licence reading for `openpreserve/format-corpus`, and the oracle over the new corpora. Promotion budget: 20 MB, and **0 MB of it is spent**. **The four-hundred-and-thirty-third surveyed the whole thing** — the owner's fast connection put **93 GB and 65 944 documents in 145 archives** on disk, and the round ran the survey over all of it, **one process per archive** because `render-cpu` rasterises under `panic = "abort"`. **Five archives produced no report and both of the round's defects are those five** (ADR 0269): three *hung* on a `OnceLock` initialiser that calls rayon — rayon runs other jobs while `collect` waits, so the initialising thread was handed one that re-entered the initialiser and waited for itself, which a twenty-line reduction reproduces 10 times in 10 — and two *aborted* in `tiny-skia`'s anti-aliased blitter on paths reaching 10²⁵ device units, now drawn by the aliased converter because §10.7 bounds no coordinate and §7.3.3 hands a number's range to the implementation. **The baseline is 1144 incomplete of 65 944, 1.735%**, against 1.75% of 4000 and **6.98% of the 974** — so 1.7% is a fact about the web and the corpus's four-times rate is what a bug-report corpus is for. §11.4.7's five conditions have numbers (151 / 106 / 78 / 27 / 7) and the resource budgets are **84, 0.127%**, stable at three sample sizes with none of them slow. **The four-hundred-and-thirty-fifth took both of the populations that round left** (ADR 0271). The **two slow documents** are one construct and it is this tree's: neither is slow to parse, and both spend their time in `build_soft_mask`, which drew every mask group into a buffer the size of the *target* and then demultiplied and derived a luminosity for all of it — `6081357.pdf` states **912 distinct soft masks** on a 4.3-million-pixel page and ran that pass over 3.87 billion pixels of which **99.96% were wholly transparent**, which is the case §11.6.5.1 answers with one constant. Naming it (`SoftMask::outside`) took the two from 25.8 s and 53.7 s to **6.6–7.3 s and 3.7–4.0 s**, three samples each, with **no page of the oracle's 1794 or quorra's 957 moving** — it is exact rather than approximate, because the branch's two arms call the same function . The survey's own `slow` count is **not** how that is stated, which three passes established: before 2, after 0, and a third pass on an idle machine 1 — a *third* document, `1284722.pdf`, at **11.13/11.14/11.23 s alone**, over the 30 s budget only under the survey's 24-way load. Every other survey line is identical, and `1284722.pdf` (11.1 s of `interpret`, 94 596 commands) is what this population offers next. The **83 budget-refused documents** (84 refusals; `7680183.pdf` reports two) were each opened with the bound lifted, and **none of the four bounds moved**: `MAX_FORM_DEPTH`'s four are *all cycles*, `MAX_TILES` is the only bound on a loop an empty cell hides from `MAX_OPERATIONS` — 1 000 000 empty tiles in 889 ms reporting nothing — `MAX_OPERATIONS`' 31 all terminate wanting 4.1–53.6 M operators, and `MAX_STATE_DEPTH`'s one witness wants 337 where §C.2's Table C.1 prints **28**. What is left is a *different* corpus and the `openpreserve` licence reading | 974 + **108** + **65 944** |
| [11](11-shapes-that-still-disappear.md) | **The sub-pixel fill and the sub-pixel stroke are closed** (ADR 0226) **and so is the diagonal one** (ADR 0268), which turned out to be the clause's *other* guarantee failing — `tiny-skia`'s hairline paints the line's longer device extent rather than its length, so a 45° rule carried 71% of its area — and to need no scan converter, only the same path stroked one device pixel wide at the alpha its own width implies. What is left is the rule that is **exactly** one device pixel wide, which the same hairline draws the same way and which is not a disappearance, the cap that substitute does not draw, and two marks abutting across a cell's box edge with no witness | 3 |
| [13](13-the-transfer-function.md) | §10.5's `/TR` decides what a screen shows, and this tree ignores it — the ledger's first `silent` row since session 35, and a question for the owner | 1 draws wrong |
| [21](21-font-substitution.md) | `Identity` orderings; per-character fallback, with no witness left. **The web's numbers arrived in the four-hundred-and-thirty-third** (ADR 0269) and the four-hundred-and-thirty-fourth read them (ADR 0270): the *silent* 51 272 codes over 635 documents are two populations, **28 837 over 359 that were never a mark missed** — a glyph the program contains and describes with no contours, which is how an sfnt stores a space — and **22 435 over 277 that were**. One mechanism holds most of the second: ten of §9.6.2.2's fourteen compiled-in faces carry only Latin, so a standard-14 name with Cyrillic in its `/Differences` lost it in silence. A substituted face is now replaced by one of its family whose code table over the declared range is a strict superset — **22 435 → 780 over 236 documents**, six web reports leaving, not one of the 974 rasters moved — and ADR 0152's silence is kept **with numbers**: 231 of the 236 lose four codes or fewer | 40 + **255** web |
| [22](22-variable-text-edges.md) | §12.7.4.3's remaining edges: a `/DA` font `/DR` lacks and cannot spell, a composite `/DA` font with no witness. The baseline's guard and the list box's cost both closed in the four-hundred-and-third (ADR 0240), the value a host could not read back closed twice over in the four-hundred-and-eleventh (ADR 0247) — the note had been stale for sixty sessions, and the *password* half of it was a live bug — and the list box's *host* half closed in the four-hundred-and-twelfth (ADR 0248), leaving the page's refusal exactly where the argument put it | 1 |
| [23](23-transparency-departures.md) | §11.4, §11.4.7 and §11.6.6, each reported where it can change a pixel — **§11.5.3's population is closed** (ADRs 0217, 0220), **§11.4.6's shape** (ADR 0234), **§11.4.4's non-isolated group** (ADR 0237), **§11.4.7's four components** (ADR 0262: §11.3.4's formula is per component, so the page is drawn twice and no raster format is needed) and **§11.7.2's conversion *into* the space** (ADR 0263: §11.7.5.3 names the conversion's target rather than its algorithm, so it is a right inverse of the ink cube and one page has one colour model). What stands needs a *second* colour space rather than a second direction: a document that names the press its `DeviceCMYK` is, a page group whose four components are not `/DeviceCMYK`, a group inside the page in another space, §11.3.5.3's rule for the black component, Table 57's black generation, and, inside §11.4.6, a knockout group whose elements blend. **The largest of them fell in the four-hundred-and-thirty-sixth** (ADR 0272): §14.11.5's Table 401 names `A2B` for a screen and §8.6.5.5 requires only the *file* to carry `B2A`, so the press a document names is sampled through the evaluator ADR 0009 already wrote — no dependency — and the conversion in is a right inverse of that sampling. **292 of the 65 703 documents that open name a press and all 286 four-component profiles among them parse**; the 151 that reported one are 0. What is left, after a re-survey of all 65 944 — **369 blending reports become 127 and 233 documents become complete**: **85** compose in a different space inside the page (with **30** more introducing one), **28** use a non-separable blend, **9** state Table 57's `/BG` or `/UCR` and **5** have components that are not four this tree can sample. The residue the round created is measured too — a grid of seventeen samples per axis is a median 5.99 and at most 14.52 of 255 from the profile, against the 48 to 51 that compositing in the wrong press costs | 6 + **127** web |
| [24](24-image-sampling-intent.md) | Carry an image *and its sampling intent* to the backends — **the vocabulary is built and the mask is on it** (ADR 0210); JPEG 2000's resolution level is written, measured and waiting on one push to `close2/hayro` (ADR 0233), and `render-gpu`'s sampled shading is the rest | 1 |
| [25](25-view-dependent-annotations.md) | `/FixedPrint`, which waits on a printing path | 15 |
| [26](26-icons-a-clause-only-recommends.md) | `Stamp`'s icon, whose standard names are legends rather than symbols | 1 |
| [27](27-the-interfaces-own-font.md) | The interface's own font: a character it cannot set is a box now; what is open is *coverage* | 74 |
| [28](28-a-catalogue-that-draws-nothing.md) | A catalogue that drew nothing: the YCCK image is decoded, §11.5.3's masks are finished (ADRs 0217, 0220), §11.4.6's knockout draws (ADR 0234) and §11.4.4's non-isolated groups do (ADR 0237); **§11.4.7's blending space is all that is left** and is priced at a four-component raster (ADR 0251), and one run over the file is a check rather than a question | 0, and that is the point |
| [30](30-a-native-host.md) | **All three hosts are built** — GTK4 (ADR 0244), Qt 6 through a C++ bridge (ADR 0246) and **the C ABI** (`crates/viewer-ffi`, ADR 0247), whose three amendments were taken first and one of which was a live bug rather than the doc sentence this file predicted. **No host added a message, three running.** What is left is *surface*: 39 entry points is not the whole vocabulary, and every missing one is a symbol that costs a compiled C caller nothing. **§12.7.5.4's list box — the one gap three hosts found — closed in the four-hundred-and-twelfth** (ADR 0248): `Edit::SetField` carries a set of Table 234 `/Opt` indices, both native hosts offer real multiple selection, and no *message* was added. **43 entry points since the four-hundred-and-fourteenth**, and `PDFV_EVENT_KIND_COUNT` moved 15 → 16 for the first time: a find bar in all three hosts needed `Command::Find` and `Event::Searched` (ADR 0250). What is left is the scale a form host draws at and `RadiosInUnison` | 102 of 964 documents have a delegable widget on page one |
| [31](31-accessibility-host.md) | AccessKit over `Query::AccessibilityTree` — **built and read back off a real AT-SPI bus** (ADR 0214); a `TH`'s axis, a `Form`'s control role, the `Text` interface and actions are what is left | — |
| [32](32-presentation-player.md) | **Seven of Table 164's twelve styles are drawn** (ADR 0230); `Blinds`, `Glitter`, `Dissolve` and `Fly` are reported by name, and §12.4.4.2's states have no control | — |
| [33](33-annotation-editing.md) | Editing a free text annotation the *file* states, and Table 177's `/CL` callout line — §12.5.6.10's markup, §14.8.2.5's logical order, the caret, the click that places it, the selection inside a value and §12.5.6.6's free text a person draws and types into are done | — |
| [34](34-sandbox-the-interpreter.md) | Confine the interpreter and rasteriser — **built, drawing real pages behind seccomp, answering all twenty-five questions and now stoppable** (ADRs 0218, 0223, 0241): a cancel is a kill, because a cancel a hostile document can decline is not one, and a 1567-byte document that draws for 44 s stops in 1.5 ms. What is left is the window, which is tier 2; items 4 and 5 are repriced with measurements and both need a decision this round refused to take blind | — |
| [35](35-confinement-off-linux.md) | Confinement on macOS and Windows — what the snapshot release cannot ship, and the three ways out | — |
| [36](36-a-retrieval-api.md) | Retrieve the standard from the standard. **The CLI is built** — `tools/pdf-retrieve`, JSON on stdout — and all three gaps are closed: §12.3.3's outline addresses a section, §12.5.6.10's `/QuadPoints` decide which section an annotation is in, and `Tree::walk` no longer sees half of ISO 32000-2 (ADR 0257). The REST shape is closed by the owner's note. What is left: **one `Query` variant** for a page's text on the confined pipe, and the substitution itself — clause existence free, 29 quotations to re-read | — |
| [38](38-a-documents-restrictions-have-levels.md) | A document's restrictions are the reader's to set: **the shape is built and Table 22 is consulted at last** (ADR 0212); *ask* and *warn* wait for a host that can answer, and bit 5's copy for a host that can name it | 7 assert something |
| [39](39-a-fragment-that-says-where-to-open.md) | Annex O's fragment identifiers: **eight of eleven carried out**, three reported by name. **`search` came off that list in the four-hundred-and-fourteenth** — `Command::Find` is a document-wide search, started as the document opens and walked one page per step by the host (ADR 0250) — and what is left is `fdf`, a fetch; `ef`, todo 38's *ask* level, the one part of it ADR 0212 deliberately did not build; and `highlight`, a concept the vocabulary lacks | 0, and no file could ever have one |
| [40](40-mask-chain-crop.md) | A clip chain as one crop and one intersect — **unblocked and re-priced** (ADR 0236), **and since the four-hundred-and-thirty-fifth it has a second buffer and two web witnesses**: a soft mask's buffer and a group's are target-sized where a clip mask's is band-sized, which is 4.3 GB allocated for 82 MB of band on `0423548.pdf` and 31.6 GB for 487 MB on `6081357.pdf`. The arithmetic over them is gone (ADR 0271); the copying is not, and `initial_backdrop` is 2.85 s of the first document's remaining 6.6. The part to settle is that a soft mask's outside value need not be zero: worth 42% of `MaskCache::get` rather than most of it, and the memory that blocked it was 27.9 MB in session 113 and is 12.31 today. The worst page halved this round for a different reason — 3490 `sh` operators drawn page-wide and kept in 24 pixels each — and what is left to settle is whether a parent's mask rows are *bit*-exact in a child's band, which ADR 0219 says they are not | 1 |
| [41](41-decoded-stream-cache.md) | 0.7% of interpretation, priced and not taken | — |
| [42](42-the-launch-path.md) | The launch path: 145 ms to 110, four of five items closed, the fifth is quorra's | all |
| [43](43-the-projects-own-turnaround.md) | **The *project's* performance rather than the program's**: a round was 608 s and is 268; §5's fat link is what is left, and caching our own renders is priced and refused | — |
| [45](45-where-a-frame-goes.md) | **What the instrument found once it could attribute a frame** (ADR 0227 closed todo 44). Two of its four items are closed by ADR 0228 — the 2 ms page-turn publication was two X11 round trips for a window position, and the bimodal translation was one image reduction paid per source sample. What is left: quorra's `encode`, 45% of a page turn and reported upstream as `QUORRA_FEEDBACK.md` §13; a reduced raster still recomputed on every redraw, wanting a witness; and no run on the owner's own machine | — |
| [46](46-a-region-of-a-huge-image.md) | Reduced resolution answers *zoomed out*; the witness is one tile with no precinct partition, so *zoomed in* needs code-block skipping and a bounded IDWT — census first | 1 |
| [47](47-search-performance.md) | Half answered: a **repeated** search is 7.27 ms where it was 5.45 s, and 0.021 s in the window (ADR 0256). A **cold** one is unchanged at 4.79 s, and 19.6% of it is §7.7.3.2's page tree walked from the root once per page. **Parallelism measured and declined on memory** — 1.18 s over 1023 pages for 966 MB of peak resident against 225 (ADR 0260) — and a second single-threaded lead found by counting: nothing memoises a decoded stream, so a *second* sweep re-runs 11 975 filter chains | all |
| [48](48-the-specification-we-check-against.md) | `doc/md/` dropped every annotation, and the census found the annotations are the **errata** — 11 462 in ISO 32000-2, 434 strikeouts over 4038 words. **All 120 distinct passages read** (`doc/errata-read.md`), and three clauses turned out to be implemented from retired text: §12.5.2's `/BM`, §14.13.5's property-list key, and §7.8.3's first step for a Type 3 glyph's resources. The **977 ledger spans ADR 0249 left unchecked are swept**, and a third population nobody had counted — quotation marks inside rustdoc prose — gave five more defects. What is left: §8.9.5.4, §14.8.6.3's enclosure requirement, single-quoted ledger spans, and — named in the four-hundred-and-nineteenth — every quotation of the standard in `doc/*.md`, `doc/todo/` and the 255 ADRs, which nothing reads at all. **That round swept a fourth population and a fifth**, a `"` inside an ordinary `//` comment and a quotation with an ellipsis in it, for six more stale quotations in six files (ADR 0255). **And the substrate question has a number since the four-hundred-and-twenty-first**: clause existence is free and the quotation half is 29 readings once spaces and dashes are folded (ADR 0257) | — |
| [49](49-restrictions-worth-re-examining.md) | **The budget half is answered in the four-hundred-and-thirty-fifth** (ADR 0271): all 83 documents the four interpreter bounds refuse were opened with the bound lifted, none of the four moved, and what the file now carries in their place is that **`MAX_TILES` and `MAX_OPERATIONS` count where they mean to weigh** — 30 of `MAX_OPERATIONS`' 31 are legitimate drawings and the thirty-first makes 495 marks from 53.6 M operators, so no larger count separates them. Which restrictions are load-bearing and which are habit: the search is a pump for the hosts, and `interpret` already uses rayon. **Four of five settled** — the readback cache under a 4 MiB bound with `--cache-text` declined by measurement (ADR 0256), item 5's structure-tree bound, which was hiding a quadratic walk (ADR 0257), and item 1's `!Sync`, which **was not the blocker it was called**: N documents in N threads never needed it. `Document` is `Sync` now at 0.021% of a page's instructions; the parallel search is declined on memory (ADR 0260). Item 3, the API that hands a pool in, is what is left | — |
| [51](51-signatures-and-public-keys.md) | A signature's three questions: **the first two are answered** — has the document changed since it was signed (ADR 0215), and does the signature verify under the RSA key in the certificate the file carries (ADR 0229). What is left is Table 260's DSA and ECDSA, both named at runtime, and the signer's *trustworthiness*, which is a certificate store and a network; public-key handlers; `/R` 5 | 10 signatures, 4 changed, **10 verify** |
| [52](52-zlib-rs-deallocates-through-the-wrong-pointer.md) | `zlib-rs` fails both of Miri's aliasing models — an upstream report to write | — |
| — | [`_scan-conversion.md`](_scan-conversion.md) | shared: §10.7.4, what this tree departs from and why |
| — | [`_image-codecs-and-the-sandbox.md`](_image-codecs-and-the-sandbox.md) | shared: the three sandboxed codecs are already pure safe Rust — what the sandbox is really for, corpus demand measured, and what a subset would and would not buy |

**Closed by decision rather than by work** — recorded in `doc/conformance/ledger.toml` and not
here: `/ColorTransform` (Table 13, whose one corpus witness contradicts the clause), a stream
whose data is in an external file (§7.3.8.1 — the renderer has no filesystem, principle 3),
§12.7.6.2's submit and §12.6.4's remote, launch, sound and movie actions (a network, a second
file, a media engine), a filled degenerate subpath's device pixel (§8.5.3.3.1, which the clause
itself calls "device-dependent and not generally useful"), and grid-fitting a stroke's
coordinates under `/SA` (see `_scan-conversion.md`).
