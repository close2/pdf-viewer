# The conformance ledger: one row per subclause of ISO 32000-2's technical clauses.

# GENERATED — the set of rows is. Their statuses are not: a status is a claim a person makes
# after reading the clause against this code, and `cargo run -p conformance --bin ledger`
# preserves every one of them. It only ever adds rows the standard has and this file lacks.

# Statuses, and what each is for. The vocabulary exists to keep five situations from wearing
# one word: the project choosing, the project not knowing, the project owing out loud, the
# project owing in silence, and the requirement having no meaning for a screen.

#   implemented   every normative requirement in the clause is executed; names code and test
#   partial       some are; the note says which, which are not, and what is reported
#   reported      not implemented yet, but detected and reported at runtime. Still owed
#   silent        not implemented, and nothing says so. A page is drawn wrong without a word
#   inapplicable  the requirement has no meaning for this device. Nothing is owed
#   writer-side   addresses a PDF generator; this program writes only §7.5.6's updates
#   out-of-scope  covered by CLAUDE.md principle 5's closed exclusion list, which the row names
#   unreviewed    nobody has read this clause against this code

# `unreviewed` is debt, not absence: it says only that the question has not been asked.
# `silent` is the status worth hunting. Every missing subsystem here reports, because whoever
# decided not to build it wrote the report; what ships is the gap inside a feature that is
# already there, and only reading the clause finds one.

# See doc/PLAN.md section 5a for the design, and tools/conformance for the checker that reads this.

[[clause]]
clause = "7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/lib.rs", "crates/pdf-model/src/lib.rs"]
test = ["crates/pdf-syntax/tests/real_documents.rs::every_specification_pdf_opens"]
note = "Clause 7's own map, and the sentence that decides the crate boundary this project is built on: \"[a] PDF file is a data structure\", read here by `pdf-syntax`, whose meaning is clause 8's onward and lives in `pdf-model`. Every subclause of 7 is reviewed except §7.11's file specifications and §7.12's extensions dictionary, which are the two this tree does not read."

[[clause]]
clause = "7.2"
title = "Lexical conventions"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::every_byte_value_terminates"]
note = "The whole of tokenisation, which `lexer.rs` is. The clause's framing decides the crate's shape rather than any one function: \"a PDF file is a sequence of bytes\", grouped into tokens, assembled into objects — so the lexer works on `&[u8]` throughout and hands the parser tokens that carry no meaning. §7.2.3 is where the byte classes are; §7.2.4 is why skipping white space and skipping a comment are one function."

[[clause]]
clause = "7.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::every_byte_value_terminates"]
note = "Framing, and one sentence with a consequence: \"A PDF file is represented as a sequence of 8-bit bytes, some of which are interpreted as character codes in the ASCII character set and some of which are treated as arbitrary binary data depending upon the context.\" Decoding to `str` anywhere in the lexer would either reject a valid file or invent replacement characters, which is why `Name` and `Object::String` hold bytes and `Name::as_str` is fallible. The three contexts the clause then names are read elsewhere: §7.9.2's text strings, §7.3.8's stream data, and clause 9's character codes."

[[clause]]
clause = "7.2.2"
title = "Representation"
status = "implemented"
code = ["crates/pdf-syntax/src/write.rs"]
test = ["crates/pdf-syntax/tests/incremental_update.rs::every_object_type_survives_being_written_and_read"]
note = "**Was `writer-side` until the hundred-and-thirty-seventh session, and the re-read moved it.** A reader implements none of this — it reads whatever bytes arrive — but since ADR 0121 this tree also *writes*, and every requirement here is on what it emits. All three are met by construction: \"[t]he tokens that delimit objects and that describe the structure of a PDF file shall use the ASCII character set\", and every token, keyword and key `write.rs` produces is ASCII; string data \"may be written either entirely using the ASCII character set or entirely in binary data\", and a string is written in §7.3.4.3's hexadecimal form, which is ASCII whatever the bytes are; and the file is handed to the host as bytes, which is the only way to transport it \"as a binary file rather than as a text file\". NOTE 2's warning that \"character\" here carries no logical meaning is the same distinction §7.9.2 draws for text strings and clause 9 for character codes, and both are read there."

[[clause]]
clause = "7.2.3"
title = "Character set"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::dictionary_and_array_delimiters_are_recognised", "crates/pdf-syntax/src/lexer.rs::every_byte_value_terminates", "crates/pdf-syntax/src/lexer.rs::a_digit_run_ending_in_letters_is_one_token", "crates/pdf-syntax/src/lexer.rs::a_number_does_not_end_in_the_middle_of_a_run", "crates/pdf-syntax/src/lexer.rs::a_number_against_a_delimiter_is_a_whole_number", "crates/pdf-model/tests/numbers_without_digits.rs::a_digit_run_that_swallows_an_operator_paints_nothing"]
note = "Table 1's six white-space characters exactly, including NUL, and Table 2's ten delimiters including the two braces type 4 functions add. `is_regular` is the clause's own definition — neither of the other two. The end-of-line rule that a CARRIAGE RETURN followed by a LINE FEED is one marker is applied where it decides content: after `ID` in an inline image (§8.9.7) and after `stream` (§7.3.8.1). Elsewhere an EOL is white space, which the clause permits: \"EOL markers may be treated the same as any other white-space characters.\" **The classification is a table since the five-hundred-and-thirty-fifth session and is still stated exactly once**: `class::REGULAR` is `[bool; 256]` built in a `const` block from the same two predicates, because `is_regular` is asked once per byte of every token and the two `matches!` are a dozen instructions together — the six white-space codes fit one 64-bit mask and the ten delimiters run from 37 to 125 and need two. On `doc/todo/44`'s witness `read_regular_run` was 12.86% of interpreting the page at about seventeen instructions a byte; against the table it is a load and a test, worth 5.84% of the whole. ADR 0370. **The boundary itself was checked in the five-hundred-and-fifty-seventh session from the side no test had taken**: a run of digits that continues into letters. `f` is regular, so `5f` is one token and no `f` operator exists — a lexer splitting the run at the last digit fills the rectangle before it, which is what hayro's issue 994 distinguishes the two readings by. This tree does not split it and paints nothing. What it does *not* do is report: the run is salvaged to the number 5 and the letters dropped, where §7.8.2 would have an unrecognised operator, and ADR 0303 scoped its correction to digit-less runs deliberately because the same leniency reads `12pt` as 12. `doc/todo/53`. **And since the five-hundred-and-eighty-ninth session this clause decides the token boundary in a second place** (ADR 0424): §7.3.3's fixed-format parse now runs straight off the cursor, so it stops where *its own* grammar ends rather than where the token does, and the answer is accepted only where the byte that stopped it is one this clause classifies as ending a token. `1e5`, `1,5` and `5f` are each one run and stay one token; a fast path that took its own stopping point for the token's would hand the interpreter an `e`, a `,` or an `f` as an operator. Asserted over all 256 byte values rather than over a list, because which bytes are regular is precisely what the test must not restate."

[[clause]]
clause = "7.2.4"
title = "Comments"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs", "crates/pdf-model/src/function.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::every_byte_value_terminates", "crates/pdf-model/src/function.rs::a_comment_runs_to_the_end_of_its_line"]
note = "\"PDF processors shall treat comments as single white-space characters for the purposes of lexical conversion … That is, a comment separates the token preceding it from the one following it.\" Which is why `Lexer::skip_whitespace` skips both and is the only skipping function: a caller that skipped white space without skipping a comment would join two tokens. A comment runs to the EOL marker and *not* through it, so the marker is then consumed as the white space §7.2.3 makes it. `%PDF-n.m` and `%%EOF` are §7.5's and are read there; every other comment has no semantics, as the clause says outright. **The clause has a second reader and it was wrong for the tree's whole life**: §7.10.5's type 4 programs are not lexed by `lexer.rs`, and `function.rs` split a program on white space *before* looking for a PERCENT SIGN — which destroys the very line this clause ends a comment at — then skipped one token as an approximation. A rule implemented twice is a rule that can be right once (ADR 0361)."

[[clause]]
clause = "7.3"
title = "Objects"
status = "implemented"
code = ["crates/pdf-syntax/src/object.rs", "crates/pdf-syntax/src/parser.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::integers_and_reals_are_distinguished"]
note = "The object model, which is `object.rs`, built by `parser.rs`. Read as a family in the thirty-ninth session; the subclause rows hold what each one decided."

[[clause]]
clause = "7.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/object.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::keywords_are_returned_verbatim"]
note = "\"PDF syntax includes nine basic types of objects: boolean values, integers, real numbers, strings, names, arrays, dictionaries, streams, and the null object.\" `Object` has exactly those nine variants plus `Reference`, which the clause's next paragraph makes the labelling of any of them (§7.3.10). The module comment said *eight* for the project's whole life, having merged integer and real — the pair §7.3.3 keeps apart and `as_integer`/`as_number` keep apart in code."

[[clause]]
clause = "7.3.2"
title = "Boolean objects"
status = "implemented"
code = ["crates/pdf-syntax/src/parser.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::keywords_are_returned_verbatim"]
note = "Two keywords. They arrive as `Token::Keyword`, because nothing lexical distinguishes `true` from `obj`, and the parser turns them into `Object::Boolean` — which is also why an unknown keyword where an object belongs is an error naming what it found rather than a silent `false`."

[[clause]]
clause = "7.3.3"
title = "Numeric objects"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs", "crates/pdf-syntax/src/object.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::malformed_numbers_salvage_a_leading_value", "crates/pdf-syntax/src/lexer.rs::a_run_with_no_digit_is_not_a_number", "crates/pdf-syntax/src/lexer.rs::the_fixed_format_parse_agrees_with_the_standard_library", "crates/pdf-syntax/src/parser.rs::a_number_with_no_digit_is_refused_rather_than_read_as_zero", "crates/pdf-model/tests/numbers_without_digits.rs::a_font_size_that_states_no_digit_is_reported_rather_than_read_as_zero", "crates/pdf-syntax/src/lexer.rs::a_number_longer_than_the_fast_path_is_still_the_number_written"]
note = "Both numeric types, and the sentence that makes them one where it matters: \"Wherever a real number is expected, an integer may be used instead\", which is `Object::as_number` accepting either and is why nearly every caller uses it rather than `as_integer`. The clause's own forms are all lexed, including `4.` and `-.002`. Two deliberate departures from strictness, both toward reading files that exist: PostScript radix and exponential notation are forbidden to a *writer* and this lexer accepts the exponential form rather than rejecting it, and a malformed number such as `--5` or `1.2.3` is salvaged to its leading value rather than aborting the page. Annex C's ranges are `i64` and `f64`, both wider than the clause's typical limits. **A number longer than the fixed-format fast path is checked since the five-hundred-and-fifty-seventh session**, and the check is about the fall-through rather than the fast path: past fifteen digits `fixed_format_number` declines and `str::parse` answers, so a 23-digit integer, `u64::MAX` and `1e-21` all come back correctly rounded. The failure mode it guards against is a `wrapping_mul(10)` accumulator, which turns a coordinate into a number modulo 2^64 with no bound on the error — hayro's issue 1341, found by a CAD producer writing coordinates at full `f64` precision. The one behaviour worth knowing: a token spelled as an integer that `i64` cannot hold becomes `Object::Real`, so `as_integer` refuses it rather than corrupting it. **A third departure was here until the four-hundred-and-sixty-eighth session and was not a departure but a silence** (ADR 0303): both forms are stated as \"one or more decimal digits\", so a run holding none is no number at all, and this lexer read every one of them as `Integer(0)` on a note saying that is \"what other viewers do\" — principle 5's forbidden direction, and the fallback trap 5 forbids. Such a run is now the `Keyword` it lexically is, which the parser refuses where an object was expected (§7.3.7) and the interpreter reports as an operator it does not recognise (§7.8.2). The population is small and was counted on both sides before the condition was believed: 3 of the 974, 2 of the 267 hand-made and 7 of 65 944 crawled web documents change a report, nine of the twelve were already reporting something else, and three display lists of 11 349 move. Only `issue9252.pdf` changes a pixel — its producer wrote `. .59 .84 rg` meaning zero, so the `rg` is now refused for want of an operand and its word is black rather than teal, which all three reference renderers draw in colour. That disagreement is a question answered by this clause rather than a target to move toward. A run that *does* state a digit and still salvages nothing — `.-1`, the sign arriving after the point — keeps the older reading of zero; that is a different question and no corpus offers a witness for it. **The order the two readings are tried in changed in the five-hundred-and-thirty-fifth session and the readings did not** (ADR 0370): `fixed_format_number` — the clause's own grammar, ADR 0341's fast path — is asked *before* the scan for a decimal digit, because both forms are stated as \"one or more decimal digits\" and so a run that grammar accepts is a run holding one, which the scan could only have agreed with. What it removes is a second walk over every well-formed number's bytes; a run either function declines reaches the same `from_str`-then-salvage path in the same order, so every malformed form keeps the reading this note describes. **The five-hundred-and-eighty-ninth session then removed the first walk as well** (ADR 0424): `fixed_format_number` reads from the cursor and reports how many bytes it consumed, so a well-formed number is walked once instead of twice — the run is no longer found first. It stops at the first byte outside the grammar instead of refusing the whole run, and §7.2.3 decides whether that byte ends the token; a run it declines reaches the same `from_str`-then-salvage path, in the same order, so the readings are again unchanged. Worth 5.4% of interpreting `doc/todo/44`'s witness, whose page is 17.65 million numbers in 20.83 million tokens, and byte-identical display lists over 1178 first pages."

[[clause]]
clause = "7.3.4"
title = "String objects"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::literal_strings_handle_nesting_and_escapes"]
note = "\"A string object shall consist of a series of zero or more bytes\" — bytes, so `Object::String` is `Arc<[u8]>` and never a `String`. The two written forms are §7.3.4.2 and §7.3.4.3. Annex C's implementation limit on length is `Limits::max_string_len`, and exceeding it is an error naming the limit rather than a truncated string."

[[clause]]
clause = "7.3.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs", "crates/pdf-syntax/src/parser.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::hex_strings_ignore_junk_and_pad_an_odd_digit"]
note = "The two ways a string may be written, and the length limit. Both forms produce the same `Object::String`, which is what lets §7.9.2's text-string decoding be one function that never asks which syntax delivered the bytes."

[[clause]]
clause = "7.3.4.2"
title = "Literal strings"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs", "crates/pdf-model/src/variable_text.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::literal_strings_handle_nesting_and_escapes", "crates/pdf-model/tests/variable_text.rs::quadding_moves_the_line_within_its_box", "crates/pdf-model/tests/variable_text.rs::a_composite_da_fonts_codes_are_as_long_as_its_codespace_says"]
note = "Read in the lexer and, since §12.7.4.3's variable text, written as well: the appearance stream this tree constructs shows a field's value as a literal string, so the clause's escapes have to be produced rather than only consumed. `variable_text::show` escapes the two parentheses and the reverse solidus the clause names and writes every other non-printable byte as its three-digit octal escape, which is the same subclause's octal form. **A code is its bytes rather than a byte**, since the five-hundred-and-second session: a composite `/DA` font's code occupies one to four bytes (§9.7.6.2), so all of them are written, most significant first, each still escaped by this subclause's rules — writing only the low byte produced a literal string that decoded back to entirely different codes. The reading side takes all eight escape sequences, the line continuation, and the rule that an unbalanced parenthesis is a lexical error. Errata Collection 3 retitles the `\\ddd` row (Issue #494, `/State` `Review` `Completed`) from 'Character code ddd (octal)' to 'Byte with value ddd in octal.', which is what `Lexer::read_literal_string` does — the comment beside it justified the masking by what other implementations do and was corrected in the four-hundred-and-seventeenth session against the clause's own 'high-order overflow shall be ignored'. ADR 0253."

[[clause]]
clause = "7.3.4.3"
title = "Hexadecimal strings"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::hex_strings_ignore_junk_and_pad_an_odd_digit"]
note = "Both of the clause's rules: every character outside `0`-`9`, `A`-`F` and `a`-`f` is ignored, and 'If the final digit of a hexadecimal string is missing … it shall behave as if a 0 (zero) followed the last digit.' Written as well as read, in one place — the §12.7.4.3 fuzz target puts its bytes into a `/DA` and a `/V` as hexadecimal strings so that no input can close the object around them."

[[clause]]
clause = "7.3.5"
title = "Name objects"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs", "crates/pdf-syntax/src/object.rs", "crates/pdf-model/src/content/resources.rs"]
test = ["crates/pdf-model/tests/missing_resources.rs::a_resource_name_that_is_not_utf_8_is_found", "crates/pdf-model/tests/missing_resources.rs::two_names_differing_only_in_a_byte_outside_utf_8_are_two_names", "crates/pdf-syntax/src/lexer.rs::names_resolve_hash_escapes", "crates/pdf-syntax/src/object.rs::borrows_exactly_as_it_compares", "crates/pdf-syntax/src/object.rs::a_borrowed_key_finds_what_an_owned_one_stored", "crates/pdf-font/src/composite.rs::an_escaped_cmap_name_is_the_name_it_spells", "crates/pdf-model/tests/names_are_bytes.rs::a_colour_space_name_that_is_not_utf_8_is_found", "crates/pdf-model/tests/names_are_bytes.rs::two_colour_space_names_differing_only_outside_utf_8_are_two_names", "crates/pdf-model/tests/names_are_bytes.rs::a_type_3_glyph_name_that_is_not_utf_8_paints", "crates/pdf-model/tests/names_are_bytes.rs::two_type_3_glyph_names_differing_only_outside_utf_8_are_two_names", "crates/pdf-model/tests/names_are_bytes.rs::an_appearance_state_that_is_not_utf_8_selects_its_stream", "crates/pdf-model/tests/names_are_bytes.rs::two_appearance_states_differing_only_outside_utf_8_are_two_states"]
note = "Names, with `#xx` resolved at lex time so that `Name` holds the clause's own \"sequence of characters\" and a comparison against `Type` is a byte comparison — which is the clause's definition of distinctness: \"Uniquely defined means that any two name objects that, after all escaping is expanded (see below), and the resulting sequences of bytes are not an exact binary match denote different objects.\" (That sentence stood in this note for a long time as \"PDF name objects are considered distinct objects if, after all escaping is expanded, the resulting sequences of bytes are not an exact binary match\" — most of the clause's words in a construction it does not use, inside quotation marks. Corrected in the five-hundredth session against `doc/md/`, which is the shape §4's ledger-quotation sweep exists to find.) **That sentence is now load-bearing in code rather than only cited**: `Name` implements `Borrow<[u8]>`, so `Dictionary`'s `BTreeMap` is probed with the caller's bytes and allocates no key — sound precisely because the clause makes a name's identity its bytes', and guarded by `borrows_exactly_as_it_compares`, which asserts that `Name`'s `Ord`, `Eq` and `Hash` are the bytes' over samples chosen for a prefix, a byte above 127 and the empty name. ADR 0335 has the A/B. The empty name — \"The token SOLIDUS (a slash followed by no regular characters)\" — is a name, not an error. `Name::as_str` is the clause's UTF-8 sentence, and it is fallible because that sentence is a \"should\" about names that stand for text. Two lenient readings, both named: a name containing a null via `#00` is accepted where the clause excludes character code 0, and a `#` not followed by two hexadecimal digits is kept literally, since dropping it would turn one valid name into a different valid name. **And the clause binds the *reader* of a resource name as well as the parser of one, which took a crawled document to notice** (ADR 0438): `pdf-model`'s interpreter carried a content stream's name operand as a `String` built with `from_utf8_lossy`, so `/Contr#F4le` was probed as `Contr` + U+FFFD + `le` while the dictionary held the file's own 0xF4 — \"not an exact binary match\", so the `Do` found nothing, the page came back blank and the report said the file was malformed when it was not. Every lookup in `crates/pdf-model/src/content/resources.rs` takes a `Name` now and probes with `get_by_name`; the lossy conversion stayed where the same clause permits it — \"occasionally the need arises to treat a name object as text\" — which is a report or a trace label. The reverse direction had no witness anywhere on this disk and is the worse one: two names differing only in a byte outside UTF-8 collapsed onto one replacement character, so a `Do` could draw the object it did not name. `crates/pdf-model/tests/missing_resources.rs` pins both. **The sentence binds every lookup and not only the resource one**, which the six-hundred-and-fourth session established by sweeping for the *lookup* rather than for the conversion: a dictionary key that is not a string literal is a key the document supplied, and five more of them were being probed as text — a Type 3 glyph name in `/CharProcs` (§9.6.4), an appearance state in `/AP`'s `/N` (§12.5.5, twice), a colour space name in `/ColorSpace` (§8.6.8 and §8.9.7's inline form), and a structure type in `/RoleMap` and a class in `/ClassMap` (§14.7.3, §14.8.6.2). `Document::get_key_by_name` is what they probe with now. ADR 0439 has the sweep, its discrimination test and the one site left owed."

[[clause]]
clause = "7.3.6"
title = "Array objects"
status = "implemented"
code = ["crates/pdf-syntax/src/parser.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::adversarial_structures_hit_their_limits"]
note = "\"An array may have zero elements\", elements may be of any type, and higher dimensions are arrays of arrays — all of which `Vec<Object>` is without a rule. What needs a rule is the bound: Annex C's implementation limit is `Limits::max_array_len`, and reaching it is an error rather than a shortened array, because a truncated `TJ` array renders a wrong page and reports success (the operand cap that ended three sentences mid-word on the specification's own title page). **The clause's opening sentence became load-bearing in a *content* stream in the sixty-third session**: an array is \"a one-dimensional collection of objects arranged sequentially\", so a keyword between two of its elements is not an element, and §7.8.2 puts an operator after its operands rather than inside one — which makes it not an operator either. `operator-in-TJ-array.pdf` writes `[(Grandes) 0.0 Tc … ] TJ` and had been drawing one word of five, because the content interpreter flattens arrays and the `Tc` reached the operator dispatch. It is skipped and reported now: the file is malformed and no clause states a recovery, so drawing the text without saying so would be the silent fallback principle 3 forbids."

[[clause]]
clause = "7.3.7"
title = "Dictionary objects"
status = "implemented"
code = ["crates/pdf-syntax/src/object.rs", "crates/pdf-syntax/src/parser.rs"]
test = ["crates/pdf-syntax/src/parser.rs::a_repeated_key_keeps_the_first_value", "crates/pdf-syntax/src/parser.rs::a_null_value_reads_the_same_as_an_absent_key", "crates/pdf-syntax/src/parser.rs::a_null_value_does_not_survive_beside_a_real_one"]
note = "Keys are names and only names — a token of any other kind where a key belongs is skipped rather than being fatal, which loses one entry instead of the dictionary. Entries are unordered, which a `BTreeMap` makes true rather than claimed. \"A dictionary entry whose value is null … shall be treated the same as if the entry does not exist\" is now enforced where a dictionary is built, rather than asserted through `Document::get_key` — which answers `Object::Null` for an absent key and so made the old test the implementation restated, while `Dictionary::get` still returned `Some(Null)`, `len` still counted the entry and §8.9.7's presence test for an abbreviated key could still see it. \"Multiple entries in the same dictionary shall not have the same key\" is a requirement on the *file* and the clause states no recovery — nor can one be derived, since the entries \"shall be unordered even though an arbitrary order may be imposed upon them when written in a file\" and \"[t]hat ordering shall be ignored\", so a rule preferring the first or the last reads the very order the clause discards. Keeping the first is therefore a **documented choice** rather than a derivation (principle 5); poppler agrees, which is evidence that it is unsurprising and is not a reason for it. `inline_image.rs` documents the one place that is decided differently and why. Annex C's implementation limits are `Limits::max_dict_len`."

[[clause]]
clause = "7.3.8"
title = "Stream objects"
status = "implemented"
code = ["crates/pdf-syntax/src/parser.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/real_documents.rs::the_first_page_content_stream_decodes"]
note = "Stream objects: the syntax in §7.3.8.1 and the length rules in §7.3.8.2. Table 5's entries are read where they are used — `/Length` and `/Filter` and `/DecodeParms` by the parser and `Document::decoded_stream_data`, `/F` by `Document::is_external`, `/DL` nowhere, since it is \"only a hint\". **The one question this row did not have a word for is *how* a caller reads the data**, which ADRs 0365 and 0427 made a decision rather than a given: `Document::stream_source` and `Document::nested_content_source` answer \"whole, or a window at a time\" where `decoded_stream_data` answers \"what does it decode to\", and the second question has an allocation the size of the answer in it. Both routes run §7.4's chain through the same filter table and both refuse the same streams for the same reasons; what differs is where the bytes go."

[[clause]]
clause = "7.3.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/parser.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/src/document.rs::a_stream_whose_data_is_in_a_file_has_no_data_here", "crates/pdf-model/tests/contents_entry.rs::a_part_the_file_states_is_empty_is_not_a_missing_content_stream", "crates/pdf-syntax/tests/real_documents.rs"]
note = "Stream objects are read: the dictionary, the `stream` keyword with its EOL, `/Length` including the indirect form, and `endstream`. Two sentences are load-bearing and only the first was read before. That `stream` is followed by CRLF or LF and not by CR alone is the same one-marker rule §8.9.7 applies after `ID`; a CR alone is tolerated anyway, because files write it. The second was a silent gap for the project's whole life: \"beginning with PDF 1.2, the bytes may be contained in an external file, in which case the stream dictionary specifies the file, and any bytes between stream and endstream shall be ignored by a PDF processor\" — and those bytes were being decoded and drawn. `Document::is_external` now refuses such a stream, which is the answer principle 3 leaves available, since the renderer has no filesystem to fetch the file from; Table 5's `/FFilter` and `/FDecodeParms` describe the external data and are unread for the same reason. **No corpus document writes one**, measured, so nothing but reading §7.3.8 could have found it. What is still not the clause's is the recovery for a wrong `/Length`: the parser scans for `endstream`, which is the same answer by a different route (§7.3.8.2). **And the indirect form was only half read until the five-hundred-and-thirty-first session**, which is trap 5's own failure mode — a clause with two routes and one of them implemented in silence. A parser cannot resolve a reference, so it took the scan's answer and dropped one end-of-line before `endstream`; where a producer wrote none — this clause asks for one with a *should* — the byte dropped was the data's. `Document::with_stated_length` applies the file's own number one layer up, under the same guard the parser puts on a direct length. ADR 0366. **And the clause's first sentence had a consequence nobody had drawn until the four-hundred-and-thirtieth session**: \"A stream shall consist of a dictionary followed by zero or more bytes bracketed between the keywords stream (followed by newline) and endstream :\", so a stream holding nothing is conforming, and Table 5 has `/Filter` name what \"shall be applied in processing the stream data found between the keywords stream and endstream \" — with no data found there, nothing is applied and the result is the empty sequence. `flate` refuses zero bytes, because RFC 1950 gives a zlib stream a six-byte floor, so `<< /Filter /FlateDecode /Length 0 >>` was reported as a content stream this reader could not decode, on a page that was missing nothing. `Document::states_no_data` is the condition, and it asks §7.3.8.2 as well as this clause for the reason that row gives. ADR 0266."

[[clause]]
clause = "7.3.8.2"
title = "Stream extent"
status = "implemented"
code = ["crates/pdf-syntax/src/parser.rs", "crates/pdf-model/src/image.rs", "crates/pdf-model/src/function.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::a_lying_stream_length_is_recovered_from", "crates/pdf-model/tests/contents_entry.rs::a_part_truncated_to_nothing_is_still_reported", "crates/pdf-model/tests/image_extent.rs::an_image_a_row_short_leaves_that_row_unpainted_and_reports_it", "crates/pdf-model/tests/sampled_function_extent.rs::a_sampled_function_whose_stream_is_short_is_refused_rather_than_filled_with_zeroes", "crates/pdf-syntax/tests/stream_length_bound.rs::an_indirect_length_is_the_files_own_statement_of_where_a_stream_ends"]
note = "\"Every stream dictionary shall have a Length entry\", the filters are mostly self-limiting, and \"[a]ll of these constraints shall be consistent\". The parser takes `/Length` when it is a direct integer *and* `endstream` is where it says, and searches for `endstream` otherwise — which covers the indirect form the clause's own EXAMPLE 3 shows, and the wrong-`/Length` corruption. The clause's closing allowance is honoured in the same place: \"there may be an extra end-of-line marker in the PDF file before the keyword endstream\". Reading a filter's EOD to bound the data instead is a third route the clause offers and this tree does not take; it would only matter for a file where `/Length` is wrong *and* `endstream` cannot be found, which is a file with no stream in it. **And the same recovery is what makes this clause the discriminator for §7.3.8.1's empty stream**, found in the four-hundred-and-thirtieth session: a stream truncated to nothing arrives holding no bytes exactly as an empty one does, so \"the bytes are empty\" cannot tell them apart. This clause can. \"Every stream dictionary shall have a Length entry that indicates how many bytes of the PDF file are used for the stream's data\", and \"[a]ll of these constraints shall be consistent\" — a truncation leaves `/Length` stating bytes that are not there, and only a stated zero the bytes agree with is a silence the producer asked for. Two SafeDocs documents of 5944 state one and two more are truncations. ADR 0266. **And the clause's third constraint had no reader at all until the five-hundred-and-twenty-first session**, which is the one its own EXAMPLE is about: \"streams are used to represent many objects from whose attributes a length can be inferred\", and \"[a]n error occurs if Length is too small, if an explicit EOD marker occurs too soon, or if the decoded data does not contain 200 bytes.\" Two objects in this tree infer one. An **image** whose decoded samples fall short of `/Width` × `/Height` × the components × `/BitsPerComponent` used to have the shortfall read as *zero samples* — black rows in a `DeviceGray` picture, and a marked page for an `/ImageMask`, which is how `178360.pdf` painted 99.3% of a stencil nobody wrote; what the stream carries is now drawn where it belongs, the rest of the grid is left unpainted, and `image::short_of_its_grid` reports it beside the drawing. A **sampled function** is refused instead, on §7.10.2's own sentence and trap 5's substitutive test. The population is the crawl's: **54 short images in 8 of 65 944 documents and not one short sampled function**, `examples/damaged_stream_census`. ADR 0356. **And the sentence about `/Length` itself was read further in the five-hundred-and-thirty-first**: the clause's EXAMPLE 3 shows the indirect form, and taking the scan's answer for it rather than the file's cost a byte on every stream whose producer wrote no end-of-line before `endstream` — which for a `FlateDecode` stream is usually RFC 1951's final block, so the stream read as *truncated* while being whole. `Document::with_stated_length` takes the stated end where `endstream` is actually there, which is the same guard the parser applies to a direct length; a wrong `/Length` still loses to the search. It moves the damaged-stream population by two thirds — 2260 damaged streams over 726 crawled documents to 1273 over 99, and 90 damaged page-one `/Contents` to 28 with the *corrupt* count unchanged at 21, which is the mechanism's own signature — so sessions 521's and 524's figures were measurements of this reader rather than of the files. A third statement of the same clause, and the one that made the other two countable. ADR 0366. **And the search that recovery rests on is on the launch path rather than on an error path, which nobody had priced until the five-hundred-and-eighty-ninth** (ADR 0424): a producer that writes the indirect form Table 5 calls for gets the scan on every stream, and on `doc/todo/44`'s witness — one 49.7 MB content stream with an indirect `/Length` — comparing nine bytes at every offset was 446 M of the 11 471 M it takes to interpret page one, about five instructions a byte. Looking for the `e` first and comparing the other eight only there is the same answer for 253 M. What is still paid twice on that file is the *data*: the parser copies the scan's answer into an `Arc<[u8]>` and `with_stated_length` then copies the file's answer into a second one, 99 MB of memcpy for a 49.7 MB stream, which is `doc/todo/10` §3's residue and not this row's to fix. **And the clause's three constraints were read again in the five-hundred-and-ninety-second, from the side road D asks them from: none of them bounds a *decoded* stream.** \"Every stream dictionary shall have a Length entry that indicates how many bytes of the PDF file are used for the stream's data.\" — and \"(If the stream has a filter, Length shall be the number of bytes of encoded data.)\", which Errata Collection 3 sharpens further with a NOTE at Issue #319: the encoded data \"encompasses all enveloping markers of the encoding, e.g. end-of-data markers\". The second constraint, \"most filters are defined so that the data shall be self-limiting; that is, they use an encoding scheme in which an explicit end-of-data (EOD) marker delimits the extent of the data\", bounds the *input* and says nothing about how much comes out of it; the third, \"Finally, streams are used to represent many objects from whose attributes a length can be inferred.\", holds only for the objects §7.10.2 and Table 17 give attributes to, and a content stream is not one of them. So **the standard offers a reader no way at all to know a content stream's decoded extent before decoding it**, which is not a gap but the reason road D exists: a reader that cannot pre-size either allocates whatever the producer's compressor asks for or reads through a window, and there is no third answer to derive. ADR 0427, and `doc/todo/14` for what is left. The same second constraint is the standard's own statement that a filter's output is produced sequentially up to its EOD, which is what makes the four filters that are not Flate pumpable in principle."

[[clause]]
clause = "7.3.9"
title = "Null object"
status = "implemented"
code = ["crates/pdf-syntax/src/object.rs", "crates/pdf-syntax/src/parser.rs"]
test = ["crates/pdf-syntax/src/parser.rs::a_null_value_reads_the_same_as_an_absent_key"]
note = "One keyword, one value, and two rules that live elsewhere and both point here: an indirect reference to a nonexistent object is null (§7.3.10), and a dictionary entry whose value is null is an absent entry (§7.3.7). `Object::Null` is therefore what `Document::get_key` answers for every kind of nothing there is, which is what makes those two rules one behaviour rather than three. **And the four-hundred-and-twenty-second session found what that unification costs**: \"Specifying the null object as the value of a dictionary entry (7.3.7, \"Dictionary objects\") shall be equivalent to omitting the entry entirely\" is a rule about the *value*, and a reader that also makes it a rule about what it says out loud cannot tell a page whose producer stated no `/Contents` from one whose `/Contents` names an object the file does not define. Both draw nothing; only the second is worth a word. `Page::content_with_report` keeps the object as written beside the resolved one for that reason alone (§7.7.3.3, ADR 0258)."

[[clause]]
clause = "7.3.10"
title = "Indirect objects"
status = "implemented"
code = ["crates/pdf-syntax/src/parser.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::a_reference_cycle_resolves_to_null_rather_than_looping", "crates/pdf-syntax/tests/cross_references.rs::a_body_object_the_table_does_not_list_is_not_reachable"]
note = "Indirect objects and references. `obj` … `endobj` with an object and generation number, `R` references resolved on demand and cached, and the clause's own rule for a reference that goes nowhere: \"An indirect reference to an undefined object shall not be considered an error by a PDF processor; it shall be treated as a reference to the null object.\" That is what `Document::get` returns for a missing entry, and as of the twenty-second session also for a reference that is reached while the object containing it is still being loaded — a cycle is a reference that resolves to nothing. **Errata Collection 3 gives the two numbers a grammar this lexer is more tolerant than** (Issue #379, `/State` `Review` `Completed`, read in the four-hundred-and-seventeenth session and recorded here in the four-hundred-and-eighteenth): an object number and a generation number are now defined without a leading PLUS SIGN and without leading zeros, so `+5 0 obj` and `007 0 obj` are malformed rather than merely undescribed. `Lexer::integer` accepts both and this reader keeps them, because refusing an object header costs the object and a producer's spelling of a number is not a question about what the object is. It is a reader's tolerance of a malformed file, which is the ordinary shape here, and it is now a *documented* one; `doc/md/` shows none of the erratum (ADR 0252). **The clause's own permission is honoured and, since the four-hundred-and-twenty-second session, is no longer honoured in silence where it decides a page.** Not considering it an error is what the sentence asks for; drawing a blank page and saying nothing is a second thing the sentence does not ask for, and `ContentIssue::Unreachable` is the difference. Its witness is `doc/corpora/pdf-differences`'s `UnknownFilter-PageContentStream.pdf` — the first defect this tree found from outside the pdf.js corpus — and the pdf.js corpus's own count did not move (ADR 0258)."

[[clause]]
clause = "7.4"
title = "Filters"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::lzw_decodes_the_clauses_own_example", "crates/pdf-syntax/src/filter.rs::image_codecs_are_reported_as_unsupported", "crates/pdf-syntax/src/filter.rs::run_length_expands_literal_and_repeated_runs"]
note = "Table 6's ten standard filters, reviewed as a family in the twenty-seventh session, and **all ten decode** — `LZWDecode` was the last one absent and landed in that session. Four of them are stream filters implemented here, one is a pass-through for §7.6.6's crypt filters, and four are image codecs that belong to the image pipeline and answer `None` here so that a *content* stream naming one is visibly unsupported rather than silently empty. **A fifth answer was added in the four-hundred-and-seventy-first session and it is not a filter at all**: `FilterRefusal` separates \"this module does not decode that\" from \"the data is damaged\" from \"the decode passed `Limits::max_stream_len`\", because two of the four stream filters used to answer the third by handing back the prefix they had decoded and saying nothing. `Document::decoded_stream_data_reported` carries the distinction up and `ContentIssue::TooLarge` is what a page reports. ADR 0306. **A sixth answer arrived in the five-hundred-and-eighth and it is not a refusal either**: `filter::Decoded` carries a `Damage` beside the bytes, so a stream that decoded only as far as its damage is distinguishable from one that reached its own end-of-data. Three of the four stream filters can say it — `FlateDecode`, `LZWDecode` and `RunLengthDecode`, each of which has an end-of-data marker to miss — and they all kept a prefix in silence before, which is trap 5 in the one place that had a plausible excuse. `ContentIssue::Damaged` is what a page reports and `pdf_font::program` is what refuses one. ADR 0343. **`FlateDecode`'s loop is split from its judgement since the five-hundred-and-nineteenth**, so that a test can read the buffer's `capacity` rather than only the report: a bound that caps an allocation is not observable in the bytes a caller gets back, and it was twice its stated value for two rounds while every test of it passed. ADR 0354. **And a seventh answer arrived in the five-hundred-and-thirtieth, which is a shape rather than a statement**: `filter::Pump` is `FlateDecode` made resumable — the decoder held across turns, writing into a caller's window, answering `Wrote`, `Ended` or `Damaged` each time — so a page's content stream can be read without existing all at once (ADR 0365). It is not a second decoder: `turn()` classifies RFC 1951's three outcomes for the pump and for `inflate_buffer` alike, so the whole-buffer route and the window route cannot drift. The other four stream filters have no pump yet and are decoded whole, which `Document::stream_source` decides and `doc/todo/14` carries: writing a second implementation of each beside the existing one is trap 6, and the fix is to make each existing one resumable."

[[clause]]
clause = "7.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/document.rs", "crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/tests/filters.rs::a_filter_array_is_a_pipeline_decoded_in_the_order_written", "crates/pdf-syntax/tests/filters.rs::each_stage_takes_its_own_entry_of_a_decodeparms_array"]
note = "`/Filter` and `/DecodeParms` in both their forms — one name or an array — with the array \"cascaded to form a pipeline that passes the stream through two or more decoding transformations in sequence\", applied in order, each stage taking the matching entry of a `/DecodeParms` array. Table 92's abbreviations are expanded for inline images by `pdf_model::inline_image`. Not implemented and named rather than left implicit: `/FFilter` and `/FDecodeParms`, which belong to an *external* stream — this reader does not follow `/F` to another file at all, so the parameters that would describe how to decode it are not the missing piece. **The pipeline is memoised since the four-hundred-and-eighty-second session** and the clause is what decides the key: the pair of entries above *is* the transformation, so two streams over one buffer with different chains are two decodes and are kept apart by comparing the chain. ADR 0317. **The clause's own sentence has two halves and the second is what a damaged stream fails**: a reader \"shall invoke the corresponding decoding filter or filters to convert the information back to its original form\", and a filter that stopped short did the first and not the second. Since the five-hundred-and-eighth the difference is a value rather than an assumption — `filter::Damage`, carried up by `Document::decoded_stream_data_reported` and memoised in the decoded-stream cache beside the bytes, because a report that depended on whether the cache still held the entry would depend on a budget. ADR 0343."

[[clause]]
clause = "7.4.2"
title = "ASCIIHexDecode filter"
status = "partial"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::ascii_hex_decodes_and_stops_at_the_terminator", "crates/pdf-syntax/src/filter.rs::ascii_hex_pads_an_odd_final_digit", "crates/pdf-syntax/src/filter.rs::ascii_hex_skips_a_character_the_clause_calls_an_error"]
note = "\"One byte of binary data for each pair of ASCII hexadecimal digits\", white space ignored, `>` the EOD marker, and an odd final digit treated \"as if a 0 (zero) followed the last digit\" — all four implemented. `partial` for one sentence, deliberately: \"any other characters shall cause an error\", and this decoder skips them instead. That is a recovery rather than an oversight — a hex stream with a stray byte in it decodes to what its producer meant everywhere except at that byte, and refusing loses the whole stream — but it is a departure and belongs in a row rather than only in a comment. Re-read against the code, sentence by sentence: all five hold as this note says, and the departure now has a test of its own rather than prose alone — a choice nothing exercises is one a later round undoes by accident with every gate agreeing. Two things here read like gaps and are not. It is the only filter taking no `Limits`, which is sound because §7.4.2 is a 2:1 contraction and `Parser::parse_stream_data` bounds the raw bytes — that is what makes `max_stream_len` \"raw or decoded\" rather than only the second. And §7.4.3's identical \"shall cause an error\" *is* enforced, in `ascii85`, because base-85 cannot resynchronise after a skipped character where hexadecimal can: skipping one shifts every group after it."

[[clause]]
clause = "7.4.3"
title = "ASCII85Decode filter"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::ascii85_round_trips_a_known_value"]
note = "The base-256 to base-85 relation, `!` to `u` plus `z` for four zero bytes, white space ignored, `~>` as EOD, and a partial final group of *n* characters yielding *n* − 1 bytes. All three of the clause's \"shall never occur\" conditions are refused rather than guessed at: a character outside the range fails the stream, a `z` inside a group fails it because `z` is outside `!`..=`u`, and a final group of one character yields nothing. An optional `<~` introduction is accepted, which the clause does not describe and PostScript does. **The length guard had a hole at `z` until the four-hundred-and-seventy-first session**: the check sat after the group push and the `z` arm reached it by way of a `continue`, so eight `z` under a bound of eight produced thirty-two bytes and reported nothing. Found by asserting that all four stream filters answer a bound with the same variant, which is the whole value of having one. ADR 0306."

[[clause]]
clause = "7.4.4"
title = "LZWDecode and FlateDecode filters"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::lzw_decodes_the_clauses_own_example"]
note = "Both, as of the twenty-seventh session; `FlateDecode` since the first parser and `LZWDecode` written from §7.4.4.2, which states the whole algorithm."

[[clause]]
clause = "7.4.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::lzw_decodes_the_clauses_own_example", "crates/pdf-syntax/tests/stream_length_bound.rs::flate_truncated_keeps_what_it_inflated_and_says_so"]
note = "Descriptive apart from naming the two methods and their references: LZW is Adobe TIFF Revision 6.0's, Flate is RFC 1950 and 1951. Flate is `flate2` with the pure-Rust `zlib-rs` backend, and accepts a raw deflate stream as well as a zlib-framed one because streams missing the two-byte header occur. NOTE 2's worst cases are why both decoders are bounded by `Limits::max_stream_len`: LZW \"provides a compression approaching 1365:1 for long files\", which is a bomb written in nine bits. **That bound was two gibibytes against a four-gibibyte ceiling and is now one** (ADR 0306): `pdf_sandbox`'s `INTERPRETER_ADDRESS_SPACE_LIMIT` is 4 GiB of which `MAX_PIXELS` x 4 is a gibibyte of raster, and decoding costs about twice the decoded length before the bytes are handed over — measured at 3694 MB peak for a 1.9 GB decode — so one stream could command the whole ceiling and leave nothing to draw with. The new figure is bounded from above by that arithmetic and from below by what documents contain: the largest decoded stream in 5 047 187 streams over 65 967 crawled documents is 483.84 MiB. **And reaching the bound is now a refusal rather than a clamp**: `io::Take` yields end-of-file at its limit and `read_to_end` reports that as `Ok`, so a bomb came back as a complete decode of its own prefix. The ceiling handed to `take` is one byte past the bound, which is what tells a bomb from a truncation. **`take` and `read_to_end` are gone in the five-hundred-and-eighth and `flate2::Decompress` drives the inflate directly**, because the `Read` adapter could express neither fact this clause needs: RFC 1951's final block and an input that merely ran out both arrive as `Ok`, so a truncated stream was indistinguishable from a whole one; and on an error the adapter discards whatever the erroring `read` call produced, so the prefix survived only to the last whole call — 1024 bytes of a partial ICC profile on one witness and nothing at all on another, by where the damage fell against a buffer boundary. The zlib-then-raw-deflate fallback and the one-byte-past ceiling are unchanged. ADR 0343. **The ceiling was a statement about a `Vec` and `Vec::reserve` is amortised**, which the five-hundred-and-nineteenth session found by re-measuring rather than by reading: the loop computed the right growth step and handed it to a method documented to take `max(2 x capacity, len + additional)`, so the last step before a gibibyte granted 1804 MiB and a 1.85 MB bomb peaked at 1811 MB. `reserve_exact` takes the step as stated; the doubling below the bound is unchanged and the buffer now stops at the bound, measured at 1031 MB. **The clause's arithmetic did not move and that is the point**: this row's own sentence above derives the gibibyte from a decode costing about twice its output, and the code cost up to three times it, because the buffer's slack is resident and `Arc<[u8]>` is a copy beside it. `shrink_to_fit` before the hand-over makes the code cost what the number was derived from, at -0.145% and -0.116% of instructions on an open and on an interpreted page. ADR 0354."

[[clause]]
clause = "7.4.4.2"
title = "Details of LZW encoding"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::lzw_decodes_the_clauses_own_example", "crates/pdf-syntax/src/filter.rs::early_change_moves_the_width_increase_by_one_code", "crates/pdf-syntax/src/filter.rs::a_code_the_table_does_not_have_stops_rather_than_guessing", "crates/pdf-syntax/tests/stream_length_bound.rs::lzw_corrupt_keeps_what_it_decoded"]
note = "The whole algorithm, and **the clause supplies its own test vector** — EXAMPLE 1's input, EXAMPLE 2's packed bytes — which is the rarest kind of expected value in this tree: one the standard states rather than one derived from a rule. Three details decide correctness and each is a sentence here: codes packed high-order bit first and straddling byte boundaries; the width growing \"following the creation of table entry 511\", moved one code earlier by Table 8's `/EarlyChange`; and a code naming the entry about to be created, which the standard's own example reaches on its third code. A code past the end of the table keeps what was decoded rather than inventing bytes, for the reason `flate` keeps a truncated inflate. **A decode past `Limits::max_stream_len` does not, and the two used to be one code path** (ADR 0306): damage means the encoder had no more to give and the bound means it had a great deal more, so the first salvages and the second answers `FilterRefusal::TooLarge`. NOTE 2's 1365:1 is what makes the difference matter here rather than in the other three filters. **And the salvage says so since the five-hundred-and-eighth**: the clause states EOD as code 257, so a stream that runs out of bits without one has ended early and `Damage::Truncated` is the difference between that and a stream that ended where it meant to. ADR 0343. **The algorithm is a resumable `Lzw` state since the five-hundred-and-ninety-fourth, and the whole-buffer decode is a loop over it** — the shape trap 6 asks of a filter that also feeds `filter::Pump`, so that the two routes are one implementation of the clause rather than two that drift. A single `LZWDecode` on a content stream is now pumped through the reader's window exactly as `FlateDecode` is, which is what road D wanted first of the four non-Flate filters because NOTE 2's 1365:1 makes it the sharpest bomb: a 2.37 MB file naming a 1.5 GiB decode costs **10 MB and reports `MAX_OPERATIONS`** where whole-decoding it cost **1035 MB and refused `TooLarge`** (and ran 2.12 s against 0.11 s). What stays in `lzw` rather than moving into the state is the bound, because a bound is a statement about an allocation and the windowed route has none — there the aggregate bound is the reader's, §7.7.3.3's array bound `content::reader` applies. ADR 0429."

[[clause]]
clause = "7.4.4.3"
title = "LZWDecode and FlateDecode parameters"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::early_change_moves_the_width_increase_by_one_code"]
note = "Table 8's five entries with the defaults it states: `/Predictor` 1, `/Colors` 1, `/BitsPerComponent` 8, `/Columns` 1, and `/EarlyChange` 1. The last is `LZWDecode` only and is the one entry in this table that changes what a *byte* decodes to rather than how bytes are rearranged, because it moves where the code width grows and desynchronises the bit stream from that point on."

[[clause]]
clause = "7.4.4.4"
title = "LZW and Flate predictor functions"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/tests/filters.rs::each_png_row_carries_its_own_filter_type", "crates/pdf-syntax/tests/filters.rs::the_tiff_predictor_is_a_horizontal_difference_with_no_type_byte", "crates/pdf-syntax/tests/filters.rs::a_predictor_the_clause_does_not_define_is_refused"]
note = "Both groups: TIFF Predictor 2, and the PNG group's five filter types with the per-row type byte ISO/IEC 15948 puts in front of each row. Predictors are applied *inside* the decode rather than beside it, which is trap 4 in `doc/HANDOVER.md`: the code once called this \"the caller's responsibility\" and then did not, so every cross-reference stream — which is compressed *and* PNG-predicted almost universally — failed with a misleading `/Root is not a dictionary`. A predictor code the clause does not define returns `None` rather than passing the data through, because unpredicted data read as predicted is silently wrong and for a cross-reference stream that means fabricated byte offsets."

[[clause]]
clause = "7.4.5"
title = "RunLengthDecode filter"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs"]
test = ["crates/pdf-syntax/src/filter.rs::run_length_expands_literal_and_repeated_runs"]
note = "A length byte and its run: 0 to 127 copies the following length + 1 bytes literally, 129 to 255 repeats the next byte 257 − length times, and 128 is EOD. All three arms and the boundary. **\"A length value of 128 shall denote EOD\" is a requirement on the data, so data that ends without one is short**, and since the five-hundred-and-eighth this decoder says which of the two it saw rather than handing back a prefix as though it were the stream. `Damage::Truncated`, the same answer `flate` and `lzw` give to the same question. ADR 0343."

[[clause]]
clause = "7.4.6"
title = "CCITTFaxDecode filter"
status = "partial"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-sandbox/src/decode.rs", "crates/pdf-sandbox/src/protocol.rs"]
test = ["crates/pdf-model/tests/ccitt.rs::black_is_1_gives_the_exact_complement", "crates/pdf-model/tests/ccitt.rs::a_scanned_page_comes_back_mostly_white", "crates/pdf-model/src/image.rs::end_of_block_overrides_the_rows_parameter", "crates/pdf-model/tests/ccitt_bound.rs::end_of_block_false_stops_at_rows_and_blanks_the_rest_out_loud", "crates/pdf-model/tests/ccitt_bound.rs::end_of_block_true_ignores_a_short_rows_and_draws_the_whole_image", "crates/pdf-model/tests/ccitt_bound.rs::end_of_block_false_reaching_the_whole_height_says_nothing", "crates/pdf-model/tests/corpus.rs"]
note = "Group 3 and Group 4 decode through `hayro-ccitt` in the sandboxed worker, twelfth session, and the clause's own content is Table 11 — the coding is ITU-T T.4 and T.6, which this clause says it does not describe. Seven of Table 11's eight entries are honoured with the defaults it states: `/K`'s sign selects among Group 4, Group 3 one-dimensional and Group 3 mixed (\"it shall not distinguish between different positive K values\"), `/Columns`, `/Rows`, `/EndOfLine`, `/EncodedByteAlign`, `/EndOfBlock` and `/BlackIs1`. Partial for `/DamagedRowsBeforeError`, which is error concealment the decoder has none of: a value above zero is refused and reported rather than decoded without it, since a producer that asks for concealment is one expecting damage. Two further readings of the clause that are decisions rather than code: a `/Columns` disagreeing with the image's `/Width` is refused, because the row stride the filter produces and the one §8.9.5.1 makes the unpacker read then differ and nothing here says which wins; and a stream whose data runs out before the row count is legal, with the samples never delivered left blank, which the standard does not specify and `decode.rs::pad_to_height` records as a choice. **`/Rows` was read as the row count and is not one, corrected in the five-hundred-and-fifty-seventh session** (ADR 0392). Table 11 gives `/EndOfBlock` the last word — \"A flag indicating whether the filter shall expect the encoded data to be terminated by an end-of-block pattern, overriding the Rows parameter. If false , the filter shall stop when it has decoded the number of lines indicated by Rows or when its data has been exhausted, whichever occurs first.\" — so the sentence this note used to quote as the rule is the *conditional* half of it, and its precondition had been dropped. `/EndOfBlock` defaults to true, which is what a document never mentioning the entry has, and true overrides `/Rows` outright: the decode is then bounded by §8.9.5.1's `/Height`, which `image.rs::ccitt_rows` now derives. A producer stamping a wrong `/Rows` is real — hayro's issue 1337 has a CAD driver copying `/Columns` onto a 523-line logo — and honouring it truncated the raster, which this tree then refused on the height check rather than drew as a black block, but a refused logo is not a drawn page either. **The one case where the clause does bind the filter to `/Rows` — `/EndOfBlock` false with a `/Rows` below `/Height` — was refused until the five-hundred-and-ninety-ninth session and is now drawn and reported.** The short raster is the clause obeyed rather than damage, so what it needed was for the two numbers to travel separately over `pdf-sandbox`'s pipe, which carried one and now carries both: `CcittParameters::rows` bounds the decode and `CcittParameters::height` is the grid §8.9.5.1 states and `pad_to_height` fills. The lines between them are blank, which the standard states nowhere and `image::ccitt_bound_below_its_height` therefore says out loud — the report's condition is Table 11's three parts and nothing wider, so an `/EndOfBlock` left at its default, a `/Rows` of zero and a `/Rows` reaching `/Height` are all silent. **Unwitnessed by the corpus, and the witness is built rather than found** (trap 8): `tests/ccitt_bound.rs` is a pair of hand-written pages differing in one entry's value, with four T.4-encoded scan lines of black between them, drawing four lines under `/EndOfBlock true` and two lines and two blanks under false."

[[clause]]
clause = "7.4.7"
title = "JBIG2Decode filter"
status = "partial"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-sandbox/src/decode.rs"]
test = ["crates/pdf-model/tests/jbig2.rs"]
note = "Decoded by `hayro-jbig2` in the confined worker (ADR 0014), with Table 12's `/JBIG2Globals` carrying the page-0 segments several images may share. The clause's own content beyond that is a set of *restrictions on the bit stream*, and they are worth listing because they are what a reader may assume: the embedded organisation of ISO/IEC 14492 Annex D.3, page 1 only, no colour palette segments (type 54) and `COLEXTFLAG` 0, and — the one this tree does not enforce — \"the JBIG2Decode filter shall not be used with inline images\". An inline image naming it is decoded rather than refused, which draws what the producer wrote; the strongest evidence the decode is right is not a reference renderer but the corpus's own invariant, one image encoded many ways and producing byte-identical pixels. **That invariant is also an instrument to point at the references, which the five-hundred-and-forty-sixth session did** (ADR 0381): asked the same question over the same family — each renderer compared only with *itself*, so no reference is being treated as truth — this tree returns one image and `poppler`, `mupdf` and `ghostscript` return eight, six and six. `mupdf` and `ghostscript` are `jbig2dec` twice, and the image it produces on the encodings it is consistent about is byte-identical to ours. **And the population had a hole**: `issue20439.pdf` is a member of the family whose name does not begin `bitmap-`, so the one test that can judge it did not run on it while the oracle listed it as contradicted; `tests/jbig2.rs` admits it by name, and the admission is self-checking because a different picture would make the test report two images instead of one. The clause's inline-image restriction is still the unenforced half, which is what keeps this `partial`."

[[clause]]
clause = "7.4.8"
title = "DCTDecode filter"
status = "partial"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/src/image.rs::a_ycck_codestreams_four_channels_stay_four", "crates/pdf-model/tests/dct_components.rs::a_three_component_frame_marked_transform_zero_is_three_components", "crates/pdf-model/tests/corpus.rs", "crates/pdf-model/tests/dct_components.rs::a_dictionary_that_contradicts_the_frames_dimensions_does_not_cost_the_image"]
note = "Baseline and progressive JPEG through `zune-jpeg`, in this process. **A four-component codestream stays four components, in both of the spellings Adobe's APP14 marker gives it**, since the three-hundred-and-fifty-fourth session (ADR 0203): transform 0 is CMYK and transform 2 is YCCK, and `decode_jpeg` asks the decoder for whichever four-component space the codestream states. `zune-jpeg` has no YCCK → CMYK conversion — its two YCCK arms both go to RGB and composite the black channel away — so `ycck_to_cmyk` does JFIF's inverse here and leaves the inversion an Adobe four-component JPEG already assumes, which is what makes a file's own `/Decode [1 0 1 0 1 0 1 0]` undo exactly one of them. §8.9.5.1 is the reason it matters: the dictionary's `/ColorSpace` interprets an image's components, so a decoder that turned four into three would have answered that clause's question on its behalf. **No corpus document is YCCK**; the witness is a 92-page commercial catalogue outside this repository, every page of which drew *nothing* until this, and on whose cover ours is 0.0113 from `poppler` where `mupdf` is 0.0384. **Table 13's `/ColorTransform` is not read**, and the twenty-seventh session sized that silence rather than guessing at it. The clause's rule has three cases: an Adobe APP14 marker wins outright; with no APP14 the dictionary entry governs; with neither, the default is 1 for three components and 0 otherwise. Four corpus documents write the entry and all four write 0. Two are single-component images, where the clause says the entry \"shall be ignored\". One is three-component with a JFIF marker. **One — `issue12841_reduced.pdf` — is three-component with neither marker, which is exactly the case where the clause says the dictionary decides, and obeying it would mean not transforming YCbCr to RGB.** All four reference renderers transform it, the photograph is plainly right when transformed, and the producer evidently did not mean what it wrote. So the entry stays unread and this row records *why*: implementing the sentence would break the only file that exercises it, which is a finding about the corpus and not a licence to ignore the clause. **And the two-hundred-and-thirty-fourth session found the other half of that departure, which this row was silent about for two hundred sessions.** The clause's *third* case — no `APP14`, no dictionary entry — has a stated default of 1 for three components, and `issue11931.pdf` is a three-component codestream with neither marker whose `SOF0` identifiers are `0x52 0x47 0x42`, the ASCII letters R, G and B. `zune-jpeg` reads those and skips the transform, so the samples are taken as RGB; `ghostscript` obeys the clause and paints the band magenta at six and a half times the page's ink, and `poppler`, `mupdf` and `hayro` do what we do. No clause of ISO 32000-2 or of ISO/IEC 10918-1 gives a component identifier any meaning — the convention is `libjpeg`'s — but this clause's own closing `shall` binds a *producer*: \"PDF DCT encoding shall exactly follow all those rules established by Adobe for the PostScript language\", which a codestream with neither marker does not. So the file is outside what the clause describes and the question is `CLAUDE.md`'s second one, robustness, where the codestream's own declaration is the only evidence there is. **Stated as one policy rather than two exceptions: on a `DCTDecode` image this tree lets the codestream decide the colour transform, not the dictionary, in both directions.** `AMBIGUOUS_JPEG_COMPONENT_IDS` in `oracle.rs` holds the page. **A four-component codestream is read as CMYK since the hundred-and-seventy-eighth session, and the sentence this row used to end with — that one \"cannot arise here, since `decode_jpeg` refuses a four-component codestream\" — was wrong twice over.** Nothing refused one: `zune-jpeg` converts to RGB by default, so a CMYK JPEG was silently run through *its* `(1 − C)(1 − K)` on samples it assumes are stored inverted, which is the standalone-JPEG convention and a second colour conversion where `CLAUDE.md` permits one. `cmykjpeg.pdf` drew a black sky for it. The codestream is now asked for as CMYK and the four components are converted by `ColourSpace::to_rgb` like every other colour, with §8.9.5.2's `/Decode` applied first — the array is where a PDF states a sample's polarity, and Table 88's default for `DeviceCMYK` is the identity. **And which codestreams are four was read off the wrong thing until the four-hundred-and-thirtieth session.** `decode_jpeg` decided it from `zune-jpeg`'s `input_colorspace()`, which is that decoder's reading of the APP14 marker before it has seen a frame — transform 0 maps to `CMYK` there, and the library itself defers the correction to a three-component `RGB` until `SOF` has been read. §7.4.8 says where the number comes from: \"The values of these parameters, which include the dimensions of the image and the number of components per sample, are entirely under the control of the encoder and shall be stored in the encoded data.\" Table 13 then states the transform *in terms of* that number — \"If the image has four components, CMYK values shall be transformed to YCbCrK before encoding and from YCbCrK to CMYK after decoding\" — so transform 0, \"No transformation\", says nothing about how many components there are. Asking a three-component codestream for four made it `Unimplemented colorspace mapping from RGB to CMYK` and lost the image whole: **21 images over four documents of a 4000-document SafeDocs sample**, none in the 974. The count is now `info.components`, and the `YCCK → CMYK` conversion is gated on the same test, because a three-component frame marked transform 2 is read as `YCbCr` and running the four-channel conversion over it would walk past every pixel boundary rather than refuse. ADRs 0149, 0266. **And the *dimensions* in that same sentence were read off the wrong thing until the five-hundred-and-fifth session, in the opposite direction.** `decode_jpeg` required the codestream's frame to agree with the dictionary's `/Width` and `/Height` and refused the image outright where it did not — on a comment saying the two \"must agree, because the display list carries the dictionary's and the samples carry the JPEG's\", a premise that expired when a reduced JPEG 2000 decode taught the display list to carry the raster's own grid. This clause states no match requirement at all: the dimensions are \"entirely under the control of the encoder and shall be stored in the encoded data\", and \"DCTDecode may obtain the parameter values it requires directly from the encoded data\". §7.4.9 states one for JPEG 2000 — \"Width and Height shall match the corresponding width and height values in the JPEG 2000 data\" — and that refusal stays, which is what makes this one a reading rather than leniency. A disagreement costs no mark either, because §8.9.5.1 puts every image in the unit square whatever its resolution, the same sentence §8.9.6.2 leans on when it lets a stencil mask and its base image differ outright. The witness is `pdfCabinetOfHorrors/veraPDFHiResChangedHeight.pdf`, whose only defect is one digit of `/Height` — 1227 against the frame's 1226 — and which lost a whole photograph for it where `poppler`, `mupdf` and `ghostscript` all draw it; it now renders pixel-identical to the intact `veraPDFHiRes.pdf` beside it. The codestream's grid carries `MAX_SAMPLES` with it, since it is no longer the dictionary's grid `decode_parts` already bounded. **The disagreement is reported beside the drawing** — `image::contradicted_frame`, the seventh place this tree reports while drawing — and the oracle is what decided that rather than taste: silent, `xobject-image.pdf` entered the judged set for the first time and was contradicted at 99.78, because its dictionary says 200x100 where its codestream holds **one red pixel** and the page is then a flat red rectangle no reader could question. The three references do not agree about that file either — `poppler` and `ghostscript` draw nothing, `mupdf` draws it black — which is what left the clause to decide it. ADR 0340."

[[clause]]
clause = "7.4.9"
title = "JPXDecode filter"
status = "partial"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-sandbox/src/decode.rs"]
test = ["crates/pdf-model/tests/jpeg2000.rs::every_corpus_codestream_decodes_to_the_reference_softwares_samples", "crates/pdf-model/tests/corpus.rs"]
note = "Decoded by `hayro-jpeg2000` in the confined worker (ADR 0014). The clause inverts the usual relationship between an image dictionary and its data, and getting that backwards renders JPEG 2000 images in the wrong colours with nothing to say so: `/ColorSpace` is *optional* and where present overrides the codestream's, `/BitsPerComponent` is ignored either way, and `/Decode` is ignored unless `/ImageMask` is true. All three are implemented that way round. The channel types — ordinary, opacity, premultiplied opacity — reach §11.6.5.2 through `/SMaskInData`. **NOTE 3's resolution progression is used since the four-hundred-and-eighty-sixth session** (ADR 0321): a codestream over the worker's sample budget is re-read asking for half of what the last reading offered until it fits — each step a header parse — and the raster leaves the sandbox on the grid the codec produced, beside the grid the codestream *states*, so the clause's \"Width and Height shall match the corresponding width and height values in the JPEG 2000 data\" stays checked against the data's own statement rather than being relaxed to admit the reduction. `issue19517.pdf`'s 12608x16806 scan — refused for wanting gigabytes since the seventh session — decodes at 3152x4202 and the page draws what `poppler` draws. The capability's other half was the fork's: until `close2/hayro`'s `feat/reduced-resolution-allocates-less` (`1dc833f7`, the revision this workspace pins) a reduced decode still reserved a coefficient for every full-resolution sample, 3.4 GB in one allocation, which the worker's gigabyte of address space turns into a dead process (ADR 0233 measured it). `partial` for two things: the clause's restriction to JPX baseline plus colour spaces 12 and 19 is not checked, and thirteen corpus codestreams still decode one level off the reference software — see below. \"This filter shall only be applied to image XObjects, and not to inline images\" is not enforced, as with §7.4.7. **And the decoding itself had never been checked against anything until the two-hundredth session.** The clause hands the whole of it to ISO/IEC 15444-1, which defines it exactly, so a codec has one right answer per codestream and a rendered page cannot say whether it got it: `tests/jpeg2000.rs` decodes every one of the corpus's 30 `JPXDecode` streams twice, once here and once through `opj_decompress`, and compares the samples exactly. **Fourteen are byte-identical and thirteen are not**, and the discriminator is `qntsty`: the reversible 5/3 path agrees and the irreversible 9/7 one differs by up to 87 levels of 255 over three quarters of an image, with our samples moving toward the image mean two to one. That is `hayro-jpeg2000` 0.4.0's to fix and is written up in `doc/JPEG2000_FEEDBACK.md`; the thirteen are held by name so an upstream release closing it fails the build. ADR 0161. **NOTE 2's *location* progression is measured and deliberately not used** (ADR 0373): `examples/image_region_census` reads A.5.1's SIZ and A.6.1's COD out of every corpus codestream, and over a 6622-document SafeDocs sample's 10 485 codestreams **none states a precinct partition at all**, while of the 125 that are eight megapixels or more — the only size at which decoding a rectangle rather than the image would buy anything — 121 state a single tile. The witness this began with, `issue19517.pdf`'s 12608x16806 scan, is one of those: one tile, no precincts, LRCP. So the format's two cheap forms of random access are absent exactly where they would be needed, and a reader magnifying that page gets the reduced level magnified rather than the region's own samples. What would reopen it is a codestream of 64 megapixels or more stating either partition, which that census is the command to find."

[[clause]]
clause = "7.4.10"
title = "Crypt filter"
status = "implemented"
code = ["crates/pdf-syntax/src/filter.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::an_identity_stream_filter_leaves_streams_alone"]
note = "A pass-through here, and that is the whole of it: the filter \"allows the document-level security handler to determine which algorithms should be used to decrypt the input data\", so the decryption happens where an object's identity is known — `document.rs`, §7.6.2 — and by the time the filter chain runs the bytes are already plaintext. Table 14's `/Name` selects among §7.6.6's crypt filters and defaults to `Identity`; the clause's closing sentence, that a stream specifying a crypt filter is decrypted \"using the key as is\" rather than through Algorithm 1, is implemented in `crypt.rs` (ADR 0031). \"The Crypt filter shall be the first filter in the Filter array entry\" is a requirement on the writer that this reader does not need: a pass-through commutes with everything."

[[clause]]
clause = "7.5"
title = "File structure"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::junk_before_the_header_shifts_every_offset_in_the_file"]
note = "The four elements of §7.5.1 and the two ways the last two of them may be written. `xref.rs` is all of it, and it is the crate's most defensive file for the clause's own reason: this is the structure real files corrupt. Linearized PDF (Annexes F and G) is read as what the clause makes it — an *access strategy*, not a different file — and this reader takes the clause's own advice, \"all PDF files should be read using the trailer and cross-reference table\"."

[[clause]]
clause = "7.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::the_table_decides_which_copy_of_an_object_is_read_rather_than_the_bodys_order"]
note = "Header, body, cross-reference table, trailer — and the sentence that decides how they are read: \"Reading a non-linearized file in a serial manner is not reliable because of the way objects are to be processed after an incremental update.\" So the reader starts at the end and follows `startxref`; scanning for `N G obj` headers exists only for files whose table is unusable, and `XrefTable::recovered_by_scan` records when that happened rather than hiding it."

[[clause]]
clause = "7.5.2"
title = "File header"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::junk_before_the_header_shifts_every_offset_in_the_file"]
note = "\"The PDF file begins with the 5 characters '%PDF -' and byte offsets shall be calculated from the PERCENT SIGN (25h).\" The second half of that sentence was **not implemented before the thirty-ninth session**, and it is not a tolerance: a file with bytes before its header — NOTE 1 permits any number of them — has a *correct* table whose every offset is short by the junk's length, and this reader fell through to scanning the whole file for object headers instead. `read_from_startxref` now adds the header's position to `startxref`, to `/Prev`, to `/XRefStm` and to every entry, which is zero for a file whose header is at zero. One corpus document has junk before its header (`issue6069.pdf`, which says so in its own page text). The version *number* is deliberately not enforced: the clause lists nine acceptable headers and Annex I's compatibility rules mean a reader that refused a tenth would refuse files it can draw."

[[clause]]
clause = "7.5.3"
title = "File body"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::a_body_object_the_table_does_not_list_is_not_reachable"]
note = "\"The body of a PDF file shall consist of a sequence of indirect objects\", and since PDF 1.5 also of object streams (§7.5.7). Nothing here is a rule a reader applies: the body is reached through the cross-reference table, never sequentially, and `scan_for_objects` is the exception that proves it — it reads the body serially only when the table is gone."

[[clause]]
clause = "7.5.4"
title = "Cross-reference table"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::a_subsection_displaced_by_one_is_realigned_by_the_objects_own_headers"]
note = "Classic tables: the `xref` keyword, one or more subsections each introduced by a first object number and a count, and 20-byte `n` and `f` entries. Two of the clause's rules are deliberately not enforced, both because enforcing them loses documents rather than protecting anything. The 20-byte fixed width is read as *tokens* rather than as a fixed stride, because producers write 19- and 21-byte entries and the token reading accepts all three; and a subsection whose count exceeds the entries written stops at the `trailer` keyword rather than reading past it, which `outline_goto_action.pdf` needs. The free list is read only for its `f` marker, and that marker is *recorded* rather than skipped — a free entry says this section's answer for that object number is nothing, which §7.5.6 needs to be able to overrule an older section. The linked list through the next free object's number is a *writer's* structure and nothing a reader resolves consults it, so the first field of an `f` entry is read and discarded. **A table is also written**, since the hundred-and-thirty-sixth session: subsections of consecutive object numbers and 20-byte `n` entries, for a document whose own last section is a table (ADR 0121). **And a third rule is enforced against the file rather than against the table**, since the hundred-and-seventy-seventh: a subsection's header gives an entry's object number by position and §7.3.10's object header gives it again at the offset, so where two or more in-use entries agree that the header is displaced by the same amount, the subsection is renumbered from the objects. `issue7229.pdf` declares `1 7` and then lists object 0's free entry — \"[t]he first entry in the table (object number 0) shall always be free\" — and this reader drew its second page as its first until that was read. Bounded at four header reads per subsection, which is what keeps it off the startup path. ADR 0148."

[[clause]]
clause = "7.5.5"
title = "File trailer"
status = "partial"
code = ["crates/pdf-syntax/src/xref.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::a_subsection_counting_more_entries_than_it_has_still_finds_the_trailer", "crates/pdf-syntax/tests/cross_references.rs::a_trailer_further_back_than_the_window_is_still_the_files_trailer"]
note = "`startxref`, `%%EOF` and the trailer dictionary, read from the end as \"PDF processors should read a PDF file from its end\" asks. **How far \"the end\" reaches was a 2048-byte window until the five-hundred-and-forty-fourth session**, whose comment said a `startxref` further back \"is not a trailer, it is a coincidence\" — disproved by `jhove-errors/PDF-HUL-138/6.2017-0960.pdf`, one complete document with a truncated copy of itself appended, whose correct `startxref` sits eight megabytes from the end and whose 21 pages this reader therefore showed none of. The window is a fast path now and the rest of the file is searched backwards behind it, because the alternative is `rebuild`, and §C.4 licenses that only \"[w]hen a PDF processor reads a PDF file with a damaged or missing cross-reference table\" — which a table that is merely not last is not. 425 of 65 944 crawled documents miss the window and 189 of those are reached by the wider search; surveyed on both trees they go 10 pageless and 10 incomplete to 5 and 8, with nothing moving the other way, and the display-list digest over the 974 is byte-identical. ADR 0379. Table 15: `/Root` is required and its absence is fatal (`SyntaxError::TrailerMissing`) — and since the hundred-and-seventh session it is also a *test of the table*: `/Root` is \"[t]he catalog dictionary for the PDF file\", so a cross-reference table that leads to no catalog has been disproved by the file itself, whatever it parsed as, and `Document::open` rebuilds by scanning and tries again. `xref::read` scans only where the table is absent, unreadable or empty, which left the case a hand edit produces: a complete, self-consistent table whose offsets all point a few bytes wrong. One corpus document, `issue18986.pdf`, is exactly that and now opens and **agrees with the reference consensus**. The rebuild is tried once and kept only where it does better, so a well-formed document pays one dictionary lookup for it, `/Prev` chains the sections, `/Encrypt` is §7.6's, `/ID` is read as §7.6.4.3.2 step (e) needs it and never encrypted, `/Info` is §14.3.3's document information dictionary, deprecated in PDF 2.0 and read all the same — `metadata::Information::read` takes it off this trailer for the properties panel. **This sentence said it was \"unread because §14.3.3 deprecates it\" long after that reader landed**, found by the second sweep's first run as a program (ADR 0324). **`/Size` is a documented departure and it is measured.** The clause says \"[a]ny object in a cross-reference section whose number is greater than this value shall be ignored and defined to be missing by a PDF reader\"; enforcing that takes the corpus gate's \"no page one\" count from **11 to 77**, so 66 documents would lose their page tree to a `/Size` their producer understated. 68 of 974 documents write at least one entry beyond `/Size`. **A trailer is also written**: `/Size`, `/Root`, `/Prev`, §14.4's `/ID`, and `/Encrypt` and `/Info` carried forward because a reader stops at the first section and both are properties of the file (ADR 0121). The rule protects nothing here — a number beyond `/Size` is looked up like any other and fails like any other if the offset is wrong — and it costs 66 documents, so it is not applied. `partial` for exactly that. **Errata Collection 3 edits this clause’s `startxref` sentence in three places** — Issue #101, `/State` `Completed`: \"in the decoded stream\" struck out, \"section\" for \"stream\", and \"or the beginning of the previous cross-reference stream\" added. The third is the one with teeth, since the offset may then name a cross-reference stream rather than the `xref` keyword — which `xref::read_at` has read for hundreds of sessions, so this is a note and not a defect. What *is* a defect is that nothing here could see the erratum until the four-hundred-and-sixteenth session: the sponsored copy states EC3 as annotations and `doc/md/` dropped every annotation in all fourteen documents (ADR 0252, `tools/spec-errata`)."

[[clause]]
clause = "7.5.6"
title = "Incremental updates"
status = "partial"
code = ["crates/pdf-syntax/src/xref.rs", "crates/pdf-syntax/src/write.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::an_incremental_update_that_deletes_an_object_is_not_undone_by_the_older_section", "crates/pdf-syntax/tests/incremental_update.rs::an_update_replaces_an_object_and_keeps_the_file_under_it", "crates/pdf-model/tests/save_round_trip.rs::every_corpus_document_saves_and_three_readers_see_the_edit"]
note = "\"[T]he most recent copy of each object shall be the one accessed from the PDF file\", which is three lines in `XrefTable::fill`: sections are read newest first into one list, a *stable* sort by object number keeps that order among entries for the same number, and the first of each run is the one kept. **It was one line in `XrefTable::add` — an insert that did nothing if the number was already there — until the two-hundred-and-seventy-sixth session**, which found 101 318 searched inserts costing 40% of `Document::open` and stated the rule once for a file rather than once per entry (ADR 0180). Same rule, and it is now the sort that carries it. **The hundred-and-tenth session found that rule applied to two of the three things an update section can say and not to the third.** The clause's own list is \"objects that have been changed, replaced, or deleted\", and a deletion is written as a free entry over an in-use one while \"[d]eleted objects shall be left unchanged in the PDF file\" — so the bytes are still there, and a reader that drops free entries rather than recording them lets the older section's offset win and resurrects what the file deleted. An entry is now `Option<Location>` and a free entry is a `None` that an older section cannot fill in. Three corpus documents write such a deletion — `prefilled_f1040.pdf` deletes three form-field appearance streams, `issue13520.pdf` an image, `issue4800.pdf` an object whose older copy does not parse — and none of the three still references what it deleted, so all three pages are byte-identical either way: the rule is here on the clause's evidence rather than on a picture's. The `/Prev` chain is bounded twice, by a visited set against cycles and by `MAX_XREF_SECTIONS` against a chain long enough to be an attack. **And written, since the hundred-and-thirty-sixth session**: `pdf_syntax::write::incremental_update` appends the new objects, a cross-reference section covering exactly them, and a trailer chaining to the offset the file's own `startxref` names — \"leaving its original contents intact\", which is the clause's own words for why this is the one form of writing `CLAUDE.md` permits. The section is the kind the file already uses. Four documents are refused rather than written wrong: one whose table was rebuilt by scanning, one whose encryption cannot be applied to what is written, one with no `startxref` and one with no `/Root`. **An encrypted document is written since the hundred-and-forty-fourth session**: §7.6.2's ciphers run on the way out through the same code that removed them on the way in, so the six corpus documents covering every revision and method §7.6 states take a string and give it back, and `mutool` and `pdftotext` read every one of them. ADRs 0121, 0129, 0130. **And judged at corpus scale since the four-hundred-and-ninety-ninth session** (ADR 0334): `save_round_trip`'s ignored test writes one free text annotation into every corpus document that opens — and a field value wherever a fillable text field exists — and asserts three exact things of each save: the producer's bytes are byte-for-byte a prefix, this tree reads the edit back, and poppler and mupdf independently report the annotation where it was put and the field holding its value, with every refusal printed by reason and a reference that cannot read the original excluded rather than miscounted as a disagreement. **The update adds objects as well as replacing them** since the hundred-and-forty-fifth session — the clause's own list is \"changed, replaced, or deleted\" — and the number it starts from is the larger of `/Size` and the highest the table holds, because 68 corpus documents understate the first. **Errata Collection 3 adds a rule about the version across updates and this reader does not carry it out** (Issue #399, `/State` `Review` `Completed`, read in the four-hundred-and-seventeenth session and recorded here in the four-hundred-and-eighteenth): an update's catalog `/Version` upgrades the document's version and \"shall not reduce\" it. `version::Document::version` answers the larger of the header and the *newest* catalog's entry, so a file whose first update states 2.0 and whose second states 1.7 is reported as 1.7. Correcting it means resolving a catalog per `/Prev` section — every previous trailer read and every previous `/Root` loaded — which is work on the open path for a number Annex I uses to *warn* rather than to decide anything drawn. So it is a deliberate omission with its cost stated: one warning may name too low a version on a multiply-updated file that lowered its own, and no corpus document does. **And since the four-hundred-and-sixty-ninth session the update replaces an object the *producer* wrote**, which is the clause's second case rather than a new one: a free text annotation whose text a reader retyped is written under its own number with a new `/Contents` and a new appearance, and the bytes it read like before are still in the file beneath the update. Until then everything this writer replaced was either a form field's `/V` or a page's `/Annots`, and the difference was never in the writing but in what `pdf_model::view` was willing to say. ADR 0304."

[[clause]]
clause = "7.5.7"
title = "Object streams"
status = "implemented"
code = ["crates/pdf-syntax/src/document.rs", "crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::an_object_stream_locates_its_objects_at_first_plus_their_own_offset", "crates/pdf-syntax/tests/cross_references.rs::an_object_a_damaged_object_stream_does_not_wholly_carry_is_not_read", "crates/pdf-syntax/tests/cross_references.rs::a_rebuild_enters_the_objects_an_object_stream_names", "crates/pdf-syntax/tests/cross_references.rs::a_scanned_object_outranks_an_object_streams_claim_on_its_number", "crates/pdf-syntax/tests/cross_references.rs::a_rebuild_that_cannot_read_an_object_stream_says_so"]
note = "Object streams: Table 16's `/N` and `/First`, the header of object-number and offset pairs, and each object parsed at `/First` plus its offset — the clause's own instruction, \"[a] PDF processor shall rely on the First entry in the stream dictionary to locate the first object\", rather than on the pairs ending where the first object begins. NOTE 7's 2020 correction is why the objects are parsed by offset rather than by scanning for white space between them. **One sentence is satisfied by construction and is worth naming**: \"strings occurring anywhere in an object stream shall not be separately encrypted\" — `expand_object_stream` parses out of the already-decrypted stream data with a plain parser and never calls `decrypt_object`, so a second decryption is not something that was suppressed, it is something there is no path to. `/Extends` is unread and that is the clause: it groups streams into a collection for a *writer*, while a reader reaches a compressed object through the cross-reference entry, which names its stream directly. A generation number other than zero on a compressed object is a rule on the file; the reader does not check it, because the cross-reference entry is what it looks the object up by. **Two more sentences read in the four-hundred-and-twenty-fourth session, which touched this code for ADR 0260.** \"An object in an object stream shall not consist solely of an object reference\" is a rule on the writer that this reader tolerates rather than enforces: such an object parses as a reference and `resolve` follows it, bounded by `MAX_REFERENCE_DEPTH`, so a file that breaks the rule — including one whose compressed object references itself — costs 64 cache reads and answers §7.3.10's null. And an object stream inside an object stream is refused outright in `expand_object_stream`, which takes only a `Location::Offset`: the clause makes an object stream \"like any stream … an indirect object\", and following a nested one would be a route to unbounded recursion. `/Type /ObjStm` is unchecked for the same reason §7.5.8.2's `/Type /XRef` is — the cross-reference entry already said what this object is.**And NOTE 7 states an extent as well as an order, which had no reader until the five-hundred-and-thirty-first session**: \"processing of each object in an object stream starts at the specified byte offset in the decompressed stream and ends prior to the byte offset of the next object or when the end of stream is encountered\", with \"[t]he byte offsets shall be in increasing order\" above it. So every object but the last has its end stated by the next offset, and the last one's end is the end of the stream — which a decode stopped by damage has not reached. `expand_object_stream` used to parse that last object out of bytes that stop early, and a truncated token still parses: `/Length 12345` cut short is `/Length 123`, so the number named a value the producer never wrote. It is now not read, and `Document::objects_lost_to_damage` says which numbers went and how many more `/N` counts whose header pair the prefix never reached; `viewer-core`'s `notes::losses` is what a person is told, said when it becomes known rather than at open, because nothing expands an object stream until an object inside it is wanted. **No document on this disk loses an object this way** — 0 of 65 944 crawled, and the 6 in two of the 974 are unnamed ones nothing could have read — so it is pinned by a hand-built pair differing in RFC 1951's BFINAL bit. ADR 0366.**And a *rebuild* reads this header too, which it did not until the five-hundred-and-sixtieth session.** §C.4 — informative, so it has no row of its own — licenses a reconstruction \"[w]hen a PDF processor reads a PDF file with a damaged or missing cross-reference table … by scanning all the objects in the file\", and this clause's first sentence is why a scan finds only some of them: an object stream holds indirect objects \"as an alternative to their being stored at the outermost PDF file level\", where the `N G obj` headers are. So every compressed object was lost whenever the table was unusable — 30 of the 261 documents of the crawl that reach a rebuild carry object streams, and 223 661 object numbers resolved to nothing (`pdf-model/examples/rebuild_census`). `Document::recover_compressed_objects` enters them from the `N` pairs above, **the header only**: the objects stay unparsed until something asks for one, which is `CLAUDE.md`'s startup rule and is worth 3× the open of the widest such document. Two sentences of this clause decide the rest. A number the scan already found at the outermost level keeps its entry, because \"[i]f either an object stream or a compressed object is deleted and the object number is freed, that object number shall be reused only for an ordinary (uncompressed) object other than an object stream\" — and every collision on this disk, 23 of them, is named by a stream whose decode stops short, so the reading is not carrying the population. And the scan can always reach the streams themselves, because \"[t]he following objects shall not be stored in an object stream: Stream objects\". **The sentence above about `/Type /ObjStm` going unchecked now holds only where an entry exists**: `scan_for_objects` reads Table 16's `/Type` off each object it parses, since a rebuilt table has no entry to have said what the object is. What a stream refuses is counted rather than swallowed — `Document::compressed_objects_recovered` — so that a rebuild recovering part of a file cannot read like one that recovered all of it. ADR 0395."

[[clause]]
clause = "7.5.8"
title = "Cross-reference streams"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::a_cross_reference_stream_and_a_classic_table_describe_the_same_document", "crates/pdf-syntax/tests/cross_references.rs::a_cross_reference_stream_shorter_than_its_own_index_says_what_it_lost"]
note = "Cross-reference streams, which most modern files use for everything. Read by the same function that reads a classic table, chosen by whether the `startxref` offset begins with the `xref` keyword — which is the only thing that distinguishes them, since a cross-reference stream is an ordinary indirect object.**And the stream states its own extent, which had no reader until the five-hundred-and-thirty-first session**: Table 17 makes `/W`'s \"sum of the items … the total length of each entry; it can be used with the Index array to determine the starting position of each subsection\", so the rows `/Index` states times the row width is a length §7.3.8.2 then requires the data to agree with. A stream carrying fewer rows than that is short whether a filter failed or a producer miscounted — the *arithmetic* is the predicate rather than the damage flag, which is ADR 0356's lesson one clause over — and the rows it does carry are whole, since each field's width comes from `/W` and each row's object number from `/Index` and its position. What the shortfall is worth saying is the asymmetry: everywhere else here a number with no entry has been **deleted** (§7.5.6, ADR 0100), and here the file meant to say something about it and the bytes are gone. `XrefTable::entries_lost` counts them and `viewer-core`'s notes say so when the document opens. **The condition has no members on this disk** — 0 over 65 944 crawled documents, the 974 and `format-corpus`'s 267, and the crawl's ten damaged cross-reference streams lose not one *stated* row, because a truncated `FlateDecode` loses RFC 1951's final block and not the data — so it is pinned by a hand-built pair. ADR 0366."

[[clause]]
clause = "7.5.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::a_cross_reference_streams_own_dictionary_is_the_trailer"]
note = "\"Each cross-reference stream contains the information equivalent to the cross-reference table … and trailer … for one cross-reference section\" — so the stream's own dictionary *is* the trailer, and `Section` carries one field for both cases rather than two. The value after `startxref` is the stream object's offset rather than an `xref` keyword's, which is what `read_section` tries second."

[[clause]]
clause = "7.5.8.2"
title = "Cross-reference stream dictionary"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::an_absent_index_covers_object_zero_to_size", "crates/pdf-syntax/tests/cross_references.rs::a_zero_first_field_width_defaults_every_entry_to_type_one"]
note = "Table 17: `/W`, `/Index` with its `[0 Size]` default, `/Size`, `/Prev`, and `/Type /XRef` unchecked because the offset already said what this object is. The clause's restriction that all of these \"shall be direct objects; indirect references shall not be permitted\" is what makes reading them with `Dictionary::get` rather than `Document::get_key` correct rather than lazy — the document is not open yet, so there is nothing to resolve an indirect value against, and the clause says there cannot be one. The same paragraph forbids a `/Crypt` filter here and requires the stream to be unencrypted, which is why the xref reader runs before `Document::authenticate`. **A stream is also written** for a document whose own last section is one: uncompressed, `/W [1 4 2]`, `/Index` in runs of consecutive numbers, and the stream's own entry in it — since the hundred-and-thirty-sixth session (ADR 0121). **Errata Collection 3 widens the directness rule this reader is built on, and the widening is what `decode_direct` already does** (Issue #246, `/State` `Review` `Completed`): the 2020 sentence made it conditional on encoding — \"If the stream is encoded, the Filter and DecodeParms entries in\" Table 5 \"shall also be direct objects\" — and the amended one is unconditional over the whole of that table: \"The values of all entries shown in\" it \"shall also be direct objects. For arrays, all array elements shall be direct objects and for dictionaries, all key values shall be direct objects as well. The F entry defined in Table 5 shall not be used.\" `decode_direct` reads `/Filter` and `/DecodeParms` straight from the dictionary whether or not anything is encoded and refuses an indirect `/Filter` outright, and the new prohibition on `/F` is met by construction, since nothing in this program opens §7.3.8.1's external file (principle 3). Issue #720 is the same change from the other side, striking \"not listed in\" Table 17 from the bullet that says which entries may be indirect, because the exception set is now two tables rather than one."

[[clause]]
clause = "7.5.8.3"
title = "Cross-reference stream data"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::an_entry_type_the_standard_does_not_define_is_the_null_object"]
note = "Table 18's three entry types, big-endian fields of the widths `/W` gives, with a zero width meaning the field is absent and takes its default — including the clause's special case that a zero first field defaults the *type* to 1. Type 0 is a free entry and names no object, type 1 is an offset, type 2 is an object stream and an index. \"Any other value shall be interpreted as a reference to the null object\" — **and the hundred-and-tenth session corrected what that had been taken to mean**: it used to be no entry at all, on the argument that a number with no entry is null by §7.3.10, which is true of a *file* with no such entry and false of a section that has one. An entry the reader has no meaning for is still a statement the section makes, so it is recorded as a null and an older section cannot answer instead. Same rule, same line of code, as §7.5.6's deletion."

[[clause]]
clause = "7.5.8.4"
title = "Compatibility with applications that do not support compressed reference streams"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-syntax/tests/cross_references.rs::a_hybrid_reference_file_finds_through_xrefstm_what_a_previous_section_marks_free"]
note = "Table 19's `/XRefStm`, and the clause states the order rather than leaving it to be inferred: \"if an entry is not found in any given standard cross-reference section, the search shall proceed to a cross-reference stream specified by the XRefStm entry before looking in the previous cross-reference section\". `read_from_startxref` reads the hybrid stream immediately after its own section and before following `/Prev`, appending to one list, and `XrefTable::fill` keeps the first entry for each number — so the precedence is the clause's, produced by the order the sections are appended in rather than by a comparison. The clause's second sentence, \"A PDF reader shall look in the cross-reference stream first, find the object there, and shall ignore the free entry in the previous section\", is the one place §7.5.6's rule about a free entry must *not* apply, and the two compose for free: the free entry is in a section the ordering reaches later, so it arrives at a number already answered."

[[clause]]
clause = "7.6"
title = "Encryption"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs"]
note = "Aggregate of the thirty-three rows below, read as a family in the twenty-second session with encryption as the demand item (ADR 0031). The standard security handler is implemented at every revision §7.6.4.2 Table 21 specifies — 2, 3, 4 and 6 — over /V 1, 2, 4 and 5, with Table 25's V2, AESV2 and AESV3 methods and Table 26's Identity; 19 of the corpus's 26 encrypted documents open with the default user password and the other 7 with theirs. What is left is named rather than silent: §7.6.5's public-key handlers, /R 5, which Table 21 says shall not be used and states no algorithm for, and a revision 4 password containing a character `PDFDocEncoding` has no code for. **That last reason used to read \"whose PDFDocEncoding bytes are Annex D data this crate does not hold\", and it had expired**: `pdf_syntax::text_string` has held the whole of Annex D Table D.3 in both directions since long before this row was last touched, and `crypt.rs`'s own module comment says so and dates it. What is left is the encoding's limit rather than this crate's — a character with no code has no bytes to hash — and it is narrower than the sentence it replaces by every character in Table D.3 outside ASCII."

[[clause]]
clause = "7.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/real_documents.rs::every_specification_pdf_opens", "crates/pdf-syntax/tests/encryption.rs::each_revision_and_method_decrypts_to_a_readable_content_stream"]
note = "One requirement, and it is the one that decides whether any of the rest runs: \"The absence of this entry from the trailer dictionary means that a PDF processor shall consider the document to be not encrypted.\" `Document::authenticate` returns early on a trailer with no /Encrypt and does nothing else. A trailer whose /Encrypt is not a dictionary is refused rather than treated as absent, because a file that says it is encrypted and will not say how is not a file we can draw."

[[clause]]
clause = "7.6.2"
title = "Application of encryption"
status = "implemented"
code = ["crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::the_file_identifier_is_not_decrypted", "crates/pdf-syntax/tests/encryption.rs::strings_in_the_encryption_dictionary_are_not_decrypted", "crates/pdf-syntax/tests/incremental_update.rs::a_string_written_into_an_encrypted_document_comes_back_out_of_it"]
note = "All four exceptions. The trailer's /ID is read before the handler exists and never walked; the encryption dictionary is exempted by object number, recorded before authentication; objects inside an object stream are parsed out of already-decrypted bytes and not decrypted again, which is the third exception by construction; and a signature dictionary's /Contents is skipped by `decrypt_dict`, §12.8.1 Table 255 requiring that value to be the hexadecimal string the exception names. Table 20 adds two more, both here: a cross-reference stream and a stream naming its own /Crypt filter. The sentence about a stream that \"refers to an external file\" has no site — §7.3.8's external streams are not read at all — rather than being unimplemented here. **All of it runs in both directions since the hundred-and-forty-fourth session.** §7.5.6's writer hands each new object to `Document::encrypt_for_update`, which is `decrypt_object`'s mirror and shares its three decisions — the exempt object number, the exempt signature `/Contents`, and the method a stream's own dictionary selects — rather than stating them a second time. The two exceptions that are not shared are not remembered either: §7.5.8.2's cross-reference stream and Table 15's `/ID` are built by the writer itself and never pass through the cipher. ADR 0129."

[[clause]]
clause = "7.6.3"
title = "General encryption algorithm"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::each_revision_and_method_decrypts_to_a_readable_content_stream", "crates/pdf-syntax/tests/incremental_update.rs::a_string_written_into_an_encrypted_document_comes_back_out_of_it"]
note = "Aggregate of the three below, all three `implemented`, and **this row was `partial` while its own note said everything was done** — corrected in the two-hundred-and-ninety-eighth. Both algorithms are implemented in both directions: decrypting is what a reader needs, and encrypting is what §7.5.6's incremental update needs on the way out, which this tree has written since the hundred-and-thirty-sixth session and encrypted since the hundred-and-forty-fourth. §7.6.3.1 makes the first direction nearly free for RC4 — \"the same algorithm shall be used for both encryption and decryption\" — and the AES half differs only in adding RFC 8018's pad rather than removing it and generating the initialisation vector rather than reading it. **That quotation is of a retired sentence and is kept because `doc/md/` is what the gate checks against**: Errata Collection 3 rewrites the whole of it (Issue #542, `/State` `Review` `Accepted`, found in the four-hundred-and-eighteenth session), moving the block size into a sentence of its own and adding the whole-extra-block pad the 2020 wording left to an EXAMPLE. RC4's symmetry is unaffected — it is a stream cipher and the amended clause says so in the same place — and `crypt.rs`'s `AES_BLOCK` carries the erratum's own words. ADR 0129."

[[clause]]
clause = "7.6.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::an_initialisation_vector_with_no_ciphertext_is_an_empty_stream", "crates/pdf-syntax/tests/incremental_update.rs::a_string_written_into_an_encrypted_document_comes_back_out_of_it"]
note = "Chooses the algorithm the way the clause does — \"When the value of the V key of the encryption dictionary is 1, 2, 3 or 4 … Algorithm 1 … When the value of the V key … is 5 … Algorithm 1.A\" — and implements RFC 8018's padding, MD5 for key generation at versions 1 to 4, and AES's fixed 16-byte block. The padding sentence is load-bearing in an unobvious direction: \"the pad is present when M is evenly divisible by 16\", so a stream of exactly 16 bytes is an initialisation vector with no message, which three corpus documents write for an empty page. **One sentence in this clause binds a writer and nobody else, so it had no site until this tree wrote one**: \"the initialization vector is a 16-byte random number that is stored as the first 16 bytes of the encrypted stream or string\". It is met by `getrandom`, the platform's own cryptographic source, rather than by anything derived from the file: a vector a reader of the file can predict is a CBC mode without one. The cost, and it is the reason the writer's tests read a file back rather than compare it: an update of an encrypted document is no longer byte-identical from one save to the next. ADR 0129."

[[clause]]
clause = "7.6.3.2"
title = "Algorithm 1: Encryption of data using the RC4 or AES algorithms"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/src/crypt.rs::an_object_key_appends_the_bytes_the_clause_names", "crates/pdf-syntax/src/crypt.rs::an_aes_object_key_is_salted_and_the_rc4_one_is_not", "crates/pdf-syntax/src/crypt.rs::a_key_length_outside_table_20s_range_is_refused"]
note = "Steps (a) to (d), including the clause's own worked example for object 258 generation 7, the sAlT extension for AES and only for AES, and the cap at 16 bytes. Step (a)'s \"If the string is a direct object, use the identifier of the indirect object containing it\" is why decryption walks a whole object from its identifier rather than acting on strings where they are found. **Step (b)'s `n` is a number the document supplies and the cap in step (d) is what stops it being an index into nothing**, checked in the five-hundred-and-fifty-seventh session because hayro's issue 1273 is that index: `/Length 256` — an ordinary thing to write when AES-256 is meant — divided by 8 is twice an MD5 digest, and their Algorithm 2 sliced sixteen bytes at thirty-two. `key_length` refuses anything outside Table 20's \"a multiple of 8, in the range 40 to 128\" before the value reaches a slice, and the test states the property rather than a list: whatever the file says, the answer is an error or a length an MD5 output can supply. One value is not refused and the test says so — a *negative* `/Length` fails `usize::try_from`, which makes it unreadable rather than out of range, and an unreadable entry takes Table 20's default exactly as an absent one does. Safe, since the default is in range; it costs a worse diagnosis, the document being refused later for an apparently wrong password."

[[clause]]
clause = "7.6.3.3"
title = "Algorithm 1.A: Encryption of data using the AES algorithms"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::each_revision_and_method_decrypts_to_a_readable_content_stream"]
note = "One step, and its content is the difference §7.6.3.1 names: the 32-byte file encryption key is used directly, with no per-object derivation. Also implemented here is the clause's closing paragraph, which decides *where* decryption sits: \"Stream data shall be encrypted after applying all stream encoding filters and shall be decrypted before applying any stream decoding filters\", so a stream is decrypted as its object is loaded and the filter chain above it needs no change."

[[clause]]
clause = "7.6.4"
title = "Standard security handler"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs"]
note = "Aggregate of the **twenty** rows below — four subclauses, and sixteen algorithms and framings under two of them. **This said twelve until the four-hundred-and-thirty-seventh session**, which is a count no reading of the family gives, found by the tenth sweep's arithmetic over a parent's prose. Revisions 2, 3, 4 and 6 authenticate and derive keys; revision 5 is refused by name."

[[clause]]
clause = "7.6.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs", "crates/pdf-syntax/src/document.rs", "crates/viewer-core/src/viewer.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "The reader's obligations are implemented: try the default user password first, accept either password, and preprocess a revision 6 password with SASLprep before converting to UTF-8 and truncating to 127 bytes. **And then prompt**, since the hundred-and-thirty-second session: `viewer-core` answers a locked file with `Event::PasswordRequired` rather than a failure — a document that wants a password is not one this program cannot read — and `viewer-ui` asks at the terminal and opens again with what it is told. Eight corpus documents reach it. **And the sentence that follows the permissions list is honoured for every operation this program has, since the three-hundred-and-seventy-third session** — \"PDF readers shall respect the intent of the document creator by restricting user access to an encrypted PDF file according to the permissions contained in the file\" — which is an obligation on copying, printing and editing rather than on drawing, and which this row recorded for a hundred and fifty sessions as owed to \"an application that has none of those operations yet\". It has two: filling in a form field and adding an annotation, both refused by name where Table 22 withholds them (`pdf_model::restriction`), with the reader able to turn that off, which `CLAUDE.md` requires. `partial` for the two operations that do not exist here — printing (bits 3 and 12) and assembling (bit 11) — and for **bit 5's copying**, which is the one this program could do and does not gate: what crosses the boundary is a readback the same query answers for a *drag*, and Table 22 carves the bit itself for assistive technology, so it needs a host that says \"this is a copy\" (`doc/todo/38`). The clause's own preceding sentence says why any of this is a requirement about the application rather than about `pdf-syntax`: \"There is nothing inherent in PDF encryption that enforces the document permissions specified in the encryption dictionary.\" ADR 0212."

[[clause]]
clause = "7.6.4.2"
title = "Standard encryption dictionary"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs", "crates/pdf-model/src/restriction.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::permissions_come_from_the_p_entry", "crates/pdf-model/tests/restrictions.rs::an_encrypted_document_can_grant_one_operation_and_withhold_the_other"]
note = "Table 21 and Table 22. /R, /O, /U, /OE, /UE, /P, /Perms and /EncryptMetadata are all read, the last only at /V 4 and 5 as the table says. Table 22's flags are read as an unsigned 32-bit quantity numbered from bit 1, and bit 10 is ignored because the table says to. /R 5 is refused here rather than guessed at: Table 21's entry for it is \"Shall not be used. This value was used by a deprecated proprietary Adobe extension\", so the standard states no algorithm and one corpus document, `issue21579.pdf`, writes it anyway. **And since the three-hundred-and-seventy-third session the flags are acted on rather than only carried**, which is §7.6.4.1's `shall` on a reader — \"PDF readers shall respect the intent of the document creator by restricting user access to an encrypted PDF file according to the permissions contained in the file\" — read against the two operations this program has: bit 6 for adding an annotation, bit 6 or bit 9 for filling in a field. **Bit 9 only from revision 3**, which the row above this one had no way to say: Table 22 makes the meaning of a position depend on /R, positions 13 to 32 \"must be 1\", and a reader consulting bit 9 of a revision-2 word would read a *reserved* bit as a grant — the clause's own example, /P -44, has it set while \"disallow[ing] modifying the contents and annotations\". `bug900822.pdf` is the corpus's revision-2 witness. `Permissions` therefore carries /R now. Measured over the corpus: 26 documents carry an /Encrypt, 19 open, 4 of those as the owner — whom §7.6.4.1 exempts — and 6 withhold one of the two operations. **What a reader does about them is `CLAUDE.md`'s policy and not this clause's**: obeying is the default and it can be turned off (ADR 0212)."

[[clause]]
clause = "7.6.4.3"
title = "File encryption key algorithm"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::each_revision_and_method_decrypts_to_a_readable_content_stream"]
note = "Aggregate of the four below; the clause itself only says which algorithm each revision uses."

[[clause]]
clause = "7.6.4.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::each_revision_and_method_decrypts_to_a_readable_content_stream", "crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "One sentence, and it is the branch: Algorithm 2 for revision 4 and earlier, Algorithm 2.A for revision 6."

[[clause]]
clause = "7.6.4.3.2"
title = "Algorithm 2: Computing a file encryption key in order to encrypt a document (revision 4 and earlier)"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/src/crypt.rs::a_short_password_is_extended_from_the_start_of_the_padding_string", "crates/pdf-syntax/src/crypt.rs::an_empty_password_is_the_padding_string"]
note = "Steps (b) to (i) in full, including step (f)'s four 0xFF bytes when /EncryptMetadata is false at revision 4 or greater, and step (h)'s fifty rehashes of the first n bytes. Step (a) is complete since the hundred-and-fifty-second session, and how it got there is worth the sentence. It wants the password \"converted to PDFDocEncoding\"; **this row said for a hundred and twenty-nine sessions that \"this crate holds no Annex D table\"**, so anything outside the ranges where PDFDocEncoding and Unicode provably agree was refused. The crate held the whole of Table D.3 the entire time, in `text_string.rs`, put there in the ninety-second session for §7.9.2.2 — a code-to-Unicode table, which is exactly what this conversion inverts. One function is now shared. What is still refused is refused by the *encoding*: U+00A0 has no code at all, because 0xA0 is the euro sign, and there are no bytes to hash. No corpus document needs either. Step (a)'s padding rule is also the session's one real defect: appending \"the first 32 − n bytes of the padding string\" is not the same as overlaying the password on the padding string, and the two differ for every password except the empty one, which is the one every reader tries first. **Re-read off the blame list in the five-hundred-and-seventeenth**: `crypt.rs` still shares one conversion with `text_string.rs`, still overlays the password on the padding string rather than appending to it, and still refuses only the characters the encoding gives no code — the row holds sentence by sentence."

[[clause]]
clause = "7.6.4.3.3"
title = "Algorithm 2.A: Retrieving the file encryption key from an encrypted document in order to decrypt it (revision 6 and later)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "Steps (a) to (f), reading the 48-byte /O and /U as the hash, validation salt and key salt the clause describes. Step (f) is implemented as §7.6.4.4.12's Algorithm 13 and its result outranks the plaintext /P, because the clause says the decrypted bytes *are* the user permissions."

[[clause]]
clause = "7.6.4.3.4"
title = "Algorithm 2.B: Computing a hash (revision 6 and later)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "Steps (a) to (f). The remainder modulo 3 is taken as the sum of E's first sixteen bytes, which is exact because 256 is congruent to 1 modulo 3 and avoids owning 128-bit arithmetic for one comparison. The loop carries a bound of 256 rounds against NOTE 3's \"between 65 and 80\", because its exit test reads a byte of ciphertext the document supplies; reaching it yields a key that does not authenticate, which surfaces as a wrong password rather than as a hang."

[[clause]]
clause = "7.6.4.4"
title = "Password algorithms"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "Aggregate of the twelve rows below: Algorithms 3 to 13, and §7.6.4.4.1's framing. Every algorithm a reader runs is implemented. **Six** of the eleven compute what a writer stores — Algorithms 3, 4, 5, 8, 9 and 10 — and only *three* of those are `writer-side`, because a reader's authentication re-runs the others: Algorithm 6 runs 4 or 5 whole, and Algorithm 7 begins with Algorithm 3's steps (a) to (d), which is why §7.6.4.4.2 is `partial` rather than `writer-side`. **The sentence here said \"the eleven below … the five that compute what a writer stores are writer-side, and two of those have their first four steps implemented\" until the four-hundred-and-thirty-seventh session**, and none of the three numbers survived the rows below it — the tenth sweep's subject, in a row that states three counts of its own family in one sentence."

[[clause]]
clause = "7.6.4.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/src/crypt.rs::a_long_password_is_truncated"]
note = "Descriptive. Its one constraint that reaches code is that revision 4 passwords \"are limited to characters in the PDFDocEncoding character set\", which is §7.6.4.3.2 step (a) and is recorded there."

[[clause]]
clause = "7.6.4.4.2"
title = "Algorithm 3: Computing the encryption dictionary's O -entry value (revision 4 and earlier)"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "Steps (a) to (d) are implemented, because §7.6.4.4.6's Algorithm 7 begins by running them; steps (e) to (h) compute the /O entry a writer stores and are writer-side."

[[clause]]
clause = "7.6.4.4.3"
title = "Algorithm 4: Computing the encryption dictionary's U -entry value (Security handlers of revision 2)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::each_revision_and_method_decrypts_to_a_readable_content_stream"]
note = "Steps (a) and (b), which §7.6.4.4.5's Algorithm 6 runs to authenticate a revision 2 user password. Step (c) stores the result and is writer-side."

[[clause]]
clause = "7.6.4.4.4"
title = "Algorithm 5: Computing the encryption dictionary's U (user password) value (Security handlers of revision 3 or 4)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "Steps (a) to (e), which Algorithm 6 runs at revisions 3 and 4. Step (f) appends \"16 bytes of arbitrary padding\", which is why Algorithm 6 compares only the first sixteen; the corpus's files write zeros there and nothing may depend on that."

[[clause]]
clause = "7.6.4.4.5"
title = "Algorithm 6: Authenticating the user password (Security handlers of revision 4 and earlier)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "Both steps, with the revision-dependent comparison length the clause states in parentheses."

[[clause]]
clause = "7.6.4.4.6"
title = "Algorithm 7: Authenticating the owner password (Security handlers of revision 4 and earlier)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "All three steps, including the twenty decryptions under keys XORed with a counter running from 19 down to 0 — the reverse of Algorithm 3's 1 to 19. No corpus document is known to be opened by its owner password at revision 4 or earlier, so this path is exercised only by files where both passwords are the same; the reverse counter order is stated by the clause and is not otherwise pinned."

[[clause]]
clause = "7.6.4.4.7"
title = "Algorithm 8: Computing the encryption d ictionary's U (user password) and UE (user encryption) values (Security handlers of revision 6)"
status = "writer-side"
note = "Computes the /U and /UE entries a writer stores. Their *format* is read by §7.6.4.3.3, whose row records that."

[[clause]]
clause = "7.6.4.4.8"
title = "Algorithm 9: Computing the encryption dictionary's O (owner password) and OE (owner encryption) values (Security handlers of revision 6)"
status = "writer-side"
note = "Computes the /O and /OE entries a writer stores; read by §7.6.4.3.3."

[[clause]]
clause = "7.6.4.4.9"
title = "Algorithm 10: Computing the encryption dictionary's Perms (p ermissions) value (Security handlers of revision 6)"
status = "writer-side"
note = "Computes the /Perms block a writer stores. Its layout — permissions in bytes 0 to 7 low-order first, /EncryptMetadata in byte 8, the \"adb\" marker in bytes 9 to 11 — is read by §7.6.4.4.12."

[[clause]]
clause = "7.6.4.4.10"
title = "Algorithm 11: Authenticating the user password (Security handlers of revision 6)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::a_document_with_a_password_opens_with_it_and_not_without"]
note = "One step, and it is §7.6.4.3.3 step (e)'s validation half."

[[clause]]
clause = "7.6.4.4.11"
title = "Algorithm 12: Authenticating the owner password (Security handlers of revision 6)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::an_empty_password_may_be_the_owner_password"]
note = "One step, and it is §7.6.4.3.3 step (c). `pr6531_2.pdf` is opened by it: the empty password is that file's owner password, which is what `Permissions::owner` reports."

[[clause]]
clause = "7.6.4.4.12"
title = "Algorithm 13: Validating the permissions (Security handlers of revision 6)"
status = "implemented"
code = ["crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/src/crypt.rs::a_perms_block_yields_the_permissions_algorithm_10_stored"]
note = "Decrypts the 16-byte /Perms block with AES-256 over a single block, checks the \"adb\" marker, and takes the permissions and the metadata flag from it. The clause says they \"should match\" /P and §7.6.4.3.3 step (f) says they \"shall\"; where they disagree this tree follows the encrypted copy, because step (f) says the decrypted bytes *are* the user permissions and the encrypted copy is the one a file cannot be edited to change."

[[clause]]
clause = "7.6.5"
title = "Public-key security handlers"
status = "reported"
code = ["crates/pdf-syntax/src/crypt.rs", "crates/pdf-model/src/x509.rs"]
note = "Not implemented, and named at runtime: a /Filter that is not /Standard produces `SyntaxError::UnsupportedEncryption` quoting the name. What it would take is not a cipher but a public-key infrastructure — CMS enveloped data (RFC 5652) and X.509 certificates, plus access to the user's private keys. **One of those three arrived in the three-hundred-and-ninety-second session for §12.8.3's sake and is worth recording here honestly, because it is less than it sounds** (ADR 0229): `pdf_model::x509` reads a certificate's `subjectPublicKeyInfo` and the two ways RFC 5652 names one, and `pdf_model::der` and `pdf_model::cms` read `SignedData`. What §7.6.5 needs on top of that is `EnvelopedData` rather than `SignedData` — a different structure with recipient information in it — an RSA *decryption* rather than a verification, and a way to reach the reader's own private key, which is where the threat model starts and where nothing has been decided. So the parsing side is perhaps half built and the rest is untouched; no corpus document uses one. **Read and kept in the five-hundred-and-forty-fifth**: `SyntaxError::UnsupportedEncryption` is still the whole answer and nothing in this tree names `EnvelopedData`, so the half that is built is still the `SignedData` half §12.8.3 needed."

[[clause]]
clause = "7.6.5.1"
title = "General"
status = "reported"
code = ["crates/pdf-syntax/src/crypt.rs"]
note = "As §7.6.5."

[[clause]]
clause = "7.6.5.2"
title = "Public-key encryption dictionary"
status = "reported"
code = ["crates/pdf-syntax/src/crypt.rs"]
note = "Table 23 and Table 24 are not read; the handler is refused before them. **The clause is split in Errata Collection 3 and this family's numbers move with it**, found in the five-hundred-and-sixty-second session by `spec-errata emit`: Issue #196 (`Review`/`Completed`) inserts \"7.6.5.3 Public-key security permissions\" immediately below the NOTE under Table 23, with an editor's note saying \"current text and Table 24 remain unchanged\" — so §7.6.5.3's public-key encryption algorithms take the number one above and the row below this one holds the 2020 number. Nothing new is required of a reader that refuses the whole handler, which is why this is recorded rather than acted on; the numbers stay as the published standard prints them because the citation gate resolves against `doc/md/`. §14.7.5's Issue #452 and §14.8.4.7's Issue #133 are the same shape."

[[clause]]
clause = "7.6.5.3"
title = "Public-key encryption algorithms"
status = "reported"
code = ["crates/pdf-syntax/src/crypt.rs"]
note = "The SHA-1 and AES seed algorithms are not implemented; the handler is refused before them."

[[clause]]
clause = "7.6.6"
title = "Crypt filters"
status = "partial"
code = ["crates/pdf-syntax/src/crypt.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::an_identity_stream_filter_leaves_streams_alone", "crates/pdf-syntax/tests/encryption.rs::a_document_whose_attachment_alone_is_encrypted_still_opens", "crates/pdf-syntax/tests/encryption.rs::an_embedded_file_stream_with_no_crypt_specifier_takes_the_eff_filter"]
note = "Table 25's V2, AESV2 and AESV3 and Table 26's Identity are implemented, /CF, /StmF, /StrF and — as of the four-hundred-and-forty-second session — Table 20's /EFF are read with Identity as the stated default, and a stream's own /Crypt filter overrides all of them through its /DecodeParms /Name. /CFM /None is refused by name, because it \"allows the security handler to do its own decryption\" and there is no such handler here. The clause's binding of a failed authorization to the *data* rather than to the file is implemented and is the reason `encrypted-attachment.pdf` and `auth-event-ef-open.pdf` display: both write /StmF /Identity /StrF /Identity with a StdCF the embedded file stream reaches through its own /Crypt specifier, and neither authenticates against any known password. **/EFF was read by nothing until this session, on a reason that had expired**: the row said \"the /EFF path itself is unexercised — no embedded file's bytes are ever read\", and a host has extracted an attachment's bytes since §7.11.4's panel and `Command::Extract`. An embedded file stream with no /Crypt specifier therefore took /StmF, so a document stating /Identity there and a cipher in /EFF handed its attachment back as ciphertext without a word. Table 20 states both halves and `stream_method` now follows them — the crypt filter \"that shall be used when encrypting embedded file streams that do not have their own crypt filter specifier\", and /StmF where the entry is absent — and §7.6.6's rule that \"related files ( RF ) shall use the same crypt filter as the embedded file ( EF )\" holds by construction, both being /Type /EmbeddedFile streams. Still `partial` for /AuthEvent, which is read only far enough to know it does not gate the body."

[[clause]]
clause = "7.6.7"
title = "Unencrypted wrapper document"
status = "writer-side"
note = "Every normative sentence here addresses a producer: the wrapper \"shall include a Collection dictionary\", \"shall include the file specification dictionary … in the EmbeddedFiles name tree\", \"shall include the AFRelationship key\". The one sentence about readers is descriptive and describes what this viewer already does — \"PDF processors without the custom security handler will present the unencrypted wrapper document with helpful instructions to the user\" — because a wrapper is an ordinary unencrypted PDF and draws as one. Presenting the payload instead would need §12.3.5 collections and a handler for whatever /EP names, and is that clause's work rather than this one's."

[[clause]]
clause = "7.7"
title = "Document structure"
status = "partial"
code = ["crates/pdf-model/src/page.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs", "crates/pdf-syntax/tests/real_documents.rs"]
note = "The object hierarchy from the trailer's `/Root` down to a page, reviewed as a family in the twenty-ninth session. What this reader needs of it — the catalog, the page tree and a page object's geometry, resources and content — is complete. **The sentence that used to follow said \"what it does not read is everything the catalog holds for a *viewer* rather than a renderer\", and §7.7.2's own row is the evidence against it**: eighteen of the twenty-five entries that sentence covered are read, most of them by the sessions that built their clause — the outline, page labels, destinations, viewer preferences, requirements, the collection, the structure tree, optional content, the form. This is the fifth failure shape at the top of the family: a parent row is not maintained by the sessions that implement its members, because the clauses do not cite each other. `partial` for what the subclauses still name, each of which says its own reason."

[[clause]]
clause = "7.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-syntax/tests/robustness.rs::a_subsection_counting_more_entries_than_it_has_still_finds_the_trailer"]
note = "Descriptive. The object hierarchy is entered at the trailer's `/Root`, which `Document::catalog` resolves, with §7.5.5's recovery when the trailer is damaged."

[[clause]]
clause = "7.7.2"
title = "Document catalog dictionary"
status = "partial"
code = ["crates/pdf-syntax/src/document.rs", "crates/pdf-model/src/page.rs", "crates/pdf-model/src/optional_content.rs", "crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/viewer_preferences.rs"]
test = ["crates/pdf-syntax/tests/real_documents.rs", "crates/pdf-model/tests/optional_content.rs", "crates/pdf-model/src/viewer_preferences.rs::table_29s_display_entries_are_not_table_147s"]
note = "Table 29's entries, and which of them this tree reads. **This row was wrong for a hundred and twenty sessions and its own list is the evidence**: it said \"[n]ot read, and every one of them is a *viewer* feature rather than a rendering one\" over twenty-five keys, and eighteen of the twenty-five are read — most of them by the session that built their clause, none of which came back here. `/PageLabels` (§12.4.2, session 48), `/Names` and `/Dests` (§12.3.2.4, 48 and 49), `/Outlines` (§12.3.3, 50), `/OpenAction` (§12.3.2, 49), `/Threads` (§12.4.3, 90), `/URI` (§12.6.4.8's base, 80), `/StructTreeRoot` and `/MarkInfo` (§14.7, 78), `/Lang` (§14.9.2, 60), `/ViewerPreferences` (§12.2, 81), `/Requirements` (§12.11, 84), `/Collection` (§12.3.5, 93), `/Perms`, `/Legal` and `/DSS` (§12.8, 98 and 99), `/Extensions` (§7.12, 84), `/OCProperties` (§8.11), `/AcroForm` (§12.7), `/OutputIntents` (§14.11.5), `/Pages` and `/Version`. The sentence \"`CLAUDE.md` puts page labels, outlines and destinations in scope as things that display; none is started\" was written before all three shipped and outlived them by a hundred and twenty sessions. **`/PageMode` and `/PageLayout` were read in the hundred-and-seventieth**, because the clause had become reachable: `/PageMode` names a *panel* — \"UseOutlines Document outline visible … UseOC Optional content group panel visible … UseAttachments Attachments panel visible\" — and until the sidebar existed there was nothing for three of its six values to name. `viewer-ui` obeys **four** of the six since the two-hundred-and-sixty-first session, when §12.3.4's thumbnail panel arrived and `UseThumbs` stopped naming nothing, and says once what it cannot do for `FullScreen`; **`/PageLayout` is obeyed since the six-hundred-and-sixth session, and this row's \"handed over\" was the shape of refusal `doc/habits.md` calls a capability that reached the crate and never reached the program.** All six values are arranged by `viewer_core::layout`: `OneColumn` and the two `TwoColumn` values scroll across page boundaries, the two `TwoPage` values show one spread at a time, and \"odd-numbered\" is the page's own number, so `TwoColumnRight` leaves page one alone on the right as a bound book does. The entry is the state the document *opens* in — \"shall be used when the document is opened\" is an initial state, not a permanent one — so `Command::Layout` is what a reader changes it with, on the same argument `CLAUDE.md` makes about a document's assertions over its reader. Table 147's `/Direction` is deliberately not applied to it: that entry's rearrangement is a *may* and `/PageLayout` states a side outright. **All three hosts draw all six since the six-hundred-and-seventh session** (ADR 0442): `viewer-gtk` and `viewer-qt` one raster per page on the screen, and `viewer-ui` — which asked the viewer *back* for `SinglePage` for one round, because a tier-2 surface drew one display list per frame — one frame carrying every page of the arrangement, each under its own `TargetSpec`. It needed no new message: `Query::PageGeometry` already answers `Answer::None` for a page the arrangement does not show, which is what tells a host holding a render request that the page has scrolled off. Measured: 38 of 961 corpus catalogs state a `/PageMode` (22 `UseNone`, 11 `UseOutlines`, 4 `UseOC`, 1 the undefined `/Use0`) and 43 a `/PageLayout` (24 `SinglePage`, 19 `OneColumn`). **Genuinely unread, and each for a reason**: `/SpiderInfo` (§14.10's web capture, 5 documents), `/PieceInfo` (§14.5's private producer data, 5), `/NeedsRendering` (Annex K's XFA, deprecated in PDF 2.0 and on `CLAUDE.md`'s exclusion list, 3), `/DPartRoot` (§14.12 reads document parts from a jump's own reference rather than from here), the catalog's own `/AA` (§12.6.3's document-level triggers, 3) and `/AF` (§14.13 reads it wherever an object states one, 6), and `/Metadata` was in that list until the three-hundred-and-seventy-fifth session, where it was false twice over: `Xmp::document` reads *this* entry — \"[t]he document-level packet, from the catalog's `/Metadata` (§7.7.2, Table 29)\" is its own doc comment — since ADR 0186, and §12.2's `/DisplayDocTitle` stopped waiting for it in the two-hundred-and-ninety-fourth, when `dc:title` became what a window is named from. **The XMP sweep of the two-hundred-and-ninety-fifth session corrected four rows and three comments about that noun and missed this one**, because this row says an entry is \"[g]enuinely unread\" rather than that the tree reads no metadata — which is `doc/todo/01`'s own lesson about grepping the shape rather than the wording, one sweep later. `partial` for the six above."

[[clause]]
clause = "7.7.3"
title = "Page tree"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_page_tree_of_any_shape_yields_its_pages_in_the_order_its_kids_arrays_give", "crates/pdf-model/tests/page_geometry.rs::a_user_unit_on_an_ancestor_is_not_inherited"]
note = "The tree, its nodes, a page object's entries and the inheritance between them. See the subclauses."

[[clause]]
clause = "7.7.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_page_tree_of_any_shape_yields_its_pages_in_the_order_its_kids_arrays_give"]
note = "\"Compliant PDF processors shall be prepared to handle any form of tree structure built of such nodes\", and `Pages::get` walks whatever it finds rather than assuming the flat or balanced shape the NOTE describes. Two bounds guard the walk against a tree that is not one: 64 levels of nesting — a balanced tree over a hundred thousand pages is about six — and a million nodes visited per lookup. The clause's own reason for the tree is why the walk is on demand: \"PDF readers using only limited memory, can quickly open a document containing thousands of pages\", which is also this project's time-to-first-page requirement."

[[clause]]
clause = "7.7.3.2"
title = "Page tree nodes"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_page_tree_of_any_shape_yields_its_pages_in_the_order_its_kids_arrays_give", "crates/pdf-model/tests/page_tree_nodes.rs::a_kids_entry_that_names_no_node_is_not_a_page"]
note = "Table 30's `/Kids` is followed, and the walk decides a leaf by the *absence* of `/Kids` rather than by `/Type`, because files omit the name and a malformed tree should still yield its pages. **The one exception is the file that answers the question itself**, since the four-hundred-and-seventieth session: a dictionary stating `/Type /Pages` and no `/Kids` has said in one breath that it is a node and that it has no children, and Table 31 makes `/Type` required of a page object and \"shall be Page for a page object or Template for an invisible Template page\" — so it is not drawn as a page. `page.rs::declares_a_node`, pinned as pairs of fixtures in `crates/pdf-model/tests/page_tree_nodes.rs`, witnessed by `doc/corpora/format-corpus`'s `T02-02_005_page-tree-no-kids.pdf`, which drew blank in silence and now draws its page; **not one of the 65 703 crawled web documents that open has such a node**, which is why it needs pinning by hand (ADR 0305). **Where the tree yields nothing at all, `/Type` on a page is asked instead** (hundred-and-seventh session): a document whose tree is broken can still be asked which of its objects declare themselves pages, with §7.7.3.4's inheritance applied up each one's own `/Parent` chain, in ascending object number — a documented choice, because §7.7.3.2's tree is where page *order* lives and a file whose tree is gone has not stated one. It runs only where the tree produced nothing, so no document that opens normally pays for it. **`/Count` is trusted exactly as far as the `/Kids` beside it**, and this note said \"deliberately not trusted\" for many sessions while the code read it twice: it is the page *count* where the root has children and it skips a subtree in `find_leaf` where that subtree has children of its own. What the clause's own NOTE licenses is the fallback rather than the refusal — the entry \"is redundant\" because \"the value of the Count key is consistent with the number of entries in the Kids array and its descendants which definitively determines the number of descendant pages\" — so a `/Count` with no `/Kids` under it describes a subtree the file never wrote and the walk settles it. **And Table 30's \"children shall only be page objects or other page tree nodes\" is now a rule with teeth in both directions**: a `/Kids` entry naming something that is neither is stepped over without consuming one of the pages the walk is counting down, so the pages after it keep their indices. It was that before and nothing said so, because resolving such an entry produced no dictionary and the walk moved on; since the four-hundred-and-ninety-fifth session the walk holds a node by *name* until it needs it, so the question is asked outright and answered by `Document::get_key_of` (ADR 0330)."

[[clause]]
clause = "7.7.3.3"
title = "Page objects"
status = "partial"
code = ["crates/pdf-model/src/page.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs", "crates/pdf-model/tests/contents_entry.rs", "crates/pdf-model/tests/page_geometry.rs::a_page_whose_ancestry_states_no_media_box_is_drawn_on_a_default_and_says_so", "crates/pdf-model/tests/contents_entry.rs::a_contents_entry_that_is_a_bare_integer_says_so_too"]
note = "Table 31's entries that decide what is drawn: `/Resources`, `/MediaBox`, `/CropBox`, `/Rotate`, `/Contents` (one stream or an array \"concatenated with at least one white-space character added between the streams' data\", since a token may straddle the boundary — and **the same sentence is what bounds the total**, because it makes the array \"a single stream\" and a single stream has a bound: the concatenation had none at all while `max_array_len` admits 2^20 parts, and passing it now reports `ContentIssue::TooLarge { part: None }` rather than nothing, ADR 0306 — and **the white-space character the sentence asks for used to cost a copy of the whole page**, because the concatenation asked the buffer for exactly the part's length and then pushed one byte into a full `Vec`, which doubled it; the separator is reserved with the part since ADR 0354), `/Annots` (§12.5), `/Group` (§11.4.7's page group) and — **as of the twenty-ninth session** — `/UserUnit`. That last is \"a positive number that shall give the size of default user space units, in multiples of 1/72 inch\", and it had been read nowhere: **all three of the oracle's page-geometry disagreements were this one entry**, two documents writing `/UserUnit 3` and one writing `/MediaBox [0 0 8.5 11]` with `/UserUnit 72`, which is a page stated in inches. `mutool` and `ghostscript` scale by it and `poppler` does not; the clause settles it rather than the vote. A value that is not positive is the default of 1.0, and values above 1000 are refused — the clause's \"range of supported values shall be implementation-dependent\", answered here. **This row's \"not read\" list was eighteen entries and eleven of them are read**, which is the understating shape ADR 0101 named and no gate can watch. Read since the sessions that built them: `/BleedBox`, `/TrimBox` and `/ArtBox` (§14.11.2's boundaries, which §12.2's viewer preferences select between), `/Thumb` (§12.3.4), `/B` (§12.4.3's articles), `/Dur`, `/Trans` and `/PresSteps` (§12.4.4's presentation), `/AA` (§12.6.3's triggers), `/StructParents` (§14.7.4) and `/VP` (§12.9's viewports). **And the list was wrong twice more, found by `doc/todo/01`'s second sweep in the three-hundred-and-thirteenth session.** `/Tabs` has been read since the two-hundred-and-ninety-seventh — all five of Table 31's orders, in `pdf_model::tab_order`, with §12.5.1's own row carrying the argument — and this list went on calling it unread for sixteen rounds, which is the fifth failure shape one page-entry over. And `/PZ` is not unread but *declined*: Table 31 defines it as \"the factor by which it shall be scaled to achieve the natural display magnification\" and hands the meaning to §14.10.6, which says the page \"**may** be scaled to undo the downscaling and view the page at its original size\" — a permission, and one this program declines because a magnification is already decided twice, by `Zoom::FitPage` and by §12.3.2.1's `/OpenAction` view, and the standard states no precedence among the three. Counted before the refusal: 18 of the 974 corpus documents state a `/PZ` on 33 pages. Genuinely not read: `/LastModified`, `/BoxColorInfo` (guideline colours for a production workflow), `/Metadata` (§14.3.2's reader takes a dictionary and so would read a page's packet; nothing in scope consumes an *object-level* one, which is the honest statement — Table 348's entry is for interchange and no clause makes a page's metadata change what is drawn), `/PieceInfo`, `/ID`, `/SeparationInfo` and `/TemplateInstantiated`. **A `/Contents` naming an object this reader cannot reach now says so**, since the four-hundred-and-twenty-second session: Table 31's \"If this entry is absent, the page shall be empty\" is a statement about an *absent* entry, and §7.3.9 makes a null value equivalent to one — so a reference that lands on nothing draws the same blank page and is not the same statement. `ContentIssue::Unreachable`, tested in `crates/pdf-model/tests/contents_entry.rs`, witnessed by `doc/corpora/pdf-differences`'s `UnknownFilter-PageContentStream.pdf` and costing the pdf.js corpus nothing, because no document in it does this (ADR 0258). **And Table 31's `/Type` decides one thing in the *tree* as well as in the page**, since the four-hundred-and-seventieth: the entry is required and names `Page` or `Template`, so a dictionary that says `Pages` instead is not a page object and is not drawn as one where its `/Kids` is missing — §7.7.3.2's row has the reading and ADR 0305 the argument. **And `/MediaBox` — \"( Required; inheritable )\", the one Table 31 entry without which a page has no geometry at all — is substituted loudly since the five-hundred-and-fifty-fourth session.** A page whose ancestry states none, or states one that is not §7.9.5's four finite numbers, is drawn on `Page::DEFAULT_MEDIA_BOX` and reports `Unsupported::MediaBox`; it was drawn on it in silence before, so a page whose size — and therefore whose every mark's place — was this reader's guess looked exactly like a page its producer had measured. The population across every corpus on this disk: 4 of the 974, 1 of `pdfbox`'s 64, 4 of `format-corpus`' 167 (two of them built to carry this defect and nothing else) and **1 document of the 65 703 crawled that open**, a 22-page worksheet this tree draws 50 points low and 16 units narrow. Not one page of the whole population states another of §14.11.2's boxes, so there is nothing in the file to prefer to the guess. §7.7.3.4's row has the clause and ADR 0389 the argument. **And Table 31's `/Type` decides one thing in the *tree* as well as in the page**, since the four-hundred-and-seventieth: the entry is required and names `Page` or `Template`, so a dictionary that says `Pages` instead is not a page object and is not drawn as one where its `/Kids` is missing — §7.7.3.2's row has the reading and ADR 0305 the argument."

[[clause]]
clause = "7.7.3.4"
title = "Inheritance of page attributes"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_user_unit_on_an_ancestor_is_not_inherited"]
note = "The four entries Table 31 marks inheritable — `/Resources`, `/MediaBox`, `/CropBox` and `/Rotate` — are overlaid on the way down the tree so that the nearest ancestor's value wins, which is what inheritance means here. The sentence that makes the *rest* of Table 31 a closed question is §7.7.3.3's rather than this one's — \"attributes that are not explicitly identified in the table as inheritable shall not be inherited\" — which is why `/UserUnit` is read from the page object alone even though reading it through the same path as `/MediaBox` would be one word's difference. \"All values shall be inherited as-is, without merging, even for composite data types\" is why a page's `/Resources` is the first dictionary found and not a union of the ones above it. **The clause's second sentence about a required attribute is a rule about the file rather than about the walk, and until the five-hundred-and-fifty-fourth session nothing in this tree read it as one**: \"[i]f the attribute is a required one, a value shall be supplied in an ancestor node\", and a page whose whole ancestry supplies no usable `/MediaBox` has broken it. The standard states no recovery, so `Page::DEFAULT_MEDIA_BOX` stands in — a choice, kept A4 against three references' 612 × 792 because principle 5 forbids moving a constant to match a vote on a question the standard answers nowhere — and `Page::substituted_media_box` now carries *why* so that `Unsupported::MediaBox` can say it. **Three** ways to reach it, counted apart — and the third was found in the five-hundred-and-seventy-fifth session, on the one page of the oracle's `no render` bucket that was this reader's: no ancestor writes the entry; one writes it and no ancestor writes §7.9.5's four finite numbers; or one writes four finite numbers enclosing **no area**, which §7.9.5's own NOTE makes a rectangle and Table 31 does not make a medium. That third value passed every test a rectangle reader can apply, so `TargetSpec::for_page` refused the page as degenerate and `boundingBox_invalid.pdf` — whose first page is captioned *Empty /MediaBox* — was **unviewable**, where the substitution exists precisely so that a page whose extent the file botched still draws. `MediaBoxSubstitution::Empty` is the third variant; an unusable value on a *leaf* falls back to an ancestor's usable one rather than erasing it, since the file has still said how big the page is one level up. `examples/media_box_census` is the population — zero pages of the 919 995 crawled ones and one deliberate corpus witness — and ADRs 0389 and 0410 the argument."

[[clause]]
clause = "7.7.4"
title = "Name dictionary"
status = "partial"
code = ["crates/pdf-model/src/attachment.rs", "crates/pdf-syntax/src/tree.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_embedded_file_is_read_with_its_parameters"]
note = "The catalog's `/Names` name dictionary — Table 32's ten trees, each a §7.9.6 name tree. Four are read: `/Dests` (§12.3.2.4's named destinations, since the forty-ninth session), `/EmbeddedFiles` (§7.11.4's attachments, since the eighty-sixth), and `/Pages` and `/Templates` (§12.7.7's named pages, `named_page::NamedPages` running both trees' invariants — **this row went on owing both to that clause after its reader landed**, the second sweep's shape in a family map). The other six are each owed to a feature rather than to this row: `/AP` to §12.7.4.3's appearance streams by name, `/JavaScript` to a clause principle 5 excludes, `/IDS` and `/URLS` to web capture, `/AlternatePresentations` to a deprecated feature, and `/Renditions` to clause 13 — and no source quotes any of those four tree names, checked in the same sitting as the correction above. `tree::name_pairs` is what a caller with no key to look up needs, and is the walk `number_pairs` already was for §7.9.7."

[[clause]]
clause = "7.8"
title = "Content streams and resources"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/content_streams.rs::a_compatibility_section_silences_an_unrecognised_operator"]
note = "Both halves: a content stream is lexed and executed sequentially, and its resources are resolved by name against the dictionary the clause associates with it. See §7.8.2 and §7.8.3."

[[clause]]
clause = "7.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/render_real_pdf.rs::every_specification_pdf_interprets"]
note = "Introductory. Its one claim — that a content stream and its resource dictionary \"in combination … form a self-contained entity\" — is the shape of `Interpreter::run`, which takes the two together and nothing else about the document except what a resource resolves to."

[[clause]]
clause = "7.8.2"
title = "Content streams"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-model/tests/content_streams.rs::the_silence_stops_at_the_matching_ex", "crates/pdf-model/tests/content_streams.rs::compatibility_sections_nest", "crates/pdf-model/tests/leftover_operands.rs::a_show_operator_reads_the_string_that_precedes_it", "crates/pdf-model/tests/leftover_operands.rs::a_show_with_no_font_set_is_reported_rather_than_skipped", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_form_xobject_draws_its_prefix_and_reports_the_shortfall", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_tiling_pattern_draws_its_prefix_and_reports_the_shortfall", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_type3_glyph_description_draws_its_prefix_and_reports_the_shortfall", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_appearance_stream_draws_its_prefix_and_reports_the_shortfall"]
note = "Every clause of it. **This row said that for four hundred and sixty-six sessions and one sentence of the clause was not implemented** (ADR 0302): \"In PDF, all of the operands needed by an operator shall immediately precede that operator. Operators do not return results, and operands shall not be left over when an operator finishes execution.\" The interpreter's operand accessors indexed from the *front* of everything the stream had stated since the previous operator, which is the same slice on a conforming stream — nothing is ever left over — and a different one on every malformed stream. `operands_before` and `count_of` give each operator the last of them, the count being the operand list stated by the table Annex A points at for it; `TJ`, `d`, `sc`, `scn`, `SC` and `SCN` are given everything, because the lexer leaves their arrays flattened and a leftover in front of a variable-arity operator is indistinguishable from one of its own. The witness is `openpreserve/format-corpus`'s `T02-05-01_008_Font-set-operator-missing.pdf`, whose deleted `Tf` keyword left `/F0 36.` standing in front of a `Tj`: reading from the front found a name where a string was wanted, so the show operator disappeared before `show_text` could report it and the page drew blank **in silence**. Nothing is reported for the leftovers themselves, deliberately — after the fix the mark the file states is drawn, so the report would name no lost mark while taking every page carrying one off the oracle's judged set (trap 11). Two of the 974 change and neither is a regression; 4000 crawled web documents are untouched, which is what the clause predicts. The stream is lexed by §7.2's rules and executed in order; the five places the clause names as also being content streams — pages, form XObjects, patterns, Type 3 glyph descriptions and annotation appearances — all reach the same interpreter. \"An operand is a direct object belonging to any of the basic PDF data types except a stream … Indirect objects and object references shall not be permitted at all\" holds by construction: the content lexer has no reference token, so a `1 0 R` in a content stream lexes as two integers and an unrecognised operator. Table 33's `BX` and `EX` were matched and ignored for thirty-one sessions and are now the compatibility *section* the clause defines, kept as a nesting depth because \"these operators shall occur in pairs and may be nested\" — and it is the one place in this interpreter where unsupported input is deliberately silent, because the file has said in advance that ignoring it is appropriate. No corpus document exercises it: every document that reports an unrecognised operator reports one no producer meant, `issue9252.pdf`'s lone `.` being the newest of them (ADR 0303 — §7.3.3 makes a run holding no decimal digit no number at all, so the lexer hands it over as the keyword it lexically is rather than as zero). **`MAX_OPERATIONS` bounds how long the program in the stream may run, and it counted the wrong thing for its whole life** (ADR 0306). This clause's own sentence is why: all of an operator's operands shall immediately precede it, so `x1 y1 x2 y2 x3 y3 c` is seven lexer tokens and one operator — and the single increment site was the token loop, which charged a curve seven times over. The counter now increments where the interpreter knows it has an operator. **The value did not move and the unit did**, which is `doc/todo/10` §6's rule applied rather than a decision deferred: over 926 680 pages of 65 967 crawled documents (`content_budget_census`) 48 pages state more than four million tokens and **8** state more than four million operators, the corpus-wide ratio being 3.76 and not a constant — about 2 for text and about 7 for Bézier artwork, which is why one could never stand in for the other at any value. The four-hundred-and-thirty-fifth session's finding stands with its unit corrected: all 31 documents of 65 944 that reached the *token* count terminate with it lifted a hundredfold, so the population is maps, plans and charts rather than decompression bombs. It is left at four million because **a count is not a cost**: one `sh` paints the whole page, so no number here bounds the time, and what does bound it is the confined worker's cancel at 0.83–1.97 ms (ADR 0241). ADRs 0271 and 0306. **And the clause's second paragraph reached the code in the five-hundred-and-twenty-fourth**: it names the four objects that are content streams without being Table 31's entry — forms, patterns, Type 3 glyph descriptions and annotation appearances — and ADR 0343's rule for a damaged `/Contents` is therefore theirs word for word. All four drew a damaged prefix in silence until then; `Unsupported::DamagedContentStream` is what each of the five call sites says now, through `Interpreter::content_stream`. ADR 0359. **A lexer fed from a fixed window reads the same content stream**, which this clause's own sentence about where a division between `/Contents` parts may fall is what permits: session 527's spike refills a buffer from the inflate pump and re-lexes whatever ran up to the boundary, and page one of every pdf.js document through a 512-byte window agrees with the whole-buffer route token for token — 948 agree, 0 disagree. The largest single token in 39 976 documents is 390.16 KiB and none passes 1 MiB (`token_window_census`), so the window a reader-fed lexer needs is a measured quantity rather than `max_string_len`'s 2²⁶. ADR 0362. **And in the five-hundred-and-thirtieth that is how a page's `/Contents` is read**: `content::reader` pumps the parts into a 64 KiB window that grows to 1 MiB for one token, and the interpreter lexes through it — the same tokens, proved as bytes (the readback of ISO 32000-2's 1023 pages and the display list of every corpus document are identical), for +5.74% instructions on an ordinary page and a bomb that costs 8.4 MB where it cost 1032. **The clause sentence this rests on is Table 31's rather than this one's**, and `doc/todo/14` attributed it here for nine sessions: \"The division between streams may occur only at the boundaries between lexical tokens\" is §7.7.3.3's, which is what lets the parts chain into one reader. What a bounded buffer cannot hold it says out loud — `ContentIssue::TokenTooLong` — because a cut token is bytes the file never wrote. **And in the five-hundred-and-ninety-second the other four go through it too**, which that round left as the honest hole: \"Content streams shall also be used to package sequences of instructions as self-contained graphical elements, such as forms (see 8.10, \"Form XObjects\"), patterns (8.7, \"Patterns\"), certain fonts (9.6.4, \"Type 3 fonts\"), and annotation appearances (12.5.5, \"Appearance streams\").\" The criterion that kept them whole is real — §11.6.6's paired runs read a form two and three times over, a tiling cell runs once per cell and a glyph description once per character, so none of the four is read \"once, forwards\" — and what answers it is not a new number but the *decoded-stream memo's own condition*: a stream the memo keeps is decoded whole and every read after the first is a cache read, exactly as before, and one the memo declines is pumped through the window, which costs nothing it was not already paying because a decode the memo declines is re-run on every read today. Every bomb is in the second half by construction. **That premise has one exception and the `page` fuzz target found it**: it is a claim about *who reads*, and a tiling pattern's `Tiling` holds the cell's decode for the whole tiling, so windowing that one inflates the cell again per cell — 0.24 s against 9.0 s on a mutated pattern, with `MAX_TILES` allowing four thousand cells. §8.7.3.1 keeps the whole decode, in a type (`HeldContent`) rather than in a comment, because a route decision is invisible in its output. `Interpreter::run` now takes a *source* rather than a buffer and makes a reader per run; `ContentIssue` is translated at the boundary rather than escaping, because every indexed variant of it is about a part of Table 31's entry and a form reached through `/XObject` has no such index. A bomb in a form costs **10.7 MB where it cost 1032**, ISO 32000-2 page 101 costs **+0.089%** instructions and the corpus's 974 display lists are byte-identical. ADR 0427. **And in the five-hundred-and-ninety-fourth the window learned the second of §7.4's streaming filters**: `Document::is_pumpable` became `Document::pumping`, the one place a chain's route is decided, so a page's `/Contents` and these four answer it together — and it now admits a single `LZWDecode` beside `FlateDecode`. The three §7.4 filters left out are left out on their expansion ratio, which is what a bomb needs: `ASCIIHexDecode` shrinks its input, `ASCII85Decode` reaches 4:1 and `RunLengthDecode` 64:1, against LZW's 1365:1 and Flate's measured 1032:1. An LZW bomb in a form's decode is now the window rather than the gibibyte, on the same route the flate one takes, and the 974 display lists stay byte-identical. ADR 0429. **And Annex A's operand counts reached the type system in the five-hundred-and-thirty-fifth**: every operator `count_of` names takes a fixed number, so `numbers_from` and `points_from` take it as a `const` parameter and answer with an array on the stack. They collected a `Vec` from a `filter_map` whose lower size hint is zero, so six operands cost a `malloc` and two `realloc`s — 18.16% of interpreting `doc/todo/44`'s witness, three million path operators. The reading is unchanged: the old form collected what parsed and then required all of them, and the new one refuses at the first that does not. ADR 0370."

[[clause]]
clause = "7.8.3"
title = "Resource dictionaries"
status = "implemented"
code = ["crates/pdf-model/src/page.rs", "crates/pdf-model/src/content.rs", "crates/pdf-model/src/type3.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_user_unit_on_an_ancestor_is_not_inherited", "crates/pdf-model/tests/missing_resources.rs::a_form_that_states_no_resources_uses_the_pages", "crates/pdf-model/tests/missing_resources.rs::a_form_with_its_own_resources_does_not_reach_past_them", "crates/pdf-model/tests/type3.rs::a_glyph_description_finds_the_resources_its_own_stream_names", "crates/pdf-model/tests/accessibility.rs::an_af_tagged_section_reads_the_property_lists_mcaf_key"]
note = "All four of the clause's ways a resource dictionary reaches a content stream: a page's `/Resources`, inherited through the page tree (§7.7.3.4); a Type 3 font's own; a form XObject's, a pattern's and an appearance stream's; and the fourth, for older files that omit it, whose resources are inherited from the page's dictionary — which is why a form XObject, a pattern or an appearance stream stating no `/Resources` at all is run against its parent's. **That sentence used to end \"which is why every resource lookup falls back to the enclosing dictionary rather than failing\", and the code has never done that**: the fallback is on the *entry's* absence and not on a name's, so a stream that states a `/Resources` is searched in that one alone. The four-hundred-and-nineteenth session corrected the claim and made the failure loud (ADR 0255) — a `Do`, a `gs` or an `scn` whose operand the current resource dictionary does not define now reports `Unsupported::MissingResource` rather than drawing nothing in silence, which is Table 86's, Table 56's and §8.7.3.2's `shall` on the operand and this clause's on the dictionary. Two corpus documents joined the incomplete list for it, `issue6541.pdf` and `issue8702.pdf`, and the condition is stated where a report can cost nothing: content §8.11.3.1 hides is skipped whole, report and all. **That fourth bullet is struck out by Errata Collection 3 and this row quoted it until the four-hundred-and-eighteenth session** (Issue #128, `/State` `Review` `Completed`), and what replaces it is a *search order* rather than a fallback: for a Type 3 glyph description the clause now names four places in turn — the glyph description content stream's own dictionary, the Type 3 font dictionary that held the `/CharProcs` entry, the parent page, and what that page inherits (§7.7.3.4). **The first of the four was not implemented**: `type3::Type3Font::resources` searched the font and then the page, so a glyph stream that carried its own `/Resources` was read against somebody else's. It is implemented and tested since that session; §9.6.4's row carries the other half. **A resource name is scoped to the dictionary that defines it, and for thirty-one sessions the font cache was not.** The interpreter keyed loaded fonts by the name a `Tf` used, so a form XObject's `/F1` was answered with whatever `/F1` the page had loaded — wrong glyphs, in silence, on two corpus documents (ADR 0115, hundred-and-twenty-seventh session). It is keyed by the font dictionary's object identity now, which is what `shading::Cache` had been doing for the same reason since the seventy-third. **And a resource a stream *names* but does not define is now answered where the name is one of §9.6.2.2's fourteen** (two-hundred-and-eighty-second session, ADR 0183). The clause's third bullet is a `shall` on the writer — the stream's dictionary shall specify a resource dictionary \"which contains all the resources used by that content stream\" — and `issue17492.pdf` breaks it: a text widget's stored appearance carries `/Resources <<>>` and sets its value in `/Helvetica 12 Tf`. Nothing is invented for a name the standard does not define, and `/F1` still reports on two documents; but for the fourteen names §9.6.2.2 lists, which Table 109 lets a document state without any metrics at all, drawing the compiled-in program is a better reading of that stream than drawing nothing. (This sentence quoted §9.6.2.2's \"shall be available to the PDF processor\" until the four-hundred-and-eighteenth session; Errata Collection 3 struck it outright — Issue #47 and #48 — and `pdf_font::standard` carries the warrant that survives.) `mupdf` and `ghostscript` draw those three lines and `poppler` refuses, which is evidence about the reading rather than the reading itself. Of Table 34's eight resource types, seven are read; `/ProcSet` is deprecated in PDF 2.0 and §14.2 makes it advisory — that row is `inapplicable` because the array \"shall be used only when the content stream is printed to a PostScript language compatible output device\", so the entry can decide nothing on a display and reading it past is the answer rather than a debt. **This row was `partial` for `/Properties` and the sentence said it is \"read only for `/OC` (§8.11.3.3) and not for §14.6.2's marked-content property lists in general — which is clause 14's gap rather than this one's\", which the code had already retired**: `content/marked.rs`'s `property_list` resolves a `BDC` operand's *name* through the `/Properties` subdictionary for §14.9's four accessibility entries, §14.7.5.2's `/MCID` and §14.13.5's associated files, which is this clause's own rule about the two forms — a list of direct objects may be written inline and one holding an indirect reference \"shall be defined as a named resource in the Properties subdictionary\". §8.11.3.2's optional content is the one caller that cannot use it, because it needs the group's identity rather than its value, and §14.6.2's own row has listed what a property list is read for all along: two rows about one mechanism, and the one that was corrected is not the one that was wrong (`doc/todo/01`'s fifth and seventh shapes). Found in the five-hundred-and-sixty-fifth session by reading the row off the blame list."

[[clause]]
clause = "7.9"
title = "Common data structures"
status = "partial"
code = ["crates/pdf-syntax/src/text_string.rs", "crates/pdf-syntax/src/date.rs", "crates/pdf-syntax/src/tree.rs", "crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_surrogate_pair_is_one_character", "crates/pdf-model/tests/dates.rs::every_date_string_in_the_corpus_is_measured_against_the_clause"]
note = "The clause's own aggregate, and **six of its seven structures are implemented**: §7.9.2's string object types, §7.9.4's dates — with a gate over 1545 corpus date strings — §7.9.5's rectangles, §7.9.6's name trees and §7.9.7's number trees, which `tree.rs` reads as one module because the second clause defines itself as the first with integer keys (ADR 0053). `partial` is §7.9.3's text streams alone, which is `reported`. **This note said \"[d]ates, text streams, name trees and number trees belong to features this tree does not have yet\" until the two-hundred-and-ninety-eighth session**, which was false of three of the four; `doc/todo/01`'s fifth failure shape found it in the two-hundred-and-seventy-eighth and the row was not changed, which is the shape's own shape one level up — **a correction recorded in a todo file is not a correction.** **Read and kept in the five-hundred-and-twenty-fifth**: §7.9.3 is still `reported`, which is what this row's `partial` is, and it is the arithmetic sweep's standing hit for that reason rather than for a stale note."

[[clause]]
clause = "7.9.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_surrogate_pair_is_one_character"]
note = "Introductory. Its claim — that these structures are \"built from the basic types described in 7.3\" and carry conventional meanings — is the reason `text_string.rs` and `annotation::rectangle` exist as functions rather than as a rule repeated at each use."

[[clause]]
clause = "7.9.2"
title = "String object types"
status = "partial"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_string_with_no_prefix_is_pdfdocencoded"]
note = "Three of the four string object types are read where they are used. §7.9.2.2's text string is `text_string`, compiled against Table D.3; §7.9.2.4's byte string is the lexer's own output, which is what every other reader of a `/String` takes; §7.9.2.3's PDFDocEncoded string shares Table D.3 with the text string type and differs only in admitting no Unicode prefix, which no entry this tree reads distinguishes. What is not here is the *typing*: the object model carries one string type and each reader decides which of the four it holds, so a text string read as bytes is a defect nothing in this crate can catch."

[[clause]]
clause = "7.9.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/object.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_string_with_no_prefix_is_pdfdocencoded"]
note = "The clause's whole content is a statement about the object model: 'PDF supports one fundamental string object' and 'the string object shall be further qualified as a text string, ASCII string, or byte string'. This crate holds exactly that — one `Object::String` of bytes, qualified by whoever reads it. Figure 7's containment is why `text_string` can be applied to any string and why an ASCII string needs no decoder."

[[clause]]
clause = "7.9.2.2"
title = "Text string type"
status = "implemented"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_string_with_no_prefix_is_pdfdocencoded", "crates/pdf-syntax/src/text_string.rs::a_utf16be_marker_selects_utf16be", "crates/pdf-syntax/src/text_string.rs::a_utf8_marker_selects_utf8"]
note = "All three encodings the clause names, chosen by the prefix it states, with Annex D's Table D.3 compiled in — and since the hundred-and-thirty-fifth session the *write* side of two of them: `encode_text_string` produces `PDFDocEncoding` where the table covers the string and UTF-16BE with the clause's prefix otherwise, which is what a field value a person typed has to become before §12.7.4.3 can lay it out. UTF-8 is not produced: it is the PDF 2.0 form, and a file this program writes into has to stay readable by whatever could read what it was written into. The reason it exists is §12.7.4.3: a field's `/V` and a free text annotation's `/Contents` are text strings and have to become characters before they can become glyphs. Table D.3 is not ISO Latin 1 and the differences decide pages — 0xA0 is EURO SIGN, 0x18 to 0x1F are accents, 0x80 to 0x9E are punctuation — so a Latin 1 decode would draw the wrong character with nothing reported."

[[clause]]
clause = "7.9.2.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_surrogate_pair_is_one_character", "crates/pdf-syntax/src/text_string.rs::the_table_is_not_latin_1", "crates/pdf-syntax/src/text_string.rs::an_undefined_code_point_is_the_replacement_character"]
note = "The prefix rule and all three encodings, including the clause's requirement that a reader 'shall be prepared to handle supplementary characters' — UTF-16BE is decoded by pairing surrogates rather than by taking each code unit as a character, which its NOTE 5 is there to warn against. A code Table D.3 marks Undefined becomes U+FFFD, so it reaches the layout as a character no font encodes and is reported there rather than dropped."

[[clause]]
clause = "7.9.2.2.2"
title = "Text string language escape sequences"
status = "implemented"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::a_language_escape_sequence_shows_nothing", "crates/pdf-syntax/src/text_string.rs::a_lone_escape_does_not_swallow_the_rest_of_the_string"]
note = "Both shapes the clause states — U+001B, a two-letter language code, an optional two-letter country code, U+001B — are removed from a Unicode text string, because the sequence marks a language and carries no character to display. Only from the two Unicode forms: the clause states the byte sequences for UTF-16BE and UTF-8 and not for PDFDocEncoding. A U+001B with no closing one is left where it stands rather than swallowing the rest of the value. What is *not* done with it is anything: the sequence overrides §14.9.2's hierarchy, which the sixtieth session built, so the language a `/Lang` states now reaches a caller and the one an escape sequence states is still dropped with the escape. Closing that means carrying a language *inside* a decoded text string, which changes the return type of every text-string read in the tree for a construct no corpus document writes."

[[clause]]
clause = "7.9.2.3"
title = "PDFDocEncoded string type"
status = "implemented"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::the_table_is_not_latin_1"]
note = "The same Table D.3 as §7.9.2.2, which is the whole of the type: 'a character string in which the characters shall be represented in a single byte using PDFDocEncoding'. No entry this tree reads is typed as one rather than as a text string, and the two differ only in whether a Unicode prefix is honoured, so the table has one reader."

[[clause]]
clause = "7.9.2.4"
title = "Byte string type"
status = "implemented"
code = ["crates/pdf-syntax/src/lexer.rs"]
test = ["crates/pdf-syntax/src/lexer.rs::hex_strings_ignore_junk_and_pad_an_odd_digit"]
note = "A byte string is what the lexer produces for every `/String`: 'binary data that shall be represented as a series of bytes, where each byte may be any value representable in 8 bits'. The two places the tree needs it as such are §7.6's `/O`, `/U`, `/OE`, `/UE` and `/Perms` and §14.4's file identifier, both of which take the bytes unchanged."

[[clause]]
clause = "7.9.3"
title = "Text streams"
status = "implemented"
code = ["crates/pdf-model/src/popup.rs"]
test = ["crates/pdf-model/src/popup.rs::a_rich_text_stream_is_read_as_a_text_string_after_its_filter"]
note = "A text *stream*, whose \"unencoded bytes shall meet the same requirements as a text string … with respect to encoding, byte order, and lead bytes\". **This row said \"[n]othing here reads one\" and named its own expiry condition — \"this closes the day an entry in scope uses the type\" — and that day was the round before the sweep found it.** ADR 0199 brought Table 172's `/RC` into scope for §12.5.6.14's popup window: the *characters* of a rich text string are not what principle 5 excludes, the XFA formatting is, so `popup::rich_text` reads the entry in both of the forms Table 172 types it. There are exactly **eight** entries in the standard typed `text string or text stream` — Table 172's `/RC` and Table 177's, Table 228's `/RV` and Table 249's, Table 221's `/JS`, and the FDF ECMAScript dictionary's `/Before`, `/After` and `/AfterPermsReady` — and the two `/RC`s are the ones in scope. **This sentence said \"exactly six\" while listing eight, and said `/RC` was \"the only one of them in scope, so implementing this clause is implementing it once\"**; the three-hundred-and-eighty-seventh session's ninth sweep counted the rows and the two claims came apart together. `/RC` is *two* entries in two tables with two different `shall`s — Table 172's is displayed in a popup window and Table 177's \"shall be used to generate the appearance of the annotation\" — and the second was implemented in that session (ADR 0224), which is when this clause was exercised the second time. `/RV` stays out: both of its rows say only \"[a] rich text string, as described in … XFA\" with no `shall` about displaying it, and §12.7.5.3's conventions paragraph says a `RichText` field's value is not what `/RV` describes. The clause's *unencoded* decides the order: `Document::decoded_stream_data` runs the filter and `text_string` decodes the result, so §7.9.2.2's prefixes are looked for in the stream's data rather than in its bytes. (A metadata stream is *not* one: §14.3.2 makes it XML.)"

[[clause]]
clause = "7.9.4"
title = "Dates"
status = "implemented"
code = ["crates/pdf-syntax/src/date.rs"]
test = ["crates/pdf-syntax/src/date.rs::the_clauses_own_example_parses_to_what_it_says_it_means", "crates/pdf-syntax/src/date.rs::every_field_but_the_year_may_be_absent_and_the_clause_states_its_default", "crates/pdf-model/tests/dates.rs::every_date_string_in_the_corpus_is_measured_against_the_clause"]
note = "`D:YYYYMMDDHHmmSSOHH'mm` in full, with every default the clause states — MM and DD both 01, everything else zero — so `D:1998` is a conforming date and parses. NOTE 2's older terminating apostrophe and NOTE 3's `Z` followed by zero offsets are both accepted, and a `Z` followed by *non*-zero offsets is refused as a file contradicting itself. An absent zone is GMT, which the clause decides rather than leaves open, and `Date::offset` still records that the producer claimed nothing. Every range the clause states is enforced and nothing is clamped; a non-conforming string stays available as the bytes the file wrote. The ordering is by the instant, which is what §12.3.5.1's Table 156 collection sort over a `/Subtype /D` field needs and what a string comparison gets wrong across time zones. **Audited over the whole corpus**: 1514 of 1545 date-shaped strings conform (97.99%), and all 31 that do not break a rule the clause states in as many words — 26 of them one producer writing an offset minute of 112, 144, 208, 224 or 240 against `mm (00-59)`. **Errata Collection 3 has struck out two of this clause's sentences and this row could not see it until the four-hundred-and-sixteenth session**, because the sponsored copy records EC3 as annotations and `doc/md/` dropped every annotation in all fourteen documents (ADR 0252). Issue #251, `/State` `Completed`: the GMT sentence is replaced by one that says the same thing in `/Offset`'s own units, so nothing here moves; \"Regardless of whether the time zone is specified, the rest of the date shall be specified in local time.\" is deleted outright, and what carried it is now the grammar — `O HH'mm` offsets the other fields, so those fields are local. This is the clause this project has quoted a retired sentence of, and `tools/spec-errata` names another 150 passages `doc/md/` still presents as current. **That number was 37 in this row and 79 everywhere else until the four-hundred-and-seventeenth session**, which found the checker's comparison blind to a space: both sides are extractions of the same glyphs by different programs, one writes \"inthe\" where the other writes \"in the\", and a whitespace-sensitive containment test called 72 passages absent that were there in full (ADR 0253)."

[[clause]]
clause = "7.9.5"
title = "Rectangles"
status = "implemented"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_user_unit_scales_the_page_and_its_contents"]
note = "\"A rectangle shall be written as an array of four numbers giving the coordinates of a pair of diagonally opposite corners\", and the `[llx lly urx ury]` order is the one the clause says the array \"[t]ypically\" takes rather than the one it requires — so a reader normalises rather than trusts. `annotation::rectangle` does, by taking the minimum and maximum of each axis, and it is the route for `/Rect`, `/BBox`, `/MediaBox`, `/CropBox` and `/RD`; `structure::normalised_rectangle` is a second, for the two rectangles that arrive as *attribute values* rather than as dictionary entries — Table 363's artifact `/BBox` and Table 379's layout one. A rectangle with fewer than four numbers, or any that is not finite, is refused rather than filled in. **This note quoted \"[a]lthough rectangles are conventionally specified by their lower-left and upper-right corners, it is acceptable to specify any two diagonally opposite corners\" until the four-hundred-and-sixty-sixth session, and ISO 32000-2 does not contain that sentence** — it is ISO 32000-1's wording for the same rule, checked against `pdftotext` on the PDF as well as against `doc/md/`. The rule survived the rewording; the quotation did not. **The clause's NOTE is load-bearing and was read for the first time in the five-hundred-and-seventy-fifth session** — \"Rectangles can have a width of zero or height of zero\" — because it says which questions this clause does *not* answer. A zero-extent array is a rectangle here and is exactly right for §12.5.6.4's text annotation, which is \"attached to a point\"; what it is not is Table 31's medium, and reading the permission as though it settled every consumer is how a `/MediaBox [0 0 0 0]` reached the rasteriser and cost a document its only page. A rectangle's *shape* is this clause's; whether that shape can serve a given entry is the entry's own clause. ADR 0410."

[[clause]]
clause = "7.9.6"
title = "Name trees"
status = "implemented"
code = ["crates/pdf-syntax/src/tree.rs"]
test = ["crates/pdf-syntax/src/tree.rs::a_key_is_found_through_intermediate_nodes"]
note = "Name trees: a root with `/Names` or `/Kids`, intermediate and leaf nodes with `/Limits`, keys that are *strings* and are ordered. `tree.rs` answers this clause and §7.9.7 with one walk, because the two differ only in the key type and in whether the pairs are called `/Names` or `/Nums`. **The `/Limits` are read as a hint rather than as a gate**: a node whose limits exclude the key is skipped and a node with none is searched, so a producer that got them wrong loses speed rather than entries. The clause's own reason for the structure is `CLAUDE.md` principle 2 written by ISO — the pairs \"can be looked up efficiently without requiring the entire data structure to be read from the PDF file\" — which is why the lookup descends rather than flattening. **The keys are strings and the rule is still the bytes'**, which the six-hundred-and-fourth session checked rather than assumed: \"Any encoding of the keys may be used as long as it is self-consistent; keys shall be compared for equality on a simple byte-by-byte basis.\" No clause states a case fold or a normalisation for either vocabulary, so a name tree and a name dictionary settle equality the same way through different types — `TreeKey::Name` carries `&[u8]` and `Destination::named` probes `/Dests` with `get_by_name`. ADR 0439."

[[clause]]
clause = "7.9.7"
title = "Number trees"
status = "implemented"
code = ["crates/pdf-syntax/src/tree.rs"]
test = ["crates/pdf-syntax/src/tree.rs::a_number_trees_pairs_come_back_in_order"]
note = "Number trees, which §7.9.7 defines by difference: \"similar to a name tree … except that its keys shall be integers instead of strings and shall be sorted in ascending numerical order\", with `/Nums` in place of `/Names`. `TreeKey` is that difference and nothing else. Two entry points, for two shapes of question: `lookup` descends for one key, and `number_pairs` walks the whole tree — which §12.4.2 needs, because a labelling range runs from its own key to the *next* one and no single lookup produces a neighbour."

[[clause]]
clause = "7.10"
title = "Functions"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::every_width_table_39_lists_is_read_and_no_other", "crates/pdf-model/tests/shadings.rs"]
note = "All four function types, which this tree needs more than most readers do: §8.7's shadings evaluate one per sample, §8.6.6's `Separation` and `DeviceN` tint transforms are functions, and §10.5's transfer functions and §11.5's `/TR` are too. **1501 of 1501 functions in the corpus parse**, which the corpus gate asserts by name. The clause's own framing is what makes them cacheable: \"[f]unctions in PDF represent static, self-contained numerical transformations\" — no state, so `Ramp` can sample one 256 times and keep the answers."

[[clause]]
clause = "7.10.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::an_exponential_function_interpolates_between_its_endpoints", "crates/pdf-model/tests/shadings.rs"]
note = "The framing, `/Domain` and `/Range`, and the clipping rule they carry: an input outside `/Domain` is clipped to it before evaluation and an output outside `/Range` is clipped after. Both are applied to every type here, which is where a first implementation loses a shading's end colours."

[[clause]]
clause = "7.10.2"
title = "Type 0 (sampled) functions"
status = "partial"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::every_width_table_39_lists_is_read_and_no_other", "crates/pdf-model/tests/shadings.rs", "crates/pdf-model/tests/sampled_function_extent.rs"]
note = "Type 0, sampled: `/Size`, `/BitsPerSample`, `/Encode`, `/Decode` and the samples themselves. **This row said `implemented` and named a whole test file, and auditing that population in the hundred-and-eighth session found two things wrong with it.** It said \"all five of Table 39's sample widths\"; the table says \"[v]alid values shall be 1 , 2 , 4 , 8 , 12 , 16 , 24 , and 32\", which is **eight**, and the code reads all eight — now checked by a test that encodes two samples at each width and reads both ends back. And Table 39's `/Order` is **not read at all**: \"[v]alid values shall be 1 and 3, specifying linear and cubic spline interpolation\", and this tree interpolates multilinearly whatever the entry says. That is a departure rather than a refusal, because the clause names a *cubic spline* and states no spline — refusing would lose a valid document over an entry whose algorithm the standard does not give. Measured over the corpus: **956 type-0 functions in 26 documents, 955 at 8 bits and 1 at 16, and not one states `/Order 3`** — so the departure is invisible in every file we have, which is why it is recorded here rather than reported per page. **Read and kept in the five-hundred-and-tenth session**: a grep for a \"Order\" reader still finds none, `function.rs` still interpolates multilinearly, and the four-hundred-and-ninety-sixth's grid rework (ADR 0339) moved where a shading *samples* a function, not how the function interpolates. **And one sentence of it had no reader until the five-hundred-and-twenty-first**: \"The stream data shall be long enough to contain the entire sample array, as indicated by Size , Range , and BitsPerSample ; see 7.3.8.2, \"Stream extent\".\" A stream shorter than that used to have its missing samples read back as **0** — a value nobody wrote, mapped through `/Decode` and interpolated into the samples beside it, so a tint transform or a shading was evaluated over a function the file never carried. `holds_the_sample_array` refuses one, which is trap 5's substitutive test rather than §7.8.2's prefix rule (ADR 0343): a short sample table does not draw part of a gradient, it draws a different one. **No corpus reaches it** — 0 of 65 944 crawled documents, 0 of the 974 and 0 of `format-corpus`'s 167 — so it is pinned by a hand-built pair. ADR 0356."

[[clause]]
clause = "7.10.3"
title = "Type 2 (exponential interpolation) functions"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::an_exponential_function_interpolates_between_its_endpoints", "crates/pdf-model/tests/shadings.rs"]
note = "Type 2, exponential interpolation: `/C0`, `/C1` and `/N`, which is the two-colour ramp most axial and radial shadings are made of. One line of arithmetic and the most frequently evaluated function in the corpus."

[[clause]]
clause = "7.10.4"
title = "Type 3 (stitching) functions"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::a_nested_stitching_functions_steps_map_to_the_outer_domain", "crates/pdf-model/tests/shadings.rs", "crates/pdf-model/tests/hostile_functions.rs"]
note = "Type 3, stitching: `/Functions`, `/Bounds` and `/Encode`, a one-input function assembled from a row of others. The subclause's own edge — a subdomain of zero width, where two bounds coincide — is what a naive implementation divides by. **Table 41 bounds k nowhere** and the only value it singles out is the small one, \"The value of k may be 1\"; until the four-hundred-and-twenty-fifth session this tree bounded k by a constant documented as bounding a function's *dimensionality*, which the same subclause fixes at one here — \"Domain shall be of size 2 (that is, 𝑚  =  1 )\". A 256-stop gradient is k = 255, and one SafeDocs document lost four shadings to it. The bound is now a budget over the whole tree one root builds, and a `/Functions` array naming its own object — which nothing in the standard forbids — is refused by depth rather than followed until the stack is gone."

[[clause]]
clause = "7.10.5"
title = "Type 4 (PostScript calculator) functions"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::arithmetic_follows_postscript_stack_order", "crates/pdf-model/src/function.rs::folding_agrees_with_the_evaluator_over_every_operator", "crates/pdf-model/src/function.rs::the_witness_pages_program_keeps_no_inexact_operator", "crates/pdf-model/tests/shadings.rs"]
note = "Type 4, the PostScript calculator: a small language, evaluated here rather than approximated. It is what `Separation` and `DeviceN` tint transforms are usually written in, and `postscript_type4_many_outputs.pdf` is the corpus's controlled experiment on it (see `oracle.rs`'s `CONTRADICTED_DEVICE_CMYK_CONVERSION`, which used it to prove something about `DeviceCMYK` instead). The subclause's own lexical requirement — a comment, which §7.2.4 ends at a line — is §7.10.5.1's row, and was the *tokenizer's* one defect in this clause family (ADR 0361). **The evaluator had two of its own, and neither is a clause this row can point at**: they are §7.10.5.2's deferral to the PostScript Language Reference, which that row now carries in full. **A third was the operand stack's own type**: §7.10.5.1 gives the language integers, reals and booleans and this evaluator had one type for all three, which §7.10.5.2's row now carries with what it cost (ADR 0371). Two censuses of the whole population — `cargo run --release -p pdf-model --example type4_operator_census` and `--example type4_type_census` — are what say which of Table 42 a real file ever reaches and what a change to an operator's meaning actually moves, and they are the instrument to run before believing any claim about the cost of an operator's semantics here.\n\n**A compiled program is now folded before it is stored** (ADR 0406, session 572). §8.7.4.5.2 evaluates one of these once per device pixel, so a subexpression built only from literals is recomputed a million times a frame for an answer that never changes — and `pi_seven_segment.pdf`'s whole BBP series for π is exactly that. `fold_constants` replaces a *closed island* — a run that begins with a literal push, holds no jump and no jump target, never reads below the depth it began at, and ends as one value — with the value `evaluate_postscript` itself computes for it, on a stack holding only the run's own values, so the answer is the same in every bit by construction. It changes nothing this clause states; what it changes is which clause the *device* can be handed, and §8.7.4.5.2's row carries that half."

[[clause]]
clause = "7.10.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::a_program_with_an_unknown_operator_is_refused", "crates/pdf-model/src/function.rs::a_comments_words_are_not_instructions", "crates/pdf-model/tests/shadings.rs::a_type_4_program_paints_the_same_page_with_and_without_its_comments"]
note = "The framing: a program in braces, with the input on the stack and the output taken from the top. **And the list of what the subset contains, which nobody had read against the tokenizer**: \"This subset is comprised of the following PostScript language features\" — expressions over numbers and booleans, **comments**, no composite data structures, no procedures, no variables or names. Comments are admitted by that list and were mishandled until the five-hundred-and-twenty-sixth session, when the project owner's `doc/corpora-own/type4_pi.pdf` refused whole; the same list is what makes cutting each line at its first PERCENT SIGN correct rather than merely convenient, since a language with no strings has no other place to put one. ADR 0361, whose census — `cargo run --release -p pdf-model --example type4_comment_census` — found how rarely a *producer* comments a program, and therefore that this fell almost entirely on files written by hand."

[[clause]]
clause = "7.10.5.2"
title = "Operators and operands"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::stack_operators_move_what_they_say", "crates/pdf-model/src/function.rs::round_takes_a_tie_to_the_greater_integer", "crates/pdf-model/src/function.rs::equality_is_exact_rather_than_approximate", "crates/pdf-model/src/function.rs::the_four_operators_that_drop_a_fraction_disagree_only_on_a_tie", "crates/pdf-model/src/function.rs::atan_answers_in_degrees_over_the_whole_circle", "crates/pdf-model/src/function.rs::integer_division_truncates_toward_zero_and_its_remainder_follows_the_dividend", "crates/pdf-model/src/function.rs::the_boolean_and_bitwise_operators_are_the_same_operator_on_zero_and_one", "crates/pdf-model/src/function.rs::not_is_the_operator_its_operands_type_selects", "crates/pdf-model/src/function.rs::a_boolean_is_never_equal_to_a_number_and_a_number_never_to_a_boolean", "crates/pdf-model/src/function.rs::the_bitwise_and_logical_operators_differ_in_the_type_of_their_answer", "crates/pdf-model/src/function.rs::integer_arithmetic_stays_integer_and_is_exact_where_a_float_is_not", "crates/pdf-model/src/function.rs::an_ordering_operator_reads_a_boolean_as_a_number_rather_than_refusing_it", "crates/pdf-model/src/function.rs::an_integer_operator_truncates_a_real_operand", "crates/pdf-model/src/function.rs::a_conditions_number_is_true_exactly_when_it_is_not_zero", "crates/pdf-model/src/function.rs::a_bit_shift_moves_left_for_a_positive_count_and_keeps_its_sign_going_right", "crates/pdf-model/tests/shadings.rs", "crates/pdf-model/src/function.rs::a_program_whose_comparison_provably_reads_a_boolean_is_refused", "crates/pdf-model/src/function.rs::a_conforming_program_is_not_refused", "crates/pdf-model/src/function.rs::a_refused_program_is_a_function_error_rather_than_a_value"]
note = "Table 42's operators, which is the whole language: arithmetic, the relational and boolean set, `if` and `ifelse`, and the stack operators `dup`, `exch`, `pop`, `copy`, `index` and `roll`. The clause bounds the stack at 100 entries, which is a bound this tree applies rather than a suggestion — a program that pushes without limit is a decompression bomb written in another notation.\n\n**And this clause states no semantics at all — it defers them**, which nothing in this tree had written down until the five-hundred-and-thirty-fourth session: \"The PostScript Language Reference, Third Edition shall define the semantics of these operators and all other syntax rules of the PostScript language.\" That deferral is normative and **this project does not hold the document deferred to**, so principle 5's rule for a clause that defines nothing applies to a clause that defines something elsewhere: each operator the deferral decides is a documented *choice* rather than a derivation, and no line here quotes PLRM3. What the standard does state is Annex B, which is informative and gives each operator one line — enough to settle `atan`'s two operands, `idiv`'s and `mod`'s truncation, and `and`/`or`/`xor`'s two readings, and not enough to settle a tie or a shift width.\n\nTwo arms were wrong against the deferral and were found by the quorra team reading the file rather than by any gate: `round` took a tie away from zero where PostScript takes it to the greater integer, and `eq`/`ne` compared within `f32::EPSILON` where the operator is a relation — a tolerance that made every value under 1.2e-7 equal to zero and did nothing at all above 8.4 million. Both are fixed and ADR 0369 is the argument. **The operand stack carries §7.10.5.1's three types since the five-hundred-and-thirty-sixth session, and that is what makes Annex B's operand and result columns implementable rather than decorative** (ADR 0371). It held `f32`, so a boolean was the number 1: §B.3 types `eq` and `ne` `any 1 any 2` — every object, so equality has to be decided *across* the types — and `true 1 eq` therefore answered true, which is a colour decided by a type confusion. The quorra team found it in their own device-side evaluator by running this tree's corpus against it and reported that ours had the same shape. The same typing settles `not`, which was the one entry in this row listed as a known incompleteness rather than a reading: §B.3's `bool 1 | int 1 … bool 2 | int 2` is two operators, `63 not` is `-64`, and nothing had to be inferred to get there because §7.3.2 and §7.3.3 give the token `63` its type in the file. `cvi` and `cvr` became conversions, `truncate` kept its operand's type, and `and`/`or`/`xor` answer in the type they were given.\n\nThree questions remain *choices* and say so where they are made. `bitshift`'s width on a right shift of a negative value — where a shift wider than the register now leaves the sign repeated, since answering zero would have made the answer depend on the width this row says it does not. An error the subset cannot express (`div`, `idiv`, `mod` by zero; `ln`, `log`, `sqrt` outside their domains) and, the same shape one step over, **an operand of a type its operator's line does not admit**: `Value`'s policy converts by the reading that loses least rather than inventing a refusal — a number is false exactly when it is zero under `if`, a real is truncated under `idiv`, a boolean is 1 or 0 where a `num` is wanted — and §7.3.3's \"[w]herever a real number is expected, an integer may be used instead\" is what makes one of those three directions not a choice at all. And a pop from an empty operand stack, which is the integer zero.\n\n**The population is measured rather than assumed**, by two censuses. `type4_operator_census` over 67 462 files found 7 353 type 4 functions, of which none reach `round`, `ne`, `bitshift` or `not`. `type4_type_census` — the typing's own, which rewrites each program into the untyped semantics and compares both arms over a grid of the file's `/Domain` — found 7 360 in 2 102 documents, compared every one of them, and **moved none**: exactly one program in the population can put a boolean where an `eq` will see it, and it is the project owner's hand-written `pi_seven_segment.pdf`.\n\n**One of those three was not a choice, and this row called it one for forty-one sessions** (ADR 0412, session 577). `ge`, `gt`, `lt` and `le` are typed `num 1 num 2` by §B.3, and this clause's own deferral makes that a requirement rather than a summary: the document it defers to is listed in clause 2, whose preamble says those documents are \"referred to in the text in such a way that some or all of their content constitutes requirements of this document\", and there a boolean under one of the four raises `typecheck`. So the operand has no reading that loses least — it has no reading — and converting it substitutes for the branch the producer wrote rather than adding a mark, which is trap 5's own test and the same one that refuses a §7.10.2 sample array that is short. `Function::parse_postscript` now refuses a program in which a boolean **provably** reaches one of the four, and every caller of `Function::parse` already reports that refusal; `ordering_reaches_a_boolean` is `fold_constants`' own forward walk over types instead of values, once per parse and nothing per device pixel, and it **under-approximates on purpose** — a program it cannot decide is admitted, and there the conversion above still answers, because an evaluator cannot raise §7.10.5's error at a pixel. The population was measured before the fix rather than after: `type4_type_census` over 1 251 documents finds 44 type 4 functions, 2 of them *containing* both an ordering operator and a source of a boolean, and **none** in which the walk can prove one reaches the other — both containment witnesses are the owner's own hand-written files, and reading their programs says why the looser count is not the population. `eq` and `ne` are untouched: §B.3 types them `any 1 any 2`, so the clause admits a boolean there by its own word."

[[clause]]
clause = "7.10.5.3"
title = "Type 4 function dictionary"
status = "implemented"
code = ["crates/pdf-model/src/function.rs"]
test = ["crates/pdf-model/src/function.rs::a_tint_transform_shaped_program_evaluates", "crates/pdf-model/src/function.rs::an_empty_operand_stack_reads_as_the_integer_zero", "crates/pdf-model/src/function.rs::a_program_that_computes_no_real_number_still_answers_within_its_range", "crates/pdf-model/tests/shadings.rs"]
note = "The type 4 function's own dictionary, which is a stream: `/Domain`, `/Range` and the program as the stream's data. `/Range` is *required* here and is not for every type, and it is what says how many of the stack's values are outputs — which `postscript_type4_many_outputs.pdf` is named after, since its program leaves more values than its `/Range` takes. **The clause also types both ends of the stack, and until the five-hundred-and-thirty-sixth session there was nothing that could read it that way**: \"The input variables shall constitute the initial operand stack\" — they are reals — and it \"shall be an error … for any of them to be objects other than numbers\" for what is left. A boolean left behind is that error, and the subset has no value that means one, so it is read as the 1 or 0 it stands for rather than dropped; dropping it would break the other half of the same sentence, which is about how many outputs there are. ADR 0371."

[[clause]]
clause = "7.11"
title = "File specifications"
status = "implemented"
code = ["crates/pdf-model/src/file_spec.rs", "crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/file_spec.rs::a_relative_specification_replaces_the_containing_files_name", "crates/pdf-model/src/file_spec.rs::the_unicode_name_is_used_instead_of_the_byte_string"]
note = "Aggregate of the ten below. **A file specification is now read whole and opened never** (hundred-and-fourteenth session): `file_spec.rs` is §7.11.1's two forms, §7.11.2's string format, Table 43 and §7.11.5's URL form, and the sentence the family turns on is still §7.11.1's — a file specification refers to a file \"external to the PDF file\" *in either case*, embedded or not. `CLAUDE.md` principle 3 gives the renderer no filesystem and no network, so *following* one stays refused by architecture; what this row used to record as unread was the difference between refusing a file and being unable to name it. Every path in this tree that meets a specification now names it out of one reader rather than three: §7.3.8's external stream data, §8.10.4's reference XObject drawn as its proxy, §12.5.6.15's file attachment icon, §12.6.4.4's embedded go-to and §12.7.6.4's import."

[[clause]]
clause = "7.11.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/file_spec.rs"]
test = ["crates/pdf-model/src/file_spec.rs::the_unicode_name_is_used_instead_of_the_byte_string"]
note = "The two forms — a simple specification as a string or a dictionary, a full one as a dictionary only — read by `FileSpec::parse`, which answers `None` for an object that is neither. The sentence this family turns on is \"[t]he file is considered external to the PDF file in either case\", so an *embedded* file is still an external file with its bytes carried along, which is why reading one is an attachment feature and never a rendering one. Refusals, not silence: see §7.11's row for the places this tree meets a specification and names it."

[[clause]]
clause = "7.11.2"
title = "File specification strings"
status = "implemented"
code = ["crates/pdf-model/src/file_spec.rs"]
test = ["crates/pdf-model/src/file_spec.rs::a_escaped_solidus_is_part_of_a_component_rather_than_a_separator", "crates/pdf-model/src/file_spec.rs::an_absolute_specification_starts_at_the_root"]
note = "Aggregate of the two below: how a simple file specification's string spells a path, in `FileSpec::components`, `is_absolute` and `resolve_against`."

[[clause]]
clause = "7.11.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/file_spec.rs"]
test = ["crates/pdf-model/src/file_spec.rs::a_escaped_solidus_is_part_of_a_component_rather_than_a_separator", "crates/pdf-model/src/file_spec.rs::an_empty_component_is_a_component"]
note = "The string form: SOLIDUS as \"a generic component separator\", a literal SOLIDUS escaped by REVERSE SOLIDUS, empty components kept because \"[a]ny of the components may be empty\", and the clause's own EXAMPLE `(in\\/out)` as the test. **The closing sentence is what decides the type**: \"[t]he component substrings shall be stored as bytes and shall be passed to the operating system without interpretation or conversion of any sort\", so `components` is `Vec<Vec<u8>>` and stays that way. `FileSpec::display_name` is the one place a `/F` becomes text, which is a *display* decision made where it can be seen — three call sites in `pdf-model` used to make it silently, and this row warned against exactly that before anybody read it."

[[clause]]
clause = "7.11.2.2"
title = "Absolute and relative file specifications"
status = "implemented"
code = ["crates/pdf-model/src/file_spec.rs"]
test = ["crates/pdf-model/src/file_spec.rs::a_relative_specification_replaces_the_containing_files_name", "crates/pdf-model/src/file_spec.rs::two_periods_cancel_the_component_before_them", "crates/pdf-model/src/file_spec.rs::a_relative_url_specification_may_be_a_path_and_nothing_else"]
note = "Absolute and relative specifications, and the clause states its answers rather than only its rules — both of its EXAMPLEs are tests. `resolve_against` removes the containing file's own name and appends, and cancels a `..` against the component before it \"when the component immediately preceding .. is not another . .\"; a `..` with nothing to cancel is *kept*, a documented choice, because dropping it would silently move the target to the root. The URL half is `crate::uri`'s RFC 3986 rather than this module's, and what is stated here is the restriction on top of it: a URL-based relative specification \"shall be limited to paths\", with \"[t]he scheme, network location/login, fragment identifier, query information, and parameter sections\" not allowed — **a security rule wearing a syntax rule's clothes**, checked by `is_valid_relative_url` rather than assumed."

[[clause]]
clause = "7.11.3"
title = "File specification dictionaries"
status = "partial"
code = ["crates/pdf-model/src/file_spec.rs", "crates/pdf-model/src/attachment.rs", "crates/pdf-model/src/collection.rs"]
test = ["crates/pdf-model/src/file_spec.rs::the_unicode_name_is_used_instead_of_the_byte_string"]
note = "Table 43, read across three modules that each own what they point at: `file_spec.rs` reads `/FS`, `/F`, `/UF`, the three deprecated platform keys, `/ID`, `/V`, `/Desc` and the *presence* of `/EF`; `attachment.rs` reads `/EF`, `/RF` and `/AFRelationship`; `collection.rs` reads `/CI`. The requirement a first implementation inverts is honoured and tested — \"[a] PDF reader shall use the value of the UF key, when present, instead of the F key\" — and it matters because `/UF` is a *text string* by §7.9.2.2 and `/F` a byte string, so the two are not interchangeable. `partial` for two entries nothing consumes: `/Thumb`, which §12.3.4's thumbnail reader could decode for a specification but is only defined for a *page*, and `/EP`, §7.6.7's encrypted payload, which needs a cryptographic filter this tree refuses by name. **Read and kept in the five-hundred-and-tenth session**: a grep for a \"EP\" reader finds none, and the one `/Thumb` reader outside a page's is `collection.rs`'s folder flag - Table 159's entry, not Table 43's."

[[clause]]
clause = "7.11.4"
title = "Embedded file streams"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs", "crates/pdf-syntax/src/tree.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_embedded_file_is_read_with_its_parameters", "crates/pdf-model/src/attachment.rs::a_stated_checksum_is_answered_against_the_bytes", "crates/viewer-core/tests/headless.rs::an_embedded_file_comes_out_of_the_document", "crates/viewer-ui/tests/panel.rs::the_file_tab_names_what_is_embedded_and_says_when_nothing_is"]
note = "Embedded file streams, which are the one part of §7.11 that needs no filesystem: the bytes are inside the document. `attachment::attachments` lists what the `/EmbeddedFiles` tree names — the tree's key, Table 43's `/UF` or `/F` and `/Desc`, Table 44's `/Subtype`, and Table 45's `/Size`, dates and `/CheckSum` — and §7.11.4.2's `/RF` is read too, in `attachment::related`. **This row said `/RF` was unread for as long as it has existed and §7.11.4.2's own row said `implemented`**; the second was right. **The list reached a person in the hundred-and-sixty-seventh session** and the *bytes* in the hundred-and-sixty-ninth: `Command::Extract` names a file by its tree key, undoes §7.4's filters, and hands the file to the host to write — rule 2, the same division `Command::Save` makes. Table 45's `/CheckSum` is checked *there*, which is the only place in the program holding the decoded bytes the clause's definition is about, and a mismatch is said and not acted on because the clause calls it \"strictly a checksum, and … not used for security purposes\". 10 of the 974 corpus documents carry such a tree, 23 files between them; two of the 23 are in encrypted documents and refuse to decode, which §7.6.6 makes the stream's answer rather than the file's — and the refusal is now reported by name rather than being a list nobody could act on."

[[clause]]
clause = "7.11.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs", "crates/pdf-syntax/src/tree.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_embedded_file_is_read_with_its_parameters", "crates/pdf-model/src/attachment.rs::a_stated_checksum_is_answered_against_the_bytes"]
note = "Both of the clause's routes are readable and one is walked: the name dictionary's `/EmbeddedFiles` tree for the document as a whole, which `attachment::attachments` lists, and a file specification's own `/EF`, which `attachment::read` reads for whoever holds the specification. **The caller the clause names is built since the four-hundred-and-sixtieth session** — this sentence ended '§12.5.6.15's file attachment annotations being the caller the clause names, and not yet built', and while it stood the required `/FS` of every file attachment annotation was read by nothing but §12.6.4.4's embedded go-to, so a document that attached its file to a *page* rather than to the name tree carried a file no part of this program could reach. `attachment::of_annotation` is that caller and a click on the annotation is what asks it (ADR 0295). The two routes stay two lists on purpose: the tree is a document-wide list a panel shows at launch, and the annotations are a page's, which is why `Query::Attachments` is not the union — see §12.5.6.15. Table 44 and Table 45 are read in full. **The `/CheckSum` is verified since the hundred-and-sixty-ninth session**, and where: the clause's subject is \"the bytes of the uncompressed embedded file\", so the question could not be asked until something decoded a stream, and `Command::Extract` is the one path that does — the old reason here, that checking would mean inflating every attachment, was true of a *list* and expired the moment extraction existed. `/Size` stays the document's claim rather than a measurement, and the extraction test is where the two are compared. **And the rule got a second caller in the three-hundred-and-eighty-sixth session**, which is where it stopped being answerable only by whoever holds the stream: `attachment::checksum_matches` is now a free function beside the method, because `viewer_confined::Attachment` lists an embedded file *without* its stream — a panel drawing five rows would otherwise pull five payloads across a pipe — so a confined host holds Table 45's checksum in one message and the bytes in another. One implementation, two callers, and `tests/confined.rs` asks it end to end: list, extract, compare. ADR 0223."

[[clause]]
clause = "7.11.4.2"
title = "Related files arrays"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::a_related_files_array_pairs_names_with_streams"]
note = "§7.11.4.2's related files array, in `attachment::related`. The shape is the part worth getting right: `/RF` is a dictionary keyed like `/EF` — `/F`, `/UF` and the platform keys — and each value is a *flat* array of `2 × n` elements alternating a name with an embedded file stream, not an array of pairs. The clause's own EXAMPLE is a DCS 1.0 set of five files behind one specification, and it is the test. Nothing extracts them, and the reason recorded here — \"writing a file is what the sandbox exists to prevent\" — was wrong: the host writes, not the renderer, which is what `Command::Extract` now does for `/EmbeddedFiles`. The true reason is that nothing asks: a related-files set belongs to whoever holds the specification, and no corpus file specification carries an `/RF`. No corpus file specification carries a `/RF`, measured over every object of all 974 documents."

[[clause]]
clause = "7.11.5"
title = "URL specifications"
status = "implemented"
code = ["crates/pdf-model/src/file_spec.rs"]
test = ["crates/pdf-model/src/file_spec.rs::a_url_specification_is_not_split_into_components"]
note = "`/FS /URL` makes `/F` a uniform resource locator rather than a path, which `FileSpec::url` returns and `FileSpec::components` refuses to split — a URL is not a path and applying §7.11.2.1's separator rule to one would invent components out of its scheme. **Reading it is not fetching it**: principle 3 gives the renderer no network, so a document asking this program to fetch a URL still gets nothing, and that is the intended answer rather than a missing feature — the same position §12.6.4.8's URI action takes, which resolves and prints rather than opens. The clause's own encoding note is small and exact — 7-bit US-ASCII is a strict subset of PDFDocEncoding, so a URL is already a text string under §7.9.2.2's third encoding."

[[clause]]
clause = "7.11.6"
title = "Collection items"
status = "implemented"
code = ["crates/pdf-model/src/collection.rs"]
test = ["crates/pdf-model/src/collection.rs::a_collection_subitem_keeps_its_prefix_apart_from_its_data", "crates/pdf-model/src/collection.rs::the_clauses_own_email_inbox_reads_as_five_columns"]
note = "Table 46's collection item and Table 47's subitem, in `collection::item`, read from a `/CI` on a file specification or on a folder. The dictionary has no fixed entries — every key is one the writer chose and the schema names — so this is a map, and the *values* are kept as the objects the file wrote rather than as strings: \"[t]he type of each entry shall match the type of data identified by the collection field dictionary … referenced by the same key\", so the type is the schema's to say and a reader guessing from the object would disagree with it. A subitem's `/P` prefix is kept apart from its `/D` data because Table 47 makes them behave differently — the prefix is shown and \"is ignored when an interactive PDF processor sorts the items in the collection\". The clause's own EXAMPLE 2 is the test."

[[clause]]
clause = "7.12"
title = "Extensions dictionary"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "The catalog's `/Extensions`: which developer extensions a document was written against. Read into `requirements::Extensions`, keyed by the registered prefix name the clause makes the key, with §7.12.3's whole dictionary under it. Nothing *acts* on it, and nothing should: an extension level is a statement about which developer's additions a file uses, and this reader implements the standard rather than anybody's additions. 9 of the 974 corpus documents state one, all `/ADBE` at level 3 or 8 over a base version of 1.7."

[[clause]]
clause = "7.12.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "The framing, and the two rules that decide how it is read: the keys other than `/Type` \"shall be names consisting of registered prefix names\", and the whole structure \"shall be direct objects\". The first is why `Extensions::by_prefix` is keyed by name; the second is a rule a file may break, so every part is resolved anyway — surviving it costs a reader nothing."

[[clause]]
clause = "7.12.2"
title = "Extensions dictionary"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "Table 48: `/Type`, and one entry per registered prefix whose value is a developer extensions dictionary **or, since PDF 2.0, an array of them**. Both forms are read; the array is why one prefix maps to a list."

[[clause]]
clause = "7.12.3"
title = "Developer extensions dictionary"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "Table 49: `/BaseVersion` and `/ExtensionLevel`, both required and both read, with PDF 2.0's `/URL` and `/ExtensionRevision`. **Errata Collection 3 makes `/URL` required** (Issue #732, `/State` `Review` `Accepted`), and this row called it optional until the four-hundred-and-seventeenth session read the markup `doc/md/` drops (ADR 0253). `DeveloperExtension::read` still answers an extension that states none, deliberately and with the reason beside it: the two entries it does refuse on are the extension's identity, and the URL is not. A dictionary missing either required entry names no extension and is dropped rather than half-read — inventing a level would claim to know which of a developer's extensions this is."

[[clause]]
clause = "7.12.4"
title = "BaseVersion"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "`/BaseVersion` is \"the name of the PDF version to which this extension applies\", kept as the name the file wrote. The clause's NOTE is the reason: \"[t]he value of BaseVersion is not to be interpreted as a real number but as two integers with a PERIOD (2Eh) between them\" — a reader that parsed it as a number would make 1.10 smaller than 1.7. The rule that it be no greater than the file's own version is a *writer's*, and this reader does not validate files."

[[clause]]
clause = "7.12.5"
title = "ExtensionLevel"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "`/ExtensionLevel`, \"an integer defined by the developer to denote the extension being used\", read as one. Its ordering rule — that a developer's levels against one base version increase over time — is a statement about a developer's practice that a reader has nothing to check against."

[[clause]]
clause = "7.12.6"
title = "URL"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "`/URL`, \"a URL that refers to the documentation for this extension\", read as the string it is and fetched by nobody: this reader has no network (ADR 0014), and the entry documents an extension to a human rather than to a program. §12.6.4.8's resolution is not applied to it either — that clause is about a URI *action*, and this is a citation."

[[clause]]
clause = "8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/render_real_pdf.rs::every_specification_pdf_interprets"]
note = "Introductory to the imaging model. Its division — that clause 8 says what is painted and clause 10 how it reaches pixels — is the boundary between `pdf-render`'s display list and the two rasterisers."

[[clause]]
clause = "8.2"
title = "Graphics objects"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_move_overrides_the_move_before_it"]
note = "Table 32's five graphics objects and the state machine between them. All five are interpreted — path, text, external, inline image, shading — and the clause's rule that a path object is built and then painted by one operator is what `end_path` is. \"A content stream that is not a valid graphics object sequence\" is recovered from rather than refused, which the clause does not forbid and trap 5's rule requires be visible: an unbalanced `BT` is reported by name."

[[clause]]
clause = "8.3"
title = "Coordinate systems"
status = "implemented"
code = ["crates/pdf-model/src/page.rs", "crates/pdf-render/src/lib.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_user_unit_scales_the_page_and_its_contents"]
note = "See the subclauses. The one decision this tree makes about coordinate spaces is that the display list is stated in *page* space and the device transform is applied by the backend, which is what makes one list rasterisable at any scale."

[[clause]]
clause = "8.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-render/src/lib.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_user_unit_scales_the_page_and_its_contents"]
note = "Introductory. Its claim that PDF's coordinate systems are all related by matrices is why `Transform` is the only geometry type that crosses a crate boundary here."

[[clause]]
clause = "8.3.2"
title = "Coordinate spaces"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_page_stated_in_inches_is_the_size_those_inches_are"]
note = "See §8.3.2.2 through §8.3.2.5."

[[clause]]
clause = "8.3.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_page_stated_in_inches_is_the_size_those_inches_are"]
note = "Names the spaces the subclauses below define."

[[clause]]
clause = "8.3.2.2"
title = "Device space"
status = "implemented"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/lib.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_the_basic_scene"]
note = "This device's space: y down, square pixels, origin at the top left, resolution decided by the `TargetSpec` the caller asks for. The clause's warning — that device spaces differ in origin, axis direction and resolution — is the reason the display list carries none of them and the backends carry all of them."

[[clause]]
clause = "8.3.2.3"
title = "User space"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::a_page_stated_in_inches_is_the_size_those_inches_are", "crates/pdf-model/tests/page_geometry.rs::a_user_unit_on_an_ancestor_is_not_inherited"]
note = "Every sentence of it is in `page.rs`: \"the CropBox entry in the page dictionary shall specify the rectangle of user space corresponding to the visible area\", the positive axes right and up \"subject to alteration by the Rotate entry\", and \"the length of a unit along both the x and y axes is set by the UserUnit entry\" with a default of one seventy-second of an inch (ADR 0038)."

[[clause]]
clause = "8.3.2.4"
title = "Other coordinate spaces"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints"]
note = "The spaces defined relative to user space: glyph space (§9.2.4), text space (§9.4.4), image space, form space (§8.10) and pattern space (§8.7.3). All five are implemented, and the one that is easy to get wrong is pattern space — \"the pattern matrix maps pattern space to the default (initial) coordinate space of the page\", not to the space in force at the `scn`, which is why `Interpreter::base` exists."

[[clause]]
clause = "8.3.2.5"
title = "Relationships among coordinate spaces"
status = "implemented"
code = ["crates/pdf-render/src/lib.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_the_basic_scene"]
note = "The chain from glyph or image space through user space to device space, composed by matrix multiplication. Trap 2 is this subclause read the hard way: a paint is positioned in the space the *path* is stated in, so composing the page-to-device transform into a gradient's own matrix applies it twice."

[[clause]]
clause = "8.3.3"
title = "Common transformations"
status = "implemented"
code = ["crates/pdf-render/src/lib.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_diagonal_stroke"]
note = "Translation, scaling, rotation and skew, all four expressible by `Transform` and all four reached by the `cm` operator. Nothing constructs one from an angle here — a content stream states the six numbers — so the clause's formulas are read rather than implemented, and its NOTE 1 about the order mattering is the reason `Transform::then` is named for the order it applies in."

[[clause]]
clause = "8.3.4"
title = "Transformation matrices"
status = "implemented"
code = ["crates/pdf-render/src/lib.rs"]
test = ["crates/pdf-render/src/geom.rs::a_shear_stretches_more_than_its_determinant_says"]
note = "The six-number matrix and its arithmetic, `Transform`. One derived quantity is this crate's own and is worth naming here: `Transform::max_stretch` is the largest factor a matrix can scale a length by, which is **not** `determinant().abs().sqrt()` — a shear separates the singular values without changing the determinant — and it is what decides a stroke's device width (§8.4.3.2)."

[[clause]]
clause = "8.4"
title = "Graphics state"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_initial_values_are_table_51s"]
note = "The parent row for the graphics state, reviewed with §8.4.1 to §8.4.5 in the twenty-fourth session. Every parameter Table 51 lists is carried in `GraphicsState` and every one is saved and restored by `q`/`Q`. Table 52's device-dependent set is where this device declines: black generation, undercolour removal and halftones are §10.4's and §10.6's, which a screen does not perform, flatness is ignored by §10.7.2's own permission, smoothness is the one silence inside a partial row (§10.7.3), and overprint is answered by §8.6.7 rather than skipped. See those rows."

[[clause]]
clause = "8.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_initial_values_are_table_51s", "crates/pdf-model/tests/line_parameters.rs::a_miter_limit_below_one_is_clipped_into_range"]
note = "Table 51's initial values are `GraphicsState`'s and `Stroke`'s defaults, and are asserted rather than assumed. The clause's two general rules about parameter handling are both applied: a value must \"be of the correct type or have values that fall within a certain range\", which is why a line cap code outside Table 53's three is the initial value rather than a guess; and numeric values \"shall be clipped into valid range\" — the line width at 0, the miter limit at 1, the alpha constants at 0..1. The clause's own caution that a clipped value \"shall not be adjusted to reflect capabilities of the raster output device\" is why §8.4.3.2's one-device-pixel minimum lives in `Stroke::device_width`, applied at painting time, and is never stored back into the state. NOTE 1's \"either way\" is the sentence that found this session's silent gap: see §8.4.5."

[[clause]]
clause = "8.4.2"
title = "Graphics state stack"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_stack_saves_and_restores_them"]
note = "`q` pushes a clone of the whole `GraphicsState` and `Q` pops it, so every parameter — including the clip, the soft mask, the text state and the stroke's dash pattern — is saved and restored together, which is what \"a copy of the entire graphics state\" asks for. The clause's requirement that the two \"shall be balanced within a given content stream\" is a writer-side one: an unbalanced `Q` here pops nothing and an unbalanced `q` is discarded at the end of the stream, because a viewer that refused the file would show nothing. `MAX_STATE_DEPTH` bounds the stack against hostile input and *reports* on reaching it rather than truncating in silence. **The bound has a figure from the standard behind it since the four-hundred-and-thirty-fifth session, and a census behind that.** Annex C is informative and states none of this as a requirement, but §C.2's Table C.1 is the one place ISO 32000-2 prints a number for the depth of a nested construct — \"In previous versions of PDF, a maximum depth of graphics state nesting by q and Q operators was 28\" — so 256 is nine times the standard's own figure rather than a number chosen for looking generous. Of the 65 944 crawled documents surveyed in the four-hundred-and-thirty-third session **exactly one reaches it**, `0546285.pdf`, and it wants **337**: twelve times that figure, and drawn in 0.05 s once the bound is lifted. The bound was left where it is on that evidence rather than moved to admit one file, and `tests/hostile_budgets.rs` now states both halves — 400 nested `q` refused by name, 200 still drawing. ADR 0271."

[[clause]]
clause = "8.4.3"
title = "Details of graphics state parameters"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_operators_set_all_three", "crates/pdf-model/tests/line_parameters.rs::an_ext_gstate_sets_all_three_too"]
note = "A pointer row: the six parameters below are each in their own subclause, and all six are read. §8.4.3.1 is a sentence of introduction."

[[clause]]
clause = "8.4.3.1"
title = "General"
status = "inapplicable"
note = "One sentence introducing the subclauses below it, with no requirement of its own."

[[clause]]
clause = "8.4.3.2"
title = "Line width"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/paint.rs"]
test = ["crates/render-cpu/tests/stroke_width.rs::a_zero_width_stroke_is_one_device_pixel_at_every_scale", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_the_thinnest_line_the_device_draws"]
note = "The `w` operator sets the width, and a width of 0 is drawn as \"the thinnest line that can be rendered at device resolution: 1 device pixel wide\". **This row was wrong for fifteen sessions and is the ledger's second known error**: it said a zero width \"reaches the rasteriser as the thinnest line it draws\", which was true of `tiny-skia`, whose hairline mode a zero width selects, and false of Vello, where `kurbo` expands a zero-width stroke into an empty outline and the line vanished. A row that names a *rasteriser's* convention has recorded that rasteriser rather than the clause. `Stroke::device_width` now states the minimum in `pdf-render`, where both backends read it, and the scene that would have caught the defect exists. The clause is also cited from `oracle.rs` for what it does *not* say, twice. It gives a stroke a device-resolution minimum and gives an image none — but **this row used to finish that sentence with \"which is why `issue4436r.pdf`'s half-pixel-tall image mask is drawn at its coverage rather than snapped to a whole row\", and the four-hundred-and-fifth session found the conclusion resting on the wrong clause.** This one's silence about images is real and settles nothing: §10.7.4's sampled-image paragraph states the rule, and under it that mask paints no pixel at all, because no pixel centre lies in its 0.48-tall region. Drawing it at its coverage is this tree's §10.7.4 departure (1), not a gap in this clause. §10.7.4's row carries the arithmetic. And it says the width \"shall be a non-negative number\" without saying what a *negative* one means: `issue19633.pdf` writes `-0.1 w`, this tree clamps into the domain and then applies the rule for zero, and every reference takes a different one of the three available readings — a **documented choice**, now written where it is made rather than left in a `.max(0.0)`. One operator in one of 974 documents, measured. **And what this clause does *not* say about a width that is positive and under a pixel was settled in the five-hundred-and-eighty-fourth session** (ADR 0419), because `issue12295.pdf` draws 65 859 rules 0.1366 of a device pixel wide as a ghost where all four references draw them dark. The `shall` here is about **zero**; about everything else this subclause is descriptive and permissive — \"[t]he actual line width achieved can differ from the requested width by as much as 2 device pixels, depending on the positions of lines with respect to the pixel grid. Automatic stroke adjustment may be used to ensure uniform line width\" — so it permits a processor to be two pixels out and points at §10.7.5 for the way not to be. §10.7.5 then states the references' floor outright and conditions it on a parameter Table 52 initialises to `false`, and that document states no `/SA`, no `/ExtGState` and no `/GS`. A ladder of one rule at seventeen widths, at 72 and 576 dpi, says the four references floor at four *different* device-pixel widths — `poppler` and `hayro` 1.0, `mupdf` 0.2, `ghostscript` 0.27 — and that all three C references answer **one whole device pixel** at every sub-pixel width when asked for the clause's own aliased algorithm. They agree about the clause and part where §10.7.1's NOTE hands the question to them. `spec-errata emit` over clause 8 before writing: Errata Collection 3 touches nothing in this subclause."

[[clause]]
clause = "8.4.3.3"
title = "Line cap style"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/degenerate.rs", "crates/pdf-render/src/sub_pixel.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_operators_set_all_three", "crates/pdf-model/tests/line_parameters.rs::an_ext_gstate_sets_all_three_too", "crates/render-cpu/tests/degenerate_subpath.rs::a_single_point_closed_path_is_a_disc_under_round_caps", "crates/pdf-render/src/sub_pixel.rs::a_round_cap_is_the_semicircle_beyond_the_end_and_nothing_behind_it", "crates/pdf-render/src/sub_pixel.rs::a_square_cap_is_the_half_width_the_table_states_beyond_each_end", "crates/pdf-render/src/sub_pixel.rs::a_butt_cap_states_no_mark_of_its_own", "crates/pdf-render/src/sub_pixel.rs::a_closed_subpath_is_capped_nowhere", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_sub_pixel_rules_cap_carries_its_own_area"]
note = "Table 53's three styles, set by `J` and — from the twenty-fourth session — by Table 57's `/LC`, which three corpus documents write and which reached nothing at all before. `line_cap` is the one place a code becomes a style, so the two routes cannot disagree. The clause's own scope is wider than an open subpath's ends: it says the style applies \"at both ends of open subpaths (and dashes 8.4.3.6, \"Line dash pattern\") when they are stroked\", which is the sentence that makes a zero-length dash's cap a mark — see §8.5.3.2. **And Table 53's two projecting styles have a mark of their own under the device's quantum since the four-hundred-and-fifty-fifth session**, because their areas go as the *square* of the line width where the swept body's goes with it: §10.7.4's substitute widens a sub-pixel rule to one device pixel and divides the alpha by the same factor, which restores a body exactly and a cap only once, so ADR 0268 butt-capped the substitute and the cap was not drawn at all — 44% of a rule as long as it is wide under round caps, 50% under projecting square ones, and the whole mark for a rule shorter than its own width. `pdf_render::sub_pixel_caps` builds the table's own shapes — \"[a] semicircular arc with a diameter equal to the line width\", or a continuation \"for a distance equal to half the line width\" — as a mark outside the butt-capped body, at the alpha its own square implies. In the shared crate, for `degenerate.rs`'s reason. ADR 0290, and §10.7.4's row carries the measurements."

[[clause]]
clause = "8.4.3.4"
title = "Line join style"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/degenerate.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_operators_set_all_three", "crates/pdf-model/tests/line_parameters.rs::an_ext_gstate_sets_all_three_too"]
note = "Table 54's three styles, set by `j` and by Table 57's `/LJ`; the join geometry itself is each rasteriser's **except where §8.4.3.5's limit admits a mitre one of them refuses**, where the triangle a mitre adds to a bevel — \"the resulting notch beyond the ends of the segments\", whose far corners are the two butt caps' outer ends — is `pdf_render::mitre_wedges`' own (ADR 0398). The cross-backend scenes hold them to the same pixels. Two sentences of this clause are not about joins at all and both are read elsewhere: \"[i]n a closed subpath that is dashed, if the first segment starts with an on-dash and the last segment ends within an on-dash, then they shall be joined\", which both dashers do; and the one that makes a zero-length dash on a zero-length subpath segment \"rendered in an implementation-dependent manner\" for a non-round cap, which is quoted in `degenerate.rs` as the licence for giving a square cap with no determinable direction the round cap's circle."

[[clause]]
clause = "8.4.3.5"
title = "Miter limit"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/mitre.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::a_miter_limit_below_one_is_clipped_into_range", "crates/pdf-render/src/mitre.rs::the_tip_sits_where_the_ratio_puts_it", "crates/pdf-render/src/mitre.rs::a_ratio_over_the_limit_is_left_to_the_bevel", "crates/render-quorra/tests/mitre_limit.rs::every_backend_draws_the_mitre_the_limit_admits", "crates/render-quorra/tests/mitre_limit.rs::every_backend_bevels_a_ratio_over_the_limit"]
note = "Set by `M` and by Table 57's `/ML`, clipped below at 1 because §8.4.3.5's own ratio `1 / sin(φ/2)` never goes below it. Both backends are told the limit explicitly rather than left to default, since `tiny-skia` and `kurbo` both default to 4.0 where Table 51's initial value is 10.0 — a difference that would have shown as differently bevelled corners on any document that never says `M`. \"When the line width is zero, the miter length is zero\" needs nothing: §8.4.3.2 substitutes a device pixel before any join is expanded. **And the limit is a maximum rather than a length, which `pdf_render::hull` had been treating it as until the two-hundred-and-fifth session.** It bounded a join by `half × miter_limit` in every direction, so a rectangle stroked at the default limit of 10 came out bounded five widths outside itself — sound as a bound, and useless as a containment test, which is what §8.10.2's redundant-clip rule needs. `join_extent` now puts the tip where §8.4.3.4's two outer offset lines cross and falls back to the limit only where the join nearly doubles back. Two oracle pages moved from contradicted to agreeing on it. ADR 0165. **And a maximum a *library* lowers is the same error one layer down, which the five-hundred-and-sixty-third session paid.** The clause states the ratio as `1 / sin(φ/2)`, checks that reading with its own EXAMPLE — 1.414 converts under 90°, 2.0 under 60°, 10.0 under approximately 11.5° — and prints a NOTE that says \"Very large miter lengths are allowed\"; the ratio spans the whole join, so the tip sits `(w/2) / sin(φ/2)` from the vertex. `tiny-skia`'s stroker classifies a join by a normals' dot product **before** it reads the limit and calls anything within `SCALAR_NEARLY_ZERO` (1/4096) of −1 blunt, which is the ratio `1/sqrt(1/8192)` = 90.51 in disguise, so `pdf-differences`' `LargeMitreLimit.pdf` — `333 M`, ratio 166.676, tip 833.38 units up — drew a bevel on the processor while both device backends drew the spike `mutool` and `ghostscript` draw. `pdf_render::mitre_wedges` states which joins the limit admits and where each tip goes, from the formula alone; `render-cpu` passes in the one ratio that is a fact about its library and fills the bevelled outline with the mitres appended in one path, because two draws of two shapes sharing an edge would leave §11.3.7.3's union seam along it and the witness sets `/CA 0.6`. A dashed stroke and one at or under a device pixel are declined with their reasons and have no witness: `long_mitre_census` finds **2 of 1441** first pages with such a join, both of them that file. ADR 0398."

[[clause]]
clause = "8.4.3.6"
title = "Line dash pattern"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_dash.rs::a_dash_array_reaches_the_stroke", "crates/pdf-model/tests/line_dash.rs::a_negative_phase_is_incremented_by_twice_the_arrays_sum", "crates/pdf-model/tests/line_dash.rs::an_odd_length_array_is_stated_as_pairs"]
note = "Implemented in the tenth session; before it, no dashed line in any document was dashed. The `d` operator was parsed and its array dropped, because the content lexer flattens an operator's array and the code read only the empty case. Both backends had implemented dashing all along. Now: the array, the phase, the negative-phase normalisation the clause states, and a degenerate array drawn solid. The per-subpath restart and the cap and join treatment are the rasteriser's, and both backends do it."

[[clause]]
clause = "8.4.4"
title = "Graphics state operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::the_operators_set_all_three", "crates/pdf-model/tests/line_parameters.rs::the_stack_saves_and_restores_them", "crates/pdf-model/tests/line_dash.rs::a_dash_array_reaches_the_stroke"]
note = "Table 56's ten operators. `q`, `Q`, `cm`, `w`, `J`, `j`, `M`, `d` and `gs` are executed. `ri` sets a rendering intent, which reaches colour conversion only through §8.6.5.9's black point compensation — `AbsoluteColorimetric` turns it off, and the other three intents select an ICC transform this tree does not yet choose between (see §8.6.5.8). `i` sets the flatness tolerance and is deliberately ignored: §10.7.2 says \"PDF processors may choose to ignore any flatness tolerance specified within a PDF file\", which is a permission rather than a gap. (**The quotation said \"a PDF processor may ignore this parameter\" until the four-hundred-and-thirteenth session**, which is a sentence ISO 32000-2 does not contain — while §10.7.2's own row has carried the real one since it was written. Two rows about one permission, one of them quoting something nobody wrote: `doc/todo/01`'s seventh shape, found by its eleventh sweep, which is the first thing in this project to check a ledger note's quotation marks.)"

[[clause]]
clause = "8.4.5"
title = "Graphics state parameter dictionaries"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/line_parameters.rs::an_ext_gstate_sets_all_three_too", "crates/pdf-model/tests/line_parameters.rs::the_later_of_the_two_routes_wins"]
note = "Table 57's entries, and the clause that says a parameter \"can be set either with individual graphics state operators or with gs\". Read: `/LW`, `/LC`, `/LJ`, `/ML`, `/D`, `/RI`, `/SA`, `/BM`, `/SMask`, `/CA`, `/ca`, `/TK` and `/UseBlackPtComp`. The middle three of those were added in the twenty-fourth session — the operator half had been implemented since the interpreter's first commit (`dd857ed`) and the dictionary half read nothing, which is exactly the gap-inside-a-feature this ledger exists to find. Not read, and each for its own reason: `/HT` and `/HTO` are §10.6's halftones, `inapplicable` on the standard's own condition (\"if the raster output device supports PDF-defined halftoning\", which this one does not); **`/BG`, `/BG2`, `/UCR` and `/UCR2` were on that list under the reason \"describe a marking device (§10.4 to §10.6)\" until the five-hundred-and-sixty-fifth session, and all four are read** — `content/ext_gstate.rs` sets `black_generation_stated` where a dictionary states any of them, which is what makes §11.7.2's `DeviceCMYK` group space a *reported* departure rather than a silent one, so the entries decide a report rather than nothing; **`/TR` and `/TR2` were on that list until the three-hundred-and-fifty-seventh session and are not**, because `issue6931_reduced.pdf` states one that decides what a *screen* shows — see §10.5, which is `implemented` since that session (ADR 0204); **this sentence said §10.5 was `silent` for forty-five rounds after the round that built it, which is the fourth sweep's own shape and was found by it in the four-hundred-and-second.** `/OP`, `/op` and `/OPM` are answered by §8.6.7 rather than skipped. **`/SM` was on the not-read list too and is read** — `content/ext_gstate.rs` takes Table 57's smoothness tolerance and `pdf_render::shading::Ramp::resolution_for` samples a shading's colour function by it, which §10.7.3's own row has said since the seventy-fourth session; this row said it was \"the silence recorded under §10.7.3\" while that row read `implemented`, the fifth failure shape with the answer one clause away. **So what is left unread is `/FL` alone**, and that is what keeps this row `partial`: §10.7.2 permits ignoring flatness and `i` is read and discarded, so the `gs` route to the same parameter is the one of Table 57's two routes that reaches nothing — trap 5's shape, at a cost of nothing, because the parameter is discarded either way. **`/AIS` was on the not-read list until the four-hundred-and-second session and is not**: it has been read since the three-hundred-and-ninety-seventh — where it refused §11.4.6's knockout groups while set — and **honoured** since the five-hundred-and-eightieth, because the flag decides whether the soft mask and §11.6.4.4's constants state shape or opacity (ADRs 0234 and 0415, §11.6.4.3's row). The sentence it replaced — \"`/AIS` is argued in ADR 0027\" — was the *older* argument, that one alpha channel makes the distinction immaterial, and it is exactly what ADR 0234 retired. **`/Font` was the one owed entry and is implemented**: it selects a font by indirect reference rather than by resource name, so the font cache is keyed by `FontKey::Referenced(id)` as well as by name, and both text state parameters are set exactly as `Tf` sets them. `extgstate.pdf` — the one corpus document that writes it, and whose page says \"I should be courier!\" — draws with nothing reported. **This sentence said the entry was owed for several sessions after it landed**, which is the understatement a ledger has no gate against; it was found in the eighty-fifth session by reading the row against the code. **And a `gs` naming a dictionary the `/ExtGState` subdictionary does not define is reported since the four-hundred-and-nineteenth**, which is the loudest of the three this round made: every parameter the dictionary would have set stays at whatever the last one left, so the page is drawn in a *wrong* graphics state rather than missing a mark (ADR 0255). It found a fixture in this tree's own `soft_masks.rs` that had been asserting one number twice."

[[clause]]
clause = "8.5"
title = "Path construction and painting"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/degenerate.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_move_overrides_the_move_before_it", "crates/render-cpu/tests/degenerate_subpath.rs::a_single_point_closed_path_is_a_disc_under_round_caps"]
note = "The parent row for path construction and painting, reviewed as a family in the twenty-fourth session together with §8.4.3, because the two decide a stroke between them. The clause's own framing — construction operators, painting operators, clipping operators — is the shape of the interpreter's own dispatch. \"At the beginning of each page, the clipping path shall be initialised to the size of the MediaBox\" is where this renderer differs deliberately and says so under §14.11.2.1: it draws the *crop* box, which the oracle's third trap is about."

[[clause]]
clause = "8.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_segment_after_a_close_starts_where_the_close_returned_to"]
note = "The definitions the rest of the clause rests on, and two of them decide code. \"A pair of segments shall be said to connect only if they are defined consecutively\", so a subpath is a run of consecutive segments and the walker in `degenerate.rs` is built on exactly that. And `h` \"explicitly shall connect the end of a subpath back to its starting point; such a subpath is said to be closed\" — the open/closed distinction §8.5.3.2 then gives opposite answers to for a single-point subpath."

[[clause]]
clause = "8.5.2"
title = "Path construction operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_rectangle_is_the_five_operations_table_58_names"]
note = "A pointer row for the two subclauses below."

[[clause]]
clause = "8.5.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_move_overrides_the_move_before_it", "crates/pdf-model/tests/path_construction.rs::a_rectangle_overrides_the_move_before_it", "crates/pdf-model/tests/path_construction.rs::closing_a_closed_subpath_does_nothing", "crates/pdf-model/tests/path_construction.rs::a_rectangle_is_the_five_operations_table_58_names"]
note = "Table 58's seven operators, and three of its sentences that describe *removing* a command rather than adding one — all three implemented in the twenty-fourth session, none of which changed a pixel by itself. One `m` after another leaves \"no vestige of the previous m operation\", and because Table 58 states `re` as `x y m` and three `l`s and an `h`, an `re` overrides a preceding `m` too: six corpus documents write consecutive moves, `bug1743245.pdf` 205 of them on one page and `issue12810.pdf` 60 `m`/`re` pairs. `h` on an already-closed subpath \"shall do nothing\". They became load-bearing the moment §8.5.3.2's degenerate subpaths became marks, because each stray `m` would otherwise have been a dot the document never asked for. Not implemented, and reachable by no corpus first page: \"[m]ost operators that add a segment to the current path start at the current point; if the current point is undefined, an error shall be generated\" — such a path is passed to the rasteriser unclassified rather than refused."

[[clause]]
clause = "8.5.2.2"
title = "Cubic Bézier curves"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::the_implied_control_points_are_the_ones_the_clause_names"]
note = "`c`, `v` and `y`, whose difference is only which control point is implied — `v` takes the current point as the first, `y` the endpoint as the second. Cubic Béziers are the display list's only curve, so nothing is flattened or converted between here and either rasteriser; a TrueType quadratic is raised to the cubic through the same curve during glyph loading, and `render-cpu`'s `from_skia_path` raises one the same way where a dasher has produced it."

[[clause]]
clause = "8.5.3"
title = "Path-painting operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::the_starred_operators_are_the_even_odd_ones"]
note = "A pointer row for the three subclauses below."

[[clause]]
clause = "8.5.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_path_that_is_only_a_move_paints_nothing"]
note = "Table 59's ten painting operators, all executed: `S`, `s`, `f`, `F`, `f*`, `B`, `B*`, `b`, `b*` and `n`. `s` is `h S` and `b`/`b*` are `h B`/`h B*`, stated by calling the same `close_subpath` a written `h` calls. `B`'s NOTE — the fill and the stroke \"consult different values of several graphics state parameters\" — is why the two commands carry separate paints; §11.6.2's rule that they are nonetheless one object is a reported departure, see its row. **Errata Collection 3 turned this row's one refusal into agreement** — Issue #549, `/State` `Review`/`Accepted`: \"[a]ttempting to execute a painting operator when the current path is undefined … shall generate an error\" has \"generate an error\" struck for \"be ignored\", and `doc/md/` still carries the old form. This row read \"here it paints nothing, which is the recovery a viewer owes a malformed file\"; the amended clause states that recovery, so what was a departure with a good reason is now the rule. Found by `spec-errata emit` over clause 8 in the five-hundred-and-eighty-fifth session."

[[clause]]
clause = "8.5.3.2"
title = "Stroking"
status = "implemented"
code = ["crates/pdf-render/src/degenerate.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/render-cpu/tests/degenerate_subpath.rs::a_single_point_closed_path_is_a_disc_under_round_caps", "crates/render-cpu/tests/degenerate_subpath.rs::a_single_point_closed_path_paints_nothing_under_butt_or_square_caps", "crates/render-cpu/tests/degenerate_subpath.rs::a_zero_length_dash_paints_a_cap_where_a_degenerate_subpath_paints_nothing", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_stroke_with_no_length", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_degenerate_subpaths_dot_carries_its_own_area", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_dot_lands_in_one_pixel_at_every_width_and_every_placement", "crates/render-gpu/tests/headless_gpu.rs::a_sub_pixel_dot_marks_the_devices_raster"]
note = "Implemented in the twenty-fourth session, and it is the clause's *last paragraph* that had never been read. A degenerate subpath — \"a single-point closed path or … two or more points at the same coordinates\" — is a filled circle under round caps and \"no output\" under butt or projecting square ones, and a subpath that is only a trailing `m` is no output under any cap. Neither rasteriser answered that: `tiny-skia` painted a square where the clause asks for nothing, `kurbo` painted nothing where it asks for a circle, and a path of one `m` was an error on one backend and silence on the other. The rule is now `pdf-render`'s, stated once for both, with the circle this crate's own geometry rather than either round cap's. The paragraph's second half inverts the first for a *dash* of no length — \"the line caps shall always be painted, since their orientation is determined by the direction of the underlying path\" — so `[0 6] 0 d 1 J S` is a dotted line, which Vello drew as nothing at all, and a projecting square cap there faces along the path where Skia's dasher faces it upright. Four corpus first pages state marks with no length: 64 dots and 31 dotted strokes. The clause's remaining sentences are the rasterisers': what a stroke's sides are, which parameters it consults, and that \"[p]oints at which unconnected segments happen to meet or intersect receive no special treatment\". **The circle itself was disappearing and nobody had measured it until the four-hundred-and-fifty-fifth session**: it is `pi w^2 / 4`, so at 0.1 and 0.2 of a device pixel it is 0.008 and 0.03 of one, and the processor's rasteriser drew nothing whatever for it — a `silent` failure of §10.7.4 inside a clause this row called implemented, found by building the ladder for §8.4.3.3's cap and reachable by exactly the same substitution. The dot and a zero-length dash's mark are now stated at `pdf_render::enlarged_mark`'s width with the area they gave up in the paint's alpha, which also takes away a *promotion*: at half a pixel the same circle was 28% heavy, because `tiny-skia` rounds a shape crossing one of its sample lines up to a quarter of a row. ADR 0290. **And this row's own \"under any cap\" rests on one word, which the five-hundred-and-sixty-sixth session had to say out loud when another implementation's developers asked what a glyph outline's *leading* `MoveTo` deposits.** The sentence is \"A single-point open subpath (specified by a trailing m operator) shall produce no output\", and the parenthesis is read here as a gloss rather than a restriction — on the sentence two above it, \"This rule shall apply only to zero-length subpaths of the path being stroked\", which classifies by shape and not by position. Read the other way, a *non-trailing* single-point open subpath falls under nothing in this clause — the cap-dependent rule above it is defined for a single-point *closed* path or two or more coincident points — and §8.4.3.3's general \"both ends of open subpaths\" would come back and put a dot there. §8.5.3.3.1 is the check: its parallel sentence carries the position in normative words *outside* the parenthesis (\"if the last subpath in the path is a single-point open subpath\"), and this one has no such words, which is what a gloss looks like when the same drafter also writes a restriction. ADR 0401. **The dot was still disappearing, and the five-hundred-and-eighty-fifth session found out why by moving it half a pixel.** Both backends drew nothing at 0.1, 0.05, 0.02 and 0.01 of a device pixel with the mark on a pixel *corner* and the geometry's own ink with it on a pixel *centre* — one mark, one width, one alpha, and §10.7.4's own \"unfavourable placement relative to the device pixel grid\" between them. The widened circle covers `pi/4` of a pixel at a centre and `pi/16` at a corner, so an alpha the raster could hold arrived and was quartered before it was rounded. `pdf_render::point_mark` states such a mark as the device pixel §10.7.4 identifies by flooring its own centre, at the coverage its area implies, as soon as the widened form can put no level anywhere — and all three rasterisers take it, since the device lost the mark too and vello halved it at 0.2. The population is 97 dots over 7 of 1242 corpus and specification first pages, the thinnest at 0.36 of a device pixel, and 30 zero-length dashes on one, so nothing on this disk reaches the new construction. **Errata Collection 3 qualifies the dash sentence and `doc/md/` carries it unqualified** — Issue #103 puts the words *in the opaque imaging model* in front of the dash sentence in place of its opening *This* — which adds a scope rather than an answer, and Issue #434 adds a NOTE stating no requirement. ADR 0420."

[[clause]]
clause = "8.5.3.3"
title = "Filling"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::the_starred_operators_are_the_even_odd_ones", "crates/render-cpu/tests/fill_rules.rs::the_even_odd_rule_leaves_the_hole_regardless_of_direction", "crates/pdf-render/src/sub_pixel.rs::a_bowtie_is_not_taken_for_a_rectangle"]
note = "A pointer row for the three subclauses below. **One property spans all three and is checked here since the five-hundred-and-fifty-seventh session**: no shortcut may erase a crossing, because the two rules disagree about exactly that — §8.5.3.3.2 cancels it, §8.5.3.3.1 does not — so a path recognised as a rectangle and then drawn rule-independently must not be a self-intersecting quadrilateral wearing the same four corners. `pdf-render`'s `is_axis_aligned_rectangle` tests each *side* for moving along one axis rather than testing the vertex set, which rejects a bowtie on its first diagonal; the test states that, with the rectangle of the same four corners in perimeter order beside it so the rejection is about the order rather than the points. hayro's issue 1336 is the other construction — any five-element path touching all four corners in any order — where CAD valve symbols came out as solid squares under both rules, and a bowtie clip was elided as a no-op."

[[clause]]
clause = "8.5.3.3.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::a_trailing_move_is_not_part_of_the_path"]
note = "`f` fills \"the entire region enclosed by the current path\" with the nonstroking colour, and \"the insides of all subpaths, considered together\" — one command over one path, so subpaths cannot be filled separately by construction. Open subpaths are implicitly closed by both rasterisers' fill. The exception is implemented as of the twenty-fourth session: \"if the last subpath in the path is a single-point open subpath (specified by a trailing m operator), it shall be disregarded and not considered to be part of the path\" — eleven corpus first pages end a path that way, and a path that was *only* such a subpath used to reach `tiny-skia`, which refuses it, and failed the whole page — four corpus pages, `annotation-text-without-popup.pdf`, `issue12810.pdf`, `issue6342.pdf` and `issue9017_reduced.pdf`, which were the whole of that failure class and now all four render. Partial for the sentence after it: a degenerate subpath \"shall be considered to enclose the single device pixel lying under that point\", and neither backend paints anything there. The clause calls that result \"device-dependent and not generally useful\" in the same breath, so what is owed is a pixel nothing asks to be able to see; it is recorded here rather than reported because a report would name pages on which no reader could tell. **Read and kept in the five-hundred-and-sixty-fifth**: neither backend paints the single device pixel a degenerate subpath encloses, so the row's one debt stands, and it is the shape `CLAUDE.md` records as closed by decision — the clause calls the result \"device-dependent and not generally useful\" in the sentence that states it, which is why this is recorded rather than reported."

[[clause]]
clause = "8.5.3.3.2"
title = "Non-zero winding number rule"
status = "implemented"
code = ["crates/pdf-render/src/paint.rs", "crates/pdf-render/src/geom.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/render-cpu/tests/fill_rules.rs::the_non_zero_rule_fills_a_nested_subpath_wound_the_same_way", "crates/render-cpu/tests/fill_rules.rs::the_non_zero_rule_leaves_a_hole_when_the_windings_oppose", "crates/pdf-render/src/geom.rs::a_counter_wound_the_other_way_subtracts"]
note = "`FillRule::NonZero`, which both rasterisers implement natively — `tiny_skia::FillRule::Winding` and `peniko::Fill::NonZero`. It is the rule `f`, `B`, `b` and `W` use, and the display list carries it explicitly rather than by default so that a backend cannot substitute its own. **The rule's dependence on direction is stated one clause family over, and was read there in the five-hundred-and-sixty-first session**: §9.3.6's NOTE 2 says the direction of a glyph's paths 'can cause different output for overlapping glyphs', which is this rule doing what it is defined to do. `Path::signed_area` and `Path::reversed` in `pdf-render` are what let `pdf-font` state a direction for a face it chose itself — reversing every subpath negates every winding number, so a fill under this rule paints exactly what it painted and only a *combination* with an unreversed path moves. ADR 0396."

[[clause]]
clause = "8.5.3.3.3"
title = "Even-odd rule"
status = "implemented"
code = ["crates/pdf-render/src/paint.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/render-cpu/tests/fill_rules.rs::the_even_odd_rule_leaves_the_hole_regardless_of_direction"]
note = "`FillRule::EvenOdd`, for `f*`, `B*`, `b*` and `W*`, likewise native in both rasterisers."

[[clause]]
clause = "8.5.4"
title = "Clipping path operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/display_list.rs"]
test = ["crates/pdf-model/tests/path_construction.rs::the_starred_operators_are_the_even_odd_ones", "crates/pdf-model/tests/path_construction.rs::a_clip_built_from_a_disregarded_path_admits_nothing", "crates/render-cpu/tests/empty_clip.rs::a_fill_inside_an_empty_clip_marks_nothing", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_clip_that_admits_nothing"]
note = "`W` and `W*` set a pending rule that the *following* painting operator applies, which is the clause's own timing: the operator \"shall not alter the clipping path at the point where it appears\" but modifies \"the effect of the succeeding painting operator\", and afterwards the clip is \"the intersection of the current clipping path and the newly constructed path\". A `Clip` is therefore a child of the current one rather than a replacement, and `q`/`Q` restore it with the rest of the state. \"[T]he same area that would be filled by the f operator is the area that would be used for a clip\" decides the edge this clause turned out to have: a path §8.5.3.3.1 has just disregarded down to nothing encloses no area, so the clip built from it **admits nothing**, and `Clip::admits_nothing` states that for both backends — `tiny-skia` refuses an empty path and `kurbo` clips to an empty region, two conventions where the clause has an answer. `issue9017_reduced.pdf` writes `568.938 673.022 m W n` around a shading; the page had never rendered at all, and the twenty-fourth session's own trailing-`m` rule is what made it visible enough to be found wrong. A `W` with *no* path in front of it is the other case — §8.5.3.1 calls it an error — and leaves the clip alone rather than blanking the rest of the stream. One edge is recorded rather than implemented: §8.11.3.1's hidden optional content still builds its clip, which is that clause's requirement rather than this one's. **Two identical regions share one `ClipId`** since the hundred-and-forty-seventh session, which is a statement about \"the intersection of the current clipping path and the newly constructed path\" and not about drawing: page 6 of ISO 32000-2 states one region and wraps it round 303 text runs, and both backends key their per-chain work on the identifier. Exact comparison, so a producer whose rectangles differ in the last decimal place still gets two. 4.7x on that page's rasterisation, +1.22% on interpretation, and every oracle verdict unchanged. ADR 0132. **And how that intersection composes was read in the four-hundred-and-forty-fourth session, out of this clause's other paragraph**: \"The effective shape is the intersection of the object's intrinsic shape with the clipping path; the source shape value shall be 0.0 outside this intersection.\" A clip zeroes the shape outside it and is silent about what is inside it, so a clip's own boundary is not a quantity that multiplies — which is what `render-cpu` was doing to a chain of them, and no longer is. The reading, the ladder and what is still a product are §10.7.4's row and ADR 0280. **All three backends compose a chain by `min` since `0ddaa40`**, which is quorra taking the same reading inside the graphics library (its ADR 0030) after `doc/QUORRA_FEEDBACK.md` section 18 asked what rule composed one there — so a chain of two identical clips no longer squares its own edge on any of them. **And the same sentence reaches the *mark* in the five-hundred-and-twentieth** (ADR 0355): the effective shape is the intersection of the object's intrinsic shape with the clipping path, and an intersection with a set that *contains* the shape is that shape — `S ∩ C = S`, which is what `render-cpu` now draws where it multiplied the clip mask into the mark's own coverage. §10.7.4's row has the construction, the cost and what still multiplies. **Two more of this paragraph's sentences were read in the five-hundred-and-twenty-eighth** (ADR 0363). The first is the word *intrinsic*: the clip meets the object's own shape, and §11.3.7.2's mask shape multiplies what comes out of that intersection rather than standing inside it — so a mark under a clip *and* a soft mask is `(f_j ∩ C) · f_m`, which is what `render-cpu` composes now that `MaskCache` keeps the mask's rows beside the product. The second is the sentence about groups, which had been read as saying the opposite of what it says: the shape of a transparency group is \"defined as the union of the shapes of its constituent objects\" and \"shall be influenced both by the clipping path in effect when each of the objects is painted and by the one in effect at the time the group's results are painted onto its backdrop\" — so a group's raster meeting its clip is owed the same intersection, where ADR 0355 had recorded it as not owed. This backend's group buffer carries alpha rather than shape, which is the real obstacle and is `doc/todo/11` item 4's to price."

[[clause]]
clause = "8.6"
title = "Colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "All eleven colour space families, with the CIE-based ones converted rather than approximated. See the subclauses; the ones that decide a page are §8.6.4's device spaces, §8.6.5's CIE-based ones and §8.6.6's special ones."

[[clause]]
clause = "8.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "Introductory. Its separation of *specifying* a colour from *rendering* it is clause 10's subject and this tree's `ColourSpace::to_rgb`, the one route from any space to a pixel."

[[clause]]
clause = "8.6.2"
title = "Colour values"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "A colour value is \"one to four numbers\" whose meaning the space decides, and the clause's rule that a value outside the space's range \"shall be adjusted to the nearest valid value\" is `ColourSpace::component_range`'s clamp — the same one §8.9.5.2's `/Decode` map ends with."

[[clause]]
clause = "8.6.3"
title = "Colour space families"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::an_icc_space_prefers_its_stated_alternate"]
note = "The three groups — device, CIE-based and special — and Table 61's eleven families, all of which `colour::ColourSpace` has an arm for. §8.6.5.1's withdrawn `CalCMYK` is the twelfth name and the clause redirects it to `DeviceCMYK`, which is what happens here."

[[clause]]
clause = "8.6.4"
title = "Device colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_grey_is_the_same_however_it_is_drawn", "crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The three families, reviewed as one in the twenty-fifth session alongside §8.6.5. Two of them are what this device speaks, so they are the identity; the third is a conversion **§8.6.4.4 does not define and §10.4.2.5 does**, ranked below §10.3's ICC route by §10.4.2.1. This row said \"one conversion the standard does not define\" until the hundred-and-tenth session, which is the claim `CLAUDE.md` records as having been wrong for thirty-two sessions — the correction reached the code and this file's own principle 5 and never reached the ledger. See §8.6.4.4."

[[clause]]
clause = "8.6.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_default_colour_space_replaces_the_device_space"]
note = "Two normative sentences beyond the descriptive ones. The NOTE points at §8.6.5.6's remapping, which is implemented for every route including — as of the twenty-fifth session — an image's own `/ColorSpace`. The closing paragraph says device colour operators \"should be used only if device colour spaces have been remapped to CIE-based spaces\" inside a transparency group whose group space is CIE-based, and \"otherwise, the colour results are implementation-dependent and unpredictable\". That is advice to a *writer* about a configuration this reader already reports: §11.6.6's row names a group space that is not the device's three components, and there is nothing here for a reader to do that it does not already do."

[[clause]]
clause = "8.6.4.2"
title = "DeviceGray colour space"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs", "crates/pdf-model/tests/ccitt.rs::a_scanned_page_comes_back_mostly_white"]
note = "\"A grayscale value shall be represented by a single number in the range 0.0 to 1.0, where 0.0 corresponds to black, 1.0 to white\" — one component, passed through with no gamma, which is what `ColourSpace::Gray` does. All six operators the clause names are implemented and reach the same conversion: `CS`/`cs` with the `/DeviceGray` name, `SC`/`sc` with one operand, and `G`/`g` which set space and colour together. The clause's last sentence — setting the space initialises the colour to 0.0 — is applied where a colour space is selected, and its companion in §8.6.8's initial-state rules is what `colour.rs` cites at the initial-colour comment. Reviewed in the twelfth session because `CCITTFaxDecode`'s sense depends on it: a bilevel filter delivering 1 for white is only right because this clause says 1.0 is white."

[[clause]]
clause = "8.6.4.3"
title = "DeviceRGB colour space"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_grey_is_the_same_however_it_is_drawn", "crates/pdf-model/src/colour.rs::grey_and_rgb_pass_through_unchanged"]
note = "Three components, each \"a number in the range 0.0 to 1.0, where 0.0 shall denote the complete absence of a primary component and 1.0 shall denote maximum intensity\", passed through with no curve — which is the whole of the conversion, because the clause defines a component as an intensity of *this device's* primary and §8.6.5.7 NOTE 3 says nothing in a PDF describes that device's calibration. Applying a gamma here would assert a calibration the file does not state. **What the *number* becomes is where two renderers part company, and the corpus states the arithmetic outright**: `issue840.pdf` fills `0.125 0.125 0.125 rg` and `0.235 0.235 0.235 rg` over most of a page, which are 31.875 and 59.925 of 255. Ours and `poppler` round them to 32 and 60; `mupdf` truncates to 31 and 59, one level darker on nearly every pixel, which is the whole of the 0.60 of 255 separating its ink ladder from `poppler`'s at four resolutions. §3a's first shape — the file's own numbers decide and no reference is needed to say which answer they give (the three-hundred-and-twenty-third session, `AMBIGUOUS_EIGHT_BIT_COMPOSITING`). All six operators reach it: `CS`/`cs`, `SC`/`sc`/`SCN`/`scn` with three operands, and `RG`/`rg`. The closing sentence — selecting the space initialises all three components to 0.0 — is `ColourSpace::initial_colour`."

[[clause]]
clause = "8.6.4.4"
title = "DeviceCMYK colour space"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn", "crates/pdf-model/src/colour.rs::the_process_inks_are_the_published_ink_appearances"]
note = "Four components, \"the concentrations of these process colourants\", and *this* clause stops there — but **the standard does not**, and this row said it did until the hundred-and-tenth session. §10.4.2.5 is titled \"Conversion from DeviceCMYK to DeviceRGB\" and states the formula outright; §10.4.2.1 offers it to \"a less-capable PDF processor\" as one of a set of \"crude approximations\", ranked below §10.3's ICC route, which §10.3.2 then *requires* of a processor whose device space is not the document's. So the question is answered twice and in an order, and this tree takes the higher branch: the fallback is a table of the sixteen ink corners interpolated, which is exact at the corners where §10.4.2.5's formula is up to 115 of 255 away, and the three sources that outrank even that are implemented — `/DefaultCMYK` (§8.6.5.6), an output intent's `/DestOutputProfile` (§14.11.5) and an `ICCBased` space's profile (§8.6.5.5). `partial` records a *documented choice between two answers the standard ranks* (ADRs 0009, 0042), not a silence: there is no sentence in this subclause left to implement. The closing sentence — cyan, magenta and yellow initialised to 0.0 and black to 1.0, which is black through the K channel rather than through zeroes — is `ColourSpace::initial_colour`, and it is the case a reader is most likely to get wrong by assuming every space starts at black-by-zeroes. **Read and kept in the five-hundred-and-tenth session**: `ColourSpace::initial_colour` still answers black through K, and the three outranking sources are the ones the code implements - checked against `colour.rs`, not believed."

[[clause]]
clause = "8.6.5"
title = "CIE-Based colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::lab_maps_its_anchor_colours", "crates/pdf-model/src/colour.rs::calgray_is_a_luminance_and_must_be_encoded_for_the_display", "crates/pdf-model/src/colour.rs::calrgb_stating_srgbs_own_parameters_is_the_identity"]
note = "The family, reviewed as one in the twenty-fifth session. All four of its space families are read from the document and converted through CIE XYZ rather than approximated (ADR 0012), and there is exactly one route from XYZ to a pixel. The subclauses below carry what each one found."

[[clause]]
clause = "8.6.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::a_calcmyk_space_is_device_cmyk_and_its_dictionary_is_ignored", "crates/pdf-model/src/colour.rs::a_space_starts_at_the_colour_the_clause_gives_it", "crates/pdf-model/tests/soft_masks.rs::a_soft_mask_group_is_not_bound_by_an_uncoloured_glyphs_restriction"]
note = "Three requirements, and one of them was a refusal until this review. **`CalCMYK`**: \"A PDF reader shall ignore CalCMYK colour space attributes and render colours specified in this family as if they had been specified using DeviceCMYK\" — the family was withdrawn before it was ever completed, so the clause states the whole of what a reader does with one. It now parses to `DeviceCMYK`; before, such a file reported an unsupported colour space, which is a refusal where the standard gives an answer. No corpus document writes one. **The initial colour**: \"setting the current stroking or nonstroking colour space to any CIE-based colour space shall initialise all components of the corresponding current colour to 0.0\", with the nearest valid value substituted where 0.0 is out of range — `ColourSpace::initial_colour`, which §8.6.8 also asks for. **The two-stage model**: `Decode ABC` then `Matrix ABC` then `Decode LMN` then `Matrix LMN`; PDF states only spaces whose second stage is the identity, which is why the code has one stage and no more."

[[clause]]
clause = "8.6.5.2"
title = "CalGray colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::calgray_is_a_luminance_and_must_be_encoded_for_the_display", "crates/pdf-model/src/colour.rs::calgray_applies_its_gamma_before_the_white_point"]
note = "Table 62 in full: `/WhitePoint` required, `/BlackPoint` defaulting to black, `/Gamma` a positive number defaulting to 1. The table's constraint on the white point is \"[t]he numbers X_W and Z_W shall be positive, and Y_W shall be equal to 1.0\", and `white_point` enforces the first half only — a file stating a Y_W other than 1.0 is *accepted*, because the Bradford adaptation maps whatever white the space declares onto D50's and a scaled white therefore normalises itself, so refusing would lose a file this device can draw correctly. Positivity is enforced because a zero or negative component divides or inverts that adaptation. The hundred-and-tenth session corrected this note, which had read as though the whole sentence were checked. The clause's transformation is one stage — the A component decoded by the gamma function and multiplied by the white point to give L, M and N, which are X, Y and Z because there is no second stage — and that is what `to_rgb_at` computes. \"Component values falling outside that range shall be adjusted to the nearest valid value without error indication\" is `channel`, which clamps to 0.0..=1.0 and maps NaN to 0.0. `/BlackPoint` is read and *not* applied: ADR 0012 argues that a stretch built from it is undefined on input Table 62 permits, and §8.6.5.9 leaves black point compensation to the processor wherever `/UseBlackPtComp` is `Default`. **The corpus now measures what that choice costs rather than asserting it** (ADR 0296): `cargo run --release -p pdf-model --example black_point_census` counts every Cal space stating the entry at all and every one stating anything but `[0 0 0]`, and `calrgb.pdf` pages 1, 5, 11 and 12 state one space differing in nothing else, at `[0 0 0]`, `[1 1 1]`, `[8 8 8]` and `[50 50 50]` — from which this tree, `poppler`, `mupdf` and `ghostscript` all produce one raster."

[[clause]]
clause = "8.6.5.3"
title = "CalRGB colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::calrgb_reads_its_matrix_one_column_per_component", "crates/pdf-model/src/colour.rs::calrgb_stating_srgbs_own_parameters_is_the_identity"]
note = "Table 63 in full: `/WhitePoint` as in Table 62, `/BlackPoint` defaulting to black, `/Gamma` an array of three defaulting to `[1 1 1]`, `/Matrix` nine numbers defaulting to the identity. The table's order is `[X_A Y_A Z_A X_B Y_B Z_B X_C Y_C Z_C]`, which is one *column* per triple rather than one row — the code's field comment says so, because reading it the other way is a plausible transposition that no page announces. Each component is decoded by its own gamma and the vector multiplied by the matrix, with no second stage. Clamping and `/BlackPoint` are as §8.6.5.2, whose row carries the corpus's own four-page A/B for the second. This subclause's sentence about that entry is the one that sends it onward — \"[t]he WhitePoint and BlackPoint entries in the colour space dictionary shall control the overall effect of the CIE-based gamut mapping function described in subclause 10.3\" — so what the `shall` requires is decided by §10.3, and §8.6.5.9 is what makes carrying it out at all the processor's where `/UseBlackPtComp` is absent. The subclause's closing half — deriving `/Matrix` and `/WhitePoint` from phosphor chromaticities — is a writer's calculation and reaches no reader."

[[clause]]
clause = "8.6.5.4"
title = "Lab colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::lab_maps_its_anchor_colours"]
note = "`/WhitePoint`, `/BlackPoint` and `/Range` are read, and a Lab colour is converted through XYZ by the one route every CIE-based space takes (ADR 0012). Reviewed in the eleventh session for its component *ranges* rather than its conversion: L runs 0 to 100 and a and b take `/Range`'s bounds, which is what an `Indexed` space's colour table has to be scaled into (§8.6.6.3) and was not."

[[clause]]
clause = "8.6.5.5"
title = "ICCBased colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/icc.rs", "crates/pdf-model/src/colour.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::an_icc_image_is_converted_through_the_same_profile_as_a_fill", "crates/pdf-model/src/icc.rs::a_real_cmyk_profile_agrees_with_independent_evaluators", "crates/pdf-model/tests/transparency_groups.rs::a_presss_grid_is_the_profile_at_its_samples_and_near_it_between_them"]
note = "The profile is parsed and evaluated by an A2B evaluator written in this tree, and it wins over `/Alternate` and `/N`, which is the clause's own order: the profile is the document's statement of what its numbers mean. `/Range` is read. **The twenty-fifth session closed the half that was silent**: a *fill* in an `ICCBased` space went through the profile and an *image* in the same space did not — `image.rs` reduced the space to a device one by its `/N` — which is trap 6's defect with an image on one side of it. 31 corpus documents carry 1037 such images and on 15 the two answers differed by 18 levels out of 255 for a mid grey. Closing it was a performance problem before it was a correctness one: an exact per-pixel conversion is dominated by transcendental functions (36% of one page in `libm`, by callgrind) and took the corpus gate from 1.7 s to 11.4 s. What ships is an *exact* per-image memo keyed on the raw sample tuple, which puts the gate at 1.8 s. ADR 0035. **Read again in the four-hundred-and-thirty-sixth session for the direction a *blending* space needs**, which is the one sentence of this clause that had never been acted on: \"it shall have both \"to CIE\" ( AToB ) and \"from CIE\" ( BToA ) information. This is because the group colour space shall be used as both the destination for objects being painted within the group and the source for the group's results.\" That is a requirement on the *file*, and 286 of the 286 four-component blending-space profiles the web population names carry both. It is not a requirement on the processor to evaluate `BToA`: §14.11.5's Table 401 names the other direction for exactly this device — the `AToB` information \"may optionally be used to remap source colour values to some other destination colour space, such as for screen preview or hardcopy proofing\" — so a four-component blending space is sampled through the `A2B` evaluator this row already describes, and the conversion into it is a right inverse of that sampling rather than a second table. ADR 0272."

[[clause]]
clause = "8.6.5.6"
title = "Default colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/image.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_default_colour_space_replaces_the_device_space", "crates/pdf-model/tests/colour_paths.rs::a_default_colour_space_replaces_an_images_device_space", "crates/pdf-model/tests/colour_paths.rs::a_default_space_outranks_the_output_intent", "crates/pdf-model/tests/transparency_groups.rs::the_three_routes_to_a_press_all_composite_in_it"]
note = "`/DefaultGray`, `/DefaultRGB` and `/DefaultCMYK` are looked up in the resources whenever a device space is named, and the resolved space is used in its place — \"if such an entry is present, its value shall be used as the colour space for the operation currently being performed\". Because the lookup is inside `ColourSpace::by_name`, the clause's four indirect cases come free: a `Pattern`'s underlying space, an `Indexed` space's base and a `Separation` or `DeviceN`'s alternate all reach it by the same route. **The image half landed in the twenty-fifth session**: the clause requires the remapping of \"a colour space given as an entry in an image XObject, inline image, or shading dictionary\", and an image's space had been parsed against an *empty* resource dictionary. **This sentence read \"[o]ne corpus document names a default at all and its images state their space directly, so nothing in the corpus could have found it\", and the count was wrong**: nine of the 974 name one — eight a `/DefaultRGB` and `bug886717.pdf` a `/DefaultCMYK` — and one of the nine, `bug1019475_1.pdf`, states it inside an object stream where no byte search over the file can see it at all. Whether any of the nine states an image's space *indirectly* — which is what would have exercised the image half — is not measured here and is the question the corrected count makes worth asking. Re-measured in the five-hundred-and-seventieth session; ADR 0406. Not implemented, and named here rather than left implicit: the clause's validity rules on what may *be* a default — not `Lab`, `Indexed` or `Pattern` — are not enforced, because a reader that honours a malformed one draws what the producer asked for and a reader that refuses draws nothing. **Since the four-hundred-and-thirty-sixth session the entry also decides what a page *composites* in**, not only what a colour converts through: where §11.4.7's page group states `/DeviceCMYK`, a `/DefaultCMYK` naming a four-component profile makes that profile the four components §11.3.4 composites. Annex P — informative, and the standard's own algorithm for a group's blending space — puts this route first, ahead of §14.11.5's output intent, which is the ranking this row already recorded for a colour on its way to a pixel. Six of the 65 944 web documents reach a press this way. ADR 0272."

[[clause]]
clause = "8.6.5.7"
title = "Implicit conversion of CIE-Based colour spaces"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_colour_in_the_presss_own_space_is_not_converted_into_it", "crates/pdf-model/tests/transparency_groups.rs::a_colour_in_the_presss_own_space_is_not_converted_into_it"]
note = "The clause is a permission to *skip* a conversion: where a source CIE-based space \"accurately represents the particular output device being used\", a processor should use the values directly rather than converting to XYZ and back. Its own NOTE 3 says the conditions \"cannot be specified in PDF, since nothing in PDF describes the calibration of the output device\". This device is an sRGB screen and every colour is converted to it at the moment it is read, so there is no four-to-three-to-four round trip for the clause to save and no place the shortcut would apply. The paragraph that would bind us if it did — that all the semantics of the device space then apply, including overprint mode and a `Separation` selecting a component of the group space — is answered by §8.6.7 and §11.7.4 for this device's three additive colourants (ADR 0028). **The implicit conversion this clause describes is performed on a page that composites in a press, since the four-hundred-and-thirty-sixth session.** A colour stated in the very profile §11.4.7's page group composites in is passed through rather than converted, which is this clause's own reason — \"This avoids any unwanted computational error and in the case of 4 component colour spaces avoids the conversion from 4 components to 3 and back to 4, a process that loses critical colour information.\" `icc::Profile::identity` is what recognises the space, and §11.7.2's redefinition of `DeviceCMYK` inside such a group is the same passthrough reached by the other route. What is *not* done is the general case this clause is written about — treating a matching CIE-based space as the device's native space outside a transparency group — because this device's native space is a screen's three components. ADR 0272."

[[clause]]
clause = "8.6.5.8"
title = "Rendering intents"
status = "partial"
code = ["crates/pdf-model/src/content/colour.rs", "crates/pdf-model/src/content/image.rs", "crates/pdf-model/src/colour.rs", "crates/pdf-model/src/icc.rs"]
test = ["crates/pdf-model/src/icc.rs::black_point_compensation_aligns_the_darkest_colour_the_profile_reaches", "crates/pdf-model/tests/rendering_intent.rs::an_images_own_intent_turns_black_point_compensation_off", "crates/pdf-model/tests/rendering_intent.rs::an_unrecognised_intent_name_is_relative_colorimetric", "crates/pdf-model/tests/rendering_intent.rs::an_image_without_an_intent_takes_the_graphics_states", "crates/pdf-model/tests/rendering_intent.rs::a_shadings_ramp_honours_the_rendering_intent"]
note = "Table 69's four names are recognised, and one of them changes what this renderer does: `AbsoluteColorimetric` turns black point compensation off, which §8.6.5.9 requires of it. `RelativeColorimetric`, `Saturation` and `Perceptual` are recorded and reach nothing further — selecting a profile's `A2B1` or `A2B2` table by intent is not done, so a perceptual intent renders colorimetrically. \"If a PDF processor does not recognise the specified name, it shall use the RelativeColorimetric intent by default\" is satisfied by construction rather than by a branch: everything that is not `AbsoluteColorimetric` takes the same path, which is what the clause asks a reader to do with a name it does not know. Both routes to the parameter are read, the `ri` operator and `/RI` in an `/ExtGState`; the third, an image dictionary's `/Intent`, is not. **And reading a route is not obeying it everywhere, which the five-hundred-and-eighty-second found while reading §8.9.5.1's own entry list**: the parameter reaches `content/colour.rs` and stops there, so the black point compensation `AbsoluteColorimetric` turns off is turned off for a path's and a glyph's colour and for nothing else. `image.rs` converts every sample with compensation on — a literal `true` at each of its six `Compositing::paint` calls — and so do `shading.rs`'s ramp and `mesh.rs`'s vertices. §8.9.5.1's row carries the condition under which that moves a mark and the population that states the name: 0 of the 974 corpus documents, 0 of the 275 under `doc/corpora/`, 2 of the 65 944 crawled ones. **Both halves of that sentence were closed in the six-hundred-and-seventh session, and the clause's own list of routes is why.** \"Rendering intents shall be specified with the ri operator (see 8.4.4, \"Graphics state operators\"), the RI entry in a graphics state parameter dictionary (see 8.4.5, \"Graphics state parameter dictionaries\"), or with the Intent entry in image dictionaries (see 8.9.5, \"Image dictionaries\")\" — three routes, of which this row had recorded the third as unread and nobody had asked what the first two reached. They reached `content/colour.rs` and stopped. The intent is a parameter of its own now (`content::colour::Intent`, one variant per Table 69 name, unknown names mapped by the clause's own default sentence) rather than a decision folded into the black point at the moment it is set, Table 87's `/Intent` is read where an image is drawn, and `crate::colour::Conversion` carries the black point that intent decides into `image.rs`, `shading.rs` and `mesh.rs` — which between them are every colour this tree converts outside an operator. What is still `partial` is unchanged and is not about the routes: `RelativeColorimetric`, `Saturation` and `Perceptual` are recognised, kept apart and act on nothing, because selecting an ICC profile's `A2B1` or `A2B2` table by intent is not done. §8.6.5.9's row has the two orderings the old shape got wrong."

[[clause]]
clause = "8.6.5.9"
title = "Use of black point compensation"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/content/colour.rs", "crates/pdf-model/src/colour.rs", "crates/pdf-model/src/icc.rs"]
test = ["crates/pdf-model/src/icc.rs::black_point_compensation_aligns_the_darkest_colour_the_profile_reaches", "crates/pdf-model/tests/rendering_intent.rs::a_later_intent_does_not_restore_a_black_point_the_state_turned_off", "crates/pdf-model/tests/rendering_intent.rs::an_absolute_intent_overrides_a_black_point_entry_set_after_it", "crates/pdf-model/tests/rendering_intent.rs::an_ext_gstate_intent_replaces_the_one_in_force", "crates/pdf-model/tests/image_reuse.rs::a_raster_is_not_shared_across_black_points"]
note = "`/UseBlackPtComp` is read with its three values, and `BlackPoint::applies` decides. `ON` compensates, `OFF` does not, and `Default` — \"the behaviour is left to the PDF processor to determine\" — compensates, which is this processor's determination and is written down as one. \"If the current render intent of an object is AbsColorimetric then the value of UseBlackPtComp shall be treated as OFF\" is implemented for both routes to the intent, and note the spelling: this clause writes `AbsColorimetric` where Table 69 writes `AbsoluteColorimetric`, and the code tests for the name a file may actually contain. What is `partial` is the `ON` half: the clause defers to ISO 18619 for what compensation *is*, and this tree does not have that document — what it implements is the black point stretch ADR 0012 describes, applied to an ICC profile's own black point, which is undefined on input Table 63 permits and is why `/BlackPoint` on a Cal space is read and not applied. **The claim that the `AbsColorimetric` rule \"is implemented for both routes to the intent\" was wrong in three ways, and the six-hundred-and-seventh session found all three by reading the sentence's own subject.** The clause says *the current render intent of an **object***, and this tree implemented it as an *assignment*: `ri` and `/UseBlackPtComp` shared one field of the graphics state, so whichever ran last won. Both orderings were therefore wrong — a `ri` naming any other intent reset an explicit `/UseBlackPtComp OFF` back to compensating (the clause says nothing whatever about a non-absolute intent), and a `/UseBlackPtComp ON` set *after* an absolute intent compensated although the intent still in force says it shall not. The two parameters are separate now (`GraphicsState::intent`, `GraphicsState::use_black_pt_comp`) and `GraphicsState::black_point` combines them per object, which is where the clause puts the question. The third way is the count: there are **three** routes to the intent, not two — §8.6.5.8 names the `ri` operator, an `/ExtGState`'s `/RI` and §8.9.5.1 Table 87's `/Intent` — and the third had no reader at all, which §8.9.5.1's row now records against its own entry. **And a fourth thing the row did not say**: the two routes it *did* name reached a path's colour and a glyph's and nothing else, because `image.rs`, `shading.rs` and `mesh.rs` each passed a literal `true` at every `Compositing::paint` call. `crate::colour::Conversion` pairs the black point with the compositing target those three already threaded, so the flag now arrives everywhere a colour is converted and no `paint` call can omit it. The population that made this invisible is unchanged and is §8.9.5.1's: 0 of the 974 corpus documents name `AbsoluteColorimetric` at all (a cleartext `grep -a`, an undercount for a content stream), which is why the fixtures are hand-built ICC profiles whose darkest colour is a tenth of their white point."

[[clause]]
clause = "8.6.6"
title = "Special colour spaces"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs"]
note = "Aggregate of the six below, read as a family in the nineteenth session because §11.7.4's overprinting is stated in terms of the colourants a `Separation` or `DeviceN` space names, and this is the clause that says what those are. All four families draw. The reading produced two rules neither the code nor any earlier note had: §8.6.6.4's `/All` and `/None` colourants, which are decided before the alternate space rather than through it. **`partial` for what §8.6.6.5 owes, which this note named nothing of until the four-hundred-and-forty-second session** — a `DeviceN` space's `/NChannel` subtype and its `/Colorants` attributes dictionary, neither of them read anywhere in the tree, and the first of the two the one that could change a colour on a device that had the colourants."

[[clause]]
clause = "8.6.6.1"
title = "General"
status = "inapplicable"
note = "Two sentences naming the four special families — Pattern, Indexed, Separation and DeviceN — all four of which are implemented under the subclauses below. No requirement of its own."

[[clause]]
clause = "8.6.6.2"
title = "Pattern colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/tiling.rs::an_uncoloured_pattern_takes_its_colour_from_the_operator", "crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints"]
note = "Three sentences: an area is painted with a pattern rather than a colour, the pattern is a tiling (Type 1) or a shading (Type 2) one, and §8.7 has the detail. Both types are implemented on both backends. `ColourSpace::Pattern` carries the *underlying* space this clause's second form names, because an uncoloured tiling pattern takes its colour there rather than in any of its own — which is §8.7.3.3's requirement reaching this clause's array."

[[clause]]
clause = "8.6.6.3"
title = "Indexed colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/src/colour.rs::an_indexed_space_reads_its_table", "crates/pdf-model/tests/inline_images.rs::a_colour_space_name_is_looked_up_in_the_resources"]
note = "The four-element array, both forms of the table (byte string and stream), `hival` clamping, and the initial colour of 0. The sentence that had not been applied is the scaling one: \"Each byte shall be an unsigned integer in the range 0 to 255 that shall be scaled to the range of the corresponding colour component in the base colour space\". Every byte was divided by 255, which is that scaling only where the base's components run 0 to 1 — `issue2761.pdf` indexes into a `Lab` base and drew a black square where four renderers draw a pale grey gradient. Fixed in the eleventh session, and only visible then because `Indexed` images did not unpack before it."

[[clause]]
clause = "8.6.6.4"
title = "Separation colour spaces"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::the_none_colourant_marks_nothing", "crates/pdf-model/tests/colour_paths.rs::the_all_colourant_is_a_complemented_tint"]
note = "The four-element array, the tint in 0.0 to 1.0 treated subtractively, the initial value of 1.0, and the reversion to the alternate space through the tint transform. The reversion is not a fallback here but what the clause *requires* of this device: \"The preceding paragraph applies only to subtractive output devices such as printers and imagesetters. For an additive device such as a computer display, a Separation colour space never applies a process colourant directly; it always reverts to the alternate colour space as described below.\" That sentence is also what makes §11.7.3's soft-mask rule and §11.7.4's Table 146 collapse here, and it is a stronger citation for the choice than §11.7.3's own. Two rules were found missing by reading it in the nineteenth session, and both are now implemented: `/None`, which \"shall not produce any visible output\" and was painted through its tint transform like any other colourant, and `/All`, whose subtractive tint \"shall be complemented by subtracting from 1 before applying to all available colourants\" on an additive device and was likewise run through a transform the clause says to ignore. Both are decided before the alternate space and the tint transform are even parsed, because the clause requires those to be ignored \"although valid values shall still be provided\" — so an unreadable one cannot take the space with it. 2 corpus first pages set `/All`; none sets a `Separation` `/None`, which is trap 8 rather than evidence it is rare in the world."

[[clause]]
clause = "8.6.6.5"
title = "DeviceN colour spaces"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs"]
note = "The space's four elements are read and a colour is converted by running the tint transform into the alternate space, which is what the clause requires of a processor with none of the named colourants available — and this one has none, since it draws to a screen. `None` colourants are therefore passed to the tint transform rather than discarded, which is what the clause says for the reverting case. The all-`None` rule was the exception and is implemented as of the nineteenth session: \"A DeviceN colour space whose component colourant names are all None shall always discard its output, just the same as a Separation colour space for None; it shall never revert to the alternate colour space.\" Still not implemented: `/NChannel` and the `/Colorants` attributes dictionary, neither of which changes the reverting path — an `/NChannel` space's per-component reversion is the one requirement of the pair that could, and it needs a colourant this device has."

[[clause]]
clause = "8.6.6.6"
title = "Multitone examples"
status = "inapplicable"
code = ["crates/pdf-model/src/colour.rs"]
note = "Four worked examples of `Indexed` over `DeviceN` producing duotones, tritones and quadtones, all of them NOTEs and EXAMPLEs. There is no requirement here that the two clauses they compose do not already state, and both are implemented — `MAX_DEPTH` exists so that exactly this nesting resolves. Read and dismissed, which the ledger records rather than leaving blank."

[[clause]]
clause = "8.6.7"
title = "Overprint control"
status = "inapplicable"
code = ["crates/pdf-model/src/content.rs"]
note = "`/OP`, `/op` and `/OPM` are read nowhere, and this clause is why rather than an omission it excuses. Overprinting decides what happens to the device colourants a painting operation does *not* name: \"If the overprint parameter is false (the default value), painting a colour in any colour space shall cause the corresponding areas of unspecified colourants to be erased\", and if true \"and the output device supports overprinting, erasing actions shall not be performed\". This device has three additive process colourants and produces no separations, and the clause states what such a device does: \"Not all devices support overprinting. … If overprinting is not supported, the value of the overprint parameter shall be ignored\" (NOTE 1). The overprint *mode* is settled by a `shall not` in the clause's own body: \"It also shall not apply if the native colour space of the output device does not include CMYK device colourants; in that case, source colours shall be converted to the device's native colour space, and all components participate in the conversion, whatever their values.\" Conversion of every component whatever its value is exactly what `ColourSpace::to_rgb` does. §11.7.4 reaches the same answer for the transparent model by an independent route, and ADR 0028 has both arguments and the honest limit — the one configuration where overprinting would change a pixel here is a group whose blending space is `DeviceCMYK`, which §11.6.6 already reports."

[[clause]]
clause = "8.6.8"
title = "Colour operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/type3.rs::a_d1_description_takes_its_colour_from_the_page_and_a_d0_one_does_not", "crates/pdf-model/tests/tiling.rs::an_uncoloured_cell_that_sets_a_colour_is_ignored", "crates/pdf-model/tests/tiling.rs::an_uncoloured_cell_that_sets_a_transfer_function_is_ignored", "crates/pdf-model/src/colour.rs::a_space_starts_at_the_colour_the_clause_gives_it", "crates/pdf-model/tests/names_are_bytes.rs::a_colour_space_name_that_is_not_utf_8_is_found", "crates/pdf-model/tests/names_are_bytes.rs::two_colour_space_names_differing_only_outside_utf_8_are_two_names"]
note = "All twelve operators of Table 73, and the clause's two further requirements, both of which were gaps found by reading it in the tenth session. The initial colour a `cs`/`CS` sets was black for every space; the clause gives six cases, of which a Separation's full ink, an Indexed space's entry 0 and a Pattern's paint-nothing were wrong. And the restriction on an uncoloured figure — a `d1` glyph description or an uncoloured tiling pattern, and everything either of them invokes — now ignores the listed operators, every /ExtGState entry of the list this tree reads, and every image that is not an image mask. **That count said *two* until the three-hundred-and-seventy-fifth session and the third was going through**: §10.5's transfer function landed in the three-hundred-and-fifty-eighth and its `/ExtGState` reader was not put behind the flag, so a `/TR` inside an uncoloured cell decided a colour this clause reserves for whoever uses the pattern — \"[a]ll of the following entries, if present in the graphics state parameter dictionary of a gs operator shall be ignored\", and `TR` and `TR2` are the first two names in that table. Found by grepping the phrase `content.rs`'s own comment used to explain the list, which still called both entries unread thirty lines below the code that read them; `an_uncoloured_cell_that_sets_a_transfer_function_is_ignored` is the check, paired with `the_same_transfer_function_applies_to_a_coloured_cell` so that the first is a statement about this clause rather than about §10.5 being broken. **And it stops at a soft mask's group, which it did not until the two-hundred-and-thirty-seventh session.** The clause's own reason bounds it: the restriction is for \"graphical figures whose colours shall be specified separately each time they are used\", and a soft mask is not painted at all — §11.6.5.2 takes the luminosity of its group's result and uses it as alpha, which is NOTE 1's reason for exempting a stencil word for word. Leaving the flag set was destructive in two directions: a `/Luminosity` mask's values *are* its group's colours, and skipping the group's images leaves a mask of zero that erases the glyph. `issue19634.pdf` is the witness — a Skia blur test whose red text is a `d1` glyph procedure that sets a luminosity mask and fills one rectangle — and it drew none of that text, in silence, with all 51 of the page's commands present. Ink 2.87 against `mupdf`'s 7.63; 8.03 after. What still propagates is a Form XObject the glyph *invokes*, which is painted in the glyph's colour and is what the clause's second bullet is about. ADR 0173. **`cs` and `CS` find their space by bytes**, since the six-hundred-and-fourth session: `ColourSpace::parse` turned the operand into a `String` before matching the device families and before probing `/ColorSpace`, so a name outside UTF-8 reached no space at all. The families are ISO's own names and are matched against byte literals; the resource name is the document's and is probed with `get_by_name` (§7.3.5, ADR 0439)."

[[clause]]
clause = "8.7"
title = "Patterns"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/tiling.rs::a_tiling_pattern_repeats_its_cell_across_the_filled_path", "crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints"]
note = "Both pattern types, reviewed as a family in the twenty-eighth session. A tiling pattern is a content stream replayed inside a clip; a shading pattern is a paint that travels into the display list as one. The two are drawn in completely different ways and the subclauses below say what each one reads."

[[clause]]
clause = "8.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/tiling.rs::a_tiling_pattern_repeats_its_cell_across_the_filled_path", "crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints"]
note = "Descriptive: patterns are Type 1 (tiling) or Type 2 (shading), and both are implemented."

[[clause]]
clause = "8.7.2"
title = "General properties of patterns"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/tiling.rs::a_pattern_inside_a_form_is_anchored_to_the_forms_space", "crates/pdf-model/tests/tiling.rs::a_shading_pattern_inside_a_cell_is_anchored_to_the_cell", "crates/pdf-model/tests/shadings.rs::a_pattern_in_an_annotation_appearance_is_placed_in_the_appearances_own_space", "crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints"]
note = "The sentence that decides where every gradient on a page lands: \"changes to the page's transformation matrix that occur within the page's content stream, such as rotation and scaling, have no effect on the pattern; it maintains its original relationship to the page no matter where on the page it is used\". So a pattern is positioned by its `/Matrix` composed with the *parent content stream's default* space, which the interpreter carries as `base`, and never with the transform in force at the fill. Inside a form XObject the parent is the form, \"that is, the form coordinate space at the time the form is painted with the Do operator\", and that is what `base` holds while a form is being run — **since the fifty-second session; this row claimed it for twenty sessions while `base` stayed the page's throughout**. `issue6231_1.pdf` is what found it: a TeX plot whose surface is a type 5 shading pattern inside a form, positioned 180 points below and 140 to the left of where it belonged, so every triangle fell outside the clip and the page drew its axes and nothing else, silently. The row now names a test that fails if the claim stops being true, which is what `FILE_ONLY_EVIDENCE_CEILING` is counting. **And the claim was wrong a second time, in the hundred-and-ninety-ninth session, one path over.** §12.5.5 makes an annotation's appearance stream a form XObject and `content.rs` says so in a comment, but `draw_appearance` is a second way into a form's content and it never set `base`, so a pattern inside an appearance was positioned by the *page's* default space. `issue7821.pdf` is a stamp whose rounded box is filled by a `PatternType 2` axial shading: the page's crop box starts at x 445.966 and the shading's `/Coords` run 163.729 to 315.349, so the axis fell entirely off the visible page and `/Extend [true true]` painted the box one flat colour that looked exactly like a fill anybody would believe. The lesson is the one the fifty-second session already paid for: **a rule about \"the parent content stream\" needs a test per way of becoming a parent**, and there were two. ADR 0160. **And the claim was wrong a third time, one clause of nesting deeper, in the five-hundred-and-ninety-fifth session**: \"[a] pattern can be used within another pattern\", the sentence going on to give the inner pattern's matrix its relationship to the pattern space of the *outer* pattern — and a pattern named inside a §8.7.3.1 tiling cell was being anchored to the *page*, which makes the tiles differ from one another and so contradicts §8.7.3.1's own picture of \"identical copies\" of one glass tile. `issue8565.pdf` is the witness and it took ADR 0430 to make it visible: while each site re-interpreted the cell, every site re-derived the same page-anchored gradient and whichever site sat at the pattern origin looked right. `run_cell` swaps `base` for the cell's space exactly as `form` swaps it for the form's, and `a_shading_pattern_inside_a_cell_is_anchored_to_the_cell` fails in both directions. **Three ways of becoming a parent, three sessions apart, one sentence** — which is the fifty-second session's lesson stated a third time. `/PatternType` selects between the two kinds; the NOTE says PDF does not support PostScript's define-here-use-there, and neither does this."

[[clause]]
clause = "8.7.3"
title = "Tiling patterns"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/tiling.rs::a_glyph_filled_with_a_tiling_pattern_is_tiled", "crates/pdf-model/tests/tiling.rs::a_cell_is_clipped_to_its_bounding_box"]
note = "Both paint types, and — since the fifty-third session — every *fill* a tiling pattern can be the colour of, glyphs included: §8.7.2's \"All patterns shall be treated as colours\" makes text no different from a path, and `pattern_text_embedded_font.pdf` was drawing one of its two lines and leaving the other blank. `partial` for the half that is left: a **stroke** whose colour is a tiling pattern would be the cell replayed across the stroked outline, and that outline is the backends' to compute (§8.4.3, ADR 0028), so there is no path here to tile. One corpus document does it and is reported rather than stroked in whatever solid colour was last set — `scorecard_reduced.pdf`, re-counted off the corpus gate's own output in the three-hundred-and-seventy-fourth session and still exactly one. See the subclauses."

[[clause]]
clause = "8.7.3.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/repeat.rs"]
test = ["crates/pdf-model/tests/tiling.rs::a_cell_is_clipped_to_its_bounding_box", "crates/pdf-model/tests/tiling.rs::a_cells_operators_are_read_once_and_not_once_per_site", "crates/pdf-model/tests/tiling.rs::each_site_is_the_cells_marks_displaced_by_the_lattice", "crates/pdf-model/tests/tiling.rs::a_rule_spanning_its_whole_cell_deposits_the_ink_its_geometry_states", "crates/pdf-model/tests/tiling.rs::a_rule_stated_at_both_cell_edges_weighs_one_rule", "crates/pdf-model/tests/tiling.rs", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_tiling_pattern_draws_its_prefix_and_reports_the_shortfall"]
note = "Table 74's entries: `/PaintType` selects coloured or uncoloured, `/BBox` clips the cell — **since the fifty-first session, and this row asserted it for twenty sessions while the code did not do it**; `Tiling` carried no box at all, and `tiling-pattern-large-steps.pdf` was contradicted by one tile differing 128 levels of 255 because a cell painting to x = 4000 inside a box ending at 3950 ran on to the end of the page. The sentence is one line — \"These boundaries shall be used to clip the pattern cell\" — and the clip is per *cell*, because content past one cell's box would otherwise spill into the next cell's or into the gap `/XStep` leaves between them. A note in this ledger is a claim; only a test makes it a fact. **And a clip that removes nothing is not applied, since the hundred-and-eighty-eighth session.** The clause's sentence is about *geometry*, and applying it where the cell draws nothing outside its box removes coverage rather than geometry: a clip mask is anti-aliased, so a mark lying on the boundary keeps a fraction of the boundary pixel and the neighbouring cell keeps the rest, and two fractions composite as `1 − (1−a)(1−b)` rather than adding. `issue16038.pdf` rules a grid with a line spanning exactly its own cell and lost **15% of the ink its geometry states** to that (`AMBIGUOUS_TILING_CELL_CLIP`, ADR 0155). The first cell decides for all of them — they are one figure translated — and it is decided *after* the cell is drawn, by `pdf_render::outline::marked_bounds`, because a cell's extent is not known until its content stream has run and running it twice would double the readback and the text layer. Conservative wherever it cannot tell: a command whose extent cannot be bounded, or whose clip is a chain the cell's own content built on the box, keeps the clip for the whole tiling. The clip stays where it is load-bearing, which `a_cell_is_clipped_to_its_bounding_box` is the standing check on. **And where the box *does* cut a mark, the second question is whether the cell states that mark again a whole `/XStep` or `/YStep` away, since the three-hundred-and-seventy-fourth session.** A producer draws a continuous rule out of a repeating figure by putting it on the box's edge and stating it twice — once at the bottom of the cell and once at the top — so each cell contributes half and the halves meet in geometry; on the raster they composite as `1 − (1−a)(1−b)` and the rule comes out 13% light. §11.6.2 forbids that outright, because §11.6.7 makes the whole tiling one object's paint: \"Portions of an object shall not be composited with one another\". So the two statements are recognised as **one mark of the tiling**, one of them is kept, the box comes off it and it is drawn whole — no buffer, no accumulation, nothing snapped to the grid. `pdf_render::repeat::repeated_subpaths` carries the conditions that make the painted set identical and refuses where they do not hold; ADR 0213 states the argument. `/XStep` and `/YStep` space the tiles, `/Resources` names what the cell draws and `/Matrix` places it. The clause's four steps are what the interpreter does — save, install the state in force at the beginning of the parent content stream with the current transform altered by the pattern matrix, run the cell, restore. **`/TilingType` is not read**, and this row is `partial` for it: all three values describe *adjustments to the device pixel grid* — constant spacing by a multiple of a device pixel, no distortion, or constant spacing with more distortion allowed — and this renderer distorts nothing and snaps nothing, which is value 2's behaviour whatever the file asks for. 21 corpus documents ask for something else. A second departure is recorded here rather than hidden: the clause says `/XStep` and `/YStep` \"shall not be zero\", and a zero or absent step falls back to the cell's own size, because the alternative is an infinite loop or an unpainted area. **`MAX_TILES` bounds how many cells one fill may replay, and the four-hundred-and-thirty-fifth session established that it is the only thing bounding that loop.** A cell's content runs through the same interpreter, so its operators count against `MAX_OPERATIONS` — but an *empty* cell executes no operator, and the trip count is then whatever `/XStep` and `/YStep` state. Measured with the bound lifted: 1 000 000 empty tiles interpret in **889 ms reporting nothing**, 0.89 microseconds apiece, and a `/XStep` of 0.001 over a 600-unit fill states 3.6 x 10^11 of them — about four days. The 48 documents of 65 944 that reach the bound all terminate when it is lifted (0.06 s to 14.2 s, wanting 4104 to 895 500 tiles) and 14 of the 48 want fewer than twice it, so the population is legitimate hatching; it is left at 4096 because a larger *count* is no safer than this one — `7680183.pdf` wants 42 282 tiles and takes 14.2 s while `2760154.pdf` wants 765 440 and takes 8.7. A bound on the work rather than the count is what it should become, and `doc/todo/49` carries that. ADR 0271, `tests/hostile_budgets.rs`. **And the cell's own stream is reported where it decoded only part-way**, since the five-hundred-and-twenty-fourth: the clause makes the cell a content stream — \"[t]he appearance of the pattern cell shall be defined by a content stream containing the painting operators needed to paint one instance of the cell\" — so §7.8.2's prefix rule applies, and a shorter cell is replicated at the file's own `/XStep` and `/YStep` without anything standing in place of the marks the damage took. No document on this disk carries one, which is why the pair of fixtures is hand-built. ADR 0359. **And since the five-hundred-and-ninety-fifth session the cell is interpreted *once* and every other site is its commands displaced** — which is the clause's own construction rather than an optimisation of it: the cell is \"the painting operators needed to paint **one** instance of the cell\", and the tiling is \"identical copies\" of one glass tile laid down in an array. `pdf_render::Cell` carries the copy and states what makes it an equality — one translation between sites, §11.6.7's initial graphics state at each, and a display list whose every command carries its own absolute geometry, so a clip or a soft mask the cell built travels with it while one already in force is shared. What a copy is not is bit-identical: the displacement is added to a translation the cell's matrices already accumulated, so 18 of 974 corpus pages moved in the last `f32` digit, 16 of them pixel-identical at 2× and the other two differing by one level in 65 and 18 channel bytes. **A bomb hidden in a cell was what asked for this**: it cost 1055 MB, 1.27 s and *no report at all* while the cell was re-read per site, and costs **9.4 MB, 0.12 s and `MAX_OPERATIONS`** now. Ordinary tiling pages cost a tenth of the instructions (`issue2177.pdf` −94.1%). Each copy is charged to `MAX_OPERATIONS`, the budget the re-reading used to spend, so nothing that drew before reaches the bound now. **ADR 0427's exception therefore came off**: the cell's decode is windowed like the other three of §7.8.2's nested streams, because a window is now inflated once rather than once per site, and `HeldContent` is deleted. **And a cell whose stream cannot be decoded at all is reported** — `undecodable tiling pattern /P0` — where it used to leave the page `complete` with the pattern unpainted. ADRs 0427 and 0430."

[[clause]]
clause = "8.7.3.2"
title = "Coloured tiling patterns"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/tiling.rs::a_coloured_cell_starts_from_the_streams_initial_colours_rather_than_the_fills"]
note = "`/PaintType 1`: the cell's content stream sets its own colours, starting from \"the ones initially in effect in the pattern's parent content stream\" rather than from the colour in force at the fill — the same rule as the pattern matrix, and for the same reason. The clause's other `shall` is on the operand — \"[t]his name shall be the key of an entry in the Pattern subdictionary of the current resource dictionary\" — and a name that is not is reported since the four-hundred-and-nineteenth session, because §8.6.8 leaves a `Pattern` space's initial \"pattern object that causes nothing to be painted\" in force and every fill after it marks nothing (ADR 0255). No corpus document reaches it, which is said out loud rather than around."

[[clause]]
clause = "8.7.3.3"
title = "Uncoloured tiling patterns"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/tiling.rs::an_uncoloured_pattern_takes_its_colour_from_the_operator", "crates/pdf-model/tests/tiling.rs::an_uncoloured_cell_that_sets_a_colour_is_ignored"]
note = "PaintType 2 cells take their colour from the operands beside the pattern name in the underlying space, and — since the tenth session — the clause's other half holds: a cell whose stream does specify a colour has it ignored per §8.6.8, rather than obeyed."

[[clause]]
clause = "8.7.4"
title = "Shading patterns"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints", "crates/pdf-model/tests/shadings.rs::an_axial_shading_runs_from_its_first_colour_to_its_second"]
note = "All seven shading types, on both backends, as a pattern and by `sh`. See the subclauses."

[[clause]]
clause = "8.7.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_shading_pattern_fills_only_the_path_it_paints"]
note = "Table 75's `/PatternType`, `/Shading` and `/Matrix` are read, and the clause's closing paragraph — that a shading pattern paints a path, a glyph or an image mask alike, \"the geometry of the gradient fill is independent of that of the object being painted\" — is why the shading carries its own transform into the display list rather than the path's. **`/ExtGState` is not read**: a graphics state parameter dictionary \"to be put into effect temporarily while the shading pattern is painted\". No corpus document writes one, measured by walking every Type 2 pattern dictionary in all 974, which is why it is recorded here rather than reported."

[[clause]]
clause = "8.7.4.2"
title = "Shading operator"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/crop.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/shadings.rs::an_axial_shading_runs_from_its_first_colour_to_its_second", "crates/pdf-render/src/crop.rs::a_page_sized_rectangle_is_cropped_to_the_room_the_mask_leaves"]
note = "`sh` \"applies the corresponding gradient fill directly to current user space\" and \"works without reference to the current colour in the graphics state\", so it is drawn as a fill of the page rectangle whose paint is the shading, with the shading's transform being the current one. §11.6.4.4's non-stroking alpha applies to it as to any other non-stroking painting operation. **Table 76's last sentence is a performance warning and it was earned** — an unbounded shading \"paints the shading's gradient fill across the entire clipping region, which may be time-consuming\" — and the three-hundred-and-ninety-ninth session measured what it costs a rasteriser that takes the clipping region as a *mask*: the shader runs over the page-sized path and the mask multiplies afterwards, so `bug1721218_reduced.pdf`'s 3490 `sh` operators shaded 10.4 M pixels per render to keep 85 608. `pdf_render::cropped_rectangle` narrows a rectangular fill to the rectangle its mask can mark, which is the clipping region the table names, and the page halves with its ink unchanged to the byte (ADR 0236). Across the corpus's first pages, 49 documents carry a shading fill and **99.7% of their shaded pixels lie outside the clip**."

[[clause]]
clause = "8.7.4.3"
title = "Shading dictionaries"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_shadings_bbox_clips_what_it_paints"]
note = "Table 77's `/ShadingType` and `/ColorSpace` are required and read; a `Pattern` space is refused as the table requires. **`/BBox` landed in the twenty-eighth session** — \"applied as a temporary clipping boundary when the shading is painted, in addition to the current clipping path\", in the shading's *target* coordinate space, so it nests inside the state's clip and is carried by the caller's transform rather than the shading's own. Five corpus documents write one and not one changes by a pixel, so the fixture in `shadings.rs` is the whole of what defends it. Not implemented: `/Background`, which fills \"those portions of the area to be painted that lie outside the bounds of the shading object\" and only \"when the shading is used as part of a shading pattern, not when painted directly with the sh operator\" — 2 corpus documents write one. `/AntiAlias` is read nowhere and is a hint with a default of false, in the same class as §10.7.2's flatness; 19 documents set it."

[[clause]]
clause = "8.7.4.4"
title = "Colour space: special considerations"
status = "partial"
code = ["crates/pdf-model/src/shading.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_shading_in_another_colour_space_is_converted", "crates/pdf-model/tests/shadings.rs::a_mesh_with_a_function_interpolates_the_parameter"]
note = "The clause's licence to interpolate is what `Ramp` is: \"PDF processors may actually compute colour values only for some subset of the points in the target area, with the colours of the intervening points determined by interpolation\", bounded by §10.7.3's smoothness tolerance — which `Ramp::resolution_for` reads. (**This note said the tree \"fixes at 256 samples rather than reading\" it**, which stopped being true when §10.7.3's `/SM` was read.) Then four rules, one per family of colour space. A **device** space may be converted \"at any time (before or after any interpolation on the colour values in the shading)\", which is what this tree does. An **Indexed** space's values \"shall be immediately converted to the base colour space\", which is what the mesh reader and `ColourSpace::to_rgb` do. A shading with a `/Function` interpolates the *parameter* and calls the function afterwards — NOTE 1 and NOTE 3 — and since ADR 0292 that is true of the mesh types as well as of types 1 to 3. **The departure left is a mesh without a function in a CIE-based, `Separation` or `DeviceN` space**, where \"all gradient fill calculations shall be performed in that space\" and conversion shall happen \"only after all interpolation calculations have been performed\": a vertex's components become device RGB when the mesh is read, so the interpolation across the triangle happens in RGB. `examples/mesh_census` counts the population that departure can reach — of the corpus's 28 mesh shadings, 16 are `DeviceCMYK` and 9 `DeviceRGB`, which the first rule exempts, and the **3 that are not are `DeviceN`** — 2 in `personwithdog.pdf` and 1 in `bug1703683_page2_reduced.pdf`, and all three tint transforms are type 4 functions. (**The note said all three were `personwithdog.pdf`'s** until the census was re-run rather than quoted.) Closing it means interpolating components rather than colours, which is a display list carrying an n-component corner and a colour space, or a subdivision in `pdf-model` fine enough that the residual is under a level."

[[clause]]
clause = "8.7.4.5"
title = "Shading types"
status = "implemented"
code = ["crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_function_based_shading_maps_its_domain_through_its_matrix", "crates/pdf-model/tests/shadings.rs::an_axial_shading_runs_from_its_first_colour_to_its_second", "crates/pdf-model/tests/shadings.rs::a_radial_shading_grades_between_its_two_circles", "crates/pdf-model/tests/shadings.rs::a_triangle_mesh_puts_its_corner_colours_at_its_corners"]
note = "All seven, on both backends. See the subclauses."

[[clause]]
clause = "8.7.4.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs::an_unknown_shading_type_is_reported"]
note = "A `/ShadingType` outside 1 to 7 is reported by name rather than drawn as something plausible."

[[clause]]
clause = "8.7.4.5.2"
title = "Type 1 (function-based) shadings"
status = "implemented"
code = ["crates/pdf-model/src/shading.rs", "crates/pdf-render/src/shading.rs", "crates/pdf-render/src/program.rs", "crates/render-cpu/src/shading.rs", "crates/render-quorra/src/scene.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_function_based_shading_maps_its_domain_through_its_matrix", "crates/pdf-model/tests/shadings.rs::a_function_based_shadings_domain_is_a_parallelogram_and_marks_nothing_outside_it", "crates/pdf-model/tests/shadings.rs::a_function_based_shadings_discontinuity_lands_at_the_device_pixel", "crates/pdf-model/tests/shadings.rs::a_function_based_shadings_grid_is_the_same_however_it_is_divided", "crates/pdf-model/tests/shadings.rs::a_clipped_grid_is_the_whole_grids_cells_bit_for_bit", "crates/pdf-model/tests/shadings.rs::a_window_of_a_magnified_page_draws_what_the_whole_page_has_there", "crates/render-cpu/tests/sampled_shading.rs", "crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_a_sampled_shading", "crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_a_device_evaluated_function_shading", "crates/render-quorra/tests/headless_quorra.rs::quorra_names_the_program_it_will_not_bound_and_draws_the_grid", "crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_the_witness_pages_folded_program", "crates/render-quorra/tests/headless_quorra.rs::a_shading_the_device_could_not_colour_carries_no_program", "crates/render-gpu/tests/headless_gpu.rs::the_gpu_refuses_a_sampled_shading_by_name"]
note = "Table 78: `/Domain` defaulting to the unit square, `/Matrix` mapping the domain into the target space, and `/Function` evaluated over it. The `/Matrix` is composed ahead of the caller's transform so the display list carries one transform per shading — which is also why Table 77's `/BBox`, stated in the *target* space, is applied by the caller rather than here. **The clause's second half was missing until the hundred-and-eightieth session and this row said `implemented` throughout**: the matrix \"maps the domain rectangle into a corresponding rectangle **or parallelogram**\", and points outside it \"shall be left unpainted\" — where the backend's sampled pattern pads instead, so a rotated `/Matrix` painted a square. `Interpreter::domain_clip` composes the parallelogram as a clip, at both routes into a type 1 shading, exactly as `/BBox` is composed. `function_based_shading.pdf` is the witness and ADR 0150 the argument. `/Background` stays unimplemented and reported, so \"left unpainted\" is the clause's other branch rather than a choice between them. **\"[T]he colour at every point in the domain\" is resolved at the device since the five-hundred-and-fourth session** (ADR 0339): the display list carries the function's ingredients on `pdf_render::DeferredColours` — ADR 0210's shape on `Paint::Shading` — and the grid is `Grid::for_placement`'s for the domain's own placement, derived once in `Shading::sampled_at`, bounded by `MAX_FUNCTION_CELLS` under §10.7.3's \"internal limits\". Until then the grid was fixed at 128 cells at interpret time, which blurred any function detail finer than 1/128 of the domain and froze the first frame's resolution into every zoom. The Vello backend refuses the kind by name and the CPU oracle draws it; quorra, which the window presents with, draws it as an image clipped to the path. **Resolving at the device is a per-pixel cost and the five-hundred-and-twenty-ninth session paid it down without moving one bit of it** (ADR 0364): the evaluation writes into a reused buffer rather than allocating per cell — a type 4 program runs *on* that buffer, since §7.10.5 leaves its outputs on the operand stack — and the grid is divided across rows above 4096 cells and off rayon's own pool. The owner's `tmp/pi.pdf` went from 1059–1175 ms of scene to 105–284; every gate, and seven rasters at four scales, are identical. The grid's resolution and the cell centre are ADR 0339's and are untouched; what an *adaptive* grid would have to prove about a discontinuity is priced in ADR 0364 and not taken. **And since the five-hundred-and-forty-first session the colours have a *second* statement, which is a program rather than a grid** (ADR 0376): where the `/Function` is a single §7.10.5 type 4 over `DeviceGray` or `DeviceRGB`, and its own `/Domain` contains the shading's rectangle, `pdf_model::shading::device_program` lowers it to `pdf_render::ShadingProgram` and `render-quorra` hands it to the device as quorra's `Paint::Function`, evaluated per fragment. The conditions are exactly the places where the grid's colour and the program's would part — another colour space is a *conversion* (§10.4.2.5 for `DeviceCMYK`, §8.6.5.2 for a calibrated grey) rather than a device component, and a narrower function `/Domain` would be clipped on one path and not the other — and every one of them, plus each of the device's own fifteen refusal grounds, falls back to the grid above and is named on the frame line. **The clause's arithmetic is not the same on the two paths and cannot be**: §7.3.3 defers a number's precision to the machine and §7.10.5.2 defers Table 42 to PLRM3, which defers again, so what the device promises is a bounded difference *of colour* — §10.7.3's own currency — and it refuses outright any program where an inexact operator reaches a comparison or a truncation, which is what would make it a difference of *branch*. Measured over 1 479 corpus files by `cargo run --release -p render-quorra --example function_paint_census`.\n\n**And the two witnesses that were refused are no longer refused, on no weakening of that rule** (ADR 0406, session 572). The project owner reported that zooming `tmp/pi.pdf` drops frames; a callgrind profile put 94.09% of the page in `Function::eval_into`, and reading the program said why — the BBP series it evaluates per pixel is a *constant*, every operand of every `div` and `exp` in it a literal. `pdf_model::function::fold_constants` computes it once at compile time (§7.10.5's row has the construction), after which no operator WGSL 15.7.4.1 gives an error budget is left in the program the device is handed. A zoom step went 96.1 ms → 3.5 ms and 2.5 MB of uploaded grid → none; over 1 251 documents the census went 8 device-evaluated shadings to 10 and two `Agreement::Unbounded` refusals to none, leaving one typing refusal that is correct. `cpu_and_quorra_agree_on_the_witness_pages_folded_program` asserts the pair that matters — that the device took the program, and that its raster still agrees with the oracle's, which on a page of digits is a difference of branch if it fails. What is *not* claimed is in section 5 of `doc/QUORRA_FUNCTION_PAINT_EXACTNESS.md`.\n\n**The grid is now cut to the part of the domain a target can sample** (ADR 0408, session 573), which is the lever ADR 0406 section 7 named and did not take. A viewer past the magnification at which a page fits does not rasterise the page — `viewer-ui`'s surface builds a *window*-sized `TargetSpec` at the page's own transform — so the grid `Grid::for_placement` derives from the domain grows as the square of the zoom while the window does not. Measured rather than assumed: `cargo run --release -p pdf-model --example shading_grid_census` reads 55.7% of the resolved cells inside a 900x1100 window at 1x and 5.5% at 8x, and the wasted evaluation is the whole rest. `pdf_render::Patch` names the block — the target's rectangle mapped back through the placement, as a box in the domain's unit square — and `ColourGrid::covers` says which part of the domain came back, so both backends place a block and a whole grid by one expression and neither reads `/Domain` for it. **The exactness is structural rather than measured**: a cell's centre is decided by its index in the *lattice*, and the lattice is the grid the whole domain would have had, budget included — so a cell inside the block carries the colour it would have carried unclipped, bit for bit, which `a_clipped_grid_is_the_whole_grids_cells_bit_for_bit` asserts on `f32::to_bits`. The one place it is conservative is a cell of margin on each side, for the bilinear filter both backends read the grid with; deleting it moves 151 pixels of the window in `a_window_of_a_magnified_page_draws_what_the_whole_page_has_there`, whose every constant is chosen to make it. §8.7.4.5.4's radial raster and §8.7.4.5.5's mesh were already cut to the target and this is the type that was not."

[[clause]]
clause = "8.7.4.5.3"
title = "Type 2 (axial) shadings"
status = "implemented"
code = ["crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs::an_axial_shading_runs_from_its_first_colour_to_its_second", "crates/pdf-model/tests/shadings.rs::a_shading_that_does_not_extend_paints_nothing_beyond_its_ends", "crates/render-cpu/tests/shading_placement.rs"]
note = "Table 79 in full: `/Coords` as the axis, `/Domain` defaulting to `[0 1]`, `/Function` sampled across it, and `/Extend` deciding what happens beyond each end — both halves independently, which is what the clause's two booleans mean and what a single flag would get wrong on one document in four. `shading_placement.rs` pins the geometry at three scales against this clause, and exists because both backends once applied the page transform to the paint as well as to the path (trap 2)."

[[clause]]
clause = "8.7.4.5.4"
title = "Type 3 (radial) shadings"
status = "implemented"
code = ["crates/pdf-model/src/shading.rs", "crates/pdf-render/src/shading.rs", "crates/render-cpu/src/shading.rs", "crates/render-gpu/src/shading.rs", "crates/render-quorra/src/scene.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_radial_shading_grades_between_its_two_circles", "crates/pdf-render/src/shading.rs::the_greatest_root_a_cone_admits_is_not_always_the_greater_root", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_radial_cone", "crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_a_radial_cone"]
note = "Table 80: two circles from `/Coords`, `/Domain`, `/Function`, and `/Extend` for each end, all read, with the ramp sampled from the function. **`implemented` since the two-hundred-and-thirty-second session, and it was `partial` for twenty-six because of one sentence.** \"[I]f a point lies on more than one blend circle, its final colour shall be that of the last of the enclosing circles to be painted, corresponding to the greatest value of s\" — the *greatest admissible* s, and where the greatest root of `|P - c(s)| = r(s)` falls outside `/Extend` the other root still paints. A two-point conical gradient solves for one root and clamps it with a spread mode, so all three backends left the disc of the ending circle unpainted on §8.7.4.5.4 NOTE 3's cone. `pdf_render::blend_parameter` now solves the quadratic and takes the greatest root that `/Extend` admits and whose `r(s)` is not negative, with the degenerate linear case where the circles are internally tangent; `pdf_render::RadialRaster` evaluates it at each device pixel's centre and all three backends draw those same bytes through the shape, exactly as they already did for §8.7.4.5.5's mesh. **The exact evaluation is paid for only on a cone, and that is derived rather than tuned**: the quadratic's leading coefficient is `|c1 - c0|^2 - (r1 - r0)^2`, and where it is not positive `|p - c(s)| - r(s)` is convex with limits of opposite sign, so it has exactly one root and there is nothing for the clause's rule to choose between — a gradient cannot be wrong there. `radial_gradients.pdf` pages 4 and 5 went from worst tile 108.49 and 109.58 against a bound of 40 to 15.78, and `AMBIGUOUS_RADIAL_CONE` now records a departure that is closed. What the clause leaves to the device is unchanged and is §10.7.4's: the raster is point-sampled, so the shading's own boundary is hard where the *shape*'s is antialiased, which is `MeshRaster`'s documented trade one type over."

[[clause]]
clause = "8.7.4.5.5"
title = "Type 4 (free-form Gouraud-shaded triangle mesh) shadings"
status = "implemented"
code = ["crates/pdf-model/src/mesh.rs", "crates/pdf-model/src/shading.rs", "crates/pdf-render/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_triangle_mesh_puts_its_corner_colours_at_its_corners", "crates/pdf-model/tests/shadings.rs::a_mesh_with_a_function_interpolates_the_parameter", "crates/pdf-model/tests/shadings.rs::a_truncated_mesh_is_reported", "crates/pdf-render/src/shading.rs::a_parametric_mesh_interpolates_the_parameter_and_not_the_colour"]
note = "Table 81's `/BitsPerCoordinate`, `/BitsPerComponent`, `/BitsPerFlag`, `/Decode` and optional `/Function`, with the three vertex flags — a new triangle, or one sharing the previous triangle's second and third or first and third vertices — and the byte padding \"[e]ach set of vertex data shall occupy a whole number of bytes\" states. The interiors are the clause's own Gouraud interpolation, evaluated by `MeshRaster` at each device pixel's centre so that both backends draw identical bytes (ADR 0051). **This row described a construction that had been gone for four hundred sessions**: it said each triangle was subdivided until its corner colours agreed, filled flat and grown \"by a measured 0.8-pixel overlap to close the seams\", leaving \"a faint lattice\" that made `mesh_shading_empty.pdf` contradicted — and the forty-third session replaced the whole of it with the per-pixel raster, after which that page agrees. A note describing *how* something is drawn decays at the pace of the code and not of the clause. **And the sentence it did not have was the `shall`**: \"[a]ll linear interpolation within the triangle mesh shall be done using the t values. After interpolation, the results shall be passed to the function(s) specified in the Function entry to determine the colour at each point\". Where a mesh states a `/Function` this tree called it at each *vertex* and interpolated the colours it returned, which is the same picture only where the function is a straight line and nothing reported the difference. `pdf_render::Corners` carries the parameter now and the function crosses as a `Ramp`, sampled across its own breakpoints at §10.7.3's resolution, exactly as an axial shading's is — `breakpoints_over` is the one construction both use. Table 81's own `/Function` row states the same order a second time: \"[t]he designated function(s) shall be called with each interpolated value of the parametric variable to determine the actual colour at each point\". ADR 0292."

[[clause]]
clause = "8.7.4.5.6"
title = "Type 5 (lattice-form Gouraud-shaded triangle mesh) shadings"
status = "implemented"
code = ["crates/pdf-model/src/mesh.rs"]
test = ["crates/pdf-model/tests/shadings.rs::a_lattice_mesh_triangulates_between_its_rows"]
note = "Table 82's `/VerticesPerRow` with the same vertex encoding as Type 4 and no flags, triangulated between consecutive rows. **`partial` until the four-hundred-and-fifty-seventh on \"§8.7.4.5.5's interpolation departure, which it shares\"** — the subdivision the forty-third session removed, and after that the parametric order ADR 0292 corrected. Neither is left, and the clause states nothing else: a lattice's own geometry is the triangulation, which the test names. `issue6231_1.pdf` is the corpus's one lattice and its `/Function` is a type 3 stitching function, so it is also the one corpus witness to the order of operations ADR 0292 is about — the page draws a smooth surface where it drew a lattice of yellow streaks."

[[clause]]
clause = "8.7.4.5.7"
title = "Type 6 (Coons patch mesh) shadings"
status = "partial"
code = ["crates/pdf-model/src/mesh.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs"]
note = "Table 83's twelve control points and four corner colours per patch, with the four edge flags that let a patch share an edge and two colours with its predecessor. The surface is evaluated and triangulated here, and a patch's interior carries whichever quantity its corners do — components, or §8.7.4.5.5's parametric value, which ADR 0292 made the thing that is interpolated. **This row said `partial` for \"§8.7.4.5.5's interpolation departure\" as well**, which was the subdivision the forty-third session removed. What is left is its own: the clause defines the patch's interior by a bilinear surface between its Bézier boundaries, and a triangulation is an approximation of that surface whose fineness this tree fixes at `PATCH_STEPS` rather than deriving from §10.7.3's smoothness tolerance."

[[clause]]
clause = "8.7.4.5.8"
title = "Type 7 (tensor-product patch mesh) shadings"
status = "partial"
code = ["crates/pdf-model/src/mesh.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/shadings.rs"]
note = "Sixteen control points rather than twelve; the four interior ones are read and used. `partial` for the one reason §8.7.4.5.7 has left — a tessellation whose fineness is fixed rather than derived from §10.7.3 — the interpolation half having been closed by ADR 0292."

[[clause]]
clause = "8.8"
title = "External objects"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_forms_bounding_box_clips_what_it_draws"]
note = "Both of the two the clause names, and in PDF 2.0 there are only two: \"an image XObject … represents a sampled visual image\" and \"a form XObject … is a self-contained description of an arbitrary sequence of graphics objects\". Table 86 says the same of `Do` — \"the effect of Do depends on the value of the XObject's Subtype entry, which may be Image … or Form\". PostScript XObjects are gone from this edition, so a `/Subtype` that is neither has no defined effect and is **reported by name** rather than ignored. This note said the opposite in its first draft, on the strength of a clause of an earlier edition; the code was checked and the note was wrong. **Table 86's other two requirements are enforced and reported since the four-hundred-and-nineteenth session** — the operand \"shall appear as a key in the XObject subdictionary of the current resource dictionary\" and \"[t]he associated value shall be a stream\" — each of which a file can break and did, in silence, for the whole of this project's life (ADR 0255)."

[[clause]]
clause = "8.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_forms_bounding_box_clips_what_it_draws"]
note = "Introductory: an external object is \"defined outside the content stream and referenced as a named resource\", which is §7.8.3's resource dictionary and the reason `Do` takes a name. Table 86 is here rather than in §8.10, and both of its `shall`s on what the name finds are now reported where a file breaks them: `Unsupported::MissingResource` names the category and says whether the subdictionary lacked the key or the value was not a stream (ADR 0255)."

[[clause]]
clause = "8.9"
title = "Images"
status = "implemented"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-model/src/inline_image.rs"]
test = ["crates/pdf-model/tests/bit_depths.rs::a_row_restarts_on_a_byte_boundary"]
note = "Sampled images in both of the clause's forms — an image XObject and §8.9.7's inline image — with all five bit depths, all four masking mechanisms, §8.9.5.2's `/Decode` in full and §8.9.5.3's `/Interpolate`."

[[clause]]
clause = "8.9.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-model/src/inline_image.rs"]
test = ["crates/render-cpu/tests/image_placement.rs::an_image_lands_where_its_transform_puts_it", "crates/pdf-model/tests/inline_images.rs::an_inline_image_draws_from_its_abbreviated_keys"]
note = "Both of the two means the clause names exist: an image `XObject` painted by `Do`, and an inline image (§8.9.7), which landed in the eleventh session. **The sentence after that one said \"[s]ample components of 1 and 8 bits are unpacked; 2, 4 and 16 are refused and reported\" until the four-hundred-and-forty-second session**, and it was a *neighbour's* claim — it deferred to §8.9.3's row, which carried the same expired sentence, while §8.9's parent row three lines above said \"all five bit depths\". All five are unpacked; §8.9.3 is still the row that owns the packing."

[[clause]]
clause = "8.9.2"
title = "Image parameters"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/render-cpu/tests/image_placement.rs::the_first_row_of_an_image_lands_at_the_top_of_the_unit_square", "crates/pdf-model/tests/image_masks.rs::a_decode_array_of_one_zero_reverses_the_stencil"]
note = "All four interrelated items the clause requires are specified for every image drawn: format from `/Width`, `/Height`, the colour space's component count and `/BitsPerComponent`; the data from the stream; the user-space correspondence from §8.9.4's unit square; and the mapping from sample to colour from `/Decode` and the space. The clause states requirements rather than behaviour, so what it reviews is that nothing is left implicit."

[[clause]]
clause = "8.9.3"
title = "Sample representation"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/bit_depths.rs::a_row_restarts_on_a_byte_boundary", "crates/pdf-model/tests/bit_depths.rs::sixteen_bit_samples_are_two_bytes_most_significant_first", "crates/pdf-model/tests/bit_depths.rs::two_bit_samples_are_four_to_a_byte", "crates/pdf-model/tests/image_masks.rs::a_zero_sample_paints_the_current_colour_and_a_one_leaves_the_page_alone"]
note = "The layout is implemented as stated: a continuous bit stream, high-order bit first, each row starting on a byte boundary with the padding ignored, components interleaved sample by sample, and each unit read as an unsigned integer §8.9.5.2's `/Decode` then maps. **This row said \"only 1- and 8-bit components are unpacked, and 2, 4 and 16 — all legal — are refused\" until the four-hundred-and-forty-second session, and `raw_sample` has read all five since `bit_depths.rs` was written**: \"[t]he value shall be 1 , 2 , 4 , 8 , or (from PDF 1.5) 16\", and the test file's own header opens by saying the refusal is what it replaced. Its `a_row_restarts_on_a_byte_boundary` is the rule no corpus document exercises — three 4-bit samples end four bits into a second byte — which is why the fixtures are synthetic and say so."

[[clause]]
clause = "8.9.4"
title = "Image coordinate system"
status = "implemented"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/render-cpu/tests/image_placement.rs::the_first_row_of_an_image_lands_at_the_top_of_the_unit_square", "crates/render-cpu/tests/image_placement.rs::an_image_lands_where_its_transform_puts_it", "crates/render-cpu/tests/image_placement.rs::an_image_turns_with_its_transform"]
note = "Both backends map image space to the unit square with the clause's own matrix, `[1/w 0 0 1/h 0 1]` composed with the y flip that puts row 0 at the *top* — the coordinate (0,0) of user space corresponds to (0,h) of image space. Pinned at three scales in `image_placement.rs`, because a mirrored mapping is invisible at one scale and in one axis; see trap 2 in doc/HANDOVER.md."

[[clause]]
clause = "8.9.5"
title = "Image dictionaries"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/bit_depths.rs::an_unnamed_depth_is_reported", "crates/pdf-model/tests/decode_arrays.rs::a_general_decode_array_maps_the_samples_linearly_between_its_two_values"]
note = "Table 87 in full for what decides a drawn image; see §8.9.5.1 through §8.9.5.4 for the entries and §8.9.6 for the masks. **How large the images a reader meets actually are is measured rather than assumed** (ADR 0373): `examples/image_region_census` walks every image `XObject`'s `/Width`, `/Height` and `/Filter` chain in every object of a file and then interprets page one for the placements, because §8.9.5.1 puts every image on the unit square and nothing in the dictionary says how large it is drawn. Over 964 pdf.js documents, 489 in `doc/corpora` and a 6622-document SafeDocs sample, **24 image dictionaries state 64 megapixels or more**: 17 `CCITTFaxDecode`, 3 `DCTDecode`, 3 `FlateDecode` and exactly one `JPXDecode` — `issue19517.pdf`'s, the file that provoked the question. The largest images a reader meets are scans of paper in codecs that cannot decode part of themselves, which is what that ADR turns on. **An image is decoded once per page rather than once per `Do` as of ADR 0374**, which is a statement about this clause's cost rather than about its meaning: `image::decode_parts` ran at every painting operation, so `22060_A1_01_Plans.pdf` decoded four 2480x2630 photographs nine times each and spent 58.7 G instructions on page one against 6.5 G now, at 235.6 MB peak resident against 1031.8 MB. `image::RasterCache` holds the rasters under a 64 MiB budget derived from a replay of the corpus's own call sequence, and what makes it sound is a key with more in it than the stream: this clause's raster depends on the resource dictionary in force (§8.6.5.1's named space, §8.6.5.6's `/Default*`), on the fill colour (§8.9.6.2's stencil) and on what the samples composite into (§11.4.7, §11.6.5.1). `tests/image_reuse.rs` is one test per component, each confirmed to fail if its component is dropped. **And a fifth thing is in the key since ADR 0399, which is a bound rather than a claim about correctness**: how the stream is *named*. An `XObject` is named by the address of its allocation, which recurs because §7.8.3's dictionary hands out the same object; §8.9.7's inline image has no such object, so `content::run` built a fresh one at every `BI` and every draw added an entry nothing could ever find. The probe is a linear scan, so a page's cost was quadratic in its own image count: two SafeDocs timetables spent 330.5 G and 71.9 G instructions on page one, 72.7% and 88.5% of it inside `draw_image`, against 1.7 G and 1.0 G in the decode it was looking for. `image::StreamIdentity::Content` names an inline image by a digest of its content with the content compared exactly beside it, and the two pages cost 10.7 G and 6.3 G — every display list byte-identical over 1222 first pages."

[[clause]]
clause = "8.9.5.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-model/src/content/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs", "crates/pdf-model/tests/rendering_intent.rs::an_images_own_intent_turns_black_point_compensation_off", "crates/pdf-model/tests/rendering_intent.rs::an_image_without_an_intent_takes_the_graphics_states"]
note = "Table 87's entries, and which of them are read: `/Width`, `/Height`, `/ColorSpace` (any family but `Pattern`, which the table excludes), `/BitsPerComponent`, `/Decode`, `/ImageMask`, `/SMask`, `/SMaskInData`, `/Filter` and `/DecodeParms`. `/Alternates` is read since the hundred-and-second session — `Interpreter::alternate_image` walks the array, honours each entry's `/OC` and draws the first one shown (§8.9.5.4) — and **this sentence went on calling it unread**, which is the second entry in the same list to do so: `/Interpolate` was unread until the eleventh session and the note says so beside it. Not read: `/Name`, `/StructParent`, `/ID`, `/OPI` and `/Metadata` — the first four are identity and production entries that mark nothing, and the fifth is §14.3.2's XMP, which this tree parses since ADR 0186 and does not read *here*, for the reason §7.7.3.3's row gives: an object-level packet is interchange and no clause in scope consumes one. **`/Mask` is read and applied, in both of its forms, and this sentence said it was \"read only to report it\" for two hundred sessions** — it was true when the row was written and false from the fourteenth, when ADR 0023 landed §8.9.6.3's stencil and §8.9.6.4's colour key. That is the third entry in this one list to be recorded as unread while the tree read it, after `/Interpolate` and `/Alternates`, which the two sentences before this one already say. A list that has been wrong three times about itself is a list to check rather than to read. **Read and kept in the five-hundred-and-twenty-fifth**, with the unread five checked rather than believed: `/Name`, `/StructParent`, `/ID`, `/OPI` and `/Metadata` are quoted by `thumbnail.rs`'s list of the keys a `/Thumb` may not carry and by readers of the other clauses that state the same short names, which is `doc/todo/01`'s one-short-key-three-clauses population. Table 87's `/Type` and `/Subtype` are not on either list because they are the stream's identity — `xobject.rs` reads `/Subtype` to decide this is an image at all — and they are named here because the fifteenth sweep prints an entry no sentence of the row disposes of. **Read again in the five-hundred-and-eighty-second, and the list had a fourth hole — four entries it disposed of neither way**, which is what a list wrong three times about itself is for. `/Intent` (PDF 1.1), `/AF`, `/Measure` and `/PtData` (all three PDF 2.0) are stated by Table 87 and named by no sentence above. Each is read against the code rather than added to a list: **`/Measure` and `/PtData` are unread here and that is a boundary rather than a gap** — the tree's one reader of `\"Measure\"` is §12.10.2's viewport and its one reader of `\"PtData\"` is §12.9's, both of which take the dictionary off a *viewport* rather than off an image, so an image's scale and its geospatial point are §12.9's and §12.10's rows to owe. **`/AF` has a reader and no caller**: `attachment::associated` reads §14.13's array off any dictionary, and the only site in this tree that hands it one hands it the catalog's, so an image XObject's associated files are reachable by nobody — which is `doc/todo/01`'s fifth sweep's shape (the model implements this, and who calls it?) and which §14.13.7's row overstates by saying the entry is \"read by the same function against that dictionary\". **And `/Intent` is the one that can move a pixel.** §8.6.5.8's row already records that this third route to the rendering intent is unread; what neither row said is that the parameter reaches no image sample by *any* route — `image.rs` converts every sample with black point compensation on, as a literal `true` at each of its six `Compositing::paint` calls, and so do `shading.rs`'s ramp and `mesh.rs`'s vertices. So `ri /AbsoluteColorimetric`, which §8.6.5.9 requires to turn compensation off and which this tree obeys for a path and for text, is obeyed for nothing an image, a shading or a mesh paints. The condition for a mark to move is narrow and stated rather than guessed: an intent of `AbsoluteColorimetric` in force, a sample in a CIE-based or ICC space, and a profile whose black point is not the connection space's. The population is smaller still — **0 of the 974 corpus documents and 0 of the 275 under `doc/corpora/` contain the name `AbsoluteColorimetric` at all, and 2 of the 65 944 crawled ones do** (a cleartext grep, so an undercount for a content stream and not for a dictionary) — which is why this is written down with its condition rather than built here. **Built in the six-hundred-and-seventh, and the population is the reason it was worth doing rather than a reason not to.** A count over a corpus ranks a requirement documents exercise; it cannot rank one no document does, and `CLAUDE.md`'s two denominators say which question each instrument answers — so a `shall` declined on a corpus count had been answered for robustness and not for coverage. `Interpreter::image_conversion` reads `/Intent` where the image is drawn (Table 87's default value is the state's intent, and its \"ignored if ImageMask is true\" needs no branch: a stencil's samples carry the fill colour, which was converted under the state's own intent before the decode saw it), and `crate::colour::Conversion` carries the black point that intent decides through `image.rs`, `shading.rs` and `mesh.rs` — so the literal `true` at each of those six `Compositing::paint` calls is gone, and there is no longer a `paint` call that can omit the parameter. It joins `RasterCache`'s key with the compositing target, because one page may draw one stream under two intents. `/AF`, `/Measure` and `/PtData` are unchanged and still stand as this list's remaining three."

[[clause]]
clause = "8.9.5.2"
title = "Decode arrays"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/decode_arrays.rs", "crates/pdf-model/tests/image_masks.rs::a_decode_array_of_one_zero_reverses_the_stencil"]
note = "The clause's linear map in full, as of the twenty-fifth session, with Table 88's defaults per space and its closing sentence's clamp: an output value not permitted for a component \"shall be adjusted to the nearest allowed value\". Held as one lookup table per component, built once per image, because every input the formula takes is fixed by the image dictionary and its domain has at most 2^n points. This row was `partial` for fourteen sessions with three silences under it, and each was larger than the note admitted. **The general map**: only `[1 0]` was applied and every other array ignored, which no corpus document can see — all 974 write Table 88's default or its exact reversal, measured by walking every image dictionary. **The `DCTDecode` route**: it never consulted the entry at all, so `issue7406.pdf` drew a JPEG in complementary colours against all four references. **Table 88's `Lab` row**: a `Lab` image's default decode is `[0 100 a_min a_max b_min b_max]`, and scaling its lightness onto 0.0 to 1.0 like every device space makes the brightest colour it can hold L = 1, which is black. The `Indexed` row is the one the eleventh session already had right, and the formula keeps it right by multiplying before dividing."

[[clause]]
clause = "8.9.5.3"
title = "Image interpolation"
status = "implemented"
code = ["crates/pdf-render/src/paint.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs", "crates/render-quorra/src/scene.rs"]
test = ["crates/pdf-render/src/paint.rs::interpolate_decides_a_magnified_image_and_nothing_else", "crates/pdf-render/src/paint.rs::a_reduction_describes_the_raster_the_averaging_produces"]
note = "Implemented in the eleventh session; before it `/Interpolate` was read nowhere and every image was filtered bilinearly. The clause makes interpolation a hint for the *magnified* case — an image \"whose resolution is significantly lower than that of the output device\" — with a default of false, so `Image::is_smoothed` filters a reduced image and draws a magnified one sample by sample unless the entry asks otherwise. `issue11124.pdf` blurs sixteen samples across a page where three references draw sixteen flat squares; it agreed with the consensus the moment this landed. The clause says nothing about reduction — but the standard does, in §10.7.4, which requires point sampling and forbids averaging over the pixel area. This note claimed for two sessions that reduction was unspecified; it is specified and departed from, which ADR 0025 records as a choice. `/Interpolate` itself is unaffected: it governs magnification only, and §10.7.4 is where the other half lives. **The rule is stated once for two grids since the four-hundred-and-sixty-second session**: `Image::is_smoothed` asks it about an image's own samples and `Image::reduction` asks it about the *reduced* grid, which is a grid no `Image` exists for yet — a backend that keeps a reduced raster across frames has to know how to filter it without holding it. Both go through one function, so an axis whose reduction factor clamps to one, and which is therefore magnified while the other is reduced, cannot be answered differently by the two callers. ADR 0297."

[[clause]]
clause = "8.9.5.4"
title = "Alternate images"
status = "implemented"
code = ["crates/pdf-model/src/content/image.rs", "crates/pdf-model/src/content/xobject.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_hidden_base_image_shows_nothing_and_does_not_reach_its_alternates", "crates/pdf-model/tests/optional_content.rs::a_base_image_with_no_group_shows_the_first_visible_alternate", "crates/pdf-model/tests/optional_content.rs::an_alternate_with_no_group_is_not_shown_and_the_base_image_is", "crates/pdf-model/tests/optional_content.rs::the_selected_alternates_own_image_group_is_not_examined"]
note = "**The algorithm this row describes is Errata Collection 3's, and the four-step one `doc/md/` carries is retired** (Issue #79, `/State` `Review` `Completed`; the carets are on pages 279 and 280 of the sponsored copy and `tools/spec-errata` is what reads them back). Five steps, of which four reach this device: a) a base image whose `/OC` says the content is not visible shows nothing, **terminally** — its `/Alternates` are not examined; b) one whose `/OC` says it is visible is drawn, which is what drawing it is; c) addresses printing and this device is a screen, which is why `/DefaultForPrinting` is read by nothing here; d) a base image stating **no** `/OC` shows the first alternate whose own `/OC` says its content is visible, alternates with no `/OC` are not shown, and the selected alternate's *image* dictionary `/OC` \"shall not be examined\"; e) where c) and d) identify nothing, \"the base image shall be rendered\". `Interpreter::alternate_image` is d) and `xobject.rs` is the rest. **The 2020 step c) contradicted itself** — its selection sentence made an alternate with no `/OC` selectable and its closing sentence said that in that case \"nothing shall be shown\" — and this row carried the choice between the two readings as a documented departure for a hundred and twenty sessions. The erratum deletes the selection sentence and step e) makes the deletion cost nothing. **The rewrite was declined once and the reason was wrong**: the four-hundred-and-seventeenth session read the amended a) as terminal and concluded that it \"would leave the amended d) unreachable for a hidden base\". It does, and that is the amendment rather than a defect in it — a) and b) dispose of every base image that states an `/OC`, so c) and d) open at \"Otherwise\" and belong to a base image that states none. Read that way the five steps are total, disjoint and reachable, which the four were not. Measured: **0 of the 964 openable corpus documents carry an `/Alternates` entry**, so every rule here rests on the clause and on six synthetic fixtures, and no page moves either way. `implemented`: every step the clause states for a screen is executed, and the one case with no step — an alternate dictionary stating no `/Image`, which Table 89 makes required — falls to e) with the document's defect named."

[[clause]]
clause = "8.9.6"
title = "Masked images"
status = "partial"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs"]
note = "Aggregate of the four below, and of the family the fourteenth session implemented. /ImageMask, /SMask and both forms of /Mask now apply. **This row stayed `partial` partly \"for §8.9.6.2's last sentence, which asks interpolation to smooth a low-resolution stencil's edges\" until the four-hundred-and-forty-second session, and §8.9.6.2's own row had answered that sentence** — `Image::is_smoothed` decides it from the placement, and both backends premultiply before they filter, which is what makes the smoothing the mask's rather than the colour's. What the family still owes is two things its members name: §8.9.6.2 refuses a stencil under a *graphics-state* soft mask, which would be two masks on one command, and §8.9.6.4's ranges cannot reach a DCTDecode or JPXDecode image, which is RGBA before the unpacker sees it. Both are reported."

[[clause]]
clause = "8.9.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_zero_sample_paints_the_current_colour_and_a_one_leaves_the_page_alone", "crates/pdf-model/tests/image_masks.rs::an_explicit_mask_paints_the_base_image_only_where_it_marks", "crates/pdf-model/tests/image_masks.rs::colour_key_masking_removes_the_samples_inside_the_range", "crates/pdf-model/tests/image_masks.rs::a_soft_mask_overrides_an_explicit_mask"]
note = "A map rather than a requirement of its own: the clause names the masking mechanisms and hands each to a subclause, and the three `shall`s it states in passing — image data \"shall be used as a stencil mask\", an explicit mask \"shall be used\", a colour range \"shall be masked out\" — are §8.9.6.2's, §8.9.6.3's and §8.9.6.4's. All four mechanisms are implemented: `/ImageMask`, `/Mask` in both its forms since the fourteenth session, and — from the transparent imaging model — `/SMask` and `/SMaskInData`. Each member's own residue is on its own row and not hidden here: §8.9.6.2 refuses a stencil under a *graphics-state* soft mask, which would be two masks on one command, and §8.9.6.4's ranges cannot be applied to a `DCTDecode` or `JPXDecode` image, which has become RGBA before the unpacker sees it. **The first of those said \"refuses a stencil painted with a *tiling* pattern (`doc/todo/20`)\" until the three-hundred-and-seventy-fifth session**, and §8.9.6.2's own row has said the opposite since the two-hundred-and-eighteenth — ADR 0169 handed the stencil to `Interpreter::tile` as the group's soft mask, and deleted `doc/todo/20` in the same commit. Two rows about one mechanism, one corrected and the other left standing, with a pointer to a file that had not existed for a hundred and fifty-seven sessions: `doc/todo/01`'s seventh shape, found by an eighth sweep that checks every path a note cites against the tree. **This row said the graphics state's own soft mask was \"reported rather than applied on 28 corpus documents\" — the sentence §11.6.4.3 retired in the two-hundred-and-first session, still standing here fourteen sessions later, about a mechanism this clause does not even own.** `build_soft_mask` has applied it since the eighteenth session (ADR 0027). Two clauses describing one mechanism, and correcting one left the other lying; the sweep that found it was for the *shape* of the claim — \"reported rather than applied\" as a string — rather than for the clause."

[[clause]]
clause = "8.9.6.2"
title = "Stencil masking"
status = "partial"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_zero_sample_paints_the_current_colour_and_a_one_leaves_the_page_alone", "crates/pdf-model/tests/image_masks.rs::a_decode_array_of_one_zero_reverses_the_stencil", "crates/pdf-model/tests/image_masks.rs::a_stencil_whose_current_colour_is_a_pattern_is_painted_with_the_pattern"]
note = "The three differences the clause states are implemented: no /ColorSpace is consulted, /BitsPerComponent is forced to 1, and /Decode [0 1] against [1 0] decides which sample value marks the page. **And the current colour may be a pattern, which is where this row was silently wrong until the hundred-and-eighty-first session**: \"the current colour\" is §8.7.2's, a stencil was drawn as an image carrying that colour, and no image sample can carry a pattern — so `issue13372.pdf` drew nothing and said nothing. A shading pattern is recomposed out of what the display list has: the stencil becomes a §11.5.2 alpha soft mask and the pattern fills the image's unit square through it (ADR 0151). **A tiling pattern goes the same way since the two-hundred-and-eighteenth session** (ADR 0169), and the plumbing is one line: `Interpreter::tile` already puts the state's soft mask on the group it builds out of the cells, because §11.6.7 advises treating all tiles as one transparency group — so the stencil is handed to it as that mask and the unit square is the path. One case is still refused by name: a stencil under a *graphics-state* soft mask, which would be two masks on one command. **And the fix uncovered a defect two hundred sessions old**: the tiles a path touches were computed from the pattern space's origin rather than from where the cell's own `/BBox` puts it, which §8.7.3.1 makes the difference between a pattern that lands on the page and one three steps below it. Invisible until a `/BBox` sat more than one step out. **And this row's last sentence said \"/Interpolate is read nowhere in this tree\", which was false from the eleventh session** — `image::decode` reads Table 87's entry for every image including a stencil, `Image::is_smoothed` is where both backends ask what to do with it, and §8.9.5.1's own row says so three clauses away. The fourth sweep's second catch in three sessions, and the same shape both times: two rows describing one mechanism, one of them corrected and the other left standing. **What the clause's last sentence asks for was called \"a *judgement*\" here, quoting \"the effect is to smooth the edges of the image\", and ISO 32000-2 does not contain that sentence** — it is ISO 32000-1's, and the four-hundred-and-forty-second session found it while reading the parent row that deferred to it. What this standard says is a `shall`, and it is about a different noun: \"[i]f image interpolation (see 8.9.5.3, \\\"Image interpolation\\\") is requested during stencil masking, the effect shall be to smooth the edges of the mask, not to interpolate the painted colour values.\" It is met by construction rather than by a branch. `Image::is_smoothed` decides *whether* to interpolate — from the placement rather than from the entry alone, since §8.9.5.3 makes `/Interpolate` \"only a hint, and a PDF processor may ignore it\" — and *what* is interpolated is decided by the raster: a stencil's cleared samples are `[0, 0, 0, 0]` and both backends convert to premultiplied alpha before they filter, so a filtered edge is the fill colour at partial coverage exactly. Interpolating the same raster with straight alpha is what the clause forbids: it would pull the painted colour towards black at every edge."

[[clause]]
clause = "8.9.6.3"
title = "Explicit masking"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::an_explicit_mask_paints_the_base_image_only_where_it_marks", "crates/pdf-model/tests/image_masks.rs::a_mask_finer_than_its_image_keeps_its_own_resolution", "crates/pdf-model/tests/image_masks.rs::a_mask_that_is_not_an_image_mask_is_reported"]
note = "Implemented in the fourteenth session (ADR 0023). The mask is decoded by the ordinary image route, so a stencil behind CCITTFaxDecode works (`issue4379.pdf`), and §8.9.6.2 decides which sample marks — including any /Decode [1 0], which two of the corpus's six write. The clause's sentence that the two \"need not have the same resolution\" is the substance: they are combined on the finer of the two grids in each axis, nearest-neighbour, which discards nothing either raster carries. `issue4246.pdf` is the case that decides it, a 50x40 image under a 1000x800 stencil spelling three words. The true answer is a composite at device resolution, and **this crate now has a way to say so without knowing it**: since the three-hundred-and-seventieth session `pdf_render::ImageSource` carries a raster the backend produces at `Grid::for_placement`, which is what closed the same question for §11.6.5.2's soft mask (ADR 0210), for JPEG 2000's resolution level (ADR 0321), and for §8.7.4.5.2's sampled shading (`pdf_render::DeferredColours`, ADR 0339). An explicit mask is the one claimant not moved onto that vocabulary, deliberately: nothing on the corpus asks for it, since every stencil here refines to a grid that fits, and the move is owed whenever a document does ask. Three refusals, all reported: a combined grid above 2^24 samples; a /Mask stream that is not an image mask, which Table 87 and §8.9.6.2 both exclude — `issue6621.pdf` writes one and the two readings available for it are opposite; and **since ADR 0399 a /Mask that is an image mask carrying a /Mask or an /SMask of its own**, which Table 87 forbids twice over — of /ImageMask, \"Mask and ColorSpace shall not be specified\", and of /Mask, \"shall not be present for image masks\". That one was not a fidelity gap but a *cycle*: this clause's mask is decoded by the ordinary image route, so following it recursed `decode_parts` -> `apply_explicit_mask` -> `decode` with no bound at all, and the `page` fuzz target overflowed the stack on a mutated corpus document. The standard's bound here is one and needs no constant."

[[clause]]
clause = "8.9.6.4"
title = "Colour key masking"
status = "partial"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::colour_key_masking_removes_the_samples_inside_the_range", "crates/pdf-model/tests/image_masks.rs::the_colour_key_bounds_are_inclusive", "crates/pdf-model/tests/image_masks.rs::a_colour_key_array_of_the_wrong_length_is_reported"]
note = "Implemented in the fourteenth session (ADR 0023) for every image whose samples reach the unpacker unfiltered, which is both corpus instances. The range test is on the values \"before decoding with the Decode array\", so it lives inside `unpack` rather than filtering the RGBA that comes out of it; the bounds are inclusive, and an array whose length is not twice the component count is refused rather than applied to what fits. `partial` for the codec paths: a DCTDecode or JPXDecode image has become RGBA before the unpacker sees it, and the clause's own NOTE 2 names that pair as the one lossy coding makes unreliable. JBIG2 and CCITT are refused with them rather than special-cased, since a colour key over one-bit samples is a stencil written the long way. All four are reported."

[[clause]]
clause = "8.9.7"
title = "Inline images"
status = "implemented"
code = ["crates/pdf-model/src/inline_image.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/inline_images.rs::an_inline_image_draws_from_its_abbreviated_keys", "crates/pdf-model/tests/inline_images.rs::a_colour_space_name_is_looked_up_in_the_resources", "crates/pdf-model/tests/names_are_bytes.rs::a_colour_space_name_that_is_not_utf_8_is_found"]
note = "Implemented in the eleventh session (ADR 0019). Table 91's abbreviated keys and Table 92's abbreviated colour space and filter names are expanded into the dictionary an image `XObject` would have had, so one decoder serves both; entries the table does not list are dropped, as the clause requires. `/CS` naming a resource in the page's `/ColorSpace` subdictionary is resolved, and NOTE 3's rule that the device names never do is kept — **including the half Errata Collection 3 adds**, which the source comment did not quote until the four-hundred-and-nineteenth session: the names \"never refer to resources in the ColorSpace subdictionary; they always identify the corresponding colour spaces either directly or via a default colour space (see 8.6.5.6 \"Default colour spaces\")\" (Issue #19, `/State` `Review` `Completed`). No code moved for it, because the expanded name is parsed by `ColourSpace::parse`, which asks §8.6.5.6's three `/Default` entries first. Where the data ends is answered by `/L` where the file is PDF 2.0, by §8.9.3's sample layout where there is no filter, and by a search for a token-delimited `EI` otherwise. The one thing not honoured is the prohibition on `JPXDecode`, `JBIG2Decode` and `Crypt`: an inline image naming one is decoded rather than refused, which is leniency towards a malformed file and cannot mis-draw a valid one. **Which of the three answers real files need, counted**: of 93 930 inline images in 39 976 documents, 336 state `/L`, 89 968 are unfiltered and fall to §8.9.3's arithmetic, and **3 455 reach the forward search** — the largest image in the population is 9.01 MiB and the largest one the search decides is **2.99 KiB** (`token_window_census`). That is what prices the clause's one guess for a reader with a bounded window, which is `doc/todo/14`'s road D. ADR 0362. **The window landed in the five-hundred-and-thirtieth and this clause is where it needed a decision**: an inline image is the one construction in a content stream that is not a token, so the reader buffers ahead of the cursor for it — starting at the window's own size, doubling only while the answer may have been cut by it, to 16 MiB, which is 1.78 times the largest inline image in the population. The clause's own advice is smaller by three orders of magnitude and is advice: \"it should be used only for small images (4096 bytes or less)\", and \"[t]he value of the Length key should not exceed 4096 bytes\". Past the lookahead the image is refused as `InlineImageError::Unbuffered`, **kept apart from `NoTerminator`** because one says the file states no `EI` and the other says this reader stopped looking. ADR 0365. **The clause's own first sentence has a consequence for cost that nobody had drawn until ADR 0399**: an inline image \"shall be defined directly within the content stream in which it will be painted rather than as a separate object\", so there is no object a resource dictionary hands out twice and the stream this clause's scan produces is a new allocation at every `BI`. `image::RasterCache` names a stream by that address, so the same picture drawn 327 000 times — a §8.7.3.1 hatching whose cell is one `BI` — was 327 000 entries none of which could be found, and a linear probe over them. It is named by its content now; §8.9.5's row carries the measurement. **And `/CS`'s lookup in the page's `/ColorSpace` is by bytes**, since the six-hundred-and-fourth session, for §7.3.5's reason and ADR 0439's."

[[clause]]
clause = "8.10"
title = "Form XObjects"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs"]
note = "Aggregate of the four below, read as a family in the seventeenth session because a transparency group is a form XObject and §11.6.6 sends a reader here for the rest of it. A form is run inline with its /Matrix and its /Resources and — as of that session — clipped by its /BBox; a group XObject is composited as one object (§11.6.6); a reference XObject is drawn as the proxy §8.10.4.1 tells a processor that does not import to draw."

[[clause]]
clause = "8.10.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_forms_bounding_box_clips_what_it_draws", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_form_xobject_draws_its_prefix_and_reports_the_shortfall", "crates/pdf-model/tests/damaged_content_streams.rs::the_corpus_witness_names_its_truncated_form"]
note = "The five actions Do performs on a form — save the state, concatenate /Matrix, clip to /BBox, paint the content, restore — and the sentence that the rest of the initial state is inherited. Step c) was the gap: this tree clipped an annotation's appearance to its box, where §12.5.5 made that unavoidable, and let a form invoked by Do paint outside its own. tracemonkey.pdf page 6 is what that cost — a form painting a white background beyond its box covered the figure above it, and four reference renderers draw the figure. A form with no /BBox is malformed, since Table 93 makes it required, and is drawn unclipped rather than deleted. **Step b) of the writer's two — \"[t]he name shall be defined in the XObject subdictionary of the current resource dictionary\" — is a `shall` on the file, and a file that breaks it was answered with nothing at all until the four-hundred-and-nineteenth session**; it is reported now, on the condition that the `Do` was going to mark the page (ADR 0255). **A form whose stream decoded only part-way is drawn to where its damage is and says so**, since the five-hundred-and-twenty-fourth: §7.8.2 makes a form a content stream, so a prefix of one is a shorter sequence of the same instructions in the same places. 46 of the crawl's 2260 damaged streams and 7 of the pdf.js corpus's 57 are forms; `comments.pdf` and `highlights.pdf` are the witnesses and both draw an ink annotation cut off mid-path. ADR 0359. **And a form is read as a *source* since the five-hundred-and-ninety-second**, which is what lets §11.6.6's paired runs make three readers over one stream instead of holding one decode: a form the memo keeps is whole and cached as before, and one it declines goes through the window — so a decompression bomb in a form costs 10.7 MB where it cost 1032. ADR 0427."

[[clause]]
clause = "8.10.2"
title = "Form dictionaries"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_forms_bounding_box_clips_what_it_draws"]
note = "Table 93's entries that decide what is drawn are read: /BBox as the clip — and, since the two-hundred-and-fifth session, **taken back off where it cuts nothing**, which is ADR 0155's rule for a tiling cell's box one path over. A clip mask is anti-aliased, so a mark lying exactly on the boundary keeps a fraction of the boundary pixel and the rest is lost: `bug1863910.pdf` states a one-point stroke whose outer edge is the `/BBox` and carried **22% less ink than its geometry states**. `content.rs`'s `unclip_redundant` is the shared rule and `draw_appearance` is the second caller; two pages left the contradicted list with it (ADR 0165). /Matrix, /Resources with §7.8.3's fallback to the page's where the entry is absent — **and Errata Collection 3 has moved what Table 93 says about it** (Issue #292, `/State` `Review` `Completed`): the cell reads \"Sometimes required\" rather than \"Optional but strongly recommended\", the PDF 1.1 paragraph is struck out into an informative NOTE, and independence from the enclosing content stream is \"strongly recommended\" up to PDF 2.0 and \"required\" from it. So the fallback is a leniency towards pre-2.0 and malformed files rather than a rule, and a name a stated `/Resources` omits is reported instead (ADR 0255) — /Group (§8.10.3, §11.6.6) and /OC (§8.11.3.3). Not read: /FormType, whose only valid value is 1; /Ref, which is §8.10.4's; /Metadata, /PieceInfo, /LastModified, /StructParent, /StructParents, /OPI, /Name, /AF, /Measure and /PtData, none of which changes a mark on the page. Nothing here is silently approximated: the unread entries describe the form rather than its appearance. **Read and kept in the five-hundred-and-sixty-fifth**, and the two entries worth checking rather than believing are both unread here: `Xmp::read` takes any dictionary's `/Metadata` and its one caller passes the catalog, and `attachment::associated` is written for all seven of §14.13's `/AF` sites while its one caller outside its own tests passes the catalog too — so a form XObject's copy of each is reachable by construction and reached by nothing, which is the fifth sweep's shape rather than this row's."

[[clause]]
clause = "8.10.3"
title = "Group XObjects"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_form_becomes_a_group_only_for_the_transparency_subtype"]
note = "Table 94: a group XObject is a form with a /Group, and its /S names the subtype. The only subtype the specification defines is /Transparency, and the second half of §11.6.6's sentence is the one an implementation drops — a /Group with any other subtype shall not be subject to any grouping behaviour. Tested rather than assumed, because no corpus document writes another subtype."

[[clause]]
clause = "8.10.4"
title = "Reference XObjects"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/corpus.rs"]
note = "Aggregate of the three below. Content is never imported from another document, and the clause names that case explicitly: a processor that does not recognise /Ref draws the proxy as an ordinary form XObject, which is what happens here. The gap is a feature not built rather than a requirement broken, and nothing is drawn wrong by it."

[[clause]]
clause = "8.10.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/corpus.rs"]
note = "\"PDF processors that do not recognise the Ref entry shall simply display or print the proxy as an ordinary form XObject.\" That is this renderer, and it is the clause's own provision rather than a shortcut — the same shape as §10.7.2's permission to ignore flatness. What is not implemented is Table 95: finding the target document by /F and /ID and drawing its /Page in the proxy's place. A document whose proxy is a grey box is drawn as a grey box, which is what the clause says a processor like this shall do, so there is nothing to report."

[[clause]]
clause = "8.10.4.2"
title = "Printing reference XObjects"
status = "inapplicable"
note = "What a processor may emit when printing a page holding a reference XObject. This renderer draws to a screen and prints nothing, and the clause is permissive in any case: it lists alternatives a processor may choose among rather than requiring one."

[[clause]]
clause = "8.10.4.3"
title = "Special considerations"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/corpus.rs"]
note = "Three considerations, all conditional on importing the target page: its annotation appearances must be rendered with it, its logical structure may be ignored, and the proxy's /Group applies to the imported page. None arises while the proxy is what is drawn (§8.10.4.1). The last is the one that would matter first, because this tree now composites a /Group on any form."

[[clause]]
clause = "8.11"
title = "Optional content"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/tests/optional_content.rs"]
note = "Aggregate. The default configuration is honoured wherever it decides what is drawn, and **since the hundred-and-sixty-seventh session an interactive layer panel exists**: `viewer_ui::chrome` draws §8.11.4.3's `/Order`, shows each group's state, and throws the switch through `Command::SetGroup` — with Table 99's `/Locked` refusing the change, which is the clause's own sentence. §8.11.4.4 is the row to read for the part that is missing in silence. **Re-read off the blame list in the five-hundred-and-seventeenth**: §8.11.4.4 is still the row that names what is missing, `chrome.rs` still throws the switch through `Command::SetGroup`, and Table 99's `/Locked` still refuses it."

[[clause]]
clause = "8.11.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs"]
note = "Descriptive: it names the three subclauses beside it and carries their aggregate, which is why it is `partial` rather than `implemented` — §8.11.2 and §8.11.3 are settled and §8.11.4 is not. **What the aggregate owes was not written here until the four-hundred-and-thirty-seventh session**, and a `partial` note that names nothing owed is the fourteenth sweep's whole subject: Table 98's `/Configs`, and the configuration's own `/Name` and `/Creator`, are unread because the alternate configurations exist to be chosen between (§8.11.4.3), and §8.11.4.5's rule that a usage application dictionary depending on the zoom level \"shall be reapplied\" is measured rather than met — no corpus configuration names `Zoom`, `User` or `Language`."

[[clause]]
clause = "8.11.2"
title = "Optional content groups"
status = "implemented"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_marked_section_belonging_to_a_group_that_is_off_is_not_drawn", "crates/pdf-model/tests/optional_content.rs::a_visibility_expression_outranks_the_policy_beside_it", "crates/pdf-model/tests/optional_content.rs::a_group_whose_intent_the_configuration_ignores_does_not_hide_anything"]
note = "Aggregate of §8.11.2.1 to §8.11.2.3, and all three are `implemented` — the last of them since the three-hundred-and-eighteenth session, when §8.11.2.1's two unread entries turned out to have been read since the thirty-fifth and the sixty-seventh. A parent may owe more than its children and this one does not: what §8.11 still owes is in §8.11.3.2's `DP` form and in §8.11.4's configuration rows, which are the other two branches."

[[clause]]
clause = "8.11.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/optional_content.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_marked_section_belonging_to_a_group_that_is_off_is_not_drawn", "crates/viewer-ui/tests/panel.rs::a_layer_switch_throws_unless_the_document_locked_it"]
note = "All four of Table 96's entries, and both of the clause's prose rules. A group's state comes from the default configuration (§8.11.4.3, §8.11.4.5); `/Intent` is honoured in both directions (§8.11.2.3); the nesting rule — \"if the visibility state of an outer level indicates that the content needs to be hidden, all inner levels shall be hidden regardless of their individual visibility states\" — falls out of the interpreter's hidden-section counter. **This row read `partial` for two entries the tree reads, and it was wrong about each of them for a different number of sessions.** It said \"[t]wo Table 96 entries are read by nothing: /Name, which exists to be shown in a user interface, and /Usage, which feeds the automatic state setting of §8.11.4.4\". `/Usage` has been read since the **thirty-fifth** session, by §8.11.4.4's usage application dictionaries — `optional_content.rs` fetches it per group and evaluates Table 100's categories against it — and `/Name` since the **sixty-seventh**, where `OptionalContent::name` decodes it as a §7.9.2.2 text string and, since the hundred-and-sixty-seventh, `viewer_ui::chrome` puts it on the row a person throws the switch on. Both are `doc/todo/01`'s fifth shape: a family's member row is not maintained by the session that implements its neighbour, because the clauses do not cite each other — and the entry whose whole purpose is \"presentation in an interactive PDF processor's user interface\" was recorded as unread for a hundred and fifty sessions after a panel existed to present it. Found in the three-hundred-and-eighteenth."

[[clause]]
clause = "8.11.2.2"
title = "Optional content membership dictionaries"
status = "implemented"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_membership_dictionary_can_show_content_when_its_group_is_off", "crates/pdf-model/tests/optional_content.rs::a_visibility_expression_outranks_the_policy_beside_it", "crates/pdf-model/tests/optional_content.rs::a_single_group_reference_is_read_without_losing_which_object_it_is"]
note = "/OCGs in both its shapes, all four /P policies, and /VE including nesting. /VE is used in preference to /OCGs and /P as the clause requires; a tree deeper than 32 is refused and reported rather than recursed."

[[clause]]
clause = "8.11.2.3"
title = "Intent"
status = "implemented"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_group_whose_intent_the_configuration_ignores_does_not_hide_anything"]
note = "Both directions: a group whose intents miss the configuration's has no effect on visibility, and a configuration whose /Intent is an empty array makes all content visible. /All is expanded as the set of all intents."

[[clause]]
clause = "8.11.3"
title = "Making graphical content optional"
status = "implemented"
code = ["crates/pdf-model/src/optional_content.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_marked_section_belonging_to_a_group_that_is_off_is_not_drawn", "crates/pdf-model/tests/optional_content.rs::a_form_xobject_carrying_oc_is_skipped_when_its_group_is_off", "crates/pdf-model/tests/optional_content.rs::an_annotation_carrying_oc_is_not_drawn_when_its_group_is_off"]
note = "Aggregate of §8.11.3.1 to §8.11.3.3."

[[clause]]
clause = "8.11.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_hidden_section_still_changes_the_graphics_state_it_leaves_behind"]
note = "Hiding suppresses marking and nothing else: the colour, transform and clip a hidden section sets outlive it, and the text position advances through hidden text. Form XObjects and annotations are skipped whole, which the clause permits because their state changes do not escape them. **And a report is suppressed with the marks**, since the four-hundred-and-nineteenth session: a `Do`, a `gs` or an `scn` naming a resource nothing defines says so on the page and says nothing inside a hidden section, because the clause makes such content skipped \"as if there were no Do operator to invoke it\" and a report there would cost the oracle a judged page for a difference no raster can hold (trap 11, ADR 0255). `paint_shading` had the same rule for `sh` already."

[[clause]]
clause = "8.11.3.2"
title = "Optional content in content streams"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_marked_section_belonging_to_a_group_that_is_off_is_not_drawn", "crates/pdf-model/tests/optional_content.rs::a_group_missing_from_the_properties_dictionary_hides_nothing", "crates/pdf-model/tests/optional_content.rs::a_group_named_only_by_a_marked_content_point_is_still_referenced"]
note = "BDC /OC is honoured, including the rule that the operand must name a group listed in /OCProperties /OCGs or a membership dictionary. **And the DP form with it, which this row called unimplemented for sixty-five sessions after it stopped being so.** The clause's sentence — \"a DP marked-content operator may be placed in a page's content stream to force a reference to an optional content group or groups on the page, even when the page has no current content in that layer\" — has exactly one consequence, because a marked-content *point* marks nothing: the group is *referenced* by the page, which is what Table 99's `/ListMode /VisiblePages` asks. `optional_content::groups_referenced_by` answers that from the page's `/Resources /Properties` rather than by interpreting the stream (ADR 0178, the two-hundred-and-fifty-fourth session), so a `DP` naming a property is counted **by construction** — and the walk's own comment argues the over-listing that buys: a group whose `BDC` never runs costs a person nothing, a group hidden from the panel the document asked to show costs them a switch. `a_group_named_only_by_a_marked_content_point_is_still_referenced` is the gate, because a walk that ever became an interpretation would drop this case in silence. Found in the three-hundred-and-nineteenth session, one clause after §8.11.2.1's two entries, and it is the same shape: the row was not maintained by the session that implemented its mechanism somewhere else."

[[clause]]
clause = "8.11.3.3"
title = "Optional content in XObjects and annotations"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_form_xobject_carrying_oc_is_skipped_when_its_group_is_off", "crates/pdf-model/tests/optional_content.rs::an_annotation_carrying_oc_is_not_drawn_when_its_group_is_off"]
note = "/OC on a form or image XObject and on an annotation, each combined with the visibility of the context it is invoked from. This is the entry point issue12007_reduced.pdf uses; implementing only §8.11.3.2 would have left it drawing a whole hidden screenshot."

[[clause]]
clause = "8.11.4"
title = "Configuring optional content"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs"]
note = "Aggregate of §8.11.4.1 to §8.11.4.5, and `partial` for three things its children owe and this row did not name until the four-hundred-and-thirty-seventh session: Table 98's `/Configs` and the configuration's own `/Name` and `/Creator`, unread because choosing between alternate configurations needs somebody to choose (§8.11.4.3); §8.11.4.4's usage application dictionaries for the events no corpus document states; and §8.11.4.5's reapplication on a change of zoom, which is measured rather than met. The fourteenth sweep prints every `partial` row whose note names nothing owed, and this was one."

[[clause]]
clause = "8.11.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs"]
note = "Descriptive. Carries the aggregate of the four subclauses beside it, and is `partial` for what they owe rather than for anything of its own: `/Configs`, `/Name` and `/Creator` on a configuration (§8.11.4.3), and §8.11.4.5's reapplication on a change of zoom. **Named here since the four-hundred-and-thirty-seventh session**, by the fourteenth sweep, which prints every `partial` row whose note names nothing owed."

[[clause]]
clause = "8.11.4.2"
title = "Optional content properties dictionary"
status = "implemented"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_document_with_no_properties_dictionary_draws_everything"]
note = "/OCProperties is read from the catalog, /OCGs gives the document's groups and /D the default configuration. A file with no /OCProperties has its optional content structures ignored entirely, which the clause makes decisive rather than a fallback."

[[clause]]
clause = "8.11.4.3"
title = "Optional content configuration dictionaries"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_base_state_of_off_hides_the_groups_the_on_array_does_not_name", "crates/viewer-ui/tests/panel.rs::a_layer_switch_throws_unless_the_document_locked_it"]
note = "Table 99, all of it but two entries. `/BaseState`, `/ON`, `/OFF` and `/Intent` decide the initial state; `/AS` is §8.11.4.4's and is applied for the `View` event; `/RBGroups` reached the code in the sixty-second session when §12.6.4.13's `/PreserveRB` made it load-bearing; and `/Order`, `/ListMode`, `/Locked` and a group's `/Name` reached it in the sixty-seventh, which is the whole of what a layer panel reads. `/Order` is kept as a *tree* with the clause's own distinction preserved — a nested array with a leading text string is a label and \"not to communicate actual nesting\", one without is nesting — because a panel that drew both alike would tell a person that a heading is a layer. `/Locked` binds `ViewState::set_group` and deliberately not `Action::SetOcgState`, which is the clause's own division: a locked group \"cannot be changed through the user interface\", and a processor \"may allow the states … to be changed by means other than the user interface\". **`/ListMode` is applied since the two-hundred-and-fifty-fourth session** — \"[d]isplay only those groups in the Order array that are referenced by one or more visible pages\" — and its reason for not being was the shape `doc/todo/01`'s third sweep hunts: \"which pages are visible is a question about a window this crate does not have\", eighty-six sessions after a window arrived. The clause's half lives here — `groups_referenced_by` gathers every group the three places §8.11 puts an `/OC` reach, following a membership dictionary's `/OCGs` and `/VE` and nested form `XObject`s' own resources — and the window's half is `viewer_core::layers`, where one page at a time makes \"one or more visible pages\" the page being shown, by the derivation §12.6.3's `/PV` and `/PO` already took. What *referenced by* means is not stated and is recorded as a reading: a `BDC /OC` naming a property the walk found makes the group reachable, whether or not that operator runs, because over-listing a switch costs a person nothing and under-listing hides one the document asked to show. One corpus document states the entry — `visibility_expressions.pdf`, `VisiblePages`, whose page reaches all three of its groups — so the filtering case is a synthetic fixture and says so. ADR 0178. `partial` for one entry now: `/Configs` is unread — the alternate configurations exist to be chosen between, which needs someone to choose. `/Name` and `/Creator` on the *configuration* are unread for the same reason. **The panel `/Order` was read for arrived in the hundred-and-sixty-seventh session** — `viewer_ui::chrome`, which draws the tree with the clause's label-versus-nesting distinction intact and offers the switch on everything but a `/Locked` group. Errata Collection 3 rewrites Table 99's `/RBGroups` row (Issue #225, `/State` `Review` `Completed`), adding 'None of the inner array elements shall be an empty array.' and restating the empty-`/RBGroups` case. The requirement is on a producer; an empty inner collection excludes nothing here and is dropped. Read in the four-hundred-and-seventeenth session. **Read and kept in the five-hundred-and-sixty-fifth**: the only thing in the tree that names `/Configs` is still `examples/oc_usage_census`, and an example is not a reader — the same answer the four-hundred-and-thirteenth and four-hundred-and-twenty-ninth sessions recorded, which is how this row's population is known not to have drifted."

[[clause]]
clause = "8.11.4.4"
title = "Usage and usage application dictionaries"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_usage_application_dictionary_turns_a_group_off", "crates/pdf-model/tests/optional_content.rs::every_category_of_every_dictionary_has_to_agree", "crates/pdf-model/tests/optional_content.rs::a_zoom_category_is_answered_at_the_pages_stated_size"]
note = "**The ledger's last original `silent` row, closed in the thirty-fifth session.** Table 101's usage application dictionaries are applied for the `View` event, which §8.11.4.5 makes the only one a viewer runs — `Print` and `Export` apply \"for the duration of the print operation\" and of the export. Table 100's `View`, `Print`, `Export` and `Zoom` categories are answered, with both ANDs the clause states: across the categories of one dictionary and across every dictionary a group appears in. **And the whole population is measured as of the three-hundred-and-twenty-fourth session**: `examples/oc_usage_census` reads every configuration's `/AS` across all 974 corpus documents, and of the 31 that state `/OCProperties`, **six** state a usage application dictionary at all — naming `View` 6 times, `Print` 6 and `Export` 5, and **`Zoom`, `User` and `Language` not once**. `PageElement` appears on 12 of the groups' own `/Usage` dictionaries and is never named in a `/Category`, so it decides nothing either. Partial for two categories that are questions about this *processor* rather than about the document — `User` matches \"the user's identification\" and `Language` \"the language and locale of the application\" — where the configuration's own state stands and the page reports what it could not answer, rather than taking the clause's \"otherwise OFF\" on a question nobody asked. `Zoom` is answered at a magnification of 1.0, the size a page is drawn at when nothing states otherwise, because a display list carries no magnification; that is a choice and it is recorded on `apply_auto_states`. Eight corpus documents carry an `/AS` and every one pairs a `View` event with the `View` category over groups that state `ON`, so the mechanism runs on all of them and changes nothing. **Read and kept in the five-hundred-and-twenty-fifth**: `optional_content.rs` still answers `User` and `Language` with `Recommendation::Unanswerable`, which is the reported half rather than the clause's \"otherwise OFF\", and the census example that measured the population is still the evidence for the `Zoom` choice."

[[clause]]
clause = "8.11.4.5"
title = "Determining the state of optional content groups"
status = "partial"
code = ["crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_base_state_of_off_hides_the_groups_the_on_array_does_not_name"]
note = "Steps a) and b) — the base state over every group, then the array opposite to it — are implemented, and the clause calls their result the initial state used by all PDF processors. The paragraph after it is now half implemented too: \"[t]he user may manipulate optional content group states manually or by triggering set-OCG-state actions\", and since the sixty-second session §12.6.4.13's action does exactly that against a `ViewState` the file cannot see, which is what the same sentence's \"[m]anual changes shall override the states that were set automatically\" requires. **And the sentence about the zoom is a `shall` this program does not reach**: \"[w]henever there is a change to a factor that the usage application dictionaries with event type View depend on (such as zoom level), the corresponding dictionaries shall be reapplied\". The magnification has been threaded through `ViewState` since the two-hundred-and-seventeenth session (ADR 0168), so the *reason* `optional_content.rs` used to give for answering `Zoom` at 1.0 — that a display list has no magnification — expired there; what holds now is that **no corpus document names the `Zoom` category at all** (six state a usage application dictionary and they name `View`, `Print` and `Export`), so reapplying would be a path nobody takes. Measured by `examples/oc_usage_census` in the three-hundred-and-twenty-fourth session; a document that named it would make this work to do. Still owed: usage application dictionaries for the Print and Export events, whose changes \"persist only for the duration\" of an operation this program does not perform. **The other thing this row called owed — \"a layer panel for the manual half\" — arrived in the hundred-and-sixty-seventh session and the row did not notice for a hundred and eleven**: `viewer_ui::chrome`'s sidebar draws §8.11.4.3's groups with a switch apiece, a click sends `Command::SetGroup`, and Table 99's `/Locked` refuses the ones the file says may not be changed (ADR 0145). So the clause's \"[t]he user may manipulate optional content group states manually\" is manual now in the ordinary sense of the word, and not only through §12.6.4.13's action. See §8.11.4.4. **Read and kept in the five-hundred-and-twenty-fifth**: the manual half is still `Command::SetGroup` and §12.6.4.13's action, and the `Zoom` reapplication is still a `shall` no corpus document reaches."

[[clause]]
clause = "9.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor"]
note = "The clause's own map of itself: a text object, a font, an encoding, a glyph, a position. Every subclause it names is reviewed, and the two families it separates are kept separate in code — `content.rs` owns §9.3's text state and §9.4's operators, `pdf-font` owns §9.6 and §9.7's route from a code to an outline, and neither knows the other's rules. That separation is the clause's rather than a convenience: it puts a glyph's *selection* and its *painting* on opposite sides, which is why §9.3.6's rendering modes belong to the interpreter and §9.6.5's encodings to the font."

[[clause]]
clause = "9.2"
title = "Organisation and use of fonts"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_vertical_cmap_takes_the_second_set_of_metrics"]
note = "Organisation and metrics. See §9.2.2's font dictionaries, which are clause 9's other families, and §9.2.4's two sets of metrics, both of which this tree now reads."

[[clause]]
clause = "9.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_vertical_cmap_takes_the_second_set_of_metrics"]
note = "Introductory. Its division — a font is \"a graphical description of a set of glyphs\" and the font *dictionary* is what a content stream names — is the boundary between `pdf-font`, which knows programs and encodings, and `pdf-model`, which knows where a glyph goes."

[[clause]]
clause = "9.2.2"
title = "Basics of showing text"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::mode_3_paints_nothing_and_still_extracts_its_text"]
note = "The four things the clause says showing text requires — the font, the size, the position and the other text state parameters — are §9.3's Table 102 and are all read. `Tf` selects the first two and §8.4.5's Table 57 `/Font` is the other route to them (ADR 0042). The clause also states the size a font defines its glyphs at — \"[a] font defines the glyphs at one standard size. This standard is arranged so that the nominal height of tightly spaced lines of text is 1 unit\" — which is what `pdf_font::vertical_extent` falls back to when a descriptor states no usable line, and half of what makes ADR 0216's band on `/Ascent` and `/Descent` derivable rather than invented."

[[clause]]
clause = "9.2.3"
title = "Achieving special graphical effects"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::mode_7_clips_what_follows_and_paints_nothing"]
note = "The two effects the clause names are both implemented and both belong to §9.3.6's rendering modes: glyphs painted with any colour or pattern the graphics state holds, and glyphs added to the clipping path so that other content shows through them. A Type 3 font, which the clause names as the third route, is §9.6.4."

[[clause]]
clause = "9.2.4"
title = "Glyph positioning and metrics"
status = "implemented"
code = ["crates/pdf-font/src/metrics.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_vertical_cmap_takes_the_second_set_of_metrics", "crates/pdf-font/src/loading.rs::the_pdf_widths_agree_with_the_font_programs_own_advances"]
note = "Both writing modes, from the thirty-sixth session. Mode 0's displacement is `/Widths` or `/W`; mode 1's is §9.7.4.3's `/W2` and `/DW2`, and the clause's two constraints on the vectors are stated where they are used — `w1`'s horizontal component is 0 and `v`'s horizontal component is half the glyph's width when `/W2` does not give one. \"Glyph space\" is one thousandth of text space for every font but a Type 3, whose `/FontMatrix` says otherwise, and both routes are in `show_text`. The glyph bounding box is read by nobody, and **this sentence used to say Table 111 permits that and it says no such thing** — corrected in the three-hundred-and-seventy-second session, on a corpus page where two references clip to the box and three do not. What Table 111 states about a Type 3 glyph's box is that \"[t]he declared bounding box shall be correct - in other words, sufficiently large to enclose the entire glyph\" and that \"[i]f any marks fall outside this bounding box, the result is implementation-dependent\"; Table 110's \"shall make no assumptions\" is about the *font*'s box and only where all four of its elements are zero. So the permission is the other way round from what was written: no clause requires a processor to use either box, and both tables make an incorrect one's consequences implementation-dependent — which is what makes ignoring them safe rather than merely allowed. `issue14953.pdf` declares `0 0 0 0` sixteen times and `AMBIGUOUS_DEGENERATE_GLYPH_BOX` carries the measurement."

[[clause]]
clause = "9.3"
title = "Text state parameters and operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::a_text_state_operator_outside_a_text_object_is_retained_and_saved", "crates/pdf-model/tests/text_state.rs::text_knockout_becomes_a_group_only_where_the_two_models_differ", "crates/pdf-model/tests/text_render_modes.rs::mode_2_fills_then_strokes_each_glyph_in_turn", "crates/pdf-model/tests/text_render_modes.rs::a_type3_glyph_draws_in_mode_4_and_adds_nothing_to_the_clip"]
note = "Reviewed as a family in the thirteenth session. **Was `partial` from that session until the four-hundred-and-thirty-seventh, on a reason its own child's row retired in the seventy-second**: it said \"[e]ight of Table 102's nine text state parameters are implemented … the ninth, text knockout, is §9.3.8, which was this tree's third `silent` row until the fourteenth session made it report\", and §9.3.8 has been `implemented` — drawn, not reported — since the seventy-second. Three hundred and sixty-five sessions, which is the longest this project has recorded, past §12.5.6.19's 364 and §12.7.6's 280. It was found by the fourteenth sweep on its first run (`doc/todo/01`), which prints every `partial` row whose note names nothing owed: this one named a report that had stopped being a report, so nothing else could see it — the sixth sweep's arithmetic never looks here, because §9.3.1 was `partial` for the same expired reason and a family with an owing child never qualifies. All nine of Table 102's parameters are executed and all seven of Table 103's operators; the clause's own rule that they \"may appear outside text objects, and the values they set are retained across text objects in a single content stream\" falls out of their living in the graphics state rather than in the text object, and is asserted rather than derived since this round. \"All text state parameters shall apply to Type 3 fonts unless otherwise stated\" holds by construction: `show_text` reads one state and dispatches on the font afterwards. **The status move cost this row its evidence** — it named two whole test *files*, which `FILE_ONLY_EVIDENCE_CEILING` refuses at zero on an `implemented` row — which is the same ratchet §9.4 tripped in the four-hundred-and-twenty-ninth, working the same way."

[[clause]]
clause = "9.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::zero_sized_text_neither_marks_nor_clips", "crates/pdf-model/tests/text_state.rs::a_negative_font_size_draws_and_reads_normally", "crates/pdf-model/tests/text_state.rs::a_text_state_operator_outside_a_text_object_is_retained_and_saved", "crates/pdf-model/tests/numbers_without_digits.rs::a_font_size_that_states_no_digit_is_reported_rather_than_read_as_zero"]
note = "Table 103's seven operators — Tc, Tw, Tz, TL, Tf, Tr, Ts — are all executed with the initial values the table states, and they sit in the graphics state so that q/Q saves them and a page starts from the defaults. Two sentences here are load-bearing and tested: \"Zero sized text shall not mark or clip any pixels (depending on text render mode)\", which is a guard on both the marking and the clipping path rather than only the first; and the NOTE that \"Negative text font size is permitted\", which draws a flipped glyph advancing leftwards. **This row ended \"[p]artial only because of Tk, whose absence is §9.3.8\" from the thirteenth session to the four-hundred-and-thirty-seventh, and Tk has not been absent since the seventy-second**: §9.3.8's own row opens \"[w]as this tree's third `silent` row; `reported` from the fourteenth session; drawn since the seventy-second\", and this subclause does not own the parameter in any case — what it states about `Tk` is that there is no operator for it, \"[t]he text knockout parameter, T k, shall be set through the TK entry in a graphics state parameter dictionary by using the gs operator\", which `content.rs` reads off an `/ExtGState`. Two further sentences of this subclause are asserted rather than derived since the four-hundred-and-thirty-seventh: that a text state operator \"may appear outside text objects, and the values they set are retained across text objects in a single content stream\", and that `q`/`Q` carries them with the rest of the graphics state, which is the same construction the first sentence rests on. **A third sentence became load-bearing in the four-hundred-and-sixty-eighth** (ADR 0303): \"There is no initial value for either font or size; they shall be specified explicitly by using Tf before any text is shown.\" A `Tf` whose size operand is malformed states no size, so the parameter stays unset and the show that follows is a reported loss — where reading the malformed operand as zero would have drawn nothing and said nothing, which is the same clause read as its own opposite."

[[clause]]
clause = "9.3.2"
title = "Character spacing"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::horizontal_scaling_reaches_the_character_spacing_too"]
note = "Tc is added to each glyph's horizontal displacement and is then scaled by Th, which the clause requires — it is \"specified in unscaled text space units (although it shall be subject to scaling by the Th parameter if the writing mode is horizontal)\". The vertical branch does not exist because no writing mode 1 does; see §9.2.4."

[[clause]]
clause = "9.3.3"
title = "Word spacing"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-model/tests/text_state.rs::word_spacing_moves_the_glyph_after_a_simple_fonts_space", "crates/pdf-model/tests/text_state.rs::word_spacing_does_not_reach_a_two_byte_code_32", "crates/pdf-model/tests/text_state.rs::a_space_no_method_can_name_still_reads_back_as_one", "crates/pdf-model/tests/text_state.rs::a_two_byte_code_32_is_not_read_back_as_a_space"]
note = "Corrected by this review. The clause states the rule about the code's *encoding*, not its value — word spacing applies to \"every occurrence of the single-byte character code 32 … when using a simple font (including Type 3) or a composite font that defines code 32 as a single-byte code\" and \"shall not apply to occurrences of the byte value 32 in multiple-byte codes\" — and this tree applied it to any code numerically equal to 32, so an Identity-H string containing 00 20 was pushed right by Tw for every one. `Code::takes_word_spacing` answers it from the code's own encoded length, which is where this note said it would have to move once embedded CMap streams landed: it did, and the note said `LoadedFont::has_single_byte_codes` — a function that no longer exists — for forty-odd sessions after. **And the clause says a second thing, which the four-hundred-and-sixty-fourth session read off it: it names the character.** \"Word spacing works the same way as character spacing but shall apply only to the ASCII SPACE character (20h)\", of \"every occurrence of the single-byte character code 32 in a string\" — the clause identifies the code with the character in order to say which glyph Tw applies to, and that identification is a statement about the code. So `Font::text` reads a single-byte code 32 back as U+0020 where every one of §9.10.2's methods and its closing permission have declined, which is a *last* resort behind them: a `/Differences` naming code 32 `/bullet` or a `/ToUnicode` mapping it elsewhere is answered earlier and never reaches it. Five corpus documents show a code 32 no method can name, and `issue4304.pdf` is 895 bytes named after the defect — a `/Times-Roman` with `/Differences [32 /.notdef]` that has drawn *Words that should have spaces between them.* since the four-hundred-and-fifth session gave `.notdef` its advance, and read back `Wordsthatshouldhavespacesbetweenthem.` until this. **Neither text gate could see it**: both strip whitespace from the comparison by design, so this moved no number and changed what a person selects, searches, copies, retrieves and hears."

[[clause]]
clause = "9.3.4"
title = "Horizontal scaling"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::horizontal_scaling_reaches_the_character_spacing_too", "crates/pdf-model/tests/text_state.rs::horizontal_scaling_reaches_a_tj_adjustment"]
note = "Th is a factor rather than the percentage the operator takes, and it reaches all four things the clause lists: the glyph's shape, its horizontal displacement, the spacing parameters Tc and Tw, and \"any positioning adjustments performed by the TJ operator\". The last three are the ones a plausible implementation misses, which is why each has a test."

[[clause]]
clause = "9.3.5"
title = "Leading"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::leading_moves_the_next_line_downwards", "crates/pdf-model/tests/text_state.rs::q_and_q_save_the_text_matrices_inside_a_text_object"]
note = "Tl is used by exactly the four operators Table 103 names — TD, T*, ' and \" — and by nothing else. TD sets it to the negation of its second operand and T* moves by its negation, which is the sign the clause explains in Table 106: leading is \"expressed as a positive number\" and going to the next line decreases y."

[[clause]]
clause = "9.3.6"
title = "Text rendering mode"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs", "crates/pdf-model/tests/glyph_clip_direction.rs", "crates/pdf-model/tests/render_real_pdf.rs::a_real_documents_text_clip_reaches_what_it_paints_afterwards", "crates/pdf-model/tests/selection_geometry.rs::invisible_modes_place_every_glyph_they_do_not_draw"]
note = "All eight of Table 104's modes, thirteenth session, ADR 0022. The three operations are independent: a fill in the non-stroking colour, a stroke in the stroking colour whose parameters are read \"in user space rather than in text space\" — so the outline is moved into user space rather than the width divided by the font size — and an accumulation of outlines that becomes one non-zero-filled clip at ET and lasts until Q. Four of the clause's own exceptions are implemented and tested rather than inferred: an accumulator holding no outlines sets no clip at all (\"If no glyphs are shown or if the only glyphs shown have no outlines … no clipping shall occur\"); modes 2 and 6 fill and stroke glyph by glyph, giving \"the appearance of stacked opaque glyphs\" — and, since the hundred-and-twenty-first session, make that pair one object where §11.7.4.4 asks for it; a Type 3 glyph is rendered for every mode but 3 and 7 and adds nothing to the clipping path; and modes 3 and 7 still advance Tm. A hidden optional-content layer suppresses the marking and not the clip, because §8.11.3.1 puts clipping among the graphics state operations that \"shall still be applied\". A Tr operand outside 0..7 is reported and ignored rather than silently blanking the text object. **That modes 3 and 7 still advance `Tm` was asserted by nothing until the three-hundred-and-seventy-eighth session**, and it is what makes every scanned document selectable: an OCR layer is nothing but mode 3, so a text layer built from what was *drawn* would be empty for all of them. `selection_geometry.rs` compares the two invisible modes against mode 0 over one string — the same readback, the same number of placements, every quadrilateral identical — while the display list gets no glyph. **NOTE 2 was read for the first time in the five-hundred-and-sixty-first session and it names a defect this clause makes visible**: 'Due to the use of non-zero winding number rule, the direction of the paths comprising each glyph can cause different output for overlapping glyphs.' Two glyphs wound opposite ways cancel where they overlap instead of uniting, and this tree's stand-ins for two of §9.6.2.2's fourteen were wound opposite ways — the compiled-in Helvetica an `sfnt` at −0.186 signed em² for a capital B, the compiled-in Times a bare CFF at +0.165. `OverlappingGlyphClipping.pdf` accumulates `/Times-Bold` and `/Helvetica` into one clip and lost the overlap, at −8.989 of 255 from the lightest of three references that agree to 1.2. Neither the clause nor the rasteriser is at fault: §9.5's NOTE 5 makes the *choice of face* ours, so what had to change was the set rather than the combination. `pdf_font::substituted::wound_counter_clockwise` normalises every substituted outline and the page comes to −1.116, which is the faces' own shapes and nothing else. ADR 0396."

[[clause]]
clause = "9.3.7"
title = "Text rise"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::text_rise_is_not_scaled_by_the_font_size"]
note = "Trise is the f component of the glyph-space-to-text-space matrix, so it moves the baseline in unscaled text space units — not multiplied by the font size, which is the reading that would make a superscript's height depend on the point size. It applies to the vertical coordinate regardless of writing mode, and `Interpreter::glyph_to_text` is one function whichever mode is in force: the rise goes into the matrix's `f` position and the writing mode decides only which component of §9.4.4's displacement moves the text matrix afterwards. **The last clause of that sentence read \"which is trivially true here because only mode 0 exists\" until the four-hundred-and-twenty-ninth session** — right conclusion, expired argument, which is `doc/todo/01`'s fifth way for a row to be wrong. Writing mode 1 has been drawn since the four-hundred-and-twentieth (§9.4.4, §9.7.5.1), and this is one of four rows that went on describing a tree with one writing mode in it."

[[clause]]
clause = "9.3.8"
title = "Text knockout"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::text_knockout_becomes_a_group_only_where_the_two_models_differ", "crates/pdf-model/tests/text_state.rs::text_knockout_still_reports_where_a_glyph_blends", "crates/pdf-model/tests/text_state.rs::a_text_knockout_set_inside_a_text_object_is_ignored"]
note = "Was this tree's third `silent` row; `reported` from the fourteenth session; drawn since the seventy-second. Tk has no operator and arrives only as /TK in an /ExtGState, which is read — including the clause's rule that a value installed between BT and ET \"shall be ignored\". Its initial value is true, and the clause says what that means: \"the behaviour shall be equivalent to treating the entire text object as if it were a non-isolated knockout transparency group … where each glyph is an individual element in that group's transparency stack\", after which \"the group results shall be composited with the backdrop, using the Normal blend mode and alpha and soft mask values of 1.0\". So a text object becomes one `Command::Group` with `knockout` set, alpha 1, no mask and the Normal blend mode, and its glyphs keep the state they were shown under — the clause's first additional rule, that parameters are inherited and not reset, is the difference from §11.6.6's group XObject and is why nothing is initialised here. The group is built only where both of the older conditions hold — the paint composites at all, and two glyphs of the object overlap — so an ordinary page of opaque text is not wrapped in a buffer for a difference it cannot show. What still reports is the non-isolated half: a glyph that *blends* needs the group's own backdrop, which §11.4.4's NOTE 3 cancels only under Normal. 2 corpus documents drew this and both are complete now."

[[clause]]
clause = "9.4"
title = "Text objects"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::a_text_clip_survives_et_and_ends_at_q", "crates/pdf-model/tests/text_state.rs::leading_moves_the_next_line_downwards", "crates/pdf-model/tests/text_state.rs::horizontal_scaling_reaches_a_tj_adjustment", "crates/pdf-model/tests/composite_fonts.rs::a_vertical_font_advances_down_the_page_rather_than_across_it"]
note = "Reviewed as a family in the thirteenth session. The clause states nothing above its four subclauses — the requirements begin at §9.4.1 — so this row is their aggregate, and all four are `implemented`. BT and ET, all four positioning operators and all four showing operators are executed. The clause's constraints on a *writer* — that text objects shall not be nested, that the showing operators shall only appear within one, and that q…Q must nest separately from BT…ET — are recovered from rather than enforced, which is what a reader owes a malformed file. **This row was `partial` on one sentence — \"§9.4.4 stays partial because the vertical branch of the displacement formula has no writing mode 1 to reach it\" — for nine rounds after §9.4.4 stopped being `partial`**: the four-hundred-and-twentieth session found that §9.4.4's own claim had been false for three hundred and eighty-four sessions and moved that row to `implemented`, and the parent it was quoted from was left behind. `doc/todo/01`'s fifth failure shape at family scale, found by its fourth sweep over the noun and confirmed by its sixth, which is the arithmetic that prints a `partial` parent above four settled children. **The status move cost this row its evidence**: it named two whole *files*, which `FILE_ONLY_EVIDENCE_CEILING` tolerates on a `partial` row and refuses at zero on an `implemented` one, so the gate failed the moment the status changed. One named test per subclause now, which is what an aggregate row's evidence is."

[[clause]]
clause = "9.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::a_text_clip_survives_et_and_ends_at_q"]
note = "The three parameters the clause says \"may be specified only within a text object and shall not persist from one text object to the next\" are the `TextObject` struct, so BT resets all of them in one line: Tm and Tlm are fields, and Trm \"is actually just an intermediate result\" recomputed per glyph. Table 105's BT initialises both matrices to the identity. A second BT before an ET is malformed; it restarts the text object, and an ET that never arrives is reported. **This note said \"and q/Q cannot save them\" until the five-hundred-and-eighty-sixth session, and Errata Collection 3 had contradicted it**: issue #368 adds to §9.4.2 that within a text object q and Q \"shall additionally push and pop Tm and Tlm as part of the graphics state stack\", and the same collection points §7.8.2 at this clause for it — \"[s]ee 9.4.1, 'General' for additional information that must be managed as part of the graphics state stack when q and Q operators occur within text objects\". Found by running `spec-errata emit` before writing rather than `check` afterwards, which is `doc/todo/02` §4's own distinction: nothing here quoted the sentence, so no quotation checker could see it. ADR 0421. **The same sentence decided a question one crate away in the six-hundred-and-ninth session**: text has no position that persists from one text object to the next, so there is no offset a selection could be a range of and `viewer_core::open::Chosen` carries a page beside each offset rather than a document-wide number — which is what let a drag cross a page boundary at all. ADR 0444."

[[clause]]
clause = "9.4.2"
title = "Text-positioning operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::leading_moves_the_next_line_downwards"]
note = "All four of Table 106: Td translates Tlm and copies it to Tm, TD does the same after setting the leading to the negation of ty, Tm replaces both rather than concatenating onto either, and T* is 0 -Tl TD. Tlm is tracked separately from Tm, which is what makes a run of Td or T* operators land on evenly spaced lines rather than accumulating the showing operators' advances. **And the clause has a fifth requirement that is not in Table 106 and not in the base text**, executed since the five-hundred-and-eighty-sixth session: Errata Collection 3's issue #368 adds \"[w]ithin a text object, the graphics state stack operators q and Q (see 8.4.2, 'Graphics state stack') shall additionally push and pop Tm and Tlm as part of the graphics state stack\", so the interpreter's stack entry is the graphics state *and* the two matrices, restored only inside a text object because outside one Table 105's BT sets them anyway. **The corpus exercises the operators and cannot exercise the rule**: 13 of the 974 documents put a q or a Q inside a text object and not one moves Tm between the two, so no corpus page and no oracle verdict moves either way — which is trap 8's shape and why `text_state.rs` pins it as a *pair* of streams differing only in the q. ADR 0421."

[[clause]]
clause = "9.4.3"
title = "Text-showing operators"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_state.rs::horizontal_scaling_reaches_a_tj_adjustment", "crates/pdf-model/tests/text_extraction.rs"]
note = "All four of Table 107. TJ's numeric elements are subtracted in thousandths of a unit of text space and scaled by Tfs and Th; ' is T* then Tj; \" sets Tw and Tc first. The clause's \"Showing multiple glyphs with one invocation … shall produce the same results as showing them with a separate invocation for each\" holds because nothing here is per-string except the decoding into codes. A string is split into codes by the font — one byte for a simple font, two for the composite encodings this tree reads — which is §9.6.5 and §9.7 rather than this clause."

[[clause]]
clause = "9.4.4"
title = "Text space details"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/type3.rs::a_width_advances_through_the_font_matrix", "crates/pdf-model/tests/composite_fonts.rs::a_vertical_font_advances_down_the_page_rather_than_across_it", "crates/viewer-core/tests/selection_census.rs::what_a_drag_selects_agrees_with_poppler_and_with_the_page"]
note = "The text rendering matrix and the combined displacement for both kinds of font, size and horizontal scaling and rise included, with word spacing applied to a single-byte code 32. **Both writing modes**, which this row denied for three hundred and eighty-four sessions: it read \"[t]he vertical branch is not: ty is always 0, because nothing here reads a vertical writing mode\", and §9.2.4's own row two clauses above says \"[b]oth writing modes, from the thirty-sixth session\". `Interpreter::advance_step` takes a `vertical` flag from `Font::is_vertical`, `pdf_font::LoadedFont::vertical_metrics` supplies §9.7.4.3's `w1`, and the clause's rule — \"a combined displacement shall be computed, denoted by t x in horizontal writing mode or t y in vertical writing mode (the variable corresponding to the other writing mode shall be set to 0)\" — is the `if`. Found in the four-hundred-and-twentieth session by reading this row against the code, which is what `doc/todo/02` §4 asks of a claim; the status was `partial` on the strength of the one sentence and nothing else. `vertical.pdf` now has a test that reads the *text layer* rather than the metrics: two consecutive glyphs of one string share a column and descend, which fails if either half of the branch is swapped. **And the formula decides a question about *extraction* that had been asked of position**, in the four-hundred-and-sixty-fourth session: between two codes of one show string the Tj term of \"tx = ((w0 - Tj/1000) x Tfs + Tc + Tw) x Th\" is absent, so what separates them is the first glyph's own width, plus Tc — which applies to every pair alike and is tracking rather than a word break — plus Tw, which §9.3.3 applies to the single-byte code 32 alone. The only word gap a show string can state is therefore that code, identified by its encoding rather than by a distance, and `Interpreter::separate_text` is called once per show operation instead of once per code because there is nothing else inside one to read. It had been called per code since the beginning and was a no-op after the first, the cursor being set to the post-advance matrix and compared with itself. ADR 0299. **And the displacement is what a person's finger lands on**, which is the other consumer and the one nothing judged until the five-hundred-and-eighty-sixth session: the quadrilateral in `Interpretation::text_layer` is this formula's output, `viewer-core` turns a press into a position against those quadrilaterals, and `selection_census.rs` drags across `pdftotext -bbox -cropbox`'s own word boxes to check that the round trip lands on the word — 1000 of 1011 words over 453 corpus documents, with poppler supplying every endpoint so that no part of the drag comes from the geometry it is judging. It found a defect on its first run, in the loop rather than in the formula. ADR 0421."

[[clause]]
clause = "9.5"
title = "Introduction to font data structures"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs", "crates/pdf-font/src/cff.rs", "crates/pdf-font/src/type1.rs", "crates/pdf-font/src/substituted.rs"]
test = ["crates/pdf-font/src/loading.rs::a_code_for_a_character_means_that_character"]
note = "The introduction to font data structures, and the distinction it draws is the one `pdf-font` is built on: a *font dictionary* is PDF's and a *font program* is not. So Table 124's five program formats are read by `read-fonts` and by this crate's own Type 1 reader, and which reader applies is decided by the program's leading bytes rather than by the dictionary's `/Subtype` — the clause's own separation, taken literally, and the reason a `CIDFont` embedding a bare Type 1 program is read at all (ADR 0049). Every font type the clause lists exists here: Type 1, TrueType, Type 3, Type 0 and the CIDFonts under it. The clause's NOTE 5 is what `oracle.rs`'s `CONTRADICTED_SUBSTITUTED_FONT` has argued from since the sixth session without citing it, and the four-hundred-and-sixth session put the citation where the argument is: 'If a PDF file refers to font programs that are not embedded, the results depend on the availability of fonts in the PDF processor's environment', and 'some details of font naming, font substitution, and glyph selection are implementation-dependent and can vary among different PDF processors and operating system environments'. That is the standard placing substitution beyond itself and saying so, which is why twelve contradicted pages are listed rather than chased — and it leaves the *advances* untouched, which Table 109 states and which `bug847420.pdf` was measured against at eight times the resolution: 1420 device columns of ink against both references' 1419. **The count was seventeen until the four-hundred-and-thirty-first session, which measured the five pages that had been admitted together and never opened** and found that all five name `/Times-Roman` and differ by nothing the substitution explains: at 8× the ink's bounding box is 1233 x 143 at (84, 133) in ours, `poppler`'s and `mupdf`'s alike on `issue8088.pdf`, identical over a 1600-column raster, so the advances *and* the cap height agree. They are `CONTRADICTED_GLYPH_EDGES` and moved there. What is left of the group's sans half is one number rather than a shrug: Liberation Sans draws a capital at 0.687500 em and the `NimbusSans` the references resolve at 0.729167 em, measured from the two files and reproduced exactly off the corpus rasters at 12 pt and 18 pt, which is 1.0% to 7.7% of a page's ink on six pages. ADR 0267 declines to close it and says why. **NOTE 5 decided a second question in the five-hundred-and-sixty-first session, in the other direction**: because the choice of face is ours, so is the direction its contours run in — no file said anything about it — and §9.3.6's NOTE 2 makes that direction visible where two glyphs overlap in a text clip. So a substituted outline is normalised to one winding and an embedded one never is, which is the same boundary this NOTE draws. ADR 0396."

[[clause]]
clause = "9.6"
title = "Simple fonts"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-font/src/loading.rs::the_pdf_widths_agree_with_the_font_programs_own_advances", "crates/pdf-font/src/name_keyed.rs::an_encoding_name_the_table_does_not_permit_is_no_encoding_at_all"]
note = "All three of the clause's properties hold: a simple font's codes are single bytes, each glyph has one horizontal set of metrics, and the descriptor is read where there is one. `LoadedFont::load` sends everything that is not Type0 or Type3 down one path and decides by the *program* the descriptor embeds rather than by `/Subtype`, which is what makes an `OpenType`-wrapped CFF in a `/Type1` dictionary work. **This row said `partial` 'only because the bare Type 1 format of /FontFile is not read (reported, and no corpus page one reaches one)', and both halves stopped being true in the thirty-first session**: `type1.rs` reads §9.9's `/FontFile` — a whole Type 1 program, eexec and charstrings — and 57 corpus documents embed one, against the 'no corpus page one' the note claimed. Found by a sweep in the hundred-and-twenty-second session that took every `partial` note claiming an entry is unread and grepped the tree for it. **And the row's second stale sentence was found the same way in the five-hundred-and-tenth**: it said `partial` for 'the one thing actually left: §9.6.5's MacExpertEncoding, the one name Table 112 permits and Annex D's tables here do not cover' — false since the four-hundred-and-fifty-first transcribed Table D.4's 165 assignments (ADR 0286), which corrected §D and §D.4 and left the three §9.6 rows carrying the refusal, the fifth failure shape's third visit to the font rows. `name_keyed.rs`'s own test asserts every name Table 112 permits is a name this crate has a table for, so `FontError::UnsupportedEncoding` is a branch with no member. An `/Encoding` name the table does *not* permit is a different case and does not refuse the font — see §9.6.5."

[[clause]]
clause = "9.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-font/src/cmap.rs::code_length_comes_from_the_codespace_ranges"]
note = "A simple font's codes are one byte each — `Code::single_byte`, against §9.7's variable-length codes — and there is one width per code and no writing mode 1, which is the clause's second property. The third, that a font descriptor is required except for Type 3 and certain standard Type 1 fonts, is why an absent descriptor is not an error but a substitution request."

[[clause]]
clause = "9.6.2"
title = "Type 1 fonts"
status = "implemented"
code = ["crates/pdf-font/src/cff.rs", "crates/pdf-font/src/type1.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-font/src/loading.rs::a_simple_fonts_subtype_selects_nothing", "crates/pdf-model/tests/type1.rs::a_symbolic_type1_program_draws_what_no_substitute_could"]
note = "Type 1 and MMType1 dictionaries are read and their programs drawn in both of the formats §9.9's Table 124 allows for them: the Compact Font Format of `/FontFile3 /Type1C`, and — from the thirtieth session — the original format of `/FontFile`. Both are name-keyed, which is why one encoding algorithm serves both (§9.6.5.2, `crate::name_keyed`)."

[[clause]]
clause = "9.6.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs", "crates/pdf-font/src/metrics.rs", "crates/pdf-font/src/type1.rs"]
test = ["crates/pdf-font/src/loading.rs::the_pdf_widths_agree_with_the_font_programs_own_advances", "crates/pdf-model/tests/type1.rs::an_embedded_type1_program_puts_glyphs_on_the_page", "crates/pdf-font/src/metrics.rs::a_substitute_is_condensed_to_the_widths_the_file_states"]
note = "Table 109's entries that decide a rendered page: `/BaseFont` (as the substitution request), `/FirstChar` with `/Widths`, `/FontDescriptor`, `/Encoding` and `/ToUnicode`. `/Name` is deprecated in PDF 2.0 and read by nobody, which is what the deprecation asks for. What `standard_metrics.rs` answers to is Table 109's permission to leave the four entries out — \"optional in PDF 1.0-1.7 for the standard 14 fonts\" — with §6.3.2.2's requirement to render the page contents as defined, which is the warrant `pdf_font::standard` carries and the one that survives the erratum below, **and it has a second half since the four-hundred-and-fifth session**: Adobe's published metrics name only the standard character set, so a code whose glyph name is outside it takes the advance the substitute program itself states, which this table's own `/Widths` row requires to agree with the width anyway (\"These widths shall be consistent with the actual widths given in the font program\"). `simple_widths` intended that all along and could not do it: it read the program through `skrifa`'s `FontRef`, which parses an sfnt container and refuses a bare CFF, and ten of the fourteen compiled-in faces are bare CFF. So every serif, fixed-pitch and symbolic standard-14 substitution answered nothing there and fell to Table 120's `/MissingWidth` default of 0. `pdf_font::cff::advances` is the reader that was missing. `issue4304.pdf` is what it cost — `/Differences [32 /.notdef …]` over a non-embedded `/Times-Roman`, six spaces of zero width, and a page that drew *Wordsthatshouldhavespacesbetweenthem.* against four renderers' *Words that should have spaces between them.* It left the oracle's contradicted list, 69 to 68, and it was the only verdict in 1794 that moved. NOTE 1's observation that a CFF is a \"functionally equivalent representation of a Type 1 font program\" is load-bearing here rather than incidental: it is the argument for one encoding algorithm over two readers. **A `/Widths` of nothing but zeroes is conforming and is not repaired**: the clause states no lower bound on a width, so every glyph of such a font is placed at the same point and the selection quadrilaterals built from the advance have no width — a *choice* to leave the file's own arithmetic alone rather than a clause obeyed, and the corpus's only witness is 4 font dictionaries in one fuzzed document (`poppler-90-0-fuzzed.pdf`, `font_metric_census`). **And Table 109's own sentence about `/Widths` decides a second question, in the five-hundred-and-twenty-third session** (ADR 0358): \"These widths shall be consistent with the actual widths given in the font program\" binds the array to the program the document *meant*, so it states how wide the absent font's shapes were and not only where the next glyph starts. `metrics::substitute_stretch` reads it that way for a substituted face -- the median over the declared codes of the stated width over the face's own advance, applied to the outline's x alone -- and `bug1671312_ArialNarrow.pdf` stops drawing letters that collide. The other half was already implemented, and **the two sentences this row rested it on are struck out** — Errata Collection 3's Issue #47 and #48, `/State` `Review` `Completed`, which retire this clause's closing paragraph requiring a processor to provide glyph widths and font descriptor data for the standard 14 where the entries are absent, and the half of Table 109's `/FontDescriptor` row saying that stating them overrides a standard font; both become a cross-reference to §9.6.2.2, and `doc/md/` carries neither change (ADR 0252). This row quoted both as current text until the five-hundred-and-ninety-first session, which is the fourth sweep's shape with an erratum in place of a correction: §9.6.2.2's row and `pdf_font::standard`'s own module documentation have recorded the same erratum since the four-hundred-and-eighteenth, one file away. Nothing moves — what the code rests on is the permission and §6.3.2.2 above — and the override question `mozilla/pdf.js#12725` answers by experiment against Acrobat is one the standard now answers nowhere."

[[clause]]
clause = "9.6.2.2"
title = "Standard Type 1 fonts (standard 14 fonts) (PDF 1.0-1.7)"
status = "implemented"
code = ["crates/pdf-font/src/standard.rs", "crates/pdf-font/src/standard_metrics.rs", "crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-font/src/standard_metrics.rs::known_widths_are_what_the_specification_says", "crates/pdf-font/src/standard_metrics.rs::the_metrics_agree_with_an_independently_drawn_metric_clone", "crates/pdf-font/src/standard.rs::every_compiled_in_face_winds_its_contours_the_same_way"]
note = "All fourteen names, with their published metrics compiled in, so a document that omits `/Widths` is laid out from the specification rather than from whichever face this machine happens to install. The clause used to ask for one of two things — the fonts themselves, or their metrics with suitable substitutes — and until the hundred-and-forty-eighth session only the second was here. **That disjunction is struck out**, and this sentence quoted it as the clause's current words until the five-hundred-and-ninety-first session while the same note records Issue #47 and #48 further down: a row that records an erratum is not a row that has applied it, which is `doc/todo/01`'s newest sweep and the shape it was built for. **Now both are**: `pdf_font::standard` compiles the fourteen font *programs* into the binary — ten bare CFF faces from PDFium's Foxit set and four Liberation Sans, 804 KB, `include_bytes!`d so they cost nothing at launch — and `substitute::find` consults them first for a `/BaseFont` naming one of the fourteen or a metric-compatible clone. So those pages render identically on every machine, and `substitute.rs` is no longer describable as the only machine-dependent code in the tree: the machine's fonts still serve every *other* non-embedded font, where their broader coverage outweighs reproducibility, with the compiled-in set behind them so that a machine with no fonts at all still draws text. The cost is measured and recorded: six oracle pages became contradicted and one agreed, because the three C references resolve a non-embedded standard-14 font through this machine's fontconfig and we were agreeing with them partly by reading their data. ADRs 0007, 0133. **And ten of the fourteen carry the standard Latin character set and nothing else, which the four-hundred-and-thirty-fourth session found costing a page its text.** Foxit's ten are bare CFF programs whose charsets hold §9.6.5's Latin set; a document naming `TimesNewRomanPSMT` — which folds to `Times-Roman` — and stating an `/Encoding` whose `/Differences` name `afii10017` and its neighbours asks that face for Cyrillic it has never had. Because the *Latin* codes of the same font drew, no report fired: five documents of a 65 944-document web survey account for 4912 codes lost in silence, and `0792341.pdf`'s page of Serbian is a grid of empty cells before and a table of names after. `substitute_face` now replaces the chosen face with one of the same family whose code table is a strict superset over the codes Table 109's `/FirstChar` and `/LastChar` declare, so a page can gain marks and cannot lose one; a dictionary stating neither bound decides nothing, which is what keeps `franz.pdf` — no `/FirstChar`, `/Differences` naming `ff`, `ffi`, `ffl` — from trading its typeface for three ligatures it never shows. Not one of the 974 corpus rasters moves. ADR 0270. **A third consumer arrived in the two-hundred-and-eighty-second session**: `standard::is_standard_name` answers whether a *resource name* is one of the fourteen, exactly and without case folding, so a `Tf` naming `/Helvetica` where the resource dictionary defines nothing draws the clause's own font instead of nothing (§7.8.3, ADR 0183). It is deliberately a different and narrower question than `substitute`'s, which folds case and accepts the metric-compatible clones a `/BaseFont` may name: a resource name is arbitrary, so only the clause's own spellings mean anything. **A second consumer arrived in the hundred-and-sixty-sixth session and it is not a document**: `LoadedFont::standard` loads one of the fourteen against an empty `Document`, so text this program generates for *itself* — the outline panel's titles — is set in the face the clause guarantees rather than in whatever the machine has. The route is `LoadedFont::load` on an assembled `/Type1` dictionary, deliberately, so the encoding and the metrics are the same two answers a file naming `/Helvetica` gets. **And the clause's own disjunction is what puts the artwork beyond it**, which the two-hundred-and-thirty-fifth session measured on the one corpus document that is nothing else: `standard_fonts.pdf` sets specimen text in all fourteen over fourteen pages, with no program embedded, and the five renderers' ink on page 7 is `poppler` 15.506, `mupdf` 16.402, ours 17.487, `ghostscript` 18.374, `hayro` 20.518 — a third of the page apart, with ours the second-nearest to `hayro` of the ten pairs and `poppler` the furthest from everybody. Four sets of outlines behind one list of names, and the clause requires none of them. `AMBIGUOUS_STANDARD_FOURTEEN_FACE` holds all fourteen pages. **And the disjunction's two halves have to be read together, which the four-hundred-and-fifth session found out by paying for it**: the metrics half answers only the glyph names Adobe published, so a document reaching any other glyph — `/Differences [32 /.notdef]` on `issue4304.pdf` — needs the *program* half to state the advance, and `simple_widths` could not read one out of the ten compiled-in bare CFF faces. §9.6.2.1's row carries the mechanism. What is worth recording here is that the fix does not make the layout a property of the chosen substitute: the number it produces, 250 for `.notdef`, is the same in `FoxitSerif`, `NimbusRoman` and every other metric clone of the face, because each gives `.notdef` its own `space` width — which is also why four references agreed against us. **Errata Collection 3 removes this clause's `shall`** (Issue #47 and #48, `/State` `Review` `Completed`): 'These fonts, or their font metrics and suitable substitution fonts, shall be available to the PDF processor.' is struck outright, the paragraph above it becomes an informative NOTE, and that paragraph's 'shall have' becomes 'are required to have'; §9.6.2.1's 'For compatibility reasons PDF processors shall provide glyph widths and font descriptor data for those standard fonts' goes the same way, replaced by a cross-reference. Three doc comments quoted the struck sentence as this program's warrant for the compiled-in fourteen and were corrected in the four-hundred-and-seventeenth session — the warrant is now Table 109's 'optional in PDF 1.0-1.7 for the standard 14 fonts' and §6.3.2.2, which is a stronger one. ADR 0253. **And that second consumer was asking the wrong question, which the four-hundred-and-ninety-first session found by measuring it.** A character code is one byte, so the assembled `/Type1` dictionary reaches the 149 characters `StandardEncoding` names — while the compiled-in Helvetica is Liberation Sans and states 668. Every accented Latin letter, every Greek letter and every Cyrillic letter in a panel of §12.3.3's titles, §8.11.4.3's layer names or §14.3.3's `/Info` was therefore drawn as a placeholder box by a face that had the glyph all along. `LoadedFont::character_glyph` asks the face by character, which is what a caller holding no encoding is actually asking, and `pdf-model --example interface_font_census` prices it: of the 54 corpus documents whose panels lost a character, 41 lose none at all now, and what remains is Hebrew, Thai and CJK in five documents. **A document's own text is untouched and must be**: it selects glyphs by code through §9.6.5 and §9.7.6, and drawing one the file did not select would be inventing what the page says. ADR 0326. **The clause's substitution `shall` was about *having* a face and not about what it looks like**, which the five-hundred-and-twenty-third session recorded while reading it for ADR 0358 — and it quoted the sentence as current text, four sentences from this note's own record that Issue #47 and #48 strike it out, which the five-hundred-and-ninety-first session corrected. **The conclusion is stronger without it**: nothing anywhere in §9.5, §9.6.2.2, §9.8.1 or Table 120 states a requirement on a substituted glyph's shape, and with the availability `shall` retired the clause states no requirement about a substituted face at all. So the horizontal scale that round built is a documented choice in the place §9.5's NOTE 5 leaves open, derived from the file's own `/Widths` rather than from any renderer's pixels. **And the fourteen are one set, which the five-hundred-and-sixty-first session found this program's stand-ins were not.** The clause's own first sentence is 'The PostScript language names of 14 Type 1 fonts, known as the standard 14 fonts, are as follows: …', so a document may draw two of them into one path and the fourteen it names do not disagree with each other about which way a contour runs. Ten of the compiled-in faces are Foxit's bare CFF and four are Liberation Sans `sfnt`s, and the two formats carry opposite conventions — measured at −0.186 against +0.165 signed em² for a capital B — which §9.3.6's non-zero winding rule turns into a *hole* wherever a glyph of one overlaps a glyph of the other inside a text clip. The set is normalised now, counter-clockwise, which is the direction ten of the fourteen already had and the one the Type 1 charstrings among them are drawn in; the standard states no direction anywhere, so the choice is documented as a choice. `standard.rs::every_compiled_in_face_winds_its_contours_the_same_way` asserts the property of the *set*, with no reference in it. ADR 0396. **And the fourteenth name has a hyphen in it, which `doc/md/` loses and this tree built an argument on** (Issue #384, `/State` `Review` `Completed`): the erratum replaces that first sentence's inline list with a bulleted one in alphabetical order, and its own text spells the name `Courier-BoldOblique`. The conversion writes `CourierBoldOblique`, because the standard sets the name broken across a line and the converter drops the hyphen of a word it breaks — the failure `conformance::prose::folded` is written for, here producing a *name* rather than a mangled word. `pdf_font::standard::STANDARD_NAMES` carries fifteen entries for the fourteen names and argues the unhyphenated one \"reads as the standard's own typography\"; it reads as the conversion's, and a second extractor over the same page prints `Courier-` and `BoldOblique` on two lines. Nothing moves in what this program accepts — a resource named `/CourierBoldOblique` is still matched, now as a documented tolerance for a producer that copied the same broken line rather than as one of the clause's own names — and the blockquote quoting the retired inline list stays verbatim, because `doc/md/` is what the quotation gate verifies against. Owed to `standard.rs`: the annotation, and the corpus count that says whether any file writes it."

[[clause]]
clause = "9.6.2.3"
title = "Multiple master fonts"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-font/src/loading.rs::a_simple_fonts_subtype_selects_nothing"]
note = "Nothing distinguishes an MMType1 dictionary from a Type 1 one here, and the clause is why: an embedded multiple-master instance \"shall be an ordinary Type 1 font program\", a snapshot with the design coordinates already chosen. `/Subtype` is not consulted for a simple font at all, so the entry costs no code. The BaseFont naming convention the clause describes is explicitly \"not prescribed as part of this specification\", so nothing parses design coordinates out of a name."

[[clause]]
clause = "9.6.3"
title = "TrueType fonts"
status = "implemented"
code = ["crates/pdf-font/src/truetype.rs", "crates/pdf-font/src/sfnt.rs"]
test = ["crates/pdf-font/src/truetype.rs::a_symbol_subtable_is_addressed_through_the_high_byte_of_its_range", "crates/pdf-font/src/truetype.rs::a_unicode_subtable_is_reached_through_the_adobe_glyph_list", "crates/pdf-font/src/truetype.rs::a_loca_whose_offsets_descend_is_rebuilt_in_glyph_order", "crates/pdf-font/src/truetype.rs::a_glyph_whose_offset_repeats_is_empty_and_stays_empty"]
note = "Both formats the clause requires — `TrueType` and `OpenType` (ISO/IEC 14496-22) — through `skrifa`, which reads glyf and CFF outlines from an sfnt alike. The clause's own content beyond that is the three differences from Table 109, and the load-bearing one is a pointer to §9.6.5.4. `/BaseFont` is used only as a substitution request, so the two ways the clause says to derive it are the producer's problem rather than a reader's. **Two malformations of the glyph table are repaired rather than refused, both from what the file states twice about itself**: a byte-swapped `indexToLocFormat`, and a `loca` whose offsets do not ascend — the second rebuilds `glyf` in glyph order because an entry is self-describing and only one of the two readings is self-consistent (ADR 0170). Six of the corpus's 623 embedded TrueType programs carry a non-monotonic `loca`, and before the repair 36 of one font's 71 glyphs drew nothing in silence. **The repair itself was wrong about one case for sixteen sessions**: the glyph table writes a glyph with *no outline* by giving it and its successor the same offset, which is self-consistent whatever the rest of the table does, and reading such a glyph's length from its own bytes hands it whichever entry begins there. `issue7074_reduced.pdf`'s `loca` runs 0, 108, 0, 108, 108, … so its space — glyph 3 under `Identity-H` — was given glyph 4, and the page drew `Our|2015|Graduates`. The repair now honours a repeated offset and overrules only a *descending* pair, which is the only reading that is not self-consistent (ADR 0174)."

[[clause]]
clause = "9.6.4"
title = "Type 3 fonts"
status = "implemented"
code = ["crates/pdf-model/src/type3.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/type3.rs::each_shown_code_runs_its_glyph_description", "crates/pdf-model/tests/type3.rs::a_width_advances_through_the_font_matrix", "crates/pdf-model/tests/type3.rs::a_code_with_no_glyph_description_paints_nothing_and_still_advances", "crates/pdf-model/tests/type3.rs::a_glyph_that_shows_itself_reaches_a_bound_and_stops", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_type3_glyph_description_draws_its_prefix_and_reports_the_shortfall", "crates/pdf-model/tests/names_are_bytes.rs::a_type_3_glyph_name_that_is_not_utf_8_paints", "crates/pdf-model/tests/names_are_bytes.rs::two_type_3_glyph_names_differing_only_outside_utf_8_are_two_names"]
note = "Implemented in the tenth session; before it a Type 3 font was refused. All four of the clause's steps, the CTM it defines for a glyph description, the inherited graphics state, the resources §7.8.3 designates, /Widths read through /FontMatrix, and both of Table 111's operators including d1's colour restriction. /FontBBox is read by nobody, and the permission is narrower than this sentence claimed until the three-hundred-and-seventy-second session: Table 110 says \"[i]f all four elements of the rectangle are zero, a PDF processor shall make no assumptions about glyph sizes based on the font bounding box\", and where an element is not zero \"the font bounding box shall be accurate\" with marks outside it making behaviour implementation dependent. Reading neither box is conforming under both halves — nothing in the clause *requires* a processor to use one — and Table 111's `d1` box is ignored for the reason `content.rs` states beside the operator: it \"shall be correct\", so clipping to it can only remove marks a correct file does not have, and on an incorrect one it hides the defect rather than drawing what the producer drew. `issue14953.pdf` is the corpus witness — an embedded ConTeXt Type 3 face declaring `0 0 0 0` for its font box and for all fifteen of its glyphs — where `ghostscript` draws nothing above 72 dpi and `poppler` loses the glyphs as the pixels shrink, both clipping to the declared box, while this tree and `mupdf` render a synthetic A/B of that file byte-identically. Table 111's own sentence covers all of it: \"[i]f any marks fall outside this bounding box, the result is implementation-dependent\". `AMBIGUOUS_DEGENERATE_GLYPH_BOX` in `oracle.rs` carries the ladders. **Step d)'s resource rule is Errata Collection 3's since the four-hundred-and-eighteenth session** (Issue #128, `/State` `Review` `Completed`): the clause used to name two places, the Type 3 font dictionary and then the page, and it now points at §7.8.3, whose amended search puts *the glyph description content stream's own dictionary* in front of both. That step was missing — `type3::Type3Font::resources` had the other two — so a glyph stream carrying its own `/Resources` was read against the font's or the page's. `tests/type3.rs::a_glyph_description_finds_the_resources_its_own_stream_names` is what holds it, built so that only the new step can answer. Table 110's `/Resources` row is rewritten by the same erratum and loses the page fallback it used to state; that fallback survives in §7.8.3's steps 3 and 4, which is why nothing else here moves. **The *colour* half of Table 111 has its own witness since the three-hundred-and-seventy-ninth session, and it is the sharper of the two because the two readings are not near each other.** `issue12705.pdf` embeds a Type 3 font whose 114 glyph descriptions all begin with `d1`, and every one of the 111 that paints then states `1 1 1 rg` before filling; the page's content stream sets no colour, so the current colour is black. A processor honouring those operators would paint white on white and the page would be blank. Table 111 says \"any use of such operators shall be ignored and the glyph stream continues to be processed without error\" and that the description \"is executed solely to determine the glyph's shape\", and all five renderers draw the line, so all five apply the rule. That file also states `1000 1000 0 0` for the `d1` box of its LF, CR and space descriptions — a rectangle inverted on both axes, which `poppler` warns about once per space on the page — and it costs nothing because those three make no marks. `AMBIGUOUS_UNCOLOURED_GLYPH_PROCEDURE` in `oracle.rs`. A glyph description whose marks are an inline image draws nothing yet and reports, which is §8.9.7's gap rather than this one's: 10 corpus documents are in that position. **And what a description *shows* is the glyph, not text of the page** — the clause makes it \"a content stream that contains the operators that paint the glyph\", so a `Tj` inside one is how the glyph is drawn and belongs to no readback. `pr4922.pdf` is the witness and it took a correction to find: its Type 3 glyphs are drawn by showing a character of another font, so the moment §9.10.2's second method reached Type 3 fonts the page read back every character twice — \"pp2200--4400::\". `Interpreter::read_back` returns early inside a description (the three-hundred-and-twenty-sixth session), which is the same `glyph_depth` counter §9.6.4's `d1` colour restriction already uses. **The clause's own last resort is here too**: where a name is one the Adobe Glyph List does not know, §9.10.2's closing sentence permits \"a character code of their choosing\" and this tree chooses the *code*, bounded to the printable ASCII range 0x21–0x7E where a byte and a code point agree under every encoding §9.6.5 states. `dvips` names every Type 3 glyph `aNN` after its own code, which is why `issue918.pdf` was 193 words of English reading back as nothing. **And the same clause's last resort is what the four-hundred-and-seventy-sixth session took as far as it goes and no further**: `french_diacritics.pdf`'s `/a192` is neither in the two lists §9.10.2 names nor derivable from anything the file states, so it stays refused with the clause's own words — \"there is no way to determine what the character code represents\" — and the page now says how many of its codes are in that position rather than reading back one character of twenty-nine in silence. §9.10.2's row has the reading; ADR 0311 has why `aNNN` is a producer's convention and why the printable-ASCII bound may not be widened past it. **A glyph description that decoded only part-way is drawn and reported**, since the five-hundred-and-twenty-fourth, and Table 110 makes the prefix rule stronger here than anywhere else it applies: the stream \"shall include as its first operator either d0 or d1\", so any prefix carrying a mark carries the glyph's own declaration ahead of it, and `/Widths` rather than the description supplies the advance — so the damage costs marks inside one glyph and never the position of the next. That is the opposite of ADR 0343's refusal of a damaged font *program*, where the prefix produced other glyphs entirely. ADR 0359. **And the colour half was half wrong for as long as it existed, which the five-hundred-and-fifty-eighth session found in `pdf-differences`' `Type3Test.pdf`**: `d1` was read as collapsing the two colour parameters into one, on Table 111's singular \"[i]ts colour shall be determined by the graphics state in effect each time this glyph is painted by a text-showing operator\", so a description that stroked stroked in the *non-stroking* colour. The clause refutes that three times. §9.6.4's sentence about what a stroking description must set for itself names \"the line width, line join, line cap, and dash pattern\" and no colour, because the inherited one is what it gets; NOTE 2 is plural — \"it is unnecessary and undesirable to initialise the current colour parameters because the text-showing operators are designed to paint glyphs with the current colours\"; and the clause's own EXAMPLE states a distinct `RG` before each of its three `Tj`s for a `d1` `square` whose description is `72 w 0 0 750 750 re B`, which the collapsed reading makes dead syntax. `d1` now does only what §8.6.8 says it does — the description's own colour operators are ignored — and both inherited parameters are used by whichever operation selects them. `poppler` and `ghostscript` collapse them as this tree did and `mupdf` does not, which is the reference majority being wrong on a page whose correct appearance the PDF Association publishes. One document of the 974 moves with it, and it is the corpus's own copy of the same file. ADR 0393. **And the description is read as a *source* since the five-hundred-and-ninety-second**: a glyph description runs once per character drawn with it, which is exactly why the memo rather than the window decides — an ordinary description is decoded once and hit thereafter, and only one too large for the cache to hold is pumped per run. ADR 0427. **Step b)'s lookup is a comparison of bytes**, since the six-hundred-and-fourth session: the code-to-name table was a `String` built with `from_utf8_lossy`, so a font whose glyph names carry a byte outside UTF-8 painted nothing at all — which this clause makes silent — and two such names were one name, each painting the other's description. It is a `Name` now, and §7.3.5 is why (ADR 0439)."

[[clause]]
clause = "9.6.5"
title = "Character encoding"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs", "crates/pdf-font/src/glyph_names.rs", "crates/pdf-model/src/type3.rs"]
test = ["crates/pdf-font/src/loading.rs::the_pdf_widths_agree_with_the_font_programs_own_advances", "crates/pdf-font/src/truetype.rs::a_symbol_subtable_is_addressed_through_the_high_byte_of_its_range"]
note = "The base encodings, /Differences over them, and the symbolic/nonsymbolic distinction the clause turns on. **This row said 'MacExpertEncoding is the one named encoding absent: a font naming it is refused and reported' until the five-hundred-and-tenth session**, fifty-nine sessions after the four-hundred-and-fifty-first transcribed Table D.4 and retired exactly that refusal (ADR 0286) — the session that implements a mechanism does not maintain the rows of the clauses that need it, and this clause and Annex D do not cite each other. All four names Table 112 permits have tables now, which `name_keyed.rs::an_encoding_name_the_table_does_not_permit_is_no_encoding_at_all` asserts. The clause has no prose of its own above §9.6.5.1. **And `/Differences` is now written as well as read** (two-hundred-and-eighty-fourth session, ADR 0184): §12.7.4.3's appearance builder invents a font where `/DR` defines none, and a value whose character the base encoding has no code for gets one — \"an array of character codes and character names\", the names from the Adobe Glyph List. (**The quotation said \"glyph names\" until the four-hundred-and-thirteenth session**, in this row, §9.6.5.1's and §12.7.4.3's; Table 112's own word is *character* names. Found by the eleventh sweep, which is the first thing in this project to check a ledger note's quotation marks — `tools/conformance` verifies a rustdoc blockquote and has never read this file's prose.) That is this clause's own answer to a character with no code, and it is the only place in the tree that *states* an encoding rather than obeying one. Every code the /Differences array names is kept even where the name is one no encoding defines, which is what §9.6.5.4's undefined-entry sentence does and does not say."

[[clause]]
clause = "9.6.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs", "crates/pdf-font/src/glyph_names.rs"]
test = ["crates/pdf-font/src/encoding.rs::the_assignments_made_only_in_the_notes_are_present", "crates/pdf-font/src/name_keyed.rs::differences_without_a_base_encoding_layer_over_the_programs_own", "crates/pdf-font/src/name_keyed.rs::a_named_base_encoding_still_wins"]
note = "Table 112's encoding dictionary in full, and Annex D's tables for StandardEncoding, WinAnsiEncoding and MacRomanEncoding, extracted from the standard's own Table D.2 rather than written from memory. The thirtieth session found the *default* base encoding was being taken from the wrong sentence: Table 112 gives it three cases and the first is \"for a font program that is embedded in the PDF file, the default base encoding shall be the font program's built-in encoding\", with the Symbolic flag deciding only among the other two. A name-keyed embedded program was getting StandardEncoding whenever its descriptor was nonsymbolic. **This row said 'MacExpertEncoding is the one named encoding absent: a font naming it is refused and reported' until the five-hundred-and-tenth session** — the same sentence as §9.6.5's, retired for both by the four-hundred-and-fifty-first's Table D.4 transcription (ADR 0286): `BaseEncoding::MacExpert` is the fourth table, and the clause's whole Table 112 now resolves against data this crate holds. **And `/Differences` is now written as well as read** (two-hundred-and-eighty-fourth session, ADR 0184): §12.7.4.3's appearance builder invents a font where `/DR` defines none, and a value whose character the base encoding has no code for gets one — \"an array of character codes and character names\", the names from the Adobe Glyph List. (**The quotation said \"glyph names\" until the four-hundred-and-thirteenth session**, in this row, §9.6.5.1's and §12.7.4.3's; Table 112's own word is *character* names. Found by the eleventh sweep, which is the first thing in this project to check a ledger note's quotation marks — `tools/conformance` verifies a rustdoc blockquote and has never read this file's prose.) That is this clause's own answer to a character with no code, and it is the only place in the tree that *states* an encoding rather than obeying one."

[[clause]]
clause = "9.6.5.2"
title = "Encodings for Type 1 fonts"
status = "implemented"
code = ["crates/pdf-font/src/cff.rs", "crates/pdf-font/src/type1.rs", "crates/pdf-font/src/name_keyed.rs"]
test = ["crates/pdf-font/src/loading.rs::an_uncovered_code_has_no_glyph_rather_than_a_guessed_one", "crates/pdf-font/src/name_keyed.rs::a_font_with_no_encoding_entry_is_its_own", "crates/pdf-model/tests/type1.rs::a_symbolic_type1_program_draws_what_no_substitute_could"]
note = "Implemented for both Type 1 program formats, which since the thirtieth session is both of the ones Table 124 defines. The encoding's names are resolved through the program's charset, the font dictionary's `/Encoding` overrides the program's built-in one, and — the clause's second paragraph — a `/Differences` name is looked up in the charset whether or not any encoding references it. The clause's own sentence about the built-in encoding being \"an 'encoding' array that is part of the font program\" is read from the program in both formats. **And the closing sentence is implemented since the two-hundred-and-eighty-seventh session**: \"[i]f an encoding maps to a character name that does not exist in the Type 1 font program, the .notdef glyph shall be substituted\". It is applied when the outline is asked for rather than written into the code table, because the table is what `glyph_index` answers with and three instruments read that answer — `codes_without_a_glyph`, the refusal of a font whose every code resolved to nothing, and the whitespace check. **A fourth reads it since the four-hundred-and-thirty-fourth session**, and it is the one this sentence's `.notdef` decides: `substitute_face` compares the code tables of two candidate faces and counts a code answered with glyph 0 as *unanswered*, because that is what this clause makes `.notdef` mean. `0546109.pdf`'s `/Differences` names `/.notdef` eleven times, a name-keyed CFF has a glyph for it and an `sfnt` reached through the Adobe Glyph List has no character for it, so counting it would have made every wider face look worse than the one in hand (ADR 0270). Substituting in the table would have obeyed the `shall` by blinding the gates that watch it. **One documented departure**: not for a code that *means whitespace*. A subset routinely omits `space`, a designer's `.notdef` is routinely a box — `PDF-Declarations.pdf`'s are 27 path commands — and a box where a gap belongs is trap 1's confident wrong mark. **Measured before it was written**: over the 974 first pages and the 14 specification PDFs the substitution changes not one pixel, so this is a clause implemented for the documents that will arrive. **The other half of that sentence is a width, and it was missing until the four-hundred-and-fifth session.** A `/Differences` may name `.notdef` outright — `issue4304.pdf` does, at code 32 — and then the clause's own guarantee that every Type 1 program contains such a glyph is what makes the code *encoded* rather than unmapped, so §9.2.4's horizontal displacement is the program's advance for it and not `/MissingWidth`'s default. Drawing nothing for the glyph is right and was already right; advancing by nothing was not. §9.6.2.1's row has the reader."

[[clause]]
clause = "9.6.5.3"
title = "Encodings for Type 3 fonts"
status = "implemented"
code = ["crates/pdf-model/src/type3.rs"]
test = ["crates/pdf-model/tests/type3.rs::each_shown_code_runs_its_glyph_description", "crates/pdf-model/tests/type3.rs::a_code_with_no_glyph_description_paints_nothing_and_still_advances"]
note = "The mapping is taken entirely from the font's /Encoding, and there is no default glyph name — a code the encoding does not name reaches nothing, as the clause's NOTE requires. /BaseEncoding is read underneath /Differences because Table 112 defines it for any encoding dictionary; there is no built-in encoding to fall back on, since §9.6.5.1 excepts Type 3 fonts from having one. **A Type 3 font's names are therefore the producer's alone**, which the four-hundred-and-seventy-sixth session had to establish before it could refuse one: with the `/Differences` array \"the complete character encoding for this font\" and the NOTE denying a default glyph name, no base encoding stands behind a name like `/a192` and there is no §9.6.5 table to appeal to when the Adobe Glyph List does not hold it. That is the strongest half of the argument in ADR 0311, and it is this clause's rather than §9.10.2's."

[[clause]]
clause = "9.6.5.4"
title = "Encodings for TrueType fonts"
status = "implemented"
code = ["crates/pdf-font/src/truetype.rs"]
test = ["crates/pdf-font/src/truetype.rs::a_symbol_subtable_is_addressed_through_the_high_byte_of_its_range", "crates/pdf-font/src/truetype.rs::a_unicode_subtable_is_reached_through_the_adobe_glyph_list", "crates/pdf-font/src/truetype.rs::a_suffixed_glyph_name_reaches_the_program_rather_than_its_base_letter"]
note = "All five routes from a code to a glyph, implemented in the eighth session — see ADR 0015. The predecessor was one line that covered about one and a half of them and was worth 15 contradicted corpus pages. **The last of the five was reached by nothing until the forty-second session**, and both halves of the reason are worth keeping. The clause maps a glyph name to Unicode \"by consulting the Adobe Glyph List and Adobe Glyph List for New Fonts\", which are *lists*, and no entry in either contains a FULL STOP — but `read_fonts` implements the wider Adobe Glyph List *Specification*, whose algorithm for an unlisted name strips everything after the first period, so `o.sc` answered `o` and hid the clause's next sentence behind a real letter. And that next sentence names \"the font program's `post` table (if one is present)\", which a CFF-based OpenType does not have in any useful sense: `issue215.pdf`'s `OTTO` carries a version 3.0 `post`, which by definition holds no names, and names every glyph in its `CFF ` charset instead — the same structure by another name, §9.6.2.1's NOTE 1 again. With both read, the page draws the small capitals its own `/ToUnicode` says it means (ADR 0050). **And the clause's opening sentence binds the substitution path, which it did not reach until the hundred-and-ninety-seventh session.** \"If the font has a named Encoding entry of either MacRomanEncoding or WinAnsiEncoding , or if the font descriptor's Nonsymbolic flag (see \"Table 121 -Font flags\") is set, the PDF processor shall create a table that maps from character codes to glyph names\" — and a substituted font built its table from the *substitute's* character set instead, because `substitute::Request` picked the standard-14 `Symbol` off a name containing the word and `substitute_code_table` then took its encoding from that family. `issue8697.pdf` drew Greek where the file states WinAnsi and Table 121's Nonsymbolic bit. The two conditions are read in `substitute::states_latin_codes`, and the same statement holds for a Type 1 program by §9.6.5.2's \"An Encoding entry in the PDF font dictionary, if present, shall override a Type 1 font's mapping from character codes to character names\". ADR 0158. **And a witness for the end of every route, from the two-hundred-and-forty-eighth session.** `issue14821.pdf` shows codes 101, 120 and 49 — `e`, `x`, `1` — through `/ABCDEE+Segoe#20UI,Bold`, a nonsymbolic `TrueType` subset whose `/Encoding` names object 26, **which is a content stream**: §9.6.5 makes that entry a name or a dictionary and the file wrote neither. Every route this clause states then ends at nothing in that font: its `(3,1)` `cmap` maps all three characters to glyph 0, its `post` is version 3.0 and carries no glyph names at all, and there is no `(1,0)` subtable. So the subset does not contain those three characters by any means the standard defines, and drawing nothing is the answer rather than a gap — `doc/todo/21`. `poppler` draws them, from a face this machine has. **And the clause's opening sentence binds a second thing since the four-hundred-and-thirty-fourth session**: the table it requires a processor to create maps codes to glyph *names*, and where the font is not embedded those names have to reach a face that has them. `substitute_face` is that step — a face is replaced by one of the same family whose table over Table 109's declared codes is a strict superset — and it is what a 65 944-document web survey found 22 435 codes lost to, five documents of them Cyrillic through a compiled-in face that carries only Latin. ADR 0270. **And one thing sits *after* every method and permission of this clause, which is where the four-hundred-and-sixty-fourth session put it rather than inside them.** §9.3.3 names a character — \"the ASCII SPACE character (20h)\", of \"every occurrence of the single-byte character code 32 in a string\" — and that is a statement about an encoding rather than one of this clause's routes from a code to a meaning. So `Font::text` falls back to U+0020 for such a code only once `/ToUnicode`, the glyph name, the character collection, the program's own tables and the printable-ASCII choice have all declined; a font that says what code 32 means is believed, and `text_from_the_code` still excludes 0x20 for its own reason. ADR 0299."

[[clause]]
clause = "9.7"
title = "Composite fonts"
status = "partial"
code = ["crates/pdf-font/src/composite.rs", "crates/pdf-font/src/cmap.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::the_cid_widths_agree_with_the_font_programs_own_advances"]
note = "Aggregate of the sixteen rows below, reviewed as a family in the twentieth session (ADR 0029). (**The count said seventeen until the four-hundred-and-thirty-seventh session**; the tenth sweep found it and no reading of the family gives that number.) A Type 0 font is two independent mappings and both are now read: §9.7.6.2's CMap from codes to CIDs, embedded (§9.7.5.3) or Identity, and §9.7.4.2's CID to glyph index, through a CID-keyed CFF's charset or a /CIDToGIDMap stream. **This row named two gaps and both had been closed before it was last touched** — corrected in the four-hundred-and-twenty-ninth session, by `doc/todo/01`'s fourth sweep run over the nouns fifteen rounds gave the tree. It said what was left is \"Table 116's seventy-odd predefined CMaps, refused by name on 15 corpus fonts\", which shipped in the hundred-and-fifty-sixth session — all 239 of Adobe's files are compiled in and §9.7.5.2's own row has read `implemented` ever since, the same sentence §9.7.6's row was corrected for in the two-hundred-and-ninety-eighth — and \"vertical writing, which is §9.2.4's missing /W2 metrics\", where §9.2.4 has read both writing modes since the **thirty-sixth** session and says so in its own note. Three rows in this family and §9.3.7 carried one or other half of that pair; the sessions that implement a mechanism do not maintain the rows of the clauses that need it. `partial` now for what its children actually owe: §9.7.5.4's `beginbfchar` in an `Encoding` CMap, which is two subclauses of the standard disagreeing and is decided in that row, and §9.7.5.1's remainder."

[[clause]]
clause = "9.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/composite.rs", "crates/pdf-font/src/cmap.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_utf8_shaped_codespace_yields_codes_of_several_lengths"]
note = "The clause's one requirement of a processor is the sentence that separates a composite font from a simple one: \"for composite fonts, a sequence of one or more bytes are decoded to select a glyph from the descendant CIDFont\". `LoadedFont::decode` returns a `Code` carrying its own byte length for exactly that reason, and the one-descendant limitation the clause states is why only `/DescendantFonts`' first element is read."

[[clause]]
clause = "9.7.2"
title = "CID-Keyed fonts overview"
status = "inapplicable"
note = "Architecture rather than requirement: it introduces CIDs, character collections and the CMap/CIDFont split, and every normative sentence it contains is restated with effect in §9.7.3 to §9.7.6. Its one processor-facing statement — that a CMap's mapping \"yields a font number (which in PDF shall be 0)\" — is §9.7.6.2's, and is honoured there by reading only the first descendant."

[[clause]]
clause = "9.7.3"
title = "CIDSystemInfo dictionaries"
status = "inapplicable"
note = "`/CIDSystemInfo` identifies the character collection a CMap and a CIDFont share, and the clause's requirements are on the *file*: \"In order for a CIDFont and a CMap to be compatible, their Registry and Ordering values shall be the same.\" A renderer that takes both from the same file has nothing to decide — the CIDs it resolves come from that file's own CMap and are looked up in that file's own CIDFont, so a mismatch cannot change which glyph is drawn. It would matter for a *predefined* CMap, where the collection is external; that is §9.7.5.2's gap, and this row will need revisiting with it."

[[clause]]
clause = "9.7.4"
title = "CIDFonts"
status = "implemented"
code = ["crates/pdf-font/src/composite.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_vertical_cmap_takes_the_second_set_of_metrics"]
note = "Table 115's `CIDFont` in full for what it decides about a drawn page: both subtypes, both glyph-selection routes (§9.7.4.2), and both sets of metrics (§9.7.4.3). `/CIDSystemInfo` is read only to the extent §9.7.5.2's compatibility rule needs, since an embedded program's own charset is what actually selects the glyph."

[[clause]]
clause = "9.7.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/composite.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::the_cid_widths_agree_with_the_font_programs_own_advances"]
note = "Table 115. `/Subtype`, `/CIDSystemInfo`, `/FontDescriptor`, `/DW`, `/W`, `/DW2`, `/W2` and `/CIDToGIDMap` are read. **This row said `/DW2` and `/W2` were not read and that a font whose CMap asks for vertical writing is refused before they would be needed, and both halves had been false since the thirty-sixth session** — `Vertical::read` reads the pair, §9.2.4's and §9.7.5.1's own rows have said so for hundreds of sessions, and `vertical.pdf` is drawn. Corrected in the five-hundred-and-second while reading this table for something else, which is `doc/todo/01`'s shape: the sessions that implement a mechanism do not maintain the rows of the clauses that need it. **What was read here is `/FontDescriptor`, which has a second reader since the same session**: Table 119 gives a Type 0 dictionary no descriptor of its own, so §12.7.4.3's `Metrics::read` follows the one-element `/DescendantFonts` array to this table for the `/Ascent` and `/Descent` a form field's baseline is built from (ADR 0337). The clause's prohibitions are on the file rather than on a reader — a CIDFont \"may not be used as the operand of the Tf operator\" — and are satisfied by construction, because nothing reaches a descendant except through its Type 0 parent."

[[clause]]
clause = "9.7.4.2"
title = "Glyph selection in CIDFonts"
status = "implemented"
code = ["crates/pdf-font/src/composite.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::the_cid_widths_agree_with_the_font_programs_own_advances", "crates/pdf-model/tests/ocr_text_layer.rs::the_remapping_stream_is_read_not_the_identity"]
note = "All four routes, in the twentieth session (ADR 0029). A CID-keyed CFF goes through its charset; a CFF whose Top DICT has no CIDFont operators, and a TrueType with `/CIDToGIDMap /Identity` or none, take the CID as the glyph index; a `/CIDToGIDMap` stream is read as Table 115's two big-endian bytes at 2c. **The stream route has a hand-built witness since the five-hundred-and-fifteenth session** (ADR 0350, trap 8): no corpus document combines a remapping stream with a wholly hollow program, which `examples/hollow_glyph_census` counts rather than assumes — 42 fonts in 30 documents state a stream, 214 of 221 readable embedded programs have *some* empty glyph, none has only empty glyphs, and the intersection is empty (`issue7901.pdf`'s stream selects real outlines; `issue14821.pdf`'s empty glyphs sit under the identity) — so `test-scenes::scanned_ocr_pdf` pins the combination OmniPage CSDK 22 actually writes, a stream that is the identity for no CID it covers over a program whose every `loca` entry is empty, with the `/W`-against-`hmtx` cross-check agreeing only through the stream. Two findings. The route is chosen by what the *program* is, not by `/Subtype`, because a CIDFontType0 may arrive as a CFF inside an OpenType wrapper. And an explicit `/CIDToGIDMap` stream outranks \"the CIDs shall be used directly as GID values\": Table 115 conditions the entry's presence on Type 2 and defines its meaning unconditionally, and `issue7901.pdf` — a CIDFontType0 wrapping a name-keyed CFF and carrying such a stream — drew garbage under the other reading where four renderers draw its sentence. The clause's own rule for a non-embedded program is honoured too: the entry \"shall be ignored\", and a substituted font reaches glyphs through `/ToUnicode` instead. **And that route ends in a sentence this row did not quote until the three-hundred-and-seventy-ninth session**, which is what decides how far the oracle can be believed about such a page: \"[t]he means by which this is accomplished are implementation-dependent\", after \"CIDs shall not participate in glyph selection, and only predefined CMaps may be used with this CIDFont\". `issue9084.pdf` is the corpus witness — a non-embedded `ArialMT` under `Identity-H` with no `/CIDToGIDMap`, reachable only through `/ToUnicode` — where the four references split into two camps 1.46 of 255 apart and stay there at four resolutions. What the clause does *not* leave open is the placement: \"[e]ven though the CIDs are not used to select glyphs in a Type 2 CIDFont, they shall always be used to determine the glyph metrics\", and at eight times the page's scale ours and `mupdf` put the line's ink in the same 1022x123 box to the pixel while all four span the same width to 0.7%. `AMBIGUOUS_SUBSTITUTED_FACE` in `oracle.rs`. **A witness for the honest refusal at the end of this route, from the two-hundred-and-forty-eighth session.** `issue14821.pdf` shows four CIDs — 21, 22, 26 and 91 — through an `Identity-H` `/ABCDEE+Arial` whose descendant states no `/CIDToGIDMap`, so this clause's default identity applies and the CID *is* the glyph index. Its embedded 4237-glyph subset has a well-formed ascending `loca` in which all four of those glyphs are **empty by the table's own statement** — start equal to the next start — and the descendant's `/W` array does not list one of the four either. The document asks for glyphs its own font does not contain and does not claim to; the mapping resolved correctly and there is nothing to draw. **And since the four-hundred-and-thirty-fourth session the instrument says so too**: `Interpretation::codes_without_a_glyph` counted those four with the codes that reach nothing at all, and they now fall under `codes_reaching_a_blank_glyph` — a glyph the program contains and describes with no contours is the program's own statement that the code makes no mark, which is exactly what this row's sentence above says and is what the count had been contradicting. Over 65 944 web documents that distinction is 28 837 codes of the 51 272 ADR 0269 reported as lost. ADR 0270. `poppler` draws something there, which is a fallback to a face this machine has rather than a reading of any clause."

[[clause]]
clause = "9.7.4.3"
title = "Glyph metrics in CIDFonts"
status = "implemented"
code = ["crates/pdf-font/src/metrics.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_vertical_cmap_takes_the_second_set_of_metrics"]
note = "Both arrays and both defaults. `/W` and `/DW` in the twentieth session, `/W2` and `/DW2` in the thirty-sixth — the same two formats one field wider, with Table 115's default `/DW2` of `[880 -1000]` and the clause's rule that `v`'s horizontal component is half the glyph's width. \"Specifying a given CID value more than once should not be done. In the case where it is done, the first specification is the one that shall be used\" holds for both, by `or_insert`."

[[clause]]
clause = "9.7.5"
title = "CMaps"
status = "partial"
code = ["crates/pdf-font/src/cmap.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_one_byte_codespace_range_makes_one_byte_one_code"]
note = "Aggregate of the four below. An embedded CMap file is read in full, the two Identity CMaps are built, and every other predefined name resolves against the 239 files `build.rs` deflates into the binary. **This row ended \"the other predefined names are refused because their data is not in the tree\" until the four-hundred-and-twenty-ninth session**, which is the sentence §9.7.5.2's own row retired in the hundred-and-fifty-sixth (ADR 0140) and §9.7.6's in the two-hundred-and-ninety-eighth — the third row in this family to carry it and the one no earlier sweep reached, because the arithmetic that finds a `partial` parent above settled children never looks here: §9.7.5.4 is `partial` for a reason of its own. `partial` for that reason and no other — §9.7.5.4's c), where a document writes `beginbfchar` in a CMap used as an `/Encoding` and two subclauses of the standard disagree about it."

[[clause]]
clause = "9.7.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/cmap.rs", "crates/pdf-font/src/composite.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_vertical_cmap_takes_the_second_set_of_metrics", "crates/pdf-model/tests/variable_text.rs::a_vertical_composite_da_font_is_refused_and_says_which"]
note = "Both of the two forms the clause names are recognised — a predefined name and a stream — and a CMap yields \"CIDs in a CIDFont\", which is what `CMap::cid` returns. The writing mode is read from the file's own /WMode and from Table 118's, and the clause makes it decide \"which metrics shall be used\" — §9.7.4.3's /W2 and /DW2, which `Vertical::read` has read since the thirty-sixth session, so mode 1 is **drawn** and the row's own named test is what draws it. **This note said /W2 was unread and mode 1 refused for eighty-five sessions after it stopped being**, and so did `composite_cmap`'s own doc comment; both were corrected in the hundred-and-twenty-second, along with two claims in the handover that had inherited it — a retired claim in four places, which is the shape ADR 0101 named. Both of Table 119's forms are now resolved in full: a predefined name reaches `pdf_font::predefined`, which has carried §9.7.5.2's registered data since the hundred-and-fifty-sixth session, and the 13 corpus fonts that named one — of which exactly **one** names a vertical CMap — are drawn rather than refused. **`partial` until the four-hundred-and-twenty-ninth session, above a note that named nothing owed**, which is a row breaking the ledger's own definition of the status — `partial` says \"the note says which are not\". Every requirement this subclause states is executed and each is named above: the selectors are CIDs (`CMap::cid`), the font number is 0 by §9.7.5.4's b), the writing mode is specified and decides the metrics, and both of the two forms resolve. The status was left behind when the predefined data landed in the hundred-and-fifty-sixth, the same expiry §9.7.5's and §9.7.6's rows had — `doc/todo/01`'s sixth failure shape, the note corrected and the status not. **And the mode became a refusal one clause over in the five-hundred-and-second** (ADR 0337): §12.7.4.3's variable text places glyphs along the horizontal axis and measures them with §9.4.4's `w0`, so a `/DA` naming a font whose `CMap` sets mode 1 is refused by name and the document's own appearance stream is left standing. The sentence quoted above is what makes that a refusal rather than an approximation — it is the mode that decides which metrics are used — and 0 corpus objects name such a font in a `/DA`."

[[clause]]
clause = "9.7.5.2"
title = "Predefined CMaps"
status = "implemented"
code = ["crates/pdf-font/src/cmap.rs", "crates/pdf-font/src/predefined.rs"]
test = ["crates/pdf-font/src/predefined.rs::the_collections_the_clause_requires_are_carried", "crates/pdf-font/src/predefined.rs::every_carried_cmap_states_a_codespace_and_a_mapping", "crates/pdf-font/src/composite.rs::an_escaped_cmap_name_is_the_name_it_spells"]
note = "Table 116's two Identity CMaps are built here — as one two-byte range rather than 65 536 entries, which is what the table's own sentence describes — and **every other name is data, which this binary has carried since the hundred-and-fifty-sixth session**. `data/cmaps/` holds all 239 files Adobe publishes for the six character collections, `build.rs` deflates each on its own and `pdf_font::predefined` inflates one when a document names it, so the clause's \"A PDF processor shall support Adobe-CNS1-7, Adobe-GB1-5, Adobe-Japan1-7 and Adobe-KR-9 character collections\" is met rather than refused. The `usecmap` chains inside the files are followed by name, which is how a vertical CMap inherits its horizontal twin's mappings. This row was `partial` for a hundred and fifty sessions on the grounds that vendoring was \"a licensing decision rather than a coding one\" — it was, the decision was taken in the hundred-and-thirtieth session and recorded in the handover, and what was left was the work. Thirteen corpus fonts stopped being refused; fifteen documents came off the incomplete list, and the two that draw a non-embedded Japanese face now differ from the references about *which* face rather than about what to draw. **This clause also carries a `shall not` about the file, which nothing in this tree quoted until the five-hundred-and-ninety-eighth session**: \"The Identity-H and Identity-V CMaps shall not be used with a non-embedded font. Only standardized character sets may be used.\" It is what settles the head of `doc/todo/00` step 7's ink sweep — eleven ambiguous pages this tree draws *nothing* on, three of them the sweep's top three, every one an `/Identity-H` Type 0 over a `CIDFontType2` with no font program, no `/ToUnicode` and `/Ordering (Identity)`. The refusal in `loading.rs` is therefore the clause rather than a gap: with no program there is nothing a CID indexes (§9.7.4.2's \"CIDs shall not participate in glyph selection\") and `Identity-H` names no characters to translate. The four references demonstrate the prohibition's reason instead of contradicting it — on `issue15977_reduced.pdf` one reads the codes as Unicode (\"5(&20('$&,Ï1\"), two as the standard Macintosh glyph ordering (\"RECOMEDACIÓN\") and the fourth as something else again — and on all eleven the closest *voting* pair is 4.63 to 16.51 of 255 apart, so `pdfref::decide` returns `Ambiguous` before our raster is consulted at all. ADR 0433."

[[clause]]
clause = "9.7.5.3"
title = "Embedded CMap files"
status = "implemented"
code = ["crates/pdf-font/src/cmap.rs", "crates/pdf-font/src/composite.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_one_byte_codespace_range_makes_one_byte_one_code"]
note = "Table 118 in full, in the twentieth session (ADR 0029): the stream's data is parsed by `CMap::parse`, `/WMode` is read and checked against the file's, and `/UseCMap` is followed for a stream and refused for a predefined name whose data the tree lacks. A stream that states no codespace ranges, or no character mappings, is refused and named rather than drawn as a page of .notdef. **§9.7.5.4 a)'s companion rule was read as a licence to refuse and is read as a statement of agreement since the four-hundred-and-sixty-third session**: the clause requires an in-file `usecmap` reference to \"also be identified by the UseCMap entry in the CMap stream dictionary\", so on a conforming file the two say the same thing and the file's own name is what a file that omitted the entry has left. It is resolved where this binary carries the `CMap` it names, and the refusal is kept for the case its stated reason actually describes — a name nobody publishes, where what the map inherits genuinely cannot be found. No corpus `/Encoding` `CMap` references another; the witness is on §9.10.3's row, where the same rule was missing outright."

[[clause]]
clause = "9.7.5.4"
title = "CMap example and operator summary"
status = "partial"
code = ["crates/pdf-font/src/cmap.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_bfchar_in_an_encoding_cmap_selects_a_cid"]
note = "Four of the clause's five constraints are honoured. a) a usecmap reference is followed — through the `/UseCMap` entry the clause requires beside it, and through the name in the file itself where the dictionary is silent, since the clause makes the two agree; a name this binary does not carry is still refused, for the `/Encoding` form, and answers nothing for the `/ToUnicode` one. b) a font number other than 0 cannot arise, since only the first descendant is read. d) notdef mappings are implemented, and a notdefrange gives its whole range one CID rather than consecutive ones — which ISO 32000-2 leaves to Adobe Technical Note #5014 and §9.7.6.3's purpose for the mapping implies. e) `beginrearrangedfont` and `beginusematrix` are not implemented and no corpus CMap writes one; an unrecognised keyword ends the current section, so their operands are discarded rather than misread. The row stays `partial` for c): \"beginbfchar and endbfchar shall not appear in a CMap that is used as the Encoding entry of a Type 0 font\", and `bug920426.pdf` writes an Encoding CMap whose only mappings are bfchar lines. §9.7.6.2's own account of the decoding algorithm names bfchar among the character mappings, so the two subclauses disagree and the one describing what a processor does is followed: the destination is read as a character selector, and the page draws its sentence instead of nothing."

[[clause]]
clause = "9.7.6"
title = "Type 0 font dictionaries"
status = "implemented"
code = ["crates/pdf-font/src/composite.rs", "crates/pdf-font/src/cmap.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_one_byte_codespace_range_makes_one_byte_one_code"]
note = "Aggregate of the three below, all three `implemented`. Table 119's entries are read; the decoding algorithm and its undefined-character rules are implemented; and **the predefined `CMap` data this row was waiting on shipped in the hundred-and-fifty-sixth session** — all 239 of Adobe's, deflated into the binary by `build.rs` and inflated only when a document names one (ADR 0140). §9.7.5.2's own row has read `implemented` ever since and this one went on naming it as the gap for a hundred and forty-two sessions; `doc/todo/01`'s fifth shape, found by asking which parents are `partial` while every child is settled."

[[clause]]
clause = "9.7.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/composite.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_predefined_cmap_is_resolved_by_name"]
note = "Table 119. `/Encoding` in both forms, `/DescendantFonts`' single element, and `/ToUnicode`. `/BaseFont` is deliberately unread for a Type 0 font: the clause's own NOTE calls it \"an arbitrary name, since there is no font program associated directly with a Type 0 font dictionary\", so the descendant's descriptor is what substitution is derived from."

[[clause]]
clause = "9.7.6.2"
title = "CMap mapping"
status = "implemented"
code = ["crates/pdf-font/src/cmap.rs"]
test = ["crates/pdf-model/tests/composite_fonts.rs::a_utf8_shaped_codespace_yields_codes_of_several_lengths", "crates/pdf-font/src/cmap.rs::a_codespace_range_is_matched_byte_by_byte", "crates/pdf-font/src/cmap.rs::a_code_the_codespace_excludes_is_never_offered", "crates/pdf-font/src/loading.rs::a_code_for_a_character_means_that_character"]
note = "The whole algorithm, in the twentieth session (ADR 0029). Codespace ranges decide how many bytes the next code takes, shortest first; a code is matched against a range *byte by byte* rather than on its value, which is what makes a UTF-8-shaped CMap work and which no corpus document can distinguish — the numeric reading changes no page of the 1794 compared, so the synthetic test named above is the only thing holding the clause to its words. A code is then looked up \"in the character code mappings for codes of that length\", so `Code` carries its length and every table is indexed by it; failing that, in the notdef mappings. The font number is 0 by construction. **And since the five-hundred-and-second session the algorithm runs backwards as well** (ADR 0337), because §12.7.4.3 has a processor *write* a string in a font's own codes: a character has to become a code, and a code is a length as well as a value, which nothing outside the `CMap` can supply. `CMap::each_addressable_code` walks the codes the character mappings state and offers only those this same `CMap` would extract from their own bytes — a file whose codespace is one byte wide and whose `cidrange` states two-byte codes maps codes no string can ever contain, and writing one would produce two one-byte codes selecting two other glyphs. Bounded, and all or nothing: a partial inverse would answer that the font has no code for a character it has a code for, which is trap 5 wearing a table. The bound is measured rather than picked — `every_registered_cmap_is_inside_the_addressable_bound` walks every registered `CMap` this binary carries and Table 116's two Identity maps, whose 65 536 codes are the largest of them all."

[[clause]]
clause = "9.7.6.3"
title = "Handling undefined characters"
status = "implemented"
code = ["crates/pdf-font/src/cmap.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-font/src/cmap.rs::an_unmatched_first_byte_consumes_the_shortest_code", "crates/pdf-font/src/cmap.rs::a_partial_match_consumes_that_ranges_length"]
note = "All four of the clause's rules. A code with no character mapping falls to the notdef mappings and then to CID 0; a CID whose glyph the CIDFont lacks does the same, which is also the clause's sentence about a /CIDToGIDMap stream too short for the CID. An *invalid* code — one matching no codespace range — skips the character mappings entirely, because the clause sends it straight to a substitute, and the modified algorithm decides how many bytes it consumes: the longest partial match, ties to the shortest codes, and the shortest codes outright where even the first byte matches nothing. That last rule is where two references depart from us: `issue11768_reduced.pdf` writes a one-byte `begincidchar <e0>` under a UTF-8 codespace that admits no one-byte code above 7F, so each `e0` is invalid and consumes the three bytes its partial match implies. mupdf and ghostscript take the mapping's own length and draw three hyphens; poppler and this tree follow the clause. The standard would not have written this recovery algorithm if a mapping's length were meant to override the codespace."

[[clause]]
clause = "9.8"
title = "Font descriptors"
status = "partial"
code = ["crates/pdf-font/src/metrics.rs", "crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-font/src/metrics.rs::an_absent_missing_width_is_zero", "crates/pdf-font/src/name_keyed.rs::the_symbolic_flag_changes_nothing_for_an_embedded_program"]
note = "The descriptor is read for the three things that decide a rendered page — which program is embedded, whether the font is symbolic, and what an uncovered code's width is — and for the four attributes that choose a substitute face. The dimensional metrics are read by nobody, because this tree selects an installed face rather than synthesising one from them — **and the four-hundred-and-thirty-first session put a number on what that costs**, which is §9.8.1's row and ADR 0267: the substitute's cap height is 0.687500 em where the named face's is 0.729167, worth 1.0% to 7.7% of the ink on six oracle pages. The clause's own restriction, that a descriptor shall not be used with a Type 0 font, is satisfied by construction: `load_composite` reads the *descendant's* descriptor and never the outer dictionary's."

[[clause]]
clause = "9.8.1"
title = "General"
status = "partial"
code = ["crates/pdf-font/src/metrics.rs", "crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-font/src/metrics.rs::an_absent_missing_width_is_zero", "crates/pdf-font/src/metrics.rs::a_stated_missing_width_is_the_width", "crates/pdf-model/tests/ocr_text_layer.rs::the_selection_band_is_the_descriptors_not_the_outlines", "crates/pdf-model/tests/substituted_shapes.rs::a_substituted_glyph_fits_the_width_the_file_states_for_it"]
note = "Of Table 120: `/Flags`, `/MissingWidth`, `/FontFile`, `/FontFile2`, `/FontFile3`, `/FontWeight`, `/ItalicAngle`, `/Ascent` and `/Descent` are read. `/MissingWidth` had the wrong default — Table 120 says 0 and this tree assumed half an em, which opened six half-ems of invented space on `issue7439.pdf`, a page the reference consensus contradicted and now agrees with. **`/Ascent` and `/Descent` were on the unread list until the three-hundred-and-seventy-eighth session and had been read since the hundred-and-eighteenth**, which is this row having described the code rather than checked it: `pdf_font::vertical_extent` builds a line box from them for the one question the standard does not ask — how tall is a selection highlight over this text — and since ADR 0216 believes the pair only where it could be a measurement of a face, on the band `measured_extent` derives from Table 120's own definitions, §9.2.4's em and §14.8.5.4.4's line height. 40 of the corpus's 1629 font dictionaries state a pair no face could have, and 42 more state a `/Descent` without the negative sign Table 120 requires, which is read as the depth it states. **And since the five-hundred-and-fifteenth session the band has the witness that separates it from an outline-derived one** (ADR 0350): `test-scenes::scanned_ocr_pdf` is the shape OmniPage CSDK 22 writes — an invisible layer over a scan whose embedded `CIDFontType2` carries `head` and `hmtx` and not one glyph outline, every `loca` entry empty — on which a reader that measures the ink instead collapses every character box to zero height and paints no selection at all (old `PDFium`'s `FPDFText_GetCharBox` did). Read from the descriptor, this band is Table 120's 11.1 pt at 12 pt whatever the program contains, and four tests now fail by that much if it ever moves onto the outlines. **The band has a second reader since the four-hundred-and-third session** (ADR 0240): `variable_text::Metrics::read` builds a form field's baseline from the same two entries, and until then asked only `ascent > 0 && descent < 0` — so one descriptor could be a measurement to the highlight and not to the field, or the reverse. ADR 0216 named that divergence and left it because this one *draws*; `examples/variable_text_census` is what settled the cost, and it is nil on this corpus: of 695 objects §12.7.4.3 lays text out for — 622 widgets and 73 free text annotations — 254 state a pair, every one of which both rules read to the same two numbers. **And that second reader looks in the right place for a composite font since the five-hundred-and-second** (ADR 0337): Table 119 gives a Type 0 dictionary no `/FontDescriptor`, so `Metrics::read` follows `/DescendantFonts` to Table 115's rather than finding nothing and falling back to the em split with the document's own pair one indirection away. Unread, and each because this tree picks an installed face rather than building one: `/FontFamily`, `/FontStretch`, `/FontBBox`, `/Leading`, `/CapHeight`, `/XHeight`, `/StemV`, `/StemH`, `/AvgWidth`, `/MaxWidth`. **`/CapHeight` has a measured cost since the four-hundred-and-thirty-first session and the other nine do not**, which is the difference between a row that says a thing is unread and a row that says what leaving it unread does. The clause's own sentence is what makes the entry a candidate at all -- \"These font metrics provide information that enables a PDF processor to synthesise a substitute font or select a similar font when the font program is unavailable\" -- and it states no `shall`. Drawn straight from the two files, Liberation Sans's capital is 0.687500 em and the `NimbusSans` the reference renderers resolve is 0.729167 em, and the corpus rasters reproduce both exactly: `issue6108.pdf` at 12 pt draws 66 device rows against 70, `issue7580.pdf` at 18 pt draws 99 against 105. That is 1.0% to 7.7% of the page's ink over the six `CONTRADICTED_SUBSTITUTED_FONT` pages that name a Helvetica or Arial face, and nothing at all over the five that name a Times one. ADR 0267 declines to read the entry, because no corpus page yet states a usable `/CapHeight` for a non-embedded face -- `issue7580.pdf`'s descriptor states 0 for all three of `/CapHeight`, `/Ascent` and `/Descent` -- so acting on it would be scaling to another renderer's number rather than to a document's. `/CharSet` is deprecated in PDF 2.0 and states nothing a charset does not. **The claim that no corpus page states these entries for a non-embedded face is now false in both halves, found in the five-hundred-and-eighteenth session on `bug1671312_ArialNarrow.pdf`** -- 1913 bytes, one line at 20 pt in a non-embedded `/ArialNarrow`, stating `/StemV 66`, `/StemH 66`, `/AvgWidth 362`, `/MaxWidth 833`, `/FontBBox [-250 -210 1000 1054]` and `/CapHeight 922`. The `/CapHeight` is stated and still not *usable*: 922 is also its `/Ascent` and Arial's cap height is 716, so a processor scaling to it would draw capitals a fifth too tall -- which sharpens ADR 0267's condition rather than retiring it. The width metrics are another matter and have a measured cost: the ink's bounding box is x[10, 149] y[15, 34] in ours against x[10, 147] y[15, 34] in `poppler`'s and `mupdf`'s, so the advances and the extent are honoured, and inside that same box we mark 983 pixels against 844, 825, 812 and 702 -- page ink 18.45 of 255 against four renderers between 12.71 and 15.52. At 576 dpi the modal dark run across the x-height band is 14 device pixels in ours and 12 in `poppler`'s where the stated `/StemV 66` is 10.56, and `hayro`, which shares `skrifa` with this tree and nothing else, is with the other three. Four renderers find a condensed face and this one does not, so a substituted glyph can be *wider* than the `/Widths` the document states and then the letters collide. Not taken: §9.8.1 states no `shall`, the change is a substitution policy rather than a line, and one witness is not a population. `doc/todo/21` item 4 carries it. **Taken in the five-hundred-and-twenty-third session, and by the width half only** (ADR 0358). `/Widths` is what the scale is derived from -- Table 109's, not Table 120's -- and `/StemV` is what checked it: the drawn stem goes 14 device pixels to 12 against the file's own 10.56, from a number nothing in the derivation touched. So `/StemV`, `/AvgWidth`, `/MaxWidth` and `/FontBBox` are still unread *as inputs* and are no longer unread as evidence, and `/CapHeight` stays exactly where ADR 0267 left it. 30 of the corpus's 257 substituted first-page fonts are drawn narrower than the face standing in for them, 15 by more than half a per cent; `pdf-model --example substitute_stretch_census` counts it."

[[clause]]
clause = "9.8.2"
title = "Font descriptor flags"
status = "partial"
code = ["crates/pdf-font/src/truetype.rs", "crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-font/src/name_keyed.rs::the_symbolic_flag_changes_nothing_for_an_embedded_program"]
note = "Table 121's Symbolic bit is the one that decides a rendered page, through §9.6.5.4, and this subclause is what settles a font that sets it *and* Nonsymbolic — which Table 121 forbids in the same sentence and `issue20232.pdf` does anyway: \"A PDF processor should always check the Symbolic flag to determine whether the state is Symbolic or NonSymbolic.\" `is_symbolic` reads bit 3 alone, which is that sentence. FixedPitch, Serif, Italic, ForceBold and — since the hundred-and-ninety-seventh session — **Nonsymbolic** reach `substitute.rs`; Script, AllCap and SmallCap are read by nobody and would only refine a substitute's choice. Nonsymbolic is here because this subclause says outright what it is for: \"This influences the font's default base encoding and may affect a PDF processor's font substitution strategies.\" A `/BaseFont` whose *name* merely contains the word — `/SegoeUISymbol` — was selecting the standard-14 `Symbol`, whose glyphs carry no Latin name, over a descriptor stating bit 6 and an `/Encoding` of `WinAnsiEncoding`; §9.6.5.4 states those two conditions disjunctively and makes the Latin code-to-name table a `shall`. ADR 0158. ForceBold's own rule is a *may* about thickening at small sizes, which this device does not do. **Re-read off the blame list in the five-hundred-and-seventeenth**, and the unread three were checked rather than believed: no source in `crates/`, `tools/` or `fuzz/` names `AllCap` or `SmallCap` at all, and every `Script` in the tree is part of the word *PostScript*."

[[clause]]
clause = "9.8.3"
title = "Font descriptors for CIDFonts"
status = "partial"
code = ["crates/pdf-font/src/metrics.rs"]
test = ["crates/pdf-font/src/metrics.rs::an_absent_missing_width_is_zero"]
note = "A CIDFont's descriptor is read exactly as a simple font's is, for its embedded program and its flags — Table 120 governs both, which is what this subclause says before adding to it. **This sentence went on to say Table 122's four additional entries were \"none of which is read\" until the four-hundred-and-forty-second session**, and two of the four had been read since the session that wrote §9.8.3.2's and §9.8.3.3's rows: `/Style`'s PANOSE number chooses a substitute (§9.8.3.2, `implemented`) and `/FD`'s glyph classes are read and applied to nothing (§9.8.3.3). The session that implements a mechanism does not maintain the rows of the clauses above it, which is `doc/todo/01`'s fifth failure shape and is why the same sentence was in §9.8.3.1 one row below. `partial` for `/Lang` and `/CIDSet`, which §9.8.3.1 names."

[[clause]]
clause = "9.8.3.1"
title = "General"
status = "partial"
code = ["crates/pdf-font/src/metrics.rs"]
test = ["crates/pdf-font/src/metrics.rs::an_absent_missing_width_is_zero"]
note = "Table 120 applies to a CIDFont's descriptor and is read as far as §9.8.1 records. **This row said \"[n]one of Table 122's four entries is\" read until the four-hundred-and-forty-second session, and two are** — `/Style`'s `/Panose`, which `substitute.rs` reads and which changes the face chosen for two corpus documents (§9.8.3.2, `implemented`), and `/FD`, whose glyph classes `glyph_classes` returns and which nothing consumes because knowing a class's CIDs means holding the character collection (§9.8.3.3). Both landed in the session that wrote those two rows and neither of the two rows above them was touched. What is genuinely unread is the other two: `/Lang`, a language tag that is evidence about which installed face should substitute and is §14.9.2.2's row, and `/CIDSet`, deprecated in PDF 2.0, which states which CIDs a subset holds — which the embedded program already states, by having them. For an embedded CIDFont, which is what every corpus CIDFont that draws is, neither can change a glyph."

[[clause]]
clause = "9.8.3.2"
title = "Style"
status = "implemented"
code = ["crates/pdf-font/src/panose.rs", "crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-font/src/panose.rs::the_clauses_own_example_classifies_a_light_serif_face", "crates/pdf-font/src/panose.rs::the_digits_boundaries_are_panoses_own", "crates/pdf-model/tests/panose.rs::a_panose_number_changes_the_substitute_for_some_fonts_and_not_others"]
note = "The `/Style` dictionary, of which \"[o]nly the Panose entry is defined\", read as the twelve bytes §9.8.3.2 states — two of OS/2 family class, ten of PANOSE — and applied to substitution. The *meaning* of those ten digits is not in this standard: §9.8.3.2 hands it to Hewlett-Packard's PANOSE Classification Metrics Guide and to Microsoft's TrueType specification, both clause 2 normative references, which is why `panose.rs` cites them by name and reads only the four digits this program can act on — family type, serif style, weight, proportion. Where the classification and Table 121's `/Flags` disagree, **the classification wins**, and the corpus shows why: `noembed-eucjp.pdf` and `noembed-sjis.pdf` name MS-Gothic, a Japanese sans-serif face, with flags claiming serif *and* fixed pitch *and* both Symbolic and Nonsymbolic, while the PANOSE number says Latin Text, Normal Sans. Those two documents are the only ones of the 974 whose substitute the entry changes, measured by deriving each request twice — once with the entry and once with it removed. Serifs decide before proportion, which is a **documented choice**: advances come from `/Widths` or `/W` whenever the file states them, so the shape is what a substitute has to get right. 46 fonts reach a descriptor with a `/Style`; 44 of the values are twelve bytes and two are a producer's own length."

[[clause]]
clause = "9.8.3.3"
title = "FD"
status = "partial"
code = ["crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-model/tests/panose.rs::the_corpuss_one_glyph_class_dictionary_states_what_the_clause_forbids"]
note = "`/FD` is read as the classes it names and their overriding descriptors, and is applied to nothing — the boundary is stated in the clause rather than chosen here. \"[T]he names of the glyph classes depend on the character collection, as identified by the Registry , Ordering , and Supplement entries in the CIDSystemInfo dictionary\", so knowing which *CIDs* a class holds means having the collection itself, which is registered data published outside this standard: the same boundary Table 116's predefined `CMap`s sit behind, and the same licensing decision. What the entry is *for* is building a substitute — \"a more accurate substitution font can be created\" — and this tree selects an installed face instead (ADR 0007). **The clause also contradicts itself, and the corpus's one witness proves it**: such a descriptor \"shall contain entries for metric information only; it shall not include FontFile , FontFile2 , FontFile3 , or any of the entries listed in [Table 120]\", and every metric a descriptor can state is in Table 120. `issue13147.pdf`'s `/FD << /Proportional … >>` — the class the clause itself recommends — holds ten Table 120 entries, because there is nowhere else to put metrics. Nothing here enforces the restriction, and the test records why. Read and kept in the five-hundred-and-first session: `substitute.rs` quotes `\"FD\"` and the licensing boundary it defers to is Table 116's, unchanged — the second sweep run on one row. **The contradiction is a defect the errata repair, and the corpus document this row calls a witness is conforming under the amended text** (Issue #5, `/State` `Review` `Completed`): three carets rewrite the one sentence. Before \"contain entries for metric information only\" the erratum inserts \"be a subset of the keys defined in\" Table 120 \"that\"; it softens \"shall contain\" to \"contains\"; and it repoints the prohibition, striking Table 120's title and setting \"Table 122 - Additional font descriptor entries for CIDFonts\" in its place. So Table 120 becomes what an `/FD` descriptor's keys are drawn *from* rather than what it may not name, and what it may not name is Table 122's CIDFont-specific entries — `/Style`, `/Lang`, `/FD` itself and `/CIDSet`. Read that way the paragraph is consistent and states something a validator could check: no recursion, no CIDFont attributes, metrics only. `issue13147.pdf`'s ten Table 120 entries are exactly what the amended sentence asks for, and the test that records the contradiction is now recording a defect in the 2020 printing rather than one in the standard. Nothing this tree does moves: `/FD` is still applied to nothing, for the licensing reason above, and enforcement is still a validator's job rather than a reader's."

[[clause]]
clause = "9.9"
title = "Embedded font programs"
status = "implemented"
code = ["crates/pdf-font/src/program.rs", "crates/pdf-font/src/type1.rs", "crates/pdf-font/src/cff.rs"]
test = ["crates/pdf-model/tests/type1.rs::a_symbolic_type1_program_draws_what_no_substitute_could", "crates/pdf-font/src/loading.rs::a_simple_fonts_subtype_selects_nothing", "crates/pdf-model/tests/silent_fonts.rs::a_font_that_draws_none_of_its_codes_is_reported", "crates/pdf-font/src/program.rs::a_font_file_whose_bytes_are_a_cff_is_read_as_one", "crates/pdf-font/src/program.rs::a_font_file_that_really_is_type_1_is_untouched"]
note = "All three of Table 124's keys are read and every format it lists is drawn: `/FontFile` (bare Type 1, from the thirtieth session), `/FontFile2` (TrueType), and `/FontFile3` in all three of its subtypes. Which reader applies is decided by the program's own leading bytes rather than by the key or by Table 125's `/Subtype`, because a signature cannot be mislabelled by a producer and the clause's pairings can. **That sentence was true of two of the three keys and this row asserted it of all three, for as long as it has been written down; the five-hundred-and-fourteenth session made it true.** `/FontFile3` asked the signature and `/FontFile` did not — it took Table 124's pairing from the key's spelling and handed every stream to the Type 1 reader — which is the shape `CLAUDE.md` names as this project's fourth-most-repeated failure, a row describing what the code should do. The witness came off the oracle rather than off a re-reading: `issue5751.pdf` is a `CIDFontType0` whose descriptor writes `/FontFile`, whose stream states none of Table 125's `/Length1`, `/Length2` or `/Length3` — all three \"Required for Type 1 font programs\" — and whose first bytes are `01 00 04 03` followed by the Name INDEX string `MyriadArabic-Regular`. Three of the file's own statements disagree with the key it wrote and the bytes are the only one a producer cannot fake, so the bytes decide, exactly as they do for `/FontFile3`. Read from the key it was `InvalidFontFormat`, the page reported an unparsable font and drew nothing where four reference renderers draw *Open Access*; it now agrees with all four, and it is the only page of 1794 whose verdict moved. Neither of Type 1's own packagings can collide with the test — a PFA begins `%!`, a PFB `80 01` — so the change reroutes a program that could not be read at all and leaves every readable one where it was. **What Table 124 requires is of the file rather than of the processor, and the three-hundred-and-seventy-ninth session found the corpus witness for the difference.** Each of its rows ends \"[t]he font program provided as the value of this key shall conform to\" the format's own specification, and the standard states no consequence for one that does not: what a reader does with a malformed embedded program is nowhere in clause 9. `bug1308536.pdf` embeds a `/Subtype /Type1C` whose 77-byte Private DICT opens with six real operands the format cannot mean — `-4E-0349998`, three empty ones, an operator no Private DICT defines — and whose last 26 bytes then parse cleanly into `BlueValues`, `StdHW`, `StdVW`, `StemSnapH`, `StemSnapV`, `defaultWidthX` and `nominalWidthX`. This tree reads it and draws the producer's own outlines, and the reason is checkable rather than lucky: the broken operands are hinting parameters, the outlines are in the CharStrings INDEX, and §9.2.4's advances come from the font dictionary's `/Widths`, so nothing that decides a mark is in the broken bytes. `ghostscript` refuses the program outright — it prints *An embedded font is invalid* and loads `NimbusSans-Regular` instead — which §9.8.1 permits as a capability rather than requires, and the two readings differ by a whole page drawn in another face. Ours, `poppler`, `mupdf` and `hayro` end within 0.015 of 255 of each other at eight times the page's scale; `AMBIGUOUS_REFUSED_EMBEDDED_FONT` in `oracle.rs` carries the ladders. **A program whose *stream* decoded only part-way is refused outright, which is the other side of the same silence and is the five-hundred-and-eighth's** (ADR 0343). The clause's \"shall conform to\" is about the bytes the key provides, and a prefix does not provide them: Table 124's formats are all table directories or checksummed blobs whose offsets point forward, so a prefix is a directory describing bytes that are not there rather than a smaller font. The witness is `issue13316_reduced.pdf`, whose corrupt `/FontFile2` yields 863 bytes that parse and draw **A C E F** where the file's six CJK glyphs belong — trap 1 exactly, a count that rose while the picture got worse. The refusal is trap 5's substitutive case (ADR 0106): the wrong glyphs stand in place of the right ones rather than beside them, so giving them up costs the page nothing. `sfnt::truncation` is the same rule read off the structure and is kept, for a program whose stream decoded whole and whose directory still overruns it."

[[clause]]
clause = "9.9.1"
title = "General"
status = "partial"
code = ["crates/pdf-font/src/program.rs", "crates/pdf-font/src/type1.rs"]
test = ["crates/pdf-model/tests/type1.rs::an_embedded_type1_program_puts_glyphs_on_the_page"]
note = "Table 124 in full; see §9.9. **A `ttcf` container in a `/FontFile2` is read since the hundred-and-fifty-seventh session** (`pdf_font::collection`, ADR 0141): Table 124 states \"TrueType font program\" and a collection is not one, so the file is malformed and two corpus documents write one anyway — the face is chosen by Table 120's `/FontName`, which §9.6.2.1 makes \"the PostScript name of the font\" and every face in a collection carries. Two of the clause's rules are satisfied by having nothing that could break them: a Type 1 program's `PaintType` \"shall be ignored\" and the text rendering mode decides filling or stroking, which is where §9.3.6 already puts it; and the `vhea` and `vmtx` tables \"shall never be used by a PDF processor\", which nothing here reads. Table 125's `/Length1`, `/Length2` and `/Length3` are read by nobody: `read-fonts` finds the eexec boundary in the bytes, and the clause's one requirement on a processor — that a `/Length3` of 0 means the 512 zeros and `cleartomark` \"shall be added by the PDF processor\" — is about a fixed-content portion that holds no glyph description, so appending it would change no outline. That is the partial: a stated requirement not executed, with the reason it cannot show written down rather than assumed. **Read and kept in the five-hundred-and-forty-fifth session**: the three lengths are still named in two comments of `program.rs` and by no reader at all, which is what the sentence above claims."

[[clause]]
clause = "9.9.2"
title = "Font subsets"
status = "implemented"
code = ["crates/pdf-font/src/substitute.rs"]
test = ["crates/pdf-font/src/substitute.rs::a_subset_prefix_is_removed_only_when_it_is_one"]
note = "The subset tag is what a `/BaseFont` means to a substitution request, and `strip_subset_prefix` applies the clause's rule exactly — \"exactly six uppercase letters\" followed by a plus sign — rather than splitting on the first `+`, so a face genuinely named with one keeps its name. Nothing else in the clause asks anything of a reader: recognising subsets to merge documents is a writer's job, and `.notdef`'s presence is a requirement on the file."

[[clause]]
clause = "9.10"
title = "Extraction of text content"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs", "crates/pdf-font/src/tounicode.rs"]
test = ["crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor", "crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor_across_the_pdfjs_corpus"]
note = "Text extraction, done here as a *readback of what was drawn*: `Interpretation::text` is accumulated by the same loop that places the glyphs, so it cannot describe a code the page did not show, and `text_extraction.rs` holds it to 100% of the words `pdftotext` finds over the fourteen specification PDFs. All three of §9.10.2's methods are implemented since the hundred-and-fifty-sixth session, the third having waited on Table 116's registered `CMap` data — and **this row stayed `partial` for a hundred and forty-two sessions after the sentence saying so was written into it**, which is the two-hundred-and-ninety-eighth session's finding rather than a new capability. **The readback got a consumer that reads a page nobody is looking at in the four-hundred-and-fourteenth**: `viewer_core::Command::Find` is Annex O's document-wide `search`, so §9.10's answer for a page is now what decides whether a *document* holds a word — 2.66 MB of it over ISO 32000-2's 1023 pages, at 5.7 ms a page to produce (ADR 0250). **And that consumer stopped throwing it away in the four-hundred-and-twentieth**, which is what makes the size of a readback a number worth having written down: `viewer_core::readback` keeps it under a 4 MiB per-document budget with least-recently-used eviction, so the whole of ISO 32000-2 is held at once and a repeated document-wide search costs 7.27 ms against 5.45 s. Nothing about this clause's *answer* changed — the gate is still 100% of `pdftotext`'s words over the fourteen specification PDFs and 99.2% over the corpus, both re-run — because the cache is keyed on the page and emptied whenever the view state that produced the readback moves (ADR 0256). **And since the five-hundred-and-eighty-seventh session the readback says what it is *short of*, which is this clause's other half and had no consumer at all.** §9.10.1 divides the question into the information content and the appearance; a page can have the second whole and the first in pieces, and `Interpretation` has counted the pieces since ADR 0311 with nothing outside `pdf-model` able to read them. `Interpretation::shortfall` carries the three per-code counts as one value, and the two consumers this clause is for now take it: `viewer_core::Query::Readback` for a reader at a window — spoken into §14.7's status group, because the person who cannot see the page is the one the shortfall is invisible to — and `pdf-retrieve`'s `readback` object for a program, beside the text and never inside it. The default answer is still `Interpretation::text` byte for byte, which that tool's own test asserts. ADR 0422."

[[clause]]
clause = "9.10.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor"]
note = "The information content of text as against its rendered appearance — two questions, and the clause's insistence that they are two is what `Interpretation` follows in carrying `text` and `glyphs` as separate facts. A page can draw perfectly and extract nothing, which is also why the oracle asks the second when choosing a tolerance and got worse answers for eight sessions when it asked the first. **This row said \"[t]he clause names one route this tree does not take: `/ActualText` on a marked-content sequence\" and it had been false since the fifty-fifth session**, corrected in the five-hundred-and-eighty-seventh: the clause's second bullet is §14.9.4's replacement text, that clause gives it *two* homes — a `Span` property list and a structure element — and this tree reads both, the first since the fifty-fifth session and the second since the fifty-sixth. §14.9.4's own row has said so for five hundred sessions while this one denied it, which is why the two were read together this round rather than one of them. `content/marked.rs::accessibility` asks the property list first and the element behind it, per entry; `content/run.rs`'s `EMC` arm is where the replacement is applied. **What the clause states and this tree does not is nothing**; what §9.10.2 cannot answer is counted rather than unsaid, and that count is now readable from outside `pdf-model` — `Interpretation::shortfall`, `viewer_core::Query::Readback` and `pdf-retrieve`'s `readback` object. ADR 0422."

[[clause]]
clause = "9.10.2"
title = "Mapping character codes to Unicode values"
status = "implemented"
code = ["crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor_across_the_pdfjs_corpus", "crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_pdfboxs_frozen_extraction", "crates/pdf-model/tests/composite_fonts.rs::an_identity_h_fonts_partial_to_unicode_is_completed_by_the_program", "crates/pdf-model/tests/composite_fonts.rs::a_two_byte_code_is_not_read_as_a_character_when_nothing_names_it"]
note = "Three methods \"in the priority given\", and `LoadedFont::text` is that list in that order. **Since the sixty-third session the whole pdf.js corpus is the test rather than the 14 specification PDFs**: 974 documents against `pdftotext`, and the figure this sentence opened with — **96.5%**, with 46 documents named below the floor — was the sixty-third session's and stood here while five later sessions moved it. It is **99.2%** of `pdftotext`'s words and **25** named documents as of the three-hundred-and-twenty-eighth, and the count is the gate's own output rather than this row's arithmetic. Of the 25, most are scripts where all three methods fail and the clause says so in as many words, and 7 are right-to-left text read back in painting order (§14.8.2.5.3's row measures that population and finds no corpus document writing the tag). First `/ToUnicode`. Then, for a simple font, the glyph name the encoding selected, looked up in the Adobe Glyph List (`read_fonts::ps::agl`) — which is why `glyph_names` is kept after loading rather than discarded. **And the second list in that sentence brings an algorithm with it**, which this tree did not run until the three-hundred-and-twenty-seventh session: the Adobe Glyph List *for New Fonts* names a composed glyph `f_f_i` and a variant `oacute.sc`, so `encoding::text_for` drops everything from the first period, splits on underscores and maps each component — with a component the list cannot answer for making the whole name unanswerable, because half a ligature would put `ff` where the page shows `ffi`. `issue15516_reduced.pdf` is a Minion subset whose eight codes are exactly those names and which read back as nothing; it reads *ffi fi ffl ff fl ft Th fh* now, which is `pdftotext`'s own answer. **And a Type 3 font is a simple font, which this tree denied for three hundred sessions**: `type3.rs` said a Type 3 glyph name \"names a procedure, so … the name is no evidence at all about the character\" and answered from `/ToUnicode` alone. §9.6.4's own step b) is \"[g]et the glyph name from the Encoding entry\" and §9.6.5.3 makes `/Differences` \"the complete character encoding for this font\", so the glyph selection algorithm *does* use a name and the second method applies to it. A producer that calls its procedure `/colon` has said which character it draws. Corrected in the three-hundred-and-twenty-sixth session, and the two other things that came with it are on §9.6.4's row: the clause's own last-resort permission for a name the list does not know, and the rule that text shown *inside* a glyph description is the glyph rather than the page's. The readback went **98.2% to 99.1%** of `pdftotext`'s words and the documents below the 0.90 floor 36 to 29. **The third landed in the hundred-and-fifty-sixth session**, when the registered data arrived (§9.7.5.2): steps a) to e) are `LoadedFont::text`'s middle branch — the font's CMap gives a CID, the descendant's `/CIDSystemInfo` gives a registry and an ordering, and `pdf_font::predefined::cid_to_unicode` reads the `registry-ordering-UCS2` file those name. It applies to an embedded composite font as much as to a substituted one, because the collection says what a CID means whether or not the program defining it is present. The corpus readback went 97.9% to 98.2% and the documents below the 0.90 floor 42 to 36. **The priority is load-bearing and one page shows why.** `issue11740_reduced.pdf`'s `/ToUnicode` records the Windows-1251 *bytes* of its Russian text as Latin-1 code points, so method one returns mojibake where method two would have returned the characters — and the clause still puts the producer's own statement first. That is right for extraction and wrong for *drawing*, which is the distinction §9.10.1 draws and the reason a substitute addressed through `/ToUnicode` is now a last resort behind §9.7.4.2's own glyph route (ADR 0049). Where all three fail the clause does not stop there: it says \"there is no way to determine what the character code represents in which case a PDF processor may choose a character code of their choosing\", and the sixty-fourth session took that permission. `text_from_program` names the glyph the way the *program* does — the `post` table's name through the Adobe Glyph List, or the Unicode `cmap` subtable inverted — which is a choice made from the file's own data rather than a guess, and is the same instrument §9.6.5.4's last resort uses for the forward direction. It cannot invent text: a `post` of version 3.0 holds no names, `issue15910.pdf`'s version 2.0 holds 79 empty ones, and a name outside the list answers nothing. **Measured rather than assumed**: over the pdf.js corpus the readback went 96.5% to 97.8% of `pdftotext`'s words and no document moved the other way, which is what says this is not the fallback-that-fills-the-page. **And the same sentence permits a second choice, taken in the three-hundred-and-twenty-eighth session once the program has been asked and has said nothing: the code itself**, where it is a printable ASCII byte. 0x21 to 0x7E is the range in which a byte and a Unicode code point mean the same character under every encoding §9.6.5 states, so a code outside it is declined rather than guessed at, and space is excluded because a whitespace readback is what tells a missing mark from a blank one. `issue2017r.pdf` is the shape it is for — a symbolic `TrueType` with no `/Encoding`, a `post` naming nothing and a (3, 0) `cmap` whose inversion gives codes rather than characters. **It was measured twice and the first measurement was wrong**: taken before §9.6.4's glyph-description rule landed it bought 3 words and cost `pr4922.pdf` its whole readback, because that page draws its Type 3 glyphs by showing characters of another font. With the rule in place it costs nothing and lifts `issue5501.pdf` and `issue8187.pdf` off the floor — 99.1% to 99.2%, 27 documents below it to 25. Three documents left the text gate's list. All three of the clause's methods and its permission are now implemented; what remains below 100% is named in the gate's own output rather than by an unbuilt method. **And the third method is only half of reaching a substitute, which the hundred-and-eighty-second and -third sessions found together.** A character it produces has to be *drawn*, and `substitute::installed` ranks faces by the generic family a descriptor implies, which cannot say \"this one must have Chinese in it\": ten corpus documents got a Latin face with no glyph for anything they show, eight of them blank pages, and nothing reported it. `LoadedFont::uncovered_character` reports it now, per font and only where the face drew none of what it was asked for (ADR 0152); `substitute::installed_covering` then chooses the widest-repertoire face on the machine that can draw a character of the collection's own script, which is a **choice** in the one place §9.10.2 leaves for it — the clause says how to learn what a code means and nothing about which face to draw it with (ADR 0153). Eight of the ten now draw and seven more oracle pages agree. **And the condition the code applied was not the condition ADR 0152 stated, which the hundred-and-ninety-third session found and corrected.** The tally counted a *hit* for every code that reached no outline and was not one of §9.10.2's uncovered characters — a space, in practice — so the rule it actually applied was \"this font misses a character and shows no space\", and the rule it claimed was \"the face drew none of what it was asked for\". Correcting it costs the two documents that were left on this list — `issue11555.pdf` and `issue2128r.pdf` draw most of their text, and both pages **agree with the reference consensus**, so what they carried was an overstatement — and it buys four documents whose text is genuinely absent, which `silent_fonts.rs` holds by name. A font that draws *some* of its characters and misses others is still not reported, which is the trap-11 trade ADR 0152 argued and the gap \"a font is reported as a whole\" names. **And the permission was being declined by the one shape the clause sends to it, which the four-hundred-and-twenty-third session found with a corpus this tree had never opened.** `text_from_program` refused a composite font outright, on the note that \"a composite one selects by CID through a `CMap`, and §9.10.2's third method is the route the clause states for those\" — which reads the third method as applying to every composite font. It does not: the clause admits \"a composite font that uses one of the predefined CMaps listed in \"Table 116 -Predefined CJK CMap names\" (except Identity -H and Identity -V ) or whose descendant CIDFont uses the Adobe-GB1, Adobe-CNS1, Adobe-Japan1, Adobe-Korea1 (deprecated in PDF 2.0 (2020)) or Adobe-KR (added in PDF 2.0 (2020)) character collection\", so an `Identity-H` font over `Adobe-Identity` is excluded **by name** and has only the first method. When its `/ToUnicode` answers for some codes and not others, every method has failed for the rest and the sentence above applies. The route is the same data one step longer — the `CMap` gives a CID, §9.7.4.2's `/CIDToGIDMap` gives the glyph, and the program names it — and §9.7.6.3's notdef fallbacks are deliberately *not* taken, because a code that reached CID 0 drew a substitute and naming it would put a character on a page that shows none. Measured over the pdf.js corpus rather than assumed, which is this row's own standing rule: **23987 → 24003 of `pdftotext`'s 24187 words, 25 named documents below the floor to 23**, and no document moved the other way. `issue16553.pdf` and `javauninstall-7r.pdf` are the two, and both had sat on that list undiagnosed — the first for 357 sessions. The witness that found it is `PDFBOX-5838-0024320-reduced.pdf`, whose `/ToUnicode` maps 8 of the 15 codes its page shows and which read back `H Reeach Pec` for `Honors Research Project`. **And `text_from_the_code` now refuses a code of more than one byte**, which cost nothing while only a simple font could reach it and would have been wrong the moment a composite one could: the argument for reading a code as a character is that §9.6.5's encodings are one byte per code, and a two-byte code is outside it. `PDFBOX-4322-Empty-ToUnicode-reduced.pdf` is where the two implementations part — PDFBox reads its two-byte codes as Unicode and gets `Justin`, right only because that producer numbered its CIDs by code point — and declining is a **choice** under the same permission rather than a gap. ADR 0259. **And the other half of reaching a substitute has a simple-font case, which the four-hundred-and-thirty-fourth session found in a 65 944-document web survey.** `installed_covering` chooses a composite font's face by coverage and a *simple* font's face was whatever `substitute::find` answered — for one of §9.6.2.2's fourteen, one of the ten compiled-in Foxit faces, whose charset is the standard Latin set. A `/Differences` naming Cyrillic or Greek therefore reached no glyph while the Latin codes of the same font drew, so the \"drew none\" report stayed quiet: **22 435 codes over 277 documents**, of which five documents are 4912. `substitute_face` compares two code tables over the declared range and takes the second only where it is a strict superset, which is the same question one clause down (§9.6.5.4's routes) rather than this clause's characters — and it is deliberately not ADR 0153's coverage predicate, because `0546109.pdf`'s Greek `/Differences` also names `controlSTX` and its thirty neighbours and no face has the C0 controls in a `cmap`. ADR 0270. **And the clause's own \"there is no way\" was being carried out and never said out loud, which the four-hundred-and-seventy-sixth session fixed with a count rather than a rule.** `french_diacritics.pdf` had been the sharpest named refusal in the text band since the four-hundred-and-sixty-third: a pdfTeX Type 3 font whose `/Differences` names the Latin-1 accented letters `/a192`, `/a194`, `/a196` … , the code in decimal. Every route this row lists is exhausted on it — no `/ToUnicode` (Table 110 makes it optional), a name the Adobe Glyph List and the Adobe Glyph List for New Fonts do not hold, no composite route because a Type 3 font is simple, and the code itself declined because 0xC0 is outside 0x21–0x7E. **The bound is Annex D's and not a preference**: code 192 is `Agrave` in `WinAnsiEncoding`, `questiondown` in `MacRomanEncoding` and unencoded in `StandardEncoding`, so reading `a192` as `À` is reading pdfTeX's convention, which principle 5 forbids presenting as derived. **And the name is a published Adobe glyph name for something else** — Annex D.6 puts `a192` at code 218 of `ZapfDingbats`, as it does eight more of the file's twenty-nine names — `a49`, `a194`, `a196`, `a199`–`a203` — so the one list that holds it would read `À` as an ornament, and §9.10.2 sends a reader to that list nowhere; the eighth session already paid for the drawing half of the same coincidence. So the refusal is the clause's and what was missing was a channel: `Interpretation::codes_without_a_character` counts the codes a page showed that no method could name, whatever was drawn for them, because a page whose fonts §9.10.2 cannot read and a page with no text on it were otherwise the same interpretation. A **count and not a report**, on ADR 0152's trade — a shortfall in the readback rather than in the picture. `tests/corpus.rs` prints it beside ADR 0270's two lines. **And that shape was derived in the four-hundred-and-eighty-third session, which found one population the clause does reach.** `LoadedFont::naming_gap` answers, for a code no method named, which of this clause's methods was the highest-priority one that font could have answered with — the clause ranks them itself, so a `/ToUnicode` that omits a code has failed at the first method whatever the glyph names say — and `examples/unnamed_code_census` prints the split. Of 1342 codes: **761** are an `Identity` ordering with no `/ToUnicode`, which this clause excludes from the third method by name and §9.7.4.2 explains; **247** a glyph name neither published list holds; **210** a producer's own `/ToUnicode` leaving a code out; **123** a code selecting a glyph by code and naming nothing, which is the control range and code 0; and **1** a registered collection with nothing for the CID. Three of those four large populations are the clause's own \"there is no way\", and the fourth held `ZapfDingbats`: **114 codes over two documents whose characters ISO 32000-2 prints in Table D.6 itself**, which is a route this clause's second method is not and Annex D.1's \"shall be predefined in any PDF processor\" is. Those read back now, and so does a defect no count could see — a dingbat at code 0x21 was reaching `text_from_the_code` and coming back as `!`, because the printable-ASCII permission's own argument (\"the range in which a byte and a Unicode code point mean the same character under every encoding §9.6.5 states\") is false for a symbolic font whose set the annex documents. 1342 codes over 45 documents to **1228 over 43**, with the remainder argued as the clause's answer rather than as a backlog. ADR 0311, ADR 0318. **And the five-hundred-and-eighty-seventh session settled what to do with what is left, having first asked the question the census cannot: is any of it a method this tree does not walk?** The clause states three and ranks them — \"[a] PDF processor can use these methods, in the priority given\" — and `LoadedFont::text` walks all three, each falling through to the next *per code* rather than per font, which is what a `/ToUnicode` covering some codes and not others needs. **The order between the second and the third cannot matter and this is the argument, not an assumption**: the second is conditioned on \"a simple font\" and the third on \"a composite font\", and `load_simple` sets `collection: None` while `load_composite` sets `glyph_names: None` and `symbolic_set: None`, so no font can satisfy both. **The third method's second disjunct is honoured as well as its first**, which is the trap-5 shape a reader should check here: the exception \"(except Identity -H and Identity -V )\" governs the predefined-`CMap` disjunct alone, so an `Identity-H` font *whose descendant states one of the five registered collections* is inside the method — and `collection_meaning` reads the descendant's own `/CIDSystemInfo` without consulting the encoding's name, with all five of the `registry-ordering-UCS2` files the clause names carried in `data/cmaps/`. `spec-errata emit` over clause 9 was run before any of this was written: Errata Collection 3 touches §9.3.4, §9.3.8, §9.4.2, §9.4.4, §9.5, §9.6.2.1, §9.6.2.3, §9.6.4, §9.7.3, §9.7.4.2, §9.7.5.3, §9.7.6.2, §9.8.1, §9.8.2, §9.8.3.3 and §9.10.3, and **not one annotation lands on this subclause** — its methods, its order and its closing sentence are as published. So the band is the clause's own \"there is no way\", every method is implemented, and the decision was never about the reading. **It was about the voice, and the answer is a count that crosses rather than a report**: 41 of the 892 corpus page ones that report nothing show such a code, and reporting them would take 41 pages off the oracle's judged set (ADR 0152) to say something the standard has already answered. `Interpretation::shortfall` is the three per-code counts as one value, `viewer_core::Query::Readback` carries them to a host beside `Query::Reports` — worded apart, because a refusal of ours and an answer of the standard's are not the same sentence — `viewer-accessibility` speaks them into §14.7's status group, and `pdf-retrieve` prints them beside the text it hands a program. ADR 0422."

[[clause]]
clause = "9.10.3"
title = "ToUnicode CMaps"
status = "implemented"
code = ["crates/pdf-font/src/tounicode.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-font/src/tounicode.rs::ranges_map_to_consecutive_characters", "crates/pdf-font/src/tounicode.rs::surrogate_pairs_decode_to_one_character", "crates/pdf-font/src/tounicode.rs::a_huge_range_is_stored_as_a_range_rather_than_expanded", "crates/pdf-font/src/tounicode.rs::a_base_answers_the_codes_the_file_states_nothing_about", "crates/pdf-model/tests/composite_fonts.rs::a_to_unicode_cmap_built_on_a_published_one_reads_back_through_it"]
note = "`/ToUnicode` as a `CMap`: `beginbfchar` and `beginbfrange` in both of the latter's forms — a consecutive span and an explicit array — with values decoded as UTF-16BE including surrogate pairs, and each code's length taken from the codespace ranges, which the clause requires to be \"consistent with the encoding that the font uses\". **This row said \"Table 118's `/UseCMap` is read\" and that was a claim about the wrong reader**, corrected in the four-hundred-and-sixty-third session: `read_cmap` reads it for the `/Encoding` `CMap` and `to_unicode` read the stream and nothing else, so a `/ToUnicode` \"based on another ToUnicode CMap\" — this clause's own first bullet, and the only entry it calls pertinent — inherited nothing and every code the base defined read back as silence. `read_to_unicode` follows it now, a name through `predefined::unicode_cmap` and a stream by recursion, bounded at four deep; `ToUnicode` carries the base and consults it only after its own mappings, which is Table 118's \"shall specify only the character mappings that differ from the referenced CMap\". **And the file's own `usecmap` operator is followed where the dictionary is silent**, because §9.7.5.4 a) requires the two to say the same thing, so reading the one that is present can never contradict a conforming file. `issue5010.pdf` is the witness and the only one in 2182 documents across five corpora: a Korean `Identity-H` font whose `/ToUnicode` states five mappings for codes its page never shows and `/Adobe-Korea1-UCS2 usecmap` for the rest, with no `/UseCMap` entry. Its page read back the empty string and now reads what Adobe's own published file says, character for character. A `bfrange` is kept **as a range** rather than expanded, deliberately: one `<0000> <FFFF>` line would otherwise allocate sixty-five thousand strings from a few bytes, which is a decompression bomb wearing a font's clothes. A malformed `CMap` yields what was read before the damage, so extraction degrades to missing characters rather than to silence. **Two documents in `doc/corpora/pdfbox` state a `/ToUnicode` that is not one**, found in the four-hundred-and-twenty-third session: `PDFBOX-4322-Empty-ToUnicode-reduced.pdf` points the entry at a stream that is a verbatim copy of the `Identity-H` CID `CMap` — `/CMapType 1`, one `begincidrange`, not one `bfchar` or `bfrange` — and `sample_fonts_solidconvertor.pdf` writes the bare name `/Identity-H` for two of its ten fonts. Neither satisfies this clause's third bullet, so neither maps a code to anything, and both are correctly read as answering nothing rather than repaired: a `cidrange` read as a `bfrange` would be a CID served as a UTF-16BE code unit, which nothing here licenses. What happens next is §9.10.2's and is on its row. ADR 0298. **An erratum stands over this subclause and is recorded rather than acted on**, found by `spec-errata emit` in the five-hundred-and-eighty-seventh session: Errata Collection 3 issue #462 strikes out the first bullet — \"The only pertinent entry in the CMap stream dictionary (see \"Table 118 — Additional entries in a CMap stream dictionary\") is UseCMap, which\" — leaves `UseCMap` in its place, and inserts a new *Table 125a, \"Additional entries in a `ToUnicode` CMap stream dictionary\"* after the list. It changes what a conforming producer must write rather than what a reader must do: this reader already follows `/UseCMap` and the file's own `usecmap`, and reads no other entry of the stream dictionary, so a table naming which entries are permitted adds no requirement here. Recorded because the sentence this row quotes is the one struck out — a later round quoting it as current would be quoting text the collection has removed. **Two rounds later that sentence was already being quoted as current in two rustdoc blockquotes**, in `pdf-font/src/loading.rs` and in `composite_fonts.rs`, and neither named the erratum this row had recorded; both are corrected in the five-hundred-and-ninety-first. They were invisible to `spec-errata check` because its comparison kept the em dash `doc/md/` writes as a hyphen, which that round repaired."

[[clause]]
clause = "10.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "Introductory, and its four steps are the map of this clause: convert to the device's native space (§10.3, §10.4), apply transfer functions (§10.5, deprecated and inapplicable here), scan-convert (§10.7), halftone if the device does (§10.6, inapplicable). Three of the four are answered in the rows below; the one this tree does is the first and the third."

[[clause]]
clause = "10.2"
title = "Raster output device native colour"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "This device's native colour space is RGB and it has no spot colourants, which is the fact §8.6.7 and §11.7.4's Table 146 are read against — see ADR 0028, where it makes overprinting Normal rather than a gap. \"Colours specified in any device or CIE-based colour space shall be rendered as process colours\" is `ColourSpace::to_rgb`, the one route from any space to a pixel."

[[clause]]
clause = "10.3"
title = "CIE-Based colour to device colour"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs", "crates/pdf-model/src/icc.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The branch this tree is on. See §10.3.1 and §10.3.2."

[[clause]]
clause = "10.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/icc.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/icc.rs::a_real_cmyk_profile_agrees_with_independent_evaluators"]
note = "\"Conversion from a CIE-based source colour to a CIE-based destination colour shall be performed based on ISO 15076-1:2010 (ICC.1:2010)\", which is the A2B evaluator in `icc.rs`. The clause puts *establishing* the destination \"beyond the scope of this document\" and names output intents (§14.11.5) as one way — implemented — and its NOTE lists \"assumptions made by the PDF processor software\" as another, which is what `CMYK_CORNERS` is and where it is licensed from."

[[clause]]
clause = "10.3.2"
title = "Establishing CIE-based colour space definitions"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The sentence that authorises the `DeviceCMYK` fallback, and the thirty-third session's finding: a processor \"should establish CIE-based colour specifications for device colour spaces … when those device colour spaces do not match that of the raster output device\". A display's native space is not CMYK, so the remapping is asked of us; assuming standard process inks is the CIE-based specification established for it, and every source the document names — `/DefaultCMYK`, an output intent, an `ICCBased` space — outranks it. `DeviceGray` and `DeviceRGB` need no remapping on an RGB device."

[[clause]]
clause = "10.4"
title = "Conversions among device colour spaces"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "See §10.4.2.1: these are the algorithms an ICC-enabled processor is told to prefer §10.3 over, and this tree does *for a colour on its way to a pixel*. **Three of the five are on a route to a pixel all the same, and this note said \"the rest are not used\" until the three-hundred-and-eighty-third session.** §11.5.3 derives a soft mask by converting a composited colour to `DeviceGray`, and §10.4.2 is what \"implementation-defined means\" turns out to be: §10.4.2.2 both ways (a grey to RGB, and the NTSC weights every luminosity mask reads off a rendered pixel), §10.4.2.3 from CMYK, and §10.4.2.4 as the composed identity whose every generated term cancels in §10.4.2.3's weights. What is genuinely unused is §10.4.2.5, which §10.3 outranks, and the grey-to-CMYK direction of §10.4.2.3. The individual rows say which is which; ADRs 0217 and 0220 say how the claim decayed. **Read and kept in the five-hundred-and-forty-fifth**: §10.4.2.5 is still not implemented — `CMYK_CORNERS` is what a `DeviceCMYK` colour reaches a pixel through — and the individual rows still say which of the five is which."

[[clause]]
clause = "10.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "Introductory; it names the three conversions the subclauses below define."

[[clause]]
clause = "10.4.2"
title = "Classic colour conversion methods"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "See §10.4.2.1 for the ranking and the individual rows for what is done. `partial` for what two of the four conversions below owe and this row did not name until the four-hundred-and-thirty-seventh session: §10.4.2.5's `DeviceCMYK` to `DeviceRGB` is unused because §10.3's ICC route outranks it, and §10.4.2.3's grey-to-CMYK direction is unused for the same reason — both are stated in §10.4's parent row and in neither of theirs."

[[clause]]
clause = "10.4.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The clause that ranks the two answers, and it is the one that had never been read: \"Although ICC enabled PDF processors should always follow the provisions and recommendations provided in 10.3 … a less-capable PDF processor may choose to use the algorithms specified in the following subclauses 10.4.2.2 through 10.4.2.5. These algorithms are, however, very simple and as perceived by a human viewer they produce only crude approximations of the original colours.\" This tree is on the §10.3 branch, which is a decision recorded here rather than an omission. **And the corpus has a page that measures what that decision buys**: `issue269_1.pdf` is 100 × 100 points of Illustrator art whose every mark is a `k` operator, so at 2304 dpi the whole raster is three colours and the histogram *is* the comparison. Ours and `poppler` are byte-identical on both — the black `0 0 0 1 k` at (35, 31, 32) and the blue `1 1 0.149 0.188 k` at (38, 40, 108) — while `mupdf` is two to three levels away on every channel. Two independent §10.3 branches landing on the same bytes is evidence that the assumption `colour.rs`'s `CMYK_CORNERS` makes about a press is the conventional one, and §10.3.1's \"beyond the scope of this document\" is why it can be nothing stronger. Recorded in the three-hundred-and-twentieth session; the page is `AMBIGUOUS_DEVICE_CMYK_CONVERSION`'s second member."

[[clause]]
clause = "10.4.2.2"
title = "Conversion between DeviceGray and DeviceRGB"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "`DeviceGray` to `DeviceRGB` is the clause's rule exactly — \"a gray level shall be equivalent to an RGB value with all three components the same\" — and it is the whole of `ColourSpace::Gray`'s conversion. **The other direction is on a route to a pixel after all, and this note said it was \"asked of nothing here\" until the three-hundred-and-eightieth session.** The NTSC weights that make an RGB colour grey are §11.5.3's EXAMPLE 2 word for word, so every `/Luminosity` soft mask on every page is that conversion: `pdf_render::Color::grey_level` states it once, `SoftMask::value` takes it off the rendered pixel and `ColourSpace::ink` needs the same number for a colour it has not painted yet. A conversion for a monochrome device turns out to be the conversion for a mask, which is the same shape §10.5's transfer functions were wrong about (ADR 0204)."

[[clause]]
clause = "10.4.2.3"
title = "Conversion between DeviceGray and DeviceCMYK"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::cmyk_ink_and_grey_are_the_clauses_own_arithmetic", "crates/pdf-model/tests/soft_masks.rs::a_cmyk_mask_group_takes_its_luminosity_from_the_clauses_own_formula"]
note = "**This row said `inapplicable` for its whole life and the claim decayed in the three-hundred-and-eightieth session.** It read \"[b]oth directions are for a device whose native space is CMYK or grey. This one's is RGB (§10.2), so neither conversion is on any route to a pixel\" — and §11.5.3's device branch is exactly this conversion on a route to a pixel: a `/Luminosity` soft mask composites its group in the space its `/CS` names and then converts the result to `DeviceGray`, and where that space is subtractive the means are these. The CMYK-to-grey direction is implemented: `ColourSpace::ink` is the sum inside the clause's `min` — linear in the components, which is what lets a mask group be *painted* in one number and composited by an ordinary rasteriser — and `ColourSpace::luminosity` applies the `min`. `partial` for the other direction, grey to CMYK, whose *formula* has no caller. **The reason this row gave for that expired in the four-hundred-and-twenty-sixth session and this sentence read \"nothing here converts to CMYK\" until the five-hundred-and-forty-fifth**: `ColourSpace::to_cmyk` converts a grey to ink, and §10.4.2.4's own row says so in as many words — two rows about one mechanism, one corrected and the other left standing, which is this file's commonest shape. What it does not use is *this clause's* arithmetic: a grey goes to RGB by §10.4.2.2 and then through `rgb_to_ink`, which is ADR 0263's right inverse of the ink cube rather than the black generation and undercolour removal this clause states. The identity that matters (a grey taken through CMYK and back is that grey, because the three weights sum to 1.0) is arithmetic rather than a code path. ADR 0217."

[[clause]]
clause = "10.4.2.4"
title = "Conversion from DeviceRGB to DeviceCMYK"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/src/colour.rs::a_grey_and_an_rgb_colour_weigh_the_same_ink_in_either_device_space"]
note = "**This row said `inapplicable` on the reason \"[n]othing on this device converts *to* CMYK, so the two functions are read by nobody and cannot change a pixel\", and the second half of that had stopped being true.** §11.6.6 makes a transparency group's `/CS` \"[t]he colour space into which colours shall be converted when painted into the group\", and a `/Luminosity` mask group blending in `DeviceCMYK` is on a route to a pixel — §11.5.3 takes the luminosity of what that conversion produced. So this clause is applicable, and it is carried out: **as the composed identity, because every term it generates cancels.** §10.4.2.3's three weights sum to 1.0, so `0.3(c − k) + 0.59(m − k) + 0.11(y − k) + k` is `0.3c + 0.59m + 0.11y` whatever the black-generation and undercolour-removal functions returned, and an RGB colour taken into a `DeviceCMYK` group and back to grey is §10.4.2.2's grey of the original. `ColourSpace::ink` has one arm for that reason and the identity is checked as arithmetic. `partial` for what is genuinely not done: Table 57's `/BG`, `/BG2`, `/UCR` and `/UCR2` are read by nobody, which cannot change a mask and *would* change a painted `DeviceCMYK` group — §11.6.6's departure, reported there. The three-hundred-and-eightieth session corrected §10.4.2.2 and §10.4.2.3 for exactly this shape; the three-hundred-and-eighty-third swept their neighbours and found this one (ADR 0220). **Implemented as of the four-hundred-and-twenty-sixth session, and immediately bounded.** `rgb_to_cmyk` is the clause's two steps with the nominal black generation and undercolour removal, reached by `ColourSpace::to_cmyk` — whose test pins the property that makes the standard's own pair coherent: §10.4.2.5 applied to this result is the identity, so a processor on §10.4.2's branch pays nothing for a round trip. This tree is not on that branch for the way back (ADRs 0009, 0042), so the conversion is *not* used to paint a page composited in ink; §11.7.2's row says why and ADR 0262 has the picture. **And the four-hundred-and-twenty-seventh took it off the painting path again, deliberately.** §11.7.5.3's first paragraph puts the conversion into a group's colour space on §10.3's branch, so `colour::rgb_to_ink` inverts the ink cube instead. This clause keeps two jobs inside that search and they are the clause's own: the nominal `k`, \"the minimum of the intermediate c , m , and y values that have been computed by subtracting the original red , green , and blue components from 1.0\", and the separation the search starts from. `rgb_to_cmyk` is still exactly the clause's two steps and still tested against §10.4.2.5's round trip. ADR 0263."

[[clause]]
clause = "10.4.2.5"
title = "Conversion from DeviceCMYK to DeviceRGB"
status = "partial"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "**The clause this project spent thirty-two sessions asserting did not exist.** It states a `DeviceCMYK` to `DeviceRGB` conversion outright, and `CMYK_CORNERS`'s comment used to open \"ISO 32000-2 gives no conversion from DeviceCMYK to any other space\" on the evidence of §8.6.4.4 — true of that clause and false of the standard. It is not implemented, deliberately: §10.4.2.1 offers it to \"a less-capable PDF processor\" and calls the family's results \"crude approximations\", and §10.3 is what this tree follows instead. Measured rather than argued away — swapping the table for this formula moves the oracle from 802 agreeing and 88 contradicted to 800 and 90. **And the choice costs something a clause away, which the four-hundred-and-fifteenth session measured.** This formula is affine in the four components except for one `min`: `1 - min(1, cyan + black)` per channel, so away from the clamp it commutes with §11.3.3's weighted average and compositing in `DeviceCMYK` would be compositing on the device's components, to 3 x 10⁻¹⁶ over 200 000 random pairs — with the clamp deferrable exactly the way ADR 0220 deferred §10.4.2.3's. The multilinear table is affine on no face of the cube, so §11.6.6's blending space is a second raster format *because of this decision* rather than because of the clause. Written down as the decision's cost, which is principle 1's rule (ADR 0251). **And the four-hundred-and-twenty-sixth session measured what the alternative costs where it matters most.** §11.4.7's page group needs a conversion *into* `DeviceCMYK` as well as out of it, and §10.4.2.4 followed by this clause is exactly the identity, where §10.4.2.4 followed by the ink cube is not — which is why a page that paints a colour from outside its blending space is reported rather than drawn (ADR 0262). **And the four-hundred-and-twenty-seventh session closed the question the paragraph above opened.** A conversion *into* `DeviceCMYK` on this tree's own branch exists now — a right inverse of the ink cube, `colour::rgb_to_ink` — so §11.4.7's page group is composited in ink without either half of §10.4.2's pair being used, and the \"second raster format\" this row recorded as the decision's cost turned out to be two display lists (ADR 0262) rather than a format. What the decision still costs is a *gamut*: the sixteen corners describe a press, and a colour outside what those inks make is mapped to the nearest one they do. §10.4.2.5's formula has no such boundary and no such press. ADR 0263."

[[clause]]
clause = "10.5"
title = "Transfer functions"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/src/content/ext_gstate.rs::a_transfer_function_maps_every_colour_component"]
note = "**Was `inapplicable`, and the three-hundred-and-fifty-seventh session found a corpus document that contradicts every word of the reason.** The reason read: a transfer function \"compensates for the device's actual behaviour\", operates \"in the native colour space of the output device\", its `/TR` and `/TR2` are deprecated in PDF 2.0, and \"[a] display's calibration is not a property this document may state\". `issue6931_reduced.pdf` states one anyway, and it is not a calibration: an `/ExtGState` sets `/TR` to three type-0 sampled functions whose tables map **2/255 to 0.992** and 0.0, 0.5 and 1.0 to 0.0 — a permutation rather than a gamma — and the page's image is a near-black raster whose every sample is 2. `poppler`, `ghostscript` and `hayro` apply the function and show a red heart on white; ours and `mupdf` do not and show a black square; and the page's own text says *The color should be red*. **And the standard draws the line itself, in the place this tree's reason conflated.** ISO 32000-2 never uses the phrase \"marking device\" — zero occurrences — and §8.3.2.2's term is a \"raster output device **such as a display or a printer**\". §10.1's list of rendering steps makes halftoning conditional and the transfer function not: \"[f]or any object for which transfer functions are in effect, apply those transfer functions\" against \"**[i]f** the raster output device supports PDF-defined halftoning, apply halftoning\". §10.6.1 says it outright for a screen: \"[h]alftoning is not required for such devices; **after gamma correction by the transfer functions**, the colour components shall be transmitted directly to the device.\" So §10.6 is inapplicable here and §10.5 is not. A transfer function changes what a **screen** shows, and the clause says so without qualification: \"[t]he output shall be the transformed component value to be transmitted to the device\", every sentence a `shall`, with only a `should` addressed to a *producer* — \"a page description that is intended to be device-independent should not define a current transfer function\". It was `silent` for one round — the first row to carry that status since the thirty-fifth session — and the project owner then amended `CLAUDE.md`'s scope line on this evidence rather than dropping it: §10.6's halftones stay inapplicable on the standard's own condition and §10.5 is in scope. **Implemented in the three-hundred-and-fifty-eighth session.** `content::Transfer` is three functions wide, read from Table 57 with `/TR2` in preference to `/TR` — the table's own precedence — and applied in `fill_paint`, `stroke_paint` and to an image's samples, which is where a colour becomes the value a device receives and is after the colour-space conversion the clause puts it after. **Three answers rather than two**, which is what `Stated` exists for: a state that says nothing leaves the transfer in force, and one that says `/Identity` or `/TR2 /Default` turns it *off* — this file's own witness uses both, one graphics state after the other, so a reader that folded them together would carry the transfer past the object it was written for. An RGB device uses the first three of four, which the clause states outright, and alpha is untouched because §10.5 speaks of colour components and §11's opacity is a different quantity. **The population is one page and that was measured before the code was written**: `examples/transfer_function_census` finds 13 of the 974 documents stating a `/TR` or `/TR2` and exactly one stating anything but `/Identity` or `/Default`. `issue6931_reduced.pdf` goes from 20.3861 to **3.61878**, against `poppler` 3.12869, `ghostscript` 3.70614 and `hayro` 3.67272; `mupdf` stays at 20.6726 and is now the only renderer of the five that does not apply it. Nothing else on the corpus moves. `doc/todo/13` keeps the argument. Not to be confused with §11.6.5.2's `/TR` inside a soft-mask dictionary, which is a different entry in a different dictionary and is implemented (ADR 0027)."

[[clause]]
clause = "10.6"
title = "Halftones"
status = "inapplicable"
note = "Halftoning approximates continuous tone with a pattern of dots on a device that cannot produce it. This one can: every component reaches the raster as eight bits. §10.1 makes the step conditional — \"if the raster output device supports PDF-defined halftoning\" — and it does not."

[[clause]]
clause = "10.6.1"
title = "General"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.2"
title = "Halftone screens"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.3"
title = "Spot functions"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.4"
title = "Threshold arrays"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.5"
title = "Halftone dictionaries"
status = "inapplicable"
note = "See §10.6. Table 57's `/HT` and `/HTO` are read by nobody for the same reason."

[[clause]]
clause = "10.6.5.1"
title = "General"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.5.2"
title = "Type 1 halftones"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.5.3"
title = "Type 6 halftones"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.5.4"
title = "Type 10 halftones"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.5.5"
title = "Type 16 halftones"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.6.5.6"
title = "Type 5 halftones"
status = "inapplicable"
note = "See §10.6."

[[clause]]
clause = "10.7"
title = "Scan conversion details"
status = "partial"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs", "crates/pdf-render/src/paint.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs", "crates/pdf-render/src/paint.rs"]
note = "Reviewed in the sixteenth session, as the family ADR 0025's image resampling lives in — and nothing in this tree had cited §10.7 before that. The subclauses below split three ways: two parameters the clause lets a processor ignore and which are ignored (§10.7.2, §10.7.3), one set of rules this renderer deliberately departs from in three places because it draws to a display rather than to a printer (§10.7.4), and one requirement that is neither implemented nor reported (§10.7.5). §10.7.1 is what licenses the departures and is quoted where they are taken. **A fourth reading arrived in the four-hundred-and-thirty-third session, and it is about the numbers rather than the rules.** §10.7 bounds no coordinate and §7.3.3 hands the range of a number to the implementation — \"[t]he range and precision of numbers may be limited by the internal representations used in the computer on which the PDF processor is running\" — so a magnitude the arithmetic cannot carry is this processor's to decide about. `tiny-skia`'s anti-aliased scan converter works in supersampled 16.16 fixed point and bounds the *pixmap* by 8191 while leaving the *path* unbounded; two of 65 944 crawled documents state fills reaching 10²⁵ device units and both aborted the process. `crates/render-cpu/src/scan.rs` is the one place a path now reaches that converter, and a path outside the range is drawn without anti-aliasing — which is the substitution `tiny-skia` makes for its own overflow test one branch over, and which §10.7.1's NOTE licenses in the same words it licenses the three departures below. ADR 0269."

[[clause]]
clause = "10.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-render/src/paint.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_the_basic_scene"]
note = "The one requirement is that scan conversion happens — \"the final step of rendering shall be scan conversion\" — and both backends are that step. The clause's substance for this project is its NOTE, which says the algorithm is not part of PDF: \"Different implementations can perform scan conversion in different ways; techniques that are appropriate for one device could be inappropriate for another.\" That sentence is what makes §10.7.4's departures choices rather than defects, and it is quoted in `Image::area_averaged` beside the rule it licenses departing from. Read it before concluding that any §10.7.4 rule is binding on a screen. **It licenses one more since the four-hundred-and-thirty-third session**, and this one is a departure from a *dependency's* algorithm rather than from a rule: geometry outside what `tiny-skia`'s anti-aliased converter can express is scan-converted by its aliased one instead, which is a different way of performing scan conversion and nothing more. `crates/render-cpu/src/scan.rs`, ADR 0269."

[[clause]]
clause = "10.7.2"
title = "Flatness tolerance"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::flatness_changes_nothing_because_the_clause_permits_ignoring_it"]
note = "The normative content is a permission, and it is exercised: \"PDF processors may choose to ignore any flatness tolerance specified within a PDF file.\" The `i` operator is matched and discarded with `MP`, `DP`, `BX` and `EX`, and `/FL` in an `/ExtGState` is not read; no corpus document writes `/FL` in one at all. `tiny-skia` and Vello each flatten curves to their own tolerance, which the clause's NOTE 2 anticipates by warning that a large value makes the result \"unpredictable\" — so honouring a document's number could only ever make a curve worse. Nothing is owed and nothing is silent, because the clause itself grants the omission."

[[clause]]
clause = "10.7.3"
title = "Smoothness tolerance"
status = "implemented"
code = ["crates/pdf-render/src/shading.rs", "crates/pdf-model/src/content.rs", "crates/pdf-model/src/mesh.rs"]
test = ["crates/pdf-render/src/shading.rs::a_smoothness_tolerance_only_ever_asks_for_more_samples", "crates/pdf-render/src/shading.rs::a_finer_tolerance_reaches_the_stops_only_where_there_is_detail_to_find", "crates/pdf-model/tests/shadings.rs::a_mesh_with_a_function_interpolates_the_parameter"]
note = "Table 57's `/SM` is read into the graphics state and decides how finely a shading's colour function is sampled — `Ramp::resolution_for`, since the seventy-fourth session. What the clause asks for is a bound on \"the allowable colour error between a shading approximated by piecewise linear interpolation and the true value\", and it moves the sampling in one direction only, which is the clause's own two sentences rather than a convenience: a tolerance is a *maximum* error, so a request coarser than this device's 1/256 is met by drawing finer, and \"each output device may have internal limits on the maximum and minimum tolerances attainable\" is what permits keeping ours rather than coarsening on request. A finer request is honoured up to 4096 samples, which is the internal limit the same sentence contemplates; 5 corpus documents state 0.002 and had been getting 1/256 in silence. A value outside 0..1 is not a fraction and is ignored, which one corpus document (`/SM 6`) needs. Note what a finer sampling does and does not buy, because it is not obvious: ADR 0068 drops every stop within half an eight-bit level of the line its neighbours draw, so extra samples of a *smooth* function are discarded again — the tolerance recovers detail narrower than 1/256 of the domain, which is a type 0 function with thousands of samples or a stitching function with narrow sub-domains. Meshes are the other half and were recorded here as needing nothing: `MeshRaster` evaluates Gouraud interpolation per device pixel (ADR 0051), so a mesh stating its colours has no piecewise approximation left to bound. **A mesh stating a `/Function` does**, since ADR 0292 — its parameter is interpolated and the function is read out of a `Ramp`, which is sampled at this clause's resolution like any other. The sentence that stood here also named `Triangle::is_subpixel`, which bounded the *geometry* of a subdivision that has not existed since the forty-third session and which ADR 0292 removed; `doc/performance.md` keeps what it measured.\n\n**§8.7.4.5.2's own internal limit gained a reader in session 573 and did not move** (ADR 0408). `MAX_FUNCTION_CELLS` bounds the type 1 grid under this clause's sentence about internal limits, and now that the grid is cut to the block a target can sample there are two things it could bound: the work, or where the samples fall. It bounds the **lattice**, which is where the samples fall, because a magnified page has to keep the lattice it would have had unclipped or the clip stops being exact. The cost is stated rather than hidden: past the limit a magnified shading is exactly as coarse as it was before, and bounding the block instead would make it finer *and* faster at the price of changing pixels — which is a fidelity decision this clause would permit and ADR 0408 section 6 keeps separate from an exact one."

[[clause]]
clause = "10.7.4"
title = "Scan conversion rules"
status = "partial"
code = ["crates/render-cpu/src/lib.rs", "crates/render-cpu/src/scan.rs", "crates/render-gpu/src/scene.rs", "crates/render-quorra/src/scene.rs", "crates/pdf-render/src/paint.rs", "crates/pdf-render/src/collapsed.rs", "crates/pdf-render/src/sub_pixel.rs"]
test = ["crates/pdf-render/src/paint.rs::a_block_of_samples_becomes_their_mean", "crates/pdf-render/src/paint.rs::the_divided_reduction_answers_what_the_serial_one_does", "crates/pdf-render/src/paint.rs::an_image_with_no_samples_is_not_reduced_and_does_not_panic", "crates/pdf-render/src/paint.rs::a_reduction_describes_the_raster_the_averaging_produces", "crates/render-quorra/src/cache.rs::a_reduced_upload_is_keyed_by_the_factors_that_produced_it", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_deeply_reduced_image", "crates/render-cpu/tests/zero_area_fill.rs::a_rectangle_of_no_height_marks_the_page_at_every_scale", "crates/render-cpu/tests/zero_area_fill.rs::a_flat_fill_carries_a_hairline_strokes_ink_at_its_own_placement", "crates/pdf-render/src/collapsed.rs::a_rectangle_of_no_height_marks_the_pixel_row_it_lies_in", "crates/pdf-render/src/collapsed.rs::a_line_on_a_pixel_boundary_takes_the_pixel_above_it", "crates/pdf-render/src/collapsed.rs::the_row_is_found_in_device_space_and_stated_back_in_the_paths", "crates/pdf-render/src/collapsed.rs::a_quarter_turn_marks_a_column_of_the_device_and_a_row_of_the_page", "crates/pdf-render/src/collapsed.rs::a_shear_keeps_the_band_because_a_pixel_run_is_a_staircase", "crates/render-cpu/src/scan.rs::a_clip_that_contains_the_mark_leaves_its_coverage_alone", "crates/render-cpu/src/scan.rs::a_value_multiplies_where_a_region_intersects", "crates/render-cpu/src/scan.rs::a_clip_on_the_pixel_grid_is_drawn_the_ordinary_way", "crates/render-cpu/src/scan.rs::a_clip_folded_into_a_soft_mask_still_takes_nothing_from_the_mark", "crates/render-cpu/src/scan.rs::folding_the_clip_into_the_value_would_square_the_boundary", "crates/render-cpu/src/scan.rs::a_clip_that_contains_the_mark_takes_nothing_from_it_under_a_soft_mask", "crates/render-cpu/tests/clip_intersection.rs::a_clip_coincident_with_the_mark_takes_nothing_from_it_under_a_soft_mask", "crates/render-cpu/tests/clip_intersection.rs::a_clip_coincident_with_the_mark_takes_nothing_from_it", "crates/render-cpu/tests/clip_intersection.rs::restating_the_clip_takes_nothing_either", "crates/render-cpu/tests/clip_intersection.rs::a_turned_clip_takes_nothing_from_a_turned_mark", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_sliver_thinner_than_a_quantum_carries_its_area_on_both_backends", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_sub_pixel_rule_at_the_rasters_edge_keeps_its_ink_on_both_backends", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_turned_sub_pixel_rule_carries_its_area_on_both_backends", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_zero_width_rule_is_one_device_pixel_wide_on_both_backends", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_sub_pixel_rules_cap_carries_its_own_area", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_degenerate_subpaths_dot_carries_its_own_area", "crates/pdf-render/src/sub_pixel.rs::a_mark_stated_at_the_substitute_keeps_the_area_its_own_width_implies", "crates/pdf-render/src/sub_pixel.rs::a_mark_at_the_quantum_is_left_exactly_as_the_document_stated_it", "crates/pdf-render/src/sub_pixel.rs::the_substitute_is_one_device_pixel_wide_under_a_similarity", "crates/pdf-render/src/sub_pixel.rs::an_anisotropic_transform_widens_the_substitute_rather_than_narrowing_it", "crates/pdf-render/src/sub_pixel.rs::a_rule_thinner_than_a_pixel_becomes_its_own_row_at_its_own_coverage", "crates/pdf-render/src/sub_pixel.rs::a_rule_across_a_boundary_divides_its_coverage_between_two_rows", "crates/pdf-render/src/sub_pixel.rs::two_rules_sharing_a_row_are_left_to_the_rasteriser", "crates/render-cpu/src/scan.rs::restating_a_clip_leaves_the_mask_alone", "crates/render-cpu/src/scan.rs::two_boundaries_in_one_pixel_take_the_smaller_coverage", "crates/render-cpu/tests/clip_bands.rs::restating_a_clip_changes_nothing", "crates/render-quorra/tests/abutting_marks.rs::one_mark_leaves_none_of_the_backdrop", "crates/render-quorra/tests/sub_pixel_coverage.rs::a_rule_under_one_level_of_alpha_still_marks_the_processors_raster", "crates/pdf-render/src/sub_pixel.rs::a_coverage_under_one_level_is_stated_at_one_level", "crates/pdf-render/src/sub_pixel.rs::an_enlarged_marks_coverage_is_floored_where_its_square_takes_it_under_a_level"]
note = "Three deliberate departures, all in one direction, and the sixteenth session is the first time any of them was written down. (1) \"A shape shall be scan-converted by painting any pixel whose half-open square region intersects the shape, no matter how small the intersection is\" — both backends anti-alias instead, so a partly covered pixel is partly painted. This has been true since the first commit and cited nothing. **It has a corpus witness as of the sixty-eighth session**: `colors.pdf` pages 1 and 2 are grids of flat swatches whose interiors every renderer agrees about to the byte, and whose boundaries put the five on a spectrum of edge softness — `poppler` hardest, then `ghostscript`, then `mupdf` and `hayro`, then us. The pair the oracle votes with is the pair nearest the clause, so the two pages are contradicted by the departure itself and are listed under it rather than chased. **The sentence that follows it — \"This ensures that no shape ever disappears\" — is honoured since the hundred-and-eighty-sixth session, and it had not been.** A fill whose subpath has no extent along one axis has zero area, so anti-aliasing computes zero coverage for it at every placement and every scale, and `issue4260_reduced.pdf` ruled its whole grid with `848 1085 10159 0 re f` and drew nothing. `pdf_render::collapsed` gives such a subpath the thinnest mark the device has — the width §8.4.3.2 and §10.7.5 already agree on, stated once in `pdf_render::thinnest_line` — as geometry built in the shared crate so that neither backend decides it alone (trap 2), and filled under the non-zero rule beside the path rather than inside it, since a mark joining an even-odd path's winding would punch a hole. The reading is argued in that module: the half-open convention stated two paragraphs earlier annihilates a shape whose floor and ceiling are one line, and what breaks the tie is §8.5.3.3.1 stating that a *point* encloses a whole device pixel — a shape may not mark less of the page than a shape it contains. **A second disappearance was measured in the three-hundred-and-forty-fourth session, found to be *one backend's*, and paid in the three-hundred-and-eighty-ninth.** `tiny-skia` supersamples four times per pixel row and takes each sub-row's sample at its centre — device y = 0.125, 0.375, 0.625, 0.875 — so a fill lying between two of those lines crossed none of them and drew nothing, and one crossing a single line was rounded up to a quarter of a row; along x the same converter quantises a run to quarter-pixel steps, so its smallest non-zero coverage is a sixteenth of a pixel. On an 80-unit rule at scale 1 the ink was 0.05 units to 0, 0.1 to 0, 0.2 to 0.2471 and 0.5 to 0.4980, where the graphics device answered 0.0510, 0.1020, 0.2000 and 0.5020. The same quantum reached a *stroke* by a second route: `tiny-skia`'s painter draws one under a pixel wide as a hairline with the paint's opacity scaled by the width, which conserves the ink and smears it symmetrically about the path, so an interior rule carried 0.0941 of an expected 0.1 and one within half a pixel of the raster's edge carried 0.0549 against the device's 0.0980. **What closed both is `pdf_render::sub_pixel_bands`**: an axis-aligned rectangle thinner than a device pixel is drawn as the whole pixel line it lies in, painted at the coverage its own area there implies — which is exactly proportional coverage, in both axes at once, and is neither the clause's whole pixel nor nothing. Coverage becomes alpha, which §11.3.7.1 says is the product of shape and opacity and §11.3.7.2's NOTE 1 says anti-aliased coverage is the first factor of; the one mode where the two may not be folded is §11.4.6's knockout, which the backend declines by name. A sub-pixel *stroke* whose path is made of straight axis-aligned rules is converted to the fill of its own outline and takes the same rule; anything else — a diagonal, a dash, a round cap, a shear, a subpath that is not a rectangle, two rules meeting in one pixel row — keeps the rasteriser's own answer, and `pdf_render::sub_pixel`'s module comment argues each. Both backends now answer 0.0510, 0.1020, 0.2000, 0.5020 and 1.0000 to the five slivers and 0.098 to 0.102 to the five rules, agreeing to within one level of 255, and `sub_pixel_coverage.rs` gates both against the *shape's own area* rather than against each other. The rule is conditioned on this rasteriser anti-aliasing at all, since that departure is its whole warrant: with anti-aliasing off the clause's own answer is a whole covered pixel and a fractional alpha is neither that nor what the caller asked for. It **removes** a promotion as well as a disappearance, which is what says it does not fight the departure on ordinary thin shapes: `tiny-skia` rounded a rectangle 0.9 of a pixel thick up to a whole row, 11% heavy, and `issue8125.pdf` page 1 left the oracle's contradicted list on that half rather than the vanishing one. What it declines is a rule that is not axis-aligned, and **that residual was closed in the four-hundred-and-thirty-second session by a different reading of the same subclause** (ADR 0268). A diagonal cannot fall to the quantum: a band lying between two of `tiny-skia`'s sample lines vanishes and a band that is not parallel to them crosses one every `1 / (4 tan θ)` pixels of its length, so a filled diagonal sliver 0.05 of a pixel thick reads 9.47 to 10.23 of its own 10 at every angle from 5° to 60°. What was failing is the clause's *other* guarantee, and only for a stroke: \"The area covered by painted pixels shall always be at least as large as the area of the original shape.\" `tiny-skia`'s hairline lays one pixel down per step along the line's **longer device axis**, so it carries `cos θ` of a turned rule's area — 3.4% short at 15°, 13.4% at 30° and 29.3% at 45°, at every thickness under a pixel rather than only near the quantum. The substitute needs no scan converter and no pixel line: the same path stroked one device pixel wide (`pdf_render::substitute_width`, the reciprocal of the transform's *smaller* stretch so that the band is a whole pixel across whichever way the path runs), butt-capped, with the width it gave up carried in the paint's alpha — which conserves the ink at every angle, because widening by a factor and dividing the alpha by it cancel, and which is this clause's own run of whole pixels with §11.3.7.1's licence for where the coverage goes. It is tried *after* `sub_pixel_bands`, because it is blunter: a band one pixel wide spreads its ink over a pixel where the exact substitution spreads it over the shape's own width. The caps were the substitute's one deliberate loss until the four-hundred-and-fifty-fifth session, and the measurement that justified dropping them is the measurement that says how to keep them: a cap's area goes as the square of the width, so widening overstates it by `width / style.width` — on `issue12295.pdf`, 65 859 sub-pixel strokes all round-capped and 91.8% of them shorter than one device pixel, keeping the stroker's cap put the page 66% over its own 8× limit where the swept body alone puts it 8.9% over. **The missing factor is the answer rather than the missing mark** (ADR 0290): `pdf_render::enlarged_mark` states a mark whose area is a *square* of the width at the substitute's width with an alpha of `(w / W)²`, and Table 53's projecting caps lie outside the butt-capped body — \"beyond the endpoint\" is the table's own word — so `pdf_render::sub_pixel_caps` is a second disjoint mark whose ink adds rather than a wider stroke whose ink is overstated. A rule as long as it is wide was drawing 0.2510 of its own 0.4463 under round caps and 0.2510 of 0.5000 under projecting square ones, and a rule *shorter* than its own width drew **nothing at all**; they read 0.4392 and 0.4863 and 0.2353 of 0.2713 now. The same substitution answers §8.5.3.2's dot, which was disappearing outright under 0.2 of a pixel and is that clause's row. Every oracle verdict count is identical, 36 of 1794 per-page lines moved and 35 of them in the third decimal place, `doc/todo/00`'s step-7 sweep moved 49 rows and every one of them up, and the cross-backend gate is unmoved at 915/37/5/17. It costs +0.19% of the rasteriser's instructions on the page of the standard itself and **+146.8%** on `issue12295.pdf`, whose 65 859 capped strokes are each a second fill; `doc/todo/11` prices the one-draw construction that would take that back. What is left there is the eight-bit raster rather than the geometry: that page's caps are 1.14 levels of 255 of area and 0.133 of a level landed, because each lands as about half a level on each of a few pixels — `pdf-model/examples/sub_pixel_width_census` is the instrument. Measured over the corpus: every oracle verdict count identical, the two backends' own gate 910 agree/36 differ to **911/35** with `issue12810.pdf` leaving — a page whose 34 787 sub-pixel strokes carry 62.6% of their length within 40° to 50° of an axis, which is 19.4% of its stroke ink weighted by length — and `issue11473.pdf`'s three diagonal hatch swatches going 0.6768 to 0.7566 against an 8× rung of 0.7516. It costs 18% of the rasteriser's instructions on a page that is nothing but a hatch pattern and 0.13% on the page of the standard itself. **And `22060_A1_01_Plans.pdf` was never the witness this ledger, `oracle.rs` and `doc/todo/11` all called it**: its page one is 72 sampled images, 24 fills and 40 strokes, of which 26 are sub-pixel with 98% of their length within 5° of a device axis, so it is this subclause's *image* paragraph and departure (3) below that move it. What is left is the rule that is **exactly** one device pixel wide, which `tiny-skia` also hairlines and which is not a disappearance; `doc/todo/11` carries it with its numbers. Measured over the corpus: the oracle's agreeing pages 858 to 859 and contradicted 68 to 67 with 401 of 1794 pages' numbers moving, and the two backends' own gate 914 agree/42 differ to 920/36, `issue16038.pdf` moving 6.5359 to 1.8563. It costs 0.12% of the rasteriser's instructions on a page of the standard itself. ADR 0226. (2) The same rule's \"the area covered by painted pixels shall always be at least as large as the area of the original shape\" fails for the same reason. (3) \"There shall not be averaging over the pixel area\" — `Image::area_averaged` averages, which is ADR 0025 and what made `bug1001080.pdf` legible and `firefox_logo.pdf` and `french_diacritics.pdf` agree with the reference consensus. **And that departure's size is measured on a real page since the three-hundred-and-seventy-second session, where before it was only argued.** `issue4379.pdf` places a 1000 x 800 stencil-masked image onto device x [36, 536) and y [36, 436), an exact two-to-one reduction onto integer coordinates, so the paragraph names one raster sample by sample: device column `i` takes source column `2(i - 36) + 1`. The source samples are settled without a reference being trusted — at 2x the image is one device pixel per sample and this tree, `mupdf` and `ghostscript` produce byte-identical rasters — and against the raster the clause states, `ghostscript` differs on **0** of 500 990 pixels while this tree differs on **3 927**, 0.78% of the page, at 0.447 of 255 mean absolute error. The five renderers' ink agrees to 0.023 of 255 on that page, so no ink measurement could have seen it. `AMBIGUOUS_IMAGE_REDUCTION` in `oracle.rs` carries the table. **What that departure *costs a page turn* was measured in the three-hundred-and-ninety-first session, and it is the largest single item in this tree's display-list-to-scene walk**: the averaging is paid per *source* sample, so one 2700x3450 photograph cost 22.4 ms where a 3675-command page of text cost 1.0 ms in total. Nothing about the clause changed — the output is byte-identical, which `the_divided_reduction_answers_what_the_serial_one_does` and the oracle's 1794 unmoved verdicts both say — and what changed is that the column bands are computed once per image rather than once per output cell and the output rows are divided across rayon above a measured floor, 22.4 ms to 2.9. `pdf-render/examples/area_bench` is the instrument and ADR 0228 is the argument. The same session put `is_consistent` *before* the reduction rather than beside it: `Image::reduction` clamps into `1.0 ..= width` and `f32::clamp` panics when its minimum exceeds its maximum, so a zero-width image reached a panic through `render-quorra`, which was the one of the three backends that did not ask the question first. All three are licensed by §10.7.1's NOTE that the algorithm is not defined by PDF, and all three describe a device that quantises coverage to whole pixels, which a display does not. What *is* honoured: pixel boundaries on integers, half-open regions, a zero-width stroke drawn as the thinnest line the device can produce (the clause's own permission), and glyphs scan-converted by the font rasteriser's own algorithm, which the clause's last sentence allows. **That last permission has a corpus witness as of the two-hundred-and-thirty-first session**: `issue4402_reduced.pdf` is a 215 x 28 crop box holding one line of eight-point text and a rule, so the page's mean *is* its glyph coverage, and the five renderers spread by 3.0 of 255 on it while agreeing on the geometry — the two-ladder limit is 56.98 and ours climbs onto it. `AMBIGUOUS_GLYPH_SCAN_CONVERSION` in `oracle.rs` carries the measurement. **Where a mark goes was the other half of that sentence and was paid in the three-hundred-and-sixty-eighth session.** Until then the mark was a band of one device pixel centred on the collapsed subpath's own fractional position, which an anti-aliasing rasteriser splits across two pixel rows at every placement but one — so `issue4260_reduced.pdf`'s grid drew as a mixture of crisp lines and fuzzy grey double ones, and the project owner saw it beside Okular on a high-DPI screen. That is placement dependence, which is precisely what the clause's stated purpose forbids: \"no shape ever disappears as a result of unfavourable placement relative to the device pixel grid\". The clause states the answer twice more and both statements are about pixels rather than about a width — NOTE 1 says a filling region \"is considered to intersect every pixel through which its boundary passes, even if the interior of the filling region is empty\", and the EXAMPLE beside it says \"A zero-width or zero-height rectangle paints a line 1 pixel wide.\" The mark is therefore the run of whole device pixels the collapsed axis passes through, found by flooring that axis's device coordinate the way the same subclause identifies a pixel, and stated back in the path's own space so that the ordinary fill machinery paints it at full coverage. Only the collapsed axis is snapped: along the other one the subpath has a stated extent, and what an extent gets on this device is the coverage its area implies, which is departure (1) above. The snap needs a device axis to be a path axis, so it is taken only under an axis-preserving transform — a scale and a translation, or a quarter turn — and under a rotation or a shear the band remains, because the run of pixels a slanted line passes through is a staircase rather than a rectangle. Measured with `render-quorra/examples/mark_width`: 1–2 rows per line and 1.00 ink before, **1 row and 1.00 ink on both backends at 1x, 2x and 4x** after. One page in the oracle's 1794 moved — that document's own, mean 13.31 to 13.09, differing 10.02% to 6.87%, similarity 0.5619 to 0.5835, verdict unchanged — and the quorra gate's page count went 913 agree/43 differ to 914/42, because two rasterisers have nothing left to distribute differently once a mark is a whole row. **A `0 w` stroke down the same line deliberately does not follow it**, and §10.7.5's row carries that boundary. ADR 0208. **One further choice the clause does not reach, recorded because it was a defect until the sixty-first session**: where a page whose size is not a whole number of pixels puts its spare fraction. `TargetSpec::for_page` rounds the raster *up* so it contains the page, and anchors the flip to the page's own top edge, so the spare fraction is at the bottom — where the x axis already puts its own, because nothing flips x. Anchoring it to the raster's last row instead put every mark on such a page a fraction of a row too low, which was 11 of the corpus's 76 contradicted pages (ADR 0064). How many pixels a fractional page *gets* is a separate question the standard also leaves open, and the four renderers here answer it two ways. **And the clause's sampled-image paragraph is carried out for the first time in the three-hundred-and-seventieth session, where it decides a picture rather than an approximation.** \"The position of the centre of such a pixel -in other words, the point whose coordinate values have fractional parts of one-half -shall be mapped back into source space to determine how to colour the pixel\" is what `SoftMaskAtDeviceScale::raster` does: a §11.6.5.2 soft mask whose grid cannot be refined with its image's is read at the device's own grid, cell `i` of `n` taking the sample at `(2i + 1) x samples / 2n`. `pdf_render::Grid::for_placement` is where the device grid is decided, once, so that no backend picks its own — the fourth thing in that crate for the reason `Image::is_smoothed` is there. It is a **departure from departure (2)**, deliberately: ADR 0025 averages a reduced image on a measured witness, and averaging a mask would mean decoding every one of its samples, which is the whole cost the deferral avoids. What that costs is the clause's own last sentence — \"some source samples might not be used\" — and `issue16263.pdf`, whose mask is a 34862x4332 field of two values, loses nothing to it. ADR 0210. **That paragraph has a second corpus witness since the four-hundred-and-fifth session, and this time it is a departure rather than an implementation.** `issue4436r.pdf` draws a 1x1 image mask under `180 0 0 -0.48 10 25 cm` on a 200x50 page, so its device region is y `[25, 25.48)` and row 25's centre is at 25.5 — outside it. Under \"only those pixels whose centres lie within the region shall be painted\" **nothing is painted at all**, and the guarantee that no shape disappears is stated two paragraphs earlier for a *shape*, which the image paragraph's own \"though not identically\" declines to carry over. Coverage of row 25: the clause 0.000, the geometry 0.480, ours 0.502, and `poppler`, `mupdf`, `ghostscript` and `hayro` 1.000 apiece — all five renderers departing, in two directions. Ours is departure (1) above; the references' is the *shape* rule applied to an image. The 0.502 against 0.480 is `tiny-skia`'s four sub-rows, the quantum ADR 0226 removed for an axis-aligned rectangle and which an image does not take that path to. `CONTRADICTED_SUBPIXEL_IMAGE` in `oracle.rs` carries the arithmetic; the entry used to say the standard decided nothing here. **And the same paragraph has a third corpus witness since the four-hundred-and-forty-third session, on which this tree carries it out against the reference consensus.** `colorkeymask.pdf` draws a 200 x 267 `/DeviceRGB` image with §8.9.6.4's `/Mask` through a tiling pattern under `200 0 0 267 0 0 cm`, so its device region is x `[18, 218)` and y `[17.9998, 284.9998)` — one device pixel per source sample exactly, no reduction and no `/Interpolate`. Ours is **byte-identical to `ghostscript` over the whole 595 x 842 raster**, and `poppler`, which votes with `mupdf`, differs from both on 942 pixels of 500 990: 268 apiece in device columns 78, 138 and 218 and 141 in device row 17. Both halves of the paragraph decide those and both decide for us. Row 17's centre is at y 17.5, outside the region, so \"only those pixels whose centres lie within the region shall be painted\" paints nothing there and we paint nothing there; rows 18 to 284 are 267 rows for 267 samples. Column 78's centre is at x 78.5, which maps back to source x 60.5 — sample 60 — and the image's row 110 is `(255, 0, 0)` at samples 58 and 59 and `(0, 255, 0)` at 60 and 61, read out of the file's own uncompressed bytes; ours paints `(0, 255, 0)` and `poppler` paints `(130, 201, 77)`, which is neither sample. That is \"[t]here shall not be averaging over the pixel area\" honoured at a one-to-one placement, which is the case departure (3) has nothing to average in. `CONTRADICTED_IMAGE_SAMPLE_AT_THE_PIXEL_CENTRE` in `oracle.rs` carries it. **A fourth departure was named in the four-hundred-and-forty-third session, and this row had never mentioned the subclause's clipping paragraph at all**: \"For clipping, the clipping region consists of the set of pixels that would be included by a fill operation. Subsequent painting operations shall affect a region that is the intersection of the set of pixels defined by the clipping region with the set of pixels for the region to be painted.\" A clipping region is a *set of pixels*, obtained by the fill rule that paints any pixel the path meets however little of it is covered, and its effect on a later painting operation is a set intersection. This tree composed a clip chain by **multiplying** coverages — `MaskCache::build` filled the chain's root and then called `tiny_skia::Mask::intersect_path` for each nested clip, which multiplies — and multiplies the result into a mark's own coverage again inside the rasteriser's fill. Where two clip boundaries fall on the same device edge that is not an approximation of anything: it is a coverage raised to a power. Measured on a ladder written for it — one 178.34645-point page, the whole page filled, under **n** `W n` clips of a rectangle whose left edge lands at device 113.386 at 8x — the boundary column's coverage was 0.5020, 0.2510, 0.1255, 0.0627, 0.0314 and 0.0157 for n from 1 to 6, each rung the one above it halved. `issue21346.pdf` states that rectangle six times — a `W n`, three `/BBox` clips under §8.10.1 step c), the mark's own path and the §11.6.5.2 mask group's — and its edge was painted at 0.041 of the mark where the geometry is 0.827 of it and this clause is 1.000; `poppler` and `ghostscript` paint 1.000, `mupdf` 0.755 and `hayro` 0.327. **Half paid in the four-hundred-and-forty-fourth session**, and the half that is paid is the one this tree composes: a clip *chain*. §8.5.4 says what the intersection does to a value — \"The effective shape is the intersection of the object's intrinsic shape with the clipping path; the source shape value shall be 0.0 outside this intersection\" — so a clip zeroes what is outside it and is silent about what is inside it, and nothing in either clause makes a clip's boundary a quantity that multiplies. Neither clause chooses between `min` and a product on its own, because on a set-valued clip the two are the same function; the question exists only because departure (1) gives a boundary pixel a fraction, and what decides it is that a product moves further from the clause with every restatement while `min` is exact for coincident and nested boundaries and never *below* the product elsewhere, so it is never further from the clause. `scan::mask_intersect` fills a scratch mask and takes the smaller of the two coverages where it called `tiny_skia::Mask::intersect_path` before; the ladder above is flat at 0.5020 for every n, and `issue21346.pdf`'s edge went **0.041 to 0.163** of the mark — three of its six statements are the chain the mark draws under, so two factors come out and the fourfold is what 0.5020 predicts. The page stays contradicted, its failing structural similarity 0.9734 to 0.9781 against a bound of 0.9900, which ADR 0279 predicted before the change. Measured over the corpus: every oracle verdict count, the corpus's counts, both text gates and `render-quorra`'s 917 agree / 35 differ / 5 refused / 17 not comparable identical, 22 of the oracle's 1794 per-page lines moving in the third decimal place with no verdict changing, and `doc/todo/00`'s step 7 byte-identical in its negative tail over all 786 ambiguous pages. It costs +0.19% of the rasteriser's instructions on a page of the standard itself and **buys 8.75%** on `bug1721218_reduced.pdf`, the corpus's heaviest clip page, because a chain now allocates one scratch mask rather than one per link. **What remains is the same sentence one step along**: `tiny-skia`'s `fill_path` multiplies the finished mask into the mark's own coverage, which is two more of that page's six factors and needs this backend's own blitter rather than the library's; the sixth is §11.6.5's alpha and is a product the standard states. **`render-quorra` composed its chain inside the graphics library and multiplied too, and no gate in this tree could see it — this sentence said so, and quorra answered section 18 by taking `min` there as well** (its ADR 0030). It read the clause rather than this tree's reading of it, and reached the same rule from §8.5.4's own sentence: the graphics state holds *one* clipping path and each `W` replaces it with the intersection of two paths, so rasterising each link separately is a convenience and nothing in the standard composes two fractional coverages. Its measurement declines to arbitrate — product chain, `min` chain, and `min` chain with a `min` mark all give 915 agree / 37 differ / 5 refused on the 957 pages this gate compares, with no page's mean, worst tile or similarity moving — which is what this tree measured too when it took the same step, and is why the agreement is about the clause rather than about a picture. **Where the clip meets the *mark* quorra keeps the product, deliberately**, on the argument that two unrelated boundaries sharing a pixel are the common case and a product is the estimator that assumes it: the same position where this tree is still `tiny-skia`'s, and the same open question. ADR 0280; `doc/todo/11` carries what is owed. `CONTRADICTED_COINCIDENT_CLIP_EDGES` in `oracle.rs` carries the page. **And the substitute's own boundary became inclusive (ADR 0285)**, which is this clause's `shall` about area rather than a departure from it: \"[t]he area covered by painted pixels shall always be at least as large as the area of the original shape. This rule applies both to fill operations and to strokes with non-zero width\" — and `tiny-skia` takes its hairline for every width up to *and including* one device pixel, so a `1 w` rule carried `cos θ` of its own area, 29.3% short at 45°, on most of the line work in every technical drawing. The general substitution now takes the boundary; the *exact* one stays strictly under it, because snapping a one-pixel rule onto a row would be §10.7.5's stroke adjustment without `/SA`; and the cap comes back there, because ADR 0268's overstatement factor is exactly 1 at the quantum. **The zero-width stroke follows the same rule and that is a documented choice**: this clause permits it not to — \"Zero-width strokes may be done in an implementation-defined manner that may include fewer pixels than the rule implies\" — while §8.4.3.2 says \"[a] line width of 0 shall denote the thinnest line that can be rendered at device resolution: 1 device pixel wide\", and what settles the two readings is that `Stroke::device_width` resolves a zero width in the shared crate so that both backends draw one mark. The whole corpus gate is byte-identical either way, measured, so no document ranks it. **And departure (1)'s raster is now *kept* rather than recomputed, which changes no pixel and was 57% of a redraw** (ADR 0297). The averaging this clause licenses departing from costs one pass over the *source* samples, so on a scanned page it is the largest thing in a frame and it does not shrink with the window: 8.5 to 9.8 ms of a 12.7 to 16.8 ms scroll step on a 2700x3450 page, recomputed identically every step because the samples, the scale and the page were all unchanged. `Image::reduction` names the grid the departure would produce — the two factors, the dimensions and the filter — without producing it, so `render-quorra` keys the raster by the source's identity and those factors and hits on every redraw at an unchanged scale. The exactness is the same statement the departure itself rests on: every output byte is a function of the source samples and the two factors. Median frame 15.0 to 4.8 ms, uploads 23 to 2 over a twenty-step scroll, three runs an arm; the corpus gate at scale 1 and at scale 4 is unmoved, the second byte for byte. **Departure (2) had no witness of its own until the four-hundred-and-seventy-third session, and now has a large one** (ADR 0308). Where a document states one region as many opaque fills rather than one, every internal boundary lands inside some device pixel and §11.3.7.3 unites each mark's coverage with what is already there, so two marks covering half a boundary pixel each leave a quarter of whatever is beneath and *n* equal shares rise towards `1/e`. A 50 MB Inkscape cross-section reported by the project owner states its page as 58 003 `f` operators, 2 868 970 curve segments and no clipping path at all, and a dark frame rule under it keeps **0.1937** of itself at page scale, 0.1282 at 2×, 0.0673 at 4× and 0.0156 at 8× — which is why the reader sees it at a page fit and not when magnified. It is not a defect of either backend: on a fixture of two rectangles abutting half way across a pixel the processor leaves 0.2510, `render-quorra` 0.2471 and `render-gpu` 0.2510 against the union's 0.2500, and `mutool` leaves 0.2510 beside them. `gs` with anti-aliasing off leaves nothing, which is this clause's own aliased rule and is the whole point; `pdftoppm` also leaves nothing and **that is not conflation handling** — the control, one rectangle whose right edge lands mid-pixel, reads 0.498 here, 0.533 on `mupdf` and 0.467 on `gs` against poppler's 0.000, so poppler snaps an axis-aligned rectangle to whole pixels, and on a seam that is not axis-aligned it leaks like the rest. Nothing removes the seam short of resolving the marks against one another before compositing; `doc/todo/11` item 5 prices the two constructions that would and says why neither is started. `crates/pdf-model/examples/uncovered_share.rs` measures it on any page and `crates/render-quorra/tests/abutting_marks.rs` holds both backends to the union as an upper bound, so a conflation-free rasteriser would pass it unchanged. **And the fourth departure is narrowed again in the five-hundred-and-twentieth session, one step past ADR 0280 and to the mark itself** (ADR 0355). `tiny_skia::PixmapMut::fill_path` multiplies the clip mask into the mark's own coverage, so a mark whose boundary falls in a pixel a clip boundary also crosses was painted at the product of two fractions; `scan::intersected` now rasterises the mark's coverage into a buffer, takes the smaller of it and the region per pixel, and blits the paint through the result over the whole device pixels the mark reaches. The closed form it is tested against is the clause's own set identity rather than anybody's arithmetic — `S ∩ C = S` where `S ⊆ C` — so **a clip that contains a mark takes nothing from it**, the mark's anti-aliased boundary included. It declines where the substitution would say something else: a clip already 0 or 255 under the mark, where the product *is* the intersection; a mark that is not anti-aliased; and `BlendMode::Source`, on `carries_coverage_as_alpha`'s own argument. `issue21346.pdf` went 0.163 → **0.306** of the mark and its similarity 0.9781 → 0.9846, and what still holds it down is a clip *folded into a soft mask* by `MaskCache::combine`, which arrives as a value with no set left to intersect. The corpus population is a §12.5.5 widget appearance whose border rule sits on the `/BBox` §8.10.1 step c) clips it by: no oracle verdict moved, 88 per-page lines did, and 240 of the ink sweep's 241 moved rows moved up. It cost +1.21% of the rasteriser on a page of text and +5.54% on the corpus's heaviest clip page, and **the first version cost +54% there** — one reused coverage buffer instead of one allocation per mark, with the oracle's output byte-identical across that difference. A stroke's coverage, an image's edge and a group's raster still multiply, each for a reason `doc/todo/11` item 4 states; `render-quorra` and `render-gpu` multiply at the mark too, and the cross-backend gate parts on four pages of that population. **The fourth departure is narrowed once more in the five-hundred-and-twenty-eighth session, to a clip standing *beside* a soft mask** (ADR 0363), which ADR 0355 had declined on the ground that `MaskCache::combine` leaves no set to intersect. The standard states the two in an order the fold destroyed: §8.5.4 intersects the clipping path with the object's own shape and §11.3.7.2 multiplies the mask shape into what comes out, so the source shape is `(f_j ∩ C) · f_m` and not `f_j · (C · f_m)`. The cache keeps the soft mask's rows beside the product and `scan::intersected` composes `min(M·S, C·S)`, which needs no third buffer because multiplication by a non-negative value distributes over a minimum, and which is exact in eight bits because a minimum commutes with a monotone rounding. The decline test generalises with it — the clip is a set where the product is 0 or `S` — and is loose in one direction by a bound of half a level of 255, stated in `scan::intersected`. **No corpus page can tell the two compositions apart**: 120 commands take a clip and a soft mask together over the 974 first pages, 27 are fills reaching the composition, 14 decline as a set and 13 compose, over five documents whose rasters are byte-identical at scale 1 and at 4× — the two part only where the mark's boundary and the clip's are both fractional in one pixel. Every gate is byte-identical per page, the ink sweep moved 0 of 786 rows, and it cost −0.12% to +0.61% of the rasteriser over five measured pages. **And the same round found this row's account of `issue21346.pdf` wrong**: nothing on that page reaches `scan::fill` with a clip folded into a soft mask, and what holds its edge down is a transparency group's raster meeting its clip — which §8.5.4's third sentence says is owed, against ADR 0355's \"not owed\", and which is worth 0.306 → 0.571 of the mark there. **A fifth account arrives in the five-hundred-and-seventy-sixth session, and it is about which of quorra's two coverage lanes draws a mark rather than about a departure of this tree's own.** That library's *device* coverage lane counts samples on an ordered grid, so it asks whether a sample point lies inside the shape and never whether the shape meets the pixel — which is this clause's first requirement — and a mark whose thin axis is under the grid's column spacing therefore disappears at some sub-pixel placements and is drawn heavy at others. quorra's `cad50156` (their ADR 0070) sends such a mark down the *processor* lane whatever its cost conditions say, on a threshold of one over the square root of the sample count, which is the grid's own spacing rather than a chosen number. This tree reaches that lane past `GPU_COVERAGE_MAGNIFICATION`, so the corpus gate's non-default lane is where it shows, and the two revisions were run side by side here: at page scale on the device lane **930 agree / 25 differ becomes 932 / 23**, with exactly two lines leaving — `bug1883609.pdf` and `vertical.pdf` — and every other line character-identical; at 4× `issue12295.pdf` moves toward the processor oracle without reaching it (mean 0.9517 to 0.9201, differing 0.0490 to 0.0473, similarity 0.95585 to 0.95881). **The counts converge and the sets do not**, which is the part worth keeping: after the change each lane differs from the oracle on 23 pages at page scale and two of each 23 are its own — the device lane alone on `bug1863910.pdf` and `issue16500.pdf`, the processor lane alone on `bug1743245.pdf` and `issue21068.pdf` — so what is left on the device side is a residue this condition does not reach rather than one it caused. ADR 0411. **And the sixth account is the floor under all of the above, taken in the five-hundred-and-eighty-fourth session** (ADR 0419). Every substitution named here carries a mark's given-up area in the paint's **alpha**, and an alpha is eight bits — so each of them has a second floor, below the rasteriser's coverage quantum ADR 0226 answered and reached by a different road: a coverage under `1/255` rounds to nothing and the mark is gone. Measured on `sub_pixel_marks`' new seventh section, a 200-unit rule at 0.002 and 0.001 of a device pixel drew **no ink at all** on both backends, at 0° and at 30°, and a ladder of one rule at seventeen widths against `pdftoppm`, `mutool`, `gs` and `hayro` puts ours as the only column that reaches zero. `pdf_render::expressible_coverage` states a positive coverage under one level *at* one level, in the one place this crate is allowed to know the raster's depth, and it is applied to all three coverages the constructions produce — `SubPixelBand::coverage`, `EnlargedMark::coverage` and the turned body's `style.width / width`, which `render-cpu` had been computing itself. It costs ink and this clause asks for that direction rather than the other; what it does *not* do is §10.7.5's promotion, since the mark keeps its place and the substitute's width and only the last level of its alpha moves. **No document on this disk is a witness**: `sub_pixel_width_census` over 1005 first pages of every corpus here puts the thinnest stated sub-pixel stroke at 0.05 of a device pixel, so this is an unwitnessed `shall` taken because it is one, the same shape as `collapsed`'s. **Two things are left and both are measured rather than assumed**: `render-quorra` still loses the mark at 0.002 and 0.001, because it takes no substitution here and the floor has nowhere to be applied; and §8.5.3.2's dot is drawn as **nothing** by both backends at 0.1, 0.05, 0.02 and 0.01 of a device pixel, where ADR 0290's ladder has been printing the 0.1 row as −100% since it landed and this row's sentence above says only \"under 0.2 of a pixel\". Raising the alpha does not recover that one, and the reason is the construction rather than the arithmetic: the substitute is a circle **inscribed in** one device pixel, which covers no pixel fully, so the raster's rounding takes the mark a second time after the alpha survived it — and stating it as the whole pixel it lies in would be snapping a mark that has a width, which §10.7.5 conditions on `/SA` and ADR 0208 declines. `doc/todo/11` carries both. And the *reading* the same session settled is this subclause's largest statement in a long while: the clause's floor is one whole device pixel and belongs to the **aliased** algorithm — all three C references produce exactly that with anti-aliasing off — while for the anti-aliased algorithm §10.7.1's NOTE permits instead, the clause states no floor at all beyond \"no shape ever disappears\". The four references floor at four different device-pixel widths, and `hayro`'s source calls its 1.0 \"as required by the PDF specification\" while exempting text, patterns and Type 3 glyphs from it. **The dot came back in the five-hundred-and-eighty-fifth, and the diagnosis above was half right.** The alpha did survive: at 0.1 of a device pixel it is 2.55 levels. What took the mark is the sentence this row opens with — the same dot drawn on a pixel's *centre* lands with the geometry's own ink and on a pixel's *corner* lands with none, because a circle one device pixel across covers `pi/4` of a pixel at the first and `pi/16` at the second, and a quarter of 2.55 levels rounds away. So the loss is placement rather than arithmetic, which is what this clause's second sentence is about, and the answer is the one shape whose coverage is not a fraction: `pdf_render::point_mark` states such a mark as the pixel this clause identifies by flooring — \"let i = floor( x ) and j = floor( y ). The pixel that contains this point is the one identified as ( i, j )\" — at the coverage the mark's own area implies, as soon as the widened form's ink can reach no level in any of the at most nine pixels it can be divided between. **It is not §10.7.5's promotion**: the ink is the mark's own area, a fiftieth of a pixel where that is what the mark covers, and the pixel is the one the mark's own centre is in, so no coordinate is moved to a grid line — what is given up is the mark's shape *within* one pixel, which an eight-bit raster cannot hold. Total ink is the mark's own area on both sides of the boundary, so the two constructions differ about where the ink is and not about how much. All three rasterisers take it and all three needed it: the processor and quorra drew nothing at 0.1 and below, and vello drew half the area at 0.2 and nothing under it. Unwitnessed like the floor above — the thinnest dot over 1242 first pages is 0.36 of a device pixel. ADR 0420."

[[clause]]
clause = "10.7.5"
title = "Automatic stroke adjustment"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/paint.rs"]
test = ["crates/render-cpu/tests/stroke_width.rs::stroke_adjustment_promotes_only_a_sub_half_pixel_line", "crates/pdf-model/tests/colour_paths.rs::stroke_adjustment_is_read_and_restored", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_the_thinnest_line_the_device_draws"]
note = "The clause has two requirements and the nineteenth session implemented the one a display can state exactly: \"If stroke adjustment is enabled and the requested line width, transformed into device space, is less than half a pixel, the stroke shall be rendered as a single-pixel line.\" `/SA` is read from Table 57 — **this row said Table 58 for the whole of its life, which is the path construction operators; the graphics state parameter dictionary is Table 57, as §8.4.5's own row has always said, and the three-hundred-and-eighty-ninth session's ninth-sweep check found it** — and carried on the `Stroke`, and `Stroke::device_width` makes such a stroke one device pixel — the same width the clause's NOTE says a zero width means, which is why both rules live in one function that both backends call. 49 corpus documents set the parameter true. Not implemented: the first requirement, that \"the line width and the coordinates of a stroke shall automatically be adjusted as necessary to produce lines of uniform thickness … no more than half a pixel different\". That is a grid-fitting of the *coordinates*, and the non-uniformity it exists to remove is an artefact of the binary scan conversion §10.7.4 describes — Figure 70 shows an aliased line — which this tree already departs from by anti-aliasing. An anti-aliased stroke's coverage-weighted thickness is the requested width at every position, so the clause's stated purpose is met by a different route; what is not met is uniformity of the *rasterised* line on a device that quantises coverage. That is a departure of the same family as §10.7.4's three, licensed by §10.7.1's NOTE, and nothing reports it because there is no page on which this device could do better. ADR 0028. **The boundary against §10.7.4 was drawn in the three-hundred-and-sixty-eighth session, when a degenerate fill's mark started snapping to the pixel grid, and it is a reading of the two clauses rather than a preference.** A stroke has a width the document stated, and moving its coordinates onto the grid is this clause's first requirement, which the sentence above makes conditional on `/SA` — so doing it unasked would turn `AMBIGUOUS_STROKE_ADJUSTMENT`'s derivation on `bug1743245.pdf` into a coincidence. A fill collapsed along one axis has no width at all: every pixel of its mark is this processor's construction under §10.7.4, which states which pixels are covered with no condition attached, and which in the next sentence exempts the zero-width *stroke* from that rule — \"Zero-width strokes may be done in an implementation-defined manner that may include fewer pixels than the rule implies.\" The fill's placement is stated by the standard; the stroke's is left to the implementation and then made conditional. ADR 0208. **And the boundary was tested again in the three-hundred-and-eighty-ninth, from the other side.** That session converts a *stroke* under a device pixel wide, on a path of straight axis-aligned rules, into the fill of its own outline, so that §10.7.4's coverage rule reaches it — and this clause is untouched by that, because the outline is the rectangle the document's own width and coordinates state. Nothing is snapped: a 0.1-unit rule draws 0.1 of a row at the fractional position the document put it, where this clause's first requirement would have moved the coordinates and its second, under `/SA`, would have made it a whole pixel. **Any proposal to snap something to the grid still has to say why it is not this**, and that one does not have to, because it snaps nothing. ADR 0226. **The four-hundred-and-thirty-second session extended that answer to a rule that is not axis-aligned and it still snaps nothing** (ADR 0268): the substitute is one device pixel wide and stays centred on the path at the fractional position the document put it, and where this clause's second requirement would multiply the *ink* by `one_pixel / w`, that rule divides the paint's alpha by the same factor instead. The two are opposite operations rather than neighbouring ones, which is what keeps `AMBIGUOUS_STROKE_ADJUSTMENT`'s reading of `bug1743245.pdf` a derivation. That session also measured what this clause's own NOTE describes — \"This is the thinnest line that can be rendered at device resolution\" — being drawn *wrongly* by the library at exactly that width: `tiny-skia` chooses its hairline for a stroke up to and including one device pixel, and its hairline paints the line's longer device extent rather than its length, so a 45° rule one pixel wide carries 141.42 of its own 200 where the fill of the same outline carries 177.44. Not paid, because it is neither a disappearance nor a sub-pixel shape and because `Stroke::device_width` makes a `0 w` stroke — which §10.7.4 exempts by name — indistinguishable from it at the rasteriser. `doc/todo/11`."

[[clause]]
clause = "10.8"
title = "Rendering for separations"
status = "inapplicable"
note = "Separations are one output per device colourant, \"usually as bitmaps\", for a press. See §10.8.3 for the one part of this subclause that could apply to a screen."

[[clause]]
clause = "10.8.1"
title = "General"
status = "inapplicable"
note = "\"Whether separations are produced is up to the processing software\", and this one produces a screen."

[[clause]]
clause = "10.8.2"
title = "Separations"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The clause states this tree's behaviour as the expected one: \"Alternate colour spaces are supplied for DeviceN and Separation colour spaces so that files prepared for generation of separations can be displayed on other devices and, in many cases, the overprint controls are ignored.\" Both halves are what happens here — the alternate space and its tint transform are evaluated, and overprinting is ignored on the argument ADR 0028 makes from §8.6.7. The clause's own worked example, cyan then yellow overprinting to green on a press and staying yellow on a screen, is the picture this device draws."

[[clause]]
clause = "10.8.3"
title = "Separation simulation"
status = "reported"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::the_requirements_this_program_cannot_meet_are_named", "crates/pdf-model/src/requirements.rs::no_reason_claims_a_clause_is_unread"]
note = "Separation simulation — rendering a page as a press would, by making separations, converting each to flat XYZ against a white matte, multiply-blending them and converting the result to the device's space. Not implemented and not silent. **The reason this row gave was wrong, and this tree's own code is what disproves it**: it said the simulation is one \"a processor performs when asked to, and nothing in a PDF asks for it\", and Table 275's `SeparationSimulation` requirement type asks for exactly this: \"Requires support for simulation separations as described in 10.8\", and the sentence goes on to name this subclause. It is a type §12.11's reader has carried by name for as long as it has existed, citing *this* clause in its own doc comment. So the report has a real condition and a real population: a document stating that requirement is told by name that this program cannot meet it (`requirements::unmet`), and the row now names the code and the tests that hold it, having named neither, which is what let the claim stand. **What §10.8.3 itself requires is nothing.** Its verb is a permission — a simulation \"can be performed\" — and its four steps are a `should` conditional on performing one, over §10.8.1's \"Whether separations are produced is up to the processing software\". So the debt is owed to Table 275 rather than to this clause, and what is missing is the control: the clause's own condition is \"if it is important … to more closely match those produced when using separations\", which is a user's request rather than a document's, and this viewer has none. Recorded so that the day one exists, the algorithm is known to be stated."

[[clause]]
clause = "11.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency.rs::an_object_drawn_without_a_constant_is_opaque"]
note = "Introductory. Its one statement with a consequence here is that the transparent imaging model is an *extension* of the opaque one, so a page that uses none of clause 11 draws exactly as it did before — which is why alpha, blend mode and soft mask are graphics-state parameters with opaque defaults rather than a second pipeline."

[[clause]]
clause = "11.2"
title = "Overview of transparency"
status = "implemented"
code = ["crates/pdf-render/src/lib.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_group_is_composited_once_rather_than_object_by_object"]
note = "The model's vocabulary — object, group, backdrop, shape, opacity, transparency stack — and this tree's display list is that stack: a flat sequence with `Command::Group` as the one nested command (ADR 0026). Alpha is one channel per pixel rather than shape and opacity separately, which §11.2 permits and ADR 0027 argues from `/AIS`: with one number, shape and opacity multiply to the same number."

[[clause]]
clause = "11.3"
title = "Basic compositing computations"
status = "implemented"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency.rs::a_blend_mode_array_takes_the_first_name_this_reader_knows"]
note = "The compositing arithmetic itself, which both rasterisers perform. See the subclauses; the load-bearing rows are §11.3.4's blending colour space and §11.3.5's sixteen modes."

[[clause]]
clause = "11.3.1"
title = "General"
status = "implemented"
code = ["crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency.rs::an_object_drawn_without_a_constant_is_opaque"]
note = "Introductory to the formulas. Its framing — that compositing is defined for one object against one backdrop and then applied recursively — is why `Command::Group` needs no more than a nested command list."

[[clause]]
clause = "11.3.2"
title = "Basic notation for compositing computations"
status = "inapplicable"
note = "Notation: the names the clause's formulas use for the backdrop, source and result colours and alphas. Nothing to implement; the formulas that use it are §11.3.3 and §11.3.5."

[[clause]]
clause = "11.3.3"
title = "Basic compositing formula"
status = "implemented"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency.rs::an_object_drawn_without_a_constant_is_opaque", "crates/render-cpu/tests/soft_mask.rs::a_constant_mask_interpolates_between_the_object_and_the_backdrop"]
note = "The union of the backdrop and source alphas, and the weighted blend between the backdrop colour and the blend function's result. Performed by `tiny-skia` and by Vello rather than here, and held to the clause by the closed-form check in `render-cpu/tests/soft_mask.rs` — trap 12's lesson, that the way to judge a composite is to write the clause's arithmetic down rather than to compare with a renderer."

[[clause]]
clause = "11.3.4"
title = "Blending colour space"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_group_resets_the_alpha_constants_and_the_blend_mode_for_its_elements", "crates/render-cpu/tests/four_component_page.rs::a_four_component_page_composites_in_ink_and_converts_once"]
note = "The blending colour space is the device's three components **except where §11.4.7's page group states four**, which is drawn rather than reported since the four-hundred-and-twenty-sixth session (ADR 0262). **This row opened with \"always\" for thirteen rounds after that stopped being true**, while its own last two sentences described the construction that replaced it — a row contradicting itself three sentences later, which is the ledger's own disease and which the four-hundred-and-thirty-ninth found by reading this row against the code it names. **Which space a file asks for is read as of the four-hundred-and-fifteenth session and it is not the entry this row used to name**: §11.4.7's page group is the root and §11.6.6 inherits from it, taking a group's own `/CS` only where the group is isolated — so a non-isolated group's `/DeviceCMYK` asks for nothing and a page group's decides every mark on the page. **7 corpus documents** were reported against the 4 this row said, one of them §11.6.6's isolated group and six §11.4.7's page group, all `/DeviceCMYK`; `issue14200.pdf` left because its group states no `/I` (ADR 0251). **Two are reported now** — six of the seven are drawn in the four components they state, the last three of them in the four-hundred-and-fortieth (ADR 0276). They were *reported* rather than blended in the wrong space until then, on ADR 0251's conclusion that a second raster format was what honouring them means. **That conclusion is withdrawn and the measurement under it stands**: §11.3.3 under `Normal` is a weighted average, so the two orders of operation agree exactly when the conversion out of the blending space is affine, and this tree's `DeviceCMYK` conversion is multilinear over the ink cube (ADRs 0009, 0042). Up to 48 of 255 apart over 300 000 random pairs, 51.5 at half of registration black over paper. The clause's own requirement that the space be \"a device or CIE-based colour space\" and not a special one is satisfied by construction. **The per-component sentence is what the four-hundred-and-twenty-sixth session built on**, so a four-component blending space fits in two three-channel rasters (§11.4.7, ADR 0262). The complement rule — \"the colour component values shall be complemented (subtracted from 1.0) before the blend function is applied and the results of the function shall then be complemented back before being used\" — is met by construction there, because both rasters *hold* the complements. **And §11.3.5.3's four modes ride the same pair since the four-hundred-and-forty-first** (ADR 0277): its CMYK rule complements C, M and Y and applies the subclause's formulas to them, which is the chromatic raster's own contents, and the separate rule it gives the black component is what those same formulas return on the neutral colour the black raster holds. Nothing is mapped between the two lists."

[[clause]]
clause = "11.3.5"
title = "Blend mode"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/lib.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/lib.rs", "crates/render-quorra/src/scene.rs"]
test = ["crates/pdf-model/tests/transparency.rs::a_blend_mode_array_takes_the_first_name_this_reader_knows"]
note = "All sixteen blend modes reach all three backends — `cpu_and_gpu_agree_on_every_blend_mode` and `cpu_and_quorra_agree_on_all_sixteen_blend_modes` are the two comparisons, and **this row said \"both\" for as long as there have been three** — and §11.6.3's rule for choosing among an array of names is implemented — see §11.3.5.2 and §11.3.5.3 for the two families. The clause's own framing makes the twelve separable ones a rasteriser's parameter, since each is a function of one backdrop and one source component; the four of §11.3.5.3 are not, and §11.3.5.3's row is where the CPU backend's own compositing pass for them is recorded. **And the corpus has one page that is nothing but this clause**: `blendmode.pdf`, sixteen labelled swatches the pdf.js manifest calls \"[e]very blend mode that PDF supports\". At eight times the resolution, where scan conversion is negligible, our page differs from `mupdf`'s by 0.53 of 255 per pixel where `mupdf` and `poppler` differ from each other by 0.67, and a four-by-eight grid of tile means spreads that residual over every swatch in proportion to its ink with none of the sixteen an outlier — so the two families agree with two independent implementations to inside those implementations' own spread. The page is `AMBIGUOUS_IMAGE_REDUCTION`'s fifteenth member, because what the oracle sees at the page's own scale is thirty-two photographs reduced by 0.8 and not a mode (three-hundred-and-seventeenth session). **And this clause bounds §11.4.6's staged pair**: a mark or group carrying a blend mode is composited through the implicit one-element group this clause states, which is the step the pair replaces rather than joins, so quorra refuses a staged operator there and `pdf-model`'s shape half drops the blend mode for the same reason — §11.4.6 leaves a knockout element nothing to blend against. `quorra_states_what_it_will_not_stage` holds it (ADR 0291)."

[[clause]]
clause = "11.3.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency.rs::a_blend_mode_array_takes_the_first_name_this_reader_knows"]
note = "The two families and the rule for naming one. §11.6.3's array form is implemented — the first name this reader knows wins — and a name it does not know is `Normal`, which is what the clause requires of an unrecognised name."

[[clause]]
clause = "11.3.5.2"
title = "Separable blend modes"
status = "implemented"
code = ["crates/pdf-render/src/lib.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency.rs::a_blend_mode_array_takes_the_first_name_this_reader_knows"]
note = "Table 134's twelve separable modes, each a function `B(cb, cs)` applied per component. One of them is load-bearing outside transparency, in two places: §12.5.6.10's highlight is a wash that must leave the text under it readable, and `Multiply` is the mode whose \"result colour is always at least as dark as either of the two constituent colours\" — the standard's own guarantee that what was under the wash survives it (ADR 0043). **The host's own selection wash is the second** and is not a clause at all: `viewer-ui` draws `Query::Selection`'s quads under the same mode for the same reason, and since the two-hundred-and-fifty-second session under *one* layer, because a backend that gives every non-`Over` blend its own compositor layer prices a fill per quad at 6.4 MB of frame budget each (ADR 0176). **And one place a separable mode is *not* applied per component**, since the three-hundred-and-eighty-third session named it: inside a `/Luminosity` mask group blending in `DeviceCMYK`, where §11.5.3 lets the four components be composited as one weighted average of them \"expressed in additive form\" — exact for `Normal`, which is affine, and for nothing else. Reported by name under §11.5.3, with no corpus document stating one."

[[clause]]
clause = "11.3.5.3"
title = "Non-separable blend modes"
status = "implemented"
code = ["crates/render-cpu/src/blend.rs", "crates/render-gpu/src/lib.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_every_blend_mode", "crates/render-cpu/src/blend.rs::hue_of_red_over_blue_is_the_clauses_arithmetic"]
note = "Table 135's four non-separable modes — Hue, Saturation, Color, Luminosity — written from the clause's `Lum`, `ClipColor`, `SetLum` and `SetSat` in `render-cpu/src/blend.rs`, because three of the four were wrong when handed to `tiny-skia`: the thirty-seventh session's cross-backend scene found `Hue`, `Color` and `Luminosity` 113 of 255 from Vello, and the clause settled which was right rather than leaving a tie. Red over blue in `Hue` is `SetLum(SetSat(Cs, Sat(Cb)), Lum(Cb))`, which after `ClipColor` is 0.367; `tiny-skia` produced 0.81, the value before the clip. All sixteen modes now agree between the backends to the channel, which is two independent implementations of the clause's own arithmetic rather than one checked against itself — the functions stay in `render-cpu` for exactly that reason (ADR 0047). `ClipColor` is defined only where `Lum(C)` is itself in range, which the clause supplies by reaching it solely through `SetLum`. **The CMYK rule is read as of the four-hundred-and-twenty-sixth session and was called the one case §11.4.7's two rasters cannot carry; the four-hundred-and-forty-first drew it and the reason the row gave was wrong** (ADR 0277). The clause splits a subtractive space's four components along the line the two rasters already are. The chromatic three are the first bullet — \"[t]he C , M and Y components shall be converted to their complementary R , G and B components by subtracting each from 1.0. The formulae in this subclause shall be applied to the RGB colour values\" — which is what that raster *holds*, so the four functions see the R, G and B this clause names. The fourth is \"For the K component, the result shall be the K component of Cb for the Hue , Saturation , and Color blend modes; it shall be the K component of Cs for the Luminosity blend mode\", **and that is not a second blend function**: the black raster is neutral in all three of its channels, `Sat` of a neutral colour is 0, `SetSat(C, 0)` takes the clause's own `else` arm, and `SetLum` on a neutral colour is the neutral colour of the luminosity it is given — so `Hue`, `Saturation` and `Color` come to `Cb` and `Luminosity` to `Cs`, term for term. Checked over 200 000 neutral pairs at a worst gap of **1.19 x 10^-7**, one ulp, which is the residue of the three `Lum` weights not summing to exactly one in binary floating point. So a backend that implements this clause for three components implements the black component's rule with it, and **nothing is mapped in either list**: no display-list vocabulary, no backend arm, no refusal. `issue18032.pdf` and 31 of 65 944 web documents stopped being reported for it; the corpus page's picture moves only inside its gradient, and `test_scenes::four_component_page`'s fifth mark holds all three backends to (12, 88, 90)."

[[clause]]
clause = "11.3.6"
title = "Interpretation of alpha"
status = "partial"
code = ["crates/pdf-render/src/backend.rs", "crates/render-cpu/src/lib.rs", "crates/viewer-ui/src/software.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_transparency_group", "crates/viewer-ui/src/software.rs::a_translucent_overlay_mixes_and_stays_opaque", "crates/render-cpu/tests/soft_mask.rs::the_weighted_average_is_exact_at_every_mask_value"]
note = "The weighted average of backdrop, source and blended colour that both backends evaluate through their rasterisers, and the sentence that decides three things this session leaned on: \"An alpha value of αs = 0.0 or αb = 0.0 results in no blend mode effect\". That is why §11.4.7's transparent page group changes every blend mode over an unpainted area — the backdrop alpha is zero there, so the blend collapses to the source colour — and why an element's blend mode has no effect inside an isolated knockout group (§11.4.6). NOTE 2's undefined colour at zero result alpha is what a premultiplied raster represents naturally. For the twelve separable modes the formula is tiny-skia's and Vello's rather than this tree's, and what is implemented here is the choice of what they composite onto; for §11.3.5.3's four it is `render-cpu/src/blend.rs`, which evaluates this clause's weighted average itself with the premultiplication folded in (ADR 0047). `partial` for the first half. **A third evaluator arrived in the three-hundred-and-eighty-fourth session** and it is a host's rather than a document's: `viewer_ui::software` composites this window's own chrome over a page raster when there is no graphics device to do it (`--cpu`, ADR 0221), which is this clause's formula for the Normal mode with straight alpha, including the two sentences that decide the edges — \"[t]he result colour shall be normalised by the result alpha\" and NOTE 2's undefined colour where that alpha is zero. **Read and kept in the five-hundred-and-forty-fifth**: the three evaluators are still three — the two rasterisers, `render-cpu/src/blend.rs` for §11.3.5.3's four, and `viewer_ui::software` for the chrome — and a fourth would be a backend rather than a formula. **And in the five-hundred-and-eighty-third the sentence \"the formula is tiny-skia's\" turned out to be one library and two answers** (ADR 0418): `tiny-skia` compiles a low-precision pipeline whenever every stage of a draw has one — a solid colour through a mask always does — and its division by 255, `(v + 255) >> 8`, is an upper bound on `v ÷ 255` rather than its rounding, spent twice per pixel and biased the same way both times. So this clause's weighted average came out up to **two levels of 255** towards the backdrop on every masked or partly transparent mark. `render-cpu`'s `HIGH_PRECISION_PIPELINE` asks for the floating-point pipeline instead, which reproduces the closed form exactly at all 256 mask values — `render-cpu/tests/soft_mask.rs::the_weighted_average_is_exact_at_every_mask_value` sweeps them with no slack — and it costs nothing measurable, two of three pages being cheaper under `examples/callgrind_rasterise`. One oracle page left the contradicted list by it and none arrived. **What choosing between a library's two evaluators is not, is implementing the formula**, so the status does not move: the first half is still theirs."

[[clause]]
clause = "11.3.7"
title = "Shape and opacity computations"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/transparency.rs", "crates/pdf-model/tests/image_masks.rs"]
note = "Aggregate of the three below, read as a family in the fifteenth session because an image's masks are where shape and opacity meet in this tree. Alpha is the product of shape and opacity, which is what `image::combine_on_the_finer_grid` computes when a stencil's shape multiplies a soft mask's opacity, and what a `Color`'s alpha carries everywhere else. **This note ended \"[w]hat is absent is the distinction\" until the four-hundred-and-forty-second session, and the distinction is stated rather than absent**: since ADR 0234 the display list carries a knockout element's shape as a second command, `pdf_render::Command::Shaped`, so §11.4.6's two stages are drawn as Destination-Out with the shape and then Plus with the object. What is absent is a shape *channel*: one alpha per pixel still carries the product everywhere else, which is right wherever the two are only ever multiplied and is what keeps §11.3.7.2 and §11.3.7.3 `partial` — the two elements whose one alpha carries both quantities at once are an image's samples and a translucent shading, and a knockout group holding either is reported."

[[clause]]
clause = "11.3.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_soft_mask_finer_than_its_image_keeps_its_own_resolution", "crates/pdf-model/tests/image_masks.rs::a_matte_colour_is_undone_before_the_image_is_drawn", "crates/pdf-model/tests/image_masks.rs::an_explicit_mask_paints_the_base_image_only_where_it_marks"]
note = "One sentence, and it is the subclause's only requirement: \"the alpha values that control the compositing process shall be defined as the product of shape and opacity\". `image::combine_on_the_finer_grid` computes that product literally — the image's own alpha times the mask sample, rounded rather than truncated so that an opaque pixel under a fully opaque mask stays opaque — and structurally elsewhere, since a paint carries one alpha and every source of it multiplies in. The clause's NOTE about anti-aliased edges being *shape* rather than opacity is the rasterisers' business and both treat coverage as shape. **Was `partial` from the fifteenth session to the four-hundred-and-thirty-seventh above a note that named nothing owed**, which is what the fourteenth sweep prints (`doc/todo/01`); what it was carrying is the family's debt rather than its own, and the family's debt is stated where it lives: §11.3.7.3 keeps the `partial` for the two elements whose one alpha carries shape and opacity at once. Separating them is that subclause's requirement and not this one's."

[[clause]]
clause = "11.3.7.2"
title = "Source shape and opacity"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/transparency.rs", "crates/pdf-model/tests/image_masks.rs"]
note = "Six sources, three of shape and three of opacity. Object shape is what filling an outline does, and an explicit mask's \"1.0 in the unmasked portions\" is applied as of the fourteenth session (§8.9.6.3); mask opacity is an image's own `/SMask` (§11.6.5.2) and is reported for the graphics state's (§11.6.5.1); both constants are `ca` and `CA`. Missing: the shape of a *group*, which is the union of what it contains, and `/AIS`, which decides whether a mask and the constants are read as shape or as opacity. **The first of those used to say \"needs §11.4.6\", and that reason expired in the seventy-first session** when knockout groups were drawn — three sessions after this note was written, and unnoticed until a sweep for expired reasons in the hundred-and-fifty-first. What §11.4.6 needed was a shape *where the shape is the coverage*, which `tiny-skia` and Vello both supply through the same mask a fill uses; and a group's shape as a value in its own right is now carried beside the object wherever §11.4.6 needs it: `pdf_render::Command::Shaped` states a knockout element's shape as a second command, and a group's is the union of its elements' — which is what drawing their shapes onto transparency accumulates (ADR 0234). What is left is the general case, a shape channel every command carries, which nothing yet asks for — and it is what a *group* used as a knockout element still wants where the group is non-isolated, because this clause makes a group object's opacity \"the result of the opacity computations for all of the objects it contains\" rather than 1.0 and a non-isolated one accumulates its backdrop's alpha besides. `/AIS` was the second debt and is **honoured** as of the five-hundred-and-eightieth session, having been merely *read* since the three-hundred-and-ninety-seventh: under `/AIS true` every one of this clause's three opacity inputs is 1.0, so the source opacity is 1.0 and an element's drawn alpha is its source shape — see §11.6.4.3. ADRs 0093, 0107, 0108's class, 0234. **Read and kept in the five-hundred-and-sixty-second**, and the evidence is the clause's own bullets: all six sources it lists are here, the group-shape bullet's \"union … of the shapes of the objects it contains\" is what drawing elements' shapes onto transparency accumulates, and the two debts named are real — a shape channel on every command, which nothing asks for, and `/AIS`. One precision: `/AIS` is Table 57's and this clause never names it, so the second debt is borrowed from §11.6.4.3 and §11.6.4.4 rather than stated here; it is kept because this clause is where the shape/opacity *distinction* is drawn and the flag is what decides which a value is. **And this clause is what settled the flag in the five-hundred-and-eightieth**: the derivation that honours it is three sentences of §11.6.4.2, §11.6.4.3 and §11.6.4.4 multiplied together by *this* clause's \"[t]he three opacity inputs shall be multiplied together, producing an intermediate value called the source opacity\" (ADR 0415)."

[[clause]]
clause = "11.3.7.3"
title = "Result shape and opacity"
status = "partial"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_transparency_group", "crates/render-quorra/tests/abutting_marks.rs::two_abutting_marks_leave_no_more_than_the_union", "crates/render-quorra/tests/abutting_marks.rs::many_abutting_marks_stay_under_the_union_s_own_limit"]
note = "The union formulas for the shape and opacity a compositing step leaves behind. The alpha half is what every raster here carries, and a transparency group's result is now that union rather than nothing (§11.4.1). The separation is still not a *channel*: this renderer holds one alpha per pixel, the product of shape and opacity, where the clause keeps two. §11.4.6 names the one construction that needs them apart — \"The separate shape value shall be computed in any group that is subsequently used as an element of a knockout group\" — and since the three-hundred-and-ninety-seventh that sentence is obeyed by *stating* rather than by storing: the display list carries a knockout element's shape as a second command, a group's shape included, and each backend draws the pair (ADR 0234). What keeps this `partial` is the two elements whose one alpha carries both quantities at once, an image's samples and a translucent shading, which are reported. **The union is also what a page's *seams* are made of, measured in the four-hundred-and-seventy-third session** (ADR 0308): two anti-aliased marks that abut inside a device pixel unite to less than the whole of it, so a quarter of the backdrop survives between them and `0.75^n` survives *n* equal shares, rising towards `1/e`. That is arithmetic the clause states rather than a defect, and `abutting_marks.rs` gates it as an **upper** bound on all three rasterisers — 0.2510 on the processor, 0.2471 on `render-quorra` and 0.2510 on `render-gpu` against the union's 0.2500 — together with §10.7.4's control that one statement of the same region leaves nothing. `doc/todo/11` item 5 carries what a rasteriser that left nothing here would cost."

[[clause]]
clause = "11.3.8"
title = "Summary of basic compositing computations"
status = "implemented"
code = ["crates/render-cpu/src/lib.rs"]
test = ["crates/render-cpu/tests/soft_mask.rs::a_constant_mask_interpolates_between_the_object_and_the_backdrop"]
note = "A summary of §11.3.3 through §11.3.7, restating them with the group cases folded in. Nothing new is required of a reader; the rows above are where each of its lines is answered."

[[clause]]
clause = "11.4"
title = "Transparency groups"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/display_list.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs"]
note = "Aggregate of the eight below, read as a family in the seventeenth session with transparency groups as the demand item. Three of Table 145's four answers now reach a backend. A group's elements are composited onto a transparent backdrop and the result painted once under the constant alpha and blend mode in force at Do — §11.4.5's isolated group — **except where §11.4.4's NOTE 5 says no group is needed** (non-isolated, Normal, alpha 1.0 and no mask: the elements are drawn inline, which is both correct and less work) **and except where the group is non-isolated and an element blends**, where since the four-hundredth session the display list says so and `render-cpu` builds the group on a copy of the page (ADR 0237). §11.4.7's page group is implemented and was the seventeenth session's largest finding. Knockout (§11.4.6) is **drawn**, including where an element's shape is not the coverage it is drawn with — the display list states the two apart (ADR 0234). What is reported is what none of the three constructions reaches: a knockout group whose elements blend or whose shape one alpha channel carries with its opacity, and a non-isolated group that is one or is inside one. Corpus: 6 documents reported §11.4.4 in the three-hundred-and-ninety-ninth session and 3 do now. **Read and kept in the five-hundred-and-sixty-second**: the eight rows below are §11.4.1 to §11.4.8 and their statuses still say what this note says — §11.4.5 `implemented`, §11.4.2 `inapplicable`, and §11.4.4, §11.4.6 and §11.4.7 each `partial` for a construction named in its own note — so the aggregate holds and the reason it is `partial` is the children's rather than a debt of the clause's own prose."

[[clause]]
clause = "11.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/display_list.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_group_is_composited_once_rather_than_object_by_object"]
note = "A group is \"collected together and composited to produce a single colour, shape, and opacity at each point\" and the result treated as one object, which is what Command::Group asks a backend for; its elements are \"a separate transparency stack\" composited against §11.4.3's initial backdrop, and the result is \"an object whose shape is the union of the shapes of its constituent objects\". **All four of the further attributes this clause lists are here now.** The fourth is a group's result \"used as a source of shape or opacity values for creating a soft mask\", and this note said it was \"still reported rather than built\" until the four-hundred-and-forty-second session — `pdf_render::soft_mask` evaluates the group and both of §11.5's derivations since the eighteenth session (ADR 0027), which §11.6.4.3's row has said all along. `partial` for the one thing the clause's second bullet names and this tree reports: an isolated group \"may specify its own blending colour space\" independent of its backdrop's, and a *painted* group that introduces one is reported rather than composited in it — the argument, the measurement and the count are §11.6.6's."

[[clause]]
clause = "11.4.2"
title = "Notation for group compositing computations"
status = "inapplicable"
note = "Notation, and Table 138's renaming of §11.3's variables so that §11.4.4 can state its recurrences. There is no requirement to implement; marking it implemented would claim code for a clause that asks for none, which is the reading §11.6.1 already has."

[[clause]]
clause = "11.4.3"
title = "Group structure and nomenclature"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_group_is_composited_once_rather_than_object_by_object"]
note = "The three backdrops this clause distinguishes. The group backdrop is what is already on the page; the immediate backdrop is what a group's own buffer accumulates, and is right because the elements are drawn into that buffer in order. The **initial** backdrop is now both of the clause's answers rather than one: transparent for an isolated group, and a copy of the group backdrop for a non-isolated one whose elements blend, which `render-cpu` builds in `initial_backdrop` (ADR 0237). Its last requirement — \"A PDF processor shall ensure that the backdrop's contribution to the overall result is applied only once\" — used to hold by construction because a transparent initial backdrop has nothing to remove; where the backdrop is now genuinely there, it holds by §11.4.4's arithmetic instead: the removal cancels against §11.3.3's re-compositing and what is left interpolates between the two. `partial` for the groups §11.4.4 still reports. **Read and kept in the five-hundred-and-sixty-second**: §11.4.4 is still `partial` and still names those groups, so the deferral is live rather than expired, and `render-cpu`'s `initial_backdrop` is still the function that answers the clause's second case."

[[clause]]
clause = "11.4.4"
title = "Group compositing computations"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs", "crates/render-quorra/src/scene.rs"]
test = ["crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_a_non_isolated_group", "crates/render-quorra/tests/headless_quorra.rs::quorra_refuses_a_non_isolated_group_that_blends", "crates/pdf-model/tests/transparency_groups.rs::a_non_isolated_group_blends_with_the_page_behind_it", "crates/pdf-model/tests/transparency_groups.rs::a_non_isolated_group_reports_only_where_the_backdrop_cannot_be_stated", "crates/pdf-model/tests/transparency_groups.rs::a_non_isolated_groups_element_blends_with_the_backdrop_the_group_is_painted_over", "crates/pdf-model/tests/transparency_groups.rs::a_non_isolated_group_inside_another_keeps_the_backdrop_alpha_it_composites_onto"]
note = "The recurrence for a non-isolated, non-knockout group, and NOTE 3's backdrop removal at the end of it — `C = Cn + (Cn − C0) × (α0/αgn − α0)`. Three of the clause's own notes decide what a renderer builds. NOTE 3: with every element blending Normal the removal is exact and the backdrop cancels, so the isolated computation every rasteriser performs natively is this one — §11.6.7's NOTE 1 states that equivalence for the same formulas applied to a pattern cell, and it is why nothing here fires unless an element blends. **NOTE 5 states when the group need not be built at all**: \"the effect of compositing objects as a group is the same as that of compositing them separately (without grouping)\" where the group is non-isolated with its parent's knockout attribute and its result is composited with the Normal blend mode and shape and opacity of 1.0. Such a group's elements are emitted inline as of the hundred-and-third session, which took the corpus's incomplete count 89 → 86. **NOTE 4 is the one the four-hundredth session answered**, and by contradicting the conclusion drawn from it: it advises \"maintaining two sets of variables to hold the accumulated values\", Table 140's group alpha apart from the composite alpha, and this project had recorded twice that one raster therefore cannot do it. It can. The quantity NOTE 3's removal divides by is multiplied straight back in when §11.3.3 composites the group's result onto the same backdrop, so with the Normal blend function at the `Do` the pair collapses, exactly and for every backdrop alpha, to `result = (1 − w) × backdrop + w × E(backdrop)` — `w` the constant alpha times the soft mask, `E(backdrop)` the elements drawn onto a copy of it. `render-cpu` builds exactly that (ADR 0237), and **`render-quorra` builds it too since the four-hundred-and-thirty-eighth** (ADR 0274): `quorra_scene::GroupSpec` gained Table 145's `/I` and the three conditions it accepts are the three `pdf-model` restricts itself to, derived on both sides from this clause. **And this clause is why §11.4.6's staged pair is refused on a group that is not isolated**: the erase half is the same content drawn opaque, so the group's own alpha *is* its shape — and a non-isolated group's buffer starts as a copy of its backdrop, whose alpha would arrive inside that shape. `render-quorra` emits every staged half as an isolated group for that reason, and quorra refuses the alternative by name rather than drawing a plausible page (its ADR 0033; ADR 0291). `render-gpu` refuses it by name, because a Vello layer begins transparent and a scene cannot read what it has drawn. `partial` for the groups the collapse does not reach: a blend mode at the `Do`, a knockout group **whose rule can change a pixel**, and an element of one. That qualifier arrived in the four-hundred-and-seventy-second session (ADR 0307): §11.4.6's initial backdrop and this clause's immediate one are the same wherever no compositing element covers an earlier one, so such a group is this clause's group and takes this clause's construction, and `/K` alone had been forcing §11.4.5's substitute onto it. Corpus: 6 documents reported this in the three-hundred-and-ninety-ninth session and **1** did until the four-hundred-and-ninety-second — `issue18032.pdf`, which is the knockout group whose element genuinely blends against a backdrop that is not transparent, and which ADR 0327's construction now draws: the `partial` set is therefore a blend mode at the `Do` and an element of a knockout group, the middle member — the knockout group itself — having moved to §11.4.6's construction. `knockout_blend_multiply.pdf` is drawn on the backdrop this clause gives it and `knockout_inner_backdrop.pdf` was drawn on §11.4.6's NOTE 6 backdrop all along. §11.3.7.3 is the row for holding one alpha per pixel."

[[clause]]
clause = "11.4.5"
title = "Isolated groups"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_group_is_composited_once_rather_than_object_by_object", "crates/pdf-model/tests/transparency_groups.rs::a_non_isolated_group_blends_with_the_page_behind_it"]
note = "\"α0 = 0.0 if the group is isolated\" — a transparent buffer, which is what a tiny-skia pixmap and a Vello layer both start as (and a `quorra_scene::GroupSpec` with `isolated: true`, which since the four-hundred-and-thirty-eighth is the flag rather than the only thing it can be), and which since the four-hundredth session is a *choice* the display list states rather than the only backdrop it can name (`Command::Group`'s `isolated`, ADR 0237). The consequence the clause spends its length on follows from the flag: an element's blend mode inside an isolated group sees only what the group itself painted, and `a_non_isolated_group_blends_with_the_page_behind_it` is the test that holds the two apart — the same content in an isolated and a non-isolated group produces different pixels, which is what the flag means. Its closing sentence, about which rendering intent converts the group's colours into the backdrop's space, stopped having nothing to convert in the four-hundred-and-ninety-second session: an isolated group naming a sampleable four-component space composites in it and its pair resolves at the `Do` (§11.6.6's row, ADR 0327), with the conversion out on the same branch every other colour takes — §10.3's, black point compensation on — which is the graphics state's own `Default` answered the one way this processor answers it (§8.6.5.9). A space the pair cannot carry is still reported (§11.6.6)."

[[clause]]
clause = "11.4.6"
title = "Knockout groups"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/display_list.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs", "crates/render-quorra/src/scene.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_knockout_group_paints_only_its_topmost_element", "crates/pdf-model/tests/transparency_groups.rs::a_knockout_rule_that_can_show_nothing_leaves_the_group_the_backdrop_it_has", "crates/pdf-model/tests/transparency_groups.rs::a_group_inside_an_isolated_knockout_group_takes_the_transparency_note_6_gives_it", "crates/pdf-model/tests/transparency_groups.rs::a_knockout_group_reports_only_where_the_two_models_differ", "crates/pdf-model/tests/transparency_groups.rs::a_stated_shape_knocks_the_element_under_it_out_entirely", "crates/pdf-model/tests/transparency_groups.rs::the_object_is_added_to_the_backdrop_the_shape_left_behind", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_knockout_group", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_knockout_group_that_states_its_shape", "crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_a_knockout_element_that_states_its_shape", "crates/render-quorra/tests/headless_quorra.rs::quorra_states_what_it_will_not_stage", "crates/render-cpu/tests/group_constructions.rs::a_knockout_groups_element_composites_with_the_groups_own_backdrop", "crates/pdf-model/tests/transparency_groups.rs::a_knockout_groups_elements_blend_against_the_pages_own_backdrop", "crates/pdf-model/tests/transparency_groups.rs::alpha_is_shape_makes_the_drawn_alpha_the_knockout_shape", "crates/pdf-model/tests/transparency_groups.rs::alpha_is_shape_is_scoped_to_what_the_groups_content_painted_under", "crates/render-gpu/tests/headless_gpu.rs::the_gpu_refuses_a_knockout_group_on_its_own_backdrop", "crates/render-quorra/tests/headless_quorra.rs::quorra_refuses_a_knockout_group_on_its_own_backdrop"]
note = "Was this tree's first `silent` row, from the ninth session; `reported` from the seventeenth; drawn since the seventy-first, and drawn for the elements the clause says one alpha cannot carry since the three-hundred-and-ninety-seventh (ADR 0234). A knockout group's elements are composited with the group's initial backdrop rather than with each other, so at any point the topmost object wins. On the transparent initial backdrop of an isolated group the clause's two stages come to one line per pixel in premultiplied form — `P' = (1 - f) x P + S`, `f` the shape and `S` the object premultiplied by shape and opacity — and there are two ways to draw it. Where the shape *is* the coverage, that line is Porter-Duff Source modulated by coverage, which `render-cpu` states as a per-draw blend mode and `render-gpu` builds out of `Compose::DestOut` plus a paint because Vello's `Compose::Copy` runs over a layer's whole bounding box. Where it is not, `pdf_render::Command::Shaped` carries the object beside a second command whose drawn alpha *is* §11.6.4.2's shape — the object with §11.6.4.3's mask and §11.6.4.4's constant removed, and a group's shape being the union of its elements' — and both backends draw Destination-Out with the shape and then **Plus** with the object. **`render-quorra` draws both forms since the four-hundred-and-fifty-sixth session** (ADR 0291). A `quorra_scene::GroupSpec` states the knockout and the library composites each element with the group's initial backdrop, so an element whose shape is its coverage draws; a `Shaped` element states the two stages itself, as `Compose::DestOut` with the shape half and `Compose::Plus` with the object. **That took two releases to become writable and the history is the lesson rather than the dates**: the operators arrived in quorra's ADR 0025 at `89d7dd77` refused in the one position this tree emits them from, which the four-hundred-and-thirty-ninth session established by writing the translation out rather than by reading about it, and `doc/QUORRA_FEEDBACK.md` section 14.2 asked for two lifts — the pair inside a knockout group, and a compositing operator on a group at all, because §11.3.7.2 makes a nested group's shape \"the union … of the shapes of the objects it contains\" and no fill can state that. quorra's ADRs 0032 and 0033 are those two, at `2c9bdd0`. A half that is a group states the operator on its own `GroupSpec`; every other half is drawn inside a group of one element, which is the same arithmetic and is what a vocabulary carrying `Compose` on a fill and on a group leaves as the one uniform route. All four corpus pages that were refused for this now **agree with the CPU oracle** (`doc/todo/23`, ADRs 0275, 0291). The second operator is the clause rather than a detail: source-over would weight the backdrop by `1 - f x q` a second time, which is 32 of 255 at a half-covered pixel under a half-opaque mark. **The four-hundred-and-seventy-second session read the clause for which backdrop it hands each element and found this tree had one answer where the clause has three** (ADR 0307). First: the initial backdrop and the immediate one differ only after something has been composited into the group, so a group in which no compositing element covers an earlier one is §11.4.4's group term for term — `knockout_can_show` is that condition and it had decided only whether to *report*, while Table 145's `/K` alone forced §11.4.5's transparent backdrop onto the picture. `knockout_blend_multiply.pdf` is one non-isolated knockout group holding one Multiply element over a yellow page, which has nothing to knock out: the clause asks for §11.3.5.2's `(1,1,0) x (0,1,1)`, green, and a transparent backdrop under Multiply returns the source, cyan. It is drawn on §11.4.4's backdrop now and reports nothing. Second, **NOTE 6**: \"[w]hen a non-isolated group is nested within a knockout group, the initial backdrop of the inner group is the same as that of the outer group; it is not the immediate backdrop of the inner group\" — so where the enclosing knockout group is isolated the inner group *is* §11.4.5's isolated group by that clause's own definition, whatever its `/I` says, and drawing its elements on transparency is the clause rather than a substitution. `Interpreter::transparent_initial_backdrop` carries that, set for a knockout group's own content and cleared everywhere else because NOTE 6 reaches a direct element and not a descendant. `knockout_inner_backdrop.pdf` was drawn right and reported wrong for it, which is trap 11's shape exactly: the condition has to come from the clause, and \"non-isolated\" is not the clause's condition. `partial` for three things. An element whose alpha genuinely carries both quantities keeps the report and the report now names which: an image's samples, which may be §8.9.6.2's stencil or §11.6.5.2's `/SMask`, and a shading whose colours already carry the constant. A non-isolated knockout group whose elements blend *and* whose knockout rule can show keeps it too, because the initial backdrop is then the group's own — and **this used to add \"which is the same thing §11.4.4 lacks\", which stopped being true in the four-hundredth session**: §11.4.4's non-isolated group is drawn now (ADR 0237), on a buffer seeded from the page, and what keeps this clause from following is the *two-stage* form. §11.4.6 composites each element with that seeded backdrop and then takes a weighted average by the element's shape, where §11.4.4 composites the elements with each other and interpolates once at the end. And **§11.6.4.3's `/AIS` inverts which quantity is which**, which the five-hundred-and-eightieth session honoured rather than refused: under `/AIS true` §11.3.7.2's three opacity inputs are all 1.0, so an element's drawn alpha *is* its shape and the pair `Command::Shaped` states needs no new vocabulary and no backend change (`transparency::shape_the_alpha_already_is`; §11.6.4.3's row has the derivation and the populations). What is refused there instead is named: a group whose content painted under **both** readings, and a *non-isolated* group used as an element, whose accumulated alpha carries its backdrop's beside its own. The arithmetic of what is left is in ADR 0307 and it is a construction rather than a condition: with `B` the initial backdrop and `P` the accumulation, the Plus half's colour is the element blended against `B`, which a layer beginning transparent cannot produce, so it wants the initial backdrop retained beside the accumulation per element on three backends. Corpus: 5 documents reported this in the three-hundred-and-ninety-sixth session and 1 did until the four-hundred-and-ninety-second, `issue18032.pdf`, whose blocker was the non-isolated one; the four that left took five oracle pages from contradicted to agreeing. **The four-hundred-and-ninety-second built ADR 0307's construction on the oracle** (ADR 0327): `Command::Group` with `isolated: false` beside `knockout: true` states the group's own initial backdrop, every element arrives as a `Command::Shaped` so the weighted average's factor is per pixel, and `render-cpu` retains `B` beside the accumulation and composites each element against it in a scratch of its own — the identity `f × E = S − (1 − f) × B` recovers stage a)'s shape-1.0 composite from an ordinary draw, `blend::knockout_average` states it, and the final composite back onto the page is the same collapse ADR 0237 derived, unchanged by knockout. `render-gpu` and `render-quorra` refuse the combination by name — neither can retain a backdrop beside a layer's accumulation — and the frame goes to the oracle. Emitted only where the `Do`'s blend is Normal, no enclosing group is a knockout group, and every element's shape is statable; what stays `partial` is exactly the remainder: an element whose alpha genuinely carries both quantities under §11.6.4.3's *opacity* reading, a knockout group composited under a blend mode of its own, an element of another knockout group, a non-isolated group used as an element under the *shape* reading, and a group whose content painted under both readings. `/AIS` itself is **scoped to the group whose content stated it** rather than monotone across the page, because the entry is a graphics state parameter and a statement `Q` restored before a sibling's `Do` reaches no element of that sibling's group; `issue18032.pdf` states `/AIS true` inside a form whose group draws nothing, two forms before the knockout group the page-wide flag was refusing for it. **The same two callers §11.6.2 and §9.3.8 give this clause take the reading too** — `transparency::knockout_group_elements` — where they asked the page-wide flag before."

[[clause]]
clause = "11.4.7"
title = "Page group"
status = "partial"
code = ["crates/pdf-render/src/backend.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs", "crates/render-quorra/src/lib.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::vello_hands_back_straight_alpha", "crates/render-cpu/tests/four_component_page.rs::a_four_component_page_composites_in_ink_and_converts_once", "crates/render-cpu/tests/four_component_page.rs::converting_before_compositing_is_a_different_page", "crates/render-quorra/tests/headless_quorra.rs::cpu_and_quorra_agree_on_a_four_component_page", "crates/render-gpu/tests/headless_gpu.rs::the_gpu_refuses_a_four_component_page"]
note = "The seventeenth session's largest finding, and it was invisible until a group made it visible. Everything painted on a page belongs to a page group that \"shall be treated as an isolated group, whose results shall then be composited with a backdrop colour appropriate for the medium\" — so the page's own initial backdrop is transparent, and the medium's white is applied to the finished page. Both backends filled the raster with white and drew over it, which is a different picture for every blend mode: an object blending against an unpainted part of the page saw white where the standard says it sees nothing. transparency_group.pdf draws a Difference ellipse that four reference renderers show crimson over white, and this tree showed as its inverse. `impose_on_medium` in pdf-render is the composite, called by both backends so neither can differ about it. **The clause's other half is the page group's blending colour space, and until the four-hundred-and-fifteenth session nothing in this tree read the entry at all** — a silence rather than a departure, which is trap 5's failure. \"That initial colour space shall serve as the default blending colour space for each page, unless the page explicitly specifies an alternative default by means of its page dictionary containing a Group key that contains a CS key whose value represents a different colour space from the initial blending colour space\", and \"[a]ll page-level compositing shall be done in the default blending colour space of the page\" — so the entry decides every mark on the page and not only the groups. `page_blending_space` reads it, `Interpreter::blending` carries it as the root of §11.6.6's inheritance, and a space that is not the device's three components is reported once per page where something on that page composites. **6 of the 974 corpus documents report it**, all `/DeviceCMYK`, of the 7 that state one — `bug1365930.pdf` states one and nothing on its first page composites. What is still not implemented is compositing *in* that space, which is §11.6.6's second raster format and ADR 0251's measurement, and the clause's second case, a page used as an element of another document's page through a reference XObject (§8.10.4), which needs importing before the page group's attributes can matter. **The four-hundred-and-twenty-sixth session draws the page in that space rather than reporting it, and the construction needed no new raster format.** §11.3.4 makes the compositing formula per component — \"[t]he i th component of the result colour shall be obtained by applying the compositing formula to the i th components of the constituent colours\" — so a rasteriser with three channels composites four by drawing the page twice, once carrying cyan, magenta and yellow in §11.3.4's additive form and once carrying black, with `pdf_render::blending::resolve` putting the pair together where this clause puts the conversion. `interpret_with` runs both passes and a geometry digest checks that they differ only in colour; `render-cpu` draws both and combines; `render-gpu` refuses the list by name. **And the clause's own first sentence was doing two jobs until the six-hundred-and-eleventh session, which is a separation rather than a fix.** 𝑊 is Table 141's \"[i]nitial colour of the page\" and §11.4.7 composites the isolated page group with \"a backdrop colour appropriate for the medium\" — a property of *the page*, whose extent is §14.11.2.1's crop box. What a window shows where there is **no page** is not this clause's subject and is not any clause's: searched across §11.3, §11.4.5, §11.4.7, Table 141, §14.11.2 and Table 147's twenty-two viewer preferences, with `tools/spec-errata`'s `emit` reporting no erratum anywhere in clause 11. One `Color` served as both, so a continuous column drew white paper on white ground and a reader could not see where a page ended; making that colour grey to find the gap turned the *pages* grey, which is the proof. `pdf_render::medium::Medium` is the pair — 𝑊 and a `SURROUND` written down **as this program's choice** — `page_area` is where 𝑊 stops, and `impose_within` is the composite all three rasterisers end with, so trap 2's rule holds: neither backend can put the boundary somewhere the others do not. **Nothing moved on any gate**, and that is measured rather than argued: `examples/raster_digest` hashes every corpus first page's bytes and 957 of 974 are identical across the change, because a page-sized target asks for one colour on both sides of a boundary and `Medium::is_uniform` then takes exactly the pass this clause always had. That shortcut is a correctness decision and not an optimisation — with a non-uniform medium **193 of the 957** move, being the pages whose extent is not a whole number of pixels at 72 dpi, where `TargetSpec::for_page` rounds the raster up past the crop box. ADR 0446. **The report this leaves fires on three conditions rather than four since the four-hundred-and-forty-first**: a second colour space the document names, one a group introduces, and one whose black generation the file states. §11.3.5.3's non-separable blend was the fourth and is drawn (ADR 0277), which took 31 of 65 944 web documents and one corpus document's third report off it. **The quorra refusal stopped being a request in the four-hundred-and-thirty-eighth**: two `Target::Readback` renders against one device are supported and share their uploaded resources (`doc/QUORRA_FEEDBACK.md` section 17.1), so the two passes that clause needs are work owed here rather than a hole in that library. What is left reported is named per page: an `ICCBased` four-component space, whose conversion out is a profile rather than sixteen corners; a group inside the page that composites in a different space; an `/ExtGState` stating Table 57's black generation; a non-separable blend mode; and — the one that blocks most of them — a colour §11.7.2 would have to convert *into* the space. **Of a 1944-document sample of the web, 69 pages stated such a space and 7 are drawn in it; on the 974-document corpus 1 of 6 is.** ADR 0262. **And the four-hundred-and-twenty-seventh gave §11.7.2 its conversion, which is what the page group was waiting for.** 56 of the sample's 69 pages that state such a space are drawn in it now rather than 7, and the corpus's own count is 3 of 7 rather than 1. The five things still named are in §11.7.2's row. ADR 0263. **And the four-hundred-and-thirty-ninth drew it on the graphics device**, which is the last of the three backends to have it: `render-quorra` renders the two lists as two `Target::Readback` frames against one device — supported and cheap, and held upstream by `quorra-gpu/tests/two_rasters.rs` — and calls `pdf_render::blending::resolve` on the pair, so no backend but Vello's now differs about which space a page composites in. The quorra corpus gate's refused list falls from eight names to five and `personwithdog.pdf`, `bug1365930.pdf` and `issue12798_page1_reduced.pdf` agree with the CPU oracle at 0.0288, 0.0093 and 0.0760 mean. `render-gpu` still refuses and its refusal is now held by a test of its own. ADR 0275. **And the four-hundred-and-fortieth found that most of what kept a page off this construction was a soft mask**: the flag that says a group changed the blending space was not scoped to the page the way the space itself is, so an isolated group declared inside a `/SMask`'s group took the whole page off the ink route. `bug1703683_page2_reduced.pdf`, `bug1755507.pdf` and `issue13520.pdf` are drawn in ink now, the corpus's incomplete count is 68 → 65, and **6 of the 7 corpus documents that state such a page group are drawn in it** rather than 3. ADR 0276. **\"A group inside the page that composites in a different space\" narrowed again in the four-hundred-and-ninety-second**: a group *introducing* a sampleable four-component space is drawn in it as a pair of its own (§11.6.6's row, ADR 0327), so what that condition still fires on is a space the group-scoped pair cannot carry. **And the report the four-hundred-and-forty-first left had a reason that was not the document's, found in the five-hundred-and-eighty-first** (ADR 0416): a press arriving after this process had sampled its eighth was refused, so a page carried a verdict eight other files had decided. **The five-hundred-and-eighty-second made the budget the interpretation's** (ADR 0417), which is the shape every other budget in this tree has: a page is refused a press only when *it* names more than `colour::MAX_PRESSES` distinct ones, on every run and on every machine, and `colour::Presses` is where that is counted. So `blending_undrawable`'s reading order stops being a rule — every reason it can return is the document's now — and `Interpretation::press_beyond_this_process` and `reports_beyond_this_process`, which existed to tell the two apart, are gone with the distinction. What stayed process-wide is the *sampling*: `colour::SAMPLED` holds `MAX_CACHED_PRESSES` of them most-recently-used-first, and a cache may be shared where a budget may not because it changes how fast an answer is reached and never what it is. That split was decided by measurement rather than by argument — sampling a press costs 17 to 46 ms against a 14 to 18 ms interpretation of the same page, so a table with the interpretation's lifetime and nothing behind it would have doubled to quadrupled every page turn of such a document. **The 974 corpus reaches none of this**: 7 of its documents state this clause's condition and not one names an ICC press, so no ratchet has ever moved for the bound. The web is where it bites — `examples/press_census`, run twice and identical in the five-hundred-and-eighty-second as in the five-hundred-and-eighty-first, finds 2296 of the 65 703 crawled documents that open stating the condition and 28 distinct presses among the 287 that name one through a profile."

[[clause]]
clause = "11.4.8"
title = "Summary of group compositing computations"
status = "partial"
code = ["crates/render-cpu/src/lib.rs", "crates/render-gpu/src/scene.rs"]
test = ["crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_transparency_group"]
note = "A restatement of §11.4.4's formulas with the subscript b that selects the backdrop — 0 in a knockout group, i-1 otherwise — and with isolation folded in. Its own NOTE says the formulas \"can be significantly simplified when some sources of shape and opacity are not present\" and that each kind of group cancels some of the terms, and this tree now evaluates two of those simplifications rather than one: the isolated non-knockout case as a source-over composite of the group's buffer, and the non-isolated non-knockout case as an interpolation between the page and a buffer seeded from it (ADR 0237). The status follows §11.4.4's and §11.4.6's rather than adding anything: this clause introduces no requirement they do not. Its `b` subscript is what ADR 0234 evaluates for `b = 0`: the two-stage form in §11.4.6, restated here with source shape and alpha rather than shape and opacity, is the `(1 - f) x P + S` both backends draw. **Read and kept in the five-hundred-and-sixty-second**: this clause is a restatement and states no requirement of its own — its own NOTE is the only prose in it that is not a formula — so its status is §11.4.4's and §11.4.6's, and both are still `partial`."

[[clause]]
clause = "11.5"
title = "Soft masks"
status = "implemented"
code = ["crates/pdf-model/src/soft_mask.rs", "crates/pdf-model/src/content.rs", "crates/pdf-render/src/soft_mask.rs", "crates/render-cpu/src/lib.rs", "crates/render-gpu/src/soft_mask.rs"]
test = ["crates/pdf-model/tests/soft_masks.rs", "crates/render-cpu/tests/soft_mask.rs", "crates/render-gpu/tests/headless_gpu.rs::cpu_and_gpu_agree_on_a_soft_mask"]
note = "Aggregate of the three below, and the eighteenth session's demand item. A soft mask is a transparency group evaluated for its opacity: `pdf-model` runs the group into a command list of its own, both backends rasterise it at device resolution, and `pdf_render::SoftMask::value` — one function, called by both — turns the pixels into mask values. Was `reported` on 28 corpus documents until §11.4's groups made it buildable. ADR 0027."

[[clause]]
clause = "11.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-render/src/soft_mask.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/render-cpu/tests/soft_mask.rs::a_constant_mask_interpolates_between_the_object_and_the_backdrop"]
note = "Introduces the mask shape and mask opacity a soft mask supplies. Its NOTE 2's soft clip is what the CPU backend literally builds — the mask becomes a `tiny_skia::Mask` multiplied into the clip mask, which is the NOTE's \"generalization of the hard clip\" as arithmetic. Shape and opacity are one alpha here (§11.3.7.1's `a = f x q`), so `/AIS` changes nothing this subclause computes; see §11.6.4.3. **This sentence read \"`/AIS` is immaterial and deliberately not read\" until the four-hundred-and-second session**, and the second half stopped being true in the three-hundred-and-ninety-seventh, where `content.rs` began reading the entry — first to refuse §11.4.6's knockout groups while it is set, and since the five-hundred-and-eightieth to build their elements' shape from it (ADRs 0234 and 0415). The conclusion is unchanged and its reason is narrower than it was: immaterial *here*, and material one clause over."

[[clause]]
clause = "11.5.2"
title = "Deriving a soft mask from group alpha"
status = "implemented"
code = ["crates/pdf-render/src/soft_mask.rs"]
test = ["crates/pdf-model/tests/soft_masks.rs::an_alpha_mask_reads_the_groups_alpha_and_ignores_its_colour", "crates/pdf-render/src/soft_mask.rs::an_alpha_mask_reads_the_alpha_and_ignores_the_colour"]
note = "The group is drawn onto a fully transparent surface and its alpha channel taken, which is the clause's backdrop \"whose value does not contribute to the eventual result\" factored out by construction rather than by subtraction. Its NOTE 2 — the shape and alpha of an empty group is 0.0 — falls out of the same surface. `smask_alpha_bc.pdf`, `smask_alpha_oob.pdf` and `smask_alpha_oob_transfer.pdf` are the corpus's cases and all three now draw. **The test named above was not testing this clause until the four-hundred-and-nineteenth session**: its group named two `/ExtGState`s its own `/Resources` did not define, so both halves were painted opaque and the two levels it asserted were the same number. The fixture states `/ca 0.25` and `/ca 0.75` now and the levels are 191 and 64, derived from the clause; what found it was making a `gs` naming nothing loud (ADR 0255)."

[[clause]]
clause = "11.5.3"
title = "Deriving a soft mask from group luminosity"
status = "partial"
code = ["crates/pdf-render/src/soft_mask.rs", "crates/pdf-model/src/soft_mask.rs", "crates/pdf-model/src/colour.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/soft_masks.rs::a_luminosity_mask_takes_the_groups_grey_and_the_backdrops_outside_it", "crates/pdf-model/tests/soft_masks.rs::a_cmyk_mask_group_takes_its_luminosity_from_the_clauses_own_formula", "crates/pdf-model/tests/soft_masks.rs::an_absent_backdrop_in_a_cmyk_group_masks_everything_away", "crates/pdf-model/tests/soft_masks.rs::half_covered_artwork_over_registration_black_is_clamped_after_the_compositing", "crates/pdf-model/tests/soft_masks.rs::a_cmyk_image_in_a_mask_group_is_decoded_into_the_groups_own_components", "crates/pdf-model/tests/soft_masks.rs::a_cmyk_shading_in_a_grey_mask_group_is_sampled_into_the_groups_own_components", "crates/pdf-model/src/colour.rs::a_scaled_channel_carries_the_clauses_own_grey", "crates/pdf-render/src/soft_mask.rs::a_luminosity_mask_uses_the_clauses_own_coefficients"]
note = "The group is composited onto the opaque `/BC` backdrop and the luminosity of the result taken. **The clause's device branch is complete since the three-hundred-and-eightieth session, in the space the group names rather than in the device's** (ADR 0217): \"convert the colour to DeviceGray by implementation-defined means\" is §10.4.2 for every device space, so a group blending in `DeviceRGB` reads EXAMPLE 2's Y = 0.30 R + 0.59 G + 0.11 B off the rendered pixel, and a group blending in `DeviceCMYK` or `DeviceGray` has its colours *painted* in the ink §10.4.2.3 weighs instead — exact, because that conversion's linear part composites the way the components would and its three weights sum to 1.0, so the quantity survives the rasteriser unchanged. Those coefficients and not a library's: `tiny_skia::MaskType::Luminance` is Rec. 709 and Vello's luminance mask is the SVG formula, and both are a fifth of the mask's range away on green. **Two of the three residues that note recorded were paid in the three-hundred-and-eighty-third** (ADR 0220). The clause puts §10.4.2.3's `min` *after* the compositing and a rendered channel holds `0..=1` where registration black weighs 2.0, so the group's channel now carries `1 − ink ÷ scale` — `InkScale`, one unit for a `DeviceGray` group because §11.6.6's conversion *into* that space is itself the `min`, two for a `DeviceCMYK` one — and the `min` is composed into the mask's transfer table, where the clause spends it. And a colour that arrives already rasterised reaches the mask in the same quantity: `crate::colour::Compositing` is threaded through `crate::image` and `crate::shading`, so an image's samples and a shading's ramp are converted by §10.4.2.3 rather than to RGB first. Before the eightieth, process black in a `DeviceCMYK` mask group went through this tree's `DeviceCMYK` to RGB table (ADR 0009) and came back as a mask value of 32 of 255 where the clause gives 0; before the eighty-third, half-covered artwork over `/BC [1 1 1 1]` came back at 128 of 255 where the clause gives 0. `partial` for two residues, each reported by name: the clause's *other* branch, a CIE-based group space converted to XYZ and its Y taken, which this tree answers with the grey of the sRGB it converts every such space to (a page-wide documented choice, silent for `CalGray`, `CalRGB` and ICC and reported for `Lab`); and a blend mode inside a `DeviceCMYK` group, where §11.3.5.2 applies a separable function to each component \"expressed in additive form\" and this composites one weighted average of the four — a report with no corpus member, written because ADR 0217 had left that case silent. The scale costs one rounding and it is measured: an eight-bit channel recovers a `DeviceCMYK` group's mask value in steps of 2 of 255 rather than 1, which `a_grey_masks_the_same_whichever_of_the_two_device_spaces_the_group_blends_in` states as the one level it allows. **And what this clause takes out of the page it takes out completely**, which the four-hundred-and-fortieth session had to say in code: a mask group is composited against a backdrop of its own and reduced to one luminosity, so no colour space declared inside one is a space the *page* composites in. §11.6.6's departure was already cleared here for that reason (ADR 0220); the flag that records a change of space was not, and 77 of the 85 web documents reporting §11.6.6 were pages whose only such change was inside a mask. ADR 0276."

[[clause]]
clause = "11.6"
title = "Specifying transparency in PDF"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/transparency.rs", "crates/pdf-model/tests/image_masks.rs"]
note = "Aggregate of the seven below, read as a family in the fifteenth session (§11.6.4 was the fourteenth's). Blend modes, both alpha constants, an image's own soft mask and the graphics state's are implemented, and so are transparency groups. **This note said \"a graphics-state soft mask is reported; transparency groups are the silence\" until the four-hundred-and-forty-second session**, and both halves expired in the two sessions after it was written: §11.6.6's groups in the seventeenth and `build_soft_mask` in the eighteenth (ADR 0027), each recorded in its own row ever since. Four hundred and twenty-four sessions, which is the longest-lived stale claim this ledger has carried. What keeps the family `partial` is named below and is two things: Table 57's `/AIS`, honoured since the five-hundred-and-eightieth session and leaving one residue — a group whose content painted under *both* of its readings, which no single reading describes (§11.6.4.3, ADR 0415); and the group colour space a painted group introduces at its `Do`, reported for 1 corpus document and 8 of 65 944 web ones (§11.6.6). §11.6.7's patterns carry their own."

[[clause]]
clause = "11.6.1"
title = "General"
status = "inapplicable"
note = "One sentence of scope: the subclauses before it describe the model abstractly and this one describes its representation in PDF. There is nothing to implement, and marking it `implemented` would claim code for a clause that asks for none."

[[clause]]
clause = "11.6.2"
title = "Specifying source and backdrop colours"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_filled_and_stroked_path_is_one_object"]
note = "\"Portions of an object shall not be composited with one another\" — and `B` and its three relatives paint one object as a `Fill` and a `Stroke`, so the band a centred stroke shares with the fill composited twice. `reported` from the fifteenth session; drawn since the seventy-second, by the construction the clause itself points at: §11.4.6's knockout, where \"at any given point, only the topmost object enclosing the point shall contribute\", the topmost portion being the stroke because `B` strokes second. The pair becomes one `Command::Group` with `knockout` set wherever the report used to fire — the paint composites and both parts mark the page — and keeps the report where a portion's shape is not the coverage it is drawn with, which is `transparency::knockout_group_elements`, or where the two portions were painted under both readings of §11.6.4.3's `/AIS` — the flag itself is honoured since the five-hundred-and-eightieth and this clause's pair takes the reading its parts painted under (ADR 0415). **`Command::Shaped` is deliberately not used here**, although §11.4.6's groups use it as of ADR 0234: a portion's shape being unstatable and a portion's shape being its coverage are different populations, and this one has no corpus witness to size the second construction against. §11.7.4.4 states the same rule for the *other* population this clause governs, glyphs shown in text rendering mode 2 or 6, and that half waited until the hundred-and-twenty-first session — see that row. 2 of the 4 corpus documents that reported this now draw with nothing reported. The clause's other two examples cost nothing here: a self-intersecting path is filled once under one winding rule, and a mesh shading is one paint. **A third population arrived in the three-hundred-and-seventy-fourth session and is not an example the clause lists**: the tiles of a tiling pattern, which §11.6.7 makes portions of the one object the pattern paints — \"the colour, shape, and opacity values resulting from the evaluation of the pattern definition shall be used as the object's source colour ( 𝐶𝑠 ), object shape ( f j ), and object opacity ( qi )\". A rule drawn on a cell's `/BBox` edge is halved by Table 74's clip and reassembled from two cells, and the two halves were compositing. They are folded to one mark now (§8.7.3.1, ADR 0213). Source colour and backdrop colour are as the clause states them."

[[clause]]
clause = "11.6.3"
title = "Specifying blending colour space and blend mode"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency.rs::a_blend_mode_array_takes_the_first_name_this_reader_knows"]
note = "All sixteen names of Tables 134 and 135 reach both backends, and the deprecated array form takes \"the first blend mode in the array that it recognizes (or Normal if it recognizes none of them)\" — which is not the first *name*, and was until the fifteenth session. The blending colour space is the other half of the clause and it defers to §11.6.6 and §11.4.7: we composite in the device space throughout, which is what a page with no group attributes dictionary asks for — and **this row used to stop there, as though the other case did not occur**. 115 of the 974 corpus documents state a page group `/CS` and 7 of those name a space that is not the device's three components. Each was *reported* where something on the page composites (ADR 0251); since the four-hundred-and-twenty-sixth each is **drawn** in the four components it states (ADR 0262), and since the four-hundred-and-fortieth that is 6 of the 7 (ADR 0276)."

[[clause]]
clause = "11.6.4"
title = "Specifying shape and opacity"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_soft_mask_overrides_an_explicit_mask"]
note = "Aggregate of the four below, read as a family in the fourteenth session because §11.6.4.3 decides the precedence between the masks that session implemented. All three sources §11.6.4.1 names are implemented — object shape, the constant, and the mask in both of its forms. What keeps this `partial` is Table 57's `/AIS`, which decides whether the mask and the constants are read as shape or as opacity: it is read as of the three-hundred-and-ninety-seventh session and **honoured** as of the five-hundred-and-eightieth, with the one construction it can change built under both readings and refused only where a group's content painted under both at once (§11.6.4.3, ADR 0415). **Read and kept in the five-hundred-and-sixty-second**, and it is now the only row of this family carrying the `/AIS` debt beside §11.6.4.3's and §11.6.4.4's — §11.6.4.1 was `partial` for it too and states it nowhere, and is `implemented`. This clause has no prose of its own above its four subclauses, so the aggregate is the right shape and the debt is genuinely the family's."

[[clause]]
clause = "11.6.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency.rs::the_two_alpha_constants_reach_stroking_and_non_stroking_paint_separately", "crates/pdf-model/tests/image_masks.rs::an_explicit_mask_paints_the_base_image_only_where_it_marks", "crates/pdf-model/tests/image_masks.rs::a_soft_mask_overrides_an_explicit_mask"]
note = "Lists the three sources of shape and opacity and defers each to a subclause. Its own prose states one requirement — the values \"shall come from one or more of the following sources\", object, mask and constant — and all three are here: object shape (§11.6.4.2, `implemented`), a mask in both of its forms (§11.6.4.3 — an image's own entries and the graphics state's, the latter since the eighteenth session), and the constants (§11.6.4.4). **This row was `partial` until the five-hundred-and-sixty-second session for a debt that is not this clause's**: it said \"[w]hat this row owes is which of the two quantities the last two are, which is `/AIS` and is §11.6.4.3's\" — and `/AIS` is Table 57's entry, stated by §11.6.4.3's and §11.6.4.4's own text and by nothing here. A row `partial` because a *neighbour* owes something is the shape the five-hundred-and-twenty-fifth found in §E.1 and §I, one family over: no sweep looks for it, because the sentence names a real debt in a real row. The clause's last sentence — \"[t]he following subclauses describe how each of these shape and opacity sources shall be specified in PDF\" — is a pointer, and the three rows it points at carry their own statuses."

[[clause]]
clause = "11.6.4.2"
title = "Object shape and opacity"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/image.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_zero_sample_paints_the_current_colour_and_a_one_leaves_the_page_alone", "crates/pdf-model/tests/image_masks.rs::an_explicit_mask_paints_the_base_image_only_where_it_marks"]
note = "Every clause of the list holds here. A path or a glyph has shape 1 inside and 0 outside, which is what filling an outline does; an image has shape 1 inside its rectangle \"further modified by an explicit or colour key mask\", which became true in the fourteenth session (§8.9.6.3, §8.9.6.4); an image mask has shape 1 where it paints; a pattern's shape is constrained by the objects that define it, which falls out of replaying a tiling pattern's content stream. The last one is worth naming because it is satisfied by *absence*: the shape of an `sh` is its geometry \"disregarding the Background entry in the shading dictionary\", and nothing in this tree reads /Background at all. The intrinsic opacity of 1.0 everywhere is likewise structural — opacity enters only through `ca`, `CA` and a mask. **Where the shape is read *apart* from the opacity is §11.4.6, and the last of the three backends that could not draw it started in the four-hundred-and-fifty-sixth session**: `pdf_render::Command::Shaped` carries the object beside the same object with §11.6.4.3's mask and §11.6.4.4's constant removed, whose drawn alpha is this clause's shape, and `render-quorra` states the two stages on `quorra_scene::Compose` (ADR 0291). A row that is `implemented` in the model can still have a backend unable to *use* what it states, which is what this one had for seventeen rounds and what §11.4.6's row carries."

[[clause]]
clause = "11.6.4.3"
title = "Mask shape and opacity"
status = "partial"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_soft_mask_overrides_an_explicit_mask"]
note = "The clause's precedence is implemented as of the fourteenth session, and it was read here rather than in §8.9: an image's /SMask, and a non-zero /SMaskInData, \"shall override any explicit or colour key mask specified by the image dictionary's Mask entry\". Before that a file writing both would have had the loser applied. The graphics state's own /SMask is **applied** — `build_soft_mask` evaluates its group and every object painted while it is in force draws through it, since the eighteenth session (ADR 0027) — and only a mask this reader cannot use at all is reported. **This sentence said \"reported rather than applied (28 corpus documents)\" for a hundred and forty sessions after that stopped being true**, which is ADR 0101's shape and was found by the sweep for notes whose stated blocker has expired. **Table 57's `/AIS` is read as of the three-hundred-and-ninety-seventh session, and this row was wrong twice about it.** It first said the distinction \"cannot show until §11.4.6 exists\"; when §11.4.6 was drawn in the seventy-first session the reason was rewritten to say the distinction still cannot show, because alpha is the product `α = f × q` (§11.3.7.1) and a mask multiplies the same alpha whichever it is called. **That was already false when it was written**: a knockout group drawn by modulating Porter-Duff Source with coverage reads the constant alpha as *opacity*, and under `/AIS true` it is shape — so the one place the flag decides a pixel is the one place this tree had stopped reporting. ADR 0234 makes it worse still by removing the mask and the constant to *state* a shape. So the entry is read and a knockout group is refused by name while it is set, which is honest rather than complete: honouring it means composing the mask into the shape instead of into the object, and nothing here does that yet. **And the sentence \"No corpus document writes /AIS\" was false**: 53 of the 974 state the entry and **9 state it true** — `bug1703683_page2_reduced`, `bug1755507`, `issue12798_page1_reduced`, `issue14297`, `issue18032`, `issue18956`, `issue19360`, `issue7891_bc0`, `issue7891_bc1`. **And \"reading it costs no page\" decayed too**: the flag was monotone across the whole page, so a `gs` stating it inside one form refused every knockout group after it, however many `Q`s had restored the state between — `issue18032.pdf` states it inside a form whose group draws nothing, two forms before the knockout group it cost. Since the four-hundred-and-ninety-second the entry is a `GraphicsState` field like the parameter it is, both values are read, and the question a closing group asks — was this in force while my content ran — is seeded from the state at the `Do` and scoped to that group's run (ADR 0327). **The five-hundred-and-eightieth honoured it, and the construction turned out smaller than every sentence above predicted** (ADR 0415), because the clause makes the quantity a rasteriser already holds the right one. Three sentences settle it and none is about a rasteriser: §11.6.4.2's \"All elementary objects shall have an intrinsic opacity q j of 1.0 everywhere\", this clause's NOTE 1 handing the mask to shape, and §11.6.4.4 handing both alpha constants to shape. So all three of §11.3.7.2's opacity inputs are 1.0, their product is 1.0, and §11.3.7.1's alpha is the source shape and nothing else — **the number a rasteriser already draws the element with is the shape**. The shape command under `/AIS true` is therefore the element itself with its blend mode dropped (`transparency::shape_the_alpha_already_is`), the pair `Command::Shaped` already states draws §11.4.6's two stages on all three backends, and no vocabulary, backend arm or raster was added. **The refusal was inverted**: `/AIS true` is the one reading under which a rasteriser's single number per pixel *cannot* disagree with the shape, and it is precisely where this tree refused for eighty-three sessions. What keeps the row `partial` is a scope rather than a construction — `Interpreter::alpha_sources` records which readings a group's content painted under, and a group that painted under **both** is refused by name because no single reading describes it; the record over-approximates within one scope (\"stated while the content ran\", narrowed by `alpha_sources_mark` so that a reading nothing was painted under replaces rather than mixes). **And the populations were measured rather than assumed** (trap 11): of the 974 corpus documents 9 state `/AIS true` and **not one of their knockout groups reaches this at all**, so the claim above that none of them is drawn had expired with ADR 0327's scoping; of 65 944 crawled web documents **exactly one** carried the refusal, `6573550.pdf`, whose knockout group now draws — 13 736 of its 1 128 099 pixels move, by at most 3 of 255."

[[clause]]
clause = "11.6.4.4"
title = "Constant shape and opacity"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency.rs::the_two_alpha_constants_reach_stroking_and_non_stroking_paint_separately", "crates/pdf-model/tests/transparency.rs::an_object_drawn_without_a_constant_is_opaque"]
note = "`ca` and `CA` are two scalars in the graphics state, applied to non-stroking and stroking paint respectively, which is the clause, and which no test isolates — they reach the page through the corpus and the oracle. The first of the two sentences that were not is: \"The nonstroking alpha constant shall also be applied when painting a transparency group's results onto its backdrop\" is what `Command::Group`'s alpha carries, since the seventeenth session. The second is `/AIS`, which decides whether the constants are shape or opacity; it is read as of the three-hundred-and-ninety-seventh and **honoured as of the five-hundred-and-eightieth** — §11.6.4.3 has the derivation, and this clause supplies half of it: with both constants handed to shape and §11.6.4.2's intrinsic opacity already 1.0, §11.3.7.2's source opacity is 1.0 and an element's drawn alpha is its shape. **Read and kept in the five-hundred-and-sixty-second**, and the evidence is that this clause states `/AIS` itself rather than borrowing it: its last sentence is \"the AIS ('alpha is shape') entry in a graphics state parameter dictionary shall determine whether the alpha constants are interpreted as shape values ( true ) or opacity values ( false )\", so the debt is this row's own and the `partial` is right where §11.6.4.1's was not."

[[clause]]
clause = "11.6.5"
title = "Specifying soft masks"
status = "implemented"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-model/src/soft_mask.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/image_masks.rs::a_soft_mask_overrides_an_explicit_mask", "crates/pdf-model/tests/soft_masks.rs::a_luminosity_mask_takes_the_groups_grey_and_the_backdrops_outside_it"]
note = "Two ways to specify a soft mask, and both are implemented as of the eighteenth session: a soft-mask dictionary in the graphics state (§11.6.5.1) and a soft-mask image on an image XObject (§11.6.5.2), the latter including at a resolution of its own and with `/Matte` undone. What each of the two rows still owes is stated there. **Where the mask’s value meets a clipping region is the five-hundred-and-twenty-eighth session’s (ADR 0363), and it is this clause’s question as much as §10.7.4’s.** A soft mask supplies §11.3.7.2’s mask shape `f_m`, one of the three inputs of which \"[t]he three shape inputs shall be multiplied together, producing an intermediate value called the source shape\" — a product the standard asks for, and the reason `scan::Clip::Value` composes by one. What it is *not* is the clipping path, which §8.5.4 intersects with the object’s own shape before that product is taken; so a mark carrying both is composed as `(f_j ∩ C) · f_m` on this backend, with `MaskCache` keeping the mask’s rows beside the clip×mask product it caches for every other draw. `/AIS` does not change the answer: §11.3.7.1 makes alpha the product of shape and opacity, so the mask multiplies the clipped shape whichever row of Table 136 it lands in."

[[clause]]
clause = "11.6.5.1"
title = "Soft-mask dictionaries"
status = "implemented"
code = ["crates/pdf-model/src/soft_mask.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/soft_masks.rs::a_transfer_function_maps_every_mask_value_including_the_one_outside_the_box", "crates/pdf-model/tests/soft_masks.rs::the_masks_coordinate_system_is_the_one_in_force_at_the_gs", "crates/pdf-model/tests/soft_masks.rs::none_removes_the_mask", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_soft_mask_group_draws_its_prefix_and_reports_the_shortfall"]
note = "Table 142 entire. `/S` selects §11.5.2's or §11.5.3's derivation and any other name is refused and reported; `/G` is run as a transparency group with §11.6.6's initialisation, its own `/BBox` clipping it and its `/Matrix` concatenated with the transform in force at the `gs` — not at the painting operation, which is the sentence a later `cm` would otherwise break; `/BC` is read in the group's own `/CS`, so its component count is that space's, and defaults to the space's initial colour — and since the three-hundred-and-eightieth session it is resolved into whatever quantity the group's elements are painted in, which for a subtractive `/CS` is §10.4.2.3's grey rather than a device colour, because a backdrop and the elements composited onto it have to be the same thing (ADR 0217); `/TR` is sampled onto the 256 values an eight-bit mask value can take, exactly, and clamped as the clause requires. The value outside the group's bounding box is the transfer function of 0.0 or of the backdrop's luminosity, which falls out of drawing the group onto transparency rather than being a second code path. **It is now also a named quantity, `SoftMask::outside`, and that is a performance decision rather than a second derivation** — the two arms of the branch that uses it call the same function on the same pixel. A mask group is drawn into a buffer the size of the whole target while its marks cover its own `/BBox`, so most of the buffer is the transparency it was allocated as: on the two slowest documents of 65 944 crawled ones, **98.5% and 99.96%** of every soft-mask raster is that one pixel, and deriving it per pixel was 22.4 s of a 25.4 s page and 51.0 s of a 52.6 s one. Computing it once took them to 6.6–7.3 s and 3.7–4.0 s with **no page of the oracle's 1794 or quorra's 957 moving**. ADR 0271. The over-report the previous note described is gone: §11.6.4.3's precedence is now read, so an image carrying its own mask is drawn with the graphics state's mask off. **A `/G` whose stream decoded only part-way is evaluated and reported**, since the five-hundred-and-twenty-fourth, and this is the one of §7.8.2's five kinds where the answer had to be checked rather than carried over: the group's marks become mask *values* over other objects, which is the shape ADR 0356 refused for a sampled function. What decides it is that this clause states the mask's value where the group painted nothing — the transfer function of 0.0 for `Alpha`, `/BC`'s luminosity for `Luminosity` — so a place the damage took is a place the group did not paint, and the clause already answers for one of those. Places, not values. ADR 0359. **And the group is read as a *source* since the five-hundred-and-ninety-second**, on the same rule as every other form: the memo decides whole against windowed, and the damage report is the same sentence from whichever of the two produced it. ADR 0427."

[[clause]]
clause = "11.6.5.2"
title = "Soft-mask images"
status = "partial"
code = ["crates/pdf-model/src/image.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/image_masks.rs"]
note = "An `/SMask` supplies its image's alpha, and `/SMaskInData` is honoured for JPEG 2000. **This note said the mask \"is left unapplied and reported\" for a hundred and eighty sessions after that stopped being true** — the finer-grid combination landed in the fifteenth — and the sentence survived because the row was corrected once, about `/Matte`, and the correction was appended rather than read against what came before it. Table 143 makes a mask's dimensions \"independent\" of the parent's, with \"both images shall be mapped to the unit square in user space … regardless of whether the samples coincide individually\", and there are now two routes to that, chosen by whether the refinement of the two grids can be built — asked, since the four-hundred-and-eighty-sixth session, about the base raster that *exists* rather than the dictionary's statement of it, which is what routes a reduced JPEG 2000 base under a full-size mask to the device-scale path instead of an eager combination on a grid neither raster is on (ADR 0321): `combine_on_the_finer_grid` where it can, which discards nothing either raster carries, and — since the three-hundred-and-seventieth session — the backend, at the device resolution §10.7.4 states, where it cannot. `issue16263.pdf` is the second's witness: a 2x2 image with a 34862x4332 mask, 151 022 184 samples and 604 MB of RGBA on the finer grid, drawn as black bars until the display list could carry the two rasters apart. It agrees with the reference consensus now, and **no corpus document reports an `/SMask` at all**. `partial` for the three residues, each refused by name: a mask behind an image codec, whose samples have no position until the whole codestream is decoded — §7.4.9's resolution levels answer a *base image's* memory budget once (ADR 0321), where this route would decode per raster request, and no corpus document states a codec-carrying mask; a mask in a one-component space that is not the `DeviceGray` Table 143 requires, which this tree tolerates on the eager route and does not read a byte out of on the other; and a `/Matte` whose parent space is neither `DeviceGray` nor `DeviceRGB`, where \"inversion of the pre-blending shall precede the colour conversion\" and the conversion has happened. **`/Matte` itself is read** — `image.rs` undoes the pre-blending where the arithmetic is exact and reports where it is not, one of trap 5's deliberate report-and-draw cases (hundred-and-twenty-second session) — and Table 143 makes its grid the parent's, so it never meets the deferred route. **And Table 143's two \"Shall be absent\" rows are both read since ADR 0399, where only one was**: an /SMask inside an /SMask was refused by name and a /Mask inside an /SMask was not, which is the second door into an unbounded recursion — `apply_soft_mask` -> `decode` -> `decode_parts` -> `apply_explicit_mask` — that the `page` fuzz target reached through §8.9.6.3's. Both are `SoftMaskEntry::Unusable` now, so the base image is drawn opaque and the file's illegal entry is named. ADRs 0034, 0210."

[[clause]]
clause = "11.6.6"
title = "Transparency group XObjects"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-render/src/display_list.rs", "crates/render-cpu/src/lib.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs", "crates/render-cpu/tests/group_constructions.rs::a_group_with_its_own_blending_space_composites_in_it", "crates/pdf-model/tests/transparency_groups.rs::a_group_that_introduces_a_press_composites_in_it", "crates/render-gpu/tests/headless_gpu.rs::the_gpu_refuses_a_group_in_its_own_blending_space", "crates/render-quorra/tests/headless_quorra.rs::quorra_refuses_a_group_in_its_own_blending_space"]
note = "Was `silent` — a /Group was read nowhere and a group XObject was drawn as an ordinary form — and is implemented as of the seventeenth session. Table 145's /S selects the subtype, /I and /K are read, and the four actions this clause adds to Do are performed: the initial backdrop, the initialisation of the blend mode and both alpha constants (NOTE 1's reason — those parameters belong to the group, and leaving them in force applies them twice), compositing the elements into the group, and painting the group's result into the parent under the state at Do. The requirement that each Do be treated as a separate group falls out, since nothing is cached. What is not implemented is compositing in the group colour space *for a painted group*. **Which space that is was read wrongly until the four-hundred-and-fifteenth session**: this row said \"/CS is read and named\", and the clause makes the entry take effect only \"[f]or isolated groups\" while \"[f]or non-isolated groups, or if no group colour space is specified, the group colour space shall be inherited from the parent group or page\". So a non-isolated group's `/CS` named a departure that was not there (`issue14200.pdf`), a group with no `/CS` inside an isolated one named none where there was, and §11.4.7's page group — the root of the inheritance — was not read at all. `group_blending` resolves it and the departure is reported once, where the file introduces it: **7 corpus documents**, one by this clause and six by §11.4.7, all `/DeviceCMYK`. Honouring it was thought to mean holding the group's raster in its own components, and the sentence saying so stood here as current after the four-hundred-and-twenty-sixth session withdrew it — see this row's own last paragraph. What the measurement under it establishes is not a raster format but a *distance*, and that stands: §11.3.3 under `Normal` is a weighted average, so compositing in the blending space and converting once agrees with converting first and compositing on the device exactly when the conversion is affine over the colours involved; this tree's `DeviceCMYK` conversion is multilinear over the ink cube (ADRs 0009, 0042) and is affine on no face of it. 48 of 255 apart at worst over 300 000 random pairs and 51.5 at half of registration black over paper, against 3 x 10⁻¹⁶ if §10.4.2.5's classic formula is used away from its clamp — so the second raster format is required by the colour conversion this project chose, not by the clause alone (ADR 0251). **A mask's group is the case that turned out not to need one** — §11.5.3 reduces it to a single luminosity, and §10.4.2.3's conversion to that luminosity is linear in the components, so the group can be painted in the one number a rasteriser already composites (§11.5.3, ADR 0217). That argument does not carry to a painted group, whose result is three components rather than one, and `doc/todo/23` keeps it. **The second raster format turned out not to be needed at all**, which the four-hundred-and-twenty-sixth session found by reading §11.3.4 rather than by building one: the compositing formula is applied per component, so four components are three plus one and a page §11.4.7 composites in `DeviceCMYK` is drawn by interpreting it twice. That answers the *page* group; a group that introduces its own space inside one still needs a conversion between the two spaces at its `Do` and is still reported, which is `bug1721218_reduced.pdf`'s case and this clause's own. **And most of what was reported for this clause was not this clause at all**, which the four-hundred-and-fortieth session measured rather than argued (ADR 0276): `build_soft_mask` clears the space in force for a mask group's content — ADR 0220's finding, because §11.5.3 reduces such a group to one luminosity — and the flag recording a *change* of space was outside that scope, so every isolated group declared inside a soft mask counted as a group the page composites in. Asked its `soft_mask_depth`, **77 of the 85 web documents reporting it were that, and all three of the corpus's**; `bug1703683_page2_reduced.pdf`, `bug1755507.pdf` and `issue13520.pdf` are drawn in the ink §11.4.7 gives them and the corpus's incomplete count is 68 → 65. What was left was **1 corpus document and 8 of 65 944 web ones** where a group on the page itself introduces a space the page does not composite in, plus 30 more where a group introduces one on a page that states none — `bug1721218_reduced.pdf` being the corpus's. **The four-hundred-and-ninety-second built the construction for the largest of those shapes** (ADR 0327): an isolated group whose `/CS` names four components this tree can sample — `/DeviceCMYK` through §8.6.5.6's and §14.11.5's ranking against the group's own resources, or a four-component `ICCBased` profile — on a parent compositing on the device is §11.4.7's pair one scope down. The interpreter runs the group's content twice, once per half of the four components, rewinds the readback around every run after the first, verifies the two element lists paired structurally, and `Command::Group` carries the second list with the conversion out (`pdf_render::GroupBlending`); `render-cpu` composites both halves onto transparency, resolves them through `blending::resolve` where §11.7.2 puts the interpretation of the group's accumulated colour, and paints the result once; `render-gpu` and `render-quorra` refuse the command by name, because the pair resolves per pixel after the group composites and a scene under composition cannot be read back. The pair is built only where something in the group composites — an opaque Normal mark carries its colour through whatever space it is carried through, which is the same condition the report fired on — and not where §11.7.5.3's black generation is stated, where the group is a knockout group (the staged rewrites edit one half of a pair), or where §8.6.8 supplies the colour from outside. A group's press comes out of the same per-interpretation budget the page's does since ADR 0417, so a group refused one is refused because *this page* named more than `colour::MAX_PRESSES` distinct presses — it was refused because eight other documents had been opened first, and `Interpreter::group_press` no longer has a case for that. What keeps this row `partial` is the remainder, each still reported by name where it composites: a group introducing a *three-component or one-component* space inside a four-component parent (the conversion out of the group would have to land in the parent's ink per pixel, which is a conversion between two presses this tree does not have — `bug1721218_reduced.pdf`'s inner gray-`ICCBased` groups are the corpus witness, so that document keeps a smaller report while its `/DeviceCMYK` outer group now composites in ink), four components no profile backs, and a stated black generation."

[[clause]]
clause = "11.6.7"
title = "Patterns and transparency"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency.rs::a_shading_carries_the_non_stroking_constant", "crates/pdf-model/tests/tiling.rs::the_alpha_constant_applies_to_the_finished_pattern_rather_than_to_each_mark", "crates/pdf-model/tests/tiling.rs::a_pattern_that_composites_trivially_is_not_wrapped_in_a_group"]
note = "A shading pattern composites \"as if the shading dictionary were applied with the sh operator\", which is exactly how `paint_shading` and `fill_paint` treat it, and both carry §11.6.4.4's constant as of the fifteenth session. **The implicit group the clause wraps a *tiling* pattern in is built as of the hundred-and-seventeenth session.** \"The definition shall not inherit the current values of the graphics state parameters at the time it is evaluated; those parameters shall take effect only when the resulting pattern is later used to paint an object\" — so every cell runs with the blend mode, alpha constant and soft mask at their defaults, and the state's own are applied once to the finished tiling by one group over all the tiles, which is what NOTE 2 asks for: \"it is advisable to treat all tiles as a single transparency group. This avoids artifacts due to multiple marking of pixels along the boundaries between adjacent tiles.\" Until then each cell inherited them, so an `0.5 ca` was applied per mark rather than to the pattern and the graphics state's soft mask reached the tiling not at all. Where every one of the three is at its default no group is built, which is §11.4.4's NOTE 5 and is all 122 tiling paints in the corpus — so **no corpus page witnesses this and the rule is here on the clause's evidence**, measured. **NOTE 2 names the artefact and does not remove it**, which the three-hundred-and-seventy-fourth session found by looking: compositing inside a group is still compositing, so two tiles' halves of a rule on a cell's `/BBox` edge union to `1 − (1−a)(1−b)` there as anywhere else. What removes it is the sentence above about the pattern's *evaluation* — the tiling is one shape before anything composites, so §11.6.2 applies to the tiles and the two halves are folded to the one mark they describe (§8.7.3.1, ADR 0213). The group is *isolated* where the clause says non-isolated, which NOTE 1 makes exact wherever no element of the cell blends and which is reported where one does — **except where §11.4.6's NOTE 6 gives the cell a transparent initial backdrop**, a pattern painted inside a knockout group whose own initial backdrop is transparent being an element of it in the sense that note is about, where the isolated construction is the clause and nothing is reported (ADR 0307). `/ExtGState` in a shading pattern dictionary is not read, and a shading pattern's own implicit *knockout* group is not built: `sh` composites its result once already, so the two differ only where a shading paints a point twice, which a function of position cannot."

[[clause]]
clause = "11.7"
title = "Colour space and rendering issues"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs", "crates/pdf-model/src/soft_mask.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs", "crates/pdf-model/tests/soft_masks.rs"]
note = "Aggregate of the five below, read as a family in the eighteenth session because §11.6.5.1's `/BC` is stated in a group's blending colour space and this is the clause that says what such a space is. §11.7.1 states no requirement, §11.7.3's spot colours are `implemented` by the tint transform this tree always takes, and the other three are `partial`, each naming what it owes: §11.7.2's CIE-based `should` and Annex P, §11.7.4.4's one drawable-shape condition, and §11.7.5.3's black generation, which is read as a condition and reported rather than evaluated. **This row stated its count of the family twice and the two disagreed** — \"[t]wo of its five subclauses are satisfied\" in the first sentence and \"four of its five subclauses are satisfied\" in the last — which is `doc/todo/01`'s sixth failure shape, a correction appended in the nineteenth session without the paragraph above it being re-read, standing for four hundred and ten sessions and found by the tenth sweep in the four-hundred-and-twenty-ninth. §11.7.4's overprinting was recorded as six `silent` rows by the first reading and re-decided by the second, on §8.6.6 and §8.6.7: Table 146's blend function is the source colour for every row a three-process-component group space with no spot colourants can reach, which §11.7.4.5's row works out row by row. **And the count was left behind by the two rounds that changed this family most**: the four-hundred-and-twenty-sixth read §11.7.2 and §11.7.5.3 as what bounds §11.4.7's page group, and the four-hundred-and-twenty-seventh carried out the conversion §11.7.5.3 names — `colour::rgb_to_ink`, a right inverse of the ink cube — taking a 1944-document web sample's 61 pages blocked by this clause to 0. ADRs 0028, 0262, 0263."

[[clause]]
clause = "11.7.1"
title = "General"
status = "inapplicable"
note = "One sentence introducing the subclauses below. There is no requirement to implement, and claiming one would put code against a clause that asks for none."

[[clause]]
clause = "11.7.2"
title = "Colour spaces for transparency groups"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/soft_mask.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_colour_from_outside_the_blending_space_is_converted_into_it_and_comes_back", "crates/pdf-model/tests/transparency_groups.rs::the_three_routes_to_a_press_all_composite_in_it", "crates/pdf-model/tests/transparency_groups.rs::a_colour_converted_into_a_named_press_comes_back"]
note = "**This row opened by saying that every compositing computation here happens on the three components of the device raster, so the group blending space this clause governs is device RGB whatever a `/CS` says — and that stopped being true fourteen rounds ago**, while its own later paragraphs described the two rasters that replaced it. The same self-contradiction §11.3.4's row carried, found here by the four-hundred-and-fortieth reading this family against the code. A page composites in the four components §11.4.7 gives it since the four-hundred-and-twenty-sixth; what still happens on the device's three is a *group* that introduces a space of its own. The rules below were departures rather than gaps and each was reported where it can change a pixel: a group whose blending space is not a three-component RGB one (§11.6.6 and §11.4.7, 7 corpus documents, all `/DeviceCMYK`) and a soft-mask group whose `/CS` is not one either (§11.5.3, 7 documents). **One of them stopped being a departure in the four-hundred-and-fifteenth session and became an implementation**: \"[n]on-isolated groups shall inherit their colour space from the nearest ancestor isolated parent group (subject to special treatment for the page group…)\" is carried out — `Interpreter::blending` is that inheritance, rooted at §11.4.7's page group — and this row used to say the inheritance rules \"have nothing to inherit into\", which was true of the *compositing* and false of the reading, and cost `issue14200.pdf` a report it did not deserve for as long as the tree read a `/CS` where the group had none (ADR 0251). What is still not inherited is a device space taking the nearest CIE-based ancestor's, which needs the compositing to happen somewhere other than the device. \"For the compositing computations to work in a device-independent way, the group's colour space should be CIE-based\" is a *should*, and this tree does not: it converts every colour once, into device RGB, which is trap 6's one-conversion rule and the reason the two backends cannot disagree about colour. Annex P's algorithm is the same subject and equally unimplemented. **Read as of the four-hundred-and-twenty-sixth session, and it is what bounds §11.4.7's construction.** \"If the colour space of a graphics object within the group is not equivalent to the group's blending colour space, then it shall be converted to the group's colour space , and all blending and compositing computations shall be done in that space\" — and this tree does not perform that conversion. §11.7.5.3 names §10.4.2.4 as the route, §10.4.2.1 packages §10.4.2.2 to §10.4.2.5 as what a processor uses *instead of* §10.3, and ADRs 0009 and 0042 put this tree's conversion *out* of `DeviceCMYK` on §10.3's branch. Composing one branch with the other is not the identity on an opaque mark: a `DeviceRGB` green comes back grey-green and `0 g` comes back the process black `#231F20`. So a page that paints any colour from outside its blending space keeps §11.4.7's report, named, and `ColourSpace::is_subtractive` is the condition. 61 of 69 sampled web pages are blocked by exactly this. ADR 0262. **Carried out as of the four-hundred-and-twenty-seventh session, and the paragraph above is the argument it replaced.** §11.7.5.3 says which conversion this clause means, and it says it by naming the target rather than the algorithm: \"Whereas in the opaque imaging model the target space shall always be the native colour space of the output device, in the transparent model it may instead be the group colour space of a transparency group into which an object is being painted.\" So the conversion into a group's space is the *same* conversion as the one onto the device with a different target, and it belongs on the branch this tree's conversion out is already on — §10.3's, by §10.4.2.1's ranking. `colour::rgb_to_ink` is that conversion: a right inverse of the ink cube, so a colour the assumed inks can make is separated, composited in four components and converted back to the colour the file states. §10.4.2.4 keeps two jobs in it — the nominal `k` a black-generation function is called with, and the starting point of the search — and is no longer the route. What is left is named per page and each is a different clause: a document that says what its own `DeviceCMYK` is (§8.6.5.6, §14.11.5), a group inside the page in another space (§11.6.6), Table 57's black generation (§11.7.5.3) and a non-separable blend (§11.3.5.3). Of a 1944-document sample of the web, 61 pages were blocked by this clause and 0 are. ADR 0263. **The table that conversion reads deadlocked the process until the four-hundred-and-thirty-third session**, which is a fact about how it is built rather than about the clause: `colour::ink_table` computed 4913 searches inside a `OnceLock` initialiser with rayon, and rayon runs other jobs while it waits, so a job that wanted the same table re-entered the initialiser on the initialising thread and waited for itself. Three of 145 `SafeDocs` archives hung on it. The grid is now computed before the lock is taken. ADR 0269. **The press is the document's rather than this tree's since the four-hundred-and-thirty-sixth session** (ADR 0272), which is this clause's own second paragraph: \"If an isolated transparency group or page has an ICCBased 'CMYK' colour space , DeviceCMYK shall be redefined within the transparency group to be the same as the blending colour space and references to the process colourants Cyan , Magenta , Yellow and Black are defined to be references to the corresponding colourants in the blending colour space, even where the actual or simulated output device is not CMYK.\" `colour::Press` is a four-component space sampled onto a grid — the conversion out §11.4.7 hands a backend, and the conversion in as a right inverse of that same grid, so a page still has one colour model. Which press comes from Annex P's order: §8.6.5.6's `/DefaultCMYK` first, then §14.11.5's output intent, and a `/CS` that is itself a four-component `ICCBased` space names one outright. **292 of the 65 944 web documents state a press this way and every one of their profiles is one `crate::icc` already evaluates**, so no ICC dependency and no `B2A` evaluator was needed. What the grid costs is written down rather than assumed: interpolating seventeen samples per axis departs from evaluating the profile by a median 5.99 and at most 14.52 of 255 over those 286 profiles, against the 48 to 51 of 255 that compositing in somebody else's four components costs (ADR 0251). What is still reported is four components with no profile behind them, and a press arriving after this process has sampled its eighth. **The conversion this clause requires is performed for a *group* one scope down since the four-hundred-and-ninety-second** (ADR 0327): an isolated group naming a sampleable four-component space on a device-composited parent has its elements interpreted in that space's components — the conversion in per stated colour by the same `to_cmyk`/`rgb_to_ink` routes, the conversion out per pixel at its `Do` through the same sampled grid — so \"all blending and compositing computations shall be done in that space\" holds inside such a group, and \"[t]he resulting colours shall then be interpreted in the group's colour space when the group is subsequently composited with its backdrop\" is `pdf_render::GroupBlending`'s resolve. §11.6.6's row says which shapes remain reported. **The eighth press was a bound on the *process*, and the five-hundred-and-eighty-first session made that legible rather than moving it** (ADR 0416): every other budget in this tree is spent by the document that reaches it, and this one was spent by whatever the process interpreted first — so `tools/safedocs survey` reported a different set of documents on every run and had done since the table existed, three quiet runs of one binary over 287 documents printing 30, 36 and 33 press refusals. **The five-hundred-and-eighty-second moved the payer** (ADR 0417). `colour::MAX_PRESSES` is a budget on one *interpretation*: `colour::Presses` holds the distinct presses a page has named, refuses the ninth, and is created once per `interpret_with` so that §11.4.7's pair of runs counts one press between them. What was a `static` table of `OnceLock`s is now an `Arc<Press>` carried in `Compositing` itself — the read stays a pointer already in hand, which is what the per-colour path needs — over a process-wide cache of `MAX_CACHED_PRESSES` sampled presses, most-recently-used first. The refusal a page can meet is therefore a function of its own bytes, and the survey's counts agree between runs; `press_beyond_this_process` and `reports_beyond_this_process` are removed, having existed only to name the half of a verdict that was not the file's. The population that made the old bound bite: 28 distinct presses over the crawl against a table of eight."

[[clause]]
clause = "11.7.3"
title = "Spot colours and transparency"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::the_all_colourant_is_a_complemented_tint", "crates/pdf-model/tests/shadings.rs::a_named_colour_space_is_resolved_against_the_resources_that_paint_it"]
note = "The clause offers two treatments for a spot colour inside a group — keep a component of its own, or \"be converted to its alternate colour space\" — and this tree always takes the second: a `Separation` or `DeviceN` colour goes through its tint transform into the alternate space at the moment it is read, before anything composites. That is conforming rather than a shortcut, and it makes the clause's one *shall* about soft masks true by construction: \"spot colours shall not be available in a transparency group XObject that is used to define a soft mask; the alternate colour space shall always be substituted in that case.\" What is given up is the first treatment's separation fidelity, which needs a raster per colourant and a device that has them."

[[clause]]
clause = "11.7.4"
title = "Overprinting and transparency"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::overprinting_changes_nothing_on_a_three_component_device"]
note = "Aggregate of the five below, and **the ledger's six `silent` rows the eighteenth session opened, closed by reading Table 146 rather than by writing a blend mode**. Four of the five subclauses are satisfied here by derivation: this device's group colour space has three additive *process* components and no spot colourants, and every row of Table 146 that such a space can reach gives the blend function the source colour `C_s`, in all three of its `OP false` / `OP true, OPM 0` / `OP true, OPM 1` columns — which is the Normal blend function, and is what this tree composites through. §11.7.4.5 has the row-by-row working. The fifth, §11.7.4.4, is `partial`: both of its populations — the four combined path-painting operators and text rendering modes 2 and 6 — build the knockout group it asks for, and both keep one report. The honest limit is one configuration: Table 146's first row needs the *group* space to be `DeviceCMYK`, which §11.4.7, §11.6.6 and §11.7.2 already report as a departure on 7 corpus documents, so overprinting's one visible case here is a gap that is already named. ADR 0028. **Read and kept in the five-hundred-and-sixty-fifth**: §11.7.4.4 is still the one `partial` child, and the honest limit is still Table 146's first row needing a `DeviceCMYK` group space, which §11.7.2 reports as a departure — so this row's debt is a gap already named one clause over rather than a second one."

[[clause]]
clause = "11.7.4.1"
title = "General"
status = "inapplicable"
note = "One paragraph describing overprinting in the *opaque* imaging model — §8.6.7's parameters — and two NOTEs. It states no requirement of its own, and the paragraph's own subject is settled by §8.6.7 on this device: overprinting deals with device colourants, and NOTE 2 says as much, \"it is best to think of transparency as taking place in appearance space, but overprinting of device colourants in device space\". This device produces no separations. The requirements are in the four subclauses below."

[[clause]]
clause = "11.7.4.2"
title = "Blend modes and overprinting"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The clause's requirement is what happens *without* the special blend mode, and this tree does it: \"For colour components whose value has not been specified, a source colour value of 1.0 shall be assumed; when objects are fully opaque and the Normal blend mode is used, this shall have the effect of erasing those components. This treatment is consistent with the behaviour of the opaque imaging model with the overprint parameter set to false.\" Every colour here is converted into all three components of the device raster and painted through Normal, so no component of the group space is ever left unspecified and there is nothing to erase. Its second half — that only separable, white-preserving blend modes apply to spot colours, and Normal is substituted otherwise — has no spot colourant to apply to (§8.6.6.4 requires an additive device to substitute the alternate space always), so it is true with nothing to do. The eighteenth session's note for this row quoted a sentence — \"the blend mode shall be replaced by the special overprinting blend mode, disregarding the current blend mode\" — that **is not in the standard**; the sentence it paraphrases is §11.7.4.3's, and that clause says *may*."

[[clause]]
clause = "11.7.4.3"
title = "Compatibility with opaque overprinting"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::overprinting_changes_nothing_on_a_three_component_device"]
note = "This is where the special overprinting blend mode is defined, and the clause's own verb is permissive: \"a PDF processor **may** consider implementing a special blend mode that consults the overprint-related graphics state parameters\". What settles the row is not the permission but the mode's *value* here, which the clause gives in two bullets. The first needs both the current and the group colour space to be `DeviceCMYK`; this device's group space is three additive components, so the second applies: \"In all other cases, the value of B(Cb,Cs) shall be Cs for all colour components specified in the current colour space, otherwise Cb.\" §11.7.3 makes every object paint every *process* component of the group space, so all three are specified and the blend function is `Cs` — Normal. NOTE 2 closes the one route that might have escaped: for \"Separation and DeviceN spaces that revert to their alternate colour space\" the current colour space *is* the alternate, which §8.6.6.4 makes unconditional on an additive device, so a spot colour cannot reach the first bullet either. The implicit non-isolated group the clause asks for when the current blend mode is not Normal is therefore unnecessary by its own NOTE 3."

[[clause]]
clause = "11.7.4.4"
title = "Special path-painting considerations"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/transparency_groups.rs::a_filled_and_stroked_path_is_one_object", "crates/pdf-model/tests/text_render_modes.rs::a_translucent_mode_2_glyph_is_one_knockout_group", "crates/pdf-model/tests/text_render_modes.rs::the_implicit_group_does_not_depend_on_the_text_knockout_parameter"]
note = "With overprinting disabled — which is this device's state always, §8.6.7 and ADR 0028 — the clause's first bullet cannot be reached and the second applies unconditionally: \"In all other cases, a non-isolated knockout group shall be established. Within the group, the fill and stroke shall be performed with their respective prevailing alpha constants and the prevailing blend mode. The group results shall then be composited with the backdrop, using an alpha value of 1.0 and the Normal blend mode.\" **The clause names two populations and this row described one of them for forty-nine sessions.** The path half — `B`, `B*`, `b`, `b*` — has been a knockout group since the seventy-second session under §11.6.2, and this row went on saying \"this tree emits a Fill and a Stroke\" and that implementing it \"needs §11.4.6's knockout groups\", which the seventy-first session drew. The other half is \"the painting of glyphs with text rendering mode 2 or 6\", and that was **neither implemented nor reported** until the hundred-and-twenty-first session: its NOTE 1 says the rule \"is independent of the text knockout parameter in the graphics state\", so §9.3.8's group — which needs `/TK` true *and* two glyphs overlapping — never stood in for it, and a single translucent outlined glyph got the double border NOTE 2 exists to prevent. Both halves now build the same `Command::Group`, and where §9.3.8's own group encloses the text object it holds the glyphs' parts flat, because a knockout group inside a knockout group computes the same picture and neither backend can state one. `partial` for the condition both halves keep: a portion whose shape is not the coverage it is drawn with, or an element that blends, is pushed flat and named (`knockout_is_drawable`). No corpus page reaches the text half — counted, by comparing every corpus page's display list either side of the change — and 2 documents still report the path half. **Read and kept in the five-hundred-and-tenth session**: `knockout_is_drawable` still gates both halves, called from `content/path.rs` and defined in `content/transparency.rs`."

[[clause]]
clause = "11.7.4.5"
title = "Summary of overprinting behavior"
status = "implemented"
code = ["crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::overprinting_changes_nothing_on_a_three_component_device"]
note = "Table 146 is the whole of what an implementation executes, and it collapses on this device. Its nine rows are indexed by the source colour space and by which component *of the group space* is affected; here the group space is device RGB, three process components and no spot colourants. Row by row: the three `spot colourant` rows — the only ones whose `OP true` cells give the backdrop `C_b` — have no component to affect, because there are none. Row 1's `OPM 1` cell, the only other departure from `C_s`, applies to a `C, M, Y, or K` component of the *group* space, which §11.7.4.3's first bullet confirms by requiring both spaces to be `DeviceCMYK`. Row 2 (`DeviceCMYK` source, process component other than CMYK), row 4 (any process space, process component) and row 9 (a group) give `C_s` in all three columns. The `Separation`/`DeviceN` rows are unreachable, since §8.6.6.4 makes such a space revert to its alternate on an additive device and NOTE 2 of §11.7.4.3 says the reverted space is the current one. So `B(C_b,C_s) = C_s` everywhere, which is Normal. NOTE 2's rule that a group reverts to Normal is the same answer arriving for a second reason. What this derivation rests on is that the device has no spot colourants and three process components: a separations-capable device would owe the whole table, and this row is where that would be recorded."

[[clause]]
clause = "11.7.5"
title = "Rendering parameters and transparency"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs"]
note = "Aggregate of the three below: which rendering parameters apply where objects overlap. The halftone half is §10.6's, which a screen does not perform, and the transfer function half is §10.5's, which this tree honours per object since the three-hundred-and-fifty-eighth session and does not track per *region* — §11.7.5.2's row says why that is still `inapplicable` and **this sentence said instead that both describe a marking device, the phrase ADR 0204 retired, until the three-hundred-and-seventy-fifth**; the colour-conversion half is honoured trivially, because a colour is converted once, at the moment it is read, rather than at a group boundary."

[[clause]]
clause = "11.7.5.1"
title = "General"
status = "inapplicable"
note = "Prose dividing the rendering parameters into the two categories the two subclauses below treat. No requirement of its own."

[[clause]]
clause = "11.7.5.2"
title = "Halftone and transfer function"
status = "inapplicable"
code = ["crates/pdf-model/src/content.rs"]
note = "Requires an implementation to track the halftone and transfer function per *region* of the page, since the topmost fully opaque object at each point decides them. The halftone is §10.6's, which a screen does not perform: `/HT`, `/BG` and `/UCR` are named in `content.rs` only as the `/ExtGState` entries §8.6.8 forbids an uncoloured figure to set. **The transfer function is honoured**, since the three-hundred-and-fifty-eighth session — and this row stays `inapplicable` for its own reason rather than for §10.5's: what the clause asks for is *per-region* tracking, decided by the topmost fully opaque object at each point, and that needs a second transfer function competing with a first inside a transparency group. `content::Transfer` is a graphics state parameter applied at each mark, so the topmost object's is the one in force by construction wherever only one is stated, which is every corpus document that states any (13 state a `/TR` or `/TR2` and one states anything but `/Identity`). **This row's first sentence said both parameters described a marking device and that this tree read neither, four sentences above a paragraph of its own saying §10.5 had stopped being `inapplicable`** — `doc/todo/01`'s sixth shape, a note that contradicts itself because a correction was appended rather than read against what came before it. Corrected in the three-hundred-and-seventy-fifth."

[[clause]]
clause = "11.7.5.3"
title = "Rendering intent, black point compensation and colour conversions"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::a_cmyk_colour_is_the_same_however_it_is_drawn"]
note = "The clause's subject is *when* a conversion happens: in the transparent model the target may be a group's colour space rather than the device's, so the intent and the black point compensation in force at that moment are the ones that apply. Here every colour is converted to device RGB at the moment it is read, so the parameters in force are always the object's own — which is what the clause asks for, by a route that makes the question unaskable. `/RI` and `/UseBlackPtComp` are read and reach that conversion (§8.6.5.9, ADR 0012) — **and since the six-hundred-and-seventh session they reach every one of them rather than an operator's colour alone**, which is this clause's own second paragraph: the parameters \"may need to be applied earlier than the actual rendering of colour onto the page\", and a shading's ramp, a mesh's vertices and an image's samples are exactly that \"earlier\", converted at build or decode time rather than at the paint. `crate::colour::Conversion` is what carries them there. What is not done is the second half: black generation and undercolour removal \"shall be applied only during conversion from DeviceRGB to DeviceCMYK\", which this tree never performs, and the selection of an ICC profile's `A2B` table by intent, which §8.6.5.8 already records. **The black-generation and undercolour-removal half is read as of the four-hundred-and-twenty-sixth session, where it decides something.** \"When painting an elementary object with a DeviceRGB colour directly into a transparency group whose colour space is DeviceCMYK , the functions used shall be the current black-generation and undercolour-removal functions in effect in the graphics state at the time of the painting operation\" — which is the conversion §11.4.7's page group needs and which this tree declines (see §11.7.2's row). Table 57's `/BG`, `/BG2`, `/UCR` and `/UCR2` are still not evaluated, and a page that states one is now *reported* rather than silently drawn with the defaults: `Interpreter::black_generation_stated`, one of the 69 sampled web witnesses. **And the four-hundred-and-twenty-seventh read the clause's *first* paragraph, which is the one that decides a branch.** The rendering intent governs \"the conversion from a CIE-based colour space to a target colour space, taking into account the target space's colour gamut (the range of colours it can reproduce)\", and in the transparent model that target may be a group's colour space — so the conversion into `DeviceCMYK` is the same conversion as the one onto the device, on §10.3's branch, and not §10.4.2.4's. `colour::rgb_to_ink` performs it. Two halves of this clause are therefore in different states: the target-space half is implemented, and the black-generation half is still a *report*, because Table 57's `/BG`, `/BG2`, `/UCR` and `/UCR2` are read as a condition and not evaluated. What replaces them is stated rather than assumed: this device's default black generation returns the most black that still reproduces the colour, which is inside the freedom the clause grants — \"it may return a larger value for extra black, a smaller value for less black, or 0.0 for no black at all\" — and undercolour removal is computed by the same search rather than asked of a function. ADR 0263. **The stated-black-generation condition gates the group-scoped conversion too since the four-hundred-and-ninety-second**: a group that would otherwise composite in its own four components (§11.6.6, ADR 0327) keeps the report instead where Table 57's functions are stated, checked before the pair is attempted and again after its first run, because a `gs` inside the group can state one."

[[clause]]
clause = "12.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/actions.rs::a_hide_action_stops_an_annotation_being_drawn", "crates/pdf-model/tests/links.rs::a_links_region_is_found_and_its_destination_resolves"]
note = "Clause 12's own map: annotations, forms, actions, navigation, digital signatures. `CLAUDE.md` states the split this row records — \"everything that displays\" is in scope and *behaviour* is not, with JavaScript excluded by name — and after twelve sessions on this clause **no row of it is `silent`**, which as of the hundred-and-first session is true of the whole ledger. Every row is `implemented`, `partial`, `reported`, `inapplicable` or `out-of-scope`: §12.2's preferences, §12.3's destinations, outline, thumbnails and collections, §12.4's labels, articles and presentations, §12.5's annotations and their constructed appearances, §12.6's twenty action types with ten performed, §12.7's field appearances, its reset action, §12.7.8's forms data format read whole and §12.7.7's named pages that its templates come from, §12.8's signatures read, their byte range digested and their signature value verified under the key in the certificate they carry, §12.9's measurements, §12.10's geospatial dictionaries and §12.11's requirements. This row said \"[n]one of the parts that make a document interactive\" while a click had been following links since the fifty-seventh session. **Read and kept in the five-hundred-and-fifty-third**, with the counts in it checked against the rows they summarise rather than believed: §12.6's row says ten of Table 201's twenty and §12.6.4's says eight of the seventeen it documents, and ten is those eight plus §12.7.6's two."

[[clause]]
clause = "12.2"
title = "Viewer preferences"
status = "partial"
code = ["crates/pdf-model/src/viewer_preferences.rs", "crates/pdf-model/src/page.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/src/viewer_preferences.rs::table_147s_entries_are_read", "crates/pdf-model/tests/page_geometry.rs::view_area_and_view_clip_choose_what_is_displayed_and_what_is_clipped"]
note = "Every entry of Table 147 is read, and the two that decide pixels are applied. `/ViewArea` and `/ViewClip` name §14.11.2 boundaries — which of the five is displayed, and which the contents are clipped to — so a document may show the media box while clipping the ink to the trim box, and `Page::display_box` and `Page::clip_box` are those two answers. Both are deprecated in PDF 2.0 and both are read, for the reason §8.6.5.1's withdrawn `CalCMYK` is: deprecation tells a *writer* what to stop doing. The rest is data for chrome a *host* owns — hiding a tool bar, centring a window, a print dialogue's copy count. **Since the hundred-and-thirty-seventh session they are handed over rather than merely read**: `viewer-core`'s `Query::Preferences` answers with Table 147 whole, which is the most this crate can do about a window it has none of by construction. The reason this row used to give — \"a window this program does not have\" — expired in the hundred-and-thirty-second session when `pdf-viewer.rs` became a host, and the sweep that found it was looking for exactly that: a note whose *reason* has gone stale. Consumer #1 still hides nothing, because it has no tool bar to hide — and `/PrintArea` and `/PrintClip` are read and unused for the same reason as `/Duplex`: nothing here prints. Two entries have conditions applied rather than stored: an unrecognised `/PrintScaling` \"shall\" be `AppDefault`, and Table 148 then forbids `/Enforce` naming it. **`/DisplayDocTitle` is honoured as written since the two-hundred-and-ninety-fourth session, and the departure it carried for a hundred and twenty-one is closed.** Table 147 names \"the document title taken from the `dc:title` element of the XMP metadata stream\", and since ADR 0186 this program reads that stream, so `pdf-viewer.rs`'s `named` asks `Xmp::title` first. §14.3.3's `/Info /Title` is the *fallback* now rather than the substitution — used where the document names no metadata stream, where the packet states no `dc:title`, or where the packet could not be read, and the last of those three is printed because it is the only one where this program failed at something. Table 349's NOTE 1 is why the fallback is a reading rather than a guess: \"[t]he `dc:title` entry … **can be used to represent** the document's title\". Measured over the corpus: 93 documents state a title in both places and exactly one disagrees, which is what makes the ranking matter for a single file rather than for a population. 58 of 974 documents state a `/ViewerPreferences`; **none states any of the four boundary entries**, so the half of the clause that can change a pixel has no corpus witness and was written from the clause. **Re-read off the blame list in the five-hundred-and-seventeenth**: `Query::Preferences` still answers with Table 147 whole, and `/PrintArea`, `/PrintClip` and `/Duplex` are still read and unused because nothing here prints — a boundary rather than a debt. **And one sentence of \"chrome a host owns\" stopped being true in the six-hundred-and-sixth**: Table 29's `/PageLayout` — §7.7.2's entry, which this clause's own subject is the neighbour of — is arranged by `viewer-core` now rather than handed over, so the pages a window shows and where each of them sits is this tree's answer and not a host's. Table 147's `/Direction` is the entry that reaches into it and is deliberately declined: \"may be used to determine the relative positioning of pages when displayed side by side or printed n -up\" is a permission, and `/PageLayout` has already said which side its odd-numbered pages are on. What is still chrome, and still handed over whole, is the rest of Table 147."

[[clause]]
clause = "12.3"
title = "Document-level navigation"
status = "partial"
code = ["crates/pdf-model/src/destination.rs", "crates/pdf-model/src/outline.rs", "crates/pdf-model/src/thumbnail.rs"]
test = ["crates/pdf-model/tests/destinations.rs::an_open_action_that_states_a_destination_names_a_page", "crates/pdf-model/tests/outlines.rs::a_stated_count_agrees_with_the_count_the_clause_defines", "crates/pdf-model/tests/thumbnails.rs::every_thumbnail_decodes_and_is_compared_with_the_page_it_stands_for", "crates/viewer-ui/tests/panel.rs::the_pages_tab_draws_a_thumbnail_and_a_click_goes_to_its_page"]
note = "Document-level navigation: an outline, thumbnails, destinations, collections and navigators. Three of the five are read — §12.3.2's destinations in all three spellings, §12.3.3's whole outline hierarchy, and §12.3.4's thumbnail images decoded with Table 87's insignificant entries dropped — and **all three now have a panel**: §12.3.3's outline since the hundred-and-sixty-sixth session and §12.3.4's thumbnails since the two-hundred-and-sixty-sixth, with §12.3.2's destinations reached by a click in either. §12.3.5's collections and §12.3.6's navigators are `partial` rather than `silent` — this sentence said `silent` for as long as anybody looked and their own rows did not. **And \"both … read as data with nothing presenting them\" was false of the first of the two, from the three-hundred-and-fifty-second session to the four-hundred-and-second** (found by the tenth sweep, on this row's own count): `viewer_ui::chrome`'s files tab *is* §12.3.5's presentation, which is the `shall` that clause states — \"the interactive PDF processor shall present the document as a portable collection\" — with the folder tree as indented rows and the schema's visible columns in Table 155's `/O` order (ADR 0202), and §12.3.5.1's `/D` fallback answered to a host since the three-hundred-and-ninety-fourth (ADR 0231). §12.3.6's navigators are still read with nothing drawing one, which is what that row's own `partial` is for. **No pdf.js document states a `/Collection`**, which is why that pair is last rather than next — but the corpus is wider than pdf.js and this sentence used to say \"[n]o corpus document … at all\": `doc/corpora/format-corpus/pdfCabinetOfHorrors/digitally_signed_3D_Portfolio.pdf` states one, with eight schema fields and a `/Folders` tree. ADR 0406. **This row said \"[n]ot one of them is implemented\" for forty sessions after §12.3.2 and §12.3.3 landed**, which is the understated-parent shape §14.8.5.1 and §12.4 taught: a parent row is not maintained by the sessions that close its children. **Read and kept in the five-hundred-and-sixty-second**, and the count was checked against the rows rather than believed: of the five subclauses §12.3.1 is `implemented`, §12.3.3 is `implemented`, §12.3.2 and §12.3.4 are `partial` for debts their own notes name, and §12.3.5 and §12.3.6 are both still `partial` — which is what this note says of the last pair, so the sentence that was wrong in the four-hundred-and-second has stayed corrected."

[[clause]]
clause = "12.3.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/outline.rs", "crates/pdf-model/src/thumbnail.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/tests/outlines.rs::a_document_with_an_outline_produces_its_items", "crates/pdf-model/tests/thumbnails.rs::every_thumbnail_decodes_and_is_compared_with_the_page_it_stands_for"]
note = "The framing: an outline and a set of thumbnails, each item associated with a destination. Both are read as data, and **one of the two is now a surface**: since the hundred-and-sixty-sixth session `viewer-ui` draws §12.3.3's outline in a panel of its own, with `pdf-font`'s compiled-in Helvetica and a `pdf-render` display list, and a click on a title sends `Command::GoTo(PageTarget::Destination)`. **And the other of the two is a surface since the two-hundred-and-sixty-sixth session**: `viewer_ui::chrome`'s fifth tab draws §12.3.4's miniatures, one row per page, with §12.4.2's label under each and a click showing that page. This row's `partial` was that panel, named alone — \"which §6.3.2.2's ranking and `CLAUDE.md`'s scope statement both make owed\" — so the clause's framing is answered on both halves: an outline and a set of thumbnails, each item associated with a destination, each of them reachable. §12.3.3 itself closed in the hundred-and-sixty-eighth session, when `Command::Activate` gave an outline item the other half of its sentence."

[[clause]]
clause = "12.3.2"
title = "Destinations"
status = "partial"
code = ["crates/pdf-model/src/destination.rs"]
test = ["crates/pdf-model/tests/destinations.rs::an_open_action_that_states_a_destination_names_a_page"]
note = "Destinations, in all three of their spellings — the explicit array, the structure element and the name — read into one type by `Destination::read`, because every caller in the standard may be handed any of them. What a destination *names* is computed; what a *window* would do with it is not, and the split is stated on §12.3.2.1's row. The catalog's `/OpenAction` was the first consumer, and it is the only destination a viewer must resolve without anybody clicking anything. **All four callers the clause names now have their gesture**: a link annotation since the fifty-seventh session, a go-to action since the sixty-second, and an outline item since the hundred-and-sixty-sixth, where `PageTarget::Destination` carries one across the `viewer-core` boundary — a host cannot resolve one itself, because turning a page *object* into an index is a walk of the page tree. **What this row owes is its one unsettled child**, named here rather than left for a reader to find: §12.3.2.2 waits on the remote file §12.6.4.3 declines to open, which is the only page a destination can name that this program cannot reach. §12.3.2.1, §12.3.2.3 and §12.3.2.4 are `implemented`. Read off the blame list in the five-hundred-and-seventeenth, where the sweep for a `partial` note naming nothing owed had this row too."

[[clause]]
clause = "12.3.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/destination.rs", "crates/viewer-core/src/open.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/viewer-core/tests/headless.rs::an_open_action_states_where_to_look_at_the_page_and_how_large", "crates/viewer-core/tests/headless.rs::a_destinations_null_parameters_leave_what_they_do_not_state", "crates/pdf-model/tests/destinations.rs::an_open_action_that_states_a_destination_names_a_page"]
note = "The clause lists a destination's three items and all three are answered since the two-hundred-and-first session. \"[T]he page of the document that shall be displayed\" is a property of the document and `Destination::page_index` computes it. \"[T]he location of the document window on that page\" and \"[t]he magnification (zoom) factor\" are properties of a *window*, so `pdf-model` stops at carrying Table 149's parameters and `viewer_core::Open::apply_view` turns them into a zoom and a scroll — the one place with both a viewport and a display list, the second of which the three `/FitB` forms need. **This row said they were \"properties of a window with scrolling and zoom, which this program does not have\" and that reason expired in the hundred-and-thirty-second session**, sixty-nine before the sweep found it: `Command::Zoom` and `Command::Scroll` had been in the vocabulary the whole time. It is the capability shape §12.6.3 and §14.3.3 took, and the third instance of it. ADR 0162. 55 of the 974 corpus documents state an `/OpenAction` and 49 name a page this reader finds; the viewer opens there, at the magnification and on the part of the page the file asked for."

[[clause]]
clause = "12.3.2.2"
title = "Explicit destinations"
status = "partial"
code = ["crates/pdf-model/src/destination.rs"]
test = ["crates/pdf-model/src/destination.rs::every_form_table_149_lists_is_read", "crates/pdf-model/src/destination.rs::a_null_parameter_is_not_a_zero"]
note = "All eight of Table 149's forms are read, with the table's own distinction between an absent parameter and a zero one: \"A null value for any of the parameters left , top , or zoom specifies that the current value of that parameter shall be retained unchanged.\", and \"[a] zoom value of 0 has the same meaning as a null value\" — so `View` carries `Option<f32>` and a stated zero coordinate stays a coordinate. The clause requires the first entry to be \"an indirect reference to a page object (except in a remote go-to action …)\", and the NOTE makes the integer form belong to another document, so `Target::Number` names nothing here; no corpus link uses it and one `/OpenAction` does, where the clause's fallback and the lenient reading agree on page one anyway. **Both of the things a window does are done since the two-hundred-and-first session** (ADR 0162): `viewer_core::Open::apply_view` turns each of the eight forms into a magnification and a point of the page put at the window's top-left corner, and \"the smallest rectangle enclosing all of its contents\" — the bounding box the three `/FitB` forms need, which no page dictionary states — is the union of `pdf_render::marked_bounds` over the display list's commands. Table 149's null rule is honoured separately for each parameter rather than for the destination as a whole, because that is what it says: \"A null value for any of the parameters left , top , or zoom specifies that the current value of that parameter shall be retained unchanged.\". A `/FitB` on a page whose commands cannot all be bounded falls back to the page box, which is stated in the code rather than hidden. **And the clause bounds the bounding box in the same paragraph, which nothing here read until the five-hundred-and-seventeenth**: \"If any side of the bounding box lies outside the page's crop box, the corresponding side of the crop box shall be used instead\". `interpret` puts the displayed box at the *origin* rather than clipping to it, so the display list carries marks off the page's edge and `content_bounds` unioned them — a `/FitB` on such a page magnified to fit ink this viewer never draws. `viewer_core::open::content_box` cuts the box to the page first. This row stayed `partial` for `Target::Number` alone until the same session, and that was the wrong name for the wait: the integer form is *read* here and `Destination::page_index_in_target` resolves it against §12.6.4.4's embedded target, where Table 203 numbers the first page 0. What no reader can resolve is a page number in a file it does not have, and declining to open one is §12.6.4.3's refusal rather than this clause's gap — so `partial` here is that wait, named where it lives."

[[clause]]
clause = "12.3.2.3"
title = "Structure destinations"
status = "implemented"
code = ["crates/pdf-model/src/destination.rs"]
test = ["crates/pdf-model/src/destination.rs::a_structure_destination_finds_its_page_through_the_kids", "crates/pdf-model/src/destination.rs::the_fallback_to_page_one_belongs_to_structure_destinations_only"]
note = "A destination whose first entry is a structure element, and the clause states the whole algorithm for getting back to a page: the kids in linear array order, a marked-content or object reference answering with its page, a child element recursed into, and \"[i]n the case where no page content is identified, then the page reference shall be assumed to be the first page in the document\". **It needs `/K` and `/Pg` and not the structure tree**, which is why this row closes while §14.7's stay `silent` — the row said the opposite for two sessions. The page a reference \"belongs to\" is Table 357's and Table 358's `/Pg`, falling back to the containing element's, which §14.7.5.3 states in as many words. The fallback to page one is a sentence about structure destinations and is not applied to any other unresolvable reference. **The clause has a caller since the four-hundred-and-eighty-fourth session**: §12.6.4.2's `/SD`, which is Table 202's entry — Table 203's names one in a file this reader will not open. Before that the algorithm was reachable only where a link's or an outline item's `/Dest` array happened to begin with a structure element. **This sentence called Table 202's `/SD` \"the only entry in the standard that states a structure destination in *this* document\" and that was false, which reading Errata Collection 3 found** (Issue #162 and #288, `/State` `Review` `Completed`): §12.3.2.4's named destination may be \"a dictionary with a D entry whose value is such an array\" that \"may also optionally contain an SD entry\", and that is a second one. The erratum does not add the entry — the 2020 sentence stated it too — it repoints its definition, which had cited Table 201 and now cites Table 202, Table 203 and Table 204, so the precedence rule a named destination's `/SD` obeys is reachable from §12.3.2.4 for the first time. `Destination::of_go_to` applies it; `Destination::read_within` reads `/D` alone. §12.3.2.4's row carries the debt."

[[clause]]
clause = "12.3.2.4"
title = "Named destinations"
status = "implemented"
code = ["crates/pdf-model/src/destination.rs", "crates/pdf-syntax/src/tree.rs"]
test = ["crates/pdf-model/src/destination.rs::a_named_destination_is_found_in_either_table", "crates/pdf-model/src/destination.rs::a_named_destinations_structure_destination_takes_precedence_over_its_page_destination", "crates/pdf-model/tests/destinations.rs::a_named_destination_resolves_when_its_document_defines_it"]
note = "Both of the clause's tables — the PDF 1.1 `/Dests` dictionary in the catalog and the PDF 1.2 `/Dests` name tree in the name dictionary — and both key forms, with the dictionary-valued destination's `/D` followed. Annex J.3.3 and J.3.4, which the clause requires the lookup to use, cost nothing here: both reduce to a binary comparison of the *decoded* bytes, which is what the lexer has already produced. The corpus measures the clause's own pairing and finds it exact: of 106 named destinations reachable from link annotations, the 22 that resolve are 2 names in a catalog dictionary and 20 strings in a name tree, not one crossing. The other 84 name keys their own document does not define — five files carry named links and no table at all — which is a fact about those files and is asserted as one. **`/D` alone is not the whole of the dictionary form, and Errata Collection 3 is what made that legible** (Issue #162 and #288, `/State` `Review` `Completed`). The clause's own sentence gives the dictionary an `/SD` beside its `/D` — the 2020 printing said \"may optionally contain an SD entry as defined in [Table 201]\", which names the wrong table, and the amended text says \"In PDF 2.0, this dictionary may also optionally contain an SD entry\" and points at Table 202, Table 203 and Table 204. Table 202 is where the rule lives: \"[i]f present, the structure destination should take precedence over destination in the D entry\". `Destination::of_go_to` obeys it for §12.6.4.2's action and `Destination::read_within`, which is what reads a *named* destination's value, takes `/D` and never looks for `/SD` — so a document that states both gets the weaker of the two, silently, and §12.3.2.3's algorithm is not reached although this reader has had it since the four-hundred-and-eighty-fourth session. **Read since the five-hundred-and-ninety-fifth session**, and by one function rather than two: `Destination::preferring_structure` is what both §12.6.4.2's action dictionary and this clause's destination dictionary ask, because the clause makes them one entry — the `/SD` here is the one \"defined in\" the action's own table — and two readings of one sentence is how the two would come to disagree about which entry wins. An `/SD` that does not resolve falls back to `/D`, because the precedence is a `should` and `/D` is the required entry. **The corpus cannot rank it and that is said out loud rather than around** (trap 8): `examples/structure_destination_census` over **66 901 documents** — the 974 pdf.js corpus and 65 937 crawled — finds **374 101 named destinations and not one of them states an `/SD`**, so the witness is a hand-built pair differing in the single entry, reached once through a catalog `/Dests` name and once through a name tree string."

[[clause]]
clause = "12.3.3"
title = "Document outline"
status = "implemented"
code = ["crates/pdf-model/src/outline.rs", "crates/viewer-core/src/interact.rs", "crates/viewer-ui/src/chrome.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs", "crates/viewer-host/src/panel.rs", "crates/viewer-ffi/src/panels.rs"]
test = ["crates/pdf-model/src/outline.rs::the_visible_count_skips_a_closed_items_descendants", "crates/pdf-model/tests/outlines.rs::a_stated_count_agrees_with_the_count_the_clause_defines", "crates/viewer-ui/tests/panel.rs::closing_a_subtree_does_not_renumber_the_items_below_it", "crates/viewer-core/tests/headless.rs::an_outline_item_goes_to_the_page_its_destination_names", "crates/viewer-core/tests/headless.rs::an_outline_item_whose_action_is_a_uri_hands_it_over"]
note = "The whole hierarchy is read: Table 150's root, Table 151's items with `/Title` as a §7.9.2.2 text string, a destination from `/Dest` or from a go-to action's `/D`, Table 152's italic and bold bits, `/C` clamped to `DeviceRGB`, and the sign of `/Count` deciding whether an item is open. The walk follows `/First` and `/Next` only — `/Prev`, `/Last` and `/Parent` are redundant with what it already has and could only disagree — and refuses to visit an object twice, because all six links are indirect references a producer can get wrong. `/SE` is unread and the clause says why: \"[t]his value is not intended for navigation\". **`/Count` is stated as a three-step algorithm rather than as a number**, so a document states the same fact twice and can be checked against itself: 144 of the 146 corpus documents that state one over a non-empty outline agree with the steps run over their own items, and the two that do not contradict *themselves* — `nested_outline.pdf` marks three items open with `/Count 2` apiece, which makes nine visible, and says 3. **The three sentences the clause spends on a screen arrived in the hundred-and-sixty-sixth and -eighth sessions and this row closed with them.** The items appear \"in the order in which they occur in the linked list\"; \"[t]he user may interactively open and close individual items by clicking them\", which is a disclosure triangle whose initial state is the sign of `/Count`; \"[w]hen an item is closed, all of its descendants in the hierarchy shall be hidden\"; and \"[c]licking the text of any visible item activates the item, causing the interactive PDF processor to jump to a destination or trigger an action associated with the item\" — **both halves**, since `Command::Activate` hands the item's object to `viewer-core` and the whole of §12.6.2's chain runs, which is the same code a link click takes. Measured before it was built: of 281 corpus outline items carrying an `/A`, 249 are `GoTo`, 7 `URI`, 5 `Named`, 1 `SetOCGState`, 1 `GoToR` and 18 `JavaScript`, and every one of those is now performed or refused by name on the same terms a link gets. What a refused action costs is §12.6's rows and not this one. `section_at` stays beside the panel in the title bar, and that mapping from a page back to an item is a **documented choice** — the clause states nothing about it. **Table 151's `/Title` is \"[t]he text that shall be displayed on the screen for this item\", and until the three-hundred-and-sixteenth session a title §9.6.2.2's fourteen state no code for was displayed as nothing at all**: 7 of the 150 corpus documents with an outline state one, and three of those titles drew as empty rows. `Chrome::text` draws a box per character now and `Chrome::width` measures it, which is this host's choice and not a reading — the standard says nothing about an interface's own text (ADR 0195). What a box cannot say is *which* character; `doc/todo/27` carries that question and the measurement behind it. **And part of it was an encoding rather than a font**, found in the four-hundred-and-ninety-first session: a code is one byte, so the panel reached §9.6.5.2's 149 characters while the compiled-in face states 668, and one of those seven documents' titles was boxed for characters the face has. Six still are, over 44 characters of scripts the binary does not carry. ADR 0326. **`Answer::Outline` is owned since the four-hundred-and-eleventh session** and borrowed the viewer before it, which was a `viewer-ui`-shaped choice rather than a general one: all five consumers cloned it, because a panel outlives the query that filled it. Measured before it was changed — `viewer-host --example outline_census`, median of five — the whole answer is 481 ns for the five-page application note's fourteen items and 80.7 µs for this standard's 988, against the 3.66 ms ADR 0246 measured to build the three panel models those rows go into. And it crosses a **third** kind of host now: `viewer-ffi` flattens it depth first with a depth on every row, which is the one shape a C ABI cannot hand over as itself and is what `viewer-qt` already builds internally because `QAbstractItemModel` must answer for any node at any moment. A C caller reads a row's title, its depth, the sign of its `/Count` and §7.3.10's two numbers, and sends `Command::Activate` with them — so the clause's 'clicking the text of any visible item activates the item' is now performed from three languages. ADRs 0246, 0247. **And a second consumer of the same hierarchy arrived in the four-hundred-and-twenty-first session, which is a *capability* over this clause rather than a requirement of it**: `pdf_model::retrieval::sections` turns each item's destination into the range of pages the item's text occupies — from its own destination to that of the next item which is not one of its descendants — so that a program can ask for §9.6.5.4 and be given it. 988 items of ISO 32000-2 resolve, 946 of them carrying a clause number in their `/Title`. Nothing in the clause says a title has a number in it or that a section ends where the next begins; both are documented as choices in ADR 0257 and neither moves this row's status."

[[clause]]
clause = "12.3.4"
title = "Thumbnail images"
status = "partial"
code = ["crates/pdf-model/src/thumbnail.rs", "crates/pdf-model/src/image.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/src/thumbnail.rs::only_the_five_significant_entries_of_table_87_survive", "crates/pdf-model/src/thumbnail.rs::an_ignored_soft_mask_does_not_reach_the_pixels", "crates/pdf-model/tests/thumbnails.rs::every_thumbnail_decodes_and_is_compared_with_the_page_it_stands_for"]
note = "A page's `/Thumb`, decoded into the same RGBA raster every other image becomes — and the clause's work is *subtraction*: \"only the Width , Height , ColorSpace , BitsPerComponent , and Decode entries are significant; all of the other entries listed in `Table 87 -Additional entries specific to an image dictionary` shall be ignored if present\". `thumbnail::significant` drops those eighteen, so a `/SMask` or a `/Mask` on a thumbnail reaches no pixel, while `/Filter`, `/DecodeParms` and `/Length` survive because they are **Table 5's** and the sentence names Table 87 — which the clause's own EXAMPLE requires, writing `/Filter [/ASCII85Decode /DCTDecode]`. The clause's two producer-side constraints are recorded rather than enforced: a `/Subtype` other than `Image`, and a colour space that is not `DeviceGray`, `DeviceRGB` or `Indexed` on one of them. 11 corpus documents state a thumbnail on page one, all 11 decode, one (`issue19517.pdf`) states an `ICCBased` space the clause does not permit. **The panel arrived in the two-hundred-and-sixty-first session** — `viewer_ui::chrome`'s fifth tab, one row per page, the miniature fitted to a box seven row heights tall with §12.4.2's label under it and a click showing that page — so Table 29's `/PageMode /UseThumbs` names a panel that exists and four of its six values now do. `Query::Thumbnail` answers **one page at a time**, which is principle 2 with this clause behind it: the NOTE says thumbnails \"are not required, and can be included for some pages and not for others\", so a thousand-page document would otherwise decode a thousand miniatures to draw eight, and the host builds its list only when the tab is first shown. Three things the clause does not state are this program's and are recorded as choices: the box's size, the miniature being *fitted* to it rather than drawn at its own sample size (§8.9.5.3's `/Interpolate` is where the file gets a say in how that magnification looks), and a page with no `/Thumb` still being a row — a panel that listed only the pages that have one would be a list of the document's thumbnails rather than of its pages. `partial` for **generating** a missing thumbnail, which the clause permits and `CLAUDE.md` principle 2 forbids on the launch path. **Re-read off the blame list in the five-hundred-and-seventeenth**: `thumbnail::significant` still drops the eighteen, `Query::Thumbnail` still answers one page at a time, and the two producer-side constraints are still recorded rather than enforced, which is what `writer-side` means one row over."

[[clause]]
clause = "12.3.5"
title = "Collections"
status = "partial"
code = ["crates/pdf-model/src/collection.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/src/collection.rs::the_clauses_own_email_inbox_reads_as_five_columns", "crates/pdf-model/src/collection.rs::a_navigator_names_layouts_in_the_order_the_file_prefers_them", "crates/pdf-model/tests/collections.rs::no_pdfjs_document_is_a_portable_collection", "crates/pdf-model/tests/collections.rs::a_producers_own_portable_collection_is_read_with_its_folders", "crates/viewer-ui/tests/panel.rs::a_collection_puts_its_files_in_folders_with_the_schemas_columns"]
note = "Table 153 whole, with Tables 154 to 158 behind it: the schema's fields with their two kinds of subtype, `/D`, `/View`, `/Sort`, `/Colors`, `/Split`, `/Navigator` and `/Folders`. Three of its rules are answered rather than stored, because each is a decision a reader makes: `Collection::initial_document` is §12.3.5.1's three fallbacks for `/D` — the container, the first file, or \"an empty preview window\" — as three values rather than an `Option`, and **a host has asked it since the three-hundred-and-ninety-fourth session** (ADR 0231), which is where that clause's row has the argument; `Sort::ascending` applies Table 156's single-boolean-or-array rule; and `FieldKind::is_in_the_item` is the clause's own division between fields whose data is in §7.11.6's `/CI` and \"file-related fields\" whose data is already in the file specification. **The sentence a viewer owes is paid since the three-hundred-and-fifty-second session** (ADR 0202): \"[i]f this dictionary is present in a PDF document, the interactive PDF processor shall present the document as a portable collection\". `viewer_core::Query::Collection` answers with Table 153 whole and `viewer_ui::chrome`'s **files tab becomes that presentation** where a document states one — §12.3.5.2's folder tree as indented rows, each file under the folder its name-tree key names, with the schema's *visible* columns in Table 155's `/O` order as the row's detail. A key that names no folder is drawn at the root, which is the clause's own rule for one. A click still sends `Command::Extract` with the tree's key, folder number and all. **Not a seventh tab**: it is the same files, and a collection is how a document says they are arranged — so the tab a person already looks in for embedded files is where the arrangement belongs, and six labels is what 300 logical pixels hold. **The decision nobody had taken is taken**: the container's own pages **stay on the screen**. The clause says to present the document as a collection and does not say instead of what; §7.6.7's unencrypted wrapper settles it, since a wrapper's whole purpose is a page saying the payload is encrypted and Table 153's `/View H` is how such a document asks for the list to start hidden. A viewer that replaced the page with a file browser would hide the sentence the wrapper exists to show. The reason this row used to give — \"which needs a file browser\" — expired in the hundred-and-sixty-sixth session and was found by `doc/todo/01`'s fifth sweep in the three-hundred-and-thirteenth. `partial` for what a panel of this width does not draw: Table 153's `/View T` tile mode, `/Sort`'s order, `/Colors`, `/Split` and §12.3.6's `/Navigator` layouts are all *presentation* entries this host answers with one presentation — read, carried, and not offered as alternatives. **One corpus document states a `/Collection`** — `digitally_signed_3D_Portfolio.pdf`, under `doc/corpora/format-corpus`, with eight schema fields and a `/Folders` tree — and this row said \"[n]o corpus document\" for as long as it existed, because the count had only ever been taken over pdf.js (ADR 0406). The clause's own EXAMPLE 1, an email in-box of five columns, is still the model's test: one file cannot exercise Table 155's field subtypes, Table 156's sort rules and Table 158's splitter between them. **Read and kept in the five-hundred-and-fifty-third**: `Collection::initial_document`, `Sort::ascending` and `FieldKind::is_in_the_item` are all three still in `collection.rs`, and `Query::Collection` is still what carries them to a host."

[[clause]]
clause = "12.3.5.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/collection.rs", "crates/viewer-core/src/viewer.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/src/collection.rs::the_clauses_own_email_inbox_reads_as_five_columns", "crates/viewer-core/tests/headless.rs::a_collections_initial_document_reaches_a_host", "crates/viewer-ui/tests/panel.rs::a_collections_initial_document_is_the_row_set_in_bold"]
note = "The general clause and its tables, read in `collection.rs`. What makes a collection *presented* rather than merely described is §12.3.5.1's \"shall present the document as a portable collection\", which is a user interface; everything the interface would read is here. Table 153's `/View` defaults to `D`, and the one place the entry is load-bearing rather than a preference is §7.6.7's unencrypted wrapper document, where the clause requires `H` — a browser over the wrapper would hide the page that says the payload is encrypted. **`/D` is answered and a host acts on it since the three-hundred-and-ninety-fourth session.** `Collection::initial_document` has answered this clause's own fallbacks since the three-hundred-and-fifty-second — the container, a named embedded file, \"the first item from the list of files\", or \"an empty preview window\", as four values rather than an `Option` because they are four instructions — and for thirty-four sessions **no host could call it**, because it needs the `&Document` that only `viewer-core` holds; `doc/todo/01`'s fifth sweep found that in the three-hundred-and-eighty-sixth. It was never a transport gap, and the fix is on the same argument: `Answer::Collection` carries the resolved `Initial` beside Table 153, the confined protocol encodes its four cases, and `viewer_ui::chrome` sets the initial document's row apart — in bold, which is a choice because the clause states no appearance — while an empty tree says so instead of drawing nothing. `partial` now for Table 153's `/View`: `D`, `T` and `C` are three presentations of one collection and this panel draws one of them, which is §12.3.5's `partial` for the same reason. `H` is met by construction, the sidebar being closed until a person opens it. ADR 0231. **Read and kept in the five-hundred-and-fifty-third**, on §12.3.5's row's own evidence: the three functions are there and the host that calls `initial_document` is `viewer_ui::chrome`."

[[clause]]
clause = "12.3.5.2"
title = "Collection hierarchical folders"
status = "partial"
code = ["crates/pdf-model/src/collection.rs"]
test = ["crates/pdf-model/src/collection.rs::a_files_folder_is_written_into_its_name_tree_key", "crates/pdf-model/src/collection.rs::a_folder_name_is_a_file_name_by_the_clauses_five_rules", "crates/pdf-model/src/collection.rs::a_navigator_names_layouts_in_the_order_the_file_prefers_them"]
note = "Table 159's folder tree, walked from `/Folders` along `/Child` and `/Next` with §12.3.3's defence — forward links only, each object once — and the two rules the clause states as prose rather than as entries. **A file's folder is written into its name-tree key**: `<3>report.pdf` is folder 3, one or more digits between the brackets, and a key that does not conform means the root folder rather than an error, which is `collection::folder_of`. **A folder name is a *file name***, which the clause defines by five requirements — a text string, no NUL, \"[t]he number of characters in the string shall be between 1 and 255 inclusive\", none of eight special characters, and not ending in a full stop — and `collection::is_file_name` answers it without refusing anything, because the clause leaves that open: \"[a]n interactive PDF processor may choose to support invalid names or not\". **This sentence said *five* and listed four, and the bullet it dropped is the one the code never applied**: `is_file_name` tested `!name.is_empty()`, which is the 1 of that bound and not the 255, while the doc comment beside it declined the rest because \"truncating one would rename a file\" — a reason about an operation a `bool` predicate does not perform. Answering `false` *is* the answer, and the permission quoted above is what leaves a caller free to keep the name anyway; the bound counts characters and not bytes, as the sentence says. The test's own name has claimed five rules since it was written and it asserted four. Applied in the six-hundredth session. **This row said \"`partial` for the panel\" until the five-hundred-and-first session, and the panel arrived in the three-hundred-and-fifty-second** (ADR 0202): `viewer_ui::chrome`'s files tab draws this clause's tree — each file under the folder its name-tree key names, non-conforming keys at the root, which is the clause's own \"shall be treated as associated with the root folder\" — and the row was not maintained by the session that built it, which is `doc/todo/01`'s second failure shape. `partial` now for what the walk does not read: a folder's own `/Thumb` (no panel draws a folder miniature), `/CreationDate` and `/ModDate` — and Table 159's `/Free` is named rather than owed, since its `shall` fires \"when a new folder is added\" and adding a folder is a verb this program does not have. The pdf.js corpus's 23 embedded files are all plain keys, measured, so no document *there* has folders — and `digitally_signed_3D_Portfolio.pdf` under `doc/corpora/format-corpus` does, which is what the unqualified sentence here used to deny (ADR 0406)."

[[clause]]
clause = "12.3.6"
title = "Navigators"
status = "partial"
code = ["crates/pdf-model/src/collection.rs"]
test = ["crates/pdf-model/src/collection.rs::a_navigator_names_layouts_in_the_order_the_file_prefers_them"]
note = "**This row was wrong for thirty-odd sessions** and its own note is the evidence: it said a navigator is \"supplied as SWF\" and that anyone widening `CLAUDE.md`'s exclusion list \"should start here, with an argument\". ISO 32000-2's §12.3.6 names no media format at all — a navigator dictionary holds `/Layout`, one or more of seven **named layouts** (`D`, `T`, `H`, `FilmStrip`, `FreeForm`, `Linear`, `Tree`), each described in the clause's own prose. The SWF navigator was an Adobe extension this standard replaced. So it is a reader's question and is read: `Navigator::preferred` is the clause's selection rule — \"an interactive PDF processor should present the first one it is capable of displaying in the order present in the array\" — which takes the layouts a *caller* can draw, since the answer depends on the viewer as much as on the file. `partial` because nothing draws one. **Read and kept in the five-hundred-and-first session, with the evidence**: `Navigator::preferred` is named by no host, tool or fuzz target (the fifth sweep), the navigator crosses the confined boundary whole (`viewer_confined`'s `Reply::Collection` carries Table 160 beside Table 159's folders), and `viewer_ui::chrome`'s files tab draws the folder tree without consulting `/Layout` — so the selection rule is implemented, reachable from two boundaries, and asked by nobody, which is the fifth sweep's shape and stays owed to a host round. **Errata Collection 3 has moved this clause's own number** (Issue #477, `/State` `Review` `Completed`): \"all of subclause 12.3.6 Navigators was moved and demoted one level to subclause 12.3.5.3 Collection navigators\", so a reader holding the amended text finds this content under a number this standard does not have and finds §12.3.6 gone. **The amended number is written here as the erratum writes it — *subclause 12.3.5.3*, with no section sign — because a citation of it is a citation of no clause of ISO 32000-2, and `the_ledgers_own_prose_names_clauses_and_tables_that_exist` says so: this sentence failed that gate when it was first written with one.** **The number here is not changed and that is the standing decision**: `doc/md/` is the published text, the citation gate resolves every `§` against it, and `the_ledgers_own_prose_names_clauses_and_tables_that_exist` refuses a post-erratum number outright — so the tree cites what the standard it is checked against prints, and the *amendment* is what a command answers rather than a rename. `cargo run --release -p spec-errata -- moved doc/*.pdf` prints every erratum that moves a number with the ground standing on it — one row and seven source citations here — and `doc/errata-read.md` carries the reading. Found in the five-hundred-and-sixty-fifth session, by the hand-filter of the five-hundred-and-sixty-second becoming that command: this erratum writes its instruction in the past passive (\"was moved\") where Issue #452 writes it as an imperative, which is why a grep for *Move* found the one and not the other."

[[clause]]
clause = "12.4"
title = "Page-level navigation"
status = "partial"
code = ["crates/pdf-model/src/page_label.rs", "crates/pdf-model/src/article.rs", "crates/pdf-model/src/navigation.rs"]
test = ["crates/pdf-model/tests/page_labels.rs::the_clauses_own_example_produces_the_labels_it_states", "crates/pdf-model/src/article.rs::the_clauses_own_example_is_three_beads_on_two_pages", "crates/pdf-model/src/navigation.rs::a_page_of_bullets_walks_its_states"]
note = "Page-level navigation: page labels, articles and presentations, and all three are now read. §12.4.2's labels are `implemented` and shown in the title bar; §12.4.3's articles are read as threads of beads and `partial` for the zoom a bead's `/R` asks for; §12.4.4's presentation is read entire and, since the three-hundred-and-ninety-third session, **seven of Table 164's twelve transition styles are drawn frame by frame** by a host with a clock (ADR 0230), with **four** reported by name — `Blinds`, `Glitter`, `Dissolve` and `Fly`, each for a quantity Table 164 does not state. **This sentence said \"the other five\" until the five-hundred-and-fifty-third session, and the fifth was `R`**, which the table defines as the cut — \"[t]he new page simply replaces the old one with no special transition effect\" — so there is nothing to report and `transition::note` deliberately reports nothing for it. ADR 0230 says both: its sentence says five and its own table twelve lines below says \"`R` is therefore not reported and the other four are\", and the two rows written from it took the first half. So the clause is `partial` for what a *window* still owes any of the three, which is the same sentence three subclauses say in three ways — this row was `silent` for eighty-eight sessions while two of its children were not, which is the understated-parent shape §14.8.5.1 taught."

[[clause]]
clause = "12.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/page_label.rs", "crates/pdf-model/src/article.rs", "crates/pdf-model/src/navigation.rs"]
test = ["crates/pdf-model/tests/page_labels.rs::the_clauses_own_example_produces_the_labels_it_states", "crates/pdf-model/src/article.rs::the_clauses_own_example_is_three_beads_on_two_pages", "crates/pdf-model/src/navigation.rs::a_page_of_bullets_walks_its_states"]
note = "The framing: page-level navigation is what a reader does *within* a document once it is open, as against §12.3's overview of the whole of it. The clause names its three mechanisms — page labels, articles, presentations — and each has its own row; the framing itself states no requirement beyond naming them, and all three are read."

[[clause]]
clause = "12.4.2"
title = "Page labels"
status = "implemented"
code = ["crates/pdf-model/src/page_label.rs"]
test = ["crates/pdf-model/tests/page_labels.rs::the_clauses_own_example_produces_the_labels_it_states", "crates/pdf-model/tests/page_labels.rs::every_document_stating_labels_labels_its_first_page"]
note = "Page labels, in full: the catalog's `/PageLabels` number tree, Table 161's `/S`, `/P` and `/St`, and the rule that a range runs to the start of the next. Four things the clause states and a first implementation gets wrong: **there is no default numbering style**, so a range with a `/P` and no `/S` labels every one of its pages identically — the clause's own NOTE spells that out with `Contents`; **`/A` is not base 26**, it is \"A to Z for the first 26 pages, AA to ZZ for the next 26\", so the twenty-eighth page is `BB` and not `AB`; the Roman form is subtractive, which the clause's example fixes by running `i, ii, iii, iv` rather than `iiii`; and `/St` \"shall be greater than or equal to 1\", so a file writing zero gets the stated default. The prefix is a *text string*, decoded by §7.9.2.2's own function. **The clause's worked example is the test** — nine labels, `i ii iii iv 1 2 3 A-8 A-9` — because no corpus document exercises all three of its ranges, which is trap 8; 22 of the 974 do state labels and every one of them labels its first page. `viewer-ui` shows the label beside the index rather than instead of it, since a title reading `iv` cannot also say `of 320`."

[[clause]]
clause = "12.4.3"
title = "Articles"
status = "partial"
code = ["crates/pdf-model/src/article.rs", "crates/viewer-core/src/interact.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/src/article.rs::the_clauses_own_example_is_three_beads_on_two_pages", "crates/pdf-model/src/article.rs::the_ring_closes_and_following_it_wraps", "crates/pdf-model/tests/articles.rs::no_pdfjs_document_states_an_article", "crates/pdf-model/tests/articles.rs::a_producers_own_thread_is_walked_to_its_beads", "crates/viewer-core/tests/articles.rs::activating_a_thread_goes_to_its_first_bead", "crates/viewer-ui/tests/panel.rs::the_read_tab_lists_a_thread_and_a_click_follows_it"]
note = "The catalog's `/Threads`, Table 162's thread dictionaries with their `/I` information title, and Table 163's beads walked from `/F` along `/N`. **The chain is a ring**, which is what distinguishes this walk from §12.3.3's outline: `/N` \"[i]n the last bead … shall refer to the first bead\", so termination is a visited set rather than an absent entry, and following past the end wraps — which is what \"follow a thread from one bead to the next\" means at a thread's end. `/V` is deliberately unread, for the outline's reason: it is redundant with `/N` and could only disagree. Table 31's page `/B` is read *only* to be checked against the threads, since NOTE 2 says it \"can be created or recreated\" from them — and the two clauses disagree about its order (\"natural reading order\" in Table 31, \"in drawing order\" here), so the comparison is on the set, which is a documented choice and the only comparison both sentences license. **Table 163's `/R` is where a thread jump goes, since the two-hundred-and-fifty-fifth session**: the entry is \"[a] rectangle specifying the location of this bead on the page in default user space\", which is the same statement Table 149's `/FitR` makes about a window, so following a thread composes one of §12.3.2.1's eight forms rather than adding a ninth. A jump that turned to the bead's *page* and stopped there had not followed anything — a bead is one column of a layout and a page may hold four. The reason this row gave for leaving it — \"`viewer-ui` fits whole pages\" — expired in the hundred-and-thirty-second session and expired for destinations in the two-hundred-and-first (ADR 0162), which is `doc/todo/01`'s third sweep once more. **No *pdf.js* document states an article**: 2 of the 974 catalogs carry a `/Threads` entry, one an empty array and one a null, and no page carries a `/B` — measured. **This sentence read \"[n]o corpus document\" and the wider corpus has four**, with 115 beads between them: `PDFBOX-3110-poems-beads.pdf` and its cropbox twin lay two poems out as two threads apiece, `PDFBOX-4417-054080.pdf` carries one of two beads and `format-corpus`'s `443752.pdf` one of ninety-one. `pdf-model/tests/articles.rs` walks the first of them — both titles decoded, every bead naming its page, and each ring closing on its own first bead — so this clause is no longer defended by hand-built files alone (ADR 0406). The clause's own EXAMPLE 2 and the two-page thread in `viewer-core/tests/headless.rs` stay, because a producer's file exercises the reader and not the panel. **A person's way in arrived in the three-hundred-and-forty-seventh session** (ADR 0200), which is what this row had been `partial` for and what it had gone on not noticing for forty-seven rounds after the sidebar was drawn. `viewer_core::Query::Articles` answers with the threads in the `/Threads` array's own order, `viewer_ui::chrome`'s sixth tab draws one row apiece with the `/I` title and the bead count, and a click sends `Command::Activate` on the thread — the same message §12.3.3's outline row sends, because what activating an object means is the *document*'s to decide. `interact::activate_object` composes §12.6.4.7's own `ThreadJump` out of it rather than adding a second route to one place, so a click lands on Table 163's `/R` and not on the page the first bead sits on. The clause's own verb is the reason this is a panel rather than an obligation: \"[i]nteractive PDF processors **may** provide navigation facilities to allow the user to follow a thread from one bead to the next\", and a permission taken is still a permission. `partial` for the half no host asks: `beads_on_page` answers which article is under a point and nothing calls it, so \"which thread am I reading\" has no question yet. **No pdf.js document states an article** — 2 of the 974 catalogs carry a `/Threads` entry, one an empty array and one a null, and no page carries a `/B` — and this used to read \"[n]o corpus document\", which the four witnesses under `doc/corpora/` falsify (ADR 0406). What is still defended by a hand-built fixture and nothing else is the *panel*, because none of the four is reached by a gate that opens a window. §12.3.5's collection is the same shape and three times the size, and it landed one round after this did (ADR 0202). **Read and kept in the five-hundred-and-twenty-fifth**: `beads_on_page` is still named by no host, no tool and no fuzz target, printed on `conformance --bin callers`'s rung for a function only its own crate names, which is what this row's remaining `partial` says."

[[clause]]
clause = "12.4.4"
title = "Presentations"
status = "partial"
code = ["crates/pdf-model/src/navigation.rs"]
test = ["crates/pdf-model/src/navigation.rs::a_page_of_bullets_walks_its_states"]
note = "Presentations: full-screen page transitions, `/Trans` on a page and `/Dur` for an automatic advance, and §12.4.4.2's sub-page navigation. All three are read as of the seventieth session. **A `/Dur` is acted on since the hundred-and-fiftieth**: `Command::Tick { millis }` is how a state machine with no clock is told that time passed, and a page shown for as long as it asked advances by itself, naming the page arrived at's `/Trans` as it goes (ADR 0135). **And a transition is drawn since the three-hundred-and-ninety-third** (ADR 0230): `viewer_core::transition` shapes the frame at a *fraction* of the way through — the clock stays the host's, which is rule 3 — and the frame is a `pdf-render` display list of two page rasters, so both backends draw it. Seven of Table 164's twelve styles are shaped: `Wipe`, `Split`, `Box`, `Cover`, `Uncover`, `Push` and `Fade`, which are the ones whose frame the table's own words determine. The other five are **reported by name** rather than drawn as a cut, each because the clause states no quantity for it — how many lines a `Blinds` has, how wide a `Glitter`'s band is, what a `Dissolve` does to a pixel, and what a `Fly`'s \"changes\" are; `R` is the cut by definition and is not reported. **And there is a presentation *mode* since the four-hundred-and-eighty-first** (ADR 0316), which this row said for eighty-eight sessions there deliberately was not: §12.4.4.2's NOTE 3 respects the navigation nodes \"only when in presentation mode\", so a state machine conditioned on it cannot be built without one. `Command::Present(PresentationMode)` is the host saying so — the third policy value in that vocabulary after `Restrict` and `Delegate`, and needed because a person stepping through a slide show by hand drives no clock at all, which is what the deduction from `Tick` had been standing on. Full screen is still chrome and still the host's; what crosses is the mode and not a window. `partial` for the five styles alone. (This row said \"Table 165's thirteen\" for sixteen sessions; the table is 164 and the styles are twelve. Its \"a transition is an animation, which is nearer clause 13's exclusion than anything else in clause 12\" is retired by the round that drew one.)"

[[clause]]
clause = "12.4.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/navigation.rs", "crates/viewer-core/src/transition.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/viewer-core/tests/headless.rs::a_page_stating_a_duration_advances_when_it_is_told_the_time", "crates/pdf-model/src/navigation.rs::the_clauses_own_example_page_states_a_split_and_five_seconds", "crates/viewer-core/tests/headless.rs::a_transition_frame_is_between_the_two_pages", "crates/viewer-core/tests/headless.rs::a_transition_this_reader_does_not_draw_is_named_rather_than_cut", "crates/viewer-ui/tests/transition_frames.rs::both_backends_draw_the_same_transition_frame"]
note = "Table 164's transition dictionary and the page's `/Dur`, both read from the page's own dictionary — §7.7.3.4 makes four entries inheritable and neither of these is among them, so a `/Pages` node stating one states nothing. Every entry of Table 164 is read with the table's own default where the file states none: `/S` as twelve named styles plus a variant carrying a thirteenth name the file wrote, `/D`, `/Dm`, `/M`, `/Di` as degrees or the name `None`, `/SS` and `/B`. `R`'s row states an obligation rather than an appearance — \"the D entry shall be ignored\" — so a replacement's duration is zero whatever `/D` says. No `/Dur` at all is the clause's \"the page shall not advance automatically\" and not an advance of zero, which is why it answers `Option`. **Since the hundred-and-fiftieth session `/Dur` is acted on**: `Command::Tick { millis }` is how a state machine with no clock (rule 3) is told that time passed, and a page shown for as long as it asked advances by itself. The comparison is `>=` and every page turn restarts the clock, which is the entry's own word \"maximum\" and NOTE 1's \"[t]he user can advance the page manually before the specified time has expired\" met by one rule rather than two. The page arrived at is what names its `/Trans`, because the entry is \"the transition style that shall be used when moving to *this* page from another\" — read from the page tree rather than from the interpreted page, which at that moment is still the page just left. The last page does not advance and does not loop: the clause says nothing about what follows the end. **A transition is played since the three-hundred-and-ninety-third** (ADR 0230). `viewer_core::transition::frame` answers what a frame *at a fraction of the way through* looks like — two page placements, a rectangular reveal and an alpha — and `Frame::draw` turns that into a display list of two images, so both backends draw it and the CPU rasteriser keeps its job as the frame a device refuses. Seven of Table 164's twelve styles are shaped and the other five are reported by name, each because the table states no quantity for it; the geometry the table *does* state is followed to the pixel, including that `/Di` is measured counterclockwise from a left-to-right direction and so runs the other way from a device's y. What the standard does not state is written down as a choice: progress is linear in time, and a sweep reveals what it has passed over. **And a page turned *by hand* during a presentation plays its `/Trans` since the four-hundred-and-eighty-first** (ADR 0316), where only the clock's own advance had: the entry is \"the style and duration of the visual transition to use when moving from another page to the given page during a presentation\" — moving, not moving automatically — and §12.4.4.2 says it a second time with a `shall` for a navigation to a page with `/PresSteps`. So a person pressing an arrow key through a slide show saw every effect the file asked for skipped, which no gate could see because no corpus document states one. `partial` for the five styles. ADR 0135 for the clock, ADR 0230 for the frames, ADR 0316 for the mode. Demand measured again in the three-hundred-and-ninety-third, over the *page tree* of all 964 openable corpus documents and the 14 in `doc/` — 978 documents, 1971 pages — rather than over raw bytes as the seventieth session measured it: **not one states a `/Trans`, a `/Dur` or a `/PresSteps`** (`examples/presentation_census`), so the witness is hand-built and `examples/presentation_fixture` writes it. One finding about the sources rather than the code: `doc/md/`'s conversion of Table 164 drops `/Di`'s \"Default value: 0.\", which the standard's own PDF states — the conversion caveat, met in the wild."

[[clause]]
clause = "12.4.4.2"
title = "Sub-page navigation"
status = "implemented"
code = ["crates/pdf-model/src/navigation.rs", "crates/viewer-core/src/presentation.rs", "crates/viewer-core/src/viewer.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/navigation.rs::a_page_of_bullets_walks_its_states", "crates/pdf-model/src/navigation.rs::a_navigation_node_states_its_own_advance_timing_or_states_none", "crates/viewer-core/tests/sub_page_navigation.rs::a_forward_request_walks_a_pages_states_before_it_turns_the_page", "crates/viewer-core/tests/sub_page_navigation.rs::the_same_page_without_pres_steps_turns_at_the_first_request", "crates/viewer-core/tests/sub_page_navigation.rs::nothing_walks_a_pages_states_while_no_presentation_is_running", "crates/viewer-core/tests/sub_page_navigation.rs::leaving_presentation_mode_puts_the_optional_content_back", "crates/viewer-core/tests/sub_page_navigation.rs::a_navigation_nodes_own_duration_advances_it_and_its_absence_does_not", "crates/viewer-core/tests/sub_page_navigation.rs::a_backward_request_undoes_a_state_and_then_leaves_the_page"]
note = "Sub-page navigation: `/PresSteps` on a page, and Table 165's navigation nodes, which \"form a doubly linked list by means of their Next and Prev entries\". Each node is a pair of §12.6 actions, so what walking a page's states takes is the list and a `ViewState` to perform them against — and this row has said since the fifty-fourth session that it was \"the one presentation row whose missing piece is a control rather than a renderer\". The sixty-second and sixty-seventh sessions built the control and the sixty-ninth the list. Only `/Next` is walked: the clause makes the nodes one doubly linked list, so `/Prev` is the same order read backwards, and a file whose two chains disagree has stated two orders with no rule to choose between them. The ring a `/Next` chain may close is bounded by visiting each node once. **This row was `implemented` on the argument that nothing turns a presentation on, and the three-hundred-and-ninety-third session made that argument expire.** NOTE 3 says nodes need be respected \"only when in presentation mode\", and the condition was met the moment `viewer-ui`'s `p` began driving the clock and drawing §12.4.4.1's transitions (ADR 0230): there is a presentation mode now, and the arrow keys still turn pages where the clause says they should step through a page's states first. So the row went back to `partial`, and what it owed was a control rather than a reader — `navigation::steps` gave the list and `ViewState::perform_all` performed a node's actions, which was every part but the walk. Full screen remains chrome and remains the host's; that is not what is missing. NOTE 2's obligation to save and restore the optional content states across presentation mode is why `ViewState` is a value a caller owns and is `Clone`. **The control landed in the four-hundred-and-eighty-first** (ADR 0316), and what it needed was the mode this family had refused: `Command::Present(PresentationMode)` is a host saying that NOTE 3's condition holds, and `viewer_core::presentation` is the clause's own state machine — the current navigation node the subclause opens by requiring, `/PresSteps` making the primary node current on arrival, `/NA` then `/Next` forward, `/PA` then `/Prev` backward, and a page arrived at running the two together as the last paragraph states. `Command::GoTo(Next)` is the forward request, because \"the user requests to navigate forward (such as an arrow key press)\" is what that message already is; random access is forward on the clause's own words. **Table 165's `/Dur` was read by nothing until this round** — \"[t]he maximum number of seconds before the interactive PDF processor shall automatically advance forward to the next navigation node\" — which is a `shall` this row had never mentioned; it is a second clock beside §12.4.4.1's, because the standard states two maxima. NOTE 2 is discharged where it is stated: entering the mode saves §8.11's group states and leaving restores them, through `ViewState::optional_content_snapshot`. Two things the clause does not decide are decided here and say so: running off either end of the list leaves no current node, so the request after the last state turns the page — the alternative, leaving the last node current, re-executes its `/NA` for ever and no page could be turned — and entering the mode on a page nobody navigated to makes that page's primary node current, because a processor with none would not be maintaining one. Neither `PresSteps` nor `NavNode` occurs in the raw bytes of any of the 974 corpus documents, and `examples/presentation_census` finds no `/PresSteps` on any page of the 978 this tree opens, asked of the page tree."

[[clause]]
clause = "12.5"
title = "Annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_annotation_draws_over_the_page_content"]
note = "An annotation is drawn over the page content and in /Annots order, which is the only thing fixing its order because nothing in a content stream refers to it. Each entry is placed by §12.5.5 and drawn by the same machinery as any other form XObject, with §12.5.3's flags and §8.11.3.3's /OC deciding whether it appears at all. **This note ended \"no annotation responds to input, and §12.6's actions are not read\" until the four-hundred-and-forty-second session**, and both halves had expired: §12.6's own family is `partial` with §12.6.1 and §12.6.2 `implemented`, `viewer_core::interact` presses the annotation under a pointer, and ADR 0177 took the press to a widget rather than only to a link. What keeps this row `partial` is what the subclauses below say: an appearance this crate constructs where the file states none is complete for some subtypes and refused-and-reported for others, and §12.5.6.3's reply state is read by nothing."

[[clause]]
clause = "12.5.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_annotation_draws_over_the_page_content"]
note = "The clause's rendering content is that an annotation is associated with a location on a page and that a closed one 'appear[s] on the page in some distinctive form, such as an icon, a box, or a rubber stamp, depending on the specific annotation type' — which each subtype's clause then defines, or does not: see §12.5.6.4's icons. **Table 31's `/Tabs` is implemented since the two-hundred-and-ninety-seventh session** — all five values, in `pdf_model::tab_order`, with `viewer_core::Command::Focused` moving the input focus through the order and `pdf-viewer.rs` binding the key the clause itself names. The two places the clause leaves a choice are taken and written down: a page stating no `/Tabs` gets the `/Annots` array's own order, and an annotation §14.7's tree does not reach follows the ones it did, which the clause says outright \"is determined in a manner of the interactive PDF processor's choosing\". The geometry orders are taken after §7.7.3.3's rotation, because the clause says so — \"[t]hese descriptions assume the page is being viewed in the orientation specified by the Rotate entry\" — and the four screen mappings are `content::base_transform`'s own with the raster's y flip folded in rather than a second opinion about the rotation. Measured: 61 corpus documents state the entry on one of their first 50 pages, 95 pages `/S`, 10 `/R`, 5 `/W`. `Query::Focus` is the other half and it is what makes the feature visible: the clause lets a person walk the annotations and says nothing about showing which one they are on, so the *ring* is a host's — drawn in `pdf-viewer.rs`'s own blue, in device pixels the core computes, because a host re-deriving the origin, the magnification and the y flip would be repeating the defect ADR 0118 found. **This row said \"[w]hat is left is one piece of interaction this program does not have: opening an annotation to exhibit its object\" until the three-hundred-and-twelfth session**, which is `doc/todo/01`'s capability shape — the clause's own sentence is 'When the user activates the annotation by clicking it, it exhibits its associated object, such as by opening a popup window displaying a text note', and a press and a release on one annotation is what this program has had since the hundred-and-thirty-second. §12.5.6.14's window opens and closes now (ADR 0191); what the clause leaves open and this program chooses is that a second click puts it away. **What keeps this row partial is the clause's last paragraph and not that sentence**: 'A PDF processor shall provide annotation handlers for all of the conforming annotation types', and three of Table 171's are on `CLAUDE.md`'s closed exclusion list — Movie, Screen and RichMedia are clause 13's media engine — while Stamp's icon is owed under §12.5.6.12 and the other half of the clause's own example, 'playing a sound or a movie', is excluded with them. **And the sentence about activation is reachable without a pointer since the five-hundred-and-ninetieth session**: an assistive technology asking `org.a11y.atspi.Action` for a click on §14.7's element names a node rather than a point, and `viewer-accessibility` turns it into the node's own middle so that the whole of what a click means here — §12.6.3's triggers, §12.5.5's appearance, §12.7.5.2's toggling and this clause's activation — runs from one definition rather than two. `viewer-accessibility` declares the action only where §14.7.5.3's object reference says the element *is* an annotation, which is the condition this sentence states. ADR 0425. **The second is now the only half of \"focus\" still owed**: §12.6.3's /Fo and /Bl are raised since the two-hundred-and-fifty-seventh session, by the pointer, and what a `/Tabs` order adds is which annotation comes *next* — a keyboard question rather than a focus one. The pointer's half arrived long ago and this sentence said otherwise for a hundred and twenty sessions: a cursor over an annotation chooses Table 170's appearance (§12.5.5), raises Table 197's events (§12.6.3) and reads §12.5.3's flags through `annotation::interacts` — and until the two-hundred-and-fifty-third session the first of those three reached only a link, because `viewer-core` took the region from `link_at`. ADR 0177. **And the ring aims the keyboard since the three-hundred-and-seventy-first session**, which is the decision `doc/todo/33` left open rather than a new message: a walk that lands on a widget whose value is text puts the caret in it, and one that lands on a button or a link takes the keyboard back to the page — the same question a click already asked, since `Answer::Field`'s value is `Some` only for a field §12.7.4.3 lays text out for. The point it asks about is the centre of this clause's own ring. **And the ring's own placement was wrong for a page §7.7.3.3 turns**: `Query::Focus` mapped `/Rect` to the screen without the page transform, so a rotated page or a crop box away from the origin would have put it elsewhere — no corpus document states a rotated page with a widget, which is why nothing saw it and why the fix is arithmetic rather than a picture (ADR 0211). **Read again in the four-hundred-and-ninth session**, with its table citations checked against the standard's own entries: `/Tabs` is Table 31's, the annotation types are Table 171's, the appearance is Table 170's and the trigger events are Table 197's — all four correct. What that reading added is a line this clause draws and nothing else in the standard draws so plainly: `/Tabs` `W` orders \"[w]idget annotations ... followed by other annotation types\", which is the standard separating §12.7's controls from every other annotation on a page — the division ADR 0245 needed, because \"leave out the widgets\" and \"leave out the annotations\" are different requests. **Read and kept in the five-hundred-and-sixty-fifth**: what holds the row is still the last paragraph's \"annotation handlers for all of the conforming annotation types\", and §12.5.6.12's own row is still `reported` on Table 184's *should* — so the stamp icon this row names as owed is refused with an argument rather than unread, and Movie, Screen and RichMedia are still `CLAUDE.md`'s clause 13."

[[clause]]
clause = "12.5.2"
title = "Annotation dictionaries"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_stored_appearance_ignores_the_annotations_opacity", "crates/pdf-model/src/structure.rs::a_pages_annotations_state_where_they_are"]
note = "Table 166's entries that decide what is drawn are read: /Subtype, /Rect (normalised, and an empty one draws nothing), /F, /AP, /AS, /Border, /C, /CA, /ca, /BM and /OC. **/Border is read only where the annotation states no /BS**, which is this table's own sentence — \"If an annotation dictionary includes the BS entry, then the Border entry is ignored\" — and it was true of the width and false of the corner radii until the four-hundred-and-fifty-eighth session; §12.5.4's row has the reading and the fixture. The clause's closing rule is implemented and is what shapes crate::appearance, and **Errata Collection 3 has rewritten it** — Issue #23 and #34 for two strikeouts, Issue #56 for an insertion, all `/State` `Review` `Completed`, read in the four-hundred-and-seventeenth session and invisible to `doc/md/` before it (ADR 0253). The 2020 sentence read 'A PDF reader shall render the appearance dictionary without regard to any other keys and values in the annotation dictionary and shall ignore the values of the C, IC, Border, BS, BE, BM, CA, ca, H, DA, Q, DS, LE, LL, LLE, and Sy keys'; the amended one reads 'When rendering the appearance dictionary, a PDF reader shall ignore the values of the C, IC, Border, BS, BE, CA, ca, H, DA, Q, DS, LE, LL, MK, LLE, and Sy keys' — the blanket clause gone, `BM` out of the list, `MK` into it. **So `/BM` is applied to a stored appearance stream since this session**, and the contradiction this row recorded is half settled: §12.5.5's 'using the values of the BM, ca and CA entries' now has nothing against it for the blend mode, and Table 166's own `/BM` row states the mode for 'painting the annotation onto the page' with no condition at all. `/CA` and `/ca` stay ignored — they remain on the amended list, and Table 166 says of each that it 'shall not be used if the annotation has an appearance stream' — so that half of the contradiction stands and is still recorded in §12.5.5's row. `/MK` needed nothing: it is read on no path a stored appearance reaches. Not read, none of which marks the page: /P, /M, /AF and /Lang. **/StructParent was in that list and has been read since the four-hundred-and-ninetieth session**, which is a row describing the code as it was for thirteen: §14.7.5.4 keys an object content item from the annotation's own `/StructParent`, and `Tree::elements_on_page` asks every annotation of the page for it (ADR 0325). The list's justification survives the correction — the entry marks no page — and what it turned out to be is a route *into* the structure tree rather than a mark. **And /Rect has a second consumer since the five-hundred-and-third**: §14.7.5.3's object reference makes an annotation the whole content of a structure element, so \"[t]he annotation rectangle , defining the location of the annotation on the page in default user space units\" is where that element is — 333 of the corpus's 1675 placeless elements are placed by it on AT-SPI's `Component` interface, and only the objects the page's own `/Annots` names are answered for, because Table 166 makes `/Type` optional and a `/Rect` read off any dictionary would be ADR 0215's mistake again. ADR 0338. **/RC and /NM were in that list until the three-hundred-and-seventy-fifth session and both are read.** Table 172's /RC is \"[a] rich text string ... that shall be displayed in the popup window when the annotation is opened\", which `popup::rich_text` has taken the characters out of since ADR 0199 — the same clause §12.5.6.2's row was corrected for thirty-three sessions earlier, and this row was never re-read against it. Table 166's /NM \"uniquely identif[ies]\" an annotation among its page's, and it is what a lookup by name resolves against: `fragment::annotation_named` for Annex O's `comment` parameter (ADR 0209) and `action.rs`'s `AttachmentIndex::Name` for §12.6.4.4's embedded go-to before it. The list's own justification is what disqualified /RC — a popup's text is a mark on the screen — and its parenthesis called /RC \"§14.9.3's alternate description\", which is that clause's /Contents rather than this entry. **/Contents was in that list and is read**: §12.5.6.6's free-text appearance lays it out as the annotation's text, which is the one subtype whose clause makes the entry mark the page — so the list's own justification, \"none of which marks the page\", is what disqualified it. **And Table 166's `/AP` row carries a requirement on a *writer* that this row and two doc comments had missed**, found in the five-hundred-and-sixty-second session by running `spec-errata emit` over the standard rather than `check` over this tree's quotations: Errata Collection 3's Issue #22 (`Review`/`Completed`) replaces that row's \"Optional; PDF 1.2\" with \"Required except for conditions listed below (PDF 2.0); optional in PDF 1.2 through PDF 1.7\", and the conditions are a degenerate `/Rect` and a `/Subtype` of `Popup`, `Projection` or `Link`. **The 2020 text already said it in prose** — \"[a] PDF writer shall include an appearance dictionary when writing or updating the PDF file except for the two cases listed below\" — so the erratum only moves the requirement into the column, and the sentence in `view.rs` saying \"an annotation with no `/AP` is legal\" was wrong before the erratum as well as after. This program is a writer under §7.5.6, and it **departs from that `shall` in one place, deliberately**: `write_retypings` removes a producer's stored appearance for a free text annotation whose new text it could not lay out, because Table 177 makes `/AP` decisive over `/DA` and leaving the old stream would draw text the file no longer claims. The cost is a PDF 2.0 annotation the amended table calls non-conforming; the alternative is a page that shows the wrong words. It is reported — `Written::unappeared` names every such annotation — which is what makes it a departure rather than an oversight. `check` could not see any of this, because no quotation is involved. **And one sentence of this clause got its first caller in the six-hundred-and-ninth session**: \"A given annotation dictionary shall be referenced from the Annots array of only one page.\" Nothing had needed it while a selection was a range of one page's readback; a selection may cross a page boundary now, and §12.5.6.10's mark-up over one is therefore several annotations rather than one shared between the pages — `viewer_core::open::Done::Markup` carries a list of (page, quadrilaterals) and quotes the sentence. ADR 0444."

[[clause]]
clause = "12.5.3"
title = "Annotation flags"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/restriction.rs"]
test = ["crates/pdf-model/tests/annotations.rs::the_hidden_and_no_view_flags_draw_nothing_and_report_nothing", "crates/pdf-model/tests/annotations.rs::a_no_rotate_annotation_pivots_about_its_own_corner", "crates/pdf-model/tests/annotations.rs::a_no_zoom_annotation_keeps_its_size_when_the_page_is_magnified", "crates/pdf-model/tests/annotations.rs::a_text_markup_annotation_scales_with_the_text_it_marks", "crates/pdf-model/tests/variable_text.rs::table_167s_locked_flag_is_not_its_locked_contents_flag"]
note = "Table 167's three flags that decide whether a screen shows the annotation are honoured: Hidden and NoView suppress it, and Invisible suppresses it only for a subtype outside Table 171, which is the condition the flag's own wording places on it. Print is a printing decision this viewer does not yet make, and the twenty-first session found that it is also the reason a reference renderer disagrees with us about three link borders — this note said four until the four-hundred-and-sixth session, which counted them off `oracle.rs`'s own array rather than off this sentence, `issue18823.pdf` having left in the thirtieth for a reason that was never a border: ghostscript renders for a printer, where 'If clear, never print the annotation, regardless of whether it is rendered on the screen' means a page with no /F draws no annotation at all. All three of those files were read in the four-hundred-and-sixth and none of them states an /F at all, so Table 167's flags are the default 0 and bit 3 is clear on every one — the /F 4 experiment that established the mechanism had been run on one of the three. The flag's own next sentence would settle even that case in our favour — 'If the annotation does not contain any appearance streams this flag shall be ignored' — which matters for every appearance this crate constructs. **NoZoom and NoRotate are applied since the two-hundred-and-seventeenth session** (ADR 0168), and the reason they were not is a reason that named an *architecture*: they make an appearance's placement depend on the view, which a resolution-independent display list cannot express. Both halves of that turned out to be smaller than the sentence. NoRotate depends on §7.7.3.3's `/Rotate`, which is in the file, so it is as pure a function of the document as anything else and needed no new vocabulary at all. NoZoom depends on the magnification, which is the *reader's* — so it arrives through `ViewState`, where rule 1 says a statement about the view belongs, and `Interpretation::view_dependent` says whether a page has an annotation that would notice, so 923 of the 974 corpus documents never re-interpret on a zoom. Both are one similarity about the corner the clause names: \"the annotation's position is defined by the coordinates of the upper-left corner of its annotation rectangle\", and \"it shall not actually change the annotation's Rect entry\", which is why the adjustment is composed before the page's transform rather than written into the rectangle. A magnification nobody stated is **not** 1.0 but unknown, under which NoZoom changes nothing — which is what the corpus gate and the oracle mean and why their verdicts did not move. Table 167's remaining constraint on this pair is `/FixedPrint`, §12.5.6.22's watermark entry, which is a printing decision. **ReadOnly and ToggleNoView are applied; Locked and LockedContents are not yet reachable.** `ReadOnly` — \"do not allow the annotation to interact with the user ... should not respond to mouse clicks or change its appearance in response to mouse motions\" — is `annotation::interacts`, with the clause's own exception for a widget, whose function Table 226 subsumes; and since the two-hundred-and-fifty-third session it is what the *pointer's* region is read through, so the second half of that sentence is true and not only the first. **`ToggleNoView` is bit 9 and it is an inversion rather than a suppression**: \"invert the interpretation of the NoView flag for annotation selection and mouse hovering\", so `NoView` alone hides, the two together hide until the cursor arrives, and `ToggleNoView` alone hides only while it is there — an exclusive-or of the two bits with Table 170's appearance, which is what §12.5.5 makes \"hovering\" mean. One derivation was needed and is recorded as one: an annotation carrying both flags has to be *reachable* by the pointer, or the flag whose whole effect is conditioned on a hover would prevent the hover — so `NoView` suppresses interaction only where bit 9 is clear. No corpus document sets bit 9, on a scan of every uncompressed `/F` in all 974, so the fixture is synthetic and says so. **`LockedContents` is read since the four-hundred-and-sixty-ninth session and `Locked` is deliberately not.** This row predicted its own revisit — \"the day editing the producer's own annotation lands (`doc/todo/33`), this is the first row to revisit\" — and that day came. Bit 10 is \"do not allow the contents of the annotation to be modified by the user\", which is exactly what `ViewState::set_free_text` now does to an annotation the file states, so it is consulted — in `restriction::asserted`, beside §12.8.2.2's `/DocMDP` and Table 22, because `CLAUDE.md` makes obeying a document's restrictions the reader's own policy and a refusal wired into the edit could never become an *ask*. Bit 8 is **not** consulted and the table is why: `Locked` restricts \"the annotation ... [being] deleted or its properties (including position and size) ... modified\", and its row ends \"[h]owever, this flag does not restrict changes to the annotation's contents, such as the value of a form field\". So the flag that sounds like the prohibition is the one that states it does not apply. The corpus states bit 10 once and bit 8 three times across its 73 free text annotations and never both on one (`examples/free_text_census`), so the pair is pinned by two hand-built fixtures differing in that bit alone — trap 8's shape. What is still unread is bit 8 itself, which needs a reader that *moves* or *deletes* an annotation, and this program does neither. ADRs 0177 and 0304. **And the two flags do not reach §12.5.6.10's four subtypes**, which is a choice rather than a derivation: that clause's own `shall` — a text markup annotation appears \"in the text of a document\" — cannot hold at the same time as this one's at a magnification other than 1, and the standard states no precedence between them. ADR 0172, which counted the population first: 211 of the corpus's 511 text markup annotations carry `NoZoom` and all 211 are in one document. **`/FixedPrint` is Table 193's, not this table's**, and the sentence above said Table 167's until the four-hundred-and-ninth session read the three annotation rows against the standard: §12.5.3 never mentions the entry, and §12.5.6.22's watermark annotation is where it is defined. The rest of this row's citations were checked in the same reading and stand — Table 167 is the flags, Table 171 the annotation types, Table 170 the appearance dictionary, Table 226 the field entries."

[[clause]]
clause = "12.5.4"
title = "Border styles"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_link_border_is_drawn_inside_its_rectangle_in_its_own_colour", "crates/pdf-model/tests/annotations.rs::an_underline_border_is_drawn_inside_the_rectangle_rather_than_across_its_edge", "crates/pdf-model/tests/annotations.rs::a_border_style_dictionary_ignores_the_border_arrays_corner_radii", "crates/pdf-model/tests/variable_text.rs::a_free_texts_border_is_drawn_at_the_width_its_entries_state"]
note = "Table 168's /W and /D and three of its five styles are implemented for a constructed appearance, with §12.5.4's own rule about where the border goes: 'If present, the border shall be drawn completely inside the annotation rectangle', so the stroked path is the rectangle inset by half the width. **That sentence binds every style in Table 168 and was applied to four of the five until the four-hundred-and-fifty-eighth session**: U is 'a single line along the bottom of the annotation rectangle' and the line was centred *on* that edge, so half of it fell below /Rect — where `Constructed::bounded`'s clip cut it away, which is why the symptom was an underline half the width the document asked for rather than ink outside the rectangle. The path is that edge raised by half the width now, the same arithmetic the other four styles get. **The corpus reaches it once**: `annotation-border-styles.pdf` object 29 is a /Subtype /Link with /BS << /S /U /W 1 >> and no /AP, and it is the only U in the population — `crates/pdf-model/examples/border_precedence_census.rs` counts 33 781 annotations that state no /AP across the 964 openable documents and finds one U, one D and no B or I among them. S is solid and D dashes by /D or by /Border's fourth element with the phase the clause fixes at 0. B and I are drawn as S and reported: Table 168 calls each a 'simulated' embossed or engraved rectangle and states neither the highlight nor the shadow colour that would produce the illusion, and no corpus document writes either. Table 169's cloudy /BE is refused and reported for the same reason — 'a series of convex curved line segments' states no curve. An unrecognised style name falls back to S, which Table 168 requires. **And B and I are reported only where /BS is a *border*** — §12.5.6.5's link, §12.5.6.19's widget and, since the five-hundred-and-ninety-seventh session, §12.5.6.6's free text — because this clause names the four subtypes whose /BS is not one: \"Such dictionaries may also be used to specify the width and dash pattern for the lines drawn by line, square, circle, and ink annotations\". §12.5.6.8's row has that departure, which was the same shape as the two above and one sentence away from them. **And Table 166's precedence is honoured whole since the same session**: 'If an annotation dictionary includes the BS entry, then the Border entry is ignored' — Errata Collection 3 Issue #287 sharpens it to *shall be ignored* — and the width obeyed it while /Border's first two elements, the corner radii, were read whatever /BS said. The radii are the one thing the array states that Table 168 has no entry for, which is what made reading them beside a /BS look like completeness rather than a border the standard says is square drawn round. 192 of the constructed borders state both entries and 6 of those state a non-zero radius, all 6 on ink annotations whose mark is /InkList — so no corpus document witnesses it and the fixture is a pair differing only in whether /BS is present (trap 8). Where an appearance stream exists all of this is ignored, per §12.5.2. **And this clause's border reaches a third subtype since the five-hundred-and-ninety-seventh session**: §12.5.6.6's free text, whose Table 177 `/BS` is \"the annotation's border\" in the table's own words and whose colour no clause anywhere states — drawn in §8.4.1's initial black as a recorded choice, round the inner rectangle `/RD` names, with the sentence above about where a border goes doing the same work it does for a link. ADR 0432 has the argument and §12.5.6.6's row the measurement."

[[clause]]
clause = "12.5.5"
title = "Appearance streams"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/content.rs", "crates/viewer-core/src/interact.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_appearance_is_scaled_and_translated_onto_the_annotation_rectangle", "crates/pdf-model/tests/annotations.rs::the_pointer_chooses_between_an_annotations_appearances", "crates/pdf-model/tests/damaged_content_streams.rs::a_damaged_appearance_stream_draws_its_prefix_and_reports_the_shortfall", "crates/pdf-model/tests/names_are_bytes.rs::an_appearance_state_that_is_not_utf_8_selects_its_stream", "crates/pdf-model/tests/names_are_bytes.rs::two_appearance_states_differing_only_outside_utf_8_are_two_states"]
note = "The placement algorithm is implemented in all three steps, including the part that only a rotating /Matrix exercises: the transformed appearance box is measured around the mapped quadrilateral, and A maps its extreme corners onto /Rect's. /N is followed through an appearance subdictionary by /AS, and a state /AS names but the dictionary does not define draws nothing, which the clause gives as the specified 'reasonable behaviour'. §8.10.2's /BBox clip is applied, which this clause relies on — and taken back off where it removes no geometry, because an anti-aliased clip lying on a mark's own edge costs coverage the geometry states (ADR 0165: a widget's one-point border was losing a fifth of it). **It is applied to an appearance stream and not to every construction**, since the three-hundred-and-fourteenth session: §8.10.2's box belongs to a form XObject, a constructed appearance is not one, and four subtypes state their geometry \"in default user space\" rather than inside a box — §12.5.6.7's /L, §12.5.6.9's /Vertices, §12.5.6.10's /QuadPoints and §12.5.6.13's /InkList. A file whose /Rect does not contain those was having the marks its own clause states clipped away in silence, which no report could see because nothing refused. ADR 0193. §12.2's /ViewClip is not part of the exception: an annotation is drawn over the page and is not exempt from what the page is clipped to. The requirement that an appearance with no /Group 'shall be treated as a non-isolated, non-knockout transparency group' is satisfied by construction rather than by code: with the annotation's own /BM, /ca and /CA ignored (§12.5.2), that group has alpha 1, the Normal blend mode and no soft mask, and §11.6.7's NOTE 1 makes it identical to painting the elements directly, which is what the interpreter does. An appearance stream with **no** `/BBox` is placed by §12.7.4.3's default box — \"[t]he lower-left corner of the bounding box ( BBox ) is set to coordinates (0, 0) in the form coordinate system. The box's top and right coordinates are taken from the dimensions of the annotation rectangle\" — and named, since the hundred-and-twenty-fifth session (ADR 0113). That sentence is written for a *constructed* appearance and applying it to a stored one is an extension rather than a reading; what makes it the right one is that the alternative discards the whole annotation over an entry this algorithm needs only for a *scale*. 1 corpus document. **The same rule the other way** since the hundred-and-twenty-sixth session: an annotation with no `/Rect` is placed where its appearance's own box, mapped through its `/Matrix`, puts it — the two operands are the same kind of thing and a missing one makes the map the identity. `issue14438.pdf` states four ink annotations with no `/Rect` and `/BBox [0 0 0 0]`, so the file has stated an appearance covering no area; that draws nothing by Table 166's own excuse, and reporting the missing rectangle named the one entry that could not have changed the picture. A `/Rect` is still refused where there is no stored stream, because every construction is written into one. ADR 0114. **One sentence of this clause is not followed**: 'The transparency group shall be composited ... using the values of the BM, ca and CA entries in the annotation dictionary'. Table 166 says of each of /CA and /ca that it 'shall not be used if the annotation has an appearance stream ... in that case, the appearance stream shall specify any transparency', and §12.5.2 lists all three among the keys a reader shall ignore. Two statements against one, and the two explain the risk the one creates: highlight.pdf writes /CA 0.8 on the annotation and ca 0.8 inside the stream, so applying both would darken the highlight its producer specified. **Table 170's three appearances are chosen since the seventy-sixth session**: the clause states them as three situations rather than as a mode — normal \"when the annotation is not interacting with the user\", rollover when the cursor is in the active area, down \"when the mouse button is pressed or held down within\" it — so which one applies is a property of the pointer and lives in `view::ViewState`, beside §12.6.4's actions and for the same reason: a cursor is not in the document. Hit testing is `viewer-core`'s since the hundred-and-thirty-second session: a pointer position in device pixels comes back through the centring, the magnification, the crop box's origin and §7.7.3.3's rotation, and the annotation it lands on is the one whose appearance changes. The state is only changed for an annotation that *states* the appearance in question, because changing it invalidates the page's display list and a cursor crossing a link with only an `/N` would otherwise re-interpret the page for a picture that cannot differ. **And it is every annotation on the page since the two-hundred-and-fifty-third session, not every link.** This clause is written about an annotation — \"[a]n annotation may define as many as three separate appearances\" — and the region came from `link_at`, so `/R` and `/D` on any other subtype, and §12.5.6.19's `/H` on a widget, were unreachable from the only host in the tree. `annotation_at` is what §12.6.3's events already used, and it filters by `annotation::interacts`, so §12.5.3's ReadOnly — \"should not ... change its appearance in response to mouse motions\" — governs this choice by construction. ADR 0177. Table 170 requires only /N, so an annotation with no entry for the state it is in shows its normal appearance, which is also what printing asks for. **An appearance stream is a form XObject, and §8.7.2's pattern rule follows from that** — \"if a pattern is used within a form XObject … the pattern matrix maps pattern space to the form's default user space\" — which `draw_appearance` did not apply until the hundred-and-ninety-ninth session, so a shading pattern in a stamp was placed by the page's default space (ADR 0160). **The `NoZoom` and `NoRotate` scaling this clause defers to §12.5.3 is applied since the two-hundred-and-seventeenth session** (ADR 0168), and this row went on saying it was \"[n]ot implemented\" for twenty more — which is `doc/todo/01`'s fourth sweep on its own subject: a correction is a string, and the string was in the *other* row that describes the same mechanism. The adjustment is one similarity about the corner §12.5.3 names, composed before the page's transform; §12.5.6.10's four subtypes are outside it by the choice ADR 0172 records. **`AA` has a second caller since the three-hundred-and-seventy-first session and it draws nothing**: `annotation::stored_frame` answers with a stored appearance's box and this clause's map onto `/Rect`, so that §12.7.4.3's caret can be stated in the page's own space — the value is laid out in the *stream's* coordinates wherever the file carries one, and asking a second opinion about where that stream sits would put the caret beside the text it belongs in. ADR 0211. **And an appearance a host has undertaken to draw itself is not drawn here**, since the four-hundred-and-ninth session: §6.3.2.2 requires the appearance stream of every annotation whose flags call for one \"unless otherwise instructed\", and `pdf_model::view::WidgetAppearances::Delegated` is that instruction — a native form host places a real control over each of §12.7's widgets and would otherwise show a person every field twice. It reaches exactly the widgets `form::fields` answered for and no other annotation, so nothing leaves the page that no control replaced; `ViewState::of` is the other value, which is why the corpus, oracle, quorra and text gates produce byte-identical display lists (974 documents digested before and after). ADR 0245. **Where the standard states that annotations are painted in `/Annots` order is this clause and not §12.5.2**, which the same session found: an appearance's group \"shall be composited with a backdrop consisting of the page content along with any previously painted annotations\", and three source comments quoted a sentence about drawing order that ISO 32000-2 does not contain. **A stored stream that decoded only part-way is drawn and reported**, since the five-hundred-and-twenty-fourth: this clause makes an appearance a form XObject and §7.8.2 makes that a content stream. The damage is read where `annotation::decided` resolves the stream rather than where the appearance is run, because §12.7.4.3's regeneration replaces the content with a spliced *copy* of those bytes — so a report taken at the draw would go quiet for exactly the fields whose variable text a reader has changed. ADR 0359. **And the appearance is read as a *source* since the five-hundred-and-ninety-second**, which mattered here more than anywhere else: this clause's damage has to be answered where the stream is read rather than where it is drawn (ADR 0359), so a stored appearance was materialised twice over. Both questions now go through one `NestedContent`, so a stream the memo keeps is decoded exactly once for both and one it declines is *read* for the damage answer rather than allocated for it. ADR 0427. **`/AS` selects by bytes**, since the six-hundred-and-fourth session: the state name was folded through `from_utf8_lossy` before it probed `/N`, so a state outside UTF-8 drew nothing and two such states were one — the same defect twice over in the widget path `Field::is_on` uses. §7.3.5 and ADR 0439."

[[clause]]
clause = "12.5.6"
title = "Annotation types"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "Table 171's subtypes are all recognised. Where one carries an appearance stream it is drawn without regard to the subtype, which is what this clause's parent requires; where one does not, the subtype's own subclause decides, and the rows below say which are constructed, which are refused for stating no geometry, and which are excluded. Read against the code in the four-hundred-and-fifty-ninth session, together with §12.5.6.8's, as the last two rows written in the sitting that produced §12.5.4's two silent departures: `STANDARD_SUBTYPES` carries Table 171's twenty-eight names and `appearance::construct` switches on the subtype only where no appearance stream was found, so both sentences hold. The one qualification they do not state is §12.5.6.14's, whose popup is skipped before any other question is asked because it \"shall have no appearance stream\"; the reading that round produced is in §12.5.6.4's row."

[[clause]]
clause = "12.5.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "Table 171's list of subtypes is what decides the Invisible flag's scope in §12.5.3 and nothing else here switches on it, which is this clause's own requirement read through §12.5.5: a processor with no native support for a type still renders its normal appearance."

[[clause]]
clause = "12.5.6.2"
title = "Markup annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/popup.rs", "crates/pdf-model/src/markup.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_stored_appearance_ignores_the_annotations_opacity", "crates/pdf-model/src/popup.rs::a_rich_text_string_fills_a_window_a_plain_one_would_have_left_empty", "crates/pdf-model/src/markup.rs::only_a_group_reply_type_takes_the_primarys_entries", "crates/pdf-model/src/popup.rs::a_window_on_a_group_shows_the_primarys_words"]
note = "Table 166's `/CA` is read — **this row said \"Table 172's /CA … as Table 166's entry of the same name\" until the four-hundred-and-thirty-seventh session, and Table 172 states no `/CA` at all**: the entry was the markup annotation's in ISO 32000-1 and is one of the twenty-three common to every annotation in ISO 32000-2, beside `/ca`. `doc/todo/01`'s ninth sweep, whose whole subject is a number that exists and names the wrong table. Nothing else in the clause marks the page by itself: /T, /Subj, /RC, /IRT, /RT, /IT and /ExData are a title, a subject, formatted text, a reply relationship, an intent and external data, all of which reach a comments pane rather than a raster, and /Popup is the window §12.5.6.14 excludes from the page — **excludes from the page, and no longer from the program**: since the three-hundred-and-twelfth session /T is the title bar of that window and /Contents is the text in it, which is what this clause says both entries are for (ADR 0191). The paragraph rule is applied where the text is laid out: 'When separating text into paragraphs, a CARRIAGE RETURN (0Dh) shall be used and not, for example, a LINE FEED character (0Ah)' — a carriage return starts a paragraph, and a line feed is accepted as one too, because the sentence binds a writer and a reader that enforced it would show a producer's paragraph break as a space. **And `/RC` is read since the three-hundred-and-forty-second session** (ADR 0199), which is `doc/todo/01`'s capability shape thirty sessions after the capability arrived: Table 172 makes it \"[a] rich text string (see Adobe XML Architecture, XML Forms Architecture (XFA) Specification, version 3.3 ) that shall be displayed in the popup window when the annotation is opened\", and the window has existed since the three-hundred-and-twelfth. `popup::rich_text` takes the element content and none of the formatting — the markup is XFA's and principle 5 excludes it, while the characters are what the clause requires displayed and what NOTE 1 makes \"textually equivalent\" to `/Contents` where a file states both; a closing paragraph becomes the newline this clause asks a plain `/Contents` to spell with a carriage return, and a `/Contents` outranks an `/RC` for NOTE 1's own reason. **The corpus cannot exercise it and that is measured, not assumed**: `examples/markup_text_census` finds 71 annotations stating `/RC` over 33 documents, 18 of them with no `/Contents` — and all 18 are on `issue13447.pdf`, which states no `/Popup` at all, so not one corpus popup changes. Trap 8's converse, and the module's own tests are the only defence. What is owed is the accessibility half — /Contents is what §14.9.3 extracts as an alternate description. **/IRT and /RT were on this row's list of entries that reach ‘a comments pane rather than a raster’, and one half of them does not**, found by `doc/todo/01`'s fifteenth sweep in the four-hundred-and-eightieth session. The clause states a *group*: \"In PDF 1.6, a set of annotations may be grouped so that they function as a single unit when a user interacts with them.\" and then names the entries the group shares — \"Some entries in the primary annotation are treated as \"group attributes\" that shall apply to the group as a whole; the corresponding entries in the subordinate annotations shall be ignored. These entries are Contents (or RC and DS ), M , C , T , Popup , CreationDate , Subj , and Open .\" `/C` is ink and `/Contents` is what a free text annotation draws, so this was never only a pane: a subordinate's own entries were read from the subordinate and nothing said so. `crate::markup::group_source` is that sentence, read wherever this tree reads one of the ten keys — `crate::popup` for /T, /Contents, /RC, /M, /C, /Popup and the /Open that opens the window; `crate::appearance` for the /C of a text icon, a square or circle, a polygon, a text markup, an ink, a line and a free text border, and for the /Contents §12.5.6.6 lays out. /DS, /Subj and /CreationDate have no reader in this tree to correct, and /Subtype, /Rect, /F and /AP are deliberately not on the list: a subordinate is still drawn where and as it says. **The corpus cannot rank it and that is counted rather than assumed**: `examples/annotation_group_census` finds **one** /IRT in 34 835 annotations over the 964 openable documents — `issue13447.pdf`, a strike-out grouped with the caret that replaces it, whose primary states the same /C — so the fixtures are a pair differing only in /RT (trap 8) and each half was watched fail with the rule removed. **ISO 32000-2's own PDF is the witness**: 2074 /IRT in 11 462 annotations, 322 of them /RT /Group, every one naming a primary on the same page, and 213 popup windows hanging off a subordinate. `examples/spec_annotation_census` counted 1535 of its 2552 windows carrying text before this change and 1748 after — exactly those 213, each of them now showing the erratum the caret carries and the strike-out does not. **One hop, and no cycle is reachable from it**: the primary \"shall not have an IRT entry\", so the group is flat by construction. What is still owed is the other /RT: \"Interactive PDF processors shall not display replies to an annotation individually but together in the form of threaded comments\" — a reply is /RT /R, which is also the default, and threading them needs a comments panel no host here has; ISO 32000-2 states 1752 of them, each naming a popup window of its own, and this hands a host 1752 separate windows. /Subj and /IT reach the same absent pane, as does an `/RC` on an annotation that names no popup window to display it in. ADR 0315."

[[clause]]
clause = "12.5.6.3"
title = "Annotation states"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_appearance_state_is_selected_by_as"]
note = "This clause is about /State and /StateModel on a reply annotation — Marked or Review, set by a user in a comments pane — and neither marks the page. It is not to be confused with §12.5.5's appearance *state*, which /AS selects and which is implemented; the two words are unrelated and the test named here is for the second. Nothing is owed for rendering; a review workflow is owed for a viewer with interaction. **The clause sends a reader to the wrong table for `/IRT` twice and Errata Collection 3 corrects both** (Issue #479, `/State` `Review` `Completed`): the sentence that defines the state annotation and the bullet requiring its `/IRT` to \"refer to the original annotation\" each cite Table 176, which is a link annotation's, and the amended text cites Table 172. The tree was already on the amended side — `conformance`'s own table claims put `/IRT` under Table 172, and `markup::group_source` reads the entry citing §12.5.6.2 rather than any table — so the erratum vindicates it rather than moving anything."

[[clause]]
clause = "12.5.6.4"
title = "Text annotations"
status = "partial"
code = ["crates/pdf-model/src/icon.rs", "crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/popup.rs"]
test = ["crates/pdf-model/tests/annotations.rs::each_of_the_seven_standard_icon_names_draws_its_own_shape", "crates/pdf-model/tests/annotations.rs::table_166_s_colour_is_the_icon_s_background", "crates/pdf-model/src/popup.rs::a_text_annotations_own_open_opens_its_popup"]
note = "**§12.5.6.4's own sentence about the view is applied since the two-hundred-and-twentieth session**: 'Text annotations shall not scale and rotate with the page; they shall behave as if the NoZoom and NoRotate annotation flags ... were always set' — a `shall` about the *subtype*, so an annotation whose `/F` sets neither flag gets both and one that clears them cannot. It was recorded here as §12.5.3's departure and became reachable the moment that departure ended (ADR 0168); `icon.rs`'s module comment carried the same expired blocker in prose, which is `doc/todo/01`'s first sweep finding one in *code*. The icon is still drawn on the largest square that fits inside `/Rect`, so that a symbol in a rectangle of another shape is not distorted — **and on a square of its own where `/Rect` states no area at all**, since the two-hundred-and-sixty-fifth session. The clause's first sentence is why: a text annotation is \"a 'sticky note' attached to a point in the PDF document\" and \"shall appear as an icon\", so a degenerate rectangle is not this annotation saying it covers nothing the way a `Square`'s would be, and the sentence above supplies the size — a fixed size on the screen is by definition not `/Rect`'s. How large is a choice, twenty units, recorded beside the artwork's own. `rc_annotation.pdf` is the one corpus witness and this tree drew nothing for it inside an `ambiguous` verdict; `doc/todo/00`'s step 7 sweep found it at −1.783 of 255, and two of the four references draw an icon there. A text annotation 'shall appear as an icon' whose name comes from Table 175's /Name, and the clause requires a processor to 'provide predefined icon appearances for at least the following standard names: Comment, Key, Note, Help, NewParagraph, Paragraph, Insert' while stating what none of them looks like. **That sentence is a shall, and this row said reported for a hundred and nineteen sessions on the strength of the half of it that is a silence.** The three neighbouring icon clauses — §12.5.6.12, §12.5.6.15, §12.5.6.16 — say should, and being read as one population with this one is what hid the difference; refusing where a clause obliges is a conformance failure, not restraint. Since the hundred-and-twentieth session the seven names are drawn by crate::icon, whose artwork is this processor's invention and is documented there as a choice, with the two things the document does state read in crate::appearance: which icon (Table 175's /Name, default Note) and the colour behind it (Table 166's /C, 'The background of the annotation's icon when closed'). A /Name outside the seven is reported by name, because 'Additional names may be supported as well' is a permission and Table 175's default answers an absent entry rather than an unrecognised one. partial for two things the clause states and this does not do: Table 175's /State and /StateModel, which §12.5.6.3 makes a review status rather than a mark. **The third was \"the popup window /Open selects, which §12.5.6.14 does not draw for any subtype\", and the window is drawn since the three-hundred-and-twelfth session** (ADR 0191) — for every subtype, because a popup belongs to a markup annotation rather than to this one. **The entry that selects it was still read nowhere, which this row asserted the opposite of for a hundred and forty-seven sessions.** Table 175 gives a text annotation an /Open, \"[a] flag specifying whether the annotation shall initially be displayed open\", and this clause says what an open one is: \"when open, it shall display a popup window containing the text of the note in a font and size chosen by the interactive PDF processor\". A file saying its sticky note starts open showed no window and reported nothing — a shall failed in silence. Read since the four-hundred-and-fifty-ninth session by popup::opens_with_the_page, as a disjunction with Table 186's own /Open rather than a precedence over it: each entry states a condition under which the window is open, both default to false, and neither states one under which it is closed, so there is no conflict for Table 186's four-entry override list to have settled. Only a text annotation's counts, because Table 175 is the only table outside Table 186 that gives an annotation the entry at all — §12.5.6.7 to §12.5.6.13 each say their annotation displays a window \"when opened\" and none states an entry that opens it. **The corpus cannot rank it**: `examples/open_annotation_census.rs` finds 28 text annotations over the 964 openable documents, exactly one stating /Open true, and that one's popup already states its own — so the fixture is a pair differing only in the rule (trap 8). **The other readers disagree here and the clause is not ambiguous**: pdf.js reads /Open in one place, PopupAnnotation's constructor, and its TextAnnotation reads /Name, /State and /StateModel and not /Open — so it shows no window on the same file. Table 175 states the entry and this clause states what an open text annotation does, both as shalls addressed to the processor, which is principle 5's case for following the sentence and recording the disagreement rather than matching it. ADR 0294. **No corpus document reaches any of this**: exactly one of the 974 carries a text annotation with no appearance stream and its /Rect has zero area, so it drew nothing before this change and draws nothing after it. The '1 corpus document' this row used to claim was a count of the annotation, not of a report. **And which dictionary that /Open is read from is §12.5.6.2's**, since the four-hundred-and-eightieth session: /Open is one of the nine group attributes, so a text annotation that is a group's subordinate opens its window on the primary's entry. The /Subtype beside it is deliberately not — the clause's list names nine entries and a subtype is not one of them, so `opens_with_the_page` asks the parent what it is and the group what it says. ADR 0315."

[[clause]]
clause = "12.5.6.5"
title = "Link annotations"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/link.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_link_border_is_drawn_inside_its_rectangle_in_its_own_colour", "crates/pdf-model/src/link.rs::quad_points_narrow_the_region_and_a_stray_one_is_ignored"]
note = "**Followed since the fifty-seventh session**: a click resolves the link under it through §12.3.2's destinations and turns the page, which is what §12.5.6.5's first sentence is about — a link \"represents either a hypertext link to a destination elsewhere in the document … or an action to be performed\". `/QuadPoints` bounds the activation region and Table 176's third condition is honoured exactly: coordinates that \"lie outside the region specified by Rect\" send the *whole* annotation back to its rectangle rather than widening it. **This row contradicted itself for a hundred and forty sessions and the two-hundred-and-seventy-eighth found it**: the sentence here said \"/H's highlighting mode is still a response to a mouse this program does not draw\", and the sentence below says it is honoured since the hundred-and-thirty-eighth, with an ADR. The later one is right — a press on a link inverts its rectangle or its border (ADR 0123), and since the two-hundred-and-fifty-third the pointer reaches every subtype rather than only a link (ADR 0177). **A sixth way for a note to be wrong: not stale against the code, but against its own next paragraph.** What is true of this clause and not of the mouse is that a link whose action is a URI or a launch leads nowhere here by design (principle 3). 54 corpus documents have a link on page one — 33 125 of them, 32 768 in one stress-test file — and 36 lead to a page of their own document. A link's visible part is its border, and this is the one subtype whose appearance is stated outside its own subclause: Table 166 makes /C 'a colour used for ... The border of a link annotation' and /Border the rounded rectangle it is drawn as, and §12.5.4 puts it inside /Rect. Both are implemented, including the corner radii and Table 176's /BS. A link with no /C draws nothing, which is a documented choice rather than a derivation — Table 166 gives an *empty* /C the meaning 'No colour; transparent' and states no default for an absent one, and inventing black would put a box around every link whose file says nothing about its colour. Not implemented, and not visible: /A, /Dest and /PA are navigation, /QuadPoints bounds the activation region rather than the drawing (with one exception this crate cannot reach, 'such as when applying an underline border style'), and Table 176's /H — the same highlighting mode §12.5.6.19 states for a widget, with four modes rather than five — is honoured since the hundred-and-thirty-eighth session, so a press on a link inverts its rectangle or its border where the file says to (ADR 0123). Four corpus pages are contradicted for drawing this border at all; oracle.rs's CONTRADICTED_LINK_BORDER has the analysis, and neither silent reference is reading the clause differently. **Re-read whole in the five-hundred-and-seventeenth**, which is what this row's own sixth shape asks for — the paragraph above a correction, not the correction onwards — and the two halves agree: the press is honoured, and what stays `partial` is a URI or a launch leading nowhere by design."

[[clause]]
clause = "12.5.6.6"
title = "Free text annotations"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/variable_text.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_free_text_annotation_draws_its_contents", "crates/pdf-model/tests/variable_text.rs::a_free_text_annotation_a_person_added_answers_the_carets_three_questions", "crates/pdf-model/tests/variable_text.rs::a_callout_lines_knee_is_where_its_third_pair_of_coordinates_puts_it", "crates/pdf-model/tests/variable_text.rs::a_callouts_line_ending_decorates_the_end_the_table_names", "crates/pdf-model/tests/variable_text.rs::a_callout_line_is_drawn_only_under_the_intent_that_makes_it_meaningful", "crates/pdf-model/tests/variable_text.rs::a_free_texts_inner_rectangle_moves_its_text_and_not_its_callout", "crates/pdf-model/tests/variable_text.rs::a_callout_this_reader_cannot_read_is_named_rather_than_invented", "crates/pdf-model/tests/saving.rs::a_free_text_annotation_carries_the_font_its_default_appearance_names", "crates/viewer-core/tests/headless.rs::a_free_text_annotation_is_drawn_from_a_drag_typed_into_and_read_back", "crates/pdf-model/tests/variable_text.rs::a_free_text_annotation_the_file_states_is_retyped_redrawn_and_written_back", "crates/pdf-model/tests/variable_text.rs::a_corpus_documents_own_free_text_annotation_is_retyped_and_written_back", "crates/pdf-model/tests/variable_text.rs::clearing_a_free_text_annotation_does_not_uncover_its_rich_text", "crates/pdf-model/tests/variable_text.rs::a_free_texts_border_is_drawn_at_the_width_its_entries_state", "crates/pdf-model/tests/variable_text.rs::a_free_texts_cloudy_border_is_named_rather_than_drawn_straight"]
note = "A free text annotation \"display[s] text directly on the page\" out of `/Contents`, laid out by the `/DA` appearance string and `/Q` quadding of §12.7.4.3 — **which `appearance::free_text` does, and has since the twenty-third session.** This row said \"refused and named\" until the hundred-and-fifteenth, which is the ledger understating what the code does, and the four corpus documents it counted are refused for a different reason: their `/DA` names a font the `/DR` does not define, which is §12.7.4.3's malformed-file case. `/RD` insets the box, as §12.5.6.8's does. **Table 177's `/RC` generates the appearance where the file states no `/Contents`, since the three-hundred-and-eighty-seventh session** (ADR 0224). This row said `/RC` and `/DS` \"are XFA rich text, which principle 5 excludes\" — true of the second and false of the first, and it is the *fourth* row to carry that sentence about an `/RC`: §12.5.6.2's was corrected in the three-hundred-and-forty-second and §12.5.2's in the three-hundred-and-seventy-fifth, and neither round read this one. The distinction the standard draws is a `shall`: Table 177's entry \"shall be used to generate the appearance of the annotation\", and its NOTE separates it from the one those rows are about — \"[a]s freetext annotations do not have an open state this cannot apply to the popup window as described for the RC key in Table 172\". So `popup::rich_text` is `pub(crate)` and `appearance::free_text` takes its characters where Table 166's `/Contents` is absent, in §12.5.6.2 NOTE 1's order. `/DS` keeps the exclusion for the reason this row gave both of them: it is \"[a] default style string, as described in … XFA\", with no `shall` about what it displays. **The corpus cannot exercise it and that is counted rather than assumed**: `examples/markup_text_census` finds 22 free text annotations stating `/RC` and every one of them states a `/Contents` too, so not one corpus page changes — trap 8's converse, the same position ADR 0199 was in. **And since the four-hundred-and-first session a person can make one**, which is the other direction this row carried as owed for eighty sessions. `Edit::FreeText` takes the two corners of a **drag** — there is no text on the page for this subtype to be over, which is what separates it from §12.5.6.10's four — `ViewState::add_free_text` writes Table 177's Required `/Subtype` and `/DA` with Table 166's `/Rect`, `/Contents` and `/F 4`, `Edit::SetFreeText` puts the text in by object, and §7.5.6's update writes the annotation, its constructed `/AP` and the page's `/Annots`. Three of those are choices and are recorded as choices, because the standard describes *reading* a free text annotation and states nothing about creating one: the `/DA`'s size (12 points rather than the clause's auto-sizing, which grows one character to fill whatever box was dragged), the colour going into the `/DA` rather than into Table 166's `/C` — that entry is an icon's background, a popup's title bar and a link's border, and this subtype has none of the three — and a `/BS` stating Table 168's `/W` 0. **The last of those was found by running the program**: Table 166's `/Border` states \"Default value: [0 0 1].\", so an annotation saying nothing about its border has one a point wide, and this clause's own refusal then reported the annotation the program had just created. A file this program writes may not leave that open, so it says the annotation has no border. ADR 0238. **And typing into one works because §12.5.6.6 sends this subtype to §12.7.4.3**: `appearance::frame` and `laid_out_in` dispatch on the subtype and the caret, the point-to-offset inverse and the selection shapes are the same three functions a widget's are — the layout underneath is one layout and only the way in differs. `partial` for the rest of Table 177: **`/CL`'s callout line and the `/LE` that ends it are drawn since the four-hundred-and-ninety-fourth session** (ADR 0329). This row said they were \"reported by name … on the same grounds `/BS`'s border is refused on — no clause states a colour for either\", and the first half of that is true of both entries. What was then said to separate them — that Table 166's `/Border` default `[0 0 1]` would put a mark on nearly every free text annotation there is — **separated them for a hundred sessions and no longer does**, because it was a claim about producers rather than about the standard and the border paragraph at the end of this row is where it was measured. A callout exists only where a producer wrote the numbers and the intent, and those numbers say the one thing no other mark on the page says — which place the note is about. So the line is drawn, and its colour is not invented but **omitted**: §8.4.1's Table 51 gives the graphics state's colour parameter \"Initial value: black\", so a construction naming none paints in it, and this one writes that black out explicitly rather than depending on what ran before the appearance. Its width is a choice at §12.5.4's one point, because Table 177 binds `/BS` to a different mark — the `/RD` row says border styles and effects \"shall be applied to the border of the inner rectangle\". **The condition to draw is the table's own and it is not the entry's presence**: `/CL` is \"meaningful only if IT is FreeTextCallout\", `/IT` defaults to `FreeText`, and `FreeTextTypeWriter` states that \"no callout line is drawn\" — so the other two intents draw nothing **and report nothing**, because a report names what this program owes and the table owes a mark only where it says one is meaningful (trap 11). A `/CL` whose length is neither of Table 177's four or six is refused and named rather than drawn from a prefix; a `/LE` outside Table 179's ten names is reported while the line is still drawn, which is ADR 0075's rule one entry over. Table 179's shapes are §12.5.6.7's own and are drawn by the same code, with the one ending at the endpoint the table names — \"the endpoint defined by the pairs of coordinates ( x 1 , y 1 )\" — pointing away from the knee; the four styles the table fills \"with the annotation's interior colour, if any\" are stroked alone, since Table 177 gives this subtype no `/IC`. **And a free text annotation joined the constructions `/Rect` does not bound**: `/CL`'s coordinates are \"in default user space\", the same words ADR 0193 found on the four entries it listed, and joining costs the *text* nothing because §12.7.4.3's own example puts the clip inside the construction. **The corpus cannot exercise any of it**: `examples/free_text_census` counts 0 of 73 free text annotations stating a `/CL`, on every page rather than on first pages, so the rule is defended by hand-built pairs alone — trap 8, and the pairs differ in the two-point against three-point sentence, in `/LE`, in `/IT` and in `/RD`. `/DS` keeps principle 5's exclusion for the reason above. **And since the four-hundred-and-sixty-ninth session the annotation the *file* states can be retyped too**, which this row carried as owed for sixty-eight sessions on the grounds that replacing a producer's object is a decision nobody had made. It is §7.5.6's own second case — \"objects that have been changed, replaced, or deleted\" — and the producer's bytes survive it byte for byte, so it is the same writing `CLAUDE.md` permits rather than a different one. `ViewState::free_text_at` answers for the page's own `/Annots` as well as for what a person added, `set_free_text` records a replacement of Table 166's `/Contents`, and `save` writes the annotation with that text and a **replaced** `/AP`. Two things in that are readings rather than mechanics. Table 177 makes the file's own `/AP` decisive — \"[t]he annotation dictionary's AP entry, if present, shall take precedence over the DA entry\" — which is a precedence between two things the *file* says about one text, so once the text is the reader's the stream describes an annotation that no longer exists, and this clause's own sentence says where the appearance comes from instead: \"12.7.4.3, 'Variable text', describes the process of using these entries to generate the appearance of the text in these annotations\". And the appearance is **replaced rather than spliced**, which was measured rather than assumed: §12.7.4.3's closing paragraph appends its new contents where a stream holds no `/Tx` marked content, and `examples/free_text_census` counts **67 of the corpus's 73 free text annotations carrying a stream, 11 of them with such a region and 56 without** — so the splice reading would have drawn the new text on top of the old for five annotations in six. A retyped annotation this program cannot lay out is written with its `/Contents` and no appearance at all, and `Written::unappeared` names it, because Table 177 makes `/DA` Required and the next reader can generate what this one could not. ADR 0304. **And Table 177's `/BS` border is drawn since the five-hundred-and-ninety-seventh session** (ADR 0432), which is where the refusal above was retracted. §12.5.4 states the whole of its shape: \"If neither the Border nor the BS entry is present, the border shall be drawn as a solid line with a width of 1 point\", drawn \"completely inside the annotation rectangle\", round the inner rectangle `/RD` names — that row says border styles and effects \"shall be applied to the border of the inner rectangle\". Table 168's `/S` styles it, because §12.5.4 names the four subtypes whose `/BS` supplies width and dash alone and free text is not one of them. **The colour is a choice and is recorded as one.** Read across the neighbourhood the standard states a border colour for a link (Table 166's `/C`) and for a widget (Table 192's `/BC`) and for nothing else, so black is not invented but omitted — §8.4.1's Table 51, \"Initial value: black\" — exactly as `/CL`'s is. **The claim the refusal rested on is now counted**: `examples/free_text_census` finds 6 of the corpus's 73 free text annotations reaching a constructed appearance at all, **every one of them stating `/Border` and four of them stating a width of zero**, so not one relies on the default and the population that refusal fired on was two annotations in one fuzzed document (trap 11). A cloudy `/BE` stays refused whole — §12.5.4 gives this subtype the entry in as many words, \"Beginning with PDF 1.6, free text annotations may also have a BE entry\", and a different border is not an extra mark (ADR 0106). What is still owed on Table 177 is `/DS`."

[[clause]]
clause = "12.5.6.7"
title = "Line annotations"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_line_annotations_leader_lines_put_the_line_where_the_clause_says", "crates/pdf-model/tests/annotations.rs::a_leader_line_has_an_offset_before_it_and_an_extension_beyond_it", "crates/pdf-model/tests/annotations.rs::a_captioned_line_draws_its_contents_where_cp_states", "crates/pdf-model/tests/annotations.rs::a_caption_this_table_cannot_place_is_named_and_the_line_is_still_drawn"]
note = "Table 178's `/L` drawn as a stroked segment in `/C` at the width and dash §12.5.4 gives, **with the leader lines the clause states**: `/LL` makes `/L` \"represent the endpoints of the leader lines rather than the endpoints of the line itself\" and then says exactly where the line goes — perpendicular, by `/LL`, in the direction \"that is clockwise when traversing the line from its starting point to its ending point\", with a negative value meaning the other side — so `/LLE`'s extension and `/LLO`'s offset complete a construction that needs nothing invented. It was refused until the eighty-fifth session on the true observation that `/L` is not the line, which is a reason to compute the line rather than to decline; the refusal also fired on the entry's presence rather than its value, so a document stating `/LL 0` — the clause's own \"no leader lines\" — was declined for asking for nothing. **Table 179's ten endings are drawn since the three-hundred-and-fourteenth session** (ADR 0192). The table names ten shapes and states not one dimension, which is a `shall` behind a silence about artwork — §12.5.6.4's shape exactly — so the size is a multiple of the only length the annotation supplies, §12.5.4's border width, and the multiple is a choice recorded beside the code. Three more choices with it: an arrowhead points away from the line (which is what makes Table 179's own `ROpenArrow` and `RClosedArrow`, \"in the reverse direction\", mean anything), an \"acute angle\" is 60° at the apex, and where `/LL` moves the line the endings go on the line proper, because the endpoints Table 178 names are the leader lines' and an ending is an ending of a line. A `/LE` naming something outside the table is reported rather than dropped to `None`. **`/Cap` is drawn since the five-hundred-and-seventy-fourth session, and what it retires is a refusal whose sentence was true and whose inference was not** (ADR 0431). The sentence was \"a caption needs a font and no entry of a line annotation states one\" — true, and Table 172 gives a markup annotation no `/DA` either — while the entry's own cell is a `shall` about the appearance: \"the text specified by the Contents or RC entries shall be replicated as a caption in the appearance of the line\". ADR 0109's rule decides it, as it did for §12.7.5.4's list box one round earlier: the question a silence poses is not whether a reader may fill it but whether a sentence around it requires one to. So `/CP` and `/CO` are read with it and the position is the clause's to the point — `/CO`'s \"horizontal offset along the annotation line from its midpoint\" and \"vertical offset perpendicular to the annotation line\" are what say the caption is set in the *line's* axes rather than the page's, and the caption turns with the line. One number is this program's, and it is the size: 12 points, which is what §12.7.5.3's own EXAMPLE sets variable text at and is the same worked example `variable_text::LINE_HEIGHT` takes its ratio from. The face is §9.6.2.2's Helvetica out of this binary, ADR 0112's stand-in. **Figure 81 is what settled the rest, and it rejected a reading rather than filling a gap**: the first attempt auto-sized the caption to the line's own length, on the argument that \"centred inside the line\" makes the line the caption's only stated extent — and the figure's third example is captioned *This is a caption that is longer than the line*, drawn at the same size as the other two and overhanging both ends. The same figure says an inline caption sits in a **break** in the line and that the caption is the line's colour, both of which the sentence leaves open and the figure the sentence cites does not. A break that would leave no line at all is not taken, because §12.5.6.7 makes the line required; a `/CO` that lifts the caption clear of the line takes the break with it, which is Figure 82's case. A `/CP` outside the table's two names and a `/CO` that is not two numbers are reported and the line is still drawn. **No document in any population this project measures states `/Cap true`** — `examples/witness_census` finds two stating the key at all and both write `false` — so the witnesses are hand-built, which is trap 8's case and §12.5.6.6's `/CL` precedent (ADR 0329). `/IT` and `/Measure` are unread. **This row said the corpus's one witness could not show the change** — \"`issue13447.pdf`'s arrow-ended line states an `/L` outside its own `/Rect`, so §12.5.5 clips away the line it now draws, which is that file disagreeing with itself rather than anything this reader does\" — and the second half of that was wrong: §8.10.2's box is a *form XObject's*, a construction is not one, and this clause states its geometry \"in default user space\". The line and its ending are drawn where the clause puts them (ADR 0193), and the file's `/Rect` is wrong without being load-bearing. **Read and kept in the five-hundred-and-twenty-fifth**, with the unread four checked one at a time: the tree's one reader of `\"IT\"` is §12.5.6.6's free text callout intent in `appearance::callout`, its one reader of `\"Measure\"` is §12.10.2's viewport in `measurement.rs`, and nothing names `/CP` or `/CO` at all — two clauses sharing a short key, which is what makes this list worth re-checking rather than re-reading."

[[clause]]
clause = "12.5.6.8"
title = "Square and circle annotations"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_circle_is_an_ellipse_inscribed_in_its_rectangle", "crates/pdf-model/tests/annotations.rs::a_squares_border_style_names_no_bevel_to_report_and_a_links_does"]
note = "'Square and circle annotations shall display, respectively, a rectangle or an ellipse on the page ... inscribed within the annotation rectangle', filled with Table 180's /IC and stroked in /C at §12.5.4's width — implemented, with /RD's four differences applied in the clause's own order of left, top, right, bottom. The ellipse is four cubic Béziers rather than a conic, which is an approximation this crate documents: PDF has no operator that draws an ellipse. /BE's cloudy border is refused and reported. **What Table 180's /BS supplies here is two of Table 168's entries and no more**, which this row did not say and the code did not obey until the four-hundred-and-fifty-ninth session: the table gives it as 'specifying the line width and dash pattern that shall be used in drawing the rectangle or ellipse', and §12.5.4 states the same restriction for four subtypes at once — 'Such dictionaries may also be used to specify the width and dash pattern for the lines drawn by line, square, circle, and ink annotations'. So Table 168's /S styles nothing on this subtype: the mark is the annotation's own rectangle rather than §12.5.4's border around one. A /S of B or I was nevertheless reported as an appearance this crate could not derive, which named a gap the clause does not have — trap 11's shape and the mirror of §12.5.4's own departure one sentence over, an entry consulted where its table says it supplies nothing. §12.5.6.9's polygon, under the identically worded Table 181, never reported it. **No corpus document witnesses it**: `examples/border_precedence_census.rs` finds no B and no I among the 33 781 annotations that state no /AP, so the fixture is a pair — the same /BS on a square, which reports nothing, and on a link, whose /BS is §12.5.4's border and where the bevel is a real absence (trap 8). Read against the code in the four-hundred-and-fifty-ninth session; §12.5.6.8's other claims held — the inscription is `Border::inset`'s half-width and /RD's order is the clause's."

[[clause]]
clause = "12.5.6.9"
title = "Polygon and polyline annotations"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_polygons_interior_colour_fills_it_and_a_polylines_does_not"]
note = "Table 181's /Vertices and PDF 2.0's /Path are both built — the second as the m, l and c operands it defines, superseding /Vertices as the table requires — and stroked in /C at §12.5.4's width, closed for a polygon and open for a polyline. /IC divides the two subtypes and the clause says how: 'For Polyline annotations, the value of the IC key is used to fill only the line ending. However, for Polygon annotations, the value of the IC key is used to fill the entire shape', so a polygon's fills it and a polyline's fills its line endings — **which are drawn since the three-hundred-and-fourteenth session** (ADR 0192), so the polyline half of that sentence finally does something. Each end takes its direction from its own leg rather than from the whole shape, because a polyline bends. **A polygon's `/LE` is reported rather than drawn**, and the clause is the reason: its first and last vertex are implicitly connected, so it has no end to put one on. So is a `/Path`, whose curves this routine does not hold the ends of. A cloudy /BE is still a whole refusal, and the difference is the point — an ending is an extra mark on a shape Table 181 makes required, while a cloudy border is a *different* border, so drawing a straight one would put a shape on the page the file did not describe. Table 181's `/Vertices` are \"in default user space\", so the construction is not clipped to `/Rect` (ADR 0193): two of `issue13447.pdf`'s polylines state a rectangle beyond the page's own width and their vertices are on it. /IT and /Measure are not read. Errata Collection 3 rewords Table 181's `/Vertices` (Issue #444, `/State` `Review` `Accepted`) from '(Required unless a Path key is present, in which case it shall be ignored)' to a separate 'Shall be ignored if Path is present.'; `appearance::path` already takes `/Path` first, so nothing moves. Read in the four-hundred-and-seventeenth session. **Read and kept in the five-hundred-and-sixty-fifth**: the one reader of `\"IT\"` in the tree is still §12.5.6.6's callout intent and the one reader of `\"Measure\"` is still §12.10.2's viewport, so both entries are unread here as the row says, and a polygon's `/LE` is still reported on the clause's own reason."

[[clause]]
clause = "12.5.6.10"
title = "Text markup annotations"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::each_text_markup_draws_its_own_mark"]
note = "The four text markup subtypes are drawn — and **added**, since the three-hundred-and-twenty-first session: `ViewState::add_markup` is what a person marking up a selection produces, `Edit::Markup` is the message that asks for it, and §7.5.6's update writes the annotation and appends the reference to the page's `/Annots` (ADR 0196). The clause's own construction draws it, because an annotation this program made is not a second kind of annotation. This row said they were refused for eighty sessions after they stopped being — so did `appearance.rs`'s own module comment, and both were corrected in the hundred-and-fifteenth session. What the clause states is more than the first reading found (ADR 0043): the mark's kind — they \"shall appear as highlights, underlines, strikeouts (all PDF 1.3), or jagged (\"squiggly\") underlines\" — its region, Table 182's `/QuadPoints`, its orientation, \"[t]he text shall be oriented with respect to the edge connecting points ( x 1 , y 1 ) and ( x 2 , y 2 )\", and Table 166's `/C` for the colour. What it leaves is a *thickness*, and the quadrilateral supplies the only length there is: its own height, since it \"shall encompass a word or group of contiguous words\". A sixteenth of that for a bar, a twelfth and a third for a squiggle's amplitude and wavelength, and a strikeout across the middle — fractions rather than constants, so they are right at every font size. A highlight is filled under `Multiply`, which §11.3.5.2 makes the one blend mode whose \"result colour is always at least as dark as either of the two constituent colours\" and therefore the standard's own guarantee that the text under the wash survives it. The corpus is what says the thickness is a *choice*: three references draw three pictures of `annotation-highlight-without-appearance.pdf`. **And a second choice arrived in the two-hundred-and-thirty-sixth session, from a conflict with §12.5.3.** That clause's `NoZoom` says an annotation \"shall always maintain the same fixed size on the screen\"; this one says a text markup annotation \"shall appear ... in the text of a document\", and Table 182 states its quadrilaterals \"in default user space\", each encompassing \"a word or group of contiguous words in the text underlying the annotation\". At any magnification but 1 the two `shall`s cannot both hold, and the standard states no precedence — §12.5.6.4 shows it legislating these flags per subtype when it means to, and it does not here. This tree lets the four subtypes scale with their text: this clause says what the annotation *is*, and §12.5.3 offers a display option annotations have in general. Counted first: 511 text markup annotations in 34 documents, 211 carrying `NoZoom`, all 211 of them strike-outs in `ISO_32000-2_sponsored_EC3.pdf` at one flag value. ADR 0172. **The entry is read in the other direction too, since the four-hundred-and-twenty-first session**: Table 182 says each quadrilateral \"shall encompasses a word or group of contiguous words in the text underlying the annotation\", which makes the text under a markup a fact the *file* states, and `pdf_model::retrieval::text_under` reads it back off `Interpretation::text_layer` through `content::page_transform`. It was `tools/spec-errata`'s private function for five sessions and is now the model's, with two callers — that tool and `tools/pdf-retrieve`, which is what answers \"this section's text, with the errata attached\". A bounding box rather than the crossing-number rule the clause's counterclockwise order would want, because this clause's own NOTE says producers have written the corners \"in a different order\". ADR 0257. **A mark-up over a selection that crosses a page boundary is one per page since the six-hundred-and-ninth session**, which §12.5.2 requires outright — an annotation dictionary \"shall be referenced from the Annots array of only one page\" — and which Table 182 states again by putting the quadrilaterals \"in default user space\", a page's. One `Edit::Markup` therefore produces one annotation per page the selection touches, and they undo together because the gesture was one. ADR 0444."

[[clause]]
clause = "12.5.6.11"
title = "Caret annotations"
status = "reported"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_caret_is_reported_whether_or_not_it_asks_for_a_paragraph_symbol"]
note = "A caret annotation is 'a visual symbol that indicates the presence of text edits' and Table 183 adds only /RD, the difference between /Rect and 'the actual boundaries of the underlying caret', and /Sy naming a paragraph symbol. The symbol itself is not stated anywhere, so an appearance-less caret is refused and named. **Reconsidered in the hundred-and-twenty-fourth session against ADR 0109's rule, and the refusal stands.** The tempting reading is §12.5.6.10's, where the clause names the mark — 'shall appear as highlights, underlines, strikeouts … or jagged (\"squiggly\") underlines' — and leaves only a thickness; this clause never says the annotation shall appear as a caret. It says it *is* 'a visual symbol', indefinitely, and Table 183 then calls the thing whose boundaries /RD measures 'the caret'. A subtype name is not a statement of artwork, and the question ADR 0109 leaves is not 'may I fill this silence' but 'does a sentence around it require me to'. Nothing here does. All 6 corpus carets carry an /AP. **And this note said '[t]he symbol itself is not stated anywhere', which is wrong about half of Table 183 and was found in the five-hundred-and-eighty-ninth session by reading the table rather than the prose above it.** The *caret* is stated nowhere; the *symbol* is stated by name and by character — \"P A new paragraph symbol (¶) shall be associated with the caret\" — which is a `shall` and a code point, more than §12.5.6.4's seven mandatory icons get and more than §12.5.6.12's legends get. So the refusal cannot rest on 'nothing is stated', and what it rests on instead is trap 5's additive-or-substitutive test read off the *same table*: `/RD` measures a difference that \"can occur. When a paragraph symbol specified by Sy is displayed along with the caret\", so the pilcrow accompanies the caret rather than standing in for it. Drawing it alone would put a mark beside the mark nobody can derive, which is a worse answer than drawing neither. The refusal is therefore unchanged and its reason is stronger. **What is genuinely owed is a reader for `/Sy` and `/RD`**, which this tree has none of — no source names either key — and the row is `reported` rather than `partial` because the entries turn nothing on while the geometry they qualify is underivable. The population says why nothing found this: `/Sy` is stated as a name by **0 of the 974** (`examples/witness_census`), `/Caret` by four, and the corpus gate reports no caret at all because every one of them carries an `/AP`. **The row also named no `code` and no `test` while claiming a report**, which is §10.8.3's defect of the round before: the report is `appearance::construct`'s catch-all `Refusal::NotDerivable(\"its clause states no geometry\")` and nothing held it."

[[clause]]
clause = "12.5.6.12"
title = "Rubber stamp annotations"
status = "reported"
note = "A rubber stamp's appearance is an icon named by Table 184's /Name — Approved, Draft, Experimental and eleven more — and the clause states no artwork for any of them, exactly as §12.5.6.4 does not for its seven. **The modal verb is where the two part, which the hundred-and-twentieth session found by reading them side by side**: this table says 'PDF readers should provide predefined icon appearances', where §12.5.6.4 says shall. A should is a recommendation, and an invention with only a recommendation behind it puts a mark on the page that neither the document nor the standard asked for — so this stays refused and named while §12.5.6.4 is drawn. Table 184's /IT is unread; its StampImage and StampSnapshot values describe where an appearance came from rather than what it looks like. No corpus document reaches this without an /AP: all 19 stamps in the corpus carry one, counted, which is what a producer that cares about its stamp looking like anything has to do."

[[clause]]
clause = "12.5.6.13"
title = "Ink annotations"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_ink_annotation_is_stroked_along_its_ink_list", "crates/pdf-model/tests/annotations.rs::an_ink_annotations_path_is_drawn_and_outranks_its_ink_list"]
note = "Table 185's /InkList is drawn as one stroked subpath per entry, in /C at §12.5.4's width, and PDF 2.0's /Path is built as the m, l and c operands it defines. The clause leaves this crate a choice and it is taken openly: 'When drawn, the points shall be connected by straight lines or curves in an implementation-dependent way' — straight lines, because a curve fitted through a digitiser's samples would be smoothing the document did not ask for. **A second choice was being presented as the standard's until the four-hundred-and-forty-second session**: this row said /Path 'supersedes it as the table requires', and Table 185 requires nothing of the kind — it marks /InkList '(Required)' flatly, where §12.5.6.9's Table 181 does say of /Vertices '(Required unless a Path key is present, in which case it shall be ignored)'. Drawing the /Path alone is this crate's decision, because a file writing both describes one scribble twice; `ink`'s doc comment and `an_ink_annotations_path_is_drawn_and_outranks_its_ink_list` record it as a decision. The test /Path had lacked is that one, which is what kept this row `partial`."

[[clause]]
clause = "12.5.6.14"
title = "Popup annotations"
status = "implemented"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/popup.rs", "crates/pdf-model/src/markup.rs"]
test = ["crates/pdf-model/tests/annotations.rs::the_hidden_and_no_view_flags_draw_nothing_and_report_nothing", "crates/pdf-model/src/popup.rs::a_window_on_a_group_shows_the_primarys_words", "crates/pdf-model/src/popup.rs::a_subordinates_own_popup_entry_is_ignored"]
note = "A popup is 'a window ... containing text' belonging to a parent markup annotation and displayed in response to it, so it is never part of the page's own rendering: it is skipped before any other question is asked, and its absence is not a gap. Table 166's /AP entry agrees from the other side, naming Popup as one of three subtypes a writer need not supply an appearance dictionary for. **The interactive half is drawn since the three-hundred-and-twelfth session** — this row said \"/Open and /Parent are the interactive half\" and named neither a reader nor a caller for either. `crate::popup` reads the window: Table 186's /Open, Table 186's /Parent and the `shall` that comes with it ('the parent annotation's Contents , M , C , and T entries ... shall override those of the popup annotation itself'), with the clause's NOTE 3 for a popup that has none. `viewer_core::Query::Popups` places it in device pixels and `viewer_ui::chrome::popup_windows` draws it, because a clause that says a popup 'shall have no appearance stream' has said the window is a host's rather than the page's. §12.5.3's flags are read from the popup and **not** from its parent: Table 186 enumerates four entries the parent overrides and /F is not among them, which is a reading `pr7352.pdf` exists to contradict. Measured: 45 of the 974 corpus documents state a popup, 128 between them, 7 open, 79 with text to show. **And what the window shows is Table 172's `/RC` where a file states one and no `/Contents`**, since the three-hundred-and-forty-second session (ADR 0199): the clause's `shall` is that `/RC` \"shall be displayed in the popup window when the annotation is opened\", and a program with a popup window is what it binds. The characters and none of the formatting — the format is XFA's rich text, which principle 5 excludes, and NOTE 1 makes the two entries \"textually equivalent\" from the other end. **And a second entry opens the same window**, since the four-hundred-and-fifty-ninth session: Table 175 gives a *text* annotation an /Open and §12.5.6.4 makes an open text annotation one that \"shall display a popup window containing the text of the note\", so `opens_with_the_page` is the disjunction of the two rather than a precedence between them — each states a condition under which the window is open, both default to false, and neither states one under which it is closed. That is not in tension with the /F reading above, which turns on Table 186 enumerating four *overrides*: an override settles a conflict, and two entries that can only open a window have none. §12.5.6.4's row has the departure it corrected. **And the parent may not be the annotation whose four entries those are**, since the four-hundred-and-eightieth session: §12.5.6.2 makes Contents, M, C and T — the whole of Table 186's override list — *group attributes*, so where the parent is a subordinate in a group \"the corresponding entries in the subordinate annotations shall be ignored\" and the primary's are what this window shows. Two clauses composing rather than disagreeing: Table 186 says a popup takes its parent's four, §12.5.6.2 says which dictionary a group member's four are in, and `crate::markup` is the second. 213 of ISO 32000-2's own 2552 windows hang off a subordinate and 213 of them were empty. Table 172's /Popup is on the same list, so `popup_of` — §12.5.1's activation — now exhibits the group's one window rather than the subordinate's own, which is what \"function as a single unit when a user interacts with them\" means for the one interaction this program has. §12.5.6.2's row has the measurement and the half still owed. ADRs 0191, 0199, 0294, 0315."

[[clause]]
clause = "12.5.6.15"
title = "File attachment annotations"
status = "implemented"
code = ["crates/pdf-model/src/icon.rs", "crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/attachment.rs", "crates/viewer-core/src/viewer.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_stamps_appearance_is_reported_and_the_other_two_icons_are_drawn", "crates/pdf-model/src/attachment.rs::a_file_attachments_description_is_the_annotations_contents_and_not_the_specifications_desc", "crates/viewer-core/tests/headless.rs::a_click_on_a_file_attachment_annotation_extracts_its_file"]
note = "A file attachment annotation displays 'an icon' from Table 187's /Name — Graph, PushPin, Paperclip, Tag — and **all four are drawn since the two-hundred-and-sixty-sixth session**. The table's verb is `should` and not §12.5.6.4's `shall`, so this is a choice rather than an obligation met, and `doc/todo/26` set its own condition a hundred and nineteen sessions ago: worth doing only if the artwork can be argued from the clause's own descriptions. **It can, and that is the whole difference from §12.5.6.12's stamp**: a push pin, a paperclip, a tag and a graph are *objects*, and the clause names them — which is more than the mandatory seven of §12.5.6.4 get, where `NewParagraph` and `Insert` had to be invented out of a typographer's convention. The artwork is `icon.rs`'s, in the same unit square and the same black, and Table 166's /C is *not* filled behind it: that entry is the background of a **text** annotation's icon and this clause says nothing of the kind. A /Name outside the four is still reported, because a default is what an absent entry means. **Table 187's required /FS is read since the four-hundred-and-sixtieth session, and this row said it was 'the embedded file, which is not a rendering question'** — true about rendering and the reason nobody asked the other question, which is what §12.5.6.15 says activating one of these does: 'activating the annotation extracts the embedded file and gives the user an opportunity to view it or store it in the file system'. §12.5.1's own sentence is the same one at family scale — an activated annotation 'exhibits its associated object' — and this program has exhibited §12.5.6.14's window since the three-hundred-and-twelfth session while the file stayed unreachable. `attachment::of_annotation` reads the specification and `viewer_core::exhibit` hands the bytes over as `Event::Extracted`, which every host already writes somewhere (ADR 0295). **The clause's one shall is the description**: 'Interactive PDF processors shall use this entry rather than the optional Desc entry ( PDF 1.6 ) in the file specification dictionary … identified by the annotation's FS entry', so Table 166's /Contents overrides Table 43's /Desc where the annotation states one, and where it does not there is no entry to use instead and §7.11.4.1's /Desc keeps its own meaning. The one display path that shows such text is §12.5.6.14's popup, which takes /Contents and never /Desc by construction. **Counted before believed** (`crates/pdf-model/examples/file_attachment_census.rs`): one file attachment annotation in the 974, six in ISO 32000-2's own PDF, and not one of the seven is named by a /EmbeddedFiles tree — while all seven state a /Contents beside no /Desc, so the corpus cannot rank the shall and the fixture is a pair differing only in /Contents (trap 8). **What is deliberately not built is a document-wide list of them**: it costs a walk of every page's /Annots, 78 to 123 ms cold on ISO 32000-2's 1023 pages, and every host asks `Query::Attachments` when a document opens, which is where CLAUDE.md forbids a full page-tree walk. The corpus's one file attachment annotation carries an /AP, so the drawn fixtures are synthetic and say so."

[[clause]]
clause = "12.5.6.16"
title = "Sound annotations"
status = "implemented"
code = ["crates/pdf-model/src/icon.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_stamps_appearance_is_reported_and_the_other_two_icons_are_drawn"]
note = "A sound annotation, deprecated in PDF 2.0, is one that 'shall behave like a text annotation in most ways, with a different icon', whose /Name Table 188 gives as Speaker or Mic. The artwork is unstated and the obligation is a should — 'PDF readers should provide predefined icon appearances' — and **both names are drawn since the two-hundred-and-sixty-sixth session**, on §12.5.6.15's argument and in one function with it: `Speaker` and `Mic` name objects, so the artwork is argued from the clause's own word rather than invented beside it. A choice under a recommendation, recorded as one. Playing the /Sound stream is a media question, and principle 5's exclusion of clause 13 covers the engine; the icon is not excluded, it is unstated. The corpus's one sound annotation carries an /AP."

[[clause]]
clause = "12.5.6.17"
title = "Movie annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "A movie annotation, deprecated in PDF 2.0, carries an /AP like any other annotation and it is drawn — which is the whole of what a still page can show of one. Table 189's /T, /Movie and /A are the title, the player and its activation, which principle 5's clause-13 exclusion covers as a media engine; an appearance-less movie annotation is refused and named, because a poster frame is a decoded movie rather than a stated shape. **This row said \"Table 188's /Movie, /A and /Aw\" until the three-hundred-and-eighty-seventh session, and both halves were wrong**: Table 188 is the *sound* annotation's table and 189 is this one's — the ISO 32000-1 number §12.5.6.23's row already records itself as having carried — and ISO 32000-2 states no /Aw anywhere, in this table or in any other. Found by that session's ninth sweep, which checks every `Table NNN` citation against the entries the standard puts in that table. **Read and kept in the five-hundred-and-forty-fifth**: `Movie` is still in `annotation.rs`'s `STANDARD_SUBTYPES`, and the appearance-less refusal is still the general path rather than a subtype's."

[[clause]]
clause = "12.5.6.18"
title = "Screen annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "A screen annotation is a rectangle a media clip plays in, and its /AP is drawn like any other. Table 190's /MK is an appearance characteristics dictionary as a widget's is, but it reaches this crate only through /AP today, /T is the annotation's title and /A and /AA are activation. **This row said \"Table 189's /MK\" and named a /P this table does not have until the three-hundred-and-eighty-seventh session**: 189 is the *movie* annotation's table, 190 is this one's, and the five entries are /Subtype, /T, /MK, /A and /AA. The clip itself is clause 13 and excluded by principle 5; an appearance-less screen annotation is refused and named. **Read and kept in the five-hundred-and-forty-fifth**: every reader of `\"MK\"` in `appearance.rs` is on a field path — a widget's frame, its regenerated text, its choice list — so a screen annotation's characteristics still reach this crate through `/AP` alone."

[[clause]]
clause = "12.5.6.19"
title = "Widget annotations"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::a_widget_draws_its_background_and_reports_its_field_value", "crates/pdf-model/tests/variable_text.rs::a_widgets_contents_turn_by_table_192s_r", "crates/pdf-model/tests/annotations.rs::a_push_buttons_normal_icon_is_drawn_where_it_has_no_appearance_stream", "crates/pdf-model/tests/annotations.rs::table_250s_never_scale_keeps_the_icons_own_size_and_a_places_it", "crates/pdf-model/tests/annotations.rs::table_192s_default_caption_position_draws_no_icon", "crates/pdf-model/tests/annotations.rs::table_192s_beside_codes_are_named_rather_than_invented", "crates/pdf-model/tests/annotations.rs::table_192s_rollover_icon_is_named_rather_than_drawn", "crates/pdf-model/tests/annotations.rs::a_check_box_does_not_draw_table_192s_push_button_icon"]
note = "Table 192's /BG background and /BC border are constructed, at §12.5.4's width and style, which is what a widget looks like before its value is set. A widget stating neither and holding no value draws *nothing*, quietly, and that decision is worth as much as the drawing: Table 192 is where a widget's colours come from, so a field without one states no appearance at all, and reporting those named 23 corpus documents for a gap that is not one. **The value is drawn, and this row said it was not for three hundred and sixty-four sessions.** The sentence here read \"[w]hat is owed is the value: a text field's /V, a check box's or radio button's state, and a push-button's /CA caption all need §12.7.4.3's variable text, so a widget holding one draws its frame and reports the rest\" — written in the twenty-first session, when constructing an appearance arrived, and false from the twenty-third, when `variable_text::lay_out` did. `appearance::field_text` lays out all three: a text field's /V through §12.7.4.1's inheritance, with Table 231 bit 14's password field echoed as bullets; a toggling button's on state as Table 192's /CA caption, where §12.7.5.2.3's /AP selects none; and a push-button's /CA as the same entry. What a widget *reports* is narrower and is `variable_text::Owed`'s eight cases — a /DA carrying no `Tf`, a font name /DR does not define, a font that cannot be loaded or addressed by character, an invented stand-in that falls short, characters the font states no code for, a value past the layout's bound, §12.7.5.4's list-box selection, and a `Tm` that scales or rotates. The gate that names this row was reading as confirmation and was not: `a_widget_draws_its_background_and_reports_its_field_value` states no /DA at all, so what it proves is the first of those eight. Found by the three-hundred-and-eighty-seventh session's first sweep, on the expired blocker \"need §12.7.4.3's variable text\". Table 192's /R **is read and applied** as of the hundred-and-fifth session: \"[t]he number of degrees by which the widget annotation shall be rotated counterclockwise relative to the page\", written into the constructed stream as a `cm` rather than onto the appearance, because a constructed appearance is in the page's own space and §12.5.5's placement is the identity for it. The contents are laid out in a box at the origin whose sides are /Rect's swapped for a quarter turn — so §12.7.4.3's wrapping and auto-sizing see the width the text actually has — and the matrix brings that box back onto /Rect. A value that is not a multiple of 90, which the table forbids, is refused by name rather than rounded. No corpus widget states one, so the fixture is synthetic and measures the shape of the ink. /Parent is followed for §12.7.4.1's inheritable entries. **The rest of Table 192 is the push-button's, and three of its seven entries are implemented in the four-hundred-and-second session.** They were read by nothing until then, which is what kept this row `partial` once the false claim above it was gone, and the count came first: `examples/push_button_census` finds 42 push-buttons in the 964 corpus documents that open, **33 of them with their own /AP /N** — and Table 191's /MK is what makes it the input to *constructing* a stream — an appearance characteristics dictionary \"that shall be used in constructing a dynamic appearance stream specifying the annotation's visual presentation on the page\" — so those 33 never ask. /I is stated by 1 widget, /IF by 12, /TP by 2, and /RI, /IX, /RC and /AC by **none at all**. What is drawn now: /I, \"[a] form XObject defining the widget annotation's normal icon\", placed by Table 250's icon fit dictionary **whole** — /SW's four scaling conditions, /S's anamorphic and proportional, /A's fraction of the leftover space, and /FB, which fits the icon to /Rect itself rather than to /Rect inside §12.5.4's border. **Table 250 is printed under §12.7.8.3.2**, where an FDF field's own /IF points at it, and §12.5.6.19's /IF is the second entry to name it; the dictionary is the same. The icon's own extent is §8.10.2's /BBox under its /Matrix, and the entry's \"shall be an indirect reference\" is what lets the reference go into the constructed appearance's /XObject unresolved. /TP decides which of the icon and the caption is drawn: codes 0 (\"[n]o icon; caption only\", the default), 1 (\"[n]o caption; icon only\") and 6 (overlaid) are carried out, and **codes 2 to 5 are reported rather than invented** — each names the side the caption goes on and neither this clause nor any other states how much of the rectangle it takes, which is the same refusal §12.5.6.12's stamp legends get. `partial` now for those four codes and for /RI, /IX, /RC and /AC, which are the rollover and down states: each is defined by what the *pointer* is doing, a constructed appearance is one stream where §12.5.5 gives a stored one three, and a file stating any of them is told so by name. **Table 191's /H is implemented since the hundred-and-thirty-eighth session, and it is a gap that only opened when this program grew a pointer** (session 132). (This sentence said \"Table 192's /H\" until the three-hundred-and-eighty-seventh: 192 is the appearance characteristics dictionary /MK points at, which is where this row's /BG, /BC, /R, /CA and the icon entries live, and /H is on the widget itself.) The entry is \"the visual effect that shall be used when the mouse button is pressed or held down inside its active area\", with five modes and a **default of I**: N draws nothing extra, I inverts the contents of the annotation rectangle, O inverts the border, P displays §12.5.5's down appearance, and T is P. All five are honoured: N draws nothing extra, P and T show §12.5.5's down appearance, and I and O draw a white shape under §11.3.5.2's Difference blend — which *is* the clause's own arithmetic, `f(x) = 1 - x`, since `B(cb, cs) = |cb - cs|` with the source at 1 leaves `1 - cb`. A stated mode other than P overrides any down appearance, as the clause says. **The default is I and it is applied only where the annotation states no /D**, which is a reading rather than the sentence: taking the default flatly would make 95 of the corpus's page-one annotations state down artwork that could never be displayed, and every one of the 4 that state both a /D and an /H states /H P — so the corpus cannot decide between the two readings and the argument has to. Reachable only by a press, which no gate performs, so the tests are pixels: a red widget pressed with /H /I is cyan exactly. ADR 0123. **And for a hundred and fifteen sessions no *program* could perform one**: `viewer-core` took the pressed annotation from `link_at`, which returns a /Subtype /Link and nothing else, so the one entry that is a widget's was implemented, tested with pixels and unreachable from the only host in the tree. It follows `annotation_at` since the two-hundred-and-fifty-third session. **The same session confined the default**: two tables define /H — this one and Table 176's, on a link — and no others do, so a subtype whose clause states no such entry has no mode to default and a press draws no mark on it. The overreach was invisible for exactly as long as the caller was narrower than the code it called. ADR 0177. **A widget's appearance can now be left to a host**, since the four-hundred-and-ninth session and on this clause's own sentence: widget annotations \"represent the appearance of fields and to manage user interactions\", so a program that draws the field in a `GtkEntry` has replaced exactly the picture this clause describes. `pdf_model::view::WidgetAppearances` is the switch, §6.3.2.2's \"unless otherwise instructed\" is the permission, and the census is that 102 of the corpus's 964 openable documents have a delegable widget on page one — 637 widgets, 2723 display-list commands. Nothing about what this row owes changed: the same appearance is constructed and drawn for every caller that has not instructed otherwise. ADR 0245. **Read and kept in the five-hundred-and-sixty-fifth**: `/RI`, `/IX`, `/RC` and `/AC` are still the four `appearance.rs` names in its `interactive` list and answers with `Refusal::NotDerivable`, and `/TP`'s codes 2 to 5 are still reported by name — so every entry this row calls owed is owed *and reported*, which is what `partial` here means."

[[clause]]
clause = "12.5.6.20"
title = "Printer's mark annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "A printer's mark annotation is a registration target, colour bar or cut mark added for production, and §14.11.3 is where it is discussed. Its /AP is drawn like any other annotation's. Table 398's /MN names the mark and §14.11.3 says nothing about what any name looks like, so an appearance-less one is refused and named. **This row said \"Table 190's /MN\" until the three-hundred-and-eighty-seventh session** — 190 is the screen annotation's table — while §14.11.3's row had cited Table 398 for the same entry all along, which is the seventh sweep's shape (two rows about one mechanism) inside one number rather than one sentence. **\"Whether such a mark belongs on a screen at all is a §14.11.3 question this row does not settle\" — and §14.11.3's row settled it the wrong way for a hundred sessions**, calling itself `inapplicable` because \"a screen is not a printer\" while this row said the mark is drawn. The clause answers it outright: \"[t]he Print and ReadOnly flags in the F entry shall be set and **all others clear**\", so `NoView` is clear on a conforming mark and it is displayed. Two rows about one mechanism, disagreeing — `doc/todo/01`'s fourth sweep across a pair — corrected in the three-hundred-and-fifty-ninth session. **Read and kept in the five-hundred-and-forty-fifth**: `PrinterMark` is still in `STANDARD_SUBTYPES` and no source names `/MN`, which is the pair this row and §14.11.3's describe."

[[clause]]
clause = "12.5.6.21"
title = "Trap network annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "A trap network annotation, deprecated in PDF 2.0, defines trapping for a raster output device that has plates; §14.11.6 owns the subject. Its /AP is drawn like any other, and there is nothing else here a screen can show: trapping compensates for misregistration between printing plates, which a display does not have. **That last sentence is about what the traps *mean*, not about whether they are drawn, and §14.11.6.2's row read it as the latter for a hundred sessions** — calling itself `inapplicable` while this row said the appearance is drawn. §14.11.6.2 settles it: the flags \"shall be present, with the Print and ReadOnly flags set and all others clear\", so `NoView` is clear and a conforming trap network annotation is one whose flags say display me. Corrected in the three-hundred-and-fifty-ninth session, alongside the identical pair at §14.11.3 and §12.5.6.20. **Read and kept in the five-hundred-and-twenty-fifth**: `TrapNet` is still one of `annotation.rs`'s `STANDARD_SUBTYPES` and no corpus document states one, so what defends this row is the clause's flags sentence and not a file."

[[clause]]
clause = "12.5.6.22"
title = "Watermark annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "A watermark annotation's appearance stream is drawn like any other annotation's. Table 193's /FixedPrint, whose value is Table 194's fixed print dictionary, is what makes it a watermark rather than a stamp — a matrix and a size that keep the mark constant when the page is scaled or printed to another sheet — and it is neither applied nor reported. **This row said \"Table 191's /FixedPrint\" until the three-hundred-and-eighty-seventh session**, and 191 is the widget annotation's table: the same ISO 32000-1 carry-over §12.5.6.23's row records for itself, three rows further down the same family. **The reason this row used to give for that has expired**: it said the shape was §12.5.3's `NoZoom` and `NoRotate`, \"a resolution-independent display list cannot express a size that depends on the view\", and both halves turned out to be smaller than the sentence in the two-hundred-and-seventeenth session (ADR 0168) — a magnification arrives through `ViewState` and the interpretation says whether a page would notice. What `/FixedPrint` still waits on is *printing*, which this program does not do, and that is a capability rather than an expression problem: `doc/todo/25-view-dependent-annotations.md`. **Read and kept in the five-hundred-and-forty-fifth**: nothing under `crates/` names `/FixedPrint` at all, so it is still neither applied nor reported, and printing is still the capability it waits on."

[[clause]]
clause = "12.5.6.23"
title = "Redaction annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs"]
note = "A redaction annotation \"identifies content that is intended to be removed from the document\", and in the state this crate sees it — before removal — the clause says only that \"the user can see, move and redefine these annotations\". It is drawn from its `/AP` where it has one, and where it has none the clause states no artwork, exactly as §12.5.6.4's icons do not. **Table 195's other entries are not this annotation's appearance at all**, which the hundred-and-fifth session read and the row previously had backwards: `/IC` is \"the interior colour with which to fill the redacted region **after the affected content has been removed**\", `/OverlayText` is \"drawn over the redacted region **after the affected content has been removed**\", `/Repeat`, `/DA` and `/Q` say how, and `/RO` \"takes precedence over the IC, OverlayText, DA, and Q entries\" for the same moment. All six describe the *second* phase, which the clause makes a rewrite of the document — \"remove all content identified by the redaction annotation, as well as the annotation itself … If a portion of an image is contained in a redaction region, that portion of the image data shall be destroyed\" — **and that is excluded by what this program's one form of writing is, not by 'CLAUDE.md excludes writing files', which is the exclusion's wording from before the hundred-and-thirty-seventh session amended it** (read in the five-hundred-and-tenth): this program writes since the hundred-and-thirty-fifth, and what it writes is §7.5.6's incremental update, which appends and leaves the producer's bytes in the file byte for byte — the exact opposite of a phase whose verb is *destroy*. Applying a redaction is the one edit an incremental update cannot express, so the conclusion stands on the amended exclusion's own terms. So this program does not apply redactions and correctly draws none of their second-phase entries. `/QuadPoints` names the region to be removed rather than anything to paint. The row previously cited \"Table 193\", which is the watermark annotation's table and an ISO 32000-1 number; the redaction table is 195."

[[clause]]
clause = "12.5.6.24"
title = "Projection annotations"
status = "implemented"
code = ["crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/annotations.rs::the_hidden_and_no_view_flags_draw_nothing_and_report_nothing"]
note = "A projection annotation 'is only valid within the context of an associated run-time environment, such as an activated 3D model', and saves a 3D measurement as markup. With clause 13 excluded by principle 5 there is no such environment here, so an appearance-less projection draws nothing and reports nothing — which Table 166 supports from the other side, naming Projection with Popup and Link as the three subtypes a writer need not give an appearance dictionary. One with an /AP is drawn like any other annotation. The test named here pins the property this row needs: an annotation that is not shown reaches neither the display list nor the report."

[[clause]]
clause = "12.5.6.25"
title = "3D and RichMedia annotations"
status = "out-of-scope"
exclusion = "clause-13-multimedia"
note = "3D and RichMedia annotations are defined by §13.6.2 and §13.7.2, which principle 5's closed list excludes as a media engine rather than a rendering question. Their appearance streams are drawn where they exist, like any other annotation's, because nothing in the placement path switches on subtype."

[[clause]]
clause = "12.6"
title = "Actions"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs"]
test = ["crates/pdf-model/tests/actions.rs"]
note = "Actions — what a PDF *does* when something is triggered. `CLAUDE.md` splits this family down the middle: \"[f]ield *appearance* is not excluded; field *behaviour* is\", and \"JavaScript and script-driven form behaviour\" is on principle 5's closed exclusion list by name. Since the sixty-second session `action.rs` reads Table 196's dictionary and its `/Next` tree, and **ten of Table 201's twenty types are performed**: §12.6.4's go-to, embedded go-to, go-to-document-part, thread, hide, named, set-OCG-state and transition, with §12.7.6's reset-form and import-data beside them. The other ten are recognised and each carries its own refusal by name. **This row said \"the three types that change what a page displays are performed\" until the three-hundred-and-ninety-fourth session**, and named three of the ten; §12.6.4's row carries the count and the list. What performs them is `view.rs`'s `ViewState`, which is the first thing in this tree that is neither the document nor the page: a layer a click switched off is not in the file, so `interpret_with` takes it as a third input and `interpret` is that function at the state a document opens in. **Read and kept in the five-hundred-and-fifty-third**: the ten are §12.6.4's eight plus §12.7.6's two, which two rows state independently and which agree."

[[clause]]
clause = "12.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/action.rs"]
test = ["crates/pdf-model/tests/actions.rs::a_set_ocg_state_action_changes_what_the_next_render_draws"]
note = "The framing: an action is performed in response to a trigger, and the entries that carry one are read where they are — a link annotation's `/A` (§12.5.6.5, `link.rs`), an outline item's (§12.3.3, `outline.rs`), and the catalog's `/OpenAction` (§7.7.2, `destination.rs`). Table 190's screen annotation and Table 249's FDF field are the two this tree does not reach, and both belong to families it excludes."

[[clause]]
clause = "12.6.2"
title = "Action dictionaries"
status = "implemented"
code = ["crates/pdf-model/src/action.rs"]
test = ["crates/pdf-model/src/action.rs::a_next_chain_is_flattened_in_execution_order", "crates/pdf-model/src/action.rs::a_cycle_of_next_entries_terminates"]
note = "Table 196 in full: `/Type` (optional and ignored), `/S` dispatching on Table 201's twenty names, and `/Next`, which is where the work is. NOTE 1 makes the chain a *tree* — \"[a]ctions within each Next array are executed in order, each followed in turn by any actions specified in its Next entry, and so on recursively\" — so `action::read` flattens it depth-first into the order it is performed in, which is also what lets a go-to buried under a sound reach `link.rs`. The clause's own robustness rule is the other half and it is a rule about *broken files*: \"self-referential actions ought not be executed more than once\", so each action object is visited once and the total is bounded. NOTE 2's advice not to modify an action tree while running it is a writer's."

[[clause]]
clause = "12.6.3"
title = "Trigger events"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/view.rs", "crates/viewer-core/src/interact.rs"]
test = ["crates/pdf-model/tests/actions.rs::an_annotations_trigger_events_perform_their_actions", "crates/pdf-model/tests/actions.rs::an_annotations_own_action_takes_precedence_over_its_mouse_up_trigger", "crates/viewer-core/tests/headless.rs::the_pointer_raises_table_197s_events", "crates/viewer-core/tests/headless.rs::a_page_turn_raises_the_events_the_clause_orders", "crates/pdf-model/tests/actions.rs::the_corpus_states_these_page_scoped_triggers"]
note = "Table 197's ten annotation events and Table 198's two page events are read since the seventy-seventh session — `action::for_annotation` and `action::for_page` — and what they return is §12.6.2's flattened list, which `ViewState::perform_all` performs. Table 197's one precedence rule is applied where it belongs, on the entry it is stated about: \"[f]or backward compatibility, the A entry in an annotation dictionary, if present, takes precedence over\" `/AA /U`, so an annotation stating both performs its `/A` on mouse-up and never its `/U`. `/AA` on a page is read from the page's own dictionary, because §7.7.3.4 makes four entries inheritable and this is not among them. **Something raises them since the hundred-and-seventy-fourth session.** This row said \"[n]othing raises an event: entering, pressing and focusing are a window's business and this crate has no events\" — true when written, and false from the hundred-and-thirty-second, when `Command::Pointer` arrived. Forty-one sessions later a sweep for reasons of the shape \"this program has no\" found it. `viewer_core` now raises four of Table 197's ten from the pointer: `/E` and `/X` as the cursor crosses an activation region, `/D` on a press, and `/U` on a release over anything that is **not** the link being activated — the exclusion is Table 197's own precedence rule rather than a shortcut, since `interact::activate` performs a link's `/A` and `for_annotation` would return the same list again. **`/U` reached only a release that also activated a link until the three-hundred-and-twelfth session**, because the release arm returned early wherever the press had not landed on one: the table conditions the event on the release being \"inside the annotation's active area\" and on nothing else, so a click on a stamp, a widget or a markup annotation raised nothing at all. Found beside ADR 0191's popups, in the arm that had to be opened to add them. The region is `view::annotation_at`, which is any subtype rather than a link or a widget, gated by `annotation::interacts`: §12.5.3's Hidden, NoView **and ReadOnly** each say \"or allow it to interact with the user\", and ReadOnly is about nothing else — read for the first time here, with the clause's own exception applied (\"[t]his flag shall be ignored for widget annotations\"). Deliberately *not* gated on whether the annotation renders ink: a link renders none and is the standing example of a region. Tables 199 and 200 are excluded rather than owed: every entry of both is an ECMAScript action, which is on principle 5's closed exclusion list, and saying so is the difference between an exclusion and an oversight. **And four more since the two-hundred-and-fourth**, together with both of Table 198's, on the same sweep's second finding: `/PO`, `/PC`, `/PV` and `/PI` were said to want \"a page-visibility model a one-page-at-a-time window does not have\", and a window that turns pages *is* one. `Viewer::page_events` raises them in the order the clause states — of `/PO`, \"[t]he action shall be executed after the O action in the page's additional -actions dictionary … and the OpenAction entry in the document Catalog … if such actions are present\", and of `/PC`, that it \"shall be executed before the C action\" — so a turn is the leaving page's annotations, the leaving page, the arriving page, then its annotations, and `action::for_page` finally has a caller. **`/PV` and `/PI` coincide with `/PO` and `/PC` here by derivation rather than by concession**: the clause says the pair exists because \"[a]t any one time, while more than one page may be visible, depending on the page layout\", and this layout has one page. NOTE 1 is honoured by not consulting Table 167 — \"the values of the flags specified by the annotation's F entry … have no bearing on whether a given trigger event occurs\" — so a hidden annotation's `/PO` is performed. Nothing cascades: an action that turns the page again does not raise a second round, because §12.6.2 bounds nothing and a document could otherwise walk itself. ADR 0164. **`/Fo` and `/Bl` are raised since the two-hundred-and-fifty-seventh session, and the reason they were not is one this file has recorded four times: a blocker that named a vocabulary.** They were said to \"want keyboard focus — there is no focus model in `Command` at all, and adding one is a vocabulary change rather than a clause\", and no message was needed. Both entries are \"(Optional; PDF 1.2; widget annotations only)\", the clause says what happens when an annotation \"receives the input focus\" and nothing about how it comes to — so **a press inside a widget's active area gives it the focus and a press anywhere else takes it away**, which is a choice and the one every pointing interface makes, recorded as one beside \"a press dragged off a link does not activate it\". A page turned raises `/Bl` too, wherever the pointer is: the widget that held the focus is not on the screen. What a *keyboard* would add is Table 31's `/Tabs` order — which annotation is next — and that is §12.5.1's row and a different clause. All ten of Table 197's events are raised now; `partial` is for §12.6.3's own remaining half, which is that every action a trigger performs is one of the **ten of Table 201's twenty types this program performs** — §12.6.4's eight, with §12.7.6's reset-form and import-data beside them — and the rest are named refusals. **This sentence said \"the eleven §12.6.4 types\" until the five-hundred-and-twenty-fifth session**, a count that agreed with neither neighbour: §12.6.4's row says eight of the seventeen types that clause documents and §12.6's says ten of Table 201's twenty, and the eleventh was §12.6.4.8's `/URI`, which is read entire and printed rather than opened. `doc/todo/01`'s tenth sweep is about exactly this and cannot see it: the count is about a *neighbouring* family rather than about this clause's own children, so what found it was reading the row. 15 of the 974 corpus documents write an `/AA` at all, and the page-scoped population is measured by name: 3 pages of `doc_actions.pdf` state `/O` and `/C`, 1 annotation of `issue18305.pdf` states `/PO` and `/PC`, and **no document states `/PV` or `/PI`** — which is what the clause predicts of an entry that exists for multi-page layouts."

[[clause]]
clause = "12.6.4"
title = "Action types"
status = "partial"
code = ["crates/pdf-model/src/action.rs"]
test = ["crates/pdf-model/tests/actions.rs"]
note = "Table 201's twenty action types, seventeen of which this clause documents — §12.7.6 owns the other three, which are the form actions. The rows below take them one at a time; the summary is that **eight of the seventeen are performed**: `/GoTo` (§12.6.4.2), `/GoToE` (§12.6.4.4), `/GoToDp` (§12.6.4.5), `/Thread` (§12.6.4.7), `/Hide` (§12.6.4.11), `/Named` (§12.6.4.12), `/SetOCGState` (§12.6.4.13) and `/Trans` (§12.6.4.15). The other nine are recognised and refused by name rather than ignored as a class — `/GoToR`, `/Launch`, `/Sound` and `/Movie` reported, `/Rendition`, `/GoTo3DView`, `/ECMAScript` and `/RichMediaExecute` excluded by principle 5, and `/URI` read entire and printed rather than opened. **This row said \"three are performed and they are the three that change what is displayed\" until the three-hundred-and-ninety-fourth session**, which was true when it was written and was left behind by five rounds that each implemented one member — `doc/todo/01`'s fifth failure shape, a family's parent row nobody maintains because the clauses do not cite each other. Its last sentence was the same shape one round old and had a `shall` behind it: \"`/Trans` is the fourth that could change a mark and does not: §12.4.4's transition is an animation between two renders, and this viewer draws the second one\", retired by ADR 0230 in the three-hundred-and-ninety-third while §12.6.4.15's own row recorded the change. **Read and kept in the five-hundred-and-fifty-third**: eight performed and nine refused is Table 201's seventeen, and each of the nine still reaches `Action::Refused` carrying its own name."

[[clause]]
clause = "12.6.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/action.rs"]
test = ["crates/pdf-model/src/action.rs::a_name_the_table_does_not_hold_is_not_an_action"]
note = "The list of types, and the rule that a processor \"shall ignore\" an action whose `/S` it does not recognise — the same compatibility principle §7.8.2's `BX`/`EX` states for operators and Annex I for the file as a whole. Implemented as a *distinction* rather than a blanket: a name Table 201 holds becomes an `Action::Refused` carrying that name, and a name it does not hold produces nothing at all, because calling an unknown name a refused action would claim to know what was refused."

[[clause]]
clause = "12.6.4.2"
title = "Go-To actions"
status = "implemented"
code = ["crates/pdf-model/src/link.rs", "crates/pdf-model/src/outline.rs", "crates/pdf-model/src/destination.rs"]
test = ["crates/pdf-model/src/link.rs::a_rectangle_is_the_region_and_it_has_edges", "crates/pdf-model/tests/links.rs::a_links_region_is_found_and_its_destination_resolves", "crates/pdf-model/src/destination.rs::a_go_to_actions_structure_destination_takes_precedence_over_its_page_destination"]
note = "Go-To: \"a destination that shall be displayed\" in this document, which is Table 202's `/D` read as §12.3.2's destination. (This row cited \"Table 206's `/D`\" until the three-hundred-and-ninety-fourth session; 206 is §12.6.4.5's GoToDp dictionary, four clauses along, and its entries are `/S` and `/Dp`. `doc/todo/01`'s ninth sweep, on its second run.) Every place the standard puts one is read — a link annotation's `/A`, an outline item's, and the catalog's `/OpenAction` — and each turns the page. The action *dictionary* is the whole of this subclause: what to do with the destination is §12.3.2's, and what a viewer does with the view within the page is stated on §12.3.2.1's row. **`/SD` is read since the four-hundred-and-eighty-fourth session, and this row said it already was**: \"`/SD`, the structure destination alternative, resolves through §12.3.2.3 like any other\" is true of `Destination::read`, which resolves a structure destination handed to it, and was false of this clause — no file in this tree named the entry at all, so a go-to action read `/D` and `/SD` was never fetched. `doc/todo/01`'s fifteenth sweep found it on its first run as a committed program (ADR 0319), and it is `doc/habits.md`'s sixth refusal shape a third time: §12.3.2.3 has been `implemented` since its algorithm landed, and the entry that turns it on was wired to nothing. `Destination::of_go_to` reads Table 202's two entries in the table's own order — \"[i]f present, the structure destination should take precedence over destination in the D entry\" — and falls back to the required `/D` where the `/SD` is no destination, which is what a `should` permits rather than a silence. **Counted before believed** (`crates/pdf-model/examples/structure_destination_census.rs`): over 1187 documents on this disk, 7838 go-to actions state **ten** `/SD` between two files, and not one of the ten names a page or a view its own `/D` does not — so the corpus cannot rank the sentence and the fixture is a pair differing only in that entry (trap 8)."

[[clause]]
clause = "12.6.4.3"
title = "Remote Go-To actions"
status = "reported"
code = ["crates/pdf-model/src/action.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::a_name_the_table_does_not_hold_is_not_an_action"]
note = "Remote Go-To: a destination in *another* file, whose `page` is an integer index rather than a page object. Opening a second document is a `viewer-ui` question and a security one — §7.11's file specification can name any path. **Refused by name and said out loud**: `action.rs` answers \"GoToR: a destination in another file, which this reader has no filesystem to open\" and `viewer-ui` prints it when a click reaches one, so this is `reported` rather than `silent` — the row said `silent` while the code has named it since the sixty-second session. Table 203's `/SD` is §12.6.4.2's entry one clause along and is unread for the same reason the action is refused: its first element is \"a byte string representing a structure element ID in the remote document\", so resolving it needs the remote document. **Read again in the five-hundred-and-eighty-first, and one word of the reason had expired** (ADR 0416). \"[T]his reader has no filesystem\" is true of `pdf-model`, which is handed bytes; `action.rs`'s §12.7.6.4 comment said it of *this program*, and every host has written files since ADR 0244 — a click on an embedded file writes it beside the document in all three, and `viewer-host` carries §12.7.6.4's file policy for two of them. So the refusal stands and is smaller than it was written: what `GoToR` needs is a second `Document` in `viewer-core`'s vocabulary and a **host's decision** about which files a document may name, which is `CLAUDE.md`'s restriction-policy shape with a precedent already in the tree (ADR 0212). That is what would close it."

[[clause]]
clause = "12.6.4.4"
title = "Embedded Go-To actions"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/destination.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::the_clauses_three_example_relationships_read_as_paths", "crates/pdf-model/src/action.rs::a_target_this_file_does_not_contain_is_named", "crates/pdf-model/tests/actions.rs::an_embedded_go_to_opens_the_document_inside_this_one"]
note = "**Performed.** This is the one action of Table 201's twenty that names another document and needs no filesystem to reach it: a `GoToR` names a file on a disk, a `GoToE` names a file *inside the one already open*, and §7.11.4's embedded file streams have been read since the eighty-sixth session (ADR 0076). Tables 204 and 205 whole — `/T` flattened into a path of steps, `/R /P` popping back to the document the walk descended from and `/R /C` opening a child by its `EmbeddedFiles` key or by the file attachment annotation Table 205's `/P` and `/A` name, in both spellings each. §12.6.4.4's NOTE asks for a cycle check and `MAX_TARGET_DEPTH` is it: the path is a finite list read once, and each step opens at most one document, with the parent's `Limits` inherited so a document nested three deep is bounded like the one a person opened. `/D` is deliberately left unresolved and read in the *target*, because §12.3.2.2 makes its first element a page **number** there — \"[t]he first page shall be numbered 0\" (Table 203) — and a named destination is looked up in the target's own tables; `Destination::page_index_in_target` is that difference. Table 204's `/NewWindow` is a *should* and this program has one window, so the target replaces the source and says so. Refused by name, each separately: an `/F` naming another root document, a `/R /P` from the document a person opened, and a child this file does not embed. Measured: `issue17056.pdf` states 30 of these actions, all of the child-by-name form, and all 30 open. **Read and kept in the five-hundred-and-twenty-fifth**: the three refusals are `Action::Refused` carrying `GoToE`'s own name, so a caller can say which of them it met, and `MAX_TARGET_DEPTH` still bounds the path."

[[clause]]
clause = "12.6.4.5"
title = "GoToDp action"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/document_part.rs"]
test = ["crates/pdf-model/src/document_part.rs::a_leaf_begins_at_the_page_its_start_names", "crates/pdf-model/src/document_part.rs::a_node_begins_at_the_first_leaf_of_its_first_child"]
note = "GoToDp: \"changes the view to the Start page of a specified DPart\", performed since the hundred-and-eighteenth session. Table 206 has one entry, `/Dp`, \"[t]he indirect reference to a DPart dictionary to go to\", and it is kept as a *reference* because resolving it needs the page tree that turns Table 409's `/Start` into a number. **This row was `reported` on the ground that §14.12 \"is `unreviewed` in clause 14 and unbuilt\", and by the time anybody read it again §14.12 was `inapplicable` and the action still had nowhere to go** — which is `doc/HANDOVER.md`'s habit that an `inapplicable` row decays exactly as a `silent` one does, met for the second time. A `GoToDp` naming a *node* rather than a leaf is answered by §14.12.3's own sentence about depth-first order rather than by a guess."

[[clause]]
clause = "12.6.4.6"
title = "Launch actions"
status = "reported"
code = ["crates/pdf-model/src/action.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::a_name_the_table_does_not_hold_is_not_an_action"]
note = "Launch: run an application or open a file. **The one action in this family whose absence is a security property rather than a gap**, and it should stay absent until somebody writes the argument — principle 3's sandbox exists so that a document cannot reach the machine, and this action is a document asking to. **Refused by name and said out loud**: `action.rs` answers \"Launch: running an application, which the sandbox withholds\" and `viewer-ui` prints it when a click reaches one, so this is `reported` rather than `silent` — the row said `silent` while the code has named it since the sixty-second session."

[[clause]]
clause = "12.6.4.7"
title = "Thread actions"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs"]
test = ["crates/pdf-model/src/action.rs::a_thread_action_names_its_thread_in_all_three_forms", "crates/pdf-model/src/action.rs::a_thread_in_another_file_is_refused_by_name"]
note = "Table 209 whole: `/D` in all three of its forms — a reference to a thread dictionary, an index into the catalog's `/Threads`, or a title from the thread information dictionary — with the clause's own tie-break for a repeated title, \"the one appearing first in the document catalog's Threads array shall be used\"; and `/B` in both of its forms, a reference or an index within the thread, defaulting to the thread's first bead where the optional entry is absent. Resolution is `ThreadJump::bead_in` against §12.4.3's articles, and the answer is a bead whose `/P` names a page, which is what a viewer turns to. A `/F` makes the thread another file's and is refused by name, for `GoToR`'s reason: Table 209 says \"[i]f this entry is absent, the thread is in the current file\", so the entry's presence is the whole test. No corpus document states a thread action, and that no longer follows from anything: the premise this sentence used to rest on — that no corpus document states a thread — is false, and four documents state one without any of them stating a `/Thread` action to reach it. Measured directly in the five-hundred-and-seventieth session; ADR 0406."

[[clause]]
clause = "12.6.4.8"
title = "URI actions"
status = "partial"
code = ["crates/pdf-model/src/uri.rs", "crates/pdf-model/src/action.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/uri.rs::rfc_3986_normal_examples", "crates/pdf-model/src/action.rs::a_partial_uri_is_resolved_against_the_documents_base", "crates/pdf-model/tests/actions.rs::a_uri_action_arrives_at_the_viewer_already_resolved"]
note = "Table 210's `/URI` and `/IsMap` and Table 211's `/Base`, which is the whole of what the clause states about a *document*. What the URI is gets decided here: the clause describes a URI \"in Internet RFC 3986\" and says a partial one is interpreted relative to `/Base`, so `uri.rs` is that RFC's section 5.2 reference transformation, with its own section 5.4 examples as the tests — the concatenation a reader reaches for first is wrong on four of them. `/IsMap` appends the cursor as EXAMPLE 1's arithmetic states it, measured from the *upper-left* corner of `/Rect`, which is the half that inverts. **What is not done is the resolving itself**: `viewer-ui` prints the URI rather than opening it, because handing a document-controlled string to a browser is a decision about this machine and not about the file. With no `/Base`, a partial URI is \"interpreted relative to the location of the document itself\", which this crate does not know — `Uri::relative` says so rather than a scheme being invented, which is what one corpus document (`pr19449.pdf`, `foo.bar.com`) would otherwise get. 217 URI actions in 8 of the 974 documents; one states a `/Base` and one states a partial reference, and they are different files. **Errata Collection 3 says this clause's `/Base` text is in the wrong place, and the erratum is a statement about scope rather than a change to the text** (Issue #256, `/State` `Review` `Completed`): \"[t]he remaining text in subclause 12.6.4.8 about Base and the URI dictionary (including Table 211 and NOTE) applies to all relative URIs in a PDF document and is not limited to only URI actions as is currently implied. A future edition of ISO 32000 will move this text into a new subclause\". Nothing moves in Errata Collection 3 itself, so the number stands; what the sentence obliges is a reading, and it is taken deliberately. `uri::resolve` is a base and a reference and knows nothing about actions, so the arithmetic is already general; its one caller is this clause's action, and the other relative-URI site this tree reads is §7.11.2.2's URL-based file specification, which `file_spec::is_valid_relative_url` validates and does **not** resolve — because nothing here fetches a URL, so there is no target for a resolved one to name. That is a documented choice with the erratum beside it rather than a gap: the day a host opens a URL is the day the second caller appears. Found in the five-hundred-and-sixty-fifth session by `spec-errata moved`."

[[clause]]
clause = "12.6.4.9"
title = "Sound actions"
status = "reported"
code = ["crates/pdf-model/src/action.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::a_name_the_table_does_not_hold_is_not_an_action"]
note = "Sound: play a sound object. Adjacent to clause 13's exclusion and not covered by it, because §12.6.4.9 is in clause 12; recorded as debt, like §12.3.6's navigators. **Refused by name and said out loud**: `action.rs` answers \"Sound: clause 13's multimedia, excluded by CLAUDE.md principle 5\" and `viewer-ui` prints it when a click reaches one, so this is `reported` rather than `silent` — the row said `silent` while the code has named it since the sixty-second session."

[[clause]]
clause = "12.6.4.10"
title = "Movie actions"
status = "reported"
code = ["crates/pdf-model/src/action.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::a_name_the_table_does_not_hold_is_not_an_action"]
note = "Movie: play a movie annotation. Deprecated in PDF 2.0 in favour of §13.2's screen annotations and rendition actions, which principle 5 *does* exclude — so this row is one deprecation away from being out of scope, and saying so is cheaper than arguing it. **Refused by name and said out loud**: `action.rs` answers \"Movie: clause 13's multimedia, excluded by CLAUDE.md principle 5\" and `viewer-ui` prints it when a click reaches one, so this is `reported` rather than `silent` — the row said `silent` while the code has named it since the sixty-second session."

[[clause]]
clause = "12.6.4.11"
title = "Hide actions"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/actions.rs::a_hide_action_stops_an_annotation_being_drawn", "crates/pdf-model/tests/actions.rs::a_show_action_clears_the_flag_the_file_set"]
note = "Hide, in both directions and in all three of Table 214's forms for `/T`: an indirect reference to an annotation, a fully qualified field name resolved through §12.7.4.2, or an array of either. `/H` defaults to true. **This one changes what is drawn** — the clause sets and clears the same Hidden flag §12.5.3 defines, so `/H false` shows an annotation whose own `/F` hides it, which is the half a reader gets wrong by treating the action as a filter laid over the file. The state lives in `ViewState` and not in the document, because this program does not write files. No corpus document states one: the sixty-second session walked every object of all 964 openable documents and found no `/Hide` at all."

[[clause]]
clause = "12.6.4.12"
title = "Named actions"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::a_named_action_is_one_of_table_215s_four_page_commands", "crates/pdf-model/tests/actions.rs::a_named_action_asks_the_viewer_to_turn_the_page"]
note = "All four of Table 215's names, which are the whole of the table: `NextPage`, `PrevPage`, `FirstPage` and `LastPage`, each an `Action::Named` that reaches the viewer as a `view::Request::Page` and moves it. The movement is `Named::page_from`, clamped at both ends because no document has a page before its first. A name outside the four produces **no action at all** rather than a refusal, because the clause states that case itself — a processor that \"does not recognise the name … shall take no action\" — and the corpus's one instance is a `/Print`, which is a viewer command rather than a page. 3 of the 974 documents write a named action; 8 of their 9 are the four standard names."

[[clause]]
clause = "12.6.4.13"
title = "Set-OCG-state actions"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/optional_content.rs"]
test = ["crates/pdf-model/tests/actions.rs::a_set_ocg_state_action_changes_what_the_next_render_draws", "crates/pdf-model/src/view.rs::turning_a_radio_button_group_on_turns_its_siblings_off"]
note = "Set-OCG-state, with Table 217's `/State` processed left to right — each name applying \"to the subsequent groups until the next name is encountered\", a group set as many times as it appears, which is what the clause's EXAMPLE 2 turns on — and `/PreserveRB`, whose default of **true** is what made Table 99's `/RBGroups` load-bearing: a group turned on takes every other member of its radio-button collection off with it. `/RBGroups` had been recorded as unread for the whole project; it is read here and nowhere else, because §8.11.4.5 gives it no part in the *initial* state. **One corpus document states this action**, measured over every object of all 964 openable files."

[[clause]]
clause = "12.6.4.14"
title = "Rendition actions"
status = "out-of-scope"
exclusion = "clause-13-multimedia"
note = "Rendition actions control a media player — a rendition, a screen annotation and a playback state. Principle 5's closed list excludes \"Clause 13, multimedia and 3D — a media engine, not a rendering question\", and this action's whole subject is clause 13's; the clause number is in 12 only because that is where actions live."

[[clause]]
clause = "12.6.4.15"
title = "Transition actions"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/navigation.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/action.rs::a_transition_action_carries_table_164s_dictionary"]
note = "**Read** as of the hundred-and-ninth session. Table 219 has exactly two entries and one of them is `/S`, so the action *is* Table 164's transition dictionary — the same one a page's own `/Trans` holds, read by the same function (§12.4.4, ADR 0071's neighbour). What §12.6.4.15 adds is *when*: a processor \"shall normally suspend drawing when such a sequence begins and resume drawing when it ends\", and a transition action in the middle of one says to show the page as the previous action left it. Suspending and resuming is a window's business, so `ViewState::perform` answers `Request::Transition` and the caller decides whether it has a presentation to play — and since the three-hundred-and-ninety-third a caller that is presenting **draws** it, through the same `viewer_core::transition` shapes a page's own `/Trans` uses (ADR 0230), while one that is not shows the page, which is the transition's own end state. The clause's own effect happens anyway in this program, because every request in a `/Next` chain has already been performed by the time the transition is read. Debt with §12.4.4 for the **four** of Table 164's twelve styles no frame is shaped for — `Blinds`, `Glitter`, `Dissolve` and `Fly` — each reported by name. **This said five until the five-hundred-and-fifty-third session**, which is §12.4's row's error in the same words: the twelfth style is `R`, Table 164 defines it as the cut, and `transition::note` reports nothing for it because a cut is what the file asked for. No corpus document states a `/Trans` at all, on a page or in an action — measured over every object of all 964 openable ones in the seventieth session and over the page tree of all 978 in the three-hundred-and-ninety-third."

[[clause]]
clause = "12.6.4.16"
title = "Go-To-3D-View actions"
status = "out-of-scope"
exclusion = "clause-13-multimedia"
note = "Go-To-3D-View sets the view of a 3D annotation, which is §13.6's. Excluded by the same entry as §12.6.4.14."

[[clause]]
clause = "12.6.4.17"
title = "ECMAScript actions"
status = "out-of-scope"
exclusion = "script-behaviour"
note = "ECMAScript actions run a script. Principle 5's closed list excludes \"JavaScript and script-driven form behaviour — a sandboxed script engine is a separate project with its own security argument\". This is the row that exclusion was written for, and the one place in clause 12 where being unimplemented is a decision rather than a debt."

[[clause]]
clause = "12.6.4.18"
title = "Rich-Media-Execute actions"
status = "out-of-scope"
exclusion = "clause-13-multimedia"
note = "Rich-Media-Execute sends a command to a rich media annotation's Flash or 3D content, which is §13.7's. Excluded by the multimedia entry."

[[clause]]
clause = "12.7"
title = "Forms"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/variable_text.rs", "crates/pdf-model/src/form.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::quadding_moves_the_line_within_its_box", "crates/viewer-core/tests/headless.rs::a_page_states_its_whole_form_as_controls_a_host_can_build"]
note = "The display half of the clause is built, and **since the three-hundred-and-ninety-eighth session a whole form also crosses as data**: `pdf_model::form::fields` describes every field with a widget on a page — §12.7.5's type, the flags of Tables 227, 229, 231 and 233 that decide what kind of control it is, Table 234's items, Table 232's `/MaxLen`, Table 230's export values and the on-state name §12.7.5.2.3 makes a check box's value — and `viewer_core::Query::Fields` places its widgets in device pixels. That is what lets a host be a form in its own widgets rather than take one as pixels; the audit that found it the last of six chrome populations not to cross is in ADR 0235, which is the decision. (This sentence cited `doc/todo/37` until the four-hundred-and-thirteenth session, and that file was deleted by the four-hundred-and-ninth when `Command::Delegate` closed the last item in it — `doc/todo/01`'s eighth sweep, which is the first run of it to find a *live* claim behind a dead pointer.) A field's appearance is still constructed where §12.7.4.3 states how — its value, its caption, its quadding, its font — and read from `/AP` otherwise. What a field *does* is still mostly absent: no calculation order, no validation, no submission, and ECMAScript is excluded outright. What is no longer absent is a *click*: `viewer-ui` toggles a check box and a radio button by sending the on-state name this crate answered with, which is §12.7.5.2.3's value selecting §12.5.5's appearance. **Read and kept in the five-hundred-and-sixty-fifth**: nothing in the tree names Table 225's `/CO`, so the calculation order this row calls absent is absent, and §12.7.6.2's submission is still the refusal its own row carries."

[[clause]]
clause = "12.7.1"
title = "General"
status = "inapplicable"
note = "Two definitions and a NOTE warning that an interactive form is not a form XObject. No requirement on a processor."

[[clause]]
clause = "12.7.2"
title = "Interactive Forms"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/form.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::need_appearances_rewrites_the_marked_content_region_and_keeps_the_rest", "crates/pdf-model/tests/variable_text.rs::quadding_moves_the_line_within_its_box"]
note = "**`partial` until the five-hundred-and-sixty-second session above a note naming nothing owed**, which is the fourteenth sweep's shape found in the blame band rather than by the sweep. The clause states the hierarchy — terminal and non-terminal fields, widgets as children, and the merged single-widget dictionary — and one requirement that reaches a page: 'If such an object defines an appearance stream, the appearance shall be consistent with the object's current value as a field.' That sentence is what Table 224's `/NeedAppearances` admits is false, and it is why `appearance::regenerates` sets a stored stream aside for the two field types whose text §12.7.4.3 has a processor lay out. The merged dictionary is handled by reading a widget and its field from the same dictionary, which is what `Field::read` does when the `/Parent` chain is empty. The hierarchy itself is walked upward only; `/Kids` is never followed downward, because nothing that draws needs to — and `pdf_model::form::fields` reaches the same set from the other end, by grouping a page's widget annotations under the fully qualified names §12.7.4.2's table already builds. **Table 226's Kids sentence is what makes a widget's index in it meaningful**, which Table 230's export values depend on: \"one entry for each widget annotation in the Kids array\". **What made this row `partial` was never stated**: every other sentence of the clause is definitional — a field \"shall be defined by a field dictionary\" binds the *document*, and the hierarchy, the widget children and the merge are all `can` and `may` — so the one `shall` addressed to a processor is the consistency sentence above, and it is executed. The merged dictionary has evidence rather than an assertion now: every `variable_text.rs` fixture states one object carrying `/Subtype /Widget` beside `/FT`, `/T`, `/V` and `/DA`, and `quadding_moves_the_line_within_its_box` measures what a reader that took it for only one of the two would draw wrong."

[[clause]]
clause = "12.7.3"
title = "Interactive form dictionary"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::need_appearances_rewrites_the_marked_content_region_and_keeps_the_rest"]
note = "Three of Table 224's entries reach the page and the rest do not. `/DR` is the resource dictionary of every appearance constructed here (§12.7.4.3) and of every `/DA` string's font, including a free text annotation's; `/DA` and `/Q` are read as the document-wide defaults the table calls them, after the field's own and its ancestors'; `/NeedAppearances` decides whether a stored appearance is rewritten. `/Fields` is read, though not for drawing: **drawing walks `/Annots`**, which is why this row called the entry unread, and §12.8.1's signature scan walks the field tree from `/Fields` because a signature field need not be on any page. **`/SigFlags` is read since the two-hundred-and-seventy-seventh session, and the reason this row gave for not reading it was wrong**: it called the entry \"signature behaviour\", and Table 225's bit 1 is not behaviour but a statement about the document's *contents* — \"[i]f set, the document contains at least one signature field\" — whose stated purpose is to spare a processor \"having to scan the entire document for the presence of signature fields\". So §12.8.1's field walk now asks it first, and Table 224's default of 0 makes an absent entry the form saying there are none. Counted before it was trusted: of the 974 corpus documents, 163 have an `/AcroForm`, nine state `/SigFlags`, six set bit 1, and exactly those six have a signature field; none of the 154 that omit the entry has one. Bit 2, `AppendOnly`, asks a processor to warn before a save that \"alters its previous contents\" — a *may*, and unreachable here, because `pdf_syntax::write` performs §7.5.6's incremental update and nothing else. Not read: `/CO`, which is §12.6.3's calculation order and needs the script engine principle 5 excludes; and `/XFA`, which principle 5 excludes by name. ADR 0181. **Re-read off the blame list in the five-hundred-and-seventeenth**: nothing in the tree quotes `\"CO\"`, so the calculation order is unread exactly as this row says, and `/XFA` stays excluded on §K.1's own permission."

[[clause]]
clause = "12.7.4"
title = "Field dictionaries"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::quadding_moves_the_line_within_its_box"]
note = "Family head. §12.7.4.1's inheritance and §12.7.4.3's variable text are implemented, and so is §12.7.4.2 — **this row said its field names were \"read nowhere, because nothing that draws needs a field's name\" and that was stale by more than a hundred sessions**, which is `doc/todo/01`'s fifth shape exactly: §12.7.4.2's own row has read `implemented` since §12.6.4.11's hide action made a name decide whether an annotation is drawn, `ViewState::set_field` addresses a field by that name, and `view::field_at` answers a host with it. A family's parent row is not maintained by the sessions that implement its members. Corrected in the three-hundred-and-seventy-first session, whose caret is the third consumer of the same table: `ViewState::caret_at` finds the widget under a point and asks §12.7.4.3 where the next character goes. What keeps the family `partial` is the children's own notes and not this one."

[[clause]]
clause = "12.7.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::quadding_moves_the_line_within_its_box"]
note = "Table 226's inheritance is implemented and is what makes a merged widget work: `/FT`, `/Ff` and `/V` are taken from the nearest ancestor that states them, in one walk of the `/Parent` chain rather than one per key. **The clause forbids the bound that walk carries** — 'An interactive PDF processor shall not limit the range of inheritance for field dictionaries' — and it is bounded at 32 anyway, because a `/Parent` chain in a hostile file can be a cycle and principle 3's resource budgets outrank a depth no legitimate form approaches; reaching it is reported rather than read as 'no value'. Table 227's three flags are behaviour, and **all three cross to a host since the three-hundred-and-ninety-eighth session** (`pdf_model::form::FormField`): ReadOnly and Required constrain interaction, NoExport constrains submission, and none changes a mark — so a host builds a read-only control and marks a required field, which is what a form's user interface does and what nothing in this tree could say before. ReadOnly is enforced as well as carried; the other two are carried only, because §12.7.6.2's submission is excluded. **A person may now put a value into a field** (`ViewState::set_field`, ADR 0120), which is the fourth statement about one beside Table 226's `/V`, §12.7.6.3's `/DV` and §12.7.8's imported value — applied to every widget of the field, because the clause makes the value the *field's* and §12.7.4.2's name is what identifies it. Nothing is written to the file: `CLAUDE.md` rule 1 makes the document immutable and an edit is a log beside it. **Table 227's ReadOnly is enforced against that edit and against nothing else**, which is the flag's own distinction: 'an interactive PDF processor shall not allow a user to change the value of the field', and §12.7.6.3's reset and §12.7.8's import are the document changing its own value rather than a user. **And what a person may put there is no longer one string**, since the four-hundred-and-twelfth session: `ViewState::set_field` takes `pdf_model::view::Entered`, which is characters, §12.7.5.4's chosen options by index, or a clear. The clause is why the argument is the *field's* either way — 'a field's value is shared by all of its widgets' follows from the value being the field's — and the resolution from an index to what Table 226's `/V` holds happens once, here, because it needs the field's own `/Opt` and doing it again at the save would be two chances to disagree. ADR 0248. `/T` is the partial name §12.7.4.2 builds the qualified one from — carried on its own since the three-hundred-and-ninety-eighth, because a label wants the field's own name and not its ancestry — and `/TU` is read beside it since the two-hundred-and-fourteenth session, because §14.9.3 makes it a `shall` for any processor that identifies a field in a user interface (ADR 0167); `/TM` is for §12.7.6.2's export, which principle 5 excludes. `/Kids` is the hierarchy walked the other way, `/AA` is a trigger, and `/DV` matters only to §12.7.6.3's reset. **Read and kept in the five-hundred-and-sixty-fifth**: the bound the clause forbids is still `MAX_FIELD_ANCESTRY = 32` in `appearance.rs`, still reported rather than read as \"no value\", and `/TM` is still named by nothing outside §12.7.6.2's excluded export."

[[clause]]
clause = "12.7.4.2"
title = "Field names"
status = "implemented"
code = ["crates/pdf-model/src/view.rs", "crates/pdf-model/src/form.rs"]
test = ["crates/pdf-model/src/view.rs::a_hide_action_finds_a_field_by_its_fully_qualified_name", "crates/pdf-model/src/view.rs::one_field_name_may_reach_several_widgets", "crates/pdf-model/src/form.rs::a_nested_field_carries_both_of_its_names"]
note = "A field's partial and fully qualified names, the PERIOD that separates them, and the rule that a dictionary with no `/T` \"shall not be considered a field but simply a Widget annotation\". **This row was `inapplicable` for eighteen sessions and the reasoning was sound and wrong**: names identify a field for export, scripting and the user interface, none of which decides a mark — until §12.6.4.11's hide action names one, and then a field name decides whether an annotation is drawn. `view.rs` builds the whole table from `/AcroForm /Fields`, appending each partial name to its parent's, and a kid with no `/T` joins its parent's name rather than starting one, so one name reaches every widget of its field. The requirement that two fields sharing a fully qualified name share `/FT`, `/V` and `/DV` is on the writer. **And it is what keeps a delegated page from losing ink**, since the four-hundred-and-ninth session: a host drawing §12.7's controls itself asks for the page without the widgets' appearances, and the set that leaves is `form::delegated_widgets` — built from `form::fields`, so a dictionary this clause leaves \"simply a Widget annotation\" is drawn as before, having been handed to nobody. ADR 0245."

[[clause]]
clause = "12.7.4.3"
title = "Variable text"
status = "partial"
code = ["crates/pdf-model/src/variable_text.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::quadding_moves_the_line_within_its_box", "crates/pdf-model/tests/variable_text.rs::a_zero_size_is_auto_sized_until_the_value_fits", "crates/pdf-model/tests/variable_text.rs::need_appearances_rewrites_the_marked_content_region_and_keeps_the_rest", "crates/pdf-model/tests/saving.rs::a_text_widget_with_no_appearance_stream_is_given_one", "crates/pdf-model/tests/variable_text.rs::a_caret_stands_where_the_next_character_will_be_drawn", "crates/pdf-model/tests/variable_text.rs::a_caret_in_an_empty_field_is_where_the_first_character_will_go", "crates/pdf-model/tests/variable_text.rs::a_point_names_the_byte_whose_caret_stands_there", "crates/pdf-model/tests/variable_text.rs::a_selection_covers_the_glyphs_between_two_offsets", "crates/pdf-model/tests/variable_text.rs::a_selection_across_a_wrap_is_one_shape_per_line", "crates/pdf-model/tests/saving.rs::a_free_text_annotation_carries_the_font_its_default_appearance_names", "crates/pdf-model/tests/saving.rs::a_documents_own_default_font_is_not_replaced", "crates/pdf-model/tests/variable_text.rs::a_fields_baseline_ignores_a_descriptor_that_cannot_be_a_measurement", "crates/pdf-model/tests/variable_text.rs::a_fields_baseline_follows_a_descriptor_the_band_believes", "crates/pdf-model/tests/variable_text.rs::a_fields_baseline_reads_a_positive_descent_as_the_depth_it_states", "crates/pdf-model/tests/variable_text.rs::a_fields_baseline_reads_a_zero_descent_as_a_face_with_no_descenders", "crates/pdf-model/tests/variable_text.rs::a_composite_da_fonts_codes_are_as_long_as_its_codespace_says", "crates/pdf-model/tests/variable_text.rs::a_composite_da_font_that_names_no_characters_is_reported", "crates/pdf-model/tests/variable_text.rs::a_vertical_composite_da_font_is_refused_and_says_which", "crates/pdf-model/tests/variable_text.rs::the_arabic_free_text_declines_whole_and_names_both_halves"]
note = "The clause's operation, both of its forms. Where there is no appearance stream one is written: Table 228's `/DA` is parsed for its `Tf`, its colour and its text state, the font is resolved in `/DR`, the value is measured and positioned by `/Q`, and the marks go inside the `/Tx BMC` … `EMC` pair the clause's EXAMPLE shows. Where there is one, the clause's closing paragraph is a *splice* rather than a rebuild — the region from `/Tx BMC` to the matching `EMC` is replaced and everything outside it survives, with `/DR`'s resources copied in behind the stream's own. A size of 0 is auto-sized and at most one `Tm` is honoured with its translation replaced. **Three things the clause hands back are choices and are recorded as such**: where a baseline sits, how far apart two lines are (13/12 of the size, which is §12.7.5.3's own EXAMPLE), and what auto-sizing picks. The first of those three is `DEFAULT_ASCENT` and `DEFAULT_DESCENT`, and it is what stands in where the `/DA` font's descriptor states no usable pair — **which is §9.8.1's band since the four-hundred-and-third session** rather than a guard of this module's own (ADR 0240). `Metrics::read` asked `ascent > 0 && descent < 0`, an ordering that believed `/Ascent 4000 /Descent -1140` — a five-em line, which puts a field's baseline below its own rectangle — and refused `/Ascent 905 /Descent 211`, which is a real face with Table 120's sign convention broken rather than its measurement withheld. `pdf_font::measured_extent` is now the one rule for both readers. It moves no corpus page: `examples/variable_text_census` classified all 695 objects this clause lays text out for and not one states a pair the two rules read differently, which the corpus, oracle, quorra and text gates then confirmed line for line. **A `/DA` naming a font `/DR` does not define is the writer breaking this clause's own `shall` — 'the specified font value shall match a resource name in the Font entry of the default resource dictionary' — and until the hundred-and-twenty-third session it drew nothing at all**, which on a free text annotation is a blank page: the text *is* the appearance. Since then the value is laid out in a stand-in and the report names the font `/DR` lacked, which is ADR 0106's rule and trap 5's sixth report-and-draw case. The stand-in carries the resource name through as a base font name — a hint, not a derivation, since a resource name is arbitrary — and **declines where it cannot draw the whole value**: `freetext_no_appearance.pdf`'s value is a paragraph of Arabic, and a Latin stand-in drew its spaces and full stops and nothing else. **And five of the six name one of the fourteen abbreviations that denote §9.6.2.2's fourteen font programs**, which the two-hundred-and-fifty-eighth session stopped treating as a stand-in at all: `/Helv`, `/HeBo`, `/TiRo`, `/ZaDb` and the rest are a *bijection* with the standard 14, so a `/DA` naming one names a font this binary carries and the value is drawn in the face the name means. That is a documented choice about a malformed file rather than a reading — the clause states no recovery — and what it buys is ADR 0133's own argument: those pages reproduce on a machine with no fonts installed, where a family match would have drawn them in whatever sans face the machine offered. It is deliberately narrow: `/Rufscript` and `/F1`, the corpus's other two, still stand in and still report. **The inference may still not fall short**, which is the same asymmetry one step along: `/DR` defines nothing, so reading `/Helv` is this program's reading, and `freetext_no_appearance.pdf`'s Arabic declines rather than draw part of a line. 6 corpus documents write such a `/DA`; **5 draw their fields and 1 keeps the blank**, and the sixth changed in the two-hundred-and-eighty-fourth session — this sentence read \"4 now draw and 2 keep the blank because Helvetica has no glyph for their characters\", and for `bug1865341.pdf`'s *Załącznik* that was wrong twice: the missing set was one character, `ą`, and every Helvetica has an `aogonek`. What it lacked was a **code**, which §9.6.5.1's `/Differences` states and this module now writes (ADR 0184). The Arabic is not closed by that route — nor by `doc/todo/21`'s per-character fallback, which this note used to file it under: ADR 0348 read the witness out (session 513), and what it needs is an Arabic glyph source no compiled-in face supplies (measured: Liberation Sans's `cmap` maps the whole Arabic range to glyph 0 and its `GSUB` has no `arab` script), Unicode's joining-form selection and right-to-left ordering, together or not at all — a chain of faces asked per character would draw isolated forms left-to-right, a plausible wrong page worse than the blank. The refusal is machine-independent (36 distinct missing characters against the invented `/Differences`'s 31 free codes, and `read-fonts`' Adobe Glyph List has no name for any Arabic character), and `the_arabic_free_text_declines_whole_and_names_both_halves` pins the blank and the report. **A no-break space is written as a space**, since the same session: Annex D's note 6 gives U+00A0 no code of its own in `WinAnsiEncoding` — code 240 octal is the space, and a producer meaning the other has to say so with a `/Differences` — so a value holding one has a character this clause must write and the encoding cannot spell. (**This module writes a `/Differences` itself since ADR 0184** and could spell it that way; it does not, because the two characters differ only in where a line may break and this module breaks on the value's own white space before any code exists — so naming `nonbreakingspace` would produce the same picture from a longer stream.) The two differ only in where a line may break, and this module breaks on the value's own white space before any code exists. `bug1871353.pdf` is nine of them between two letters, and the alternative was the whole field blank. **And since the three-hundred-and-seventy-first session this layout answers one question the standard does not ask**: where a caret goes. Nothing in ISO 32000-2 states a text cursor — §12.5.6.11's caret *annotation* is a different object, a mark left in a document to say text was edited there — so the caret is a *choice*, and what makes it this clause's is that the place the next character will be drawn is this clause's own arithmetic: the x a line is positioned at by `/Q`, plus the width of the value's prefix measured the way the glyphs are, between the ascent and the descent that put the baseline where it is. `variable_text::lay_out` answers it from the same walk that writes the stream, `appearance::caret` maps it into default user space through §12.5.5's placement, and `viewer_core::Query::Caret` is what a host asks. An *empty* field is laid out where it would otherwise be skipped, because somewhere for the first character to go is the one thing an empty field can still be asked. ADR 0211. Not implemented: `/DS` and `/RV`, which are XFA rich text and excluded by principle 5. **A composite `/DA` font was the other half of that sentence and is laid out since the five-hundred-and-second session** (ADR 0337). The refusal's reason was true and was not a reason: a code cannot be produced from a character without inverting §9.7.6.2's codespace ranges, so `CMap::each_addressable_code` inverts them — offering only the codes that same `CMap` would extract from their own bytes — and `variable_text::show` writes a code as the one to four bytes §9.7.6.2 gives it rather than as one byte. What is refused instead is narrower and each half has a clause behind it: a `CMap` stating more codes than the bounded walk will visit, and §9.7.5.1's writing mode 1, which decides which metrics place a glyph and therefore cannot be laid out along this clause's horizontal axis. **0 corpus objects state such a `/DA`**, which `examples/variable_text_census` counts rather than assumes, so the rule is defended by pairs of hand-built files differing in one entry (trap 8): one file's codespace is one byte wide and its twin's is two, with the same value, the same CIDs, the same widths and — the assertion — the same picture. **And Table 119 gives a Type 0 dictionary no `/FontDescriptor`**, so `Metrics::read` follows `/DescendantFonts` to Table 115's; before that a composite font's baseline fell back to the em split with the document's own pair sitting one indirection away. **And a value this program replaced regenerates the appearance whatever Table 224 says**, since the hundred-and-thirty-fifth session: §12.7.2 obliges the *file* to keep its stored stream consistent with its own `/V`, and §12.7.6.3's reset, §12.7.8's import and a person typing all break that consistency at viewing time — the clause's strong form is one line on, for a `RichText` field, \"the entire annotation appearance shall be regenerated each time the value is changed\". Before that, a widget with a stored `/AP` in a document without the flag went on drawing the value it had before the import. No fixture caught it because the fixtures state no `/AP`, which is the path that constructs rather than splices. **And since the hundred-and-forty-fifth session the stream is *written into the file*** by §7.5.6's update, rather than left to the next reader behind Table 224's flag: the clause's \"[t]he new appearance stream becomes the normal appearance ( N ) in the appearance dictionary associated with the field's widget annotation\" is the same sentence whether the stream is built for one render or kept, and the bytes are the ones this program draws. A widget that had no `/AP` gets an object *added*, which §7.5.6's own \"changed, replaced, or deleted\" permits and §7.3.8.1 requires because a stream cannot be direct. `/NeedAppearances` is now written only for the widgets whose stream this program could not produce or could produce only part of. **And what a refused value reports names both halves since the two-hundred-and-eighty-third session**: `FontNotInResources` alone said `/DR` had not defined the `/DA`'s name, which stopped being the operative reason in the two-hundred-and-fifty-eighth when a `/Helv` began being drawn from the binary. `bug1865341.pdf`'s value is *Załącznik* and its missing set is **one character** — `ą`, because §9.6.5.2's encodings have no ogonek, not because any Helvetica lacks the glyph. A simple font reaches a glyph only through a code — **so the invented font now states its own encoding**, one session later: §9.6.5.1's `/Differences` is \"an array of character codes and character names\", the name comes from the Adobe Glyph List `read-fonts` already carries, and the rebuilt font is kept only where it reaches strictly more of the value than the base encoding did. `bug1865341.pdf` draws its *Załącznik* and `freetext_no_appearance.pdf` — Arabic, which no Helvetica has the glyphs for — is refused exactly as before. Only for a font this module invented: a `/DR` font's encoding is what the document says its field is set in. **And two more questions the standard does not ask, since the three-hundred-and-eighty-eighth**: which byte of the value a *point* falls nearest, and the shapes covering a range of it. Both are the caret's own arithmetic run differently — the first walks the boundaries between glyphs on the line the point is nearest and answers the closest, the second measures the prefix at each end of the range on every line the range touches — and both are computed in the walk that writes the stream, for the reason the caret is: a second implementation of this clause's auto-sizing, quadding and wrapping would sit a cursor beside the text rather than in it. `variable_text::Asked` is what a question asks for and `LaidOut`'s three answers are what it gets; `Query::Offset` and `Query::FieldSelection` are what a host asks. A point outside every glyph answers the nearest boundary rather than refusing, which is a choice recorded as one. **And since the four-hundred-and-first session all three are asked of §12.5.6.6's annotation as well as of a field**, which costs this clause nothing and is the clause's own doing: §12.5.6.6 says \"[s]ubclause 12.7.4.3, 'Variable text', describes the process of using these entries to generate the appearance of the text in these annotations\", so `appearance::frame` and `laid_out_in` dispatch on the subtype and every question below them is unchanged. What differs is the *box* — Table 177's `/RD` inner rectangle against a widget's border inset under Table 192's `/R` — and nothing else. **And this clause's `shall` about `/DR` now binds this program as a writer**: \"[t]he specified font value shall match a resource name in the Font entry of the default resource dictionary\", so a save that adds a free text annotation states the `/DA`'s font in Table 224's `/DR`, creating the interactive form dictionary with the Required empty `/Fields` where the document has none. The document's own definition always wins, which is the same rule `Resolution::Named` follows when drawing. **And what this clause's value *is* now says whether it is the value**, since the four-hundred-and-eleventh session: `appearance::field_text_value` answers `view::ShownValue`, the characters beside Table 231 bit 14's `obscured`, from the one reading that produces either — so no caller can be handed the bullets and told they are the field's own text. What that closed is §12.7.5.3's row and not this one; what belongs here is that the flag is a property of *this layout's* answer rather than a second reading of the field's flags. **And one of the four things this clause is asked for does not take a value at all**, which the five-hundred-and-seventy-first session found by reading the clause's own NOTE: it gives 'scrollable list boxes whose contents are determined interactively at the time the document is displayed' as its example of what has to be built here, and what a list box shows is Table 234's `/Opt` rather than Table 226's `/V`. `variable_text::Shape::ListBox` is that difference — one option per line, never rewrapped, running down from the top of the box — and it is the only shape whose lines are *items*, which is why a line too wide for the box is clipped rather than continued. Auto-sizing takes no exception for it, and that is a finding rather than an omission: a first attempt gave it one, on the reading that a list box's rectangle is a scrolling window onto its options, and the picture rejected it — fitting a single line's width to the whole box made six short labels choose 34-point type and show two and a half of them. What the window reading is right about is answered where the options are chosen instead, because `/TI` decides which of them are laid out, so the run this clause's function measures already is the visible one. §12.7.5.4's row carries the rest. ADRs 0032, 0112, 0120, 0130, 0184, 0211, 0225, 0238, 0247, 0405."

[[clause]]
clause = "12.7.5"
title = "Field types"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/form.rs", "crates/viewer-accessibility/src/role.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_check_box_shows_its_caption_only_when_it_is_on", "crates/pdf-model/src/form.rs::a_field_with_no_type_is_still_on_the_page"]
note = "Family head. All four field types are recognised and each is taken to whatever §12.7.4.3 can lay out for it: a button's caption, a text field's value, a combo box's selection and, since the five-hundred-and-seventy-first, §12.7.5.4's list of options (ADR 0407). **This sentence named a list box and a signature field as \"the two that state no appearance this tree can build\", and the first half was retired by reading the clause rather than by building anything new**: §12.7.5.4 states no appearance for a list box's *selection* and states its `/Opt` array outright. A signature field is the one that stands, and for a reason of its own — §12.7.5.5 makes its value a signature dictionary and puts its appearance in `/AP`, so a field without one states nothing to draw. **And all four now cross as controls a host builds itself** — `pdf_model::form::Control`, with buttons split the three ways §12.7.5.2 splits them and a fifth arm for the field whose `/FT` Table 226 requires and no ancestor states, which is described rather than dropped because the widget is still on the page. ADR 0235. **And the control has a third consumer since the five-hundred-and-third session**, which is not a host at all: §14.8.4.7.2's `Form` structure element is one widget annotation, so `viewer_accessibility::role` turns the same `Control` into the platform's own check box, radio button, entry or list — a screen reader is told which control it is standing on rather than that there is a group. ADR 0338."

[[clause]]
clause = "12.7.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_check_box_shows_its_caption_only_when_it_is_on"]
note = "The four field types the family defines, which `FieldKind::of` reads from Table 226's `/FT` and subdivides by each type's own flags. A fifth name is not a field type and is refused rather than guessed at."

[[clause]]
clause = "12.7.5.2"
title = "Button fields"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/form.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_check_box_shows_its_caption_only_when_it_is_on", "crates/pdf-model/src/form.rs::a_radio_set_states_one_on_state_per_widget"]
note = "Family head. Bits 15, 16, 17 and 26 divide the type and **all four are read since the three-hundred-and-ninety-eighth session**. This row said three were and that RadiosInUnison was not \"because it describes what happens when a button is clicked\" — a reason of `doc/todo/01`'s capability shape, and it expired when a click became something this tree does: `viewer-ui` toggles a button, so bits 15 and 26 are what a host needs to know what its click means, and both cross through `pdf_model::form::Control::RadioButton`. **This row then said neither is *acted* on here and that the division was deliberate, and half of that was wrong** (ADR 0346). §12.7.5.2.1's own sentence — \"[a]n interactive PDF processor shall follow the intended behaviour\" — addresses all four bits to a *processor*, and bit 26's behaviour is not a click's: it is what `/V` means when two of a set's widgets answer to the same appearance-state name. `Field::an_earlier_button_answers_to` obeys it since the five-hundred-and-eleventh session, and what needed the code was the flag being **clear** rather than set. Bit 15 stays a host's, correctly: \"selecting the currently selected button has no effect\" is a rule about a press, and a press is chrome. What a button *shows* is its `/AP` state or Table 192's caption, and both are drawn."

[[clause]]
clause = "12.7.5.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::need_appearances_does_not_reach_a_button", "crates/pdf-model/tests/radios_in_unison.rs::with_the_flag_clear_only_the_first_of_them_does"]
note = "The three kinds of button field, separated by Table 229's Pushbutton and Radio flags. **This row said the clause's requirement that 'an interactive PDF processor shall follow the intended behaviour' \"is about behaviour\" and that what it decides here is only whether a value has an on state, and that reading was too narrow** (ADR 0346). The sentence names *four* bits — 15, 16, 17 and 26 — and one of them, RadiosInUnison, decides what `/V` means rather than what a click does: with it clear, §12.7.5.2.3 requires that \"at most one radio button in a field shall be set at a time\", which binds this reader whenever it is the thing that turned one on. `FieldKind::Button`'s `toggling` still carries bit 17, and `Field::an_earlier_button_answers_to` carries bit 26."

[[clause]]
clause = "12.7.5.2.2"
title = "Push-buttons"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::need_appearances_does_not_reach_a_button"]
note = "One sentence with a rendering consequence: a push-button 'retains no permanent value'. So a push-button shows Table 192's caption unconditionally rather than a value, and §12.7.4.3's regeneration does not reach it — there is no text 'not known until viewing time' to regenerate. Table 192's `/RC` and `/AC` are the rollover and down captions, which need a pointer, and `/RI` and `/IX` are the matching icons. **`/I`, `/IF` and `/TP` were on that list until the four-hundred-and-second session and are not**: the normal icon is drawn, Table 250 fits it, and three of `/TP`'s seven codes are carried out. §12.5.6.19's row has the count and the argument. **This row also quoted 'shall not use the V and DV entries' until the four-hundred-and-eighteenth session**, and Errata Collection 3 strikes that sentence with no replacement (Issue #386, `/State` `Review` `Completed`). Nothing here moves: the definition it drew its reason from is the surviving half, and a control retaining no permanent value has no value to draw. It is worth recording *how* it was found, because it was not found by the sweep — the note stated it in single quotes and `spec-errata` collects double-quoted spans only, so this one came out of `form.rs`'s copy of the same sentence."

[[clause]]
clause = "12.7.5.2.3"
title = "Check boxes"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/annotation.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_check_box_shows_its_caption_only_when_it_is_on", "crates/pdf-model/tests/variable_text.rs::a_check_box_with_nothing_to_draw_says_so", "crates/pdf-model/tests/variable_text.rs::checking_a_box_draws_the_state_the_new_value_names", "crates/pdf-model/tests/saving.rs::a_check_boxs_states_are_not_replaced_by_one_stream", "crates/viewer-core/tests/headless.rs::a_host_can_check_a_box_with_the_name_the_page_gave_it"]
note = "The clause locates a check box's appearance and settles which entry selects it, and both are implemented. **It is also what stops §7.5.6's writer from destroying one**: because the states are the appearance and the value selects among them, a save regenerates a check box's `/AP` for nobody — writing the new `/V` is the whole of the change (ADR 0130). Each state 'shall be defined by an appearance stream in the appearance dictionary', so `/AP` is where a box's tick comes from and §12.7.4.3's regeneration is not applied to one. **Of `/V` and `/AS` the clause states two sentences and this tree read only the second for three hundred and ninety-seven sessions.** The second is 'If they are not equal, then the value of the AS key shall be used instead of the V key', and `annotation::stored_appearance` obeyed it by reading `/AS` and never `/V`. The first is 'The value of the V key shall also be the value of the AS key' — an invariant, and a processor that changes one entry is the one that has to carry the other. So a person who checked a box changed `/V` and nothing changed `/AS`, and the widget went on drawing the state it was saved in: **a host could not tick a box at all**, on either path, and no gate saw it because no gate sets a value. `appearance::appearance_state` is the fix, narrowed to the one field type whose appearance its value selects, and a widget whose `/AP` states no stream under the new name is one of §12.7.5.2.4's *other* buttons and is off. ADR 0235. A box with no `/AP` shows Table 192's caption if it has one and is reported if it does not, because a document saying a box is on while stating no mark for it is worth naming. **Table 230's `/Opt` is read since the three-hundred-and-ninety-eighth session** and this row called it not implemented, on the true observation that it changes no mark: it crosses as `pdf_model::form::Widget::export`, beside the `/AP` state name, because the clause makes them two different strings — 'the names used to represent the on state in the AP dictionary of each annotation may use numerical position (starting with 0) of the annotation in the Kids array', so `/AP` may say `/0` where `/Opt` says the export value, and only the first selects an appearance while only the second is worth showing a person. A mark is not the only thing a clause can decide. **And the clause has a third sentence about `/Opt` that nobody had read, found in the five-hundred-and-eleventh session** (ADR 0346): after the two purposes it lists for the entry, it states 'For radio buttons, the same behaviour shall occur only if the RadiosInUnison flag is set. If it is not set, at most one radio button in a field shall be set at a time.' That is Table 229 bit 26's requirement, and it is stated *here* rather than in §12.7.5.2.4 — which is why a round reading only the radio-button subclause would not have found it. `Field::an_earlier_button_answers_to` obeys it, over a hand-written pair of fixtures, because all 1293 documents this tree can reach set the flag zero times and state zero radio fields whose widgets share an on state (`field_flag_census`)."

[[clause]]
clause = "12.7.5.2.4"
title = "Radio buttons"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/form.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_check_box_shows_its_caption_only_when_it_is_on", "crates/pdf-model/src/form.rs::a_radio_set_states_one_on_state_per_widget", "crates/pdf-model/tests/radios_in_unison.rs::with_the_flag_set_both_buttons_that_share_an_on_state_go_on", "crates/pdf-model/tests/radios_in_unison.rs::the_page_draws_the_same_buttons_the_description_names"]
note = "A radio button and a check box differ nowhere in what is drawn: both have two states, both take the on state's appearance from `/AP` by `/AS`, both may carry Table 192's caption, and this clause repeats §12.7.5.2.3's `/AS`-over-`/V` sentence verbatim. `Off` as the default value is honoured by treating any state but `Off` as on. **What is specific to a radio button is behaviour, and this row said none of it reaches a page — which stopped being the whole story in the three-hundred-and-ninety-eighth session**, twice over. The sentence that decides which widget of a set is drawn on is *here*: 'The parent field's V entry holds a name object corresponding to the appearance state of whichever child field is currently in the on state', so once a reader may change `/V` (§12.7.5.2.3's correction), a widget whose `/AP` has no stream under that name is off and the rest of the set draws its `Off` state rather than nothing. And Table 229's NoToggleToOff crosses to a host — 'If clear, clicking the selected button deselects it, leaving no button selected' — which is what `viewer-ui` obeys when a click lands on a button that is already on. **RadiosInUnison crossed beside it and this row said it is *not* obeyed, \"because turning on every button of a set that shares an on state is a decision for whatever handles the press\" — and that sentence was wrong in both directions** (ADR 0346). It was wrong that the flag *set* is unobeyed: a widget is on when its `/AP /N` holds a stream under the name `/V` carries, so two widgets sharing a name have always gone on together, which is this clause's NOTE — 'An exception occurs when multiple radio buttons in a field have the same on state and the RadiosInUnison flag is set. In that case, turning on one of the buttons turns on all of them.' — obeyed by code that had never read it. And it was wrong that nothing else is owed: §12.7.5.2.3 states the flag *clear* as a requirement on a reader, 'at most one radio button in a field shall be set at a time', and this tree turned them all on. `Field::an_earlier_button_answers_to` is the fix. **Which one stays on is a documented choice** and the clause states none: `/V` is a name, so a producer that gave two buttons the same name wrote a document whose own value cannot distinguish them — Table 230 is the standard's instrument for one that wants them distinguishable — and this reader takes the first in `/Kids`, which is the order `/Opt` is indexed by. It binds a value *this reader* replaced and not the file's own `/AS`, because §12.7.5.2.3 gives that entry precedence and a file stating two widgets on has said which of its own buttons are on. `Off` is what a click sends to turn one off, which is this clause's default read forwards."

[[clause]]
clause = "12.7.5.3"
title = "Text fields"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/variable_text.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/form.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_multiline_value_wraps_into_lines_that_run_down_the_box", "crates/pdf-model/tests/variable_text.rs::a_comb_field_spreads_one_character_per_cell", "crates/pdf-model/tests/variable_text.rs::a_password_field_does_not_echo_its_value", "crates/pdf-model/tests/variable_text.rs::a_password_fields_typed_value_is_not_written_into_the_file", "crates/pdf-model/tests/variable_text.rs::a_field_that_may_not_scroll_takes_only_what_fits", "crates/pdf-model/tests/variable_text.rs::a_multiline_field_that_may_not_scroll_stops_at_the_last_line_that_fits", "crates/pdf-model/tests/variable_text.rs::a_caret_in_a_comb_field_stands_at_the_next_cell", "crates/viewer-core/tests/headless.rs::a_caret_says_where_the_next_character_goes", "crates/viewer-core/tests/headless.rs::a_point_inside_a_value_names_the_byte_it_is_nearest", "crates/pdf-model/tests/variable_text.rs::a_point_in_a_comb_field_names_the_cell_it_landed_on", "crates/pdf-model/src/form.rs::a_text_fields_flags_cross_as_the_control_they_describe", "crates/pdf-model/src/form.rs::a_comb_field_states_its_cell_count_and_a_multiline_one_does_not"]
note = "The clause's own sentence is the implementation: the value 'shall be used to construct an appearance stream for displaying the field, as described under 12.7.4.3' and 'shall be presented in a single style'. Three of Table 231's flags change the layout and all three are read — Multiline wraps, Comb divides the box into Table 232's `/MaxLen` cells and puts one character in each, Password echoes bullets instead of the value. This clause's own EXAMPLE is where the line spacing this tree uses comes from: two lines at `/Ti 12 Tf` set `0 -13 Td` apart. **`DoNotScroll` is obeyed since the three-hundred-and-thirty-eighth session** (ADR 0197). Table 231 bit 24 says that where it is set, \"[o]nce the field is full, no further text shall be accepted for interactive form filling\", and `ViewState::set_field` — the one place in this tree a person's text is accepted — now takes the longest prefix of the value that fits the widget's rectangle, which is what \"no further text\" means for a host that sends whole values rather than keystrokes. The sentence's parenthesis decides the axis, one per shape: horizontally for a single-line field, vertically for a multiline one, and a count of Table 232's cells for a comb, which is the third case Table 231's own rule for bit 25 settles. `variable_text::lay_out` answers the question through `LaidOut::overflows`, measured with the formulae that place the text rather than with a second set. **One field, one value, so the shortest prefix any of its widgets accepts wins**: §12.7.4.1 shares a value across widgets and the flag belongs to the field, so a value that overflowed one rectangle while fitting another would make the field full and not full at once. It was found by `doc/todo/01`'s second sweep in the three-hundred-and-fourth session, behind the reason that the flag \"constrains typing\" — true when written and false since the hundred-and-thirty-fifth session, which is when this program learned to fill a field; **a flag that constrains typing binds a program that types**. 260 corpus widgets over 8 documents set it, which `examples/field_flag_census` counts and which is the largest witness set of any type-specific flag in Tables 229, 231 and 233. **A person can now reach one in a window**: `viewer-ui` sends `Edit::SetField` from the keyboard since the three-hundred-and-forty-ninth session (ADR 0201), and it keeps the *point* it clicked rather than the text it typed — so the truncation this flag causes is something the host reads back rather than something it has to predict. **And it is a caret rather than an append since the three-hundred-and-seventy-first**: `Query::Caret` says where the next character goes, the arrow keys, Home and End move it, and Backspace and Delete take out the character on either side of it — so correcting the middle of a value no longer means deleting back to it. This flag is what makes the offset something to *clamp* rather than to count: a widget takes only the prefix that fits, so the host reads the value back after every keystroke and puts the caret at the nearest character boundary of what was accepted. Table 231 bit 25's comb is the one shape whose caret is not a gap between glyphs but a cell — the clause divides the box into `/MaxLen` positions, so the place the next character goes is the next position. ADR 0211. **Every flag Table 231 defines crosses to a host since the three-hundred-and-ninety-eighth session** (`pdf_model::form::TextControl`), which is not the same as every one being obeyed and the difference is worth the words. Multiline, Comb, Password and DoNotScroll are *applied* here, as above. DoNotSpellCheck, FileSelect and RichText are *carried*: each constrains a control rather than a mark — a spell-checker, a file-chooser, an editor for a rich value — and each belongs to whatever builds the control, which is exactly the host this row previously said did not exist. A `RichText` field's `/V` is still laid out as plain text, which the clause's own conventions paragraph says is not what `/RV` describes, and carrying the bit is what lets a host decline rather than show a person XFA markup. Table 232's `/MaxLen` crosses beside them, and separately from the comb count, because the entry bounds a field that is not a comb as well. **And a click places the caret and a drag selects inside the value since the three-hundred-and-eighty-eighth** (ADR 0225). Table 231 bit 25's comb decides the inverse the same way it decides the caret — a point names the *cell* it landed in, because the clause divides the box into `/MaxLen` positions — and Table 231 bit 13 is why a selection is a question of its own rather than two carets: a multiline value is broken into lines by this layout, so the lines between the two ends of a selection are this crate's to name and no host's to guess. What copy, cut and paste mean inside a field needed no message at all: the two offsets are into the value a host has already read back, so the characters are a slice it holds and the edit is the same `Edit::SetField` a keystroke sends. **Table 231 bit 14 has two sentences and only the first was read until the four-hundred-and-eleventh session.** The first — 'Characters typed from the keyboard shall instead be echoed in some unreadable form' — this tree has obeyed since the beginning: `appearance::field_text_value` answers with bullets. What that produced everywhere else is the finding. The bullets are what a *host* is told the field says, and ADR 0201 has a host write the value back into its control after every keystroke because of bit 24's truncation — so `viewer-ui` sent the bullets back as the next value and a password field's contents became a row of dots plus whatever was typed last. The fix is a shape rather than a sentence: `view::ShownValue` carries `obscured` beside the characters, produced by the same reading that makes them, so the two cannot disagree; `Answer::Field` and `Answer::Fields` both carry it; `viewer-ui` declines the field by name because it draws its own page and has no secure control, and the two native hosts refuse the write-back on the flag rather than on a control's kind. The second sentence is the NOTE: 'To protect password confidentiality, it is imperative that PDF processors never store the value of the text field in the PDF file if this flag is set.' **This tree stored it** — `ViewState::save` wrote Table 226's `/V` for every edited field — so a person filling a form and saving it put their password in the file in clear text. A NOTE is informative and this one is obeyed anyway, because the alternative is that. `ViewState::save` now writes neither the value nor the appearance for such a field, which keeps the producer's `/V` and `/AP` agreeing with each other, and `Written::withheld` names each one so `viewer-core` can report it: a save that quietly dropped what a person typed would be the silence trap 5 exists against. `a_password_fields_typed_value_is_not_written_into_the_file` searches the saved bytes for the characters rather than reading the value back, because reading it back answers bullets whether or not the secret reached the file. ADR 0247. **Read and kept in the five-hundred-and-sixty-fifth**: the three flags this row calls carried rather than applied — `DoNotSpellCheck`, `FileSelect` and `RichText` — are still `TextControl`'s fields and still change no mark here, which is the distinction the row draws and is the reason it is `partial` rather than wrong."

[[clause]]
clause = "12.7.5.4"
title = "Choice fields"
status = "partial"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/form.rs", "crates/viewer-host/src/form.rs"]
test = ["crates/pdf-model/tests/variable_text.rs::a_combo_box_draws_its_value_and_a_list_box_draws_its_options", "crates/pdf-model/tests/variable_text.rs::the_top_index_says_which_option_the_list_starts_at", "crates/pdf-model/tests/variable_text.rs::a_list_box_reports_only_what_the_clause_leaves_unsaid", "crates/pdf-model/tests/variable_text.rs::choosing_an_item_replaces_the_stored_list_with_the_clauses_own_options", "crates/pdf-model/src/form.rs::a_choice_fields_items_and_selection_cross", "crates/viewer-host/tests/host_mappings.rs::table_234s_top_index_says_where_a_hosts_list_starts"]
note = "Both forms of the clause draw, one flag bit apart. A **combo box** shows its value, which the clause makes a text string — or the first of an array where several are selected — and that goes through §12.7.4.3 like a text field's. A **list box** shows its options, which is the other thing the clause states outright: 'The Opt array specifies the list of options in the choice field, each of which shall be represented by a text string that shall be displayed on the screen', in the array's own order by Table 233 bit 20's `shall` on the reader, from Table 234's `/TI`. **This half was refused whole until the five-hundred-and-seventy-first session, and the sentence that refused it is below with the correction beside it**: the clause states no appearance for the *selection* — no highlight colour, no rule, nothing — and a mark added over an item that is drawn either way may not take the item down with it, which is ADR 0106's test. So the options are drawn and the unmarked selection is reported (ADR 0030's shape); ADR 0407 has the argument and what the picture rejected on the way. **Table 234's `/Opt`, `/TI` and `/I` are read since the three-hundred-and-ninety-eighth session**, and this row said they were \"read nowhere for the same reason — they order and index a list nothing draws\". The reason was sound and is now the argument for reading them: a list this tree cannot draw is exactly a list a *host* should draw, so the items, their export values, the top index and which of them are selected cross as `pdf_model::form::ChoiceControl`. Which are selected is the clause's own rule rather than `/I`'s — 'If the items identified by this entry differ from those in the V entry of the field dictionary ... the V entry shall be used' — so the value decides and `/I` is consulted only for the tie the clause states it for, two options with the same label. Table 233 bit 20's `Sort` is deliberately **not** carried, on the table's own instruction: 'PDF readers shall display the options in the order in which they occur in the Opt array', so handing a host the bit would be inviting it to break a `shall`. ADR 0235. `partial` rather than `reported` since the hundred-and-fifteenth session: the drawing half is drawn. **What that costs the corpus is nil, and it is a count rather than an impression since the four-hundred-and-third session**: `examples/variable_text_census` finds **10 list-box widgets over 8 documents, every one of them with an `/AP` `/N` stream and none in a document setting Table 224's `/NeedAppearances`** — so the refusal never took a mark off an untouched page, and **that is exactly where the debt was**: a list box only reaches the construction under a regeneration, which is a document setting `/NeedAppearances` or a person who has chosen an item, and until the five-hundred-and-seventy-first the answer there was the file's own stale stream with the shortfall named beside it. This sentence read \"[n]o corpus document reaches either path with no appearance stream\" and the census corrected its other half: **one combo-box widget of 26 does** — `annotation-choice-widget.pdf`'s read-only one — and it states no value, so `Ok(None)` draws nothing and owes nothing. The clause's own `shall` about the items is discharged away from the page: Table 234's `/Opt` \"shall be presented to the user\", and `pdf_model::form::ChoiceControl` presents them to a host in the array's own order (ADR 0235), which is where a list a renderer may not invent belongs. **And the selection can be *set* since the four-hundred-and-twelfth session, which is the half this row had never owed out loud.** Table 233 bit 22 — '(PDF 1.4) If set, more than one of the field's option items may be selected simultaneously; if clear, at most one item shall be selected' — is the one place three hosts found the boundary short: `Edit::SetField` carried one string, so `viewer-gtk`, `viewer-qt` and the headless harness each offered single selection deliberately. It carries a set of `/Opt` indices now (`pdf_model::view::Entered::Chosen`), and both halves of the clause's own sentence about `/V` are written: a string for one item, an array of strings for several, and no `/V` at all for none. Table 234's `/I` is written beside it and removed where the new value names no options, because the entry says which `/Opt` element was chosen where two carry the same label, and an `/I` left disagreeing with a `/V` this program wrote would be a file relying on the clause's own tie-break to hide a contradiction. Errata Collection 3 rewrote that row: 'For choice fields that allow multiple selection (MultiSelect flag set)' becomes 'For choice fields', and 'or when the value of the choice field is an array' is struck — Issue #468, `/State` `Review` `Accepted`. This note quoted the struck trigger until the four-hundred-and-seventeenth session, and reading it corrected the argument rather than the code: writing `/I` beside a single selection was a stretch of the retired wording and is the plain sense of the amended one (ADR 0253). The bit is **obeyed** rather than carried: a selection sent to a field whose `MultiSelect` is clear is cut to its first index, the shape ADR 0197 gave Table 231 bit 24. The corpus sets bit 22 on **4 widgets over 4 documents** (`examples/field_flag_census`), of which `issue17492.pdf`'s `databases` is the one the round was driven on. ADR 0248. Still `partial`, and the reason changed with the round that drew the options: the *page* now shows the list and does not show which item `/V` names, because no clause states the mark — §6.3.2.2 asks a rendering processor for the page's contents and a field's contents include its value, so this is a shortfall rather than a silence, and it is reported on the clause's own condition (a `/V` that names an option; §12.7.5.4's default null selects nothing and owes nothing). **Read and kept in the five-hundred-and-sixty-fifth**: the page still drew nothing for a list box and no clause states what a selection looks like, and Table 233 bit 20's `Sort` is still carried by nothing — the two claims this row rested on, of which the **first was retired six rounds later** and the second stands. Bit 20's other half is now obeyed twice rather than once: the order the drawing uses is the array's, as well as the order a host is handed. **Table 234's `/TI` moved from read to used** in the same round — it chooses which options are laid out, so a producer that scrolled a list gets both the options it scrolled to and, where the `/DA` states a size of zero, an auto-size fitted to them; the entry a reader is likeliest to get wrong is one naming an index the array does not have, which is clamped to the last option rather than obeyed, on ADR 0111's rule that an optional entry may not erase what the clause states. **And the entry stopped at the page: it reached the host-side mapping in the six-hundred-and-first session and not before** (ADR 0436). `viewer_host::form::ControlKind::List` carried the labels, the selection and Table 233 bit 22 and dropped the top index, so every native host built a list box starting at row 0 over an appearance that starts at `/TI` — `doc/habits.md`'s fifth sweep exactly, *the model implements this, who calls it?*, and the shape this row could not show because \"crosses\" was true of `pdf-model` and of nothing a host reads. `viewer-qt` scrolls to it now; `viewer-gtk` does not, because `GtkListView::scroll_to` is GTK 4.12 and that crate binds `v4_10`, and `doc/todo/30` carries the debt with its reason. The distinction the clause draws is what the test fixes: `/TI` is the first option *visible* and `/V` is the one selected, and a control that read either for the other agrees with itself on every list where they are the same row."

[[clause]]
clause = "12.7.5.5"
title = "Signature fields"
status = "implemented"
code = ["crates/pdf-model/src/appearance.rs", "crates/pdf-model/src/signature.rs", "crates/pdf-model/src/restriction.rs"]
test = ["crates/pdf-model/tests/annotations.rs::the_hidden_and_no_view_flags_draw_nothing_and_report_nothing", "crates/pdf-model/tests/forms_data.rs::a_signed_signature_field_locks_the_fields_its_lock_names"]
note = "A signature field is drawn from its `/AP` and from nothing else, which is what the clause describes: its value is a signature dictionary rather than text, and signing 'entails updating at least the V entry and usually also the AP entry'. So §12.7.4.3's regeneration does not reach one and a signature field with no appearance stream states nothing to draw. The clause's two visibility rules are already satisfied elsewhere: a zero-size annotation rectangle draws nothing (`annotation::decide`), and Table 167's Hidden and NoView bits are honoured there too. **Table 235's `/Lock` was called \"signing behaviour\" here and is not**: the sentence under Table 236 is a `shall` addressed to whoever changes a value — \"[t]he signature field lock dictionary … contains the names of form fields whose values shall no longer be changed after this signature has been signed\" — and this program fills fields in and saves, so it is a requirement it could break, and did, in silence. Read now: Table 236's three actions (`All`, `Include`, `Exclude`) over §12.7.4.2's fully qualified names, joining §7.6.4.2's Table 22 and §12.8.2.2's `/DocMDP` as a `Restriction` that `viewer_core` asks about once per operation and a reader may turn off (ADRs 0212, 0284). **The condition is the clause's own and is where a reader is likeliest to go wrong**: \"after this signature has been *signed*\", so the lock is read off a `/V` that is a signature dictionary rather than off the `/Lock`, and an unsigned signature field carrying one locks nothing — NOTE 1 says such a field \"can also hold information needed later when the actual signing takes place\". No corpus document states a `/Lock` — measured over the pdf.js corpus and the four under `doc/corpora/` by `signatures.rs::the_corpus_states_the_fields_one_signature_covers` — so the witness is a hand-built file (trap 8). **What a real producer writes instead is §12.8.2.4's copy of the same list**, inside the signature rather than in the field dictionary, and that clause's row carries it (ADR 0403). `FieldLock` is `FieldSelection` since that round, one type for Table 236 and Table 259, because the standard makes the second a copy of the first. **Two `shall not`s are met by construction and are worth stating rather than assuming, because the capability that could break them now exists**: the appearance dictionary's information \"shall not be used by a signature verification handler at the time of signature verification\" and \"[a] PDF processor shall not incorporate the validation status of a signature (e.g. a checkmark for passed or an X for failed) into the appearance of the signature field\" — `Signature::integrity` and `Signature::authenticity` read `/ByteRange` and §12.8.3.3's `SignedData` and never an appearance, and nothing draws over a widget's own artwork. Table 236's `/P` is deliberately not read here: \"absence of this key shall result in no effect on signature validation rules\" makes it a statement about what invalidates the signature, which is §12.8.2.2's question and that row's debt. Table 235's `/SV` — whose value is Table 237's seed value dictionary — constrains a processor that *signs*, which this is not. ISO/TS 32001 section 5.1.2 adds SHA3-256, SHA3-384, SHA3-512 and SHAKE256 to that dictionary's `/DigestMethod` — the one of that document's three table changes Errata Collection 3 leaves standing beside section 5.1.4's, its section 5.1.3 having been struck out entirely by issue #236 — and it lands on the same side of that line: the entry is \"An array of names indicating acceptable digest algorithms to use while signing\", so it constrains the signer and not the reader. `cms::Digest` computes all four since the five-hundred-and-fifty-fifth session (ADR 0390) for §12.8.3's sake, which is where a *reader* meets them. Everything else in the clause — the field type, the rectangle, one annotation per field — addresses whoever wrote the file."

[[clause]]
clause = "12.7.6"
title = "Form actions"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/src/view.rs::a_reset_form_action_names_fields_to_reset_or_fields_to_spare", "crates/pdf-model/tests/variable_text.rs::a_reset_form_action_draws_the_default_value_instead_of_the_value"]
note = "Submit, reset and import. **Two of the three are performed.** §12.7.6.3's reset is the one whose whole effect is a display change — it sets a field's value to its `/DV` and §12.7.4.3 draws whatever that value is — and §12.7.6.4's import is performed in the two halves ADR 0070 splits a URI action into: `action.rs` answers `Request::Import`, `viewer-core` asks its host for the file with `Event::NeedsFile`, and `ViewState::import` applies what comes back. Only §12.7.6.2's submission is refused by name at runtime, because it is a network request a document initiated, and its own row is `reported` for that. **This row and §12.7.6.1's said \"the other two are refused by name … an import wants a filesystem\" from the ninety-seventh session to the four-hundred-and-thirteenth, and it was false from the hundred-and-thirty-second**, when the window became a consumer and the filesystem an import wants became the host's. §12.7.6.4's own row has said \"[r]ead and performed\" ever since, and §12.6's count of Table 201's twenty types has named import-data among the ten performed since the three-hundred-and-ninety-fourth — so three rows held the answer and the two above them did not. `doc/todo/01`'s fifth failure shape at family scale, found by its tenth sweep. `partial` for the submission and for the clause's \"any other data format that it supports\", which §12.7.6.4 names. **Read and kept in the five-hundred-and-sixty-fifth**: §12.7.6.1 moved to `implemented` in the five-hundred-and-sixty-second and this row's count is of the three form action *types* rather than of the family, which is `conformance --bin counts`'s dominant noise shape and is correct here — two performed, §12.7.6.2's submission refused by name."

[[clause]]
clause = "12.7.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs"]
test = ["crates/pdf-model/src/view.rs::a_reset_form_action_names_fields_to_reset_or_fields_to_spare", "crates/pdf-model/src/action.rs::each_of_the_three_form_action_types_reaches_its_own_answer"]
note = "**The fourteenth sweep's first finding as a program, in the five-hundred-and-sixty-second session: `partial` above a note naming nothing owed.** This clause's own text is three bullets naming the three form action types and one sentence saying interactive forms support them \"in addition to those described in 12.6.4\" — no `shall`, no entry, nothing a processor can fail. All three names are recognised by `action.rs`, which is the whole of what the clause states; what each of them *does* is §12.7.6.2's, §12.7.6.3's and §12.7.6.4's, and those rows carry their own statuses (`reported`, `implemented`, `partial`). A framing row is `implemented` when its own prose is executed, which is this ledger's convention wherever a parent's children still owe something — §7.4's framing beside five `partial` filters. The framing: these actions are triggered from a widget and act on the interactive form as a whole. Two of the three are performed — §12.7.6.3's reset, whose whole effect is a display change, and §12.7.6.4's import, which asks the host for the file §12.7.8's data comes in — and only §12.7.6.2's submission is refused by name. **This sentence said \"the other two are refused by name\" until the four-hundred-and-thirteenth session**, false since the hundred-and-thirty-second; §12.7.6's row carries the correction and the dates."

[[clause]]
clause = "12.7.6.2"
title = "Submit-form action"
status = "reported"
code = ["crates/pdf-model/src/action.rs"]
test = ["crates/pdf-model/src/action.rs::a_name_the_table_does_not_hold_is_not_an_action"]
note = "Refused by name, and named at runtime: `Action::Refused` carries \"SubmitForm: §12.7.6.2's submission, which needs a network\", and `viewer-ui` prints it when a click reaches one. Table 239's `/F` file specification and Table 240's flags describe a network request *a document* initiated, which belongs with `/Launch` and `/URI` on the far side of principle 3's boundary — the decision is about this machine, not about the file. **Re-read in the three-hundred-and-seventy-third session against the round that gave restrictions a policy, and it is not one of them**: this is refused for want of a *capability*, so none of `CLAUDE.md`'s four levels would turn it on, and Table 258's `/Form /SubmitStandalone` — the usage right that would grant it — is a grant this program has nothing to unlock. The row stays `reported` and the reason stays a capability (ADR 0212)."

[[clause]]
clause = "12.7.6.3"
title = "Reset-form action"
status = "implemented"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/src/view.rs::a_reset_form_action_names_fields_to_reset_or_fields_to_spare", "crates/pdf-model/tests/variable_text.rs::a_reset_form_action_draws_the_default_value_instead_of_the_value"]
note = "Table 241 and Table 242 whole, and **performed**. The clause states the effect exactly — a processor \"shall set the value of the V entry in the field dictionary to that of the DV entry … If no default value is defined for a field, its V entry shall be removed\" — so `Field::read` reads `/DV` instead of `/V` up the same `/Parent` chain for a widget the action touched, and a field with no `/DV` anywhere draws nothing at all, which is what \"removed\" means for a program that does not write to the file. A check box answers from its `/DV` rather than its `/AS`, because the `/AS` in the file describes the state the action just replaced. `/Fields` is read in both of its forms and all three of the table's shapes: absent means \"all fields in the document's interactive form are reset\"; present with the flag clear means those fields and \"[a]ll descendants of the specified fields in the field hierarchy\", which §12.7.4.2's naming makes a prefix test; present with the flag set means everything except them. The reset set lives in `ViewState` beside §12.6.4.11's hidden annotations, for the same reason: nothing is written to the document. 3 corpus actions in 2 documents."

[[clause]]
clause = "12.7.6.4"
title = "Import-data action"
status = "partial"
code = ["crates/pdf-model/src/action.rs", "crates/pdf-model/src/view.rs", "crates/viewer-core/src/interact.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::an_import_data_action_names_its_file_and_its_format", "crates/pdf-model/tests/forms_data.rs::an_imported_value_is_the_one_that_is_drawn"]
note = "Read and performed, in the two halves §12.6.4.8's URI action is split into (ADR 0070). `action.rs` reads Table 243's `/F` in both the forms §7.11.3 gives a file specification and answers `Request::Import`; `viewer-core` asks its host for the named file (`Event::NeedsFile`) and applies what comes back through `FormsData::read` and `ViewState::import`; `viewer-ui` states the policy the clause deliberately does not — a single path component, resolved against the directory the open document is in, and nothing else — because a policy about files belongs to whoever owns the filesystem, and since the hundred-and-thirty-second session that is never the core. What is left is the clause's \"or any other data format that it supports\": ISO 19444-1's XFDF is the same data in XML and is declined by name, because an XML parser is a dependency and a decision rather than a clause. No corpus document carries this action. **Re-read off the blame list in the five-hundred-and-seventeenth**, and one word of it was imprecise: what is owed is *XFDF*, which the clause names, and not \"any other data format that it supports\", which a processor satisfies by supporting what it supports. The `shall` is to import from the named file; the two formats it lists are FDF, which `FormsData::read` reads, and ISO 19444-1's XFDF, which is declined by name. **This clause's channel has a second caller since the five-hundred-and-twenty-second**: Annex O's `fdf` parameter names a file for the same purpose, so `Event::NeedsFile { purpose: ImportData }`, `Command::Supply` and the host policy in `viewer_host::resolve_import` carry a URI's request exactly as they carry a click's — and the rule that decides which format a name states is `pdf_model::action::data_format`, one reading for the two clauses that ask (ADR 0357)."

[[clause]]
clause = "12.7.7"
title = "Named pages"
status = "partial"
code = ["crates/pdf-model/src/named_page.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/src/named_page.rs::both_trees_are_read_and_a_template_is_looked_up_first", "crates/pdf-model/src/named_page.rs::a_document_that_breaks_the_clauses_own_rule_is_named", "crates/pdf-model/tests/forms_data.rs::an_imported_template_adds_a_page_the_document_already_held"]
note = "Both name trees read, and the clause's own rule about them checked: a page in `/Pages` \"shall be left in the page tree\", and one in `/Templates` \"shall have an object type of Template rather than Page and shall have no Parent or B entry\" — four invariants, run by `NamedPages::disagreements` for §12.8.7's reason. The clause gives naming a page two purposes and only one of them is in scope: an import-data action adding it, which is §12.7.8.3.3 and is performed — `ViewState::appended_pages` holds what an import added and `Pages::detached` builds a template page without the `/Parent` it is forbidden to have. The other, a script adding it, is on principle 5's closed exclusion list. Measured over all 964 openable corpus documents: 1 names a page, `issue19389.pdf`, with one entry in `/Pages` and no disagreements; none states a `/Templates` tree. Read and kept in the five-hundred-and-first session: `NamedPages::disagreements` and `Pages::detached` stand as named, and the script purpose is still principle 5's exclusion."

[[clause]]
clause = "12.7.8"
title = "Forms data format"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::an_imported_value_is_the_one_that_is_drawn"]
note = "§12.7.8's Forms Data Format, read whole, with the two entries that would *add* to a document — `/Pages` and `/Annots` — named rather than applied. Its subclauses are recorded individually rather than as one row because the format is a document type in its own right and the ledger's unit is a subclause."

[[clause]]
clause = "12.7.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/src/forms_data.rs::an_fdf_file_is_read_by_the_pdf_reader"]
note = "The framing, and the four differences it lists are all *relaxations* of clause 7 — an optional cross-reference table, no incremental updates, one required body object, a direct stream length — so the reader that opens a damaged PDF opens an FDF file unchanged. The clause's third use is the one this program acts on: data \"imported back into the corresponding PDF interactive form\", which is §12.7.4.3 laying out a different string."

[[clause]]
clause = "12.7.8.2"
title = "FDF file structure"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs", "crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-model/src/forms_data.rs::an_fdf_file_is_read_by_the_pdf_reader"]
note = "An FDF file's structure, which is clause 7's with the differences the subclauses below name. Both of the ones that need code are here: `xref.rs` recognises the header, and `forms_data.rs` takes the object hierarchy's root from Table 245's `/FDF` rather than from §7.7.2's catalog."

[[clause]]
clause = "12.7.8.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/src/forms_data.rs::an_fdf_file_is_read_by_the_pdf_reader"]
note = "The three required elements and one optional one, all read by `pdf_syntax::Document`: the header below, a body of indirect objects, an optional cross-reference table and an optional trailer. The test's fixture deliberately has no cross-reference table at all, which is how real FDF files are written and what §12.7.8.1 permits."

[[clause]]
clause = "12.7.8.2.2"
title = "FDF header"
status = "implemented"
code = ["crates/pdf-syntax/src/xref.rs"]
test = ["crates/pdf-model/src/forms_data.rs::an_fdf_file_is_read_by_the_pdf_reader"]
note = "The header is `%FDF-1.n` rather than `%PDF-n.m`, and §7.5.2's rule that byte offsets are measured from the percent sign applies to it unchanged. `xref::read` searches for `%PDF-` first and `%FDF-` only where that is absent, so a PDF whose first kilobyte happens to contain the second marker is still measured from its own header."

[[clause]]
clause = "12.7.8.2.3"
title = "FDF body"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/src/forms_data.rs::an_fdf_file_is_read_by_the_pdf_reader"]
note = "The body is one FDF catalog and the objects it reaches, which is the object graph `pdf-syntax` already resolves. The clause's own sentence that the `%EOF` comment has no semantics needs nothing: this reader never gave it any."

[[clause]]
clause = "12.7.8.2.4"
title = "FDF trailer"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/src/forms_data.rs::a_file_with_no_fdf_dictionary_is_not_forms_data"]
note = "Table 244's one required entry: the trailer's `/Root` names the FDF catalog, which `Document::catalog` reads exactly as it reads §7.5.5's — including through the trailer a scan recovers, which is what an FDF file with no cross-reference table needs."

[[clause]]
clause = "12.7.8.3"
title = "FDF catalog"
status = "implemented"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/src/forms_data.rs::a_file_with_no_fdf_dictionary_is_not_forms_data"]
note = "Table 245's two entries: `/Version`, and the `/FDF` dictionary the table makes the catalog's one requirement — which is also how a file handed to this reader identifies itself as forms data rather than as a PDF."

[[clause]]
clause = "12.7.8.3.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/src/forms_data.rs::what_is_not_applied_is_named", "crates/pdf-model/src/forms_data.rs::the_file_says_which_document_it_is_for_and_what_the_server_said"]
note = "Table 246 whole, read, with three entries named rather than applied and each for a stated reason: **`/Pages` is applied** — `read_pages` builds §12.7.8.3.3's page dictionaries and `named_page` adds their templates to the document, which has been true since §12.7.7's named pages landed in the hundred-and-first session and which this note went on saying was owed until the hundred-and-eighteenth — `/Annots` needs a second document's objects to reach the interpreter, `/JavaScript` is on principle 5's closed exclusion list, and `/Differences` carries the target document's own incremental updates, which applying would mean writing a PDF. `/F` and `/ID` are read and `FormsData::belongs_to` compares §14.4's permanent identifier, `/Status` is the server's message and is printed because the table says it \"shall be displayed\", and `/Encoding` decides how a value's bytes become characters — its default and its two Unicode values are §7.9.2.2's own three, and its four registered character sets are refused by name for Table 116's reason, per string rather than per file. **Read and kept in the five-hundred-and-tenth session**: `read_pages`, `FormsData::belongs_to`, the displayed `/Status` and the per-string `/Encoding` refusal are all in `forms_data.rs` where this note points."

[[clause]]
clause = "12.7.8.3.2"
title = "FDF fields"
status = "partial"
code = ["crates/pdf-model/src/forms_data.rs", "crates/pdf-model/src/view.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::an_imported_value_is_the_one_that_is_drawn", "crates/pdf-model/src/forms_data.rs::ff_replaces_and_setff_modifies", "crates/pdf-model/src/forms_data.rs::a_kids_tree_flattens_to_fully_qualified_names"]
note = "Table 249, and the clause's one load-bearing sentence: \"importing a field causes the values of the entries in the FDF field dictionary to replace those of the corresponding entries in the field with the same fully qualified name in the target document\". Both halves are executed. *The same fully qualified name*: `/T` is concatenated down `/Kids` with §12.7.4.2's full stop, and the pairing runs through the same widget-by-name table §12.6.4.11 and §12.7.6.3 use. *Replace*: `ViewState::import` holds the new value beside the reset set, `Field::read` takes it instead of walking for `/V`, and a field stating no `/V` leaves the widget with none — which is what replacing with nothing means for a program that does not write to the file. The table's two flag triples are one arithmetic in `FlagChange`, with the table's own precedence: `/Ff` and `/F` replace, `/SetFf` and `/SetF` are applied before `/ClrFf` and `/ClrF`, and the modifying pair \"shall be ignored\" where the replacing entry is present. `/Opt` is read in both of the forms the table gives an element. Named and not applied: `/AP` and `/APRef`, a push-button's appearance streams in this or another file, `/IF`, the icon fit dictionary that would place them, `/A` and `/AA`, and `/RV`, which is XFA rich text and excluded. Read and kept in the five-hundred-and-first session; `forms_data.rs` quotes each of the named entries, so the refusals are read refusals rather than absences."

[[clause]]
clause = "12.7.8.3.3"
title = "FDF pages"
status = "partial"
code = ["crates/pdf-model/src/forms_data.rs", "crates/pdf-model/src/view.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::an_imported_template_adds_a_page_the_document_already_held", "crates/pdf-model/tests/forms_data.rs::a_template_this_document_cannot_reach_is_named", "crates/pdf-model/src/forms_data.rs::a_template_page_names_a_page_this_document_already_holds"]
note = "Tables 251, 252 and 253 read, and a template page **added**: the reference resolves through §12.7.7's name trees to a page this document already holds, so the whole operation is a name lookup and no page content crosses between files. Where the page is added is this program's choice and is written down — after the document's own, which §12.7.8.3.3 does not state and which is the only order leaving every existing page index meaning what it meant. Two refusals, each named: Table 253's `/F` puts the template in another file, which this reader has no filesystem to open, and a `/TRef` naming no page in either tree is a file asking for something the document does not contain. Table 252's `/Fields` is read and not applied, because it names fields of the *template's* own hierarchy rather than the target's `/AcroForm`; `/Rename` is read and cannot be applied by anybody, since the clause says outright that the flag \"does not define a renaming algorithm\" — and nothing here merges field trees for it to matter to. No corpus document states an FDF `/Pages` entry, because none carries an FDF file at all. Read and kept in the five-hundred-and-first session; the two refusals still print, and `\"TRef\"` and `\"Rename\"` are quoted where the note says."

[[clause]]
clause = "12.7.8.3.4"
title = "FDF annotation dictionaries"
status = "partial"
code = ["crates/pdf-model/src/forms_data.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::an_imported_annotation_flag_hides_a_widget"]
note = "Table 254's `/Page` and each annotation's own dictionary are read into `FdfAnnotation` and drawn by nothing, which is said out loud. `partial` rather than `reported` since the hundred-and-fifteenth session: the table is read, and a row that said otherwise understated it. Their *appearances* are §12.5's and are implemented; what is missing is that an FDF annotation's `/AP` resolves in the FDF file's object space while the page it belongs to is in the target document's, so drawing one means a second `Document` reaching the interpreter — a design question rather than an oversight. **Read and kept in the five-hundred-and-tenth session**: `FdfAnnotation` is read and reaches no display list, and the second-`Document` design question is still open, not decided by attrition."

[[clause]]
clause = "12.7.9"
title = "Non-interactive forms"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_flattened_form_field_says_what_it_was"]
note = "The clause is two sentences and both are now answered. Non-interactive forms \"do not use widget annotations but are represented with page content\", which this tree draws as page content because that is all it is; and they \"are defined by the PrintField attribute (14.8.5.6) for repurposing and accessibility purposes\", which is read as of the ninety-fifth session. So what was recorded as a gap was the *accessibility* half, and closing §14.8.5.6 closed this row with it."

[[clause]]
clause = "12.8"
title = "Digital signatures"
status = "partial"
code = ["crates/pdf-model/src/signature.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/tests/signatures.rs::every_signed_corpus_document_says_what_it_signed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed"]
note = "Every signature the document states is read — Table 255 whole — and **two of the three questions a signature asks are answered** (ADR 0215, ADR 0229). §12.8.1 divides the work itself and the parts need different infrastructure: (1) has the document changed since it was signed, which is \"[t]he digest … recomputed and compared with the one stored in the document\" and needs no certificate at all; (2) does the signature verify under the signer's public key, which needs an X.509 certificate and an RSA, DSA or ECDSA verification; (3) is the signer trusted and was the certificate revoked, which needs a trust store and a network. **Question 1 is answered**: `Signature::integrity` hashes the bytes `/ByteRange` names, with the ten digests Table 260 and Table 256 state between them — the base standard's six and ISO/TS 32001 section 5.1.4's four, added in the five-hundred-and-fifty-fifth session (ADR 0390) — and compares them against what `pdf_model::cms` reads out of §12.8.3.3's signature value. **Question 2 is answered for two of Table 260's three algorithm families** — RSA since the three-hundred-and-ninety-second session, under *both* of RFC 8017's paddings since the four-hundred-and-eighty-seventh (ADR 0322), and DSA since the four-hundred-and-seventy-ninth (ADR 0314): `Signature::authenticity` finds the certificate the `SignerInfo` names among the ones the signature itself carries, reads its `subjectPublicKeyInfo` with `pdf_model::x509`, and verifies with `pdf_model::pkcs1` (RFC 8017 section 8.2.2), `pdf_model::pss` (its sections 8.1.2 and 9.1.2, over the `RSASSA-PSS-params` the signature's own algorithm identifier carries) or `pdf_model::dsa` (FIPS 186-4 section 4.7). The elliptic-curve family is refused with an argument rather than half-written and every one of its signatures and keys is named by the object identifier the file states. Question 3 is named as unanswered, in the program's own words, on every document that carries a signature. `Signature::coverage` still answers the question that needs no signature value either — a range that stops short names bytes **nobody signed**. Measured over the corpus: 9 documents carry 10 signature dictionaries; 5 still hash to what they record, **4 do not**, and 1 records no digest in the open at all because its `SignerInfo` has no signed attributes. The 4 are `issue6127.pdf`, both of `xfa_filled_imm1344e.pdf`'s, and a fuzzed file. Not one of these sentences says a signature is valid, and that is the point of the wording."

[[clause]]
clause = "12.8.1"
title = "General"
status = "partial"
code = ["crates/pdf-syntax/src/document.rs", "crates/pdf-model/src/signature.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-syntax/src/document.rs::a_signature_dictionary_is_recognised_by_its_type", "crates/pdf-syntax/src/document.rs::a_signature_dictionary_with_no_type_is_recognised_by_its_byte_range", "crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies"]
note = "Table 255 entire: `/Type`, `/Filter`, `/SubFilter`, `/ByteRange`, `/Contents`, `/Cert`, `/Name`, `/M`, `/Location`, `/Reason`, `/ContactInfo`, `/Changes`, and `/Reference`'s transform method — which is what tells the clause's three kinds apart, since a certification signature \"shall contain a signature reference dictionary … that has a DocMDP transform method\". **The clause's digest is computed since the three-hundred-and-seventy-seventh session**: \"[t]he digest shall be recomputed and compared with the one stored in the document. Differences between the two indicates that modifications have been made since the document was signed\", and `Signature::integrity` is that comparison over the bytes `/ByteRange` names. **The sentence before it is now half done** (ADR 0229): the signer's certificate \"shall be determined and verified by the signature handler to match with any of the validation parameters and other conditions\", and `Signature::authenticity` determines it — by RFC 5652's issuer-and-serial or subject key identifier, among the certificates the signature itself carries — and uses its public key to verify the signature value. What it does not do is the rest of that sentence: nothing here validates a certification path, checks revocation or holds a trust store, so \"the validation parameters and other conditions\" are unmet and the row stays `partial`. The report says which of the three questions it answered rather than letting either result read as a verdict, and never uses the word *valid*. Two of the clause's other rules are applied rather than stored: a `/SubFilter` of `ETSI.CAdES.detached` or `ETSI.RFC3161` turns §12.8.1's \"should\" about the byte range into Table 255's \"shall\", and a signature field with no `/V` — or a `/V` with neither a `/ByteRange` nor a `/Contents` — is a field waiting to be signed rather than a signature. §7.6.2's exception is also here: a signature dictionary's `/Contents` is not encrypted. **That predicate was wrong for as long as it existed and `issue17069.pdf` is the witness** — it recognised a signature dictionary by `/Type` alone, and Table 255 makes `/Type` \"(Optional if Sig)\" with a default of `Sig`, so that document's encrypted, `/Type`-less signature had its 33 680-byte value pushed through the cipher and came back empty. A dictionary with no `/Type` is now recognised by the `/ByteRange` and `/Contents` pair Table 255 requires of every signature carrying a byte range digest; nothing else in the standard has a `/ByteRange`. ADR 0215. **Read and kept in the five-hundred-and-forty-fifth**: `revocation_lists` counts §12.8.4.1's `/CRLs` and asks nothing of them, and no trust store or path validation exists anywhere under `crates/`, so what the row calls unmet is unmet in the same way. **One entry of Table 256 is read by nothing, and it was found in the five-hundred-and-fifty-fifth**: `/DigestMethod`, \"(Required) A name identifying the algorithm that shall be used when computing the digest if not specified in the certificate\". No source under `crates/` names the string. **Its value list is the base standard's six and nothing more, which took reading the errata to establish**: ISO/TS 32001 section 5.1.3 added SHA3-256, SHA3-384, SHA3-512 and SHAKE256 to this entry, and Errata Collection 3's issue #236 — `Review/Accepted` — strikes that subclause out entirely, \"Delete all of clause 5.1.3\". A round that reads only `doc/md/` will not see that: the erratum is a PDF annotation, and `tools/spec-errata` is the instrument. What it costs today is nothing rather than a mark: the digest it names is the one a transform method's comparison would be computed with, and that comparison is §12.8.2.2.2's, which the neighbouring rows already record as not done. It is written here so that whoever does the comparison finds the entry already named — `cms::Digest` computes all ten algorithms the entry may state (ADR 0390), so what is missing is a reader for a *name* and not a function."

[[clause]]
clause = "12.8.2"
title = "Transform methods"
status = "partial"
code = ["crates/pdf-model/src/signature.rs", "crates/pdf-model/src/view.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/tests/forms_data.rs::a_certified_document_states_which_operation_its_author_forbade"]
note = "Transform methods, as far as they say what a document permits. `DocMDP` is read and its `/P` level answered (§12.8.2.2), and `UR` is recognised where a `/Reference` names it — `usage_rights` matches Table 256's `/UR` and the permissions dictionary's `/UR3` — with Table 258 read out of its parameters. **This sentence said `FieldMDP` was recognised too, was found in the five-hundred-and-thirty-seventh session to be naming a string nothing in this tree wrote, and is true again**: `signature::field_mdp` reads Table 259's `/Action` and `/Fields` out of every `FieldMDP` entry in a signed signature's `/Reference`, and `restriction::asserted` answers a `FieldCovered` for a field one of them names. All three of the clause's transform methods are therefore read, and §12.8.2.4's row carries what that cost to find: the array is plural and this tree had been reading its first entry, on the corpus's one certification signature, which states two (ADR 0403). What no transform method can do here is its *purpose*, which §12.8.2.2.2 states: comparing the signed and current revisions needs the digest to establish the signed one first. **Since the three-hundred-and-seventy-third session the `/P` level is answered per *operation* rather than as one verdict** — `restriction::asserted` says which of Table 257’s three levels withholds which of the two things this program does — because level 2 permits filling in a field and not annotating, and a reader that read the entry as one answer could not tell them apart (ADR 0212)."

[[clause]]
clause = "12.8.2.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it"]
note = "The framing: a transform method is how modification detection is specified, with `/TransformParams` carrying its variable part. Which method a signature names is read; the comparison it asks for is §12.8.2.2.2's and is not done. **Read and kept in the five-hundred-and-tenth session**, against the family this clause grew since it was written: the digest and certificate rounds (ADRs 0229, 0314) answer questions 1 and 2 and leave the comparison exactly where this note puts it - its own row says step two is not done."

[[clause]]
clause = "12.8.2.2"
title = "DocMDP"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/tests/signatures.rs::every_signed_corpus_document_says_what_it_signed"]
note = "The `DocMDP` transform, read as what it is: an author's statement of \"what changes shall be permitted to be made to the document and what changes invalidate the author's signature\". Table 257's `/P` is read with its three values and its default of 2, and §12.8.6's `/Perms /DocMDP` is what makes it binding rather than advisory. `partial` for §12.8.2.2.2's validation, which needs the digest — **not** for the prevention half, which §12.8.2.2.1's row now holds. The corpus's one certification signature states `/P 2` and has 2.5 MB appended after it — a filled-in form, which is exactly what that level permits, and `restrictions.rs::the_corpus_certification_permits_filling_in_and_not_annotating` reads that same file at both ends: the `/P` that permits the form to be filled in withholds a comment on it, which is the distinction level 3 exists to draw. **And the digest half of §12.8.2.2.2 exists since the three-hundred-and-seventy-seventh session**, which changes what this row owes: the byte range digest is recomputed, so what is left is the object-by-object comparison of the signed revision with the current one. On the corpus's one certification signature that comparison would have nothing to work from — the digest does not match, so the file is not the one that was signed (ADR 0215)."

[[clause]]
clause = "12.8.2.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it"]
note = "Table 257's parameters as a statement: `/P` 1 means \"the document shall be final; that is, any changes shall invalidate the signature\", 2 and 3 \"permit modifications that are appropriate for form field or comment workflows\". Read, with the table's default, and answered by `Modification`. The clause's own restriction — \"[a] document can contain only one signature field that contains a DocMDP transform method\" — is a statement about a *document* and is not enforced, because enforcing it would mean refusing to draw a file over a claim about its signatures. **And the sentence in the parenthesis is a `shall` addressed to a processor that modifies, honoured since the hundred-and-ninety-first session**: \"(These changes to the document shall also be prevented if the signature dictionary is referred from the DocMDP entry in the permissions dictionary.)\" An edit is refused where §12.8.6's `/Perms /DocMDP` names a signature whose level does not permit it: `/P` 1 permits neither of the two operations this program has, `/P` 2 permits filling in a field and not annotating — Table 257's own \"filling in forms, instantiating page templates, and signing\" — and `/P` 3 permits both. **Where the refusal lives moved in the three-hundred-and-seventy-third session and the `shall` did not**: `pdf_model::restriction::asserted` states which level withholds which operation, `viewer_core` asks it once per operation, and the refusal reaches a person as `Event::Refused` carrying the clause rather than as `set_field` returning zero — a number three other things also produce. `CLAUDE.md` makes obeying this the reader's own policy, defaulting to obeying and always possible to turn off; the clause is unchanged and so is what happens by default (ADR 0212). A `/P` outside 1..=3 permits, because refusing on a value the table does not define would let a malformed number lock a document a person is entitled to fill in. **It became a requirement without the clause changing**: the sentence binds a processor that modifies, and this one could not until form filling landed in the hundred-and-thirty-fifth session and saving in the hundred-and-thirty-sixth — the same shape as §7.6.3.2's initialisation vector (ADR 0129). One corpus document names a `/DocMDP` and it states `/P 2`, so nothing in the corpus changes; the fixture is hand-built, which is trap 8's rule."

[[clause]]
clause = "12.8.2.2.2"
title = "Validating signatures that use the DocMDP transform method"
status = "partial"
code = ["crates/pdf-model/src/signature.rs", "crates/viewer-ui/src/bin/pdf-viewer.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "Validating a `DocMDP` signature is two steps, and **the first of them is done since the three-hundred-and-seventy-seventh session**: \"a PDF processor first shall verify the byte range digest. Next, it shall verify that any modifications that have been made to the document are permitted by the transform parameters.\" `Signature::integrity` recomputes the byte range digest and compares it with what the signature value records, which is step one. **Its certificate half arrived in the three-hundred-and-ninety-second session** (ADR 0229): the signature value is verified under the key in the certificate it carries, so a match is now backed by an RSA verification rather than standing alone. It still proves nothing about *who* signed, because nothing here decides whether that certificate is anybody's. Step two is not done: it compares the signed revision with the current one object by object, which this reader does not reconstruct. What a person is given instead is the `/P` level the author stated, how many bytes were appended after the signed range, and whether the signed range still hashes correctly — and the corpus's one certification signature, `xfa_filled_imm1344e.pdf`, comes out **changed**, which is a fact about that file rather than about the clause. **Read and kept in the five-hundred-and-forty-fifth**: nothing reconstructs a signed revision — `Document` holds one view of the file — so step two is still the step that is missing, and §12.8.2.4's `reported` row beside it is where the transform that would name the fields went."

[[clause]]
clause = "12.8.2.3"
title = "UR"
status = "partial"
code = ["crates/pdf-model/src/signature.rs", "crates/pdf-model/src/view.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::a_save_beyond_the_granted_usage_rights_withdraws_the_signature", "crates/pdf-model/tests/signatures.rs::no_corpus_documents_usage_rights_are_exceeded_by_what_this_program_does"]
note = "The `UR` transform: a usage rights signature granting \"features of a PDF processor that are not available by default\", deprecated in PDF 2.0. **Was `reported`, and the hundred-and-ninety-eighth session closed the half of it that binds this tree.** The granting half stays out of reach and is stated rather than silent: this program has no feature behind such a gate, so there is nothing for a valid usage rights signature to unlock. The clause's other sentence addresses a processor that *modifies* — \"A PDF processor that modifies a PDF, with a UR signature in excess of the rights that are granted by that signature, should remove that signature prior to writing the newly modified PDF.\" — and this one has modified since the hundred-and-thirty-fifth session. Table 258 is read into `signature::UsageRights`, all of it but `/Msg` — \"[a] text string that may be used to specify any arbitrary information, such as the reason for adding usage rights to the document\", which is the producer's sentence to a *person* and the one entry of the table nothing here quotes (`doc/todo/01`'s fifteenth sweep, the five-hundred-and-seventeenth). It states no requirement, and where it would go is `notes.rs`, which already tells a reader what the rights are when the document opens; showing the producer's stated reason beside them is a host's line and is named here rather than built.  `UsageRights::grants` answers for the two verbs this program has, `/Form /FillIn` and `/Document /FullSave`, honouring `/P` first (\"[i]f false , any possible restriction may be ignored\", which is the table's default) and then `/V` (\"[i]f an unknown version is present, no rights shall be enabled\"); and `ViewState::save` rewrites the permissions dictionary without its `/UR3` where a save would exceed them. What is removed is the entry rather than the object, because §7.5.6 appends and §12.8.6 makes a usage rights signature the one \"referred to from the UR3 entry in the permissions dictionary\". `notes.rs` says so when the document opens rather than when it is saved, which is while a person can still decide not to. **The condition has no members in the corpus and that was measured, not assumed**: all 4 documents carrying a `/UR3` grant both rights and all 4 come out `/P false`, so nothing this program can do to one exceeds it. §12.8.2.2.2's comparison of two revisions is still not done, which is what keeps this `partial`. ADR 0159."

[[clause]]
clause = "12.8.2.4"
title = "FieldMDP"
status = "partial"
code = ["crates/pdf-model/src/signature.rs", "crates/pdf-model/src/restriction.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/forms_data.rs::a_signature_covers_the_fields_its_field_mdp_transform_names", "crates/pdf-model/tests/restrictions.rs::the_corpus_certification_also_covers_a_field_by_name", "crates/pdf-model/tests/signatures.rs::the_corpus_states_the_fields_one_signature_covers"]
note = "Table 259 is read: `signature::field_mdp` finds every `FieldMDP` entry in a signed signature's `/Reference` array and reads its `/Action` and `/Fields` into `FieldSelection`, and `restriction::asserted` answers `Restriction::FieldCovered` for a field one of them names, which `notes.rs` words to a person as the clause's own consequence rather than as a prohibition — \"any modifications to specific form fields shall invalidate that recipient's signature\", which is not what §12.7.5.5's lock says and is why the two are separate reasons (ADR 0403). `partial` for the same thing §12.8.2.2 is `partial` for: \"FieldMDP signatures shall be validated in a similar manner to DocMDP signatures\", and §12.8.2.2.2's comparison needs the signed revision reconstructed. Table 256's `/Data`, which is required here and nowhere else, is what that comparison would be scoped by and is unread for the same reason. **This row read `reported` on the claim that no corpus document states one, and that claim was false.** `xfa_filled_imm1344e.pdf` — the corpus's one certification signature — carries **two** signature reference dictionaries, a `DocMDP` and a `FieldMDP`, and this tree read only the first of that array for its whole life; `MOZILLA-1671648-0.pdf` under `doc/corpora/format-corpus` is a second witness from the same producer. The measurement that had said zero was `grep -rl FieldMDP`, which prints nothing over these files because grep classes them as binary without `-a`. The field the transform names is `form1[0].SignatureField3[0]`, which `form::fields` independently derives as §12.7.4.2's fully qualified name for the document's one field — so the fully-qualified reading in `FieldSelection::covers` has a producer's file behind it and not only an argument."

[[clause]]
clause = "12.8.3"
title = "Signature interoperability"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/pss.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "Which cryptographic containers a handler shall understand, and **which of the three questions each one lets a program without a certificate answer** (ADR 0215). Table 260 lists the digest and signature algorithms per `/SubFilter`; the ten digests it and Table 256 name between them — SHA1, SHA256, SHA384, SHA512, RIPEMD160 and MD5, and ISO/TS 32001 section 5.1's SHA3-256, SHA3-384, SHA3-512 and SHAKE256 — are all implemented in `cms::Digest`, and `Signature::integrity` recomputes the byte range digest with whichever the signature states. **The signature half is implemented for two of Table 260's three algorithm families**: RSA since the three-hundred-and-ninety-second session (ADR 0229), verified as RFC 8017's RSASSA-PKCS1-v1_5 against the key in the certificate the signature carries, up to the 4096 bits the table names and beyond — **and, since the four-hundred-and-eighty-seventh session, under the family's other padding too** (ADR 0322): `id-RSASSA-PSS` is a padding inside the table's RSA row rather than a fourth family, more real signatures use it than use ECDSA, and `pdf_model::pss` verifies it as RFC 8017 sections 8.1.2 and 9.1.2 with Appendix B.2.1's MGF1, parameterised by the `RSASSA-PSS-params` the signature's own algorithm identifier carries, for the hashes of Appendix A.2.1's set this program computes (SHA-1, SHA-256, SHA-384, SHA-512); a parameter outside the RFC — another mask generation function, a trailer field other than 1, a hash the set does not admit — is reported with the file's own numbers, never defaulted around; and **DSA since the four-hundred-and-seventy-ninth** (ADR 0314), as FIPS 186-4 section 4.7 over RFC 3279's `Dss-Parms` and `Dss-Sig-Value`, which ISO 32000-2 names and does not define. Table 260's PDF 2.0 ECDSA — with the EdDSA and the four Brainpool and Edwards curves ISO/TS 32002 section 5.1 adds beside it — is **not** implemented and is reported by the object identifier the file states rather than by a guessed name. **The six digests are the base standard's list and not the whole of it, and the rest arrived in the five-hundred-and-fifty-fifth session** (ADR 0390): ISO/TS 32001 section 5.1.4 adds SHA3-256, SHA3-384, SHA3-512 and SHAKE256 to Table 260's Message Digest row, and all four are computed, against the example values NIST publishes for FIPS PUB 202, which section 5.1.1 names as where they are defined. **SHAKE256's output length is a documented choice rather than a requirement, and only the errata say so.** The published text pinned the algorithm to `id-shake256` and its NOTE said that requirement \"fixes the SHAKE256 output length for the digest at 512 bits\"; Errata Collection 3's issue #404 struck the pinning sentence and replaced it with a deferral — \"the applicable stipulations on algorithm identifiers in RFC 8702, 3.1 and RFC 8419, 3.1, 3.2 shall be followed\" — to two documents this tree does not hold, while leaving the NOTE standing. So 512 bits is the reading the retired sentence and the surviving NOTE agree on, taken deliberately, and any identifier other than `id-shake256` is reported by its number rather than computed at a guessed length. **Where the four apply is part of the requirement and is honoured**: section 5.1.4 adds them \"to the Message Digest value entry for adbe.pkcs7.detached, ETSI.CAdES.detached or ETSI.RFC3161\", so `Digest::TRIED_WHEN_UNSTATED` — the set §12.8.3.2's `adbe.x509.rsa_sha1` is searched with, because it states no algorithm anywhere a reader can see — stays the base standard's six. **No document in the population states one of the four** (`signature_algorithm_census`), so the witnesses are hand-built (trap 8), and an identifier outside all ten is still reported by its own dotted decimal. What is left after that is trust and revocation, which the rest of §12.8.3 describes and which no amount of arithmetic answers. `partial` rather than `reported` because the container is now read rather than only named: `pdf_model::der` reads X.690's encoding and `pdf_model::cms` reads RFC 5652's `SignedData` out of it, both fuzzed at 1 000 000 runs. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/pss.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "The framing, and the `/SubFilter` entry that decides everything else: it \"shall specify the encoding of the signature value and key information, while the Filter entry shall specify the preferred handler\". Both are read, and the encoding is what `Signature::integrity` dispatches on — a PKCS #1 value is refused as needing the signer's key before it is even parsed, a detached CMS value is read for its `message-digest`, an `adbe.pkcs7.sha1` value for its encapsulated digest, and an `ETSI.RFC3161` token for RFC 3161's `messageImprint`. The interoperability the clause asks for is therefore real for the digest and for **two** of the three signature algorithm families: Table 260's RSA is verified under both of RFC 8017's paddings — RSASSA-PKCS1-v1_5 (ADR 0229) and, since the four-hundred-and-eighty-seventh session, RSASSA-PSS (ADR 0322) — and so is its DSA (ADR 0314), while PDF 2.0's ECDSA — which this clause introduces, \"as defined by Internet RFC 5480\" — is not, and is named at runtime by the object identifier the file states. **What this clause introduces is larger than the sentence in it**: ISO/TS 32002 section 5.1.3 appends a table of the ECDSA curves to this very subclause — P-256, P-384, P-521 and three Brainpool curves — and adds EdDSA over Ed25519 and Ed448. The refusal's ground moved in the four-hundred-and-ninety-sixth session (ADR 0331): ADR 0314's premise that the curves' domain parameters are in no document this tree holds no longer decides — a reviewed curve crate carries them as reviewed constants, the same footing as the `crypto-bigint` arithmetic the verifiers now run on — and what refuses today is measured coverage: the stable packages on this tree's line cover three of the eight curves and neither Brainpool witness encoding, so they would close one signature in 811. Nothing here claims to *be* a handler: a handler decides trust, and this program decides nothing about a certificate. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.2"
title = "PKCS #1 signatures"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/src/signature.rs::a_pkcs1_signature_names_the_key_rather_than_the_encoding", "crates/pdf-model/src/signature.rs::a_pkcs1_signature_verifies_against_the_certificate_in_its_cert_entry", "crates/pdf-model/src/signature.rs::a_pkcs1_signature_with_no_certificate_has_no_key_to_verify_against", "crates/pdf-model/src/pkcs1.rs::a_signature_verifies_under_the_key_that_made_it_and_under_no_other"]
note = "PKCS #1 signatures, deprecated in PDF 2.0: \"the only value of SubFilter that should be used is adbe.x509.rsa\\_sha1 , which uses the RSA encryption algorithm and SHA-1 digest method\". **Verified since the three-hundred-and-ninety-second session** (ADR 0229), and this is the one signature format where doing so answers both of the first two questions at once: such a `/Contents` is the RSA signature over the byte range itself, with the digest inside it, so there is nothing recorded in the open to compare — and `Signature::authenticity` therefore reads the certificate out of `/Cert`, which the clause requires here (\"[t]he certificate chain of the signer shall be stored in the Cert entry\") and §12.8.3.4.2 forbids for a PAdES signature, takes its `rsaEncryption` key, and verifies. The digest is not stated anywhere a reader can see it — the clause names SHA-1 while Table 260 permits five — so each of the six is tried and RFC 8017 section 8.2.2's whole-block comparison is what makes six attempts no weaker than one. **Six and not ten, deliberately** (ADR 0390): ISO/TS 32001 section 5.1.4 adds its four digests \"to the Message Digest value entry for adbe.pkcs7.detached, ETSI.CAdES.detached or ETSI.RFC3161\", and this `/SubFilter` is none of the three, so trying them here would widen a table the standard did not. **This row said PKCS #1's DSA was a gap here and Table 260 says it is not** (ADR 0314): the table's \"DSA Algorithm Support\" row states **No** in the `adbe.x509.rsa_sha1` column, so §12.8.3.2's sentence about what the PKCS #1 standard supports is not a requirement on this `/SubFilter` at all, and a `/Cert` holding a DSA key is a file departing from the table — named by its identifier, verified by nothing. `partial` rather than `implemented` for the one reason that is left: the certificate is used and never *trusted*, which is question 3. **No corpus document uses this sub-filter**, so the witness is a fixture built on a key made for it. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.3"
title = "CMS (PKCS #7) signatures"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/pss.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "CMS (PKCS #7) signatures. \"When CMS signatures are used, the value of Contents shall be a DER-encoded CMS binary data object containing the signature\" and \"[t]he CMS object shall conform to Internet RFC 5652\" — **read since the three-hundred-and-seventy-seventh session**, as far as the digest question needs: `pdf_model::cms` reads `SignedData`'s encapsulated content type and content, its certificate count, and its first `SignerInfo`'s digest algorithm and signed and unsigned attributes, with the contents of `message-digest`. Both of §12.8.3.3.1's sub-filters are handled at the level of what they record: `adbe.pkcs7.detached` in the attribute, `adbe.pkcs7.sha1` in the encapsulated content. **Both of the things the clause asks of that certificate are now done** (ADR 0229): \"[a]t minimum the CMS object shall include the signer's X.509 signing certificate. This certificate shall be used to verify the signature value in Contents .\" `pdf_model::x509` reads it, `Signature::authenticity` finds the one the `SignerInfo` names and verifies the signature value under its key, and a signature whose certificates hold no such entry is reported as failing that `shall`. `partial` for what is left: two of Table 260's three families are verified — RSA under both of RFC 8017's paddings, RSASSA-PKCS1-v1_5 (ADR 0229) and, since the four-hundred-and-eighty-seventh session, RSASSA-PSS with the `RSASSA-PSS-params` the signature's own algorithm identifier carries (ADR 0322), and DSA since the four-hundred-and-seventy-ninth (ADR 0314) — while the elliptic-curve family is named by its identifiers and not verified; and revocation, which is §12.8.3.3.2's and needs a network. **ISO/TS 32001's four digests stood in that sentence until the five-hundred-and-fifty-fifth session and are now computed** (ADR 0390), which matters to this clause in particular: section 5.1.4 adds them to Table 260's Message Digest entry for exactly the sub-filters this subclause defines. The clause's other three bullets — an RFC 3161 timestamp as an unsigned attribute, revocation information as a signed attribute, and RFC 5755 attribute certificates — are `should`s about what a *producer* puts in the object; their presence is named and their contents are not interpreted. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.3.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/pss.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "The CMS container and the digest it carries, read. §12.8.3.3.1 states what `adbe.pkcs7.sha1` encapsulates — \"[t]he SHA-1 digest of the document's byte range shall be encapsulated in the CMS SignedData field with ContentInfo of type Data\" — and `Signature::integrity` compares that content against SHA-1 of the range rather than going through the `message-digest` attribute, because for that format the attribute is a digest of the digest. The clause's padding rule is also load-bearing here and is honoured by *not* acting on it: \"the value of Contents shall be padded with zeros\", and a pair of zero octets is X.690's end-of-contents marker, so a reader that stripped the padding first would cut the marker off the indefinite-length encoding four corpus documents use. **The certificate half is done since the three-hundred-and-ninety-second session** (ADR 0229): \"[t]his certificate shall be used to verify the signature value in Contents .\" All ten of the corpus's signature dictionaries verify under the key in a certificate their own signature value carries, at 1024, 2048 and 4096 bits, and each stops verifying when one bit of it is turned over. `partial` for the elliptic-curve family being the one of Table 260's three this program does not verify — RSA does, under both of RFC 8017's paddings (ADRs 0229, 0322), and so does DSA since the four-hundred-and-seventy-ninth session (ADR 0314) — and for trust, which no arithmetic answers. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.3.2"
title = "Revocation of CMS-based signatures"
status = "reported"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "Revocation checking for CMS signatures, which needs OCSP or a CRL and therefore a network — question 3, and the one this program will not answer without infrastructure it does not have. What it *can* do is say whether the material is there, and it does: the clause prints its own object identifier, \"adbe-revocationInfoArchival OBJECT IDENTIFIER::= {adbe(1.2.840.113583) acrobat(1) security(1) 8}\", and `cms::ADBE_REVOCATION_INFO_ARCHIVAL` is that constant, matched against the signer's signed attributes and named to a person where it is present. `issue17069.pdf` is the corpus's one witness. The CRLs and OCSP responses inside it are not parsed: reading them would be inventing a validator. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4"
title = "CAdES signatures as used in PDF"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "CAdES signatures as PDF uses them — a whole subclause of requirements, of which **the structural ones are checkable with no cryptography and are checked** while the validation ones are not. `Signature::pades_departures` answers §12.8.3.4.2's three constraints and three of §12.8.3.4.3's; §12.8.3.4.5's four validation steps and §12.8.3.4.6 to §12.8.3.4.8's revocation model are certificates and a network throughout. The digest question is answered for a PAdES signature exactly as for any other detached CMS one, and since the three-hundred-and-ninety-second session so is the signature-value question where the signer's key is RSA (ADR 0229) or, since the four-hundred-and-seventy-ninth, DSA (ADR 0314). **No corpus document is a PAdES signature** — all ten signature dictionaries in the 974 are `adbe.pkcs7.*` — so the condition was counted rather than assumed to have members (trap 11) and the witness is a fixture. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "The framing, and the scope sentence the rest of this subclause depends on: \"[t]he PDF signatures using the SubFilter value ETSI.CAdES.detached are referred to as PAdES signatures\". `Signature::pades_departures` reads that as the scope it is — it answers nothing at all for any other `/SubFilter`, so an `adbe.pkcs7.detached` signature with a `/Cert` and two signing times is not reported as breaking rules that do not address it, which is what a test asserts alongside the one that finds them. The profiles themselves are ETSI EN 319 122's and are not read. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.2"
title = "Signature dictionary for PAdES signatures"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/src/signature.rs::a_pades_signature_is_held_to_the_rules_that_need_no_certificate"]
note = "The signature dictionary for a PAdES signature, and **all three of its constraints are checked** because none of them needs a certificate: \"The ByteRange shall cover the entire PDF file, including the signature dictionary but excluding the Contents entry\", \"The signature dictionary shall not contain a Cert entry\", and \"Either the time of signing may be indicated by the value of the M entry in the signature dictionary or the signing-time attribute may be used, but not both\" — which needs the signed attributes, and `cms` reads their object identifiers. `partial` for the sentence before them: the value \"shall be a DER-encoded CMS SignedData binary data object\", and `pdf_model::der` deliberately accepts X.690's indefinite lengths, which DER forbids and four corpus documents write, so this reader cannot be used to check that a producer wrote DER. That tolerance is a decision with its cost written down (ADR 0215) rather than an oversight. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.3"
title = "Attributes for PAdES signatures"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/src/signature.rs::a_pades_signature_is_held_to_the_rules_that_need_no_certificate"]
note = "The attributes a PAdES signature carries, of which **three of the eleven are checked and the reason the rest are not is principle 5's**. Checked: (a) content-type, which the clause says “shall be present and shall always have the value 'id-data'”; (d) “exactly one single SignerInfo attribute shall be present”; and (e) “message-digest: shall be present and shall be used as defined in CMS ( Internet RFC 5652 )”, which since the three-hundred-and-ninety-second session is *used* as RFC 5652 defines rather than only required to exist: it is one of the attributes the signature is verified over, re-encoded as that RFC's section 5.4 `SET OF` (ADR 0229). Also checked is the half of (i) whose object identifier RFC 5652 states — counter-signature — while content-reference, content-identifier and content-hints are defined in documents this tree does not hold, and naming an identifier nobody here has read against its source would be asserting a fact about a specification. (b), (c), (f), (g), (h), (j) and (k) are ETSI EN 319 122-1's and RFC 5035's, likewise unread. No corpus document exercises any of it. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both. **Read and kept in the five-hundred-and-fifty-third**: `pades_departures` checks exactly the attributes this row names, plus the counter-signature half of (i), and returns nothing at all where the `/SubFilter` is not `ETSI.CAdES.detached` — the clause's own population."

[[clause]]
clause = "12.8.3.4.4"
title = "Profiles of ETSI.CAdES.detached"
status = "reported"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "The two profiles of `ETSI.CAdES.detached` — PAdES-E-BES and PAdES-E-EPES — whose attributes are ETSI EN 319 122-2's rather than this standard's. Stays `reported`: this is question 3 — a trust store, a certification path and CRLs or OCSP responses obtained over a network — and this program answers questions 1 and 2 only, which it says out loud on every signed document it opens (ADR 0215, ADR 0229). Neither of those being answered changes anything here, and pretending otherwise is the failure both of those rounds were shaped to avoid: a signature that verifies under a certificate the file supplied is a signature nobody has checked the revocation of. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.5"
title = "Requirements for validation of PAdES signatures"
status = "partial"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/x509.rs", "crates/pdf-model/src/pkcs1.rs", "crates/pdf-model/src/dsa.rs", "crates/pdf-model/src/bigint.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_it_verifies", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "What validating a PAdES signature requires, in four steps, and **all four need a certificate**: (a) compare the signer's certificate against the signing-certificate attribute's hash and \"use the public key contained in the signer's certificate to verify that the document digest found in the signature is correctly signed\"; (b) validate the certification path per RFC 5280; (c) validate the timestamping unit's path; (d) check revocation. **Half of (a) is done and the row moved to `partial` in the three-hundred-and-ninety-second session** (ADR 0229). The *document digest* it names is recomputed — question 1 — and whether it is \"correctly signed\" is now checked as well, for an RSA key and — since the four-hundred-and-seventy-ninth session (ADR 0314) — a DSA one: `Signature::authenticity` uses \"the public key contained in the signer's certificate\" exactly as the step says. What is *not* done is the other half of (a) — comparing that certificate against the signing-certificate attribute's hash, which is ETSI EN 319 122-1's and RFC 5035's attribute and not one this tree holds a definition of — and (b), (c) and (d) entire, which are certification paths and a network. The clause's own NOTE is worth keeping beside that: \"[t]he claimed signing time specified by the signature dictionary value with the key M is not a trusted indication of the signing time\", which is why `Signature::signed_at` is presented as the signer's claim. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.6"
title = "Signature revocation checking model for PAdES signatures"
status = "reported"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "The revocation model: “[a] signature handler shall use either, or both, of the following methods to check the revocation status of every certificate belonging to a certification path” — CRLs from a certification authority, or OCSP responses from a network server. Stays `reported`: this is question 3 — a trust store, a certification path and CRLs or OCSP responses obtained over a network — and this program answers questions 1 and 2 only, which it says out loud on every signed document it opens (ADR 0215, ADR 0229). Neither of those being answered changes anything here, and pretending otherwise is the failure both of those rounds were shaped to avoid: a signature that verifies under a certificate the file supplied is a signature nobody has checked the revocation of. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.7"
title = "Revocation checking PAdES signatures at the current time"
status = "reported"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "Revocation checking at the current UTC time, with the CRL and OCSP freshness rules that go with it. Stays `reported`: this is question 3 — a trust store, a certification path and CRLs or OCSP responses obtained over a network — and this program answers questions 1 and 2 only, which it says out loud on every signed document it opens (ADR 0215, ADR 0229). Neither of those being answered changes anything here, and pretending otherwise is the failure both of those rounds were shaped to avoid: a signature that verifies under a certificate the file supplied is a signature nobody has checked the revocation of. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.3.4.8"
title = "Revocation checking PAdES signatures at a time in the past"
status = "reported"
code = ["crates/pdf-model/src/der.rs", "crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed", "crates/pdf-model/src/cms.rs::a_detached_signature_states_its_algorithm_and_its_message_digest", "crates/pdf-model/src/signature.rs::a_signed_document_says_whether_its_signed_bytes_moved"]
note = "Revocation checking at a time in the past, which is what a signature timestamp attribute and a document security store exist for. Stays `reported`: this is question 3 — a trust store, a certification path and CRLs or OCSP responses obtained over a network — and this program answers questions 1 and 2 only, which it says out loud on every signed document it opens (ADR 0215, ADR 0229). Neither of those being answered changes anything here, and pretending otherwise is the failure both of those rounds were shaped to avoid: a signature that verifies under a certificate the file supplied is a signature nobody has checked the revocation of. A signature's *appearance* is an ordinary widget annotation drawn by §12.5 like any other, so a signed document renders correctly and completely whatever this row says. §7.6.2's exception is implemented one clause over — and was **wrong until the three-hundred-and-seventy-seventh session** for a signature dictionary that states no `/Type`, which Table 255 permits: `issue17069.pdf` is encrypted, carries a signature, and had its whole signature value destroyed by the cipher. The disjointness this row used to record — eight signed documents, twenty-six encrypted, no overlap — was therefore false; that document is both."

[[clause]]
clause = "12.8.4"
title = "Long term validation of signatures"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "Long-term validation, counted rather than parsed. What §12.8.4 is *for* is making a signature checkable after its certificate expires, and what it stores is X.509 material: certificates, \"an array of all Certificate Revocation Lists (CRL) (see Internet RFC 5280 )\" and OCSP responses (RFC 6960). `SecurityStore` says how many of each the document carries and how many signatures have validation information — which is the one thing a program that cannot validate can usefully answer: **whether the document carries what a validator would need**. **This row used to end \"[p]arsing any of it is the trust decision §12.8 refuses\", and that was two claims in one**: since the three-hundred-and-ninety-second session `pdf_model::x509` parses a certificate, and what §12.8 still refuses is what a validator would *do* with these — build a certification path, check a revocation list, decide whom to believe. The counts stay counts because reading the bytes was never the hard half. **Read and kept in the five-hundred-and-fifty-third**: `SecurityStore` is still three counts and a fourth of the signatures carrying validation information, and nothing in the tree asks what any of the counted objects contains."

[[clause]]
clause = "12.8.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "The framing, and the reason the counts matter: validation material is stored so that a signature outlives its certificate. Counted, not parsed. **Read and kept in the five-hundred-and-tenth session**: `security_store` counts `/Certs`, `/CRLs`, `/OCSPs` and `/VRI`, and `Signature::authenticity` takes its certificates from the CMS `SignedData` rather than from the DSS - counted, not parsed, still exact."

[[clause]]
clause = "12.8.4.2"
title = "Introduction to the document security store (DSS)"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "The introduction to the DSS, whose entries are §12.8.4.3's and are read there. **This note named nothing owed while the row was `partial`, from the three-hundred-and-seventy-seventh session to the four-hundred-and-thirty-seventh**, which is what the fourteenth sweep prints; the clause's own `shall`s say what it is: they are addressed to a *validator*, and each names a step this tree does not take. \"[T]he DSS dictionary shall be used to collect the certificates, CRLs and OCSP responses that are relevant to validate that signature\", and where a timestamp token is present and valid \"the UTC time included in that timestamp token shall be used as the time reference to check the revocation status of the signer's certificate and of all the intermediate CA certificates, up to a trusted root\". That is §12.8's question 3, which needs a trust store and a clock, and what this tree does instead is count the material and say so."

[[clause]]
clause = "12.8.4.3"
title = "Document Security Store (DSS)"
status = "implemented"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "Table 261 whole: `/Certs`, `/CRLs`, `/OCSPs` and `/VRI`, each counted. The `/VRI` keys are \"the base-16-encoded (uppercase) SHA-1 digest of the signature to which it applies\", which this reader does not compute — it counts the entries, since what a caller can act on is how many signatures somebody has already validated."

[[clause]]
clause = "12.8.4.4"
title = "Validation-related information (VRI)"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "Table 262's VRI dictionaries, counted per signature. The clause's own sentence is why the count means something rather than nothing: \"[a] signature VRI dictionary shall not be used to record the information used in an unsuccessful validation attempt\", so an entry is a record of a *successful* validation by somebody. The certificates it selects are the DSS's. **This row said \"reading them is X.509\" as though that were a refusal, and since the three-hundred-and-ninety-second session this tree reads X.509** — what it does not do with a VRI's certificates is the validation they were stored for, which is question 3 and needs a trust store. **Read and kept in the five-hundred-and-fifty-third**: the count comes off the DSS's own `/VRI` dictionary and no reader takes a value out of one."

[[clause]]
clause = "12.8.4.5"
title = "Usage of the DSS VRI"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "How a validator uses the VRI, which needs the validator. What is read is that the entries exist and for how many signatures. **Read and kept in the five-hundred-and-tenth session**: `SecurityStore::validated_signatures` counts the VRI's per-signature keys and no validator arrived to use them."

[[clause]]
clause = "12.8.5"
title = "Document timestamp (DTS) dictionary"
status = "partial"
code = ["crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/src/signature.rs::a_document_timestamp_is_checked_against_its_message_imprint", "crates/pdf-model/tests/signatures.rs::every_corpus_signature_is_asked_whether_its_document_changed"]
note = "A document timestamp \"is a standard signature dictionary as described in [Table 255]\", so it is read by the same code and told apart by its `/Type`: `Signature::timestamp`. Its `/ByteRange` \"shall cover the entire PDF file\", which `Signature::coverage` checks. **And the token is parsed since the three-hundred-and-seventy-seventh session**, as far as the one thing Table 255 says it commits to: \"[t]he value of the messageImprint field within the TimeStampToken shall be a hash of the bytes of the document indicated by the ByteRange\". `SignedData::timestamp_imprint` reads RFC 3161's `TSTInfo` out of the encapsulated content and `Signature::integrity` compares that imprint with the recomputed digest — so a document timestamp answers question 1 exactly as a signature does. What it cannot answer is whether the timestamp authority is one to believe, which is question 3. **No corpus document carries a document timestamp**, so the witness is a fixture (trap 8)."

[[clause]]
clause = "12.8.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it"]
note = "The framing — \"[a] document timestamp dictionary establishes the exact contents of the complete PDF file at the time indicated in the timestamp token\" — and the reading of it is Table 255's, with `/Type /DocTimeStamp` as the distinction."

[[clause]]
clause = "12.8.5.2"
title = "Initial document timestamp dictionary"
status = "partial"
code = ["crates/pdf-model/src/cms.rs", "crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/src/signature.rs::a_document_timestamp_is_checked_against_its_message_imprint"]
note = "The initial timestamp: Table 255's dictionary with a token from \"a trusted timestamp authority\", whose byte range \"shall cover the entire PDF file, including the signature dictionary but excluding the Contents value\" — read and checked, and since the three-hundred-and-seventy-seventh session the *imprint inside the token* is checked against it too — with, since the three-hundred-and-ninety-second, the token's own signature verified under the key in the certificate it carries wherever that key is RSA (ADR 0229). No corpus document carries a document timestamp, so both are exercised by a fixture. The clause also says how a processor finds them, and this reader follows it: \"[t]he existence of one or more document timestamps shall be determined by examining signature fields\", which is the same walk `signatures` does. `partial` for the word *trusted*: whether the authority that issued the token is one to believe needs a certificate and a trust store, which is question 3 (ADR 0215). **Read and kept in the five-hundred-and-fifty-third**: `timestamp_imprint` is still what the byte range is checked against, and the word *trusted* is still the whole of what is owed."

[[clause]]
clause = "12.8.5.3"
title = "Subsequent document timestamp dictionaries"
status = "partial"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it"]
note = "A later timestamp applied before the previous one's certificate expires, with the material to prove the chain in the DSS — which §12.8.4.3's row counts. Read as another timestamp signature; the *reasoning* about expiry is a validator's. **Read and kept in the five-hundred-and-tenth session**: the parent row carries the shared reader - `Signature::timestamp`, `SignedData::timestamp_imprint` - and nothing here reasons about a certificate's expiry, which is question 3's validator."

[[clause]]
clause = "12.8.6"
title = "Permissions"
status = "implemented"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_certification_signature_states_what_may_change_after_it", "crates/pdf-model/tests/signatures.rs::every_signed_corpus_document_says_what_it_signed"]
note = "Table 263's permissions dictionary: `/DocMDP`, whose `/P` level is read through the signature's `/Reference`, and `/UR3`, whose Table 258 rights are read through the same `/Reference` walk since the hundred-and-ninety-eighth session. **The clause's composition rule is implemented since the three-hundred-and-seventy-third** — \"[f]or a permission to be actually granted for a document, it shall be allowed by each permission handler that is present in the permissions dictionary as well as by the security handler\" — which is why `restriction::asserted` answers with *every* restriction that applies rather than with the first: this dictionary and §7.6.4.2's Table 22 are two handlers, and a person deciding whether to go ahead is owed both reasons (ADR 0212). **This row said such a feature did not exist here and it had since the hundred-and-thirty-fifth**: editing a field and saving the file are two of them, and §12.8.2.3 now names the obligation that follows and how `ViewState::save` meets it. The *granting* half remains out of reach — a usage rights signature enables \"features of a PDF processor that are not available by default\", which is a permission about a *program*, and this one gates nothing. `/DocMDP` is no longer read as a statement only — §12.8.2.2.1's parenthesis makes `/Perms /DocMDP` binding on a processor that modifies, and `ViewState::set_field` obeys it. §12.8.6 makes permissions independent of encryption — they are \"similar to those defined by security handlers … but do not require that the document be encrypted\" — and this reader answers them the same way it answers §7.6's: by saying what the document states. 4 corpus documents state a `/Perms`, all four with `/UR3` and one with a `/DocMDP`."

[[clause]]
clause = "12.8.7"
title = "Legal content attestations"
status = "implemented"
code = ["crates/pdf-model/src/signature.rs"]
test = ["crates/pdf-model/src/signature.rs::a_documents_own_declaration_can_be_held_against_it"]
note = "Table 264's legal attestation dictionary, read — and **checked against the document**. The clause exists because \"[t]he PDF language provides a number of capabilities that can make the rendered appearance of a PDF document vary\", and it asks an author certifying a document to declare how many of them are in it. Ten of those counts are things this reader can count itself, so `Legal::disagreements` names every entry where the author's number and the object graph's differ. It is a question rather than a verdict, and the note says why: the clause states no counting algorithm, and a producer that counts one shared action once where this counts two references is not lying. `/Attestation` is kept as the author's own words. No corpus document states a `/Legal`."

[[clause]]
clause = "12.9"
title = "Measurement properties"
status = "partial"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::the_last_viewport_containing_a_point_is_the_one_that_applies", "crates/pdf-model/src/measurement.rs::the_clauses_own_example_formats_the_string_it_states", "crates/pdf-model/tests/measurement.rs::the_corpus_states_one_viewport_and_it_reads"]
note = "Table 265's viewports and Table 266's measure dictionaries, read whole: a page's `/VP` array, each viewport's `/BBox`, `/Name` and `/Measure`, Table 267's rectilinear entries with their number format arrays, and Table 268's ten entries with the defaults the table states. The rule worth naming is the *selection*: viewports may overlap, the array is \"in drawing order\", and the clause searches it backwards — \"starting with the last one and iterating in reverse, and the first one whose `BBox` entry contains the point shall be chosen\" — so `Viewports::at` iterates in reverse, and a forward search would answer with the background scale inside every inset. A `/BBox` is kept exactly as stated rather than normalised, because Table 265 makes the corner order mean something: it \"shall determine the orientation of the measuring coordinate system\". `partial` because nothing *measures*: the clause exists for a person dragging between two points, and this viewer has no tools — what it can do is the arithmetic, which §12.9.2's row holds. **Read and kept in the five-hundred-and-first session**: `Viewports::at` iterates `.rev()` as the note says, and no crate under `viewer-*` names `Viewports` or `measurement`, so the capability reason still holds — checked by grep, not believed."

[[clause]]
clause = "12.9.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::the_last_viewport_containing_a_point_is_the_one_that_applies", "crates/pdf-model/tests/measurement.rs::the_corpus_states_one_viewport_and_it_reads"]
note = "The framing, and its load-bearing sentence is about a viewer rather than a page: users of such documents \"require information about the scale and units of measurement of the corresponding real-world objects and their relationship to units in PDF user space\". The data a measurement needs is read; the tool that would take two points from a person is not built, which is what keeps this `partial`. Read and kept in the five-hundred-and-first session, on §12.9's own grep."

[[clause]]
clause = "12.9.2"
title = "Algorithm: Use of a number format array to create a formatted text string"
status = "implemented"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::the_clauses_own_example_formats_the_string_it_states"]
note = "The five steps, in `measurement::format`. A number format array is units in descending granularity and the algorithm carries the *fractional remainder* from each into the next — step e), \"Multiply its C entry by the fractional result from the previous step\" — stopping at step c) when nothing is left over or at step d) when the units run out. Table 268's formatting entries all take part: `/RT` between orders of thousands, `/RD` as the decimal point, `/F` choosing decimal, fraction, round or truncate, `/D` as precision or denominator with its two different defaults, `/FD` keeping a denominator unreduced, and `/PS`, `/SS` and `/O` placing the label. **The clause's own worked example is the test**: 1.4505 miles through the example's own three dictionaries is `1 mi 2,378 ft 7 5/8 in`, and every part of the algorithm is load-bearing in that one string. No corpus document exercises it, which is trap 8 and is why the example matters."

[[clause]]
clause = "12.10"
title = "Geospatial features"
status = "partial"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::a_geospatial_measure_carries_the_earth_model_and_not_a_scale", "crates/pdf-model/tests/measurement.rs::the_corpus_states_one_viewport_and_it_reads"]
note = "Geospatial features, read as data. Table 269's geospatial measure dictionary, Tables 270 and 271's coordinate systems and Table 272's point data are all read; what is not done is the *transformation*, and the reason is that the standard states it by reference to two things outside itself — an EPSG reference code, whose database is \"administered by the International Association of Oil and Gas Producers (OGP)\", and a Well Known Text string, whose format \"is specified in ISO 19162\". Turning a page coordinate into a latitude means a geodesy library and that registry; guessing would produce coordinates that look right and are somewhere else. The one corpus witness — `bug1146106.pdf` — is a `GEO` viewport with a geographic system stated as 145 characters of WKT and four `/GPTS`–`/LPTS` registration points. Read and kept in the five-hundred-and-first session: every table the note lists has its reader in `measurement.rs`, checked key by key, and the boundary is still the registry's."

[[clause]]
clause = "12.10.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::a_geospatial_measure_carries_the_earth_model_and_not_a_scale"]
note = "The framing: PDF \"is a common delivery mechanism for map and satellite imagery data\", and PDF 2.0 adds a geospatial coordinate system to carry it. Every dictionary the clause introduces is read; the projection is not, for the reason §12.10's row gives. Read and kept in the five-hundred-and-first session, on that row's own check."

[[clause]]
clause = "12.10.2"
title = "Geospatial measure dictionary"
status = "partial"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::a_geospatial_measure_carries_the_earth_model_and_not_a_scale"]
note = "Table 269 entire: `/Bounds` with the table's stated default of the whole unit square, `/GCS`, `/DCS`, `/PDU`, `/GPTS`, `/LPTS` and `/PCSM`. Two of the table's sentences are answered rather than stored: `Geospatial::registration` pairs `/GPTS` with `/LPTS`, which the table requires to hold \"the same number of number pairs as the GPTS array\", and `Geospatial::matrix_has_priority` is the table's own precedence — `/PCSM` \"has priority over GPTS\" where it is present, but \"should be ignored\" when `/GCS` is geographic, so the answer needs both entries. `partial` because a registration is not a projection: with a projected `/GCS` the points are eastings and northings, and converting them needs the EPSG registry and ISO 19162 grammar that §12.10.3's *entries point at* — outside the standard and outside this tree, which is §12.10's own reason. **This sentence said \"needs §12.10.3's external references\" until the five-hundred-and-first session**, and §12.10.3's own row is `implemented` — a blocker that names a clause as the route to an external thing reads as expired the day the clause's row settles, which is the committed blockers sweep's third noise shape and the reason the wait is now written against the registry rather than the clause."

[[clause]]
clause = "12.10.3"
title = "Geographic coordinate system dictionary"
status = "implemented"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::a_geospatial_measure_carries_the_earth_model_and_not_a_scale"]
note = "Table 270's three entries. `/EPSG` and `/WKT` exclude each other — each \"shall not be present if\" the other is — and §12.10.3's closing sentence requires one of them, which is `CoordinateSystem::is_stated`. The `/Type` is what tells a geographic system from §12.10.4's projected one, and both are one Rust type because the two tables hold the same entries and differ in meaning. The WKT string is kept and not parsed: its format is ISO 19162's, which is a different standard."

[[clause]]
clause = "12.10.4"
title = "Projected coordinate system dictionary"
status = "implemented"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::a_geospatial_measure_carries_the_earth_model_and_not_a_scale"]
note = "Table 271's three entries, read by the same code as Table 270's for the reason §12.10.3's row gives — the tables are identical in shape. What a projected system *means* is different and is recorded: its `/GPTS` \"contains values in a planar projected coordinate space as eastings and northings\" rather than degrees, and `CoordinateSystem::projected` is what a caller asks to find out which."

[[clause]]
clause = "12.10.5"
title = "Point data dictionary"
status = "implemented"
code = ["crates/pdf-model/src/measurement.rs"]
test = ["crates/pdf-model/src/measurement.rs::a_geospatial_measure_carries_the_earth_model_and_not_a_scale"]
note = "Table 272's point data, from either spelling of `/PtData` — \"a point data dictionary or an array of point data dictionaries\". `/Names` are, in the clause's words, \"in effect, column headers for the array of XPTS values\", so this is read as a table of named columns and tuples. The values are kept as objects rather than as numbers: only `LAT`, `LON` and `ALT` are predefined, and a name the standard does not define carries a type it does not define either."

[[clause]]
clause = "12.11"
title = "Document requirements"
status = "partial"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty"]
note = "The catalog's `/Requirements`: a document naming \"feature(s) of PDF beyond those commonly expected … required for correct handling\". Read in full — Table 273's `/S`, `/V` and `/Penalty`, Table 275's **twenty-five** types, Table 276's handlers — and `requirements::unmet` names each one this program cannot meet with the reason. **This row and §12.11.2's said twenty-four until the three-hundred-and-seventy-fifth session**, which is a number nobody had counted against either the table or the `match` that reads it; both have twenty-five. What is `partial` is the *acting* on it: §12.11.6 says processing \"shall not continue\" when the requirements cannot be met, and this program draws the document and says what it could not promise. See §12.11.6 for why. **0 of the 974 corpus documents state one.**"

[[clause]]
clause = "12.11.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty"]
note = "Table 273's common entries, with the defaults the clause states: `/Penalty` is 100 unless the file says otherwise, and an absent `/V` means the requirement is judged \"without regard to version number\". A dictionary with no `/S` states no requirement and is dropped, since the entry is required and is the whole content of the statement. **The fifth of Table 273's entries is `/RH`, and this note enumerated the other four and walked past it until the five-hundred-and-seventy-ninth session**, which `doc/todo/01`'s fifteenth sweep printed under a row claiming `implemented`. It is unread, and the requirement it carries is met by construction rather than skipped: the entry names \"a requirement handler that shall be disabled (not invoked) if the interactive PDF processor can check the requirement specified in the S entry\", §12.11.5 says such a handler is an ECMAScript — \"[t]raditionally, requirements handling has been accomplished with an ECMAScript segment\" — and `CLAUDE.md` excludes ECMAScript, so every handler a file could name is disabled here whatever the file says. The condition on that `shall` is worth stating rather than assuming, because it *fires*: `Kind::unmet` answers all twenty-five of Table 275's types, so this processor can check the `/S` entry and does owe the disabling. §12.11.5's row carries the same reading and has since it was written; what was missing was this row's own enumeration, which is `doc/todo/01`'s fifth failure shape between two neighbours rather than between a parent and a child. Table 274's `/Encrypt` and `/DigSig` are the two type-specific entries and neither is read, for two reasons rather than one. `/DigSig` belongs to a requirement declared unmet by type alone, so its parameters change nothing. **`/Encrypt` does not, and this sentence said it did until the three-hundred-and-seventy-fifth session**: `Kind::Encryption` answers *met*, and Table 275 makes that a claim about \"the specific set of encryption parameters that are specified by the encryption dictionary provided as the value of the Encrypt key\" — a set this program answers by type instead. What makes the answer sound rather than lucky is §7.6.4.2: every `/V` and `/R` the standard defines is implemented, and the one value refused is `/R` 5, which Table 21 itself calls \"Shall not be used\". Reading `/Encrypt` would buy the distinction between a document that needs `/R` 5 and one that needs anything else, and no corpus document states a requirement at all. **Table 273's `/S` row sends a reader to the wrong table for its own valid values, and Errata Collection 3 corrects it** (Issue #187, `/State` `Review` `Completed`): the row says \"See\" Table 276 \"for valid values\", which is the requirement *handler* dictionary, and the amended row says Table 275, which is the requirement types. `requirements::Kind::read` matches Table 275's names and has since it was written, so the erratum vindicates the code; the same erratum retitles Table 274 \"Additional entries for specific types of requirements\" and rewrites the sentence above it to name Table 273 as the table the two type-specific keys are additional to. Neither moves a requirement this tree answers."

[[clause]]
clause = "12.11.2"
title = "Requirement types"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty", "crates/pdf-model/src/requirements.rs::a_collection_is_met_and_editing_one_is_not", "crates/pdf-model/src/requirements.rs::no_reason_claims_a_clause_is_unread"]
note = "Table 275's twenty-five types, each an arm of `requirements::Kind`, and a name outside the table kept as `Kind::Other` because \"[a]dditional requirement types, including ones identifying vendor-specific features, may be registered\". `Kind::unmet` is this program's own answer per type — **seven met, eighteen not**, each with its reason — and it is **a claim about this tree rather than about the standard**, so it decays exactly as a ledger row does: a session that builds a layer panel has to come back and change `OCInteract`. **It has now decayed twice and this row with it.** The two-hundred-and-twenty-first session made `OCInteract`, `AcroFormInteract` and `Attachment` met and left this row saying \"three met, twenty-one not\" for a hundred and fifty-four sessions; the three-hundred-and-seventy-fifth found that and nine reasons besides. `Collection` is the seventh met — Table 275 asks for the collection's files \"along with any associated metadata\" and \"that the user can extract or otherwise view the contents of each item\", which are `Query::Collection` drawn by `viewer_ui::chrome` (ADR 0202) and `Command::Extract` — and the other eight reasons were corrected rather than met. **Every one of the eight named a clause as unread that this tree reads**: \"§12.8 is unread\" with §12.8 read since the ninety-eighth session, \"§12.10 is unread\" with every dictionary of it read, \"§14.12 is unread\" with `GoToDp` navigating parts, and `SeparationSimulation`'s \"§10.8 describes a marking device\" — the phrase ADR 0204 retired from `CLAUDE.md` because ISO 32000-2 does not contain it — beside an `implemented` §10.8.2. A reason names the missing capability now, and `no_reason_claims_a_clause_is_unread` is the gate on the shape."

[[clause]]
clause = "12.11.3"
title = "Requirement penalty values"
status = "partial"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty"]
note = "`/Penalty` is read and clamped to the clause's [0, 100], with its two ends' meanings recorded: 0 is \"although the document uses this feature the need is optional\" and 100 is \"this document will not produce the author's intent unless the PDF processor can fully support this feature\". **The clause states no threshold** — intermediate values \"are available to weight the value of this feature among other features\" and against \"other documents in the choosing process\" — so there is no computation here to complete, and weighing them is left to a caller that has something to choose between."

[[clause]]
clause = "12.11.4"
title = "Requirement versions"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty"]
note = "`/V` in both forms: a name that \"specifies a version number, represented as two or more decimal integers separated by a period\", kept as the name for the reason the clause's NOTE gives; and an extensions dictionary, from which the registered prefix is kept, since the version it states belongs to a developer's extension rather than to PDF."

[[clause]]
clause = "12.11.5"
title = "Requirement handlers"
status = "partial"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty"]
note = "Table 276's alternative requirement handlers, whose whole purpose is to disable an ECMAScript that would otherwise check the same requirement — \"[t]raditionally, requirements handling has been accomplished with an ECMAScript segment\". This program runs no ECMAScript (principle 5), so there is nothing to disable and the `/RH` entry is read by nobody: the clause's `NoOp` handler type describes what this reader does with every requirement it cannot meet. Re-read off the blame list: no source quotes `\"RH\"`, so the sentence still holds."

[[clause]]
clause = "12.11.6"
title = "Requirements processing"
status = "partial"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::a_requirement_states_a_type_a_version_and_a_penalty"]
note = "The processing rule, and the one place this program departs from the clause on purpose. §12.11.6 says that when requirements cannot be met \"then the processing of the document shall not continue\"; this program draws the document and prints what it could not promise. Two reasons, and the first is the clause's own: the computation it refers to is §12.11.3's penalty, for which the standard states no threshold, so \"cannot be met\" is never made decidable. The second is a choice — refusing to open a file a person asked for is a worse failure for a viewer than showing it with its limits named. The clause's own next sentence is what makes that safe: \"[i]f the reader encounters an unsupported feature (whether or not that feature was declared as a requirement), it shall take the normal fallback actions\", and NOTE 1 says \"there is no formal connection between the requirement type and the operation of the associated feature(s)\" — so every feature is still reported where it is used."

[[clause]]
clause = "13.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2"
title = "Multimedia"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.2"
title = "Viability"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.3"
title = "Renditions"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.3.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.3.2"
title = "Media renditions"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.3.3"
title = "Selector renditions"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.4"
title = "Media clip objects"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.4.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.4.2"
title = "Media clip data"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.4.3"
title = "Media clip section"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.5"
title = "Media play parameters"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.6"
title = "Media screen parameters"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.6.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.6.2"
title = "Media offset dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.6.3"
title = "Timespan dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7"
title = "Other multimedia objects"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.2"
title = "Media players dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.2.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.2.2"
title = "Algorithm: Media Player"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.3"
title = "Media player info dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.4"
title = "Software identifier dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.4.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.4.2"
title = "Algorithm: software identifier"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.4.3"
title = "Software URIs"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.4.4"
title = "Version arrays"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.2.7.5"
title = "Monitor specifier"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.3"
title = "Sounds"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.4"
title = "Movies"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.5"
title = "Alternate presentations"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6"
title = "3D Artwork"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.2"
title = "3D annotations"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.3"
title = "3D streams"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.3.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.3.2"
title = "3D animation style dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.3.3"
title = "3D reference dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4"
title = "3D views"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.2"
title = "Projection dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.3"
title = "3D background dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.4"
title = "3D render mode dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.5"
title = "3D lighting scheme dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.6"
title = "3D cross section dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.4.7"
title = "3D node dictionaries"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.5"
title = "Coordinate systems for 3D"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.6"
title = "3D markup"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7"
title = "Persistence of 3D measurements and markups"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.2"
title = "The 3D units dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3"
title = "3D measurement/markup dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3.2"
title = "3D linear dimension measurement"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3.3"
title = "3D perpendicular dimension measurement"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3.4"
title = "3D angular dimension measurement"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3.5"
title = "3D radial dimension"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.3.6"
title = "3D comment note"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.6.7.4"
title = "3D measurements and projection annotations"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7"
title = "Rich media"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2"
title = "RichMedia annotations"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2"
title = "RichMediaSettings dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2.2"
title = "RichMediaActivation dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2.3"
title = "RichMediaDeactivation dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2.4"
title = "RichMediaAnimation dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2.5"
title = "RichMediaPresentation dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.2.6"
title = "RichMediaWindow dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3"
title = "RichMediaContent dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.2"
title = "The assets name tree"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.3"
title = "RichMediaConfiguration dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.4"
title = "RichMediaInstance dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.5"
title = "View dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.6"
title = "View params dictionary"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.6.1"
title = "General"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.6.2"
title = "Saving state data"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "13.7.2.3.6.3"
title = "Loading state data"
status = "out-of-scope"
exclusion = "clause-13-multimedia"

[[clause]]
clause = "14.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::an_output_intent_says_what_the_documents_device_colours_mean", "crates/pdf-model/tests/page_geometry.rs::the_five_page_boundaries_default_and_are_clipped_to_the_medium"]
note = "The clause's own opening sentence is the one that decides how much of it this project owes: \"The features described in this clause do not affect the final appearance of a document.\" That is true of most of it and *false* of the two parts this tree implements, which is why they are implemented — §14.11.5's output intents supply a `DeviceCMYK` destination profile that changes every pixel a CMYK fill covers, and §14.11.2's page boundaries decide what is drawn at all. Everything else here is interchange: metadata, logical structure, tagged PDF, web capture, document parts and associated files. `CLAUDE.md` puts clause 14 in scope \"as far as accessibility needs it\", which is §14.7 and §14.8 rather than these rows."

[[clause]]
clause = "14.2"
title = "Procedure sets"
status = "inapplicable"
note = "Procedure sets, deprecated since PDF 1.4 and stated here only to say what `/ProcSet` was: names of PostScript language procedure sets that \"shall be used only when the content stream is printed to a PostScript language compatible output device\". This is a display, so the entry cannot decide anything — and §7.8.3's resource dictionary row already records that it is read past. Table 346's five names are not consulted; a content stream's operators are implemented by this program, not by a resource it names."

[[clause]]
clause = "14.3"
title = "Metadata"
status = "implemented"
code = ["crates/pdf-model/src/metadata.rs", "crates/pdf-model/src/xmp.rs", "crates/pdf-syntax/src/document.rs"]
test = ["crates/pdf-model/src/metadata.rs::table_349_is_read_and_its_two_prose_rules_hold", "crates/pdf-model/tests/xmp.rs::every_metadata_stream_in_the_corpus_is_read"]
note = "Metadata: information about the document as against its content. Nothing here reaches a page, and all three of the subclauses that were `inapplicable` for it have stopped being so: §14.3.3's information dictionary is read and shown since the hundred-and-seventy-third session, because the properties panel this row said did not exist now does. §14.3.2's XMP is read since the two-hundred-and-ninety-fourth, when the dependency decision its row had been naming for a hundred sessions was taken (ADR 0186), and §14.3.4 stopped being `inapplicable` in the same round — its reason was two expired capability claims and the sweep over this round's own noun is what found it. The one place metadata was never inapplicable is §14.3.2's `/EncryptMetadata`, which is Table 21's and belongs to §7.6. So all four children are settled and this row is too — it was `partial` for four rounds after the last of them closed, which is the shape the two-hundred-and-ninety-eighth session went looking for."

[[clause]]
clause = "14.3.1"
title = "General"
status = "inapplicable"
note = "The distinction between document-level and object-level metadata, and nothing else. Both are interchange."

[[clause]]
clause = "14.3.2"
title = "Metadata streams"
status = "implemented"
code = ["crates/pdf-model/src/xmp.rs", "crates/pdf-syntax/src/document.rs", "crates/viewer-core/src/viewer.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/tests/xmp.rs::every_metadata_stream_in_the_corpus_is_read", "crates/pdf-model/src/xmp.rs::a_packet_states_its_properties_in_three_shapes_and_all_three_are_read", "crates/pdf-syntax/tests/encryption.rs::a_metadata_stream_is_in_the_clear_when_encrypt_metadata_is_false"]
note = "**Was `partial` for a hundred sessions and the blocker was a decision rather than a clause; ADR 0186 took it in the two-hundred-and-ninety-fourth session.** The clause has two requirements and both are met. Table 347's `/Type /Metadata` and `/Subtype /XML` bind a writer, and all 319 corpus streams state both correctly, so a reader that refused a packet over them would be inventing a rule; `Xmp::read` reads the stream whatever the dictionary calls it. And \"[t]he contents of a metadata stream shall be the metadata represented in Extensible Markup Language (XML) and the grammar of the XML representing the metadata shall be defined according to the extensible metadata platform specification (ISO 16684-1)\" is `pdf_model::xmp`, over `xmlparser` — a pull tokenizer with no dependencies that resolves no entity and opens no file, so the billion-laughs expansion and the external entity are structurally absent rather than defended against. Both spellings of a simple property are read, all three container forms, and a name is resolved to its namespace URI rather than compared as a prefix. A *structured* value (ISO 16684-1 section 7.6) is recorded as present and uninterpreted, which nothing in clause 12 or 14 asks for. Measured over the corpus: 319 documents carry a stream, 318 read, 3191 properties between them, and the one refusal is a fuzzed file whose stream does not decode at all. The clause's interaction with clause 7 was already implemented and stays: Table 21's `/EncryptMetadata false` leaves this stream unencrypted in an otherwise encrypted document. A stream reaches a host through `Query::Properties`, whose answer carries the packet itself — `None` for a document that names none, `Err` for one this reader refused, and the two are different sentences in the properties tab. The other half of the clause is the *object-level* metadata Table 348 puts on any stream or dictionary; `Xmp::read` takes a dictionary rather than a document for exactly that reason, and no clause in scope reads one yet."

[[clause]]
clause = "14.3.3"
title = "Document information dictionary"
status = "implemented"
code = ["crates/pdf-model/src/metadata.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/src/metadata.rs::table_349_is_read_and_its_two_prose_rules_hold", "crates/viewer-core/tests/headless.rs::a_document_hands_over_what_it_says_about_itself"]
note = "Table 349, whole, from the trailer's `/Info`. **This row was `inapplicable` on the reason \"a viewer with a document-properties panel would read it; this one has no panel\", and the panel arrived in the hundred-and-sixty-sixth session** — an `inapplicable` row decays exactly as a `silent` one does, and this is the second instance after §12.7.4.2's field names. All nine entries are read and both of the clause's prose rules hold: `/Trapped` is a **name and not a boolean** — \"[t]his shall be the name True, not the boolean value true\", said once per value — with Table 349's stated `Unknown` default, and \"the value associated with any such key shall be a text string\", so a `/Title` that is not a string has not stated a title. Deprecated in PDF 2.0 for every entry but the two dates, and read anyway, on the argument §12.2's deprecated `/ViewArea` and §8.6.5.1's withdrawn `CalCMYK` are read on: deprecation binds a writer. The dates keep the file's own bytes beside the §7.9.4 parse, which is §7.11.4's and §12.8's pairing and for the same reason. Measured: 510 of 964 corpus documents state an `/Info` — 474 a `/Producer`, 468 a `/CreationDate`, 150 a `/Title`, 49 a `/Trapped`. The clause's last sentence — \"[d]ocument information dictionaries are also used with Threads\" — is why `Information::in_dictionary` is public. **45 of the 492 documents whose `/Info` this panel shows state a value §9.6.2.2's fourteen cannot set**, four of them wholly — Thai and Japanese — and until the three-hundred-and-sixteenth session those rows were drawn empty, which told a person the document says nothing there. A box per character now (ADR 0195), measured by `examples/chrome_coverage` and carried by `doc/todo/27`. **And most of that was never a coverage gap at all**: the panel asked the face for a character *code*, which §9.6.5 makes 256 wide, so an `/Info` value with an accented Latin letter in it was boxed by a face that states one. Asked by character instead, 41 of those 46 documents' `/Info` sets completely and 5 do not, over 106 characters of scripts the binary does not carry and of Table D.3's undefined code point. `pdf-model --example interface_font_census`, ADR 0326."

[[clause]]
clause = "14.3.4"
title = "Reconciling two sources of document metadata"
status = "implemented"
code = ["crates/pdf-syntax/src/write.rs", "crates/pdf-model/src/metadata.rs", "crates/viewer-ui/src/chrome.rs"]
test = ["crates/pdf-model/tests/saving.rs::saving_leaves_both_of_a_documents_metadata_sources_exactly_as_they_were"]
note = "**Was `inapplicable` on the reason \"a question for a program that *writes* or *displays* metadata, and this one does neither\", which was false twice and had been for a hundred and twenty sessions**: this program displays metadata since the hundred-and-seventy-third (§14.3.3's tab) and writes since the hundred-and-thirty-sixth (§7.5.6's update). `doc/todo/01`'s fourth sweep found it in the two-hundred-and-ninety-fourth, run over the noun `XMP` in the round that gave the tree an XMP reader — and the row would have gone on looking true, because both halves of its reason were about capabilities and neither clause cites the other. The clause states four rules for a writer and one sentence for a reader. **The second rule is the only one that binds this program and it is met by construction**: \"[w]hen writing modifications to an existing PDF document, if the PDF document already contains time and date of creation in both the document information dictionary and in the document's metadata stream, and the two are not equivalent, a PDF processor should leave the inconsistent values unchanged\" — §7.5.6's update appends, `carry_forward` repeats the trailer's `/Info` as the same indirect reference, and the catalog's `/Metadata` is never among the objects written, so both sources survive a save byte for byte whatever they said. The first rule is about creating a document, which `CLAUDE.md` excludes. The third is a `may` this tree declines: adding a date to whichever source lacks it would be this program forming an opinion about a file it did not produce. **The fourth is a `shall` and staying out of its way is a decision rather than an omission** — it binds a processor writing a modification date, and satisfying it would mean writing `xmp:ModifyDate` into the document's stream as well, so the cost of adding a `/ModDate` on save is an XMP *writer*; Table 349 makes the entry optional except beside a `/PieceInfo` this program never adds. The reader's sentence hands the question back — \"it is at the discretion of the PDF processor how to use this data\" — and the discretion is exercised twice: §14.3.3's tab shows both sources under separate headings rather than merging them, and §12.2 is obeyed where the standard does rank them, for the title alone."

[[clause]]
clause = "14.4"
title = "File identifiers"
status = "implemented"
code = ["crates/pdf-syntax/src/document.rs", "crates/pdf-syntax/src/crypt.rs"]
test = ["crates/pdf-syntax/tests/encryption.rs::the_file_identifier_is_not_decrypted"]
note = "The trailer's `/ID`, \"an array of two byte strings\" — a permanent identifier and a changing one. This tree reads exactly the half §7.6 makes load-bearing: §7.6.4.3.2 step (e) takes \"the first element of the file's file identifier array\" into the file encryption key, so `/ID[0]` decides whether an encrypted document opens at all. The clause's own purpose for the pair — telling whether a reference names the same file and the same version of it — needs a second document to compare against, which nothing here has. §7.5.5's Table 15 carries the rule that makes the encryption use safe: with an `/Encrypt` present the array and both strings \"shall be direct objects and shall be unencrypted\", which is what stops the circularity NOTE 4 describes. **Both halves are written** since the hundred-and-thirty-sixth session: a save carries `/ID[0]` through unchanged, as the clause requires — it \"shall not change when the PDF file is updated\" — and derives `/ID[1]` from the bytes being appended to, which is \"based on the file's contents at the time it was last updated\" and, being a function of those bytes alone, needs no clock. **That determinism survives only in an unencrypted file**: §7.6.3.2 step (d) requires a fresh random initialisation vector in front of every AES string, so an encrypted document's update differs from one save to the next and its second identifier differs with it — the clause's requirement rather than this writer's choice. ADRs 0121, 0129."

[[clause]]
clause = "14.5"
title = "Page-piece dictionaries"
status = "inapplicable"
note = "Page-piece dictionaries: `/PieceInfo` on a page, a form XObject or the catalog, holding \"private PDF processor data\" keyed by the name of the processor that wrote it. Table 351's `/LastModified` and `/Private` are for the producer to recognise its own work; the clause says outright that such data \"can be ignored by general-purpose PDF processors\". **The reason this row gave for that was \"[n]othing here writes a PDF, so there is no private data to recognise\", and it was the retired exclusion wording** — `CLAUDE.md` amended \"we do not create files\" in the hundred-and-thirty-seventh session, this program has written §7.5.6's incremental update since the hundred-and-thirty-fifth, and the five-hundred-and-tenth corrected the same sentence in the ledger's own generated header (ADR 0345). Right conclusion, expired argument, which is `doc/todo/01`'s fifth shape; found by the seventh sweep on the round it became a program, because `tests/saving.rs` names `/PieceInfo` under a row saying nothing here writes. The reason that holds is the clause's own and is narrower: a data dictionary's contents are \"private data needed by the PDF processor\" *that produced them*, keyed by that processor's own name, and this one has never written one — so there is none of its own to recognise, whoever else's the file carries. Nothing is owed by the writing this program does do: Table 351's required `/LastModified` binds a data dictionary a processor writes, and the clause's one `shall` addressed to a reader — \"modification dates shall be compared only for equality and not for sequential ordering\" — binds a processor that compares them against private data of its own. `metadata.rs` reads the other side of the same entry, Table 349's rule that `/ModDate` is required where the catalog states a `/PieceInfo`, which §14.3.4's row carries."

[[clause]]
clause = "14.6"
title = "Marked content"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/optional_content.rs"]
note = "Marked content as a *bracket*, which is all this tree needs of it and is genuinely two things. §8.11.3.3's optional content rides on `BDC`/`EMC`, so the stack has to be maintained exactly; and §12.7.4.3's regeneration has to find the `EMC` matching a `/Tx BMC`, which is the same nesting rule read as an algorithm. What is *not* read is any tag's meaning **beyond the four this tree acts on by name**: `/OC` (§8.11.3.3's optional content), `/Artifact` (§14.8.2.2, with Table 363's property list), `/ReversedChars` (§14.8.2.5.3) and `/AF` (§14.13.5's associated files). This row said flatly that no tag's meaning was read, and the sentence above it in the same note said optional content rides on `BDC` — the sixth failure shape, a note that contradicts itself, corrected by appending and never re-read whole. **The reason this row gave for the wider claim is also gone**: it cited NOTE 3 — the tag operand of a marked-content operator having no relationship to Tagged PDF and therefore not being role mapped — and Errata Collection 3 deletes that note outright (Issue #334, `/State` `Review` `Completed`, found in the four-hundred-and-eighteenth session), replacing the paragraph beside it with one that expects a tag *not* defined in an ISO publication or in §14.7's logical structure to use a second-class name. So the amended clause contemplates a tag that is a structure type, and nothing here reads one. The status does not move and its reason is now what it always should have been — §14.7's and §14.8's semantics are unimplemented, rather than absent from the standard. `partial` for exactly that."

[[clause]]
clause = "14.6.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/optional_content.rs::a_marked_section_belonging_to_a_group_that_is_off_is_not_drawn", "crates/pdf-model/tests/variable_text.rs::need_appearances_rewrites_the_marked_content_region_and_keeps_the_rest"]
note = "Marked content is read for two things and interpreted for neither's semantics. `BDC`/`BMC`/`EMC` maintain the stack §8.11.3.2's optional content needs, which is where the nesting rule 'shall be properly (separately) nested' becomes load-bearing; and §12.7.4.3's splice has to find the `EMC` *matching* a `/Tx BMC`, which is the same rule read as an algorithm — `appearance::spliced` counts depth over `BMC`, `BDC` and `EMC` rather than taking the first `EMC` it meets. `MP` and `DP` are matched and ignored: a point of marked content carries no marks. **Four tags are read by name and their meanings acted on** — `/OC`, `/Artifact`, `/ReversedChars` and `/AF` — and a `BDC`'s property list is where §14.9's `/ActualText`, `/Alt`, `/E` and `/Lang` and §14.7.5.2's `/MCID` are read from, so the sentence this note used to carry (\"[n]othing here reads a tag's meaning\") was false of both halves. What is genuinely not read is a tag that *is* a structure type: §14.7's tree is walked from `/StructTreeRoot` and a section's role is taken from the element that names its `/MCID`, never from the operand of the `BDC` that opened it."

[[clause]]
clause = "14.6.2"
title = "Property lists"
status = "partial"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::an_inline_property_lists_booleans_are_not_operators"]
note = "The clause decides which form a producer may use by what the list holds: values that are all direct objects \"may be written inline in the content stream as a direct object\", and a list with an indirect reference in it \"shall be defined as a named resource in the Properties subdictionary\". `content.rs` takes both forms — **the inline one only since the fifty-fifth session; this row claimed both for fifteen sessions while the content lexer flattened an inline dictionary into loose tokens**, which is how two corpus documents came to report `true` and `false` as unknown operators. A name that `/Properties` does not define is *reported* — a name that `/Properties` does not define is *reported*, since it is the route an `/OC` membership dictionary arrives by and drawing hidden content silently is the failure §8.11 exists to prevent. NOTE 2's consequence is worth carrying because it explains the shape of the whole interpreter: \"indirect references of the form of 10 0 R can never occur within content streams\", which is why the content lexer resolves nothing and the resource dictionary is the only door out. `partial` because the property list's *contents* are read for optional content, for §14.7.5.2's `/MCID`, for §14.9's four accessibility entries, for §14.8.2.2.2's Table 363 artifact entries and for §14.13.5's associated files (`/MCAF`, or `/AF` before the erratum named the key); §14.3.2's object-level metadata in one is not. **This list ended \"and nothing else\" while the artifact and associated-file readers stood** — a list of what is read is maintained by nobody, exactly like a list of what is not."

[[clause]]
clause = "14.7"
title = "Logical structure"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-core/src/accessibility.rs", "crates/viewer-accessibility/src/tree.rs"]
test = ["crates/viewer-core/tests/headless.rs::a_tagged_page_answers_with_its_structure_and_an_untagged_one_says_so", "crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles", "crates/viewer-accessibility/tests/tree.rs::the_root_is_a_window_and_every_child_named_is_present"]
note = "Logical structure: a tree of elements over the content of a document, saying that this run of glyphs is a heading and that one a table cell. **This is the half of clause 14 `CLAUDE.md` puts in scope** — \"tagged PDF as far as accessibility needs it\". The tree is read in both directions (§14.7.2, §14.7.5.4), its types are role mapped in both of the standard's two systems (§14.7.3, §14.8.6.2), its content items are read in all three forms (§14.7.5.1.1), and its attributes and namespaces are read (§14.7.6, §14.7.4). **This row said \"none of it is read\" for two sessions after the seventy-eighth built the tree**, which is the understatement a ledger has no gate against. That sentence was followed by \"[w]hat is still owed is a *consumer*: nothing in this program hands a structure tree to anybody\" for seventy-one sessions, and **it expired in the hundred-and-forty-ninth**: `viewer-core`'s `Query::AccessibilityTree` answers with the page's elements parent-first, each carrying its `/S`, §14.9's spoken form of its text, the language in force and the quadrilaterals it covers. What was owed then was a *host* — \"nothing in this tree depends on AccessKit\", as this row put it for two hundred and twenty-seven sessions. ADR 0134. **And the host arrived in the three-hundred-and-seventy-sixth**, which is what this row said it could not close: `viewer-accessibility` maps §14.8.4's forty-one types onto `accesskit::Role` and `accesskit_unix` puts the result on AT-SPI, where a real client reads it back off the bus — `Frame` → `DocumentFrame` → the page → the page's own elements, with `/Alt` where the document states one. The same round found this crate's own answer reading `/S` **past** §14.7.3's role map, which the clause makes a `shall`, and answering with every element in the file rather than this page's. ADR 0214. **What a page turn pays for the publication was measured in the three-hundred-and-ninety-first, and most of it was not this clause's**: `Query::AccessibilityTree` is 0.13 to 0.25 ms on a 65-page scanned document and the whole publication 0.17 to 0.33, against 1.8 to 3.2 ms spent asking winit where the window was — two synchronous X11 round trips for a number a page turn cannot change, and AT-SPI's screen coordinates rather than anything §14.7 states. It is asked where it can change now (bridge up, `Moved`, `Resized`), and the crate that owns the adapter answers whether it is wanted at all — `Bridge::wants_window_bounds`, deliberately a different question from `Bridge::shortfall` because `doc/todo/31`'s two adapters take a window handle instead. ADR 0228. **And the answer was one page's in name only.** `viewer_core::accessibility::nodes` reached the page's elements by walking the *document's* tree from the root and pruning afterwards, so the bound on one page's answer was spent on the whole document's tree and ran out among the first hundred pages' elements: two thirds of ISO 32000-2's 1023 pages answered with an empty list, which is the same answer an untagged page gives, and nothing said so. Since the four-hundred-and-ninetieth session the page's elements are found through §14.7.5.4 instead — `Tree::elements_on_page` and `Tree::ancestry` — and the walk descends only into the subtree the page occupies. The order stays §14.8.2.5's, which is why the walk still goes downwards: the parent tree's own order is the content stream's. ADR 0325. **And there is a corpus-scale count of what this answer carries since the five-hundred-and-seventh session**, which is ADR 0323's third instrument and the one with no reference to disagree with it: `tools/state.sh accessibility` asks every page of every document that states a tree, and classifies an empty answer by §14.7.5.4 rather than counting it. Its first run found **page one of ten tagged documents answering with nothing** — ISO 14289-1 among them — because `viewer-core` joined an index to a page *object* by inverting `Pages::indices`, a map that deliberately answers for an intermediate `/Pages` node as well, so the scan handed back a node whenever its object number was the lower one and every Table 355 `/Pg` comparison failed. `Page::id` is the join now. ADR 0342. **And a person could listen to this answer and could not move through it, until the five-hundred-and-fifty-ninth session.** A structure element crossed as a node with a name, so an assistive technology read a paragraph whole or not at all; moving by character, by word or by line — and reporting a caret — is `org.a11y.atspi.Text`, which AccessKit builds out of `Role::TextRun` children carrying each character's length, position and width. `AccessibilityNode::lines` is that answer, one line per run of glyphs sharing a baseline, taken from the text layer this crate has had since ADR 0118 and mapped into the same device pixels the quadrilaterals are in. **Which node may carry the interface is AccessKit's decision and not this project's**: `supports_text_ranges` admits only a text input or `Label`, `Document`, `Terminal`, so no role §14.8.4 maps to can have it and mapping `P` to `Label` to get it would trade the standard's vocabulary for the transport. It goes on the *page* node, which is this program's own, and the caret therefore crosses the page in §14.8.2.5's order. Read back off a real bus: the page answers `CharacterCount` 165 on `doc/PDF20_AN001-BPC.pdf`, `GetStringAtOffset` by line and by word, and `GetCharacterExtents` in screen pixels. **The cost of the whole answer was measured on the document whose size makes it a question and taken down by a third**: `Tree::child` resolved every `/K` entry twice and copied the dictionary a third time, which on ISO 32000-2's page 700 was 70.8% of the query, because the walk resolves every child of every ancestor to find out which are elements. ADR 0394. **And the tree invited nothing until the five-hundred-and-ninetieth session.** It said what is on the page and declared no `accesskit::Action` on any node, so a conforming client could hear a document and not move in it. Three are declared now, each on the condition that makes it answerable rather than on the role that suggests it: `ScrollIntoView` on an element that has a place, `Click` on one whose content is §12.5's annotation, and `SetTextSelection` on the page node that carries the text interface. Each resolves to a *place* in the viewport's own device pixels — `viewer_accessibility::Act` — and the host sends the `Command::Scroll` or the `Command::Pointer` it already had, so the boundary gained no message. Two of the three would have arrived undeclared, because `accesskit_atspi_common` offers `Component.ScrollTo` to any node with bounds and `Text.SetCaretOffset` to whatever supports text ranges; only `Click` is gated on the declaration, and it is what puts `org.a11y.atspi.Action` on the node at all. What the round also found is that the drain could not run: the requests were read inside the host's `speak`, which runs only when the page or the window changes, and the queue is filled from `accesskit_unix`'s own thread — so a request on a still window waited for an unrelated event that a person using only a screen reader never causes. It is a winit user event now. ADR 0425. **And the whole answer was one page's while a window could show six, until the six-hundred-and-tenth session.** Table 29's five continuous arrangements landed in the six-hundred-and-sixth and this question did not follow them, so a screen reader walking a column was handed the current page's elements and had no way to learn that three more pages were in front of the person's eyes — the sharpest form of trap 5, because the reader it misinforms is the one for whom the picture is no answer. `Query::AccessibilityTree` answers with one `PageStructure` per page the arrangement shows, in page order; `viewer-accessibility` publishes one `Role::Document` node per entry under the `PdfRoot`, each with an identifier band of its own; and an untagged page in a mixed column keeps its own one-sentence statement that the document states no structure, which a merged tree would have swallowed. The clause is why they are siblings rather than one tree: §14.7.5.4's route is keyed by *a page's* `/StructParents` and §14.7.5.2's identifiers are scoped to one content stream, so two pages' answers share their ancestors and share no numbering. Read back off a real AT-SPI bus on a four-page column. ADR 0445."

[[clause]]
clause = "14.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/src/structure.rs::a_marked_content_identifier_finds_its_element"]
note = "The framing, and the sentence that decides how much of §14.6 has to work first: logical structure \"is independent of the content stream\" but is *attached* to it through marked-content sequences (§14.7.5.2), whose bracketing `content.rs` maintains — for optional content, for §12.7.4.3's splice, and for the `/MCID` that reaches this tree. Both halves exist: the mechanism and the tree."

[[clause]]
clause = "14.7.2"
title = "Structure hierarchy"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles", "crates/pdf-model/src/structure.rs::a_structure_tree_that_points_at_itself_terminates", "crates/pdf-model/tests/structure.rs::the_largest_structure_tree_in_the_tree_is_walked_whole"]
note = "The catalog's `/StructTreeRoot` (Table 29) leads to Table 354's structure tree root, and it and Table 355's structure elements are read since the seventy-eighth session: `structure::Tree` walks `/K` from the root, and the root and an element are one function because both tables give `/K` the same meaning — \"[t]he K entry shall specify the immediate children of the structure tree root, which shall be structure elements\". Walked on demand rather than built, because a tagged document has an element per paragraph and nothing here needs all of them at once; the walk is bounded on depth and on fan-out, since `/K` is a reference a document controls and a cycle is reachable. A dictionary with no `/Type` is an element, which Table 355 states outright. `/ParentTree` is §14.7.5.4's and older. **This row said `/IDTree` was unread \"and needs a consumer that names one\", and both halves were false — the first since the ninety-fifth session and the second since the three-hundred-and-sixty-ninth.** `Tree::element_by_id` is Table 354's entry read as §7.9.6's name tree, and it has two callers: §12.3.2.3's structure destination, and Annex O's `structelem` fragment identifier, which `viewer_core::Open::apply_fragment` carries out. Found in the four-hundred-and-second session by the second sweep, three hundred and six sessions after the reading landed — a family's parent row is not maintained by the sessions that build its members. Unread: `/ParentTreeNextKey`, which addresses a writer; `/Namespaces` (§14.7.4); `/PronunciationLexicon`, which is §14.9.6's text-to-speech hint; and `/AF` (§14.13). `partial` for those and — **until the hundred-and-forty-ninth session** — for the same reason Table 99's layer-panel half was: the data was this crate's and the consumer was not. (This sentence stood in the present tense beside its own correction below for three hundred and fifty rounds, which is `doc/todo/01`'s sixth failure shape — a note corrected by appending.) **It reaches a consumer at last** in that session: `viewer-core`'s `Query::AccessibilityTree` answers with the page's elements parent-first, each carrying its `/S`, §14.9's spoken form of its text, the language in force and the quadrilaterals it covers in the same device pixels a selection is answered in. Six sessions of §14.7 and §14.9 reading had no consumer until then, which the note above said in as many words. **That sentence used to continue \"[t]he role is handed over as the document states it rather than mapped through §14.7.4's role map: a host that knows its platform's vocabulary is better placed to map `H1` or `TD` than this tree is\", and it was two claims wearing one coat.** §14.7.3's role map is the *file's* statement about its own names and a `shall` on whoever reads a type; §14.8.4's set onto a platform's roles is the host's. Only the second was ever the host's, and the first has been obeyed since the three-hundred-and-seventy-sixth session — `Tree::role` was already there and this query read past it. The answer is also **this page's** elements now: an element with no content item on the page being asked about is pruned, which is what this row's own \"a screen reader is being told what is on the screen\" always claimed. ADRs 0134, 0214. **And the bound this row calls \"bounded on depth and on fan-out\" was a third bound as well, and it truncated the largest document this project owns.** `Tree::walk` stopped at 65 536 *items* over the whole tree and returned the prefix with nothing said, so session 416 read 71 371 items off ISO 32000-2 and recorded that as the size of its tree; it is **129 389**, and `logical_order` walks the whole tree once per page, so §14.8.2.5's reading order for any page of the standard this project checks itself against was a truncated one. The four-hundred-and-twenty-first session gave the walk its own bound at 2^20 — eight times that tree — and made it *report*: `Tree::walk` answers a `Reading`, whose `truncated` flag is what `logical_text` and `logical_range` refuse on. The same session found the walk quadratic, its visited set a `Vec<Dictionary>` searched linearly and compared whole; keyed by `ObjectId` it is **16.8 s → 151 ms** for that tree. `doc/todo/49`'s item 5, and ADR 0257."

[[clause]]
clause = "14.7.3"
title = "Structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles", "crates/viewer-core/tests/headless.rs::a_structure_type_crosses_role_mapped_and_speaking_only_for_itself"]
note = "An element's `/S` is read, and `Tree::role` maps it through §14.7.3's `/RoleMap` — \"[w]here names other than the standard ones are used, a role map should be provided in the structure tree root using the RoleMap entry\" — transitively, because a role map may name a type that is itself mapped, and bounded, because it may name itself. An element with no `/S` answers nothing rather than a guess: Table 355 requires the entry, and inventing a type would be a fallback that fills the page in another clause's clothing. **Both halves of what this row called `partial` are closed.** §14.7.4's namespaces are read, so the `/RoleMapNS` half of the same sentence is followed — `Tree::role`'s loop carries a name *and* a namespace — and `StandardType::read` checks a mapped name against §14.8.4's set, answering `None` for one the clause does not define rather than inventing a type. What consumes the check is `viewer_accessibility::role::map`, which puts an unmapped name into the node's description rather than dropping it: §14.8.4.1 makes such a name a defect in the *document*, and a person whose reader says \"group\" is owed the reason. **And the mapping is applied where a consumer can see it at last**: `viewer-core`'s accessibility answer read the raw `/S` past this clause until the three-hundred-and-seventy-sixth session. ADR 0214. **The `/RoleMap` probe is the element's own `/S` bytes**, since the six-hundred-and-fourth session: it went through `from_utf8_lossy` first, so a structure type outside UTF-8 was never mapped and two of them were one type (§7.3.5, ADR 0439)."

[[clause]]
clause = "14.7.4"
title = "Namespaces"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_namespaces_own_role_map_is_the_one_that_applies"]
note = "Namespaces (PDF 2.0), which qualify a structure type so that a document may mix vocabularies. Read in both places they are used: `structure::Namespace` is Table 356's dictionary, `Tree::namespace` answers which namespace an element is in, and `Tree::role` maps a type through the namespace's own `/RoleMapNS` — which is §14.8.6.2's rule and not §14.7.3's."

[[clause]]
clause = "14.7.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_role_map_may_lead_into_another_namespace"]
note = "The framing. New in PDF 2.0 and the reason §14.7.3's role map is no longer the only indirection between a document's type names and the standard's: an element states `/NS`, and the map that applies is that namespace's."

[[clause]]
clause = "14.7.4.2"
title = "Namespace dictionary"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_role_map_may_lead_into_another_namespace"]
note = "Table 356's namespace dictionary: `/NS` the identifying name, `/Schema`, `/RoleMapNS`. Two of the three are used — the name identifies the namespace and the role map is followed, including its second form, \"an array where the first value shall be a structure element type name in a target namespace with the second value being an indirect reference to the target namespace dictionary\", which is how one vocabulary maps into another. `/Schema` is read and not fetched: it is a §7.11 file specification, which this reader has no filesystem to open, and NOTE 2 states no required format for it. The name is **not** parsed as a URI, because NOTE 1 says it is \"used for uniqueness\" and \"[i]t is not generally expected that a URI for a namespace name will resolve\". Re-read off the blame list: `structure.rs` stores `/Schema` and nothing fetches it, which is what this row claims."

[[clause]]
clause = "14.7.5"
title = "Structure content"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles"]
note = "How an element points at the marks on a page, in both directions and in every form the subclauses define: §14.7.5.1.1's three kinds of content item, §14.7.5.2's marked-content sequences, §14.7.5.3's object references and §14.7.5.4's parent tree. See each."

[[clause]]
clause = "14.7.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles"]
note = "The framing: content items \"exist in the document independently of the structure tree but are associated with structure elements\", which is what makes `structure::Child` an enum over the forms rather than a type of its own. §14.7.5.1.1 is where the forms are."

[[clause]]
clause = "14.7.5.1.1"
title = "Content items"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles"]
note = "The two kinds the clause names — marked-content sequences and complete PDF objects — and the three forms they take in a `/K`: a bare integer, Table 357's marked-content reference and Table 358's object reference, each read into `structure::Child`. The clause's restriction is what makes that an enum rather than a recursive type: \"[c]ontent items shall be leaf nodes of the structure tree\". A bare integer takes its page from the element's `/Pg`, which Table 355 requires for exactly that case, and a reference dictionary's own `/Pg` overrides it. **Reading one entry cost three deep copies of it until the five-hundred-and-fifty-ninth session**, and that is a fact about this clause rather than about the caller: distinguishing the three forms needs the entry *resolved*, `Document::resolve` answers with an owned object, and `Tree::child` asked twice — once to test for the bare integer and once for the dictionary — then cloned the dictionary again into `Child::Element`. One resolution, moved rather than copied, and the clause reads the same. It was 70.8% of `Query::AccessibilityTree` on ISO 32000-2's page 700, because a consumer looking for one page's elements has to ask this question of every child of every ancestor. ADR 0394. **This clause's *number* has moved and every citation of it in this tree is the 2020 one**, found in the five-hundred-and-sixty-second session by running `spec-errata emit` rather than `check`: Errata Collection 3's Issue #452 (`Review`/`Completed`) says \"[m]ove entire subclause 14.7.5.1.1 up one heading level to become 14.7.5.2 and renumber later subclauses of 14.7.5 appropriately. Subclause text is otherwise unchanged\", so §14.7.5.2, §14.7.5.3 and §14.7.5.4 each become the number one above. Nothing is inserted and no requirement changes — which is exactly why no sweep here could see it: `check` compares quotations and the erratum strikes a heading, `tables` compares table numbers and none moved, and the ledger's rows are generated from `doc/md/`, which is the un-errata'd text. The numbers stay as the published standard prints them, because the citation gate resolves against that text and the number after §14.7.5.4 exists in no document this tree holds; what changes is that the family now says so — and the citation gate proves the point rather than merely permitting it, because writing the erratum's number for this clause's last sibling fails `the_ledgers_own_prose_names_clauses_and_tables_that_exist` outright. §14.8.4.7's Issue #133 is the same shape with a new clause inserted (ADR 0273), and §7.6.5's Issue #196 the same shape again."

[[clause]]
clause = "14.7.5.2"
title = "Marked-content sequences as content items"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/src/structure.rs::a_marked_content_identifier_finds_its_element", "crates/pdf-model/tests/accessibility.rs::a_structure_element_supplies_the_language_of_its_sequence"]
note = "`BDC` with an `/MCID` in its property list, matched to a structure element by that identifier — `content.rs` reads the identifier as the property list is opened, which is where §8.11.3.3's `/OC` already arrives, and `ParentTree::element` is the match. Table 357's marked-content reference is the other direction and is `structure::Child::MarkedContent`, whose `/Pg` may move the sequence to another page. **What that comparison is made against is a page object, and a consumer got it wrong for as long as it existed**: `viewer-core` held a page *index* and inverted `Pages::indices` to reach the object, which answers for an intermediate `/Pages` node too — so on a document whose node outnumbers its first page every sequence looked like another page's and page one answered empty. Found by the accessibility census on its first run and fixed at the join rather than in this reading. ADR 0342. **The clause states the identifier's *scope*, and the six-hundred-and-tenth session is where that scope reached a boundary**: the property list holds \"an integer marked-content identifier that uniquely identifies the marked-content sequence within its content stream\" — within its content stream, which is one page's. Errata Collection 3's Issue #308 (`Review`/`Completed`) adds the consequence as a NOTE: \"MCIDs are scoped by content stream and must start at zero, so the same MCID may reappear across pages or XObjects.\" So two pages' structures share no numbering, which is why `viewer_core::Query::AccessibilityTree` answers with one entry per page on the screen and each entry's node indices are that page's own. A single flat list across a column would have had to renumber one page's tree against another's, and the standard gives no order between two pages to renumber in. ADR 0445."

[[clause]]
clause = "14.7.5.3"
title = "PDF objects as content items"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-core/src/accessibility.rs"]
test = ["crates/pdf-model/src/structure.rs::the_structure_tree_reads_its_children_and_maps_their_roles", "crates/pdf-model/src/structure.rs::a_pages_annotations_state_where_they_are", "crates/viewer-core/tests/headless.rs::an_element_reached_through_an_object_reference_is_placed_and_says_what_control_it_is"]
note = "Table 358's object reference dictionary, naming an annotation or an XObject as the content of an element: `/Obj` is the object and `/Pg` the page, and both are read into `structure::Child::Object` with the element's own `/Pg` as the default. What such an item *is* for is the annotation's place in the reading order, which needs the consumer §14.7 owes. **And the other direction is read since the four-hundred-and-ninetieth session**: §14.7.5.4 keys an object content item from the object's own `/StructParent` rather than from the page's `/StructParents`, so `Tree::elements_on_page` asks each of the page's annotations and each `XObject` its resources name — an element whose only content item is a widget annotation is on the page and is reached no other way. **And what such an item is *for* is answered since the five-hundred-and-third**, which is what the sentence above meant by \"needs the consumer §14.7 owes\". The clause names two kinds of object — \"an XObject directly or indirectly referenced by a page description or an annotation\" — and the standard places one of them: §12.5.2's `/Rect` says where an annotation is, and §12.7.5 says which control a widget annotation belongs to, so an element whose content *is* an annotation reaches an assistive technology with a place and, for §14.8.4.7.2's `Form`, with a control instead of a group. `structure::annotation_rectangles` is the first and `viewer_core::AccessibilityNode::control` the second. An `XObject` reference is deliberately left unplaced: its position is the matrix in force at the `Do` that painted it, and Table 358's NOTE 2 says as much from the other side — one reference suffices however many times the object is drawn on the page. ADR 0338. **And *which* annotation an element names crosses since the five-hundred-and-ninetieth session**, which is a third question beside the place and the control: `AccessibilityNode::annotation` is the first of this page's annotations the element's own object references name. A rectangle cannot answer it — a `Figure` has one from Table 379 and a caption has one from its own glyphs — and it is what says whether §12.5.1's \"[w]hen the user activates the annotation by clicking it\" applies to the element at all, which is the condition `viewer-accessibility` declares `accesskit::Action::Click` on. Table 368 after Errata Collection 3's Issue #437 is why the population is all three of its annotation types rather than `Form` alone: `Annot` \"[e]ncloses one or more PDF annotations and associated content, if any\" and `Form` \"[e]ncloses a PDF widget annotation and associated content, if any\". 7413 of the corpus's 102 853 reached elements name one (`tools/state.sh accessibility`). ADR 0425."

[[clause]]
clause = "14.7.5.4"
title = "Finding structure elements from content items"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_marked_content_identifier_finds_its_element", "crates/pdf-model/src/structure.rs::a_page_finds_its_elements_through_the_structural_parent_tree", "crates/pdf-model/tests/structure.rs::a_page_that_states_a_structural_parent_key_resolves_it", "crates/viewer-core/tests/headless.rs::every_page_of_a_large_tagged_document_answers_with_its_own_elements"]
note = "The last of the four rows the ledger found waiting on one data structure, and the fourth to close on it (ADR 0053). Both hops the clause states are read: the page's `/StructParents` is the key into the structure tree root's `/ParentTree` number tree, and the marked-content identifier is \"a zero-based index into the array\" that comes back — with §14.7.5.3's single-element form accepted too, since an *object* content item's entry is one reference rather than an array. The consumer is §14.9.4's replacement text on a structure element, which 8 corpus documents state on page one. 89 documents have a `/StructTreeRoot`, 76 key the parent tree from page one and 75 name an element; the one that names none states an empty array for its key, which is a document saying its first page belongs to no element and is asserted as that rather than counted. **And the clause has a second consumer since the four-hundred-and-ninetieth session, which is the one it was written for.** `Tree::elements_on_page` asks it for a *whole page* at once — every element the page's content items belong to — so `Query::AccessibilityTree` reaches a page's structure without walking the document's tree down from the root, which is what had left two thirds of a thousand-page document's pages answering with nothing. All three keyings the clause distinguishes are read: the page's `/StructParents` array for its marked-content sequences, an annotation's `/StructParent` for §14.7.5.3's object reference, and a form or image `XObject`'s, which is the clause's own EXAMPLE 1. A page stating no `/StructParents` answers `None` rather than an empty set, because Table 359 makes the entry required of \"all content streams containing marked-content sequences that are structural content items\" and a set built from such a page's annotations alone would be a complete-looking answer missing every paragraph — found by comparing the two routes over the corpus. `pdf_syntax::tree::lookup_unresolved` exists for this clause and no other: its values *are* references, and resolving one throws away the identity that distinguishes an element from an equal dictionary elsewhere. ADR 0325. **And the clause is why the answer above the crate is a *list of pages* since the six-hundred-and-tenth session**: \"[b]ecause a marked-content sequence is not an object in its own right, its parent tree key shall be found in the StructParents entry of the page object or other content stream in which the sequence resides\", so the route into a page's elements starts at that page and is taken once per page. Table 29's continuous arrangements put several pages in one window, and `Query::AccessibilityTree` now runs this route for each of them — one `Tree::elements_on_page` per page on the screen rather than one for the page a person happens to be on. ADR 0445."

[[clause]]
clause = "14.7.6"
title = "Structure attributes"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attribute_is_found_through_a_class_and_overridden_by_the_elements_own"]
note = "Layout, list, table and printer's-mark properties hung on an element, read through both routes the clause gives — the element's own `/A` and §14.7.6.2's `/ClassMap` — with both of its precedence rules applied. What an attribute *means* is §14.8.5's and is not read; what is here is which attribute objects are attached and which one wins."

[[clause]]
clause = "14.7.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attribute_is_found_through_a_class_and_overridden_by_the_elements_own"]
note = "Table 360's attribute object dictionaries and the `/O` owner that says which vocabulary an attribute belongs to, with PDF 2.0's `/NS` for an owner that is a namespace. `AttributeObject::get` refuses those two entries and answers every other, because the clause draws exactly that line: \"[o]ther entries, except the NS entry, shall represent the attributes\". An object with no `/O` is refused — a set of attributes whose vocabulary nobody named cannot be interpreted, and Table 360 makes the entry required. **Errata Collection 3 replaces \"conforming product that owns\" with \"owner of\" here** (Issue #354, `/State` `Completed`), which changes nothing this row records and was invisible until the four-hundred-and-sixteenth session: the sponsored copy states EC3 as annotations and `doc/md/` dropped every annotation in all fourteen documents (ADR 0252, `tools/spec-errata`)."

[[clause]]
clause = "14.7.6.2"
title = "Attribute classes"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attribute_is_found_through_a_class_and_overridden_by_the_elements_own"]
note = "`/ClassMap` in the structure tree root, so that a set of attributes can be named once and referred to by many elements: an element's `/C` names classes, each class names an attribute object or an array of them, and both forms are read. The precedence rule is the clause's own — \"[i]f both the A and C entries are present and a given attribute is specified by both, the one specified by the A entry shall take precedence\" — which is why `Tree::attributes` returns the classes first and the element's own after them."

[[clause]]
clause = "14.7.6.3"
title = "Attribute revision numbers"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attribute_is_found_through_a_class_and_overridden_by_the_elements_own"]
note = "Deprecated with PDF 2.0, and **read because it is what makes the `/A` and `/C` arrays parseable**: a revision is \"the second (when present)\" element of a pair inside the same array, so a reader that did not know about it would take an integer for an attribute object. NOTE 3 states that reasoning outright — \"since an attribute object reference is distinct from an integer, that distinction is used to determine whether the attribute object is represented in the array by a single or a pair of entries\". The number itself is a *validity* mechanism for a processor that modifies a structure element or its content items — **and 'which this is not' stood here until the five-hundred-and-tenth session, ninety-two sessions expired**: this program has edited and saved since the hundred-and-thirty-fifth, and a filled widget is a content item wherever an `OBJR` names it, so the sentence was the capability shape the third sweep hunts. What keeps the disposal is the clause's own two words, read now rather than assumed: the whole subclause is \"deprecated with PDF 2.0\", and the increment is a *may* — \"it **may** signal the change by incrementing the revision number\" — a permission this PDF 2.0 processor declines, recorded as a choice. `AttributeObject::revision` carries the number and nothing consumes it."

[[clause]]
clause = "14.7.6.4"
title = "User properties"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::user_properties_are_read_with_their_formatting_and_their_hidden_flag"]
note = "Table 361's `/O /UserProperties` and Table 362's `/N`, `/V`, `/F` and `/H`, read as `structure::UserProperty`. The value is kept as the object the file wrote, because the clause tells a processor what to do with the types it does not display — \"they should not treat other values as errors\" — and a reader that coerced everything to a string would have decided that for the consumer. `/F` is kept beside `/V` rather than replacing it, which is the point of the example the clause gives: \"($123.45)\" for the number -123.45."

[[clause]]
clause = "14.7.7"
title = "Example of logical structure"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/logical_structure_example.rs::the_clauses_worked_example_reads_as_the_structure_it_describes"]
note = "The clause is a worked example rather than a requirement, so it is a **test**: `tests/logical_structure_example.rs` is the example's own objects, at its own object numbers, and asserts that each mechanism it demonstrates is read. The role map takes `/Chap`, `/Head1` and `/Para` to `/Sect`, `/H` and `/P`; the class map's `/Normal` supplies a `/WritingMode` while the element's own `/A` overrides its `/TextAlign` (§14.7.6.2); the parent tree answers per page, with the second page's array naming one element twice; the `/IDTree` finds three elements by identifier; and the paragraph that \"spans pages\" resolves to one content item on each, the second through a marked-content reference that names the other page. It also carries the example's `101 1 obj` — the only non-zero generation number in this tree's tests, and exactly the sort of thing a reader assumes away."

[[clause]]
clause = "14.8"
title = "Tagged PDF"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_document_says_it_is_tagged_in_its_mark_information_dictionary"]
note = "Tagged PDF: \"a stylised use of PDF that builds on the logical structure framework described in 14.7\". Of its sixty rows, what is read is the whole of §14.8.4's vocabulary, §14.8.2's artifacts and reversed show strings, §14.8.6's namespaces, and this row's own mark information dictionary. What is left is most of §14.8.5's attributes — what a `/BBox` *says* — with Table 384's `/Scope`, `/RowSpan` and `/ColSpan` read since ADR 0300, and §14.8.2.5's ordering, which is a question for a consumer that walks the tree rather than for the reader that builds it."

[[clause]]
clause = "14.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_document_says_it_is_tagged_in_its_mark_information_dictionary"]
note = "What a tagged PDF is, and the one requirement stated here rather than in a table: \"A tagged PDF document shall contain a mark information dictionary … with a value of true for the Marked entry.\" `structure::MarkInfo` reads Table 353's three flags, and the distinction it makes is real rather than technical — a document may have a structure tree without claiming to follow §14.8's rules, and `/Marked` is the claim. `/Suspects` is read and deprecated in PDF 2.0; `/UserProperties` is a hint the clause states as one, since `Tree::attributes` finds them either way."

[[clause]]
clause = "14.8.2"
title = "Tagged PDF and page content"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_artifact_is_recorded_over_its_own_range_and_left_in_the_text"]
note = "What tagged PDF adds to a *page*, as opposed to what §14.7 adds to a document. Of the thirteen rows below, the two that decide what `text_extraction.rs` produces are read as of the eighty-third session: §14.8.2.2's artifacts, recorded as spans over the readback, and §14.8.2.5.3's `ReversedChars`, which reverses a show string before it is extracted. **This note ended \"[w]hat is left is the reading-order half, which needs a consumer of the structure tree rather than a change here\", and that debt was paid**: §14.8.2.5 is `implemented`, `Query::LogicalSelection` answers with `Tree::logical_order`'s reading and `tools/pdf-retrieve`'s sections rest on `logical_text`. What is left is what the rows below name — §14.8.2.2.1's untagged-content test, §14.8.2.2.2's `Artifact` structure element type, §14.8.2.3's rejoining decision. **The count said twelve until the five-hundred-and-thirty-seventh session**, and it was written by a sweep round while all thirteen rows had been there since the ledger was generated — `doc/todo/01`'s tenth sweep, whose subject is a parent's stated count of its own family."

[[clause]]
clause = "14.8.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::a_reversed_chars_sequence_is_read_back_forwards", "crates/pdf-model/tests/logical_order.rs::a_range_of_the_readback_reorders_or_refuses", "crates/viewer-core/tests/headless.rs::the_logical_order_of_no_selection_is_no_answer"]
note = "The six rules the rest of the family states, listed. Four are read: artifacts against real content (§14.8.2.2), an unambiguous Unicode mapping (§14.8.2.6.1, through §9.10.2's three methods), word breaks as characters rather than as gaps where a file states them (§14.8.2.6.2), and §14.9's four accessibility entries, which are done (ADR 0063). The other two are about *order* — that page content order should coincide with logical content order, and that the logical order be derivable — and both are questions for a consumer that walks the structure tree. **Both have one since the hundred-and-forty-ninth session and the four-hundred-and-thirteenth**: `Query::AccessibilityTree` answers in the structure tree's order, which is the logical one, so a page whose producer wrote its columns out of order is *answered* in the right order there. A selection is still *taken* in content order — that is the space the shapes are in — and **the map between the two orders' offsets is `Tree::logical_range`**, which `viewer_core::Query::LogicalSelection` answers with and `viewer-ui` copies by, saying which of the two orders it got. This sentence said the map \"is what remains\" until the five-hundred-and-seventeenth session read the row off the blame list; §14.8.2.5's own row had said the opposite since the four-hundred-and-thirteenth, which is the seventh failure shape — two rows about one mechanism, disagreeing. Every rule this clause states is a pointer to a subclause and every one of the six is answered: nothing here is owed. ADR 0134, ADR 0250."

[[clause]]
clause = "14.8.2.2"
title = "Real content and Artifacts"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_artifact_is_recorded_over_its_own_range_and_left_in_the_text"]
note = "The division of a page into real content and artifacts, in both of the forms §14.8.2.2.2 gives it. See the two below."

[[clause]]
clause = "14.8.2.2.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_artifact_is_recorded_over_its_own_range_and_left_in_the_text"]
note = "The definition, and what a reader is *permitted* rather than required to do with it: \"[a] text-to-speech engine, for instance, may decide not to speak running heads or page numbers\", and NOTE 3 says the purpose of tagged PDF \"is not to prescribe what the PDF processor does, but to provide sufficient declarative and descriptive information to allow it to make appropriate choices\". So `Interpretation::artifacts` names the ranges and **removes none of them**: dropping an artifact from the text would be this crate making the consumer's choice. What is not implemented is the clause's other test — \"[a]ny content that is not included in the structure tree is an artifact even when not enclosed in a marked-content sequence using the tag Artifact\" — which needs the whole structure tree walked per page and a consumer that would use the answer."

[[clause]]
clause = "14.8.2.2.2"
title = "Specification of Artifacts"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_artifact_is_recorded_over_its_own_range_and_left_in_the_text"]
note = "Both marked-content forms are read — EXAMPLE 1's `/Artifact BMC` generic artifact and EXAMPLE 2's `/Artifact <<propertyList>> BDC` — with the whole of Table 363: `/Type`'s four kinds, `/Subtype`, `/BBox` and `/Attached`'s four edges. **Reading it found a parser gap**: an inline property list's *array* values were recognised as brackets and discarded, on a comment saying no entry this tree reads is an array, and `/BBox` and `/Attached` both are. 30 of the 953 corpus first pages mark at least one artifact. What is left is the third form, the `Artifact` structure element type (§14.8.4.8.7), which is a type in §14.8.4's vocabulary rather than a mark in a content stream."

[[clause]]
clause = "14.8.2.3"
title = "Soft hyphens"
status = "partial"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/content/marked.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::actual_text_replaces_what_a_sequence_reads_back", "crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor"]
note = "A soft hyphen — the one a line break introduced — \"may be represented as a soft hyphen, mapped to the Unicode value U+00AD\", and a writer \"shall distinguish explicitly between soft and hard hyphens\". The reader's half is that a character mapping to U+00AD arrives in the readback as U+00AD and is not turned into a hyphen-minus: §9.10.2's mapping is what produces it and nothing here rewrites it. The clause names 14.8.2.6's routes for saying so, and the one a real producer uses is §14.9.4's `/ActualText` on a marked-content sequence — read in `content/marked.rs` and asserted by name in the first test above, which was written against this clause and which this row did not cite until `doc/todo/01`'s reading found it. What is *not* done is the repurposing decision the distinction exists for — removing a soft hyphen when text is rejoined into a line — which belongs to a consumer, and the clause's NOTE 2 warns that in some languages \"hyphenation can change the spelling of words\", where the answer is §14.9.4's `/ActualText` and not a rule about the character. The tree's own *harness* already folds U+00AD (`text_extraction.rs::without_hyphens`, witness `bug1997343.pdf`) so that a rejoining reference and this reader compare; that is a comparison rule and not the product doing it, and a later round must not read one as the other."

[[clause]]
clause = "14.8.2.4"
title = "Hidden or invisible page content"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::mode_3_paints_nothing_and_still_extracts_its_text"]
note = "One rule, and this tree already satisfies it: \"page content shall be considered to include all graphics objects in their entirety, regardless of whether they are visible when the document is displayed or printed.\" Text in rendering mode 3 is invisible and is still extracted here — which the oracle records from the other side, in the entry about a page of invisible text over a scanned image that reads back plenty of words and marks the page with no glyph. The NOTE gives the reason and it is this project's own: \"invisible elements can become visible when content is repurposed, or a text-to-speech engine could choose to speak invisible text\"."

[[clause]]
clause = "14.8.2.5"
title = "Page content order and logical content order"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/logical_order.rs::the_logical_order_reorders_what_the_stream_showed", "crates/pdf-model/tests/logical_order.rs::a_range_of_the_readback_reorders_or_refuses", "crates/pdf-model/tests/logical_order.rs::the_two_orders_of_a_tagged_page_mostly_coincide", "crates/viewer-core/tests/headless.rs::a_drag_across_a_line_selects_what_it_crossed"]
note = "Both orders, and the mapping between them. Page content order is the interpreter's own — \"the sequencing of graphics objects within a page's content stream\" — and `Interpretation::marked` records, per `BDC` … `EMC` carrying an `/MCID`, the range of the readback that sequence covered. `Tree::logical_order` is the other one: \"a depth-first traversal of the document's logical structure hierarchy\", filtered to one page, and `Tree::logical_text` is the page's text put through it. Measured over the corpus: 89 documents have a structure tree, 77 mark content on page one, and the two orders coincide on 72 of those 77 — the clause says they \"should\", and five producers disagree, among them a tax form whose logical order starts with its title and whose stream starts with a margin note. **And the clause had a reader for a hundred and forty sessions and no *caller* for it, which is `doc/todo/01`'s fifth sweep's shape**: a selection is taken in page content order — that is what the shapes are in — so a person copying off a page whose producer wrote its columns out of order got them in that order. `Tree::logical_range` is the map between the two orders' *offsets* rather than between the whole strings, and `viewer_core::Query::LogicalSelection` is what asks for it. **It answers `None` where the tree does not reach every byte of the range**, which is a decision rather than a limitation: dropping the sequences a tree does not reach is right for a page's reading (§14.8.2.5.1's own position, and what `logical_text` does) and wrong for a copy, so the range form gives a rearrangement of exactly the bytes asked for or nothing at all. A second query rather than a field on `Answer::Selected`, because a drag asks that one sixty times a second and this walks the structure tree. **And the query had no *program* until the four-hundred-and-thirteenth session**, which is `doc/todo/01`'s fifth sweep run over `viewer-core`'s own vocabulary rather than over `pdf-model`'s functions: `Query::LogicalSelection` was named by no host of the six — only by the core's headless test and by `viewer-confined`'s transport, which is a pipe rather than a program — while this row read `implemented` on the strength of the query alone. `viewer-ui` copies the page selection on `c` now, asking this question first and naming the order it got, because a copy that silently rearranged and a copy that silently did not are the same keystroke to a person. The row's status does not move: what was owed was a caller, and every normative requirement of the clause was already executed. **And the range it maps may now span pages** (the six-hundred-and-ninth session): a drag across Table 29's row boundary selects part of two readbacks, so `Query::LogicalSelection` puts each page's range through that page's own filtered traversal and joins the answers — the clause is about \"the sequencing of graphics objects within a page's content stream\" and states no order between two pages, so the page order is the document's and the join is the only thing chosen. The refusal is unchanged in shape and now reaches further: a page whose tree does not cover its part of the range takes the whole answer with it, because a copy missing one page of a selection is the same silent loss as one missing a run. ADR 0444."

[[clause]]
clause = "14.8.2.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/logical_order.rs::the_logical_order_reorders_what_the_stream_showed", "crates/pdf-model/tests/logical_order.rs::the_two_orders_of_a_tagged_page_mostly_coincide"]
note = "The two definitions, both executed, and the clause's own exclusion applied: only structure elements are part of the logical content order, so `Tree::logical_text` leaves out every marked-content sequence the tree does not reach — 3 across the corpus — and NOTE 3 states the case that would otherwise be tempting, that \"[a]rtifacts not contained within an Artifact structure element are not considered part of the logical content order\". Nothing reorders what is *drawn*: the two orders are two readings of one page, and the display list is the stream's. **And the traversal was cut short on any document with more than 65 536 tree items, which is one of the fourteen under `doc/`**: `Tree::walk`'s bound applied in silence, so this clause's \"depth-first traversal of the document's logical structure hierarchy\" was a depth-first traversal of the front of it. Corrected in the four-hundred-and-twenty-first session with the bound raised and reported, and `Tree::logical_text` now answers `None` where the read was truncated rather than a prefix of the page — the same decision `logical_range` already made about a range the tree does not cover, for the same reason. ADR 0257. **And this order is now what a caret moves along.** Since the five-hundred-and-fifty-ninth session each element's own text also crosses as lines of characters, and the platform's text interface sits on the *page* node, whose text runs are its elements' in the order this clause defines — so `GetStringAtOffset` walking forwards from offset zero walks the tree's depth-first order rather than the content stream's, which is the whole of what a tagged document is worth reading for. ADR 0394."

[[clause]]
clause = "14.8.2.5.2"
title = "Sequencing of annotations"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/logical_order.rs::the_two_orders_of_a_tagged_page_mostly_coincide"]
note = "An annotation \"is not interleaved within the page's content stream but [is] listed in the Annots array\", and \"[t]he position of an annotation in the logical content order is determined from the document's logical structure\" — which is §14.7.5.3's object reference, already read as `Child::Object`. `Tree::logical_order` therefore returns annotations *in place* among the marked-content sequences, which is the whole of what this subclause asks a reader to do; 295 annotations across the corpus's tagged first pages are placed that way. `Tree::logical_text` deliberately leaves them out of the string, because an annotation's text is its own `/Contents` or its field's value rather than a range of the page's readback."

[[clause]]
clause = "14.8.2.5.3"
title = "Reverse-order show strings"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::a_reversed_chars_sequence_is_read_back_forwards", "crates/pdf-model/tests/accessibility.rs::the_same_strings_outside_the_tag_are_not_reversed"]
note = "`/ReversedChars`, a marked-content tag saying that the show strings inside it hold their characters in reverse — for right-to-left text set with a font whose glyphs run left to right. \"[T]he sequence of the characters as found in the show string operator shall be reversed before using them. If the sequence encompasses multiple show strings, only the individual characters within each string shall be reversed\", so the readback is reversed per string and per *code* rather than per `char`: a ligature whose `/ToUnicode` says `fi` would come back as `if` from a character-wise reversal. The inferred word breaks are suppressed inside such a string, because the clause states that a break there is a SPACE the file wrote \"at the beginning or end\" rather than a gap a reader measures. **No corpus document writes the tag** — measured over all 953 openable first pages — so the seven right-to-left documents on the text gate's list are painted in visual order and are not this clause. The clause's own EXAMPLE is the test. **And no document in the 108 new ones writes it either**, measured in the four-hundred-and-twenty-third session over `doc/corpora`'s three submodules with every `FlateDecode` stream inflated — which is what makes `hello3.pdf` and `FC60_Times.pdf`, the two Arabic documents the PDFBox comparison names, the same population as those seven rather than a new one. A file that has not used the mechanism the standard provides has not stated the order, and the reordering PDFBox performs on them is the Unicode bidirectional algorithm rather than this clause."

[[clause]]
clause = "14.8.2.6"
title = "Unicode mapping in tagged PDF"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/logical_order.rs::the_two_orders_of_a_tagged_page_mostly_coincide"]
note = "The family: every character code in a tagged document should map to Unicode, and word breaks should be in the character stream. The first is §9.10.2's machinery, which this tree implements in full and which its own row records; the second is §14.8.2.6.2's, where every requirement is addressed to a *document* and what a reader owes is to read the stream it is given. **This row was `partial` \"for that inference, which is measured rather than assumed\"** — the reconstruction of separators from glyph positions — and its own child's row already said why that is not a departure: NOTE 1 says a processor \"can determine word breaks without having to rely on heuristics\" *when the document states the white space*, and `Interpretation::inferred_separators` counts 2392 places over 77 tagged corpus first pages where it does not. A defence against documents that break the clause is not a debt to it. Both children are `implemented` and so is this."

[[clause]]
clause = "14.8.2.6.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/text_extraction.rs::the_text_we_draw_agrees_with_an_independent_extractor"]
note = "\"Every character code that belongs to a structure element in the structure tree shall map to Unicode\", by §9.10.2's three methods — which this tree implements in full and in the clause's stated priority, and which is the whole of what a *reader* can do here. **The exception this clause adds is read, and this row said it was not**: \"except where an associated Alt or ActualText entry applies\" — §14.9.4's `/ActualText` replaces what a marked-content section reads back, from the property list of the `BDC` that opened it and from the structure element where the section states none, and §14.9.3's `/Alt` is what `viewer-accessibility` speaks. Both are on §14.9.3's and §14.9.4's rows as `implemented`, which is the third failure shape: a note claiming an entry is unread where the tree reads it. **And with the exception read, nothing here is owed.** Every requirement in this clause is addressed to a *document* — \"PDF documents conforming with 14.8 … should map every character code\", \"[e]very character code that belongs to a structure element … shall map to Unicode\", \"Private Use Area Unicode values should only be used if no other Unicode value is available\" — and the only sentence that names a processor is NOTE 3's, which says some \"could choose to use the Alt or ActualText entry\". So what a reader owes this clause is §9.10.2's methods and the exception's two entries, and both are here. The family's debt is §14.8.2.6.2's word breaks and stays on the rows that hold it."

[[clause]]
clause = "14.8.2.6.2"
title = "Identifying word breaks"
status = "implemented"
code = ["crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/logical_order.rs::the_two_orders_of_a_tagged_page_mostly_coincide"]
note = "What the clause asks of a *reader* is done, and what it asks of a *document* is measured. A reader \"may find words by sequentially examining the Unicode character stream\", and the division into show strings \"shall have no semantic significance\" — so nothing here treats a `Tj` boundary as a word boundary, and the readback is one string. What a conforming tagged document owes is the white space itself: \"any white-space characters that would be present to separate words in a pure text representation shall be present in the tagged PDF representation of the text\", whose NOTE 1 then says a processor \"can determine word breaks without having to rely on heuristics based on information such as glyph positioning on the page\". **This reader relies on them anyway, and now counts how often**: `Interpretation::inferred_separators` is the number of separators reconstructed from position, and over the corpus's 77 tagged first pages it is 2392 — only 20 of the 77 need none. So the heuristic is not a departure from the clause but a defence against documents that claim to follow it and do not; removing it would empty the readback of most of its word breaks on 57 of 77 tagged pages. **NOTE 1 is also what says which kind of thing the heuristic is**, and the four-hundred-and-sixty-fourth session put that where the code is: a clause that describes what stating the breaks *spares* a reader is a clause acknowledging the reliance it leaves on an untagged page, so `separate_text` is a documented choice rather than a clause obeyed, and it says so. The same session took one class of gap out of the heuristic's hands altogether — §9.3.3 names the single-byte code 32 the ASCII SPACE character, so a word break inside a show string is read from the code rather than inferred from a distance (ADR 0299)."

[[clause]]
clause = "14.8.3"
title = "Basic layout model"
status = "inapplicable"
note = "Aggregate of the three below. The basic layout model is the vocabulary §14.8.5's layout attributes are defined against — reference areas, block and inline progression directions — and it exists so that a processor *reflowing* a document knows what the attributes mean. This program lays nothing out: it draws the page the file describes, at the positions the file states."

[[clause]]
clause = "14.8.3.1"
title = "General"
status = "inapplicable"
note = "The model: content laid out in reference areas, each with a writing direction, block and inline progression. Every term here is an input to reflow, which §14.8.1's NOTE lists as one of tagged PDF's five uses and which is not one this program has."

[[clause]]
clause = "14.8.3.2"
title = "Reference area"
status = "inapplicable"
note = "A reference area is the rectangle a block-level element is laid out in. Read as a definition; nothing here computes one, because nothing here decides where content goes."

[[clause]]
clause = "14.8.3.3"
title = "Progression direction"
status = "inapplicable"
note = "Progression direction — inline and block — for the four writing modes. The one place a *rendering* question hides nearby is §9.7's vertical writing, which is implemented from §9.2.4's metrics rather than from this model, and the two must not be confused: `/W2` places glyphs, this places blocks."

[[clause]]
clause = "14.8.4"
title = "Standard structure types"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-accessibility/src/role.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest", "crates/viewer-accessibility/src/role.rs::every_standard_structure_type_maps_to_something_that_is_not_unknown"]
note = "The standard structure types: forty-one names in `structure::StandardType`, which is what `Tree::standard_role` answers after §14.7.3's and §14.8.6.2's role mapping. Every subclause's table is read; **A consumer arrived in the three-hundred-and-seventy-sixth session**: `viewer_accessibility::role::map` takes each of the forty-one onto an `accesskit::Role`, and the fourteen places the two vocabularies do not line up are written out in that module rather than defaulted to `Unknown` — `DocumentFragment`, `Part`, `Title`, `FENote`, `Sub`, `Lbl`, `Annot`, `Form`, `RB`/`RP`/`WT`/`WP`, `Warichu`, `LBody`, `TH` and `Figure`. What stays `partial`: Annex L's nesting rules are not checked, and are a validator's job rather than a reader's."

[[clause]]
clause = "14.8.4.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_contextual_type_takes_its_category_from_its_parent"]
note = "The four categories, and the rule for a type that is in more than one, which the standard prints as **two bullets** rather than as a sentence: \"[i]f the structure element is used inside a block level element, it is an inline level structure element\" and \"In all other cases it is a block level structure element.\" `Category::of` is that rule, taking the parent's category from a caller walking the tree — because this crate walks `/K` on demand and keeps no ancestry. `Category::Internal` is the fifth answer the clause gives without calling it a level: `LI` is \"[i]nternal to L (List) structure elements\" and `TR` \"[i]nternal to a Table structure\", which is a statement about where an element may appear rather than about what it may contain."

[[clause]]
clause = "14.8.4.2"
title = "Nesting of standard structure elements"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_mapped_role_reads_as_its_standard_type"]
note = "Nesting. The clause defers the rules themselves to Annex L and this reader checks none of them: a nesting rule is a statement about whether a *document* is well formed, and nothing here validates documents — the same position §7.11.2.1's path rules and §7.12.4's version ordering are recorded in. What is implemented is the sentence this row shares with §14.8.4.1: a non-standard type nests \"according to the requirements of the structure elements to which they are role mapped\", which is what makes `standard_role` the question a consumer asks rather than `role`. Read and kept in the five-hundred-and-first session, with one precision recorded: the consumer asks it in two halves across the boundary — `viewer_core::accessibility` crosses `Tree::role`'s mapped string and `viewer_accessibility::role` reads it as a standard type, which is `standard_role`'s own composition, so the fifth sweep listing `standard_role` as unnamed by hosts is the two-crate split rather than an unasked question."

[[clause]]
clause = "14.8.4.3"
title = "Document level structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Document` and `DocumentFragment`, the two document level types, both read. `DocumentFragment` is one of the eight PDF 2.0 additions `StandardType::since_pdf_2_0` names, which matters because §14.8.6.1's default namespace is PDF 1.7's and cannot mean it."

[[clause]]
clause = "14.8.4.4"
title = "Grouping level structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Part`, `Sect`, `Div`, `Aside` and `NonStruct`, the grouping level types. `Aside` is PDF 2.0's."

[[clause]]
clause = "14.8.4.5"
title = "Block level structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`P`, `H`, `Hn`, `Title` and `FENote`. `Hn` is the one entry that is a *family* rather than a name — Table 372 writes it as `H n` \"with n being a sequence of digits representing an unsigned integer greater than or equal to 1\" — so it is matched before the table and `H17` is as standard as `H1`, while `H0` is not. `Title` and `FENote` are PDF 2.0's and are contextual: the clause gives them two and three categories respectively."

[[clause]]
clause = "14.8.4.6"
title = "Sub-block level structure type"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Sub`, PDF 2.0's sub-block level type, \"a sub-division inside a block level\" element. One row, one name, inline."

[[clause]]
clause = "14.8.4.7"
title = "Inline level structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "The inline level types. See the three subclauses below §14.8.4.7.1's framing: §14.8.4.7.2's seven general ones, §14.8.4.7.3's link element and §14.8.4.7.4's ruby and warichu assemblies. **The third and fourth of those were one subclause until Errata Collection 3 Issue #133 split them**, and this family carried the pre-errata division until the four-hundred-and-thirty-seventh session — see §14.8.4.7.3."

[[clause]]
clause = "14.8.4.7.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "The framing: inline types \"enable structural organisation of content inside block level elements\", which `Category::Inline` is."

[[clause]]
clause = "14.8.4.7.2"
title = "General inline level structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-accessibility/src/role.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest", "crates/viewer-accessibility/tests/tree.rs::a_form_element_becomes_the_control_its_widget_annotation_is", "crates/viewer-accessibility/tests/tree.rs::a_toggling_button_carries_its_state_and_nothing_else_does"]
note = "`Lbl`, `Span`, `Em`, `Strong`, `Link`, `Annot` and `Form`. `Em` and `Strong` are PDF 2.0's; the last three are contextual, being \"Grouping, Block or Inline\" depending on what they enclose, and each is about an annotation or a field rather than about the content. **The word was *association* until the four-hundred-and-eighteenth session and Errata Collection 3 makes it enclosure** (Issue #437, `/State` `Review` `Completed`): `Annot` becomes \"[e]ncloses one or more PDF annotations and associated content, if any\" and `Form` \"[e]ncloses a PDF widget annotation and associated content, if any\" — so the content is the optional part and the annotation is what the element is for, which is the reverse of what an association reads as. Issue #133 goes with it, relaxing §14.8.4.7.3's link element from two content items to one and from one link annotation to \"one or more\", and striking the condition that their `/A`, `/Dest` and `/PA` be identical. Nothing here changes: `structure::Type` carries the type a document states and this crate validates no element's children. **`Form` reaches a person as a control since the five-hundred-and-third session**, and that is this row's one reader-side requirement: the type \"[e]ncloses a PDF widget annotation and associated content, if any\" — the erratum's own replacement, which this sentence quoted the struck original of until the five-hundred-and-ninetieth — with \"Form shall be used for each PDF widget annotation that belongs to the real content of the document\" — one widget, so one control. The widget arrives through §14.7.5.3's object reference and §12.7.5's field type decides the role: a check box, a radio button, a text or password or multi-line entry, a list or combo box, with a toggling button's state beside it. All 272 of the corpus's `Form` elements name a widget the field tree reaches (`pdf-model --example element_bounds_census`) and every one of them was a generic group before. `Annot` stays a group and the same table is why — it \"shall not be used for link annotations … or widget annotations\", so the three types divide the annotations and only this one means a control. ADR 0338. **And all three of them invite a click since the five-hundred-and-ninetieth session**, which is what the enclosure wording makes obvious and the association wording did not: an element whose content *is* an annotation is one §12.5.1 says a person activates by clicking it, so `viewer-accessibility` declares `accesskit::Action::Click` on it and the host carries the request out as a press and a release at the node's own middle — the same three steps a mouse takes, so a screen reader's click cannot become a different click from a person's. `Annot` still becomes a group and `Form` still becomes its control; what changed is that the group can now be acted on. ADR 0425."

[[clause]]
clause = "14.8.4.7.3"
title = "Link elements"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/link.rs"]
test = ["crates/pdf-model/src/structure.rs::a_link_element_names_one_annotation_whose_quad_points_are_the_active_areas", "crates/pdf-model/src/link.rs::quad_points_narrow_the_region_and_a_stray_one_is_ignored"]
note = "**This row described the ruby assembly until the four-hundred-and-thirty-seventh session, and the clause it described had moved.** Errata Collection 3 Issue #133 inserts a heading here — \"Insert a new clause heading below NOTE 1 and modify text in subclause 14.8.4.7 as follows\" — and renumbers the ruby one to §14.8.4.7.4, saying so in its own editor's note: \"Clause is renumbered - existing text is unchanged\". The old row's last sentence is why nobody looked: it said the number \"appears twice in `doc/md/` … the citation checker finds the first\", which reads the renumbering as an artefact of the conversion. It is the standard. **Exactly one number of the standard's 1017 headings occurs twice and this is it**, so `ClauseIndex::title` now takes a number's *last* heading rather than its first — a corrigendum is printed after the text it amends — and the title above is the generator's. The clause states two `shall`s and both are about a *wrapped* span: \"the Link structure element shall include a single object reference that associates the span with the associated link annotation\", and \"the link annotation shall use the QuadPoint entry to denote the active areas on the page\". The first binds a producer and this crate validates no element's children (§14.8.4.7.2). The second is a reader's and is what `pdf_model::link::region` does, on the route §12.5.6.5's row records as followed since the fifty-seventh session: Table 176's `/QuadPoints` is the activation region where the clause admits it, one quadrilateral per line, so EXAMPLE 1's sixteen numbers activate the two lines of the span and not the rest of either. `structure.rs`'s `Link` type and `link.rs`'s region meet in `a_link_element_names_one_annotation_whose_quad_points_are_the_active_areas`. Issue #133's other three edits are recorded in §14.8.4.7.2's row."

[[clause]]
clause = "14.8.4.7.4"
title = "Ruby and warichu elements"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::every_ruby_and_warichu_type_is_inline"]
note = "Table 369's **seven** types, all inline: `Ruby`, `RB`, `RT` and `RP` for the ruby assembly, `Warichu`, `WT` and `WP` for the warichu one. **This row named only the warichu three until the four-hundred-and-thirty-seventh session**, because the ledger had this clause's text split across two rows — Errata Collection 3 Issue #133 renumbers §14.8.4.7.3 to this number \"existing text is unchanged\", so what lives here is the whole of it. `RP` is the one entry with a condition on its use — punctuation \"used only when a ruby annotation cannot be properly formatted in a ruby style and instead is formatted as a normal comment, or when it is formatted as a warichu\" — which is a producer's decision and changes nothing a reader does with the type. **The sentence that stood in the old row was ISO 32000-1's** — \"used only when a PDF processor cannot place the ruby annotation text adjacent to the ruby base text\", which ISO 32000-2 does not contain anywhere — and it is below the eleventh sweep's floor, because a quotation of the *older standard* shares fewer than five opening words with the sentence that replaced it. The two constraints the table states on children (`Ruby` \"shall contain one RB element followed by either an RT element or a three-element sequence\", `Warichu` \"shall contain a three-element sequence consisting of WP , WT , and WP\") are validations this crate deliberately does not perform, for §14.8.4.7.2's reason."

[[clause]]
clause = "14.8.4.8"
title = "Other structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "The other structure types: lists, tables, captions, figures, formulae and the `Artifact` element. See the seven below."

[[clause]]
clause = "14.8.4.8.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "The framing for the types that are neither a level nor an inline: they are structures of their own with internal parts, which is what `Category::Internal` records."

[[clause]]
clause = "14.8.4.8.2"
title = "Standard structure types for lists"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`L`, `LI` and `LBody`. `L` is contextual — block or inline — and the other two are internal to it, which is the clause's own wording rather than a category: `LI` is \"[i]nternal to L (List) structure elements\"."

[[clause]]
clause = "14.8.4.8.3"
title = "Table structure types"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest", "crates/pdf-model/src/structure.rs::the_search_stops_at_a_data_cell_after_a_header_cell", "crates/viewer-core/tests/headless.rs::a_cell_is_given_the_header_cells_that_describe_it"]
note = "`Table`, `TR`, `TH`, `TD`, `THead`, `TBody` and `TFoot`. `Table` is block level and the other six are internal to it. Three of the four attributes that make a table *readable* are read since ADR 0300 — `/Scope`, `/RowSpan` and `/ColSpan`, §14.8.5.7's row has them — and this clause's own contribution is the sentence that says what a `TH` is for: \"A table header cell containing content describing one or more rows, columns or rows and columns of the table.\" The clause's own *algorithm* is read since ADR 0312 — the search that finds a cell's headers where `/Headers` states none, run by `TableStack::headers` over the grid the walk already keeps. Its four conditions are implemented as stated: \"the edge of the table is reached\", \"a data cell is found after a header cell\", a header cell that itself states `/Headers` (whose entries are appended and the search ends), and the scope filter — \"[w]hen a header cell is found in the search and the (implicit or explicit) Scope attribute of the header cell is either Both or Row/Column, the header cell is appended to the end of the list of row/column headers\". The direction is the *grid*'s rather than the page's, on this clause's own NOTE: \"[t]his algorithm works for languages with different intrinsic directionality of the script … because the structure always reflects the logical content order of the table\". Measured by `examples/cell_header_census` over 1251 files: 17 431 of 21 883 cells end with at least one header and **17 152 of those get it from this search rather than from Table 384's array**, so a reader that implemented only the array would have answered for 279 of them. The longest answer is 23, from a table in ISO 32000-2 whose every cell is a `TH` — a run of header cells the two termination conditions cannot cut, which is the clause working rather than a defect."

[[clause]]
clause = "14.8.4.8.4"
title = "Caption structure type"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Caption`, grouping or block, \"[f]or lists and tables\" and for figures and formulae."

[[clause]]
clause = "14.8.4.8.5"
title = "Figure structure type"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Figure`, which encloses graphical content and is contextual. Its `/BBox` and `/Alt` are §14.8.5's and §14.9.3's respectively; the second is read (ADR 0063) and the first is not."

[[clause]]
clause = "14.8.4.8.6"
title = "Formula structure type"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Formula`, contextual, with the same two attributes as `Figure` and the same split between them."

[[clause]]
clause = "14.8.4.8.7"
title = "Artifact structure type"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_heading_carries_its_level_and_the_table_carries_the_rest"]
note = "`Artifact` as a *structure element*, PDF 2.0's, which is the third of §14.8.2.2.2's three ways to mark one — \"[b]y inclusion in the logical structure tree through the use of the Artifact structure element type\". The marked-content forms are read by `content.rs` (ADR 0073); this is the type that lets an artifact sit in the tree \"in cases that have positional context relative to real content\", which its NOTE 1 gives line numbers as the example of."

[[clause]]
clause = "14.8.5"
title = "Standard structure attributes"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attributes_owner_and_its_ancestry_decide_which_value_applies"]
note = "The attributes a structure element may carry. **How an attribute is found is implemented** — where it lives (§14.8.5.1), whose vocabulary it is in (§14.8.5.2) and which of several values applies (§14.8.5.3) — and **what most of them mean is not, deliberately**: eleven of this family's sixteen rows are `inapplicable`, because a layout attribute describes the layout process that produced an appearance this reader already has. **Two are not, and both were found the same way**: §14.8.5.6's `PrintField` says what a flattened form field was, and §14.8.5.7's `/Scope` says which axis a table header describes. Neither is drawn and both are read aloud, which is the test this family's `inapplicable` rows should be put to rather than \"does it change a pixel\"."

[[clause]]
clause = "14.8.5.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attribute_is_found_through_a_class_and_overridden_by_the_elements_own"]
note = "Where an attribute lives: a structure element's `/A`, naming an attribute object or an array of them, or its `/C`, naming a class looked up in the structure tree root's `/ClassMap`. Both are read as of the eighty-second session (ADR 0072), with §14.7.6.3's revision numbers, which is what makes the arrays parseable at all. **This row was `silent` while §14.7.6's rows recorded the same code as implemented** — two clauses describing one mechanism, and the ledger's own way of understating."

[[clause]]
clause = "14.8.5.2"
title = "Standard attribute owners"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attributes_owner_and_its_ancestry_decide_which_value_applies"]
note = "Table 376's owners, read as `structure::Owner`. The distinction that earns the type is the one §14.8.5.3 turns on: five owners are PDF's own vocabulary — `Layout`, `List`, `PrintField`, `Table`, `Artifact` — and everything else is a *format's*, `HTML-4.01` or `CSS-3` or a name registered under Annex E. The thirteen format owners are one arm rather than thirteen, because this reader translates to none of those formats and what it needs from the name is only that it is not one of the five. `UserProperties` and `NSO` are Table 360's two other values and are read with them."

[[clause]]
clause = "14.8.5.3"
title = "Attribute values and inheritance"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::an_attributes_owner_and_its_ancestry_decide_which_value_applies"]
note = "The clause's five-step priority, of which the first four are here. A format-specific owner's value is skipped rather than preferred, which is the clause's own condition — priority 1 applies \"if processing based on the format indicated by the owner value\" and this program processes to no format; priority 2 is the PDF-native owners' `/A`, priority 3 the class map, and priority 4 is `Tree::inherited_attribute` walking `/P`, bounded like §14.9.2.3's language walk. **Priority 5 and the choice between 3 and 4 belong to the attribute rather than to the element**: §14.8.5.4's tables say which attributes are inheritable and what each defaults to, and a reader that inherited every attribute would give a paragraph its table's `/ColSpan`. So the caller picks, and the clause's closing sentence is why the two answers are the same kind — \"[t]here is no semantic distinction between attributes that are specified explicitly and ones that are inherited\". Read and kept in the five-hundred-and-first session: `Tree::inherited_attribute` and its test stand as named, and the format-specific skip is still the clause's own condition."

[[clause]]
clause = "14.8.5.4"
title = "Layout attributes"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-core/src/accessibility.rs", "crates/viewer-accessibility/src/tree.rs"]
test = ["crates/pdf-model/src/structure.rs::a_block_level_element_states_the_rectangle_enclosing_its_visible_content", "crates/viewer-core/tests/headless.rs::an_element_that_marks_no_text_crosses_with_the_bounds_the_document_states"]
note = "Aggregate of the seven below. Layout attributes \"specify parameters of the layout process used to produce the appearance described by a document's PDF content\", and the NOTE states the intent outright: \"[t]he intent is that these parameters can be used to repurpose the content or export it to some other document format with at least basic styling preserved.\" That is why every attribute of §14.8.5.4.2 and ten of §14.8.5.4.3's thirteen are `inapplicable` here: the appearance is already drawn, nothing in this program can change it, and nothing in it exports. **This row said exactly that about the whole family and was `inapplicable` on it until the five-hundred-and-seventy-eighth session, while §14.8.5.4.3 had been `partial` since ADR 0301** — three of Table 379's attributes are *results* rather than process, and `/BBox` is read: `Tree::bounds` takes the rectangle that \"completely encloses its visible content\" and `viewer_accessibility::tree::place` puts it on AT-SPI for an element the text layer cannot place, on 61 of the 132 elements that state one. So one requirement of this family is executed and the rest have no meaning for a screen, which is `partial` rather than `inapplicable` — the ledger's own definition of the second is that nothing is owed, and what is owed here is `/Width` and `/Height`, named in that child's row. Found by `doc/todo/01`'s seventh sweep: a family's parent row is not maintained by the session that implements one of its members, because the clauses do not cite each other."

[[clause]]
clause = "14.8.5.4.1"
title = "General"
status = "inapplicable"
note = "Which attribute objects are layout ones — owner `Layout`, or any owner other than `List`, `Table`, `PrintField` and `Artifact` — and Table 377's map from attribute to the element types it applies to. **Read and kept `inapplicable` in the five-hundred-and-seventy-eighth**, when the parent row above stopped being one: the clause's single `shall` — \"[a]ttributes in this category shall be defined in attribute objects whose O (owner) entry has the value Layout or whose owner is any other owner excluding List, Table, PrintField and Artifact\" — binds whoever *defines* an attribute object, and this program defines none. The one layout attribute it reads, §14.8.5.4.3's `/BBox`, is reached through §14.8.5.3's priority over the objects the element already attaches, which is that clause's rule rather than this one's; policing a producer that put a layout attribute where this sentence forbids it is not a requirement this clause places on a reader."

[[clause]]
clause = "14.8.5.4.2"
title = "General layout attributes"
status = "inapplicable"
note = "Table 378's attributes common to every structure type: `/Placement`, `/WritingMode`, `/BackgroundColor`, `/BorderColor`, `/BorderStyle`, `/BorderThickness`, `/Padding`, `/Color`. Every one describes what the page already shows; a reader that drew from them would draw the page twice."

[[clause]]
clause = "14.8.5.4.3"
title = "Layout Attributes for BLSEs"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-core/src/accessibility.rs", "crates/viewer-accessibility/src/tree.rs"]
test = ["crates/pdf-model/src/structure.rs::a_block_level_element_states_the_rectangle_enclosing_its_visible_content", "crates/viewer-core/tests/headless.rs::an_element_that_marks_no_text_crosses_with_the_bounds_the_document_states"]
note = "Table 379's block-level attributes: `/SpaceBefore`, `/SpaceAfter`, `/StartIndent`, `/EndIndent`, `/TextIndent`, `/TextAlign`, `/BBox`, `/Width`, `/Height`, `/BlockAlign`, `/InlineAlign`, `/TBorderStyle`, `/TPadding`. Ten of the thirteen describe the layout process that produced an appearance this reader already has, and a reader that drew from them would draw the page twice. **`/BBox`, `/Width` and `/Height` are not among them**, and this row was `inapplicable` on a reason that only answers for the other ten — then `silent` for one session, because where an element *is* turns out to be a question this program is asked from the other direction and nothing was answering it. `/BBox` is read since ADR 0301: `Tree::bounds` reads \"the coordinates of the left, bottom, right, and top edges, respectively, of the structure element's bounding box (the rectangle that completely encloses its visible content)\" in default user space, `viewer_core::AccessibilityNode::bounds` carries it into the viewport's pixels through §7.7.3.3's rotation and §14.11.2.1's crop box, and `viewer_accessibility::tree::place` puts it on AT-SPI's `Component` interface for an element the text layer cannot place — read back off a real bus as `(35, 181, 105, 48)` on a `Figure` that had no extents at all. Measured by `pdf-model --example element_bounds_census` over 1080 documents: 28 state a `/BBox`, 132 elements do, and **61 of them mark no text**, 60 of those `Figure`s, which is NOTE 1's own prediction. 8 of the 132 rectangles reach outside their page's crop box and one is `[-32768 -32768 32767 32767]`, so what crosses is the rectangle's intersection with the page — §14.11.2.1 makes the crop box what \"shall be clipped (cropped) when displayed\", and the attribute's own word is *visible*. What is left is `/Width` and `/Height`, which are also results rather than process: they give the content rectangle's extent and **no origin**, so they cannot place anything an element that states no `/BBox` would want placed, and nothing reads them. The population is 3 `P` and 51 `Table` elements' worth of `/BBox` that this program now answers with and had no use for before. **What is left of the placeless population is smaller since the five-hundred-and-third session and this row is no longer the only answer to it**: §14.7.5.3's object reference places 333 more of them out of §12.5.2's annotation rectangle, which is a different clause answering the same question for a different population — every `Form` element among them, since a widget annotation marks no text by nature. Table 379's rectangle still wins where an element states both, because it is a statement about the element and the other is a statement about its content. ADR 0338."

[[clause]]
clause = "14.8.5.4.4"
title = "Layout Attributes for ILSEs"
status = "inapplicable"
note = "Table 380's inline-level attributes: `/BaselineShift`, `/LineHeight`, `/TextDecorationColor`, `/TextDecorationThickness`, `/TextDecorationType`, `/RubyAlign`, `/RubyPosition`, `/GlyphOrientationVertical`, `/TextPosition`. A text decoration described here is one the content stream already drew as a rule or a stroke."

[[clause]]
clause = "14.8.5.4.5"
title = "Content and allocation rectangles"
status = "inapplicable"
note = "How a content rectangle and an allocation rectangle are derived, by structure type: from the bounding box of a cell's graphics objects, from the sum of contained block heights, from `/LineHeight` for text. This is the arithmetic of a layout engine, stated so that two processors reflowing the same file agree."

[[clause]]
clause = "14.8.5.4.6"
title = "Figure, Form and Formula attributes"
status = "inapplicable"
note = "Three restrictions on where a `Figure`, `Form` or `Formula` may leave its extent unstated, each conditioned on Table 377's `/Placement`: `Block` requires an explicit numeric `/Height`, `Inline` an explicit numeric `/Width`, and `Inline`, `Start` or `End` make `/BaselineShift` decide the after edge. All three are addressed to whoever *lays the content out* — the first two say which number is \"the sole source of information about the element's extent\" for a reflowing processor, and this one has the appearance already. **The note said `/BBox` was among them and it is not**: that entry is Table 379's, §14.8.5.4.3 owns it, and it is read there since ADR 0301. A `Figure` is exactly the element that clause's NOTE 1 names, so the two rows are about one population and only one of them states a requirement."

[[clause]]
clause = "14.8.5.4.7"
title = "Column attributes"
status = "inapplicable"
note = "`/ColumnCount`, `/ColumnGap` and `/ColumnWidths` on a grouping element laid out in columns. A processor reflowing the content needs them; a processor drawing it has the columns already."

[[clause]]
clause = "14.8.5.5"
title = "List attributes"
status = "inapplicable"
note = "Table 382's `/ListNumbering`, which says how a list's labels were numbered — `Decimal`, `UpperRoman`, `Disc` and the rest. The labels themselves are `Lbl` elements holding the marks the page shows; this names the scheme for a processor that intends to renumber. Worth noticing that §12.4.2's page labels use the same vocabulary for a question this tree *does* answer. (The number said 381 until the three-hundred-and-eighty-seventh session, which is the standard column attributes; §14.8.5.5's own table is 382. The whole §14.8.5 family was one off, and the ninth sweep is what found the block.)"

[[clause]]
clause = "14.8.5.6"
title = "PrintField attributes"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_flattened_form_field_says_what_it_was"]
note = "Table 383 whole, in `Tree::print_field`: `/Role` in all five of its values, `/Checked` in all three of its, and `/Desc`. The clause is the one part of §14.8.5 a *reader* can act on, and its own paragraph says why — a non-interactive form \"may have originally contained interactive fields such as text fields and radio buttons but were then converted into non-interactive PDF files\", so the widget is gone and this attribute is all that says the marks were a check box, whether it was ticked, and what the field was called. Both spellings of the state entry are read, the current one first: Table 383 lists \"Checked, checked\" and deprecates the lower-case form in PDF 2.0, which — like §12.2's `/ViewArea` — tells a *writer* what to stop doing. Table 383's default of `off` is applied here, because an unticked box is what a form full of them looks like. The `tv` role's value is deliberately not read from the attribute: \"[t]he text that is the value of the field shall be the content of the Form structure element\", which §14.8.2.5's logical order already returns. No corpus document states one."

[[clause]]
clause = "14.8.5.7"
title = "Table attributes"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/viewer-core/src/accessibility.rs", "crates/viewer-accessibility/src/role.rs", "crates/viewer-accessibility/src/tree.rs"]
test = ["crates/pdf-model/src/structure.rs::a_header_cell_states_its_axis_and_a_cell_states_its_spans", "crates/pdf-model/src/structure.rs::a_cell_states_the_identifiers_of_the_headers_that_describe_it", "crates/pdf-model/src/structure.rs::a_stated_headers_array_replaces_the_search_and_carries_its_own_headers", "crates/viewer-core/tests/headless.rs::a_header_cell_crosses_with_the_axis_it_describes", "crates/viewer-accessibility/tests/tree.rs::a_cells_header_cells_are_said_in_the_order_the_clause_builds_them"]
note = "Table 384's `/RowSpan`, `/ColSpan`, `/Headers`, `/Scope`, `/Summary` and PDF 2.0's `/Short`. **This row was `inapplicable` on the reason \"[n]othing here is drawn; a spanned cell was drawn as whatever marks the content stream made\", which is the refusal shape whose reason names a *rendering* argument for a requirement addressed to a reader.** §14.8.5.6 next door had already stopped being inapplicable for exactly that: a `PrintField` attribute is not drawn either and it is what a screen reader says. `/Scope`, `/RowSpan` and `/ColSpan` are read since ADR 0300 — `Tree::header_scope` and `Tree::cell_span`, with `TableStack` keeping the grid a walk of §14.7's tree needs to place a cell — and the axis reaches AT-SPI as `RowHeader` or `ColumnHeader`, read back off a real bus. The assumption for a `TH` that states no `/Scope` is the clause's own four bullets over the *logical* row and column, which §14.8.4.8.3's NOTE is what licenses: \"the structure always reflects the logical content order of the table\", so `WritingMode` decides where a row is drawn and not which row is first. `/Headers` is read since ADR 0312, in **both** of the routes §14.8.4.8.3 gives it — the stated array, expanded by this table's own recursion (\"the headers associated with any cell shall be those in its Headers array plus those in the Headers array of any TH cells in that array, and so on recursively\"), and the clause's search where a cell states none — and reaches a person as the cell's AT-SPI description, read back off a real bus on `bug2014080.pdf` (the search) and `pdfjs_wikipedia.pdf` (the array). The order is the entry's own, row headers then column headers, most specific first. `examples/cell_header_census` counts the population: 281 cells state the entry, 2 of them an empty array — which is the attribute *specified*, so no search runs for those — naming 475 identifiers, all 475 of which resolve, all 475 naming a `TH` in the same `Table` earlier in the walk, and **188 of them naming a cell that states `/Headers` itself**, so the recursion is exercised rather than theoretical. What is left is `/Summary`, a table-level sentence nothing yet says, and `/Short`, whose value is what a screen reader should repeat in place of a long header — **0 of the corpus's 6197 `TH` state one**, measured. Measured over 978 documents by `examples/table_header_census`: 25 state a table, 5965 `TH` in all, 227 of them state `/Scope`, and the axes come to 3114 `Row`, 1670 `Column` and 1181 `Both` — so a reader that called every header a column's was wrong about more than half of them. (The number said 383 until the three-hundred-and-eighty-seventh session, which is the PrintField attributes §14.8.5.6 owns and which `structure.rs` cites correctly for `/Checked`; the table attributes are 384.)"

[[clause]]
clause = "14.8.5.8"
title = "Artifact attributes"
status = "inapplicable"
note = "Table 385's `/Type` and `/BBox` on an `Artifact` element, whose four kinds — `Pagination`, `Layout`, `Page` and PDF 2.0's `Inline` — are the ones §14.8.2.2.2's Table 363 property list names. Read and dismissed for the same reason as the rest of §14.8.5: it describes rather than draws. (Two corrections in the three-hundred-and-eighty-seventh session: the number said 384, which is the table attributes, and the entry list said `/Subtype`, which is Table 363's and not this one's — an attribute object here states only the two.)"

[[clause]]
clause = "14.8.6"
title = "Standard structure namespaces"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_namespaces_own_role_map_is_the_one_that_applies"]
note = "Which namespace a structure type's name belongs to, which is what makes two documents' tagsets comparable. The three names are constants here and the mapping rule is implemented; what is owed is the *requirement* the clause states about them — that in a tagged document every element be in a standard namespace or role mapped into one — a statement about whether a *document* is well formed, which this reader deliberately does not validate (§14.8.4.2's reading, one family over). This sentence used to call that gap \"`silent`\", the ledger's word for a processor requirement failed without a word, and it is neither: the requirement addresses the document. See the three below."

[[clause]]
clause = "14.8.6.1"
title = "Namespaces for standard structure types and attributes"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_namespaces_own_role_map_is_the_one_that_applies"]
note = "Two namespaces, named by URI: PDF 2.0's `http://iso.org/pdf2/ssn` and PDF 1.7's `http://iso.org/pdf/ssn`, with the **default being the older one** — \"[w]hen a namespace is not explicitly specified for a given structure element or attribute, it shall be assumed to be within this default standard structure namespace\". A reader that assumed the newest would misread every untagged-namespace element in every file written before 2020. Both are constants in `structure.rs` and `Tree::namespace` returns the default for an element that states none. Errata Collection 3 replaces the default-namespace sentence (Issue #151, `/State` `Review` `Completed`) with one that states the order: an element with no stated namespace is placed in the default standard structure namespace *after* the role map has been applied transitively, and the type it ends at shall be one that namespace defines. `Tree::role` is that walk already. ADR 0253."

[[clause]]
clause = "14.8.6.2"
title = "Role maps and namespaces"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_namespaces_own_role_map_is_the_one_that_applies", "crates/pdf-model/src/structure.rs::a_role_map_may_lead_into_another_namespace"]
note = "Which map applies to which element, and it is not §14.7.3's: \"if the structure element does not explicitly identify its namespace using an NS entry, it should use the RoleMap entry in the Structure Tree Root dictionary … If the structure element is in an explicit namespace, then … the RoleMapNS entry within that namespace dictionary shall provide the role mapping\". `Tree::role` carries a name *and* a namespace through the walk for that reason, follows a mapping into a target namespace, and is bounded because \"applied transitively\" is an invitation to a cycle. **Whether every element ends in a standard namespace is a conformance requirement on the *document*, and this row called the whole of it a validator's job for a hundred and eighty sessions.** Half of it is a reader's, and the half that is has a caller since the five-hundred-and-fortieth: \"[a]n element shall be considered to be in one of these namespaces if:\" and its three bullets are what decide whether a name is §14.8.4's word or a foreign vocabulary's homonym, so `Tree::standard_role` now answers `None` for a name that ends in a namespace `Namespace::is_standard` refuses. `Tree::role` still answers the *name*, because that is what the document wrote and what an accessibility consumer is handed. What is still a validator's job, and is not done, is *reporting* the document that breaks the `shall`. **The same correction reaches `/RoleMapNS` and §14.7.6.3's `/ClassMap`**, whose keys are equally the document's own names (§7.3.5, ADR 0439)."

[[clause]]
clause = "14.8.6.3"
title = "Other namespaces"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/src/structure.rs::a_role_map_may_lead_into_another_namespace", "crates/pdf-model/src/structure.rs::a_foreign_namespaces_homonym_is_not_a_standard_type"]
note = "MathML is the one namespace this subclause defines — \"MathML is the only domain-specific namespace defined in PDF 2.0\" — and it is a constant here, along with the exemption the clause gives it: a namespace named in this subclause \"[does] not require a RoleMapNS entry\", so an element in it keeps its own type. Any other namespace a document declares is read and carried as its name, and since the five-hundred-and-fortieth session it also decides what that name *means*: `Tree::standard_role` refuses to read a foreign namespace's `Table` as §14.8.4's, which is §14.8.6.2's rule and gives `Namespace::is_standard` the caller it had waited for since it was written. **The version came off in the four-hundred-and-eighteenth session**: this row and `structure.rs` both said `MathML 3.0`, and Errata Collection 3 takes the edition out of every sentence of this subclause (Issue #72) because §2's normative reference to MathML 3.0 is itself replaced by MathML Core (Issue #719). The namespace URI does not move, so nothing here does either. **The erratum's other half is the enclosure requirement, and it is a `shall` on whoever writes the mathematics rather than on whoever reads it** — read in the five-hundred-and-fortieth and left undone deliberately, which is a different thing from the debt this row used to record. The amended sentence opens \"[w]hen including mathematics structured as MathML\", and what it then requires — that the `math` element \"be used to enclose the formula under the Formula structure element type\", and that \"[a]ll MathML structure element types and their attributes shall have the MathML namespace explicitly defined\" — is addressed to the producer of the tagging, which `CLAUDE.md`'s closed exclusion covers. What a reader owes is the other side of the same sentence and is done: the namespace identifies the mathematics, and this tree reads it. `partial` stands for one thing only, which is the thing a validator would report: a document that breaks either `shall` is drawn and read here without a word said about it. **And the conversion is a witness rather than a bystander in this clause**: `doc/md/` writes the namespace name as `' http://www.w3.org/1998/Math/MathML '` where the PDF sets `“http://www.w3.org/1998/Math/MathML”`, a single quote and two inserted spaces for one double quotation mark — which is why `conformance::quote::normalise` drops every shape of quotation mark, and why a gate that did not would call a verbatim quotation of this subclause absent from the standard."

[[clause]]
clause = "14.9"
title = "Repurposing and accessibility support"
status = "partial"
code = ["crates/pdf-model/src/accessibility.rs", "crates/pdf-model/src/structure.rs", "crates/viewer-accessibility/src/tree.rs"]
test = ["crates/pdf-model/tests/accessibility.rs"]
note = "Aggregate of the ten below. Accessibility support: a natural language, an alternate description, a replacement text, an expansion and a pronunciation hint. **Four of the five are read, since the sixtieth session** — `/Lang`, `/Alt`, `/ActualText` and `/E`, each in both of the places §14.9 puts it: a `Span` marked-content sequence's property list and, through §14.7.5.4's parent tree, the structure element the sequence belongs to. The fifth, §14.9.6's pronunciation hints, the clause itself excuses. Two things separate the entries and `accessibility.rs` keeps them apart: `/ActualText` is a *character* substitution and belongs to extraction, so it is applied to `Interpretation::text`; `/Alt` and `/E` are phrase substitutions and belong to vocalisation, so they are recorded as spans and applied by `Interpretation::speech`. What this row called still owed was a *consumer*: `CLAUDE.md` names AccessKit as this project's accessibility layer. **The `Query` landed in the hundred-and-forty-ninth session** — `Query::AccessibilityTree` carries each structure element's spoken form, built by `speech` over that element's own span — and the host that hands the runs to AccessKit was what remained after it. ADR 0134. **This row claimed §14.9.3's third location — an annotation's `/Contents` — was owed as well, and §14.9.3's own row records it as read since the sixty-sixth session.** An aggregate row that understates its children is the shape session 115 met six of in one family; found again by the hundred-and-thirty-seventh session's sweep, and this is what the sweep is for. Measured over the corpus: **89 of the 953 page ones state a document `/Lang`, 35 carry §14.9 spans — 13 `/Alt`, 702 `/Lang`, no `/E`.** **The host landed in the three-hundred-and-seventy-sixth**, and with it one correction this row's own reading implied and the code did not do: §14.9.3 makes `/Alt` \"a complete (or whole) word or phrase substitution for the current element\", so an element stating one now *replaces* what is under it rather than being published beside it, and an element that states none speaks its own content items rather than its whole subtree's. ADR 0214."

[[clause]]
clause = "14.9.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/accessibility.rs", "crates/viewer-accessibility/src/tree.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_alternate_description_is_spoken_but_not_extracted", "crates/pdf-model/tests/accessibility.rs::an_expansion_is_spoken_in_place_of_its_abbreviation", "crates/viewer-accessibility/tests/tree.rs::a_substitution_stops_the_walk"]
note = "The four facilities, and the reason they exist: \"many computer users with visual impairments use screen readers to read documents aloud\". Three of the four are read and the fourth is §14.9.6's, which the clause does not require. Nothing here reaches a pixel, which is why a page draws identically whether or not any of it is read — **and the debt this row carried for two hundred and sixteen sessions was a consumer rather than a parser, which the three-hundred-and-seventy-sixth paid**: `viewer-accessibility` hands the four facilities to AccessKit and `accesskit_unix` puts them on AT-SPI, where a screen reader is what reads them. ADR 0214."

[[clause]]
clause = "14.9.2"
title = "Natural language specification"
status = "partial"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/accessibility.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::a_span_overrides_the_documents_language"]
note = "Aggregate of the four below. Three of the four locations a `/Lang` may occupy are read and resolved into §14.9.2.3's hierarchy — the catalog's, a structure element's with its ancestry, and a `Span` property list's — and §7.9.2.2.2's escape sequences are still recognised and removed from every text string this tree decodes. `partial` for the fourth location, Table 122's `/Lang` on a CIDFont's font descriptor, which is evidence about which installed face should substitute and is §14.9.2.2's row. Re-read off the blame list: no source names a descriptor's `\"Lang\"`, so the fourth location still stands owed."

[[clause]]
clause = "14.9.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/structure.rs", "crates/pdf-model/src/accessibility.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::a_structure_element_supplies_the_language_of_its_sequence"]
note = "The hierarchy's inventory, and every item of it is read: `/Lang` in the catalog (`structure::document_language`), in a structure element (`structure::language`, which walks `/P` for the inheritance §14.9.2.3 requires), in a `Span` property list (`content.rs`'s `accessibility`), and the escape sequence inside a Unicode text string that overrides all of them (§7.9.2.2.2, `text_string.rs`, where it is removed). NOTE 2 names the one place a language decides what is *drawn* — an optional content usage dictionary's `/Language`, whose row in §8.11.4.4 records it as a departure this tree measured and declined."

[[clause]]
clause = "14.9.2.2"
title = "Language identifiers"
status = "partial"
code = ["crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_empty_language_identifier_states_nothing"]
note = "What a language identifier is: \"either be the empty text string, to indicate that the language is unknown, or a Language-Tag as defined in BCP 47\", compared case-insensitively, with \"zxx\" for non-linguistic content. The tag is carried as the file writes it and never parsed — a BCP 47 grammar would be a judgement about a registry this program does not hold — but the clause's one *semantic* rule about the value is honoured: an empty identifier states that the language is unknown, which is not a language, so it records nothing and leaves the enclosing statement in force. Three of the four places one may appear are ordinary text strings this tree decodes and now reads. The fourth is the one that could change a *glyph*: Table 122 gives a CIDFont's descriptor a `/Lang`, and where a composite font is not embedded that is evidence about which installed face should stand in — a substitution-quality question, like §9.8.3's `/Style` and `/FD`. **This sentence said those two carried \"the same debt\" after both had readers** — `pdf_font::panose` reads `/Style`'s classification and `substitute.rs` reads `/FD`'s classes, which §9.8.3.2's and §9.8.3.3's own rows have said all along — so the descriptor's `/Lang` is the one of the four still read by nothing."

[[clause]]
clause = "14.9.2.3"
title = "Language specification hierarchy"
status = "implemented"
code = ["crates/pdf-model/src/accessibility.rs", "crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::a_structure_element_inherits_a_language_from_its_ancestry"]
note = "The precedence, in full: the catalog's default, overridden by a structure element's — inherited from the nearest ancestor element that states one, which `structure::language` walks `/P` for — overridden by a nested element's or a marked-content sequence's. The clause's closing rule, that a structure element's specification beats the unstructured content it sits inside, is the *innermost* statement winning rather than a special case, which is what its own EXAMPLE 3 shows and what `accessibility::speech` implements by nesting. The walk is bounded by `MAX_ANCESTRY` because `/P` is a reference a document controls and nothing forbids a cycle; reaching the bound answers \"no language stated\", which is the answer an untagged document gives."

[[clause]]
clause = "14.9.2.4"
title = "Multi-language text arrays"
status = "out-of-scope"
exclusion = "clause-13-multimedia"
note = "Multi-language text arrays — `[(en-US) (My vacation) (fr) (mes vacances) () (default text)]`, matched exactly and then by prefix, with an empty identifier as the default. The clause names exactly two places one may appear, Table 285's media clip data dictionary and Table 288's media clip section dictionary, and both are clause 13 objects — `CLAUDE.md`'s closed exclusion of multimedia and 3D. The matching rule is general and nothing in scope carries an array to match."

[[clause]]
clause = "14.9.3"
title = "Alternate descriptions"
status = "implemented"
code = ["crates/pdf-model/src/accessibility.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_alternate_description_is_spoken_but_not_extracted", "crates/pdf-model/tests/accessibility.rs::an_annotation_with_no_text_is_described_by_its_contents"]
note = "`/Alt`, \"human-readable text that could, for example, be vocalised by a text-to-speech engine\", read from **all three** of the locations the clause names: a `Span` property list, the structure element an `/MCID` names, and — since the sixty-sixth session — an annotation's `/Contents`. The third carries the clause's own condition and it is *checked* rather than assumed: \"any type of annotation … that does not already have a text representation\", so the description is recorded only where the annotation's appearance read back nothing, which a widget laying out its field value does not. It substitutes for the whole span it encloses when the page is *spoken* and changes nothing about what the page *extracts*, which is the distinction §14.9.4's NOTE 2 draws. The clause's word-break rule between consecutive descriptions is implemented by inserting one on each side of every phrase. 13 corpus page ones state an `/Alt` and the shape they take is the clause's own example — `bug1937438_from_word.pdf` describes a summation as \"6 sum from n equals 1 to infinity of 1 over n squared , equals pi squared\". **Table 226's `/TU` is read since the two-hundred-and-fourteenth session, and the reason it was not is the shape ADR 0122 warns about.** This row said it \"names a field in a user interface this program does not have\" — a claim about the *program*, and false since the hundred-and-thirty-second session put a window on it. What kept the clause unreachable was the interface rather than the window: the sentence is a `shall` — \"[a]n alternative name may be specified for an interactive form field (see 12.7, \"Forms\") which, if present, shall be used in place of the actual field name when an interactive PDF processor identifies the field in a user-interface\" — and `Query::FieldAt` answered with one string, which cannot be both the identity `Edit::SetField` addresses and the label a person is shown. It answers with `view::FieldName` now, carrying both, and `shown()` is the clause's own choice between them. **And with the field's *value* beside them since the three-hundred-and-forty-ninth session** (ADR 0201), which is the same shape a third time: a host typing into a field has to append to what the field says, and §12.7.5.3's `DoNotScroll` makes what it says a thing only the core knows. `None` there is a field whose value is not text and `Some(\"\")` a text field with nothing in it — the distinction a host deciding where to send the keyboard needs, and §12.7.4.3's own line. `/TU` is *not* inheritable, which decides where it is read from: the terminal field, which is the widget itself under §12.5.6.19's merge and its `/Parent` otherwise. ADR 0167. **And a substitution has no glyphs, which is what decides where a caret may go.** Since the five-hundred-and-fifty-ninth session an element's own text also crosses as characters with places on the screen, and an element stating `/Alt` or §14.9.5's `/E` carries none: the phrase is \"a complete (or whole) word or phrase substitution for the current element\" and nothing drew it, so a run built out of one would report positions for characters nobody marked. The two answers are carried side by side — what is *said* is the substitution and what is *moved through* is what is on the page. ADR 0394."

[[clause]]
clause = "14.9.4"
title = "Replacement text"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/structure.rs"]
test = ["crates/pdf-model/tests/text_render_modes.rs::actual_text_replaces_what_a_sequence_reads_back", "crates/pdf-model/tests/accessibility.rs::consecutive_replacement_texts_are_not_separated"]
note = "An `/ActualText` replaces the text its marked-content sequence reads back — \"a replacement, not a description, for the content\", a *character* substitution — which is extraction rather than drawing, so the marks are untouched. Both of the clause's locations are read: the `Span` property list since the fifty-fifth session and the structure element since the fifty-sixth, when §14.7.5.4's parent tree made one reachable. **This row said the structure-element half was owed for four sessions after it landed**, which is the ledger's own failure mode seen from the other side — a note describing what somebody found rather than what is there. The last requirement closed in the sixty-sixth session: \"[i]f each of two (or more) consecutive structure or marked-content sequences has an ActualText entry, they shall be treated as if no word break is present between them\", and the word break in question is not in either sequence — it is what the placement pass inferred from the gap the glyphs left, so it is removed where two replacements are adjacent and kept where anything else was drawn between them. `issue13226.pdf` is the corpus witness and it is §14.8.2.3's soft hyphen written the way the clause describes: a space glyph whose `/ActualText` is U+00AD. The clause's own example is a hyphenated German `Druk-`/`ker` whose `/ActualText` is `c`, and it is the test. 5 corpus documents write one."

[[clause]]
clause = "14.9.5"
title = "Expansion of abbreviations and acronyms"
status = "implemented"
code = ["crates/pdf-model/src/accessibility.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_expansion_is_spoken_in_place_of_its_abbreviation"]
note = "`/E`, an abbreviation's expansion, read from both places the clause names — a `Span` property list and a structure element. \"[A] word or phrase substitution for the tagged text and therefore shall be treated as if a word break separates it from any surrounding text\" — the opposite of §14.9.4's rule, and the distinction is the whole difference between the two entries, so `/E` replaces the span in the *spoken* form with a break on each side while leaving the extracted text alone. The clause's own two-abbreviation example is the test. **No corpus document states one on page one**, which is measured rather than assumed and is why the test is the clause's example rather than a file."

[[clause]]
clause = "14.9.6"
title = "Pronunciation hints"
status = "implemented"
code = ["crates/pdf-model/src/accessibility.rs"]
test = ["crates/pdf-model/src/accessibility.rs::a_sequence_overrides_the_documents_language"]
note = "Pronunciation hints — `/PhoneticAlphabet` and `/Phoneme` on a structure element, `/PronunciationLexicons` on the structure tree root — and the clause states its own obligation: \"A PDF processor is not required to process pronunciation hints.\" So nothing is owed and nothing is silent, which is the same reading §10.7.2's flatness permission gets. **And that sentence was this row's own contradiction until the three-hundred-and-fifty-ninth session**: §10.7.2 is `implemented` — `CLAUDE.md` says in as many words that \"a clause that permits is a clause that has been read, and it is a stronger answer than one that does not apply\" — while this row claiming the same reading was `inapplicable`. Two statuses for one argument. `implemented`, on the permission the clause states and the evidence that the alternative it names is what this tree does: §14.9.2's language and Unicode text, which `accessibility.rs` answers `Query::AccessibilityTree` with. The NOTE says why the omission is usually harmless: a text-to-speech function does well enough \"based on text provided in Unicode encoding combined with an indication of the language that applies to that text\", which is §14.9.2's job and not this one's."

[[clause]]
clause = "14.10"
title = "Web capture"
status = "inapplicable"
note = "Aggregate of the seventeen below. Web capture is bookkeeping about where a page's content came from, and the clause opens by retiring itself: \"The features described in this clause are deprecated with PDF 2.0.\" None of it reaches a pixel — §14.1's opening sentence covers it, and every structure here is a registry a *capturing* application keeps so that it can avoid downloading what it already has. **This row said \"[n]o corpus document writes a `/SpiderInfo`\" and §7.7.2's row in this same file said \"5 documents\", measured** — five of the 974 state one on their catalog (`annotation-tx.pdf`, `annotation-tx2.pdf`, `annotation-tx3.pdf`, `issue15262.pdf`, `issue16176.pdf`), and two more outside pdf.js. Two rows of one ledger, one counted and one asserting the absence, and nothing compares them (ADR 0406). The omission is unchanged and its reason is unchanged; what is gone is the false half of it. Two of its structures are name trees this tree could now walk (§7.7.4's `/IDS` and `/URLS`), which is worth recording only because it says the omission is a decision rather than a missing component."

[[clause]]
clause = "14.10.1"
title = "General"
status = "inapplicable"
note = "What the six operations are — save, retrieve, update, find by URL, find by identifier — and the two structures that support them: the information dictionary and the content database. Every one of the six is an operation on a *file*, not on a page, which is why this family owes a renderer nothing even before the deprecation."

[[clause]]
clause = "14.10.2"
title = "Web capture information dictionary"
status = "inapplicable"
note = "The catalog's `/SpiderInfo`: Table 386's `/V`, whose \"version number shall be 1.0 in a conforming PDF file\" and which is \"a single real number, not a major and minor version number\" — so 1.2 exceeds 1.15, which is the one sentence here worth remembering, because §7.5.2's header version and §7.12.4's `/BaseVersion` are the opposite convention. Unread."

[[clause]]
clause = "14.10.3"
title = "Content database"
status = "inapplicable"
note = "Aggregate of the three below: the content database that maps a URL or an MD5 digest to the PDF objects made from it, organised into content sets keyed `SPS` for a page set and `SIS` for an image set."

[[clause]]
clause = "14.10.3.1"
title = "General"
status = "inapplicable"
note = "How the database is organised and where it hangs: the `/URLS` and `/IDS` name trees in §7.7.4's name dictionary, either of which may name a single content set or an array of them, and the arrays may mix the two subtypes."

[[clause]]
clause = "14.10.3.2"
title = "URL strings"
status = "inapplicable"
note = "A seven-step canonicalisation of a URL before it is used as a key — absolute, truncated at the first NUMBER SIGN, lowercased scheme and host, `localhost` removed for `file`, default ports removed, dot segments resolved per RFC 3986. The NOTE is the interesting part and it generalises past this clause: because PERCENT SIGN is both unsafe and the escape character, \"no number of encoding or decoding passes on a URL can ever cause it to reach a stable state\"."

[[clause]]
clause = "14.10.3.3"
title = "Digital identifiers"
status = "inapplicable"
note = "MD5 over the source data, and for a page set over the source followed by the identifiers of each auxiliary file in first-reference order. A page set may also carry a *text* identifier over the text alone, which is how a capturing application tells a changed page from a changed image. RFC 1321's MD5 is already in this tree for §7.6's encryption; nothing calls it from here."

[[clause]]
clause = "14.10.3.4"
title = "Unique name generation"
status = "inapplicable"
note = "How a captured link or form field is given a name that will not collide: the page set's digital identifier is appended to the original name, with Table 387 escaping NUL, PERIOD and REVERSE SOLIDUS — PERIOD because §12.7.4.2 makes it the separator in a field's fully qualified name. A field name gets a second encoding, one source byte to two, each 65 plus a nibble. Writer-side work described in a deprecated clause, and reading a name so constructed needs none of it."

[[clause]]
clause = "14.10.4"
title = "Content sets"
status = "inapplicable"
note = "Aggregate of the two below: Table 388's entries common to every content set."

[[clause]]
clause = "14.10.4.1"
title = "General"
status = "inapplicable"
note = "Table 388: `/S` for the subtype, `/ID` the digital identifier, `/O` the objects the set holds, `/SI` the source information, and `/CT`/`/TS` the MIME type and timestamp. A description of provenance, and a page draws the same with or without it."

[[clause]]
clause = "14.10.4.2"
title = "Page sets"
status = "inapplicable"
note = "A page set, `SPS`: the pages generated from one source \"listed in the O array … in the same order in which they were initially added to the PDF file\", each page in at most one set, with Table 389's `/TID` the optional text identifier."

[[clause]]
clause = "14.10.4.3"
title = "Image sets"
status = "inapplicable"
note = "An image set, `SIS`: the image XObjects made from one source image, with Table 390's `/R` giving each object's reference count so a capturing application knows when an image is no longer used."

[[clause]]
clause = "14.10.5"
title = "Source information"
status = "inapplicable"
note = "Aggregate of the four below: where a captured object came from."

[[clause]]
clause = "14.10.5.1"
title = "General"
status = "inapplicable"
note = "Table 391's source information dictionary — `/AU` the URL or alias, `/TS` when it was retrieved, `/E` when it expires, `/S` the submission method, `/C` the command that produced it. Provenance, not appearance."

[[clause]]
clause = "14.10.5.2"
title = "URL alias dictionaries"
status = "inapplicable"
note = "A URL alias dictionary, which records that several URLs resolved to the same content — `/U` the chain of redirects and `/C` the content sets they reached."

[[clause]]
clause = "14.10.5.3"
title = "Command dictionaries"
status = "inapplicable"
note = "Table 393's command dictionary: the URL a capture was asked for, the flags it ran under, the POST data it sent, and its settings. It is a log of what an application did, which is the clearest statement in this family of what web capture is for."

[[clause]]
clause = "14.10.5.4"
title = "Command settings"
status = "inapplicable"
note = "Table 394's command settings: the conversion engine's own configuration, keyed by MIME type. Explicitly an application's private data — the clause says the settings \"are specific to the conversion engine\" — and nothing in the standard defines their contents."

[[clause]]
clause = "14.10.6"
title = "Object attributes related to web capture"
status = "inapplicable"
note = "The entries web capture adds to objects that are *not* its own: `/ID` and `/SI` on a page, `/PtData` on an XObject or an annotation. This is the one row of the family that touches a page dictionary, and it changes nothing about how the page is drawn — which is the whole family's answer in miniature."

[[clause]]
clause = "14.11"
title = "Prepress support"
status = "partial"
code = ["crates/pdf-model/src/page.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::an_output_intent_says_what_the_documents_device_colours_mean"]
note = "**This row said \"[t]wo of its seven subclauses reach a screen … and both are implemented\" and named printer's marks among \"[t]he rest [that] are for a press\", and it was wrong on both halves until the four-hundred-and-second session** — found by the tenth sweep, which compares a parent row's stated *count* of its children with what the children say. **Three reach a screen, not two**: §14.11.3's printer's marks left the press list in the three-hundred-and-fifty-ninth, when the seventh sweep read the clause's own sentence — the `/F` flags \"shall be set and **all others clear**\", which leaves `NoView` clear — and `PrinterMark` has been in `annotation.rs`'s `STANDARD_SUBTYPES` from the start. §14.11.6.2's trap network annotations are the same reading one clause down. And **neither of the two named was `implemented`** when that correction was made: §14.11.2 was `partial` for §14.11.2.2's guidelines. **That half decayed in its turn and stood for a hundred and twenty sessions**: the four-hundred-and-forty-second read §14.11.2 and §14.11.2.1 off the blame list and moved both to `implemented` — a child that owes nothing cannot make its parent owe something, §14.11.2.2's guidelines being a permission this program declines on its own row — and this row went on saying §14.11.2 is `partial` until the five-hundred-and-sixty-second. **So the sentence naming the shape was true of the sentence beside it**, twice over, which is why it is worth keeping rather than deleting. So, today: §14.11.2's page boundaries, §14.11.5's output intents and §14.11.3's printer's marks reach a screen and are `implemented`, `implemented` and `partial`; §14.11.4's separation dictionaries and §14.11.7's OPI are for a press and are `inapplicable`; §14.11.6's trapping is deprecated in PDF 2.0 and `inapplicable`, with its annotation row `partial` for the same flags sentence — and §14.11.3's is what keeps this row `partial`, alone. **A parent row is not maintained by the sessions that correct its children**, which is `doc/todo/01`'s fifth failure shape, and this is the first instance of it a *count* found — and now the first to have been found twice on one row."

[[clause]]
clause = "14.11.1"
title = "General"
status = "inapplicable"
note = "Introductory to the prepress features, which it describes as \"specialised features to support prepress production workflows\". The two entries in it that a viewer reads have rows of their own."

[[clause]]
clause = "14.11.2"
title = "Page boundaries"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/page_geometry.rs::the_five_page_boundaries_default_and_are_clipped_to_the_medium"]
note = "All five boundaries are read, defaulted and intersected with the medium, and `Page::boundary` is the rectangle one of them names — which is what §12.2's `/ViewArea` and `/ViewClip` need, since those entries name a boundary rather than stating one. **`partial` until the four-hundred-and-forty-second session on a debt that was never one**: the row's own last sentence said what it owed was §14.11.2.2's guidelines, \"which are `inapplicable` on their own row\" — a clause that owes nothing cannot make its parent owe something, and the note had held both halves of that since it was written. §14.11.2.1 carries the two boxes and the requirements; there is no other child."

[[clause]]
clause = "14.11.2.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/page.rs"]
test = ["crates/pdf-model/tests/render_real_pdf.rs::the_fixture_page_is_modelled_correctly", "crates/pdf-model/tests/page_geometry.rs::the_five_page_boundaries_default_and_are_clipped_to_the_medium"]
note = "The crop box is honoured: it is inherited down the page tree, defaults to the media box, is refused and replaced when degenerate, and gives the rendered page both its size and its origin — which is also what every reference renderer is asked for, see tools/pdfref. The media box is read as the crop box's default. **The bleed, trim and art boxes are read since the eighty-first session**: each defaults to the crop box, none is inheritable (§7.7.3.4 leaves the four Table 31 marks alone), and each is treated as \"its intersection with the media box\" as this clause requires of all four. They bound a production process rather than a display, so nothing here draws with them — what reaches a screen is §12.2's `/ViewArea` and `/ViewClip`, which may name any of the five. **This row said `/UserUnit` was \"neither applied nor reported\" for thirty-two sessions after the twenty-ninth applied it** — §7.7.3.3's own row records it landing, and this one, four clauses away, was never revisited. Where the crop box's *origin* lands in a raster whose size had to be rounded up is §10.7.4's row, and was a defect until the sixty-first session (ADR 0064). **`partial` until the four-hundred-and-forty-second session, which read the clause and found nothing owed**: the boundaries, their defaults, the clipping and \"its intersection with the media box\" are all here, and what the clause has after that is a NOTE about placing a page in another application, printing a finished page and imposing several on a press sheet — advice to whoever chooses a box, not a requirement on whoever reads one. That the production boxes reach no pixel is §14.11.3's subject and not this clause's. **And the sentence \"refused and replaced when degenerate\" above was true of four boxes and not of the fifth, which the five-hundred-and-seventy-fifth session found from the other end**: this clause gives the crop, bleed, trim and art boxes a *default* — \"[t]he default value is the page's media box\" for the crop box, \"the page's crop box\" for the other three — so an empty one has somewhere known-good to fall back to, and `build_page` has done that since each was read. The media box is what all four fall back *to*, so an empty one takes the page's whole geometry with it, and the rule it needed is §7.7.3.4's rather than this clause's. Four boxes carrying a rule and the fifth carrying none is the shape to look for wherever a clause defines a family by defaulting each member to the one before it. ADR 0410. **And since the six-hundred-and-eleventh session the crop box decides one more thing: where §11.4.7's 𝑊 stops.** \"The crop box defines the region to which the contents of the page shall be clipped (cropped) when displayed or printed\" and, three sentences on, \"the crop box determines how the page's contents shall be positioned on the output medium\" — so on a screen showing several pages the boundary between the page's own colour and whatever surrounds it is *this* rectangle. `pdf_render::page_area` maps it through a target's transform and `impose_within` composites 𝑊 inside it and the program's own surround outside; §11.4.7's row has the argument for why only the first of those two colours is the standard's. **What is still owed to the first half of that sentence is the *clip*, and it is named rather than left implicit**: `pdf_model::interpret` deliberately keeps the marks a stream made outside the box — `Open::content_box` says so in as many words — and a page-sized raster cuts them at its own edge, so the requirement is met wherever a target is its page and is not met on a window-sized one, where a mark outside the box draws over the ground beside the page. Invisible while the ground was page white and one page filled the window; visible now, on both counts. `doc/todo/30` carries it with the clause and the population is not yet measured."

[[clause]]
clause = "14.11.2.2"
title = "Display of page boundaries"
status = "inapplicable"
note = "`/BoxColorInfo`, the colours an *interactive* processor draws page-boundary guidelines in. The clause makes both the guidelines and the entry optional — \"[i]nteractive PDF processors **may** offer the ability to display guidelines\" — and this one does not offer it, so there is nothing for the entry to colour. It becomes a row to fill the day a viewer grows a prepress view. **`inapplicable` here means a permission declined, not a device this program is not**, and the three-hundred-and-fifty-ninth session's sweep found the ledger using one word for both: §10.7.2's flatness permission is `implemented` on `CLAUDE.md`'s rule that \"a clause that permits is a clause that has been read\", and it earns that status because there is code that reads `i` and discards it. There is no code here to name, so the row stays `inapplicable` with its reason said precisely rather than borrowing a status it has no evidence for. Table 397's `/C`, `/W`, `/S` and `/D` are a dash pattern and a colour this tree already knows how to draw; what is missing is a person who asked to see the boxes."

[[clause]]
clause = "14.11.3"
title = "Printer's marks"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::the_hidden_and_no_view_flags_draw_nothing_and_report_nothing"]
note = "**Was `inapplicable` on two reasons and both were wrong**, found in the three-hundred-and-fifty-ninth session by re-reading every `inapplicable` row the way §10.5 was re-read. The row said printer's marks are \"placed \"outside the boundaries of the page's trim box\", which is by construction outside what this viewer draws\" and that \"Table 167's Print flag governs them and a screen is not a printer\". (1) **This viewer draws the crop box**, not the trim box, and the clause's only `should` is \"[t]he annotation rectangle should not intersect the TrimBox\" — a mark between the trim box and the crop box is inside what is drawn, and the clause's own word for the wider claim is *typically*. (2) The clause settles the display question in the opposite direction: \"[t]he Print and ReadOnly flags in the F entry shall be set and **all others clear**\", and `NoView` is among all others — so a conforming printer's mark is one whose flags say *display me*. **And §12.5.6.20's own row already said the code draws them** — \"[i]ts /AP is drawn like any other annotation's\" — so the two rows contradicted each other, which is `doc/todo/01`'s sixth shape across a pair rather than inside one note. `annotation.rs` has never special-cased the subtype. What the clause requires of a *processor* is met by §12.5.5's route, which this tree implements including the `/N` subdictionary selected by `/AS` and NOTE 2's rule that `/R` and `/D` are never displayed. `partial` for Table 399's `/MarkStyle` — \"a text string representing the printer's mark in human-readable form and suitable for presentation to the user\" — which is a label for a panel this program has no printer's-mark list in, and for Table 398's `/MN`, read and reported by §12.5.6.20 where a mark states no appearance. **No corpus document states one**, so nothing here is exercised by a file. **Read and kept in the five-hundred-and-sixty-fifth**: nothing in the tree names `/MarkStyle`, and the reason it is unread is still a capability this program does not have — there is no printer's-mark list to put a human-readable label in — which is the third sweep's shape as a true statement rather than an expired one."

[[clause]]
clause = "14.11.4"
title = "Separation dictionaries"
status = "inapplicable"
note = "A separation dictionary describes \"a single separation of a colour-separated document\" — one plate of a press run. §10.8.1 makes producing separations \"up to the processing software\", and this one produces a screen."

[[clause]]
clause = "14.11.5"
title = "Output intents"
status = "implemented"
code = ["crates/pdf-model/src/content.rs", "crates/pdf-model/src/colour.rs"]
test = ["crates/pdf-model/tests/colour_paths.rs::an_output_intent_says_what_the_documents_device_colours_mean", "crates/pdf-model/tests/colour_paths.rs::a_default_space_outranks_the_output_intent", "crates/pdf-model/tests/transparency_groups.rs::the_three_routes_to_a_press_all_composite_in_it"]
note = "`output_intent_space` reads the catalog's `/OutputIntents` and takes the `/DestOutputProfile` of the first one that has a readable profile, which becomes what the document's device colours mean where it names no `/DefaultCMYK`. That ranking is the clause's own — §8.6.5.7 NOTE 3 sends a reader here for \"the intended meaning\" of a device space, and §8.6.5.6's default spaces are the nearer statement and win. `/S` is not filtered on: the clause's subtypes name the *kind* of press the profile describes, and a profile is a profile whichever it is. §10.3.1 is where this fits in clause 10's picture. **Table 401's `/DestOutputProfile` entry was read in the four-hundred-and-thirty-sixth session for the sentence that decides which direction a screen uses**: \"The output transformation uses the profile's \"from CIE\" information (BToA in ICC terminology); the \"to CIE\" (AToB) information may optionally be used to remap source colour values to some other destination colour space, such as for screen preview or hardcopy proofing.\" This processor's device is a screen, so the optional clause is the one in force and the `A2B` evaluator ADR 0009 built is the whole of what an output intent needs — no `B2A` and no colour-management dependency. Since that session the intent also decides what a page whose group states `/DeviceCMYK` *composites* in, which is §11.4.7's blending space rather than a colour conversion: 94 of the 65 944 web documents reach a press this way, and none of the 974. ADR 0272."

[[clause]]
clause = "14.11.6"
title = "Trapping support"
status = "inapplicable"
note = "\"The features described in this clause are deprecated with PDF 2.0.\" Trapping overlaps adjacent colours to hide misregistration between a press's plates; there is no misregistration on one raster."

[[clause]]
clause = "14.11.6.1"
title = "General"
status = "inapplicable"
note = "See §14.11.6."

[[clause]]
clause = "14.11.6.2"
title = "Trap network annotations"
status = "partial"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/appearance.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance"]
note = "**The second of the two prepress rows corrected in the three-hundred-and-fifty-ninth session, and the same defect as §14.11.3's.** The row said \"drawing it on a screen would paint the artefact-hiding overlaps *as* artefacts\" — a claim about a behaviour this tree does not have, since `TrapNet` is in `annotation.rs`'s `STANDARD_SUBTYPES` and its `/AP` is drawn like any other's, which **§12.5.6.21's own row said in as many words**. Two rows, one mechanism, opposite claims. And the clause decides the display question itself, in the same sentence §14.11.3 uses: the `/AP`, `/AS` and `/F` entries \"shall be present, with the Print and ReadOnly flags set and **all others clear**\" — `NoView` among all others. The clause's `/N` subdictionary keyed by `/AS`, which is how alternate trap networks are selected, is §12.5.5's route and implemented. `partial` for Table 403: `/Version` and `/AnnotStates` bind \"any PDF writer that generates a new trap network\" and `/FontFauxing` records what a trapping engine substituted — writer-side, all three. **One reader-side sentence is unread and is not writer-side**: if the page object's `/LastModified` is more recent than the annotation's, \"the page's trap networks are invalid and shall be regenerated\", and a reader that cannot regenerate them is drawing traps the clause has called invalid. `doc/todo/01` carries it; **no corpus document states a `/TrapNet` at all**, so nothing here is exercised by a file. **Read and kept in the five-hundred-and-twenty-fifth**: the `/LastModified` sentence is still unread and still carried by `doc/todo/01`, and `TrapNet` is still drawn like any other subtype."

[[clause]]
clause = "14.11.6.3"
title = "Trap network appearances"
status = "inapplicable"
note = "See §14.11.6.2."

[[clause]]
clause = "14.11.7"
title = "Open prepress interface (OPI)"
status = "inapplicable"
note = "OPI substitutes a low-resolution proxy image for a high-resolution one held outside the file, for a prepress workflow that swaps them back at output time. The `/OPI` entry Table 87 and Table 93 state on an image or a Type 1 form XObject is a *proxy's*: its value is Table 405's OPI version dictionary, whose single `1.3` or `2.0` key carries Table 406's or Table 407's attributes, and a processor that draws the image the file contains — which is what this one does — has already drawn what OPI describes. (This row cited \"Table 402\" until the three-hundred-and-eighty-seventh session, which is the `DestOutputProfileRef` dictionary of §14.11.5's output intents, two subclauses away and about a colour profile.)"

[[clause]]
clause = "14.12"
title = "Document parts"
status = "inapplicable"
note = "Aggregate of the six below. Document parts are a *production workflow* structure: a second hierarchy over the same pages, carrying metadata for a job ticket. §14.12.1 states its own purpose — \"[t]he use of a separate job ticket file for specifying page ordering and processing requirements\", targeted at \"a production workflow, digital printing device, or other messaging channel\" — and nothing in the family changes a mark on a page. That is the same reading §14.2's procedure sets and §14.5's page-piece dictionaries got, and it is why this is `inapplicable` rather than owed. **No corpus document states a `/DPartRoot`.** One sentence of the family is *not* `inapplicable` and was found so in the hundred-and-eighteenth session: §14.12.3's depth-first ordering, and Table 409's `/Start`, decide which page §12.6.4.5's GoToDp action shows — so a DPart dictionary decides what is on screen, which is `document_part::first_page`. The rest of the family stays `inapplicable`, and the distinction is the habit's: an `inapplicable` row means nothing is owed *by the clauses that reach it today*. The two rows below it that are `writer-side` rather than `inapplicable` are the ones whose every sentence constrains how a file is built."

[[clause]]
clause = "14.12.1"
title = "General"
status = "inapplicable"
note = "Why document parts exist: a PDF written for one workflow is \"not portable across different production workflows or digital printing devices once they are created\", and a DPart tree lets a job ticket name page ranges without rewriting the file. NOTE 1 is the one sentence with a reader in it — \"a PDF processor can also access page object entries indirectly from the DPart leaf nodes … for structured presentation of pages\" — and it is a NOTE, describing a presentation this program does not offer and the standard does not require."

[[clause]]
clause = "14.12.2"
title = "DPart tree structure"
status = "writer-side"
note = "The tree's shape, and every sentence in it constrains the *file*: the ranges \"shall not overlap and therefore shall not share the same PDF page objects\", each page \"shall be included in the page range defined by one and only one DPart dictionary\", and \"[a] child DPart dictionary shall not be referenced by more than one parent\". A reader implements none of it; it reads whatever tree arrives, if it reads one at all."

[[clause]]
clause = "14.12.3"
title = "Connecting the DPart tree structure to pages"
status = "writer-side"
note = "How the tree connects to pages, and again every requirement is on the writer: each page in a leaf's range \"shall have a DPart key\" pointing back at that leaf, page numbers within a range \"shall be monotonically increasing, as defined by the document's page tree\", and the depth-first order of the DPart tree shall match the page tree's. NOTE 2 states the consequence, which is why a reader owes nothing: \"the pages will be enumerated the same using either mechanism\" — so this tree's page order is already the one the DPart hierarchy would give."

[[clause]]
clause = "14.12.4"
title = "Data structures"
status = "partial"
code = ["crates/pdf-model/src/document_part.rs"]
test = ["crates/pdf-model/src/document_part.rs::a_leaf_begins_at_the_page_its_start_names"]
note = "Aggregate of the two below: Table 408's `DPartRoot` and Table 409's `DPart`. **The note here read \"[n]either is read, and neither reaches a screen\" until the three-hundred-and-fifty-ninth session, and its own parent row said the opposite**: §14.12 names `document_part::first_page` and the sentence that puts Table 409's `/Start` on the screen through §12.6.4.5's `GoToDp` action. `doc/todo/01`'s first shape — a note that outlived the code — found this time by the seventh sweep, which greps each `inapplicable` row's own vocabulary against the source. Table 409's `/Start` and `/DParts` are read; Table 408 is not, and `partial` is for that half. **Read and kept in the five-hundred-and-twenty-fifth**: nothing in the tree names `DPartRoot` — the half this row is `partial` for — while `/Start` and `/DParts` are read by `document_part.rs`."

[[clause]]
clause = "14.12.4.1"
title = "General"
status = "partial"
code = ["crates/pdf-model/src/document_part.rs"]
test = ["crates/pdf-model/src/document_part.rs::a_leaf_begins_at_the_page_its_start_names"]
note = "Table 408 and Table 409. **Table 409's `/Start` and `/DParts` are read** — `document_part::first_page` follows `/DParts` depth-first to the leaf a `GoToDp` names and answers with the page `/Start` points at, which §14.12.3 makes the first page of the range. Table 29's `/DPartRoot` — leading to Table 408's DPartRoot dictionary, whose `/DPartRootNode` is the root — is not read, because the action names a `DPart` dictionary directly and nothing here enumerates the tree from its root; that half is what `partial` is for. This sentence used to give the entry to Table 408 under the catalog's key's name, an attribution the ninth sweep is for. Two entries touch families this project does read and neither changes that: a `/DPart`'s `/AF` is §14.13.8's associated files, `partial` on their own row — this sentence said `silent`, a status the ledger has not held a row of since Annex O's five were built, and its `/Metadata` is §14.3.2's metadata stream, read since ADR 0186 at the document level and not consumed at the object level, which §14.3.2's own row states. `/DParts` and `/Start`/`/End` are exclusive — the clause's NOTE says only leaf nodes may name pages, which is why `first_page` descends rather than reading both."

[[clause]]
clause = "14.12.4.2"
title = "Document part metadata"
status = "inapplicable"
note = "Document part metadata: \"application-specific information communicated between the creator of PDF data and a receiving system\". The clause's own definition of the audience, and this program is not it. Two constraints are worth having read for the *shape* they impose rather than for any use here: every key in a DPM dictionary \"shall conform to the rules of an XML name token as well as the rules for PDF name objects\", and the values \"shall only be of type text string, date string, array, dictionary, boolean, integer or real\" — a PDF dictionary restricted to what XML can carry."

[[clause]]
clause = "14.13"
title = "Associated files"
status = "partial"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "Associated files: a file specification carrying an `/AFRelationship`, named by an `/AF` array on the object it belongs to. `attachment::associated` reads such an array from any dictionary, which is what the clause's own shape asks for — it lists seven objects that may carry one and says the same sentence about every one. What is not done is *using* the relationship, and the clause says a processor need not: \"[t]he value of AFRelationship does not explicitly provide any processing instructions for a PDF processor. It is provided for information and semantic purposes\". 7 corpus documents state an `/AF`: 6 on catalogs, 30 on structure elements, with `Supplement` 33, `Source` 4 and `Unspecified` 2."

[[clause]]
clause = "14.13.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "The framing and the list of objects that may carry an `/AF`. All eight of Table 43's relationship values are read, plus Annex E's second-class names, which are kept rather than flattened to `Unspecified` — the table's NOTE 2 says `Unspecified` \"is to be used only when no other value correctly reflects the relationship\", so a producer that registered a name has said something a reader should not throw away."

[[clause]]
clause = "14.13.2"
title = "Embedded associated files"
status = "partial"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "The two forms an associated file may take, and this reader takes the recommended one: \"[b]oth types are allowed for associated files but the embedded form is recommended\", and an external one is §7.11.1's refusal, which no filesystem here can lift. §7.11.4's stream is read with its `/Params` and `/Subtype`; the clause's requirements *on a producer* — a `/ModDate` and a valid MIME type, `application/octet-stream` where it is unknown — are checked by nobody, because this reader does not validate files."

[[clause]]
clause = "14.13.3"
title = "Associated files linked to the PDF document's catalog"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship", "crates/pdf-model/src/attachment.rs::a_file_associated_with_the_catalog_is_listed_without_a_name_tree", "crates/pdf-model/src/attachment.rs::a_file_filed_in_the_tree_and_associated_with_the_catalog_is_one_attachment"]
note = "`/AF` on the document catalog, read by `attachment::associated` against the catalog dictionary — **and, since the four-hundred-and-eighty-ninth session, listed**: `attachments` appends the catalog's associated files to §7.7.4's tree, deduplicated by embedded stream, because until then `associated` had no caller outside its own tests and a payload a producer associated without filing was a file no panel could list and no host could extract — §12.5.6.15's `/FS` shape (ADR 0295) one family over, found by reading this family off the blame list (ADR 0324). 6 corpus documents state one."

[[clause]]
clause = "14.13.4"
title = "Associated files linked to a page dictionary"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "`/AF` on a page dictionary, which is the same function against `Page::dict`. No corpus document states one; the entry's own NOTE says why a producer would — associated files \"serve not only as alternative to PieceInfo (which is vendor specific) but also enable richer semantic content when doing page/document merging\"."

[[clause]]
clause = "14.13.5"
title = "Associated files linked to graphics objects"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs", "crates/pdf-model/src/content.rs"]
test = ["crates/pdf-model/tests/accessibility.rs::an_af_tagged_section_associates_a_file_with_what_it_draws"]
note = "`/AF` as a marked-content tag, which is the one place in this clause that is not a dictionary entry: a `BDC` whose tag is `AF` and whose property list carries the array associates those files with the graphics objects the section encloses. `Interpretation::associated_files` records it over the same readback range §14.8.2.2's artifacts use. On `BDC` alone, which is the clause's own NOTE 2 — \"[t]he BMC operator does not take properties and therefore cannot be used with the AF key\" — and the property list has to be a *named resource*, since §14.6.2 forbids an inline list from holding the indirect references an `/AF` array is made of. **The property list's key is `/MCAF`, not `/AF`, since Errata Collection 3** — Issue #374, `/State` `Review` `Completed`, whose caret puts 'a dictionary with an MCAF entry defining' in front of the clause's sentence about the property list and points it at 'Table 409a - Property list entries for associated files'. The 2020 text named no key at all, so `/AF` was an inference and a conforming PDF 2.0 file was read as stating no associated file, silently. Both keys are read since the four-hundred-and-seventeenth session, `/MCAF` first (`attachment::associated_in_property_list`); Table 409a is in neither `doc/md/` nor the annotations, so whether `/AF` should now be refused there cannot be decided from this project's copy. ADR 0253."

[[clause]]
clause = "14.13.6"
title = "Associated files linked to structure elements"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "`/AF` on a structure element, read by the same function against the element dictionary — which is where the corpus actually puts them: 30 of the 37 `/AF` arrays in the corpus are on a `/StructElem`, all of them `Supplement`, and all of them MathML from one LaTeX producer. That is the clause's own example: a MathML version of an equation \"associated using a structure element or a form XObject depending on how the equation is rendered\"."

[[clause]]
clause = "14.13.7"
title = "Associated files linked to XObjects"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "`/AF` on a form or image `XObject`'s stream dictionary, read by the same function against that dictionary. No corpus document states one."

[[clause]]
clause = "14.13.8"
title = "Associated files linked to DParts"
status = "partial"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "`/AF` on a `DPart` dictionary. The array reads like any other, and what is missing is a walk that reaches `DPart` dictionaries at all except through §12.6.4.5's jump — §14.12.4.1's row is `partial` for exactly that, `document_part::first_page` following `/DParts` only from the part a `GoToDp` names — and `DPartInteract` in §12.11's requirement table names the same gap. **This note called §14.12 \"a `silent` row of its own\"**: the ledger has had no `silent` row since Annex O's five were built, and the fourth sweep run over the noun found the claim here and in §14.12.4.1 in the same sitting."

[[clause]]
clause = "14.13.9"
title = "Associated files linked to an annotation dictionary"
status = "implemented"
code = ["crates/pdf-model/src/attachment.rs"]
test = ["crates/pdf-model/src/attachment.rs::an_associated_file_carries_its_relationship"]
note = "`/AF` on an annotation dictionary, read by the same function against the annotation. Its NOTE names the case that motivates it — a rich media annotation whose data a reader should be able to identify — which is clause 13 and excluded, and the entry is read anyway because the array is the same in every one of the clause's seven places."

[[clause]]
clause = "14.13.10"
title = "Associated file examples"
status = "inapplicable"
note = "Three worked examples — a presentation associated with the catalog, a `.doc` associated with a marked-content sequence, a MathML file associated with a form XObject. Nothing normative that §14.13.1 to §14.13.9 do not state; read and dismissed, which the ledger records rather than leaving blank."

[[clause]]
clause = "D"
title = "Character sets and encodings"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs"]
test = ["crates/pdf-font/src/encoding.rs::the_tables_encode_the_number_of_codes_the_specification_defines"]
note = "Aggregate of the six below, and all six are `implemented`. The Latin, PDFDoc, Symbol, ZapfDingbats **and expert** tables are transcribed from `doc/md/` and gated. **This row said `MacExpertEncoding` \"is refused by name and reported\" and was `partial` for it**, which the four-hundred-and-fifty-first session retired four rounds before this one when it transcribed Table D.4's 165 assignments and moved D.4 to `implemented` (ADR 0286) — the fifth failure shape, a parent row nobody maintains because the session that implements a member has no reason to open it. **This annex had no row at all until the three-hundred-and-sixtieth session** — it is normative, and the ledger's population was the eight numbered clauses (ADR 0206)."

[[clause]]
clause = "D.1"
title = "General"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs"]
test = ["crates/pdf-font/src/encoding.rs::unknown_encoding_names_are_refused_and_the_four_permitted_ones_are_not"]
note = "The annex's own table of the four predefined encodings, and its two `shall`s are met: \"PDF processors shall have a predefined encoding named WinAnsiEncoding\" and the same for `MacExpertEncoding`. The first is `BaseEncoding::WinAnsi`, transcribed here rather than copied from Windows code page 1252 — the module comment says why, and it is the difference between a right glyph and a plausible one. The second is *had* rather than *used*: the name is recognised and a font naming it is refused out loud, which D.4's row states. `PDFDocEncoding` is the one the annex says PDF has no predefined encoding for, and §7.9.2.2 is where this tree reads it."

[[clause]]
clause = "D.2"
title = "Latin character set and encodings"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs"]
test = ["crates/pdf-font/src/encoding.rs::the_assignments_made_only_in_the_notes_are_present", "crates/pdf-font/src/encoding.rs::the_encodings_differ_where_they_are_known_to_differ"]
note = "Table D.2's Latin character set — `StandardEncoding`, `MacRomanEncoding`, `WinAnsiEncoding` and `PDFDocEncoding` in four columns. Transcribed with the table's *notes*, which carry assignments the body does not show and are the entries most easily got wrong, and gated three ways: every glyph name is one the CFF specification knows, each table encodes the number of codes the annex defines, and the encodings differ exactly where they are known to differ. `StandardEncoding` is deliberately not transcribed — it is the CFF standard encoding, which `read-fonts` already carries, and one table is easier to keep right than two."

[[clause]]
clause = "D.3"
title = "PDFDocEncoding character set"
status = "implemented"
code = ["crates/pdf-syntax/src/text_string.rs"]
test = ["crates/pdf-syntax/src/text_string.rs::every_text_string_survives_the_round_trip"]
note = "`PDFDocEncoding`, the encoding of a text string outside a content stream. Read in `pdf-syntax` rather than in `pdf-font`, because §7.9.2.2 is where the type is defined and a text string is syntax rather than typography: the fourth column of Table D.2, with every code round-tripped."

[[clause]]
clause = "D.4"
title = "Expert set and MacExpert encoding"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs", "crates/pdf-font/src/glyph_names.rs"]
test = ["crates/pdf-font/src/encoding.rs::the_expert_set_is_annex_d4s_own_table", "crates/pdf-font/src/encoding.rs::the_expert_set_shares_only_punctuation_with_the_latin_tables", "crates/pdf-font/src/name_keyed.rs::an_encoding_name_the_table_does_not_permit_is_no_encoding_at_all"]
note = "Table D.4's 165 assignments, transcribed as `MAC_EXPERT` beside the other base encodings and cross-checked twice — the markdown conversion and `pdftotext -layout` over the PDF agree on every code and neither has one the other lacks, which is a check `doc/HANDOVER.md` asks for because the conversion drops content and a *table* is where to expect it. **This row read `reported` until then, and the refusal it recorded was right**: a font naming this encoding was refused by name rather than given Latin glyph names, because the annex says \"only certain fonts have the appropriate character set for use with this encoding\" and none of the standard 14 has it. What the transcription then measured is that the refusal was righter than its own reason said — **six codes mean the same thing in the expert set and in `WinAnsiEncoding`, and all six are punctuation** (space, comma, hyphen, period, colon, semicolon), so a Latin fallback would have drawn a document's punctuation correctly and every letter, ligature, fraction and small capital as something else. Not one of the 974 corpus documents names the encoding, so nothing here is demand-driven and no gate moves; what changes is that a document that does name it now draws instead of drawing nothing. The annex's own caveat is recorded beside the table rather than acted on here: \"[t]he built-in encoding in an expert font program can be different from MacExpertEncoding\", which is §9.6.5.1's question. ADR 0286."

[[clause]]
clause = "D.5"
title = "Symbol set and encoding"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs"]
test = ["crates/pdf-font/src/encoding.rs::every_glyph_name_is_one_the_cff_specification_knows"]
note = "The `Symbol` set and its encoding, one of the two symbolic fonts among the standard 14. It is not reachable through a base encoding — the glyph names of Table D.2 do not name Greek letters or mathematical operators — so it is its own table, with `/Differences` still applying on top."

[[clause]]
clause = "D.6"
title = "ZapfDingbats set and encoding"
status = "implemented"
code = ["crates/pdf-font/src/encoding.rs", "crates/pdf-font/src/loading.rs"]
test = ["crates/pdf-font/src/encoding.rs::every_glyph_name_is_one_the_cff_specification_knows", "crates/pdf-font/src/encoding.rs::the_dingbats_names_and_characters_describe_the_same_codes", "crates/pdf-font/src/encoding.rs::the_dingbats_set_is_annex_d6s_own_table", "crates/pdf-model/tests/silent_fonts.rs::a_dingbats_page_reads_back_the_characters_annex_d6_states"]
note = "The `ZapfDingbats` set and its encoding, the other symbolic font of the standard 14, on the same construction as D.5's. **And the table has a third column, which nothing read until the four-hundred-and-eighty-third session.** Table D.6 prints `CHAR`, `NAME` and `CODE`, and only the last two were transcribed — so this font's codes reached the right glyphs and meant nothing, which is the population `doc/todo/21` §5 sent a round to characterise: 114 of the corpus's 1342 unnamed codes are this one font. The `CHAR` column is the route §9.10.2's second method cannot be, and the annex says as much twice: D.1 opens \"This annex lists the character sets and encodings that shall be predefined in any PDF processor\" and adds that the dingbats \"are ordered by code instead of by name, since the names in that font are meaningless\", and §9.6.5.1 names the two symbolic standard-14 fonts as the ones \"whose encodings and character sets are documented in Annex D\". A processor predefining this set has predefined what its codes represent. `ZAPF_DINGBATS_CHARACTERS` is that column, transcribed beside the names and gated by a test that the two describe the same 188 codes — out of step by one, a code would take its neighbour's character, and nothing else in the tree could see it. **The column is the standard's own text rather than a picture of a glyph**, checked the way trap 9 asks: `pdffonts` over the two pages carrying the table lists Cambria, Calibri, Arial and MS-Gothic and no `ZapfDingbats`, so ISO typeset those characters in Unicode text fonts. Keyed by **code within this font's own encoding** and never by name, which is what keeps pdfTeX's `/a192` — a Type 3 procedure name that collides with this set — from being read as an ornament (ADR 0311's refusal is unchanged and its document is still counted). The second half of what this closed was never a count at all: a dingbat at code 0x21 used to read back as `!`, because §9.10.2's printable-ASCII permission was reached for a font whose encoding is not Latin. ADR 0318."

[[clause]]
clause = "E"
title = "Extending PDF"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "Aggregate of the two below. Every requirement in this annex is addressed to a *developer* extending PDF, and this tree is a reader of what they wrote — with one sentence that reaches it, in E.1, and that sentence points at Annex I. **Both below are settled since the five-hundred-and-twenty-fifth session** — E.1 where its pointer lands, E.2 `writer-side` — and this annex states nothing above them."

[[clause]]
clause = "E.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs"]
test = ["crates/pdf-model/src/requirements.rs::extensions_are_read_under_their_registered_prefixes"]
note = "\"Developers shall only add data to PDF files when such additions conform to this annex\" — a `shall` on a writer. The one sentence that binds a reader hands the question on: \"[i]f a user opens a PDF document using a PDF processor that does not support the developer-specific features, the PDF processor shall behave as described in Annex I\", which is §I's row, `implemented` since the five-hundred-and-twenty-fifth session — so the one sentence of this annex that binds a reader is met where it points, and this row was `partial` for a neighbour's status rather than for anything of its own. §7.12.3's developer extensions dictionary, which this annex tells developers to add, is read: `requirements::extensions` collects them under their registered prefixes."

[[clause]]
clause = "E.2"
title = "Classes of PDF names"
status = "writer-side"
note = "First-, second- and third-class names, and every requirement is on the developer choosing one: second-class names carry a four-byte registered prefix, third-class names begin `XX`. A reader treats an unknown key the same whichever class it belongs to — which is exactly what Annex I says makes the extension mechanism work — so there is nothing here to implement and nothing to report."

[[clause]]
clause = "F"
title = "Linearized PDF"
status = "out-of-scope"
exclusion = "writer-side"
note = "Aggregate of the twenty-two below. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route. Annex G, which describes how a reader *would* use the hints, is informative and outside the ledger's population."

[[clause]]
clause = "F.1"
title = "General"
status = "out-of-scope"
exclusion = "writer-side"
note = "General: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.2"
title = "Background and assumptions"
status = "out-of-scope"
exclusion = "writer-side"
note = "Background and assumptions: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3"
title = "Linearized PDF document structure"
status = "out-of-scope"
exclusion = "writer-side"
note = "Linearized PDF document structure: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.1"
title = "General"
status = "out-of-scope"
exclusion = "writer-side"
note = "General: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.2"
title = "Header (Part 1)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Header (Part 1): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.3"
title = "Linearization parameter dictionary (Part 2)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Linearization parameter dictionary (Part 2): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.4"
title = "First-page cross-reference table and trailer (Part 3)"
status = "out-of-scope"
exclusion = "writer-side"
note = "First-page cross-reference table and trailer (Part 3): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.5"
title = "Document catalog dictionary and document-level objects (Part 4)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Document catalog dictionary and document-level objects (Part 4): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.6"
title = "Hint streams (Parts 5 and 10)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Hint streams (Parts 5 and 10): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.7"
title = "First-page section (Part 6)"
status = "out-of-scope"
exclusion = "writer-side"
note = "First-page section (Part 6): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.8"
title = "Remaining pages (Part 7)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Remaining pages (Part 7): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.9"
title = "Shared objects (Part 8)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Shared objects (Part 8): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.10"
title = "Other objects (Part 9)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Other objects (Part 9): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.3.11"
title = "Main cross-reference and trailer (Part 11)"
status = "out-of-scope"
exclusion = "writer-side"
note = "Main cross-reference and trailer (Part 11): how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4"
title = "Hint tables"
status = "out-of-scope"
exclusion = "writer-side"
note = "Hint tables: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.1"
title = "General"
status = "out-of-scope"
exclusion = "writer-side"
note = "General: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.2"
title = "Page offset hint table"
status = "out-of-scope"
exclusion = "writer-side"
note = "Page offset hint table: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.3"
title = "Shared object hint table"
status = "out-of-scope"
exclusion = "writer-side"
note = "Shared object hint table: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.4"
title = "Thumbnail hint table"
status = "out-of-scope"
exclusion = "writer-side"
note = "Thumbnail hint table: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.5"
title = "Generic hint tables"
status = "out-of-scope"
exclusion = "writer-side"
note = "Generic hint tables: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.6"
title = "Extended generic hint tables"
status = "out-of-scope"
exclusion = "writer-side"
note = "Extended generic hint tables: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "F.4.7"
title = "Embedded file stream hint tables"
status = "out-of-scope"
exclusion = "writer-side"
note = "Embedded file stream hint tables: how a *writer* lays out a linearised file. Linearised PDF, and `CLAUDE.md` principle 5 excludes producing one by name: \"no clause whose requirements fall on a generator is in scope: linearisation, object-stream packing, optimisation\". **What is not excluded is reading one**, and this tree does: F.1 states that a linearised file \"shall be a conforming file\", so the hint tables are an accelerator a reader may ignore and every corpus document that is linearised opens through §7.5.4's ordinary cross-reference route."

[[clause]]
clause = "I"
title = "PDF versions and compatibility"
status = "implemented"
code = ["crates/pdf-model/src/requirements.rs", "crates/viewer-core/src/notes.rs"]
test = ["crates/pdf-model/src/requirements.rs::the_requirements_this_program_cannot_meet_are_named"]
note = "Aggregate of the three below. The annex's obligations are `should` rather than `shall`, and this tree meets one of the two: §12.11's requirements it cannot process are named to the reader, and the version number was not read at all until the three-hundred-and-sixty-first session, so a document written to a newer PDF than this program understands opened without a word. Both halves are met now (ADR 0207). **This row was `partial` for I.2's writer-side sentence until the five-hundred-and-twenty-fifth session**, where that row turned out to say in its own note that the sentence was met; all three below are settled and this annex states nothing above them."

[[clause]]
clause = "I.1"
title = "General"
status = "implemented"
code = ["crates/viewer-core/src/notes.rs"]
test = ["crates/viewer-core/tests/headless.rs::a_page_that_could_not_be_drawn_whole_says_so"]
note = "Why the annex exists, and its one instruction to a processor is a choice rather than a requirement: PDF \"enable[s] the PDF processor to ignore or inform the user about objects not understood[; t]he decision whether to ignore or inform the user is made on a feature-by-feature basis, at the discretion of the PDF processor\". This project made that decision once and wrote it into the ledger's own vocabulary: `reported` is *inform*, `silent` is *ignore*, and `silent` is a gate held at zero."

[[clause]]
clause = "I.2"
title = "PDF version numbers"
status = "implemented"
code = ["crates/pdf-syntax/src/version.rs"]
test = ["crates/pdf-syntax/src/version.rs::the_catalogs_version_counts_only_when_it_is_later", "crates/viewer-core/tests/headless.rs::a_file_newer_than_this_program_says_so_before_a_page_is_drawn"]
note = "The version number, in the two places the standard states it and in the order it ranks them. §7.5.2's header is \"'%PDF1. n ' or '%PDF2. n ' followed by a single EOL marker, where ' n ' is a single digit number\", and Table 29's `/Version` counts \"if later than the version specified in the file's header\" — \"[i]f the header specifies a later version, or if this entry is absent, the document shall conform to the version specified in the header\". `Document::version` reads both and takes the later, as a pair of numbers rather than a name, because `1.10` beside `1.7` is the string comparison that gets *later* backwards. **Read at last in the three-hundred-and-sixty-first session**; until then `xref::read` located `%PDF-` to fix the byte offsets and discarded the digits, which is what this row said. Annex I's `should` is met with it: `viewer_core::notes` says a file states a version newer than the 2.0 this program implements, before any page is drawn. **`partial` until the five-hundred-and-twenty-fifth session for the half that addresses a writer saving the file, above a sentence of its own that said the half was met**: \"a PDF file's version should never be changed to an older version\", which §7.5.6's incremental update meets by leaving the header where it found it and writing no `/Version` of its own — the note corrected and the status not, which is `doc/todo/01`'s sixth failure shape. The clause's two other writer sentences go the same way: updating the version \"if changes using newer features were made\" is a *may* this program declines, having no newer feature to write, and the version of a document \"modified by inserting the contents of another PDF file into it\" is about a verb this program does not have. And the clause's one `shall` on a processor is met by construction — \"[a] PDF processor shall attempt to read any PDF file, even if the PDF file's version is more recent than that for which the PDF processor was created\" — because nothing on the open path consults the version at all: the note is written beside the document rather than in front of it. **No corpus document reaches the warning**: of 974, the headers are 354 at 1.7, 235 at 1.4, 212 at 1.6, 104 at 1.5, 41 at 1.3, 9 at 2.0, 7 at 1.2, 5 at 1.1 and 5 at 1.0, so the note exists for a file nobody has written yet — which is the difference between coverage and demand said in one row."

[[clause]]
clause = "I.3"
title = "Feature compatibility"
status = "implemented"
code = ["crates/pdf-model/src/annotation.rs", "crates/pdf-model/src/shading.rs"]
test = ["crates/pdf-model/tests/annotations.rs::an_unknown_subtype_still_draws_its_normal_appearance", "crates/pdf-model/tests/shadings.rs::an_unknown_shading_type_is_reported"]
note = "Feature compatibility, and it is descriptive throughout: new entries in existing dictionaries are ignored by older processors, some features \"impossible to ignore\" leave \"little choice but to give an error\", and a few degrade silently. No `shall` and no `should` addressed to a processor. What this tree does with the first case is what the annex describes — an unknown key is not an error — and the second is what the corpus gate's `incomplete` verdict is for."

[[clause]]
clause = "K"
title = "XFA forms"
status = "out-of-scope"
exclusion = "xfa"
note = "Aggregate of the two below. XFA, and **`CLAUDE.md`'s reason for excluding it was half wrong until the three-hundred-and-sixtieth session**: the file said XFA is \"deprecated by ISO 32000-2 itself and specified outside it\", and this annex is *normative* and *inside* it. The exclusion survives on a better argument than the one it had — the annex grants it in as many words: \"[t]he implementation of such a schema driven page generation involves considerable effort beyond that for a simple PDF viewer and therefore **a PDF processor may choose to not implement this feature**\". What is documented separately is the *template* architecture, not the annex. ADR 0206."

[[clause]]
clause = "K.1"
title = "General"
status = "out-of-scope"
exclusion = "xfa"
note = "Why XFA exists — a form whose number of fields is decided by a schema rather than by the page — and the sentence that lets a viewer decline it: \"a PDF processor may choose to not implement this feature\". One `shall` in the clause is conditional on the choice this project did not make: \"[i]f an interactive processor supports XFA forms, that processor shall clearly indicate to the user when they are interacting with an XFA form\". And one sentence is worth carrying even so, because it decides what a *hybrid* file looks like here: \"[w]hether or not the pages generated by the processing of an XFA schema are materialized as PDF pages is implementation dependent\", so a dynamic XFA document may have nothing on its pages but a notice, and drawing that notice is drawing what the file says."

[[clause]]
clause = "K.2"
title = "Access to the XFA template and other resources"
status = "out-of-scope"
exclusion = "xfa"
note = "Where the template, the data and the configuration live: Table 224's `/XFA`, a stream or an array of named packets. Not read. **The annex's writer-side rules are what make ignoring it defensible, and they are worth stating rather than assuming**: \"[t]he other entries in the interactive form dictionary shall be consistent with the information in the XFA resource\", \"PDF interactive form field objects shall be present for each field specified in the XFA resource\" and \"[t]he XFA field values shall be consistent with the corresponding V entries of the PDF field objects\". So in a conforming hybrid file the AcroForm this tree reads *is* the form — which is a derivation from the standard rather than the usual claim that ignoring XFA works in practice."

[[clause]]
clause = "L"
title = "Parent-child relationships between the standard structure elements in the standard structure namespace for PDF 2.0"
status = "writer-side"
code = ["crates/pdf-model/src/structure.rs"]
note = "One normative table: which standard structure element may be the child of which, in the PDF 2.0 namespace. Every cell constrains the *document*'s structure tree, and a reader that walks §14.7's tree takes it as it finds it — `structure::Tree` follows `/K` and reports what is there, because a malformed nesting is still content a person needs read to them. A checker of tagged PDF would implement this table; a viewer implements §14.7 and §14.8, whose rows carry the reading."

[[clause]]
clause = "O"
title = "Fragment identifiers"
status = "partial"
code = ["crates/pdf-model/src/fragment.rs", "crates/viewer-core/src/open.rs"]
test = ["crates/viewer-core/tests/fragments.rs::a_fragment_opens_the_page_it_names"]
note = "Aggregate of the four below. **Was `silent` from the three-hundred-and-sixtieth session, when it was found, to the three-hundred-and-sixty-ninth, when it was built** — the ledger's only `silent` rows, and the only ones no file could ever have triggered, because a fragment identifier arrives with the *request* rather than in the document. `pdf_model::fragment` reads all eleven parameters and `viewer_core::Open::apply_fragment` carries out **all eleven since the five-hundred-and-twenty-second**: `page`, `nameddest`, `structelem`, `comment` and `ef` from Table Annex O.3, `zoom`, `view`, `viewrect`, `highlight`, `search` and `fdf` from Table Annex O.4. `Parameter::unhonoured` names none of them and is kept because the answer decays in both directions; `pdf-viewer doc.pdf#page=5` is the first caller. **Four have come off that list, and each for its own kind of reason.** `search` in the four-hundred-and-fourteenth, when a document-wide search existed to carry it out: `viewer_core::Command::Find` reads one page per step and `Event::Searched` says how many are left, so the annex's \"selecting the first matching word in the document\" is started as the document opens and finished by the host — 5.84 s of interpretation over ISO 32000-2's 1023 pages, which is why it is not done before `Command::Open` returns. `ef` in the four-hundred-and-seventy-fifth, where nothing arrived at all: its refusal was two claims in one coat and only one of them was ever about this parameter. `highlight` and `fdf` in the five-hundred-and-twenty-second: the first because the *annex* is what asks for the shape a host draws — `viewer_core::Query::Highlight`, on ADR 0316's precedent that a clause may ask for a message — and the second because `Event::NeedsFile` reached three hosts, so \"no host supplies one\" stopped being true while the sentence stood. ADRs 0209, 0250, 0310, 0357. **`doc/todo/01`'s arithmetic sweep flags this row and the answer is one level further down**: its two direct children are settled and what it owes is §O.2's own children, §O.2.1 and §O.2.2."

[[clause]]
clause = "O.1"
title = "General"
status = "implemented"
code = ["crates/pdf-model/src/fragment.rs"]
test = ["crates/pdf-model/src/fragment.rs::every_parameter_the_two_tables_define_is_read"]
note = "What a fragment identifier is: text after the URI that \"anchor[s] onto specific content or characteristics of the document\". Descriptive, and it is the definition the three rows below rest on — including the one thing the annex uses it for that nothing else states, which is that these are a *URI's* parameters and so are spelled the way a URI's are. Recorded `silent` beside them until the three-hundred-and-sixty-ninth session on the argument that a definition nobody has built against is not a requirement met; something is built against it now."

[[clause]]
clause = "O.2"
title = "Fragment identifiers"
status = "implemented"
code = ["crates/pdf-model/src/fragment.rs", "crates/viewer-core/src/open.rs"]
test = ["crates/pdf-model/src/fragment.rs::the_order_the_fragment_states_is_the_order_that_comes_back", "crates/viewer-core/tests/fragments.rs::a_comment_is_looked_for_on_the_page_chosen_before_it"]
note = "The grammar, and its three `shall`s are met: parameters separated by `&`, arguments by `,`, and \"[f]ragment identifiers shall be processed and (if required) executed from left to right as they appear\" — which `apply_fragment` does in one pass, so `page=3&comment=x` selects a comment on page 3 and the reverse does not, exactly as the `comment` row's own NOTE says. **Two things the annex does not state and one it states wrongly, all three found in the three-hundred-and-sixty-ninth session.** It prints \"the AMPERSAND (28h) character\" and 28h is `(`; Table D.2 gives AMPERSAND 0x26, so the reader takes the character the sentence *names*. It never states the `=` that joins a parameter to its arguments, nor gives one example, so that is a documented choice rather than a reading. And it says nothing about case, so the names are matched as the tables print them. ADR 0209."

[[clause]]
clause = "O.2.1"
title = "PDF object identifiers"
status = "implemented"
code = ["crates/pdf-model/src/fragment.rs", "crates/viewer-core/src/open.rs", "crates/pdf-model/src/destination.rs"]
test = ["crates/viewer-core/tests/fragments.rs::a_named_destination_opens_the_page_the_document_files_it_under", "crates/pdf-model/src/destination.rs::a_structure_element_named_by_id_identifies_its_page", "crates/viewer-core/tests/fragments.rs::an_embedded_file_the_fragment_names_comes_out_of_the_document", "crates/viewer-core/tests/fragments.rs::an_embedded_file_carries_the_parameters_after_it", "crates/pdf-model/src/fragment.rs::the_parameters_after_an_embedded_file_are_kept_for_it"]
note = "Table Annex O.3's five, and **all five are carried out since the four-hundred-and-seventy-fifth session**: `page` against §12.4.2's numbering from one, `nameddest` through §12.3.2.4's two tables, `structelem` through §14.7.2's `/IDTree` and then §12.3.2.3's algorithm — including the annex's own fallback, \"the first page in the document shall be identified\" — `comment` through Table 166's `/NM` on the page selected so far, which becomes §12.5.1's focus because the annex says a comment shall be \"selected\" and nothing about what that looks like, and `ef` through §7.7.4's `/EmbeddedFiles` tree. **`ef` was `reported` for a hundred and six sessions on two sentences, and only one of them was ever about `ef`.** The row's `shall` is \"the PDF processor shall open the embedded file contained within the EmbeddedFiles name tree identified by name\": §7.11.4's bytes are inside the document, so nothing is fetched, and what *opening* one means is the host's — which is what the annex says next, \"[s]ecurity should be strongly considered when opening an embedded file … a PDF processor may choose to prompt the user or even prevent opening of the file\", a `should` and a `may` addressed to whoever faces the person. So the bytes cross as `viewer_core::Event::Extracted`, the channel `Command::Extract` and §12.5.6.15's annotation already use, and no host needed a new message. **The other sentence was owed for a hundred and twenty-one sessions and is carried out since the five-hundred-and-ninety-sixth**: \"[a]ny remaining parameters after this parameter apply to the selected embedded file\" needs a *second document* opened, which `Command::Open` makes a host's — so the three parts are split where the boundary already runs. `Fragment::parse` stops at `ef` and keeps the remainder whole and undecoded in `after_embedded_file`, because splitting it is §O.2's grammar and six consumers re-deriving that grammar is six chances to disagree; `Event::Extracted` carries it beside the bytes, a variant changing shape rather than a message being added; and a host composes the two into `Command::Open`, which is where the sentence is finally true — `pdf-viewer 'issue17056.pdf#ef=destination-doc.pdf&page=3'` opens the *embedded* document at its page three, verified in the window rather than only in a test. Two policy questions rather than one, in `viewer_host::policy`: showing that file in this reader is the row's own `shall` and is answered `Ok`, writing it into somebody's directory is what the row's caution is about and is still declined for a URI. The chain terminates without a counter because the remainder is strictly shorter each time. ADR 0431. **Ten of the corpus's 964 documents carry an `/EmbeddedFiles` tree, 23 files between them**, which is the population this parameter can reach. **This row said \"[n]o corpus document has an `/IDTree`\" and it was false when written** — 12 of the 974 state one, holding between 1 and 285 identifiers apiece, and `structure::Tree::element_by_id`'s own note had already carried the correction while three other places in the tree repeated the retired sentence (ADR 0406). `structelem`'s positive half is still tested against a document written for it, because what the fixture exercises is an element whose `/Pg` is absent and whose page §12.3.2.3 has to derive from a content item; the count is what was wrong, not the fixture. ADRs 0209, 0310, 0431."

[[clause]]
clause = "O.2.2"
title = "PDF open parameters"
status = "implemented"
code = ["crates/pdf-model/src/fragment.rs", "crates/viewer-core/src/open.rs", "crates/viewer-core/src/viewer.rs", "crates/viewer-core/src/query.rs"]
test = ["crates/viewer-core/tests/fragments.rs::a_zoom_is_a_percentage_and_a_view_is_a_factor", "crates/viewer-core/tests/fragments.rs::a_view_rectangle_is_measured_from_the_top_left_corner_of_the_page", "crates/viewer-core/tests/fragments.rs::a_highlighted_rectangle_is_measured_from_the_page_corner_and_crosses_as_a_quadrilateral", "crates/viewer-core/tests/fragments.rs::the_fdf_a_fragment_names_is_asked_for_and_imported"]
note = "Table Annex O.4's six, and **four are carried out**: `view` is Table 149 read where §12.3.2.2 already reads it (`View::from_keyword`, one spelling of the table and two callers), `zoom` is `/XYZ` with two differences the annex states three rows apart — a percentage against a factor, and a corner measured from the page's top left against default user space's own origin — and `viewrect` is `/FitR` after the same conversion. The clause's coordinate rule has two halves that pull apart and both are true at once only one way: \"[a]ll coordinate values … shall be expressed in the default user space coordinate system\" gives the *units*, each row's \"top left corner of the page\" gives the *origin*. **`search` is the fourth since the four-hundred-and-fourteenth**, and it is the one this row had reported for a *capability* rather than a concept: \"Open the document and search for one or more words, selecting the first matching word in the document\". `viewer_core::Command::Find` is that search and `viewer_core::search::Searching` is the plan it walks — the word list is Annex O's own, so any of the words matching is a match, and it begins at the first page and does not wrap because \"first … in the document\" would otherwise mean nothing. **It is started when the document opens and finished by the host**, through `Event::Searched`: a step reads one page, and reading all 1023 of ISO 32000-2 is 5.84 s of interpretation, which `CLAUDE.md`'s startup rules do not permit on the launch path. \"[S]electing\" is carried out as the selection `Query::Selection` answers with, which is what every host already draws a highlight from. **The last two are carried out since the five-hundred-and-twenty-second**, and each of the two refusals expired in a different way. `highlight` — \"[o]pen the document with the specified rectangle highlighted\", with \"[t]he nature of the highlighting is implementation-dependent\" — is not the selection `search` sets, and the annex is what says so: the two neighbouring parameters that point a person at something both say *selected* and this one says *highlighted*. That reading stood; what fell was the sentence after it, \"no host has asked for one to draw\". `viewer_core::Query::Highlight` answers with the rectangle in device pixels and three hosts wash it in a colour of their own, which is precisely what an implementation-dependent nature means here — and a *clause* asking for a message is legitimate on ADR 0316's precedent, sharpened by the fact that no host can answer this one for itself: no host sees the fragment, which arrives inside `Command::Open` undecoded. `fdf` — \"[o]pen the document and then import the data from the specified FDF or XFDF file\" — was refused because \"fetching a URI is the host's, and no host supplies one yet\": the first half describes the division rather than a blocker, and the second stopped being true when `Event::NeedsFile` and `Command::Supply` reached three hosts for §12.7.6.4's import action. The name now crosses on that channel with `Purpose::ImportData` and `viewer_host::resolve_import` is the policy, so a relative URI beside the document imports and an absolute one meets whatever the host decides — no network is reached and none is needed for the clause. ISO 19444-1's XFDF is declined by name for want of an XML parser, which is the format's limit rather than the annex's and is the same decision §12.7.6.4 already takes. The annex's placement rule is met — these run \"immediately after any other document-specified open parameters\", which is Table 29's `/OpenAction` and then the fragment — and `fdf`'s own \"is recommended to be the last parameter so that the document can open directly to the appropriate view\" is met by asking for the file after the first page's events. ADRs 0209, 0250, 0357."

[[clause]]
clause = "Q"
title = "Method for determining transparency on a page"
status = "inapplicable"
note = "Aggregate of the five below. A method for answering one question: does this page contain transparency. The annex states its own optionality in a NOTE: the subset standards \"choose to mandate the use of this method because they restrict the processing of documents … PDF 2.0 is highly generalized to accommodate various processing needs so **this method is not required by this document**.\" Nothing in this tree asks the question: §11's compositing is done where the page asks for it, per object, rather than decided page-wide. `inapplicable` here means a method this document does not require and this program never invokes, not a device this program is not — the distinction ADR 0205 had to make and this row inherits."

[[clause]]
clause = "Q.1"
title = "General"
status = "inapplicable"
note = "The method's scope and its own NOTE. The annex states its own optionality in a NOTE: the subset standards \"choose to mandate the use of this method because they restrict the processing of documents … PDF 2.0 is highly generalized to accommodate various processing needs so **this method is not required by this document**.\""

[[clause]]
clause = "Q.2"
title = "Page content"
status = "inapplicable"
note = "The page-content half of the method: `/SMask` a dictionary, `ca` or `CA` below one, `BM` not `Normal`, a Type 1 pattern treated as a form XObject, and every annotation's appearance processed as one. Each of those five is read by `content.rs` where it is *used*; what is absent is the pass that answers whether any of them occurs. The annex states its own optionality in a NOTE: the subset standards \"choose to mandate the use of this method because they restrict the processing of documents … PDF 2.0 is highly generalized to accommodate various processing needs so **this method is not required by this document**.\""

[[clause]]
clause = "Q.3"
title = "Form XObjects"
status = "inapplicable"
note = "A form XObject whose `/Group` has `/S /Transparency` puts transparency on any page it is placed on, and its content stream is processed as page content. The condition is implemented as a *behaviour* — §11.6.6's transparency groups are composited — rather than as the question this annex asks. The annex states its own optionality in a NOTE: the subset standards \"choose to mandate the use of this method because they restrict the processing of documents … PDF 2.0 is highly generalized to accommodate various processing needs so **this method is not required by this document**.\""

[[clause]]
clause = "Q.4"
title = "Image XObjects"
status = "inapplicable"
note = "An image with an `/SMask` stream or `/SMaskInData` above zero has transparency. Both are read and applied by §8.9.5.4 and §7.4.9's JPXDecode path; neither is counted. The annex states its own optionality in a NOTE: the subset standards \"choose to mandate the use of this method because they restrict the processing of documents … PDF 2.0 is highly generalized to accommodate various processing needs so **this method is not required by this document**.\""

[[clause]]
clause = "Q.5"
title = "Text objects"
status = "inapplicable"
note = "Text: a Type 3 font's `CharProcs` are processed as form XObjects, because a glyph procedure is a content stream and may set a soft mask of its own. Drawn as such by §9.6.5's Type 3 route. The annex states its own optionality in a NOTE: the subset standards \"choose to mandate the use of this method because they restrict the processing of documents … PDF 2.0 is highly generalized to accommodate various processing needs so **this method is not required by this document**.\""
