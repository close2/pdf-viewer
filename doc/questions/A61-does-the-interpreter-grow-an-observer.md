Status: complete
Given: 2026-09-14, in conversation — transcribed by the round
Owes: the cross-check test, scoped to the constructs the survey walks (acting round)

> Cross-check test first.

Reading: two state machines stay, held to each other by a test over the corpus, and the observer
is revisited only if that gate fires systemically. One detail the test's round must carry, and it
is the one the question's own phrasing glosses: the comparison is scoped to **the constructs the
survey walks**. The survey deliberately does not walk a soft mask, an `SMask` image or a shading
function, so resources the interpreter selects *inside* those are not things the survey fails to
report — they are things it has decided not to judge, and an unscoped "the other way round" would
force it to report them, which changes verdicts into exactly the false failure `survey.rs`'s
header says the crate exists to avoid. Within what the survey walks, selections agree both ways.

If the observer is ever built, the shape decision this answer also settles: it exposes
`pdf-model`'s own resolved state per operator — the resources dictionary in force, the graphics
state — not the survey's questions, with the survey filtering what it does not judge and
`pdf-render` as a second consumer. A validator-designed interface living in `pdf-model` is the
one shape the layer rule forbids.