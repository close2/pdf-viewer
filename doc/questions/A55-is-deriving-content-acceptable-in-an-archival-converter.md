Agreed with the recommendation as written: offer the mode, and make it impossible to get by accident — never a default, never reachable without the configuration naming the site *and* the tool, reported per document in the words "this is derived, not original", and recorded in the file's own `xmpMM:History`. Refusing to offer the mode does not keep archives faithful; it drops the annotation instead, which loses more and says less.