Status: complete
Given: 2026-09-12, in conversation — transcribed by the round
Owes: the derive guardrails in RFC 0007's implementation (acting round)

> Q55 agree with recommendation

Reading: the recommendation of the `Q` file beside this one, adopted as written — offer the mode,
and make it impossible to get by accident: never a default, never reachable without the
configuration naming the site *and* the tool, reported per document in the words "this is derived,
not original", and recorded in the file's own `xmpMM:History`. Refusing to offer the mode does not
keep archives faithful; it drops the annotation instead, which loses more and says less.
