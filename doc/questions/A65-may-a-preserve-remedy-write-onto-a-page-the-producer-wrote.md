Status: complete
Given: 2026-09-14, in conversation — transcribed by the round
Owes: the fourth amendment to `CLAUDE.md`'s authoring exclusion, naming the bounded class; the two refusals with sentences — the unbalanced-`q`/`Q` producer beyond the surviving construction, and the annotation draw-order overlap computed from the rectangles (acting round)

> Rule provenance.

Reading: the fence's sentence — "it composes new content over pages" — is about **the
provenance of the marks**, not about the operation of writing. The program decides nothing a
reader will see in the smaller construction: the appearance stream is the producer's, §12.5.5
fixes the placement to the matrix, and the operators added are that placement. This is the
fourth argued amendment to the exclusion, and it names the bounded class it opens:
**clause-driven relocation of producer-written appearance streams** — a clause requires the
content to move and fixes where it goes. It will take in later sites of the same shape, a
flattened form field among them, and it stops where the marks become this program's invention:
the watermark, and the redaction's overlay text and fill, stay on the far side.

The two honest costs become refusals with sentences rather than silent behaviour: a producer
whose content pops further than it pushed, beyond the surviving `q`-prepending construction, is
refused by name; and a moved appearance that would change what a reader sees under an annotation
that remains is detected from the rectangles and refused by name. ADR 1099's appended page
stands — as the fallback where the page-written construction is refused, and as the whole
permission for the genuine append case, an embedded image carried as a page of its own.