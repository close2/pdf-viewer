Status: void
Given: 2026-09-14, in conversation — transcribed by the round
Owes: none

> Close as void.

Reading: the question dissolved when session 1007 measured what it asserted (ADR 1026), and the
retraction — the useful part, which is why the `Q` file is kept — was the asking round's own.
Both premises failed on the drawn side: nine documents on this disk are converted with one of the
two rewrites applied (`PostScriptXObject` on two, `SymbolicTrueTypeEncodingRemoved` on seven), so
retiring either would remove a remedy that runs; and 25 of the 43 symbolic cases are refused by
`sites::selects_the_same_glyphs` rather than ADR 0816's fence, so "reopen the fence" would not have
reached what the question thought it would. There was never a decision left for the owner.

What the question leaves behind, and where each lives: `examples/withdrawn.rs`, the instrument
that counts reach per requirement — it exists because the question could not be settled without
it; the eleven of 175 implemented rows that fail no document in the seven read corpora — ADR 1026
§5.1, `doc/todo/62`; and the lesson, trap 25's shape in a new place: a population asserted from
the fixtures in front of the author, where the corpora had the drawn case in numbers.