Status: complete
Given: 2026-09-12, in conversation — transcribed by the round
Owes: the two-switch departure design in RFC 0007's implementation (acting round)

> Q59 agree with recommendation

Reading: as written in the `Q` file — a departed conversion omits the PDF/A identification by
default, and an operator may demand the claim with a second, separate switch that the departure
does not imply. "Depart from a clause" and "claim conformance anyway" are two decisions, taken
twice; a downstream validator fails the file either way, and the only difference is whether the
file lied before it failed, which is the operator's to own explicitly rather than acquire as a
side effect.
