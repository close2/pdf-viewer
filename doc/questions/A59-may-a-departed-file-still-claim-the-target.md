Agreed with the recommendation as written: a departed conversion omits the PDF/A identification by default, and an operator may demand the claim with a second, separate switch that the departure does not imply — "depart from a clause" and "claim conformance anyway" are two decisions, taken twice. A downstream validator fails the file either way; the only difference is whether the file lied before it failed, and that difference is the operator's to own explicitly.