Status: complete
Given: 2026-09-12, in conversation — transcribed by the round
Owes: the `Lexical::Date` widening from the held working draft (acting round)

> I have downloaded iso-tc154-wg5_n0038_iso_wd_8601-1_2016-02-16.pdf; keep the GPSCoordinate
> check

Reading: the first point is settled — the `GPSCoordinate` check stays, A020's list withdrawing
nothing about a type it does not name. The download takes the second point's option 3: the text is
held (`doc/iso-tc154-wg5_n0038_iso_wd_8601-1_2016-02-16.pdf`, ISO/TC 154/WG 5 N0038, ISO/WD 8601-1
*Basic rules*, 2016-02-16), and `Lexical::Date` is widened to the ISO 8601 forms that text defines
rather than to an enumeration of them. Two things the acting round carries: the ground is the
working draft, not the published ISO 8601-1:2016 — its own cover says it "is not an ISO
International Standard" — so the check's citation names the WD as what is held, and if the
difference ever matters the published part is the purchase to make. And the widening is implemented
from the text alone, per principle 5; the XMP Specification's six profiles remain the forms an XMP
packet is *written* in, and what changes is what the check admits, not what the converter emits.
