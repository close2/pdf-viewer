I have downloaded the working draft — `doc/iso-tc154-wg5_n0038_iso_wd_8601-1_2016-02-16.pdf`, ISO/TC 154/WG 5 N0038, ISO/WD 8601-1 *Basic rules* (2016-02-16) — so the second point takes its option 3: the text is held, and the `Lexical::Date` check is widened to the ISO 8601 forms that text defines rather than to an enumeration of them. Two things the round that acts on this should carry: the ground is the working draft, not the published ISO 8601-1:2016 — the draft's own cover says it "is not an ISO International Standard" — so the check's citation names the WD as what is held, and if the difference ever matters the published part is the purchase to make. And the widening is implemented from the text alone, per principle 5; the XMP Specification's six profiles remain the forms an XMP packet is *written* in, and what changes is what the check admits, not what the converter emits.

Keep the `GPSCoordinate` check — the silence of A020's list withdraws nothing about a type it does not name.