# 1077 — The refusal that moved a conforming font's glyphs, and 297 pairs judged against no band

The corpus slot; the oracle's undiagnosed head is empty for an eighth round. **The brief called the
37 "words the reference finds and this tree does not"; they are not.** `11094/11131` is the *geometry*
gate's verdict — 37 **matched** words, found by both readers, whose boxes are out of bounds.

## The 37, by principle 5's three cases — none of them an invented word

- **24, the file broke it.** No usable metrics stated, so each reader substitutes and the delta grows
  along the word: `issue20232.pdf` (15, no `/BaseFont`), `issue2391-2.pdf` (4, `/Monaco`, no
  `/Widths`), `issue18099_reduced.pdf` (2, same), `issue14497.pdf` (2), `TrueType_without_cmap.pdf` (1).
- **11, the instrument's frame rather than a glyph.** Under a rotated, sheared or vertically-set text
  matrix an axis-aligned box carries each extractor's ascent/descent convention on *both* axes — ADR
  0323 Finding 3's excluded quantity: `issue1905.pdf` 5, `issue11555.pdf` 3, `vertical.pdf` 2,
  `bug1771477.pdf` 1.
- **2, ours — and the list's last undiagnosed entry is diagnosed.** `issue6127.pdf`'s `(réf.` and
  `S3182).` sit 3.0158 pt *before* both references. Page one states
  `/C2_14 1 Tf  5.737 0 Td  <0003>Tj  /C2_2 1 Tf  [<0003>187<000b>…]TJ`; `/C2_14` is `/TimesNewRoman`
  `/Type0` `/Identity-H` over a `/CIDFontType2` with no program, which §9.7.5.2 forbids outright — so
  the file broke it and `pdf_font`'s refusal is right. **The refusal does not stay inside the font it
  is about**: `show_text` returns before §9.4.4's `t x` is computed, so the `TJ` after it — drawn by
  `/C2_2`, a font this tree *does* load — begins where the `Td` left the pen. That displacement is
  the file's, not the program's (§9.7.4.3; the descendant's `/W` states 250 for CID 3): `t x` =
  (0.25 + `Tc` 0.0013) × 12.0008 = 3.0158 pt whatever the program does, and both references advance
  it. The two earlier `/C2_14` spaces are each followed by a `Td`, which is why only these two move;
  closing it wants a font carrying the dictionary's metrics and no program — `pdf-font`'s to build.

## The cross-axis count was 297 pairs too large

`the_band_the_file_states` walked the page's resources and its form `XObject`s'; its own comment named
§12.5.5's appearance streams as the narrow edge and assumed that cost nothing. It cost 297 pairs on
four forms whose matched words a widget's `/AP` `/N` drew — `prefilled_f1040.pdf` 258,
`issue12706.pdf` 34, `js-buttons.pdf` 3, `resetform.pdf` 2, the last of them the mechanism whole: its
page states `/Resources << >>`, so a walk found no `/Font` to refuse and answered *yes* to a question
it had asked nothing of, while the words come from `/HeBo` — the standard-14 `/Helvetica-Bold`, no
`/FontDescriptor`, so no Table 120 pair anywhere. `CROSS_AXIS_FLOOR` 8563 → **8266**;
`JUDGED_FLOOR` 503, verdict 11094/11131 and `SELECTION_BELOW_FLOOR` all unchanged, so this
took a claim off the instrument rather than a failure off the tree. Table 57's `/ExtGState` `/Font`
was tried on the same walk, fires on no corpus document, and is therefore not in the code: an
unreached branch is not evidence (traps 8, 13).
