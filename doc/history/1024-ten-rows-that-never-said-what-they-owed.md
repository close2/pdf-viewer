# 1024 — Ten rows that never said what they owed

Round 1017 handed back ten `partial` rows whose notes do not name a residue, two contradicting a
second row about the same entry. Each was read against its clause and its code; five keep `partial`
with the debt at the top of the note, five had an empty residue and moved.

## Moved, each on the clause read whole

| row | to | the residue, and why it is empty |
|---|---|---|
| §12.5.6.20 | `implemented` | the subclause states no requirement of a processor: Table 398 and every `shall` about a mark are §14.11.3's, whose row keeps the debt (`/MarkStyle`, `/MN`) |
| §12.5.6.21 | `implemented` | Table 403 is a writer's staleness test whole; the only act "shall be regenerated" names is adding marks along colour boundaries, which is principle 5's authoring exclusion |
| §12.7.5.2.3 | `implemented` | nine sentences, every one executed — appearance per state, `Off`, `/V`, the `/V`-and-`/AS` invariant, `/AS`'s precedence, `/Opt`, and the `RadiosInUnison` paragraph |
| §12.7.5.2.4 | `implemented` | the same, per widget, with the clause's own conflict read rather than departed from |
| §14.8.5.4.3 | `implemented` | `/BBox` is executed; the other twelve are §14.8.5.4.1's "parameters of the layout process", inapplicable to a program that exports to no format |
| §12.7.5.2 | `implemented` | forced: the standard prints no text under the heading, and with its last child settled `Problem::AggregateWithoutDebt` fires on it (ADR 1035) |

## Kept `partial`, with the debt named

- **§12.2** — the print half of Table 147: eight entries read and handed to a host, nothing prints.
- **§12.5.6.8** — Table 180's `/BE`: Table 169's cloudy style states no curve, so it is refused.
- **§12.5.6.23** — applying the redaction, **owed rather than excluded**. Its argument ("the one
  edit an incremental update cannot express") was overtaken by RFC 0002's serializer; the conclusion
  survives narrower — RFC 0002 section 10 copies stream bytes untouched, so nothing here rewrites a
  content stream — and removing content composes no marks, so no exclusion reaches it.
- **§12.6.4.15** — two: the four Table 164 styles no frame is shaped for, shared with §12.4.4; and
  **a transition action outside presentation mode is not animated**, this subclause's own departure,
  because unlike §12.4.4.1 it states no presentation condition.
- **§14.7.4.2** — the closing paragraph's equivalence between a namespace name and a Table 376
  owner value is computed nowhere; settling it needs a list the clause declines to give.

## The two contradictions, and one repair that was not this round's work

`/MN`: §12.5.6.20 was right that nothing reads it; §14.11.3's sentence claiming the other row
reported it is corrected. `/LastModified`: §12.5.6.21 was right that Table 403 is writer-side;
§14.11.6.2's "one reader-side sentence" is corrected, its row settled, `doc/todo/01`'s entry closed.

`doc/conformance/ledger.toml` would not parse: a §12.8 row written this batch carried `\uXXXX`
escapes, which `toml_subset` rejects. Decoded in place — content identical — because an unparseable
ledger stops tier 1 for every round sharing the worktree.
