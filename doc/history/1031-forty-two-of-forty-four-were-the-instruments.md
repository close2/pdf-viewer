# 1031 — Forty-two of forty-four were the instrument's, and one was a table

Instruments slot of batch 1026–1031, five siblings in one worktree. Subject: the four
contradictions session 1025 left with the verdict *act*; ADR 1048 is the argument and the rule.
**All 44 hits were read: 42 the instrument's rule, 1 a live ledger defect, 1 left standing.** None
was resolved by changing a status, and nothing was filtered — every demoted hit still prints, with
the mark that demoted it.

| sweep | was | now | which half was false |
|---|---|---|---|
| `blockers` | 21 source + 9 ledger expired | **0** assert a dependency in the present tense | the instrument's: `while` and `until` are conjunctions; 3 more a past tense the adjacent auxiliary makes visible |
| `tables` | 7 denials a table contradicts | **6** | the instrument's, for one: a negation with a noun of its own between it and the key |
| `counts` | 5 places counting one family twice | **1** | the instrument's: four pairs sat under leaf rows, counting a raster row, a table row and an erratum's row |
| `overstated` | 2 of 9 unmarked | **1 of 9** | one the ledger's (below); one by construction — a denial about another term — plus a new ellipsis shape found mid-round |

**1. The live defect is a table.** §8.11.4.3 said "`partial` for one entry now: `/Configs` is
unread" inside an inventory opening "Table 99, all of it but two entries", then named three
entries. Table 99 states eleven and `/Configs` is **Table 98's**, §8.11.4.2's row's. The debt stays
here — this clause's own opening sentence says what the alternate configurations are for — but the
table and the count were wrong. `--bin tables` could not see it (the row never writes the two nouns
adjacently); `--bin overstated` found it by pairing parent against child.

**2. Four instrument changes, each with a calibrating pair** — `blockers::CONJUNCTIONS` and
`PAST`; `tables::denies` requiring the negation adjacent to the key; `counts` refusing to pair two
numbers under a clause the ledger gives no rows below; `overstated::excepted` plus
`Rung::Elsewhere` demoting itself. Every test asserts both that the new rule fires and that the
sentence one word away does not (trap 13), and each module gained the shape it was blind to.

**3. `inapplicable`'s 253 was read at the head and left alone.** The 27 terms with three
witnessing files or fewer, over 17 rows, each resolving as two rows about different sentences:
`LineHeight` under a font's metrics, `Ruby` the element against `RubyAlign` the attribute,
`PrintField` named by §14.8.5.4.1 only to exclude it. The rarity sort is doing its job; the tail is
this project's own vocabulary — "Table", "ADR", "One" — and sorts last by construction.

**Gates.** `cargo test -p conformance` green throughout the round; `cargo fmt -p conformance
--check` and `RUSTFLAGS="-D warnings" cargo clippy -p conformance --all-targets` clean. `--bin
quotations` and `--bin pointers` re-run for the two documents added: no new divergence, no new
absent pointer. The last run of `--test conformance` fails on eight rows naming
`pdf-signature/src/revision.rs`, `pdf-syntax/src/file.rs` and `viewer-core/tests/headless.rs` — a
sibling's in-flight work, none of it a file this round touched. No pixel moved.
