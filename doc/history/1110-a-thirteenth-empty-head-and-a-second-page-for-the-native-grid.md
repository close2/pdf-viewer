# 1110 — A thirteenth empty head, and the second page whose grid is native after the reduction

2026-09-15. Files: `crates/render-raster/{examples/image_phase.rs,tests/corpus.rs}`,
`doc/QUORRA_FEEDBACK.md` §48, this file. No ADR: the one reading decided is a clause applied to a
placement, and §48 is where a later round finds it. Every other path in `git status` is a sibling's.

**The oracle's undiagnosed head is empty a thirteenth round** — `1962 pages in 60.9s`,
`agrees 1012, contradicted 46, ambiguous 835`, "ambiguous, undiagnosed" printed with nothing under
it — so the contract was the `render-raster` gate's populations: `958 compared, 943 agree, 8 differ,
7 refused, 16 not comparable`.

## The eight, each read on this build — none closed, none drawn wrong

ADR 1102's six left and stayed out. `examples/ink_ladder` sorts the rest: a gap that halves at
every rung is a boundary, a gap that does not is a rule.
- **Named edits, all raster's, all filed.** `22060_A1_01_Plans` (§46), `issue19083` (§24c — 396.96
  climbing to 447.93 against a flat 448.3, the clip-against-the-mark product in ink), `issue20232`,
  `issue21068`, `issue15150` (§45's clamped winding integral), `pr12564`, `issue269_2` (§47/§48).
- **Standing.** `issue2177` — a tiling pattern of ellipses, so almost every inked pixel is a curve
  boundary; heavier at 1× than at 8× on both backends, the side §10.7.4's third sentence permits.

**`issue269_2` was misclassified and is `pr12564`'s rule one reduction earlier.** Its ink is
142 089.03 v 142 078.17 at 1× and **141 996.09 on both** at 2×/4×/8× — same ink, different pixels.
One 200×200 image drawn 32× at 100×100 (`image_region_census`), ratio exactly two, so the reduction
leaves no residual and the grid is native; `render-cpu` asks `is_smoothed` of the grid it draws and
point-samples per §10.7.4, while `render_raster::scene` hands the stated samples over for quorra's
copy to decide after its own reduce. `image_phase`'s new third rung — sixteen rows in like pairs
onto eight device pixels — reproduces the 1:1 rung's two columns to the thousandth at all ten
phases. §48 is the ask, §47's with the grid named.

## The twenty-three re-confirmed, and two stale numbers corrected

Every class holds every member; none emptied, none moved to *compared*. A ten password (§7.6.4.1),
B `PDFBOX-4352-0`'s null `/Encrypt`, C five with no page one, D four refused (§43), E two budgets,
F one capability. `ContentStreamCycleType3insideType3` prints the resource-cache ceiling its own
note predicts from where the walk reaches it.

**No ratchet moved.** Two feedback figures did, both because ADR 1102 moved the *oracle* after they
were written with quorra's column unchanged: §45's `issue20232` excess is +22.7% (was +31.3%), §46's
1× oracle column 40 501.30 (was 40 681.01). Corrected at source. Gates are in the report.
