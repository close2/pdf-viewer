# 1070 — The seventeen were six facts, and one of them was the instrument's

2026-09-15. No ADR: the budget's reason sits above the constant, the ask is a feedback section. The
oracle's undiagnosed head was empty a seventh time (`tools/state.sh oracle`, 0, head blank), so the
contract was `render-raster --test corpus`'s two unread figures. Files: that gate,
`doc/QUORRA_FEEDBACK.md` (section 44, new), `doc/todo/00`, this file. **The six refused are whole
and stayed whole** — four constructions the scene vocabulary cannot state (§11.6.6, §11.7.2 at a
group's `Do`; §11.5.3's luminosity) and two budgets, all drawn by the CPU backend. One correction:
the cycle page's figure is a *function of the run* — 377 221 152 scene-derived bytes alone against
536 871 684 resource bytes in the corpus, one rasteriser serving 974 documents — not the page's.

**The seventeen were one count over six causes reached by four `continue`s.** Named now, by cause,
in `NOT_COMPARABLE`, held both ways. Eleven do not open — ten wanting a password §7.6.4.3 does not
supply, one whose `/Encrypt` (§7.6.2) resolves to nothing — and five yield no first page;
`oracle.rs`'s `NO_RENDER_*` and `pdf-model`'s `MAX_UNREADABLE_ENCRYPTION`/`MAX_PAGELESS` had read
every one, so the mapping is cited rather than re-derived; all are principle 5's second case. Four
causes are empty at 1×, each planted and watched to name itself (trap 13), and one of the four is a
measurement: **no page of the 974 was refused by the oracle**, so principle 2's fallback drew every
page the device would not.

**The sixth cause was hiding a page, and the bound was the instrument's.** `PIXEL_BUDGET` was `64 <<
20` under *"generous enough that no real page reaches it"*; `issue19517.pdf` is 12608x16806.
`viewer_core::MAX_PIXELS` is `1 << 28` — the product draws the page — and the oracle gate's reason
for holding its copy down (212-megapixel reference renders) does not reach a gate that spawns none:
1.03 GiB, 22 s. With the product's number the page is refused by a capability, *target 12608x16806
exceeds this adapter's limit of 16384 pixels per side*: a page a person opens at 100% and does not
see, which is what `REFUSED_BY_THE_DEVICE` exists to name, and section 44 carries it and
`issue9418.pdf`'s 4× coverage-sheet ceiling upstream. Trap 25's amendment is in `doc/todo/00`.

Gates. `fmt --all --check` 1 (siblings only; `-p render-raster` 0). `clippy -p render-raster
--all-targets --no-deps` 0 under `-D warnings`; workspace clippy 101, 21 errors, 20 `render-cpu` and
1 `pdf-signature`, and those plus `pdf-render` were un-buildable much of the round, so `nextest
--workspace` and the doc tests are the merge's. `fuzz/` 0, 0; `conformance` 0, 251 passed. `--test
corpus` **0**, *958 compared in 41.1s: 930 agree, 21 differ, 7 refused, 16 not comparable*; at scale
4 **0**, *957 in 356.5s: 939, 10, 8, 17*, peak 6.54 GiB. `oracle` 0, *1957 pages in 34.4s*, head
empty. **Two gates red on a sibling's work**: `raster_golden` 101, *held 244, moved 730*, every
mover "raster only" — `pdf-model` depends on `render-cpu`, not `render-raster`; and `--test corpus`
re-run at 04:43, after `pdf-render/src/{crop,edge,lib}.rs` changed at 04:35, gives *936 agree, 15
differ* on the same 7 and 16 — seven left `differing_pages()`, `issue2177.pdf` arrived: that round's
list, arrival first.
