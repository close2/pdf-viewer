# How the project got here

Status: **record** — appended to, never rewritten.
Read by: whoever wants to know when something landed and which ADR argues it.

One line per session; the argument is in the ADR, and every durable lesson is in `doc/HANDOVER.md`'s
Traps, in `doc/habits.md` or in the todo file that owns the work, rather than here. This table was
`doc/HANDOVER.md`'s last section until the three-hundred-and-ninety-fifth moved it out: it is
narrative by that file's own definition, and a round needs it only when it is asking *when*.

The block summary below it is the same shape one level up — what a run of twenty rounds had in
common — and it is kept because it states a finding the per-session rows cannot. The one
measurement lesson that block carried went to `doc/habits.md`'s "Measuring", where it is used.

| Session | What landed | Where |
|---|---|---|
| 5 | The reference oracle, over every page of the corpus | ADR 0011 |
| 6 | `CalGray`/`CalRGB` through XYZ; annotation appearance streams | 0012, 0013 |
| 7 | JBIG2 and JPEG 2000 in a sandboxed worker; the first speed comparison | 0014 |
| 8 | §9.6.5.4, the `TrueType` code-to-glyph algorithm, in full | 0015 |
| 9 | The conformance ledger and citation checker; optional content | 0016, 0017 |
| 10 | Type 3 fonts; dashed lines, which had never been dashed | 0018 |
| 11 | Inline images; `/Interpolate`; `Indexed`, `Separation`, `DeviceN` images | 0019 |
| 12 | A cache for the oracle's reference renders; `CCITTFaxDecode`; `/Rotate` | 0020, 0021 |
| 13 | All eight text rendering modes; §9.3 and §9.4 reviewed | 0022 |
| 14 | `/Mask` in both forms; §11.6.4 reviewed | 0023 |
| 15 | Soft masks at any resolution and `/Matte`; a shading carries `ca` | 0024 |
| 16 | Area averaging for reduced images; §10.7 reviewed, and it forbids what was built | 0025 |
| 17 | Transparency groups; the page group is isolated | 0026 |
| 18 | Soft masks in an `/ExtGState`; overprinting is silent | 0027 |
| 19 | `/SA` and the device's thinnest line; overprinting is *not* a gap | 0028 |
| 20 | Embedded `CMap`s and `/CIDToGIDMap`; the whole of §9.7 reviewed | 0029 |
| 21 | Constructed annotation appearances; `/CA` belongs to the construction | 0030 |
| 22 | Encryption, every revision and method §7.6 states; a locked file is not unreadable | 0031 |
| 23 | §12.7.4.3's variable text; regenerating an appearance is a splice | 0032 |
| 24 | §8.5.3.2's degenerate strokes; an empty clipping path admits nothing | 0033 |
| 25 | §8.9.5.2's `/Decode` array in full; a fast path inherits no clauses | 0034 |
| 26 | An image's colour space is a fill's; an exact memo where a grid was obvious | 0035 |
| 27 | `LZWDecode`, the last standard filter; a corpus stating an invariant about itself | 0036 |
| 28 | A shading's `/BBox`; a contradicted page's diagnosis refuted by measuring it | 0037 |
| 29 | `/UserUnit`, and the geometry list emptied | 0038 |
| 30 | An embedded program's own encoding is the base; `/MissingWidth` is 0 | 0039 |
| 31 | Bare Type 1 fonts (`/FontFile`); the oracle's tolerance asks whether glyphs were drawn | 0040 |
| 32 | All five bit depths; an inline image's abbreviated keys win; `BX`/`EX` | 0041 |
| 33 | §10.4.2.5 exists; Table 57's `/Font`; the whole of clause 10 reviewed | 0042 |
| 34 | §12.5.6.10's text markup appearances; `REVIEW_OWED` emptied | 0043 |
| 35 | §8.11.4.4's usage application dictionaries — the last original `silent` row | 0044 |
| 36 | Vertical writing: §9.2.4's second set of metrics, §9.7.4.3's `/W2` and `/DW2` | 0045 |
| 37 | The blend-mode scene nobody had written; clause 11 completed as a review | 0046 |
| 38 | Clause 8 completed as a review — the graphics clause, 20 rows | 0046 |
| 39 | §11.3.5.3's four modes taken back from `tiny-skia` | 0047 |
| 40 | Four unexplained pages are one shared ICC profile; clause 7 complete | 0048 |
| 41 | A CID into a bare Type 1 program; clause 9 complete | 0049 |
| 42 | A suffixed glyph name is the program's, not the AGL's | 0050 |
| 43 | One mesh raster instead of 4096 flat triangles | 0051 |
| 44 | A font's own tables say which way round its offsets are | 0052 |
| 45 | A contradicted page's label measured and replaced; §12.6's actions reviewed | — |
| 46 | Clause 12 completed as a review; the median page profiled at last | — |
| 47 | A negative line width is a choice, written down; §14.7 reviewed | — |
| 48 | Name and number trees, and §12.4.2's page labels on top of them | 0053 |
| 49 | §12.3.2's destinations, all three spellings | 0054 |
| 50 | §12.3.3's outline, and a `/Count` the clause states as an algorithm | 0055 |
| 51 | A tiling pattern's cell clipped to its `/BBox`, per cell | 0056 |
| 52 | A pattern inside a form maps to the form's space | 0057 |
| 53 | A glyph filled with a tiling pattern is tiled | 0058 |
| 54 | A ramp that can hold a step | 0059 |
| 55 | §14.9.4's `/ActualText`, and the property list that was never a dictionary | 0060 |
| 56 | **The ledger reaches zero unreviewed rows** | 0061 |
| 57 | A click follows a link | 0062 |
| 58 | Everything re-measured, and one feature's cost attributed | — |
| 59 | The corpus's own bug trackers read; a written conclusion corrected | — |
| 60 | §14.9's four accessibility entries, in both places each may sit | 0063 |
| 61 | The page's top edge is raster row zero; 11 contradicted pages agree | 0064 |
| 62 | §12.6's actions, and the third input a viewer has | 0065 |
| 63 | A third gate: the text, over the whole corpus | 0066 |
| 64 | §9.10.2's closing sentence is a permission, and three documents took it | 0067 |
| 65 | A ramp is not a gradient: 144 G instructions become 54 G | 0068 |
| 66 | §14.9 completed, and the text gate's remaining list is all naming | — |
| 67 | Table 99's layer-panel half: `/Order`, `/ListMode`, `/Locked` | — |
| 68 | Two contradicted pages are §10.7.4's own departure, measured | — |
| 69 | §12.4.4.2's sub-page navigation, on the control two sessions built | — |
| 70 | §12.4.4.1's page transitions, read from Table 164 and played by nobody | — |
| 71 | §11.4.6's knockout groups, drawn where a shape is a coverage | — |
| 72 | §9.3.8's text knockout and §11.6.2's one object in parts, on it | — |
| 73 | One shading object, built once: a fifth of the corpus's worst page | 0069 |
| 74 | §10.7.3's `/SM`, the silence that was hiding inside a `partial` row | — |
| 75 | Eight "unexplained" contradicted pages are one population, measured | — |
| 76 | Table 170's rollover and down appearances, on the pointer | — |
| 77 | §12.6.3's trigger events, and the one precedence rule Table 197 states | — |
| 78 | §14.7.2's structure tree, read downwards at last | — |
| 79 | Everything re-verified, including the fuzzers and `cargo deny` | — |
| 80 | §12.6.4.8's URI by RFC 3986, and §12.6.4.12's four page commands | 0070 |
| 81 | §12.2's viewer preferences, and the two that decide which boundary is displayed | 0071 |
| 82 | §14.7.6's attributes and §14.7.4's namespaces | 0072 |
| 83 | §14.8.2's artifacts and reversed show strings; an inline array nobody parsed | 0073 |
| 84 | §7.12's extensions and §12.11's requirements | 0074 |
| 85 | §12.5.6.7's leader lines, and the first corpus ratchet in six sessions | 0075 |
| 86 | §7.11.4's embedded files, listed; everything re-measured | 0076 |
| 87 | §14.13's associated files: one array in seven places | 0077 |
| 88 | §14.8.4's forty-one standard structure types | 0078 |
| 89 | §14.8.5's owners and the five-step priority for an attribute's value | 0079 |
| 90 | §12.4.3's articles and §12.6.4.7's thread action | 0080 |
| 91 | §12.3.4's thumbnails, and the page as its own producer drew it | 0081 |
| 92 | §12.9's viewports and §12.10's geospatial dictionaries | 0082 |
| 93 | §12.3.5's collections, §12.3.6's navigators, clause 7's last two silences | 0083 |
| 94 | §14.8.2.5's logical content order | 0084 |
| 95 | §14.8.5.6's `PrintField`: clause 14 reaches zero silences | 0085 |
| 96 | §9.8.3's substitution hints, the ledger's oldest silences | 0086 |
| 97 | §12.7.6.3's reset-form action, and nine refusals the viewer had swallowed | 0087 |
| 98 | §12.8's signatures: what a renderer can say about one without a trust store | 0088 |
| 99 | §12.8.4's store, §12.8.7's attestations, and everything re-verified | 0089 |
| 100 | §12.7.8's forms data format read and §12.7.6.4's import performed | 0090 |
| 101 | §12.7.7's named pages, and **the ledger reaches zero silences** | 0091 |
| 102 | §7.9.4's dates audited over 1542 corpus strings; §8.9.5.4's alternate images | 0092 |
| 103 | §11.4.4's NOTE 5: the non-isolated group that need not be built | 0093 |
| 104 | §12.6.4.4's embedded go-to: the document inside the document | 0094 |
| 105 | Table 192's `/R` drawn; §12.5.6.23's overlay read as writer-side | 0095 |
| 106 | A fifth fuzz target; everything re-verified | 0096 |
| 107 | Two recovery rules from the file's own declarations: 11 pageless → 5 | 0097 |
| 108 | §7.10 audited out of the file-only evidence population, and two claims corrected | 0098 |
| 109 | §12.6.4.15's transition action, which is §12.4.4's table at a different moment | 0099 |
| 110 | §7.5's free entries: an update that deletes an object is no longer undone | 0100 |
| 111 | Clause 8's file-only rows audited; a retired claim found in four places | 0101 |
| 112 | The file-only evidence population reaches zero, over four audits | 0102 |
| 113 | A to-do item measured at a hundredth of its listed price, and removed | 0103 |
| 114 | §7.11's file specifications, read whole and opened never | 0104 |
| 115 | Five `reported` rows that understated what the tree already does | 0105 |
| 116 | §12.5.6.7's `/LE` and `/Cap` named beside the line rather than instead of it | 0106 |
| 117 | §11.6.7's implicit group: a pattern's alpha belongs to the pattern | 0107 |
| 118 | §12.6.4.5's `GoToDp`, and a sweep for reasons that had expired | 0108 |
| 119 | Everything re-verified: four gates, five fuzzers, both performance numbers | — |
| 120 | §12.5.6.4's seven icons: the *shall* hiding behind a silence about artwork | 0109 |
| 121 | §11.7.4.4's other half: a glyph filled and stroked is one object too | 0110 |
| 122 | A sweep for entries claimed unread; an `/Encoding` name that erased a font | 0111 |
| 123 | A `/DA` font `/DR` lacks is stood in for — and the stand-in must not fall short | 0112 |
| 124 | Everything re-verified; the interpretation drift floor measured as an A/B | — |
| 125 | An appearance with no `/BBox` gets §12.7.4.3's default box rather than a refusal | 0113 |
| 126 | And the same rule the other way: no `/Rect`, so the appearance's own box | 0114 |
| 127 | The font cache was keyed by a resource *name*; a form's `/F1` is not a page's | 0115 |
| 128 | Every other cache's key checked; the decompression item priced and found small | — |
| 129 | Everything re-verified after ten sessions of change | — |
| 130 | MIT; `CLAUDE.md`'s writer exclusion amended; §0's UI boundary specified | — |
| 131 | `viewer-core` is real: a vocabulary, a scheduler, and a consumer with no display | 0116 |
| 132 | The window becomes a consumer; a locked file is asked for its password at last | 0117 |
| 133 | The text layer, and the click that had been mapped to the wrong half of the page | 0118 |
| 134 | Text is selected, and the shapes cross as geometry for the host to draw | 0119 |
| 135 | An edit is a log beside the document; a replaced value was not being drawn | 0120 |
| 136 | §7.5.6's incremental update: the one kind of writing this project does | 0121 |
| 137 | The ledger re-read against six sessions of new capability; `/H` became reachable | 0122 |
| 138 | Table 192's `/H`: the clause about a moment that had never happened | 0123 |
| 139 | Everything re-verified after eight sessions of change | — |
| 140 | Search, and a contradicted page that measured our own grid-fitting for us | — |
| 141 | A page turn walked the page tree once per outline item: 380 ms → 9 ms | 0124 |
| 142 | A frame that failed was reported as one that was drawn; the CPU draws it now | 0125, 0126 |
| 143 | A page the device drew nothing of, and said nothing about; banded, and the backend question asked properly | 0127, 0128 |
| 144 | §7.6.2 on the way out: an encrypted document can be saved | 0129 |
| 145 | §12.7.4.3's appearance stream is written into the file, not owed to the reader | 0130 |
| 146 | The glyph coverage cache, counted: what is shared is the outline, not the coverage | 0131 |
| 147 | One clipping region stated 303 times became one: 4.7× on a dense page | 0132 |
| 148 | §9.6.2.2's fourteen font programs compiled in, and the notices that go with them | 0133 |
| 149 | §14.7's structure tree reaches a consumer: `Query::AccessibilityTree` | 0134 |
| 150 | §12.4.4.1's `/Dur`: a state machine with no clock is told the time | 0135 |
| 151 | The ledger re-read against seven sessions of new capability | — |
| 152 | §7.6.4.3.2 step (a): the Annex D table this crate said it did not hold | — |
| 153 | Everything re-verified after ten sessions of change | — |
| 154 | `rasterrocket` measured rather than read; the strips it named, built and refused | 0136, 0137, 0138 |
| 155 | The rows a cut may fall on: the CPU rasteriser draws a page on every core, byte for byte | 0139 |
| 156 | §9.7.5.2's predefined `CMap`s compiled in, and §9.10.2's third method with them | 0140 |
| 157 | A `TrueType` Collection where §9.9 states a font program, and the face the file names | 0141 |
| 158 | "units per em is zero" was a symptom; two font programs are simply short | — |
| 159 | Five `partial` rows swept: one blocker expired twice over and fourteen entries called unread | — |
| 160 | An unexplained contradicted page measured: one `k` operator, and four renderers' profiles | — |
| 161 | Eleven more measured: the unexplained list is 14 → 2, and the instrument had to be checked | — |
| 162 | Everything re-verified after seven sessions of change; the strips' instruction cost priced | — |
| 163 | 541 300 calls to `Path::bounds` become 3 007: the strips' overhead is a third of what it was | — |
| 164 | The window re-run on `Xvfb` after ten sessions, because no gate turns a page | — |
| 165 | What a rendering library would have to be, for a team writing one; the frame split measured | — |
| 166 | The first panel: §12.3.3's outline drawn in the fourteen fonts the clause guarantees | 0142 |
| 167 | The other two: §8.11's layers with their switches, and §7.11.4's files | 0143 |
| 168 | Activate the item rather than its destination, and §12.3.3 closes | 0144 |
| 169 | An embedded file comes out of the document, and its checksum finds a place to be checked | 0145 |
| 170 | A catalog row wrong about eighteen entries, and the two the sweep made worth building | 0146 |
| 171 | A photograph's colours converted on eight threads; the priced item named the wrong loop | 0147 |
| 172 | `/NOTICE` over the page in Courier: the About panel the owner asked for | — |
| 173 | §14.3.3's `/Info` shown, and an `inapplicable` row that decayed when a panel arrived | — |
| 174 | §12.6.3's events raised at last, and Table 167's `ReadOnly` read for the first time | — |
| 175 | Everything re-verified after ten sessions of change | — |
| 176 | The ambiguous bucket gets a ratchet, a ranking and its first diagnosis | — |
| 177 | A subsection filed one object too high: the page one nobody had ever seen | 0148 |
| 178 | A CMYK JPEG converted by its decoder rather than by the one conversion | 0149 |
| 179 | §10.7.5's last sentence decides a page four renderers could not | — |
| 180 | A type 1 shading's domain is a parallelogram, and we painted a square | 0150 |
| 181 | A stencil whose current colour is a pattern: a blank page, reported complete | 0151 |
| 182 | A substitute that draws nothing, and the sentence that said it drew | 0152 |
| 183 | A substitute chosen by coverage: eight blank pages draw | 0153 |
| 184 | §10.7.4 says no shape may disappear, and a page of ruling lines does | — |
| 185 | Everything re-verified after ten sessions of change | — |
| 186 | §10.7.4 says no shape may disappear, and a grid of ruling lines now draws | 0154 |
| 187 | A pattern's cell clipped where it cuts nothing, measured: 15% of the ink | — |
| 188 | And not applied where it cuts nothing — which found the bound it replaced wrong | 0155 |
| 189 | The quorra backend over the whole corpus: a fifth gate, and what it found | 0156 |
| 190 | A page that is all sub-pixel line work, and the pair that antialiases it | — |
| 191 | The three sweeps run again: §12.8.2.2's parenthesis became a `shall` when the program learned to write | — |
| 192 | The 320-page book's cover is one reduced JPEG, and `pdfimages` said so in twenty minutes | — |
| 193 | A font program that draws nothing says so, and the condition was not the one written down | 0157 |
| 194 | A one-bit scan reduced by six: the bucket's fourth entry is one image too | — |
| 195 | Everything re-verified after ten sessions of change; a 2.2% counter given back | — |
| 196 | The corpus feedback answered by the library's team, and re-measured here | — |
| 196 | The corpus feedback answered; the owed work moved into `doc/todo/` and this file halved again | — |
| 197 | A name that ends in the word "Symbol" is not the standard-14 `Symbol`, and §9.6.5.4 says which half of the file wins | 0158 |
| 198 | §12.8.2.3: a save that outgrows a usage rights signature withdraws it — and no corpus document can trip that | 0159 |
| 199 | An annotation's appearance is a form, so §8.7.2 places its patterns in *its* space — a stamp's gradient was flat | 0160 |
| 200 | A JPEG 2000 decoder nobody had ever checked: 13 of 30 corpus codestreams disagree with the reference software | 0161 |
| 201 | §12.3.2.1's other two items: a destination's magnification and its place on the page, owed for sixty-nine sessions after the window arrived | 0162 |
| 202 | The ink recipe was halving our own page, two sessions ran on it, and the fix for it was written in the wrong file in 161 | 0163 |
| 203 | Four pages off the ranking with the repaired instrument: three reductions against their own high-resolution limit, and an `scn` the file broke | — |
| 204 | A page turn is six events in one order, and Table 198's two had been read since session 77 with no caller | 0164 |
| 205 | A widget's border lost a fifth of its ink to a `/BBox` clip lying on its own edge; the miter bound was the thing in the way | 0165 |
| 206 | A radial shading is not a two-point conical gradient, and all three backends inherit the same wrong one | — |
| 207 | A miter over the limit is a *bevel*, not a long miter — the second bound in the same function, and a comb field's separators | 0165 |
| 208 | Six more of the ranking measured against the closed form; `render_at` asks *our* renderer the same question | — |
| 209 | A link border and a sub-pixel one on the same page; the second sweep run clean, which is a result | — |
| 210 | Sizing the radial fix found trap 2 in the middle of it | — |
| 211 | A 112-unit border on a 150×20 rectangle, and §12.5.4's one sentence settles it against `poppler` | — |
| 212 | A page whose ink everybody agrees on is one for the heatmap, not the ink table | — |
| 213 | Everything re-verified; two recipes that had stopped working; the miter arithmetic costs −0.01% | 0165 |
| 214 | Ctrl + wheel zooms about the pointer, which needed a page point the scroll cannot express; and a field's two names, one of which a `shall` asks for | 0166, 0167 |
| 215 | The ambiguous ranking above 1.6 emptied in one sitting — seven pages, four groups, and the only defect in them found by a synthetic ladder | — |
| 216 | A fourth sweep — grep the *string* a correction retired — and a page where the high-resolution limit is the thing that is wrong | — |
| 217 | §12.5.3's `NoZoom` and `NoRotate`, blocked for 217 sessions on a reason that was two reasons | 0168 |
| 218 | A stencil through a tiling pattern — and then the page was still blank, because every tile in the tree was measured from the wrong place | 0169 |
| 219 | Three more off the ranking, one of them a reference that drew nothing; and the fourth sweep's second catch | — |
| 220 | §12.5.6.4's own sentence about the view, reachable three sessions after the flag it names | — |
| 221 | The four sweeps run over the *source* instead of the ledger: four claims false for between forty and two hundred sessions | — |
| 222 | A `loca` that goes backwards, and half a sentence missing in silence — the eleventh defect the ambiguous bucket has produced | — |
| 223 | And the repair: a `glyf` rebuilt in glyph order, because an entry is self-describing | 0170 |
| 224 | The repair checked against the other five documents it could have touched, and a page where the spaces are marks | — |
| 225 | Three more where ours is nearest the geometry — and one where the two ladders do not converge, which is the rule working | — |
| 226 | One text field, four renderers, four answers — and one of them draws the string backwards | — |
| 227 | Three more, one of them the case where step 6 has one ladder and not two | — |
| 228 | A page where all five renderers paint one to seven levels more than the geometry, and ours the least | — |
| 229 | Why half the ambiguous bucket is ambiguous — and it is not the reason this file gave for it | — |
| 230 | Everything re-verified after seventeen rounds of change: five fuzzers, `deny`, the six gates and the window | — |
| 231 | The ranking's top page, and §10.7.4's own last sentence is the answer to it | — |
| 232 | A radial shading is not a gradient: §8.7.4.5.4's greatest *admissible* root, on all three backends | 0171 |
| 233 | A quarter of the ambiguous bucket was one paper under twelve names — and `pdftoppm` renders the wrong box | — |
| 234 | Two pages where the *reference* is alone, and both corrected a ledger row rather than a pixel | — |
| 235 | Fourteen pages of the standard 14, where §9.6.2.2 states the names and not one outline | — |
| 236 | Two `shall`s that cannot both hold: a strike-out follows the text it strikes out | 0172 |
| 237 | A blurred word nobody drew: §8.6.8's restriction reached inside a soft mask's group | 0173 |
| 238 | A page that states its own answer, and two rows whose neighbour's correction never reached them | — |
| 239 | An empty glyph stays empty — the `loca` repair had been handing the space a real glyph | 0174 |
| 240 | The whole bucket swept for missing content, and it is empty: −0.84 to +0.42 of 255 | — |
| 241 | A sixth fuzz target, over the two glyph-table repairs, and two crashers in its first minute | 0175 |
| 242 | The contradicted list's older unexplained page, settled by two ladders rather than a debugger | — |
| 243 | And the last one: `CONTRADICTED_UNEXPLAINED` is empty, from 42 | — |
| 244 | Codes that reach no glyph, counted at last: 109 over 14 documents, two thirds in two | — |
| 245 | And half of the count was a space the font reads back as `#`: the real silence is 24 codes | — |
| 246 | The one document left in that silence, diagnosed to an `/Encoding` naming a content stream | — |
| 247 | And the font named, which split it in two | — |
| 248 | Both halves traced to the end of every route the standard states: the subsets do not hold the glyphs | — |
| 249 | Everything re-verified after eighteen rounds: eight fuzzers, `deny`, the six gates and the window | — |
| 250 | Twenty rounds' worth of prose brought up to date, and what the twenty had in common | — |
| 251 | A selection costs a compositor layer a quad: reproduced, measured to the byte, not fixed | — |
| 252 | And it costs one now, whatever is selected: 268 quads in 16.5 ms, from a refusal at 63 | 0176 |
| 253 | The pointer was on a *link*: §12.5.5's appearances and §12.5.6.19's `/H` reached no other subtype | 0177 |
| 254 | The same sweep the other way — every `pub fn` nobody calls — and §8.11.4.3's `/ListMode` was one | 0178 |
| 255 | A thread followed to the *bead*: Table 163's `/R` composes Table 149's `/FitR` | — |
| 256 | A todo file's own claim was stale: both witnesses of the per-character fallback already draw | — |
| 257 | All ten of Table 197's events raised: `/Fo` and `/Bl` wanted a *press*, not a vocabulary | — |
| 258 | `/Helv` is not an arbitrary name: fourteen `/DA` abbreviations, and a no-break space | — |
| 259 | The ambiguous ranking's head: one level of green over a third of a page, and ours is the arithmetic | — |
| 260 | The next two off the ranking: an image reduced by four, and one line of ideographs | — |
| 261 | §12.3.4's thumbnail panel: a fifth tab, and `/PageMode /UseThumbs` names something at last | — |
| 262 | Both long books diagnosed as populations — 489 undiagnosed to **136** — and one page the band caught | — |
| 263 | `TAMReview.pdf` as a third population, and `calrgb.pdf` as §10.3.1's own sentence | — |
| 264 | The tail's new head: four pages, the tightest three-way ink agreement the bucket has produced | — |
| 265 | A page this tree drew nothing on, found by the sweep at −1.78: §12.5.6.4's point, not a rectangle | — |
| 266 | The sweep corrected three ways and run over all 787: twenty names past −1, every one explained | — |
| 267 | Six icons a clause only recommends, because their names name *objects*; `Stamp`'s do not | — |
| 268 | Everything re-verified after sixteen rounds — and ADR 0126's own recipe had decayed | 0126 |
| 269 | §12.3's parent rows, one round after their member landed; two more off the ambiguous tail | — |
| 270 | The capability sweep over `crates/`: 89 matches, 87 of them true, and both false ones ten rounds old | — |
| 271 | Twenty rounds' prose brought up to date, and the question the twenty had in common | — |
| 272 | quorra took all three of §7's suggestions: a refused present is 6 ms, not 1.008 s | 0176 |
| 273 | Page one goes to the GPU by decision, so bring-up is measured: 43.5 ms, 32.2 of it adapter choice | — |
| 274 | A launch is a timeline: **145 ms** to the first frame, and a 1023-page document opens 33× slower than a 5-page one | 0179 |
| 275 | Bring-up is on quorra's critical path now, and the brief still told it otherwise in three places | — |
| 276 | §7.5.6's rule stated once for a file rather than once per entry: `Document::open` 41% off | 0180 |
| 277 | A form says whether it has signature fields, and Table 225 says to ask: 1.681 ms to 0.017 | 0181 |
| 278 | The five sweeps: a row that contradicted *itself*, a parent stale about three children, a `shall` no host asked | — |
| 279 | Two off the ambiguous tail's head, and both were a *width*: 0.4 of a pixel, and one sentence on a stamp | — |
| 280 | The first frame pays 12 ms the tenth does not, and a 1 s sleep proves it is not the shaders | — |
| 281 | The document opens *beside* the window: nothing the window needs has ever looked at a PDF | 0182 |
| 282 | A `Tf` naming `/Helvetica` with an empty resource dictionary: the standard says the name means something | 0183 |
| 283 | A refused value's report named the wrong half: `Załącznik` is missing a *code*, not a glyph | — |
| 284 | So the invented font names the glyph: `/Differences [1 /aogonek]`, and poppler reads back *Zacznik* | 0184 |
| 285 | Everything re-verified after eleven rounds: eight fuzzers, `deny`, the seven gates, the window | — |
| 286 | Two more off the ambiguous tail, both into groups that already existed: an image and a border | — |
| 287 | §9.6.5.2's last sentence: `.notdef` substituted where an encoding names a glyph the program lacks | — |
| 288 | quorra took both of §8's asks and refused §8.3's knob: bring-up 33–45 ms to 13–19 | 0185 |
| 289 | A 1023-page document and a 5-page one now cost the launch the same: 5 ms of join | — |
| 290 | The sweeps over four rounds' nouns: §12.7.4.3 still said the reason was a glyph | — |
| 291 | Step 7 over all 786 after three rounds of new pixels: four names past −0.7, all diagnosed | — |
| 292 | Everything re-verified after quorra's update — and the round-285 deletion `rm -f` reported had not happened | — |
| 293 | Twenty rounds' prose brought up to date, and the sentence the twenty had in common | — |
| 294 | §14.3.2's XMP, 319 documents' worth: an XML parser taken, fuzzed, and `dc:title` in the title bar at last | 0186 |
| 295 | The sweep over the noun round 294 had just corrected — and §14.3.4, `inapplicable` on two capabilities that arrived a hundred and twenty sessions ago | — |
| 296 | §14.8.2.5's logical order reaches a selection at last: the clause had a reader for 140 sessions and no caller | — |
| 297 | Table 31's `/Tabs`: all five navigation orders, and the key §12.5.1 names bound to them | — |
| 298 | The sixth sweep, which is arithmetic: five parents owing more than their own children, four of them wrong | — |
| 299 | A reference that decoded an image wrongly and had the right amount of ink — the picture, not the metric | — |
| 300 | Four names for one measurement: the ranking pointed at a page whose answer was already written two pages over | — |
| 302 | The other half of §12.5.1: where the focus *is*, so a host can draw a ring round it — and one does | — |
| 303 | A gap between two clusters is still a question for the ladder — and three rounds of counts the handover never received | — |
| 304 | The second sweep, in a "Not read:" list: a flag that constrains typing binds a program that types | — |
| 305 | A page where our 8× lands *between* the two reference limits, 0.003 from one | — |
| 306 | An empty field's border and a page of two ramps: one climbs onto a limit 0.001 wide, the other has none | — |
| 307 | Nine of 255 painted and given back: the sub-pixel line-work group's extreme, and nobody is wrong | — |
| 308 | Two ladders ending 0.70 apart, which is the text tolerance's own premise demonstrated | — |
| 309 | The tightest *ratio* the tail has produced, and it was a page with no limit tight enough to be alone from | — |
| 310 | quorra's coverage lane chosen per frame from the magnification; the page extent taken once, in f64 | — |
| 311 | The fourteen specification documents out of the tree and back in encrypted; a snapshot release, and the two platforms it refuses to ship; the pipeline read for the first time in three days, and both its failures were this tree's; a budget that priced a texture nobody allocated; a JPEG 2000 hypothesis from session 200 confirmed, and the half of the same clause it had been hiding | 0187, 0188, 0189, 0190 |
| 312 | A window the page does not draw: §12.5.6.14's popups, from a capability reason that had expired — and Table 197's `/U` reaching only a release that also followed a link | 0191 |
| 313 | Four names off the ambiguous list for one measurement, and two `partial` rows whose blocker has been a sidebar since session 166 | — |
| 314 | Ten shapes with no size — Table 179's line endings, drawn — and the box a constructed appearance turned out not to have, which had been clipping four annotations away in silence | 0192, 0193 |
| 315 | Three platforms and one confinement: the `compile_error!` that kept macOS and Windows out becomes a sentence the viewer prints, by the owner's decision; two defects a cross-check could not see, and the Windows read path run on Linux | 0194 |
| 316 | The chrome's own silence measured — 74 documents state text §9.6.2.2's fourteen cannot set — and a box per character instead of nothing | 0195 |
| 317 | The tail's tightest ratio was the page whose *name* was the hypothesis: sixteen blend modes, and not one of them is the difference — and the bucket's own count was `wc -l` of a file with a header | — |
| 318 | A page of TeX where ours lands between the two limits; and the entry whose whole purpose is a user interface, recorded as unread for a hundred and fifty sessions after a panel arrived to present it | — |
| 319 | A page whose references are 0.18 under their own limits at 72 dpi; and §8.11.3.2's `DP`, unimplemented in the row for sixty-five sessions after a resource walk covered it by construction | — |
| 320 | Three `k` operators on a 100 × 100 page: ours and `poppler` byte-identical, `mupdf` uniformly two levels off, and a flat offset is a colour result wearing an ink measurement's clothes | — |
| 321 | §12.5.6.10's markup, added by a person and written by §7.5.6: a page learns which object it is, and the log records what was done rather than what was asked for | 0196 |
| 322 | `h` marks up the selection in a real window, and the file states its own marks: with the appearance stream written, `poppler` and `mupdf` go from 18% apart to 1.7% | 0196 |
| 323 | One document, two pages, two answers: a ladder that never converges is a colour (0.125 is 31.875 and `mupdf` truncates), and the page beside it has the tightest limit the bucket has measured | — |
| 324 | A checkerboard of one photograph, where two ladders agree to 0.0001 of 255; and Table 100's three unanswerable categories counted at last — six documents state a usage application and not one names `Zoom` | — |
| 325 | A 149 × 68 page read row by row: 78% of the whole difference is two border lines, where a reference puts 214 into one row and ours 176 across two | — |
| 326 | A Type 3 font is a simple font, and §9.10.2's second method applies to it — three hundred sessions of `/ToUnicode` alone; 98.2% → **99.1%**, and a page that read every character twice found the rule that a glyph description's own text is the glyph | — |
| 327 | The *second* list in that sentence brings an algorithm: `f_f_i` is three components and `oacute.sc` is a variant, and eight codes of a Minion subset stopped reading back as nothing | — |
| 328 | The same permission a second time, once the program has said nothing — and the first measurement of it had been taken before the round that made it free | — |
| 329 | A ratio of two, which is as alone as the tail gets, and the two ladders close from opposite sides to 0.0196; and §9.10.2's row was still quoting the sixty-third session's 96.5% | — |
| 330 | Three off the tail: a line five renderers draw three different subsets of, and a hairline grid a reference paints 34% over its own limit | — |
| 331 | Four more, and the tightest pair of ladders yet: 0.0008 of 255 on the tax form, where a reference is 2.70 over its own limit at the page's own scale | — |
| 332 | A selection was a range of the page "that has just been replaced", and a page drawn again is not a page turned — every edit took a person's selection away; the six sweeps run clean | — |
| 333 | Two more, and a new group for the one the standard puts beyond itself: three renderers within 0.09 of each other and two 0.9 above, on three commands of §11.4.7 | — |
| 334 | Step 7 over all 786 after twenty rounds of change: three names past −1 and all three diagnosed, which is the alarm holding rather than a finding | — |
| 335 | Everything re-verified after twenty rounds: nine fuzzers, `deny`, two cross-targets, the eight gates and the window — and twenty rounds' prose brought up to date | — |
| 336 | Zoomed in until something broke: the keyboard clamps at 6400% and the sidebar stops being drawn above ~2000% on the graphics device and not on the processor — `examples/zoom_ladder`, the first instrument in this tree that magnifies past 4× | — |
| 337 | The wrong glyph, reproduced in two frames on one device: a ladder that does not switch coverage lanes is not measuring what a person sees past 1000%, and `extensive` reading `extens:ve` is a glyph *replaced* rather than lost — `doc/QUORRA_FEEDBACK.md` §11 | — |
| 338 | A field that may not scroll: §12.7.5.3's Table 231 bit 24, a `shall` about *accepting* text that has bound this program since session 135 — 260 corpus widgets over 8 documents, and four of the twenty field flags have no witness at all | 0197 |
| 339 | The sidebar at high zoom and the wrong glyph are one defect: `chrome_ladder` draws the window's *whole frame* offscreen — page and chrome in one scene, which no gate in this tree does — and a device per rung is clean where one device is not, so it is state between frames and not magnification | 0198 |
| 340 | quorra `52b07f29` in, and both halves verified the day it landed: every rung of the descent equals its ascent, the chrome ladder's two passes agree, and the corpus gate is unmoved — with the gate that would have caught it checked by pinning the broken revision back | 0198 |
| 341 | Three names off the ambiguous list at once — one page under three names — and the instrument that did it is a per-row ink profile: every one of the twelve worst rows is within a fifth of a pixel of one of four table rules, and the prose half of the page agrees to 0.02 of 255 | — |
| 342 | The six sweeps run again, and the third paid: §12.5.6.2 owed `/RC` to "a comments pane this program has no panel for" thirty sessions after the popup window arrived — the characters read and the XFA formatting declined, with the corpus demand measured at zero and said so | 0199 |
| 343 | Two more off the ranking, and the profile named the same thing on both: a shape's boundary row. `bug_jpx`'s image covers exactly 128.004 columns while two references round outwards to 129 and one to another phase; the tensor mesh's two edge rows are 61% of the page's whole spread | — |
| 344 | §10.7.4's "no shape ever disappears" is obeyed by the backend a page goes to and not by the oracle: a 0.05-unit sliver is 0 on the processor and 0.0510 on the device, and a rule at the raster's edge is 0.0549 against 0.0980 — two of `doc/todo/11`'s three items are one rasteriser's | — |
| 345 | Two more: a page that is its own instrument — four labelled quadrants, and the two `knockout` ones split the five renderers 6.4 apart while the two `normal` ones agree within 1.1 — and a table where the difference is which *row* a rule lands in, three answers to one question | — |
| 346 | §7.9.3 named its own expiry condition — "this closes the day an entry in scope uses the type" — and the round before had brought Table 172's `/RC` into scope; `reported` 30 becomes 29. And a 200 × 50 page where one row of glyph edges is a whole reference's excess | — |
| 347 | A sixth tab: §12.4.3's articles, whose row said "that is a panel rather than a clause" for forty-seven rounds after the panel arrived. A click sends the outline's own message and `activate_object` composes §12.6.4.7's `ThreadJump` out of it, so one behaviour stays in one place | 0200 |
| 348 | `doc/todo/00`'s step 7 over all 786 again: the negative tail has not moved in fourteen rounds of change, and the *positive* side produced its first name — `poppler` drawing fourteen `.notdef` boxes where the other four draw the words, which no ranking accuses anybody of | — |
| 349 | A person types into a form field **in the window**, which this program has been able to do since session 135 and no window could: the host keeps the point it clicked and re-asks what the field says on every keystroke, so `DoNotScroll`'s truncation is impossible to diverge from | 0201 |
| 350 | Two more off the ranking, and one is the bucket's extreme: a 612 × 792 page whose *whole ink* is 0.03 of 255, where two ladders end 0.00065 apart and ours is 4% light in relative terms and six ten-thousandths of a level in absolute ones | — |
| 351 | A third link-border page, and a narrower question than the other two: every link states an `/AP` whose stream decodes to the empty string, so what is being decided is whether a stated appearance outranks the entries a border would be built from — §12.5.5 says it does | — |
| 352 | §12.3.5's `shall` paid, and not with a seventh tab: a collection is the same files with a statement about how they are arranged, so the files tab becomes the folder tree and the schema's columns. The container's pages stay on the screen, argued from §7.6.7's wrapper | 0202 |
| 353 | Two pages that are ambiguous *because* everybody agrees: one where our 8× render equals `poppler`'s 576 dpi limit to seven figures, and one 88 × 31 page where the five renderers do not agree about the raster's own size | — |
| 354 | A 92-page catalogue that drew nothing draws: a four-component YCCK JPEG was asked for three, because `zune-jpeg` has no YCCK → CMYK conversion and its two YCCK arms composite the black channel away. Ours is 0.0113 from `poppler` on the cover where `mupdf` is 0.0384 | 0203 |
| 355 | Four off the list: three pages of a §7.10.5 calculator function where ours and `hayro` agree to four decimals and `mupdf` draws white paper — two of them the same page under two names, by md5 — and a Coons mesh whose profile is its tensor sibling's to the value | — |
| 356 | Five more, four of them where step 6's ladders bracket ours inside a fifth of a level — twice with ours at 8× *equal* to a reference's limit to six figures — and a 10 × 10 page whose whole spread is less than half a pixel's ink | — |
| 357 | The ledger's first `silent` row since session 35: §10.5's transfer function decides what a screen shows on `issue6931_reduced.pdf`, three references apply it and we do not, and the page's own text says what it should look like. Found by step 7's positive side, and a question for the owner | — |
| 358 | §10.5 implemented, after the project owner split `CLAUDE.md`'s scope line on the standard's own evidence: 13 of 974 documents state a `/TR` and one states a real one, so the census made it a small change rather than a brave one. And a `CLAUDE.md` rule for a document's restrictions — four levels, and the shape they need before any of them exists | 0204 |
| 359 | The `inapplicable` rows read for the first time, at the owner's instruction: five were wrong and all five the same shape — a §14 row saying a screen does not do this, beside a §12 row saying the tree draws it. No page changed and no corpus document states any of the four entries, which is the point | 0205 |
| 360 | The ledger learns the standard's eight **normative annexes**, 52 rows it could not previously spell: `§K.2` was a malformed citation. Annex O — eleven `shall`s about what a URI's fragment opens — is `silent`, and no file could ever have triggered it. `CLAUDE.md`'s XFA exclusion loses the half of its reason that was false | 0206 |
| 361 | The cheapest thing the new annexes asked for: §7.5.2's header version and Table 29's `/Version`, ranked as §7.7.2 ranks them, and Annex I's warning that a file is newer than this program. No corpus document reaches it, which is what a coverage obligation looks like | 0207 |
| 362 | Three off the ambiguous ranking, all one shape and all closed by step 6 alone: `issue13193` and `issue3584` are one-line specimens whose ladders end 0.011 and 0.057 of 255 apart, and `issue1905` is a chart poster where the *references* spread 0.97 and we are nearer `poppler` than `ghostscript` is. And `doc/todo/14`, raised by the owner: a selection highlight over a bad OCR font | — |
| 363 | Two more, and both were a reference standing alone: a barcode whose bars are narrower than a pixel (our column profile disagrees with `poppler` no more than `mupdf`'s does, ladders 0.026 apart) and a 5280 × 3792 photograph reduced by nine, where ours at 8× is 0.0015 from `mupdf`'s limit | — |
| 364 | Two more, and both are one pixel wide: a 132 × 14 bilevel image enlarged 3.8× (ladders 0.017 apart) and four hairline widget borders on a 198 × 204 page, where three ladders land inside 0.022 and ours is 0.003 from `mupdf`'s. The same page found a trap: five renderers round a fractional device height to 203 or 204 rows, so an ink *mean* is over a different denominator | — |
| 365 | The widest spread in the bucket, and it needed no ladder: a ten-degree-rotated square under a `/Luminosity` mask whose `/BBox` is a strip. Three renderers draw a sloping edge whose slope is tan 10° and whose maximum column is 0.9848 × 80; two draw a vertical one at the device-space *bounding box* of the same rotated rectangle. §11.6.5.1 and §8.10.2 decide it, and the agreement was evidence rather than the reason | — |
| 366 | Two from the bottom of the ranking, where it says loudest that the page is not ours: a gradient band where ours and `poppler` converge to 0.0375 while `ghostscript` keeps 0.43 at 8×, and two indexed images where **our raster and `ghostscript`'s are the same bytes** and `poppler` alone smooths what Table 87 defaults to false | — |
| 367 | The end of a block of thirty: every gate re-run whole and every number in this file checked against what printed. Nothing moved, which is the result a closing round wants — the block's work was two `CLAUDE.md` corrections, a population of 52 ledger rows nothing could previously address, one `should` paid, and ten pages off the ambiguous ranking | — |
| 368 | A §10.7.4 mark stops moving with the shape's fraction of a pixel: it is the whole device pixel row the collapsed axis passes through, which the clause states in a NOTE and an EXAMPLE that had been in `doc/md/` for the rule's whole life. The `0 w` stroke deliberately does not follow, because §10.7.5 makes *its* coordinates conditional on `/SA`, and the byte-identity test became an ink test that says so. One page of 1794 moved and the quorra gate gained one | 0208 |
| 369 | Annex O built: a URI's fragment says where a document opens, and `pdf-viewer doc.pdf#page=5` is the first caller. Seven of the eleven parameters are carried out and four are reported by name, each with a different blocker — **`silent` is 0, from 5**. Three findings in the annex's own text: it prints `(28h)` for the AMPERSAND its own Table D.2 gives 0x26, it never states the `=` that joins a parameter to its arguments, and its coordinate rule holds only if the units are default user space's and the origin is the page's top-left corner | 0209 |
| 370 | The display list learns to name a raster it does not hold: `ImageSource::AtDeviceScale` plus `Grid::for_placement`, so an image and the mask that belongs with it reach a backend apart and are combined where the device scale is known — §10.7.4's own answer, and the interpreter still never learns the scale. `issue16263.pdf`'s 2×2 image under a 34862×4332 `/SMask` drew black bars because the refinement of the two grids is 604 MB; it now draws in 49 MB and **agrees with the reference consensus**. Corpus 73 → 72, oracle 856/1685 → 857/1686. Two of the three claimants remain and neither is blocked by the display list any more | 0210 |
| 371 | A caret, and it comes out of §12.7.4.3's layout rather than out of the text layer: an empty field has no glyphs and 147 of the corpus's first-page widgets are empty, so the place the *next* character goes is the only thing that can be asked, and the layout is what knows it. `Query::Caret { at, offset }` answers with a segment in device pixels; the arrow keys, Home and End move it, Backspace and Delete take out the character on either side, and §12.5.1's tab walk aims the keyboard at a field it lands on — the decision `doc/todo/33` left open, which needed no new message because `Query::Focus` already answers in the pixels `Query::FieldAt` takes. Two defects found by reading the round's own feature: **Escape exited the program** while a field had the keyboard, three branches before the arm ADR 0201 wrote for it, and `Query::Focus`'s ring was mapped to the screen without the page transform | 0211 |
| 372 | Three names off the ambiguous ranking, none of them a defect, and two of the three replace a group's argument with **arithmetic**. `bug1889122.pdf` is one stroked rectangle whose ink is `150 × 22 − 148 × 20 = 340` square points over 19 635 pixels — 4.4156 of 255 — and ours is 0.05% over it where `ghostscript` is 26.7% over and `hayro` 17% under. `issue4379.pdf` places a stencil-masked image at an exact two-to-one reduction onto integer device coordinates, so §10.7.4 names one raster sample by sample: `ghostscript` reproduces **all** 500 990 pixels of it and we depart on **3 927**, which is ADR 0025's cost measured rather than argued — and invisible to ink, where the five renderers agree to 0.023 of 255. `issue14953.pdf` declares `0 0 0 0` for its Type 3 font box and all fifteen glyph boxes, and a synthetic A/B differing only in `d1`'s four operands shows `ghostscript` drawing nothing above 72 dpi and `poppler` losing the glyphs as the pixels shrink, while ours and `mupdf` are byte-identical across the pair. Its by-product is the spec-track item: §9.2.4's and §9.6.4's rows both credited Table 111 with a permission it does not contain | — |
| 373 | A document's restrictions get the shape the project owner's four levels need, and the round's finding is in the shape rather than in the levels: the refusal used to be `set_field` returning **zero**, which is also what "no such field" and "every widget is `ReadOnly`" return — so an *ask* level had nothing to put in front of a person. `pdf_model::restriction::asserted` now states what the file asserts about one **operation**, with its clause and its `/DocMDP` level; `viewer-core` holds the one policy value a host supplies (`Command::Restrict`), asks it once per `Edit`, and refuses with `Event::Refused` carrying the operation — which is *ask* minus a host that can answer. **Table 22 starts being consulted**, for bit 6 and bit 9, and reading it turned up a rule this tree would have got wrong: bit 9 grants only from revision 3, so in every conforming revision-2 file it is set by the table's own reservation — the standard's own example, `/P` −44, has it set while "disallow[ing] modifying the contents and annotations". `Permissions` carries `/R` now. Seven of the 968 corpus documents that open assert something, measured by running the function. §12.8.2.3's `/UR3` withdrawal is untouched: turning a restriction off is the reader's, making the file lie is not | 0212 |
| 374 | A rule two pattern cells describe is drawn once. `issue16038.pdf`'s second square states its rule on **both** `/BBox` edges, so Table 74's clip halves it and the halves — one cell each — composited as `1 − (1−a)(1−b)` instead of adding: 13% under the ink its own geometry states. **The clause answers it, and not in the NOTE that looks like the answer.** §11.6.7 makes a pattern's evaluation produce the shape the *painted object* is then given, so the tiles are portions of one object and §11.6.2 applies outright — "[p]ortions of an object shall not be composited with one another"; §11.6.7's own NOTE 2, one transparency group for all the tiles, names the artefact and does not remove it, because compositing inside a group is still compositing. So the two statements are recognised as **one mark of the tiling** and it is drawn whole: no coverage buffer, nothing snapped to the grid, and the display list keeps its resolution independence. The right square goes 0.858 → **0.989** of the geometry at 1× and 0.951 → **1.003** at 8×, and now matches the left at every scale. The finding is in the first draft: re-deriving the fold per cell folded **180 of 1296 tiles** — an `f32`'s neighbours at x ≈ 65 are already wider than the tolerance — and half a folded tiling reads in a count as *the change did nothing*, 0.1197 to 0.1205 of an expected 0.1333. One page of 1794 moved | 0213 |
| 376 | §14.7's tree reaches a screen reader, which is the last of the five things §0 listed as blocked on the `viewer-core` boundary and had been owed since the hundred-and-forty-ninth session answered the query nobody asked. `viewer-accessibility` maps §14.8.4's forty-one types onto `accesskit::Role` — with the **fourteen** places the two vocabularies do not line up written into the module rather than defaulted to `Unknown` — and `accesskit_unix` puts the tree on AT-SPI, where `busctl` walks it off the bus under `dbus-run-session` + `at-spi2-registryd` + `Xvfb`. **The dependency was decided before the first line and measured after it**: 61 packages, all permissive, `memchr` build-time only (ADR 0186's rule intact), the runtime confined to one Linux-only crate, the adapter created *after* `Launch::arrived`, and every step of the launch timeline unmoved to a millisecond. Three findings in this tree, none of them on a list. §14.7.3's role map was **not applied** — a `shall`, implemented in `pdf-model` since the seventy-eighth session and read past by the query, behind a sentence that was two claims wearing one coat: the *file's* map is not the *platform's*. The answer carried every element in the **document** rather than the page's, which ADR 0134 claimed it did not. And an element spoke its whole subtree, so a paragraph and its span said the span's words twice — `name` is the element's own content items now, `quads` still everything it encloses, and `substituted` says which kind of name it is. The end-to-end run found a fourth that no `TreeUpdate` test could: `Role::Label`'s accessible name comes from its **value**, so every static-text node was on the bus with an empty name | 0214 |
| 375 | A sweep round, and the phrase it hunted was one this project had already retired from `CLAUDE.md`. All seven sweeps re-run over `ledger.toml` **and** `crates/` after five rounds that added verbs and none that re-swept. **"Marking device" was still in six places** eighteen sessions after ADR 0204 established ISO 32000-2 does not contain it — including the ledger's own *definition of the `inapplicable` status*, which is the worst place for it, since §10.5 spent three hundred and fifty-seven sessions in that status partly because the word explaining it named a device the standard does not have. **One of the six was a defect**: `content.rs` explained §8.6.8's ignore-list by saying `/TR` and `/TR2` "describe a marking device and are read nowhere here", thirty lines below the `Transfer::read` that has read both since the three-hundred-and-fifty-eighth — and the reader was **not** behind the uncoloured-figure flag, so a transfer function inside an uncoloured tiling pattern or a `d1` glyph description decided a colour the clause reserves for whoever uses the figure. Nine of `requirements::unmet`'s reasons were false, every one of them naming a clause as *unread* that this tree reads ("§12.8 is unread", "§12.10 is unread", "§14.12 is unread"), and `Collection` was not unmet at all — ADR 0202's view plus `Command::Extract` are the two things Table 275 asks for. §12.5.2 listed `/RC` and `/NM` as unread; §7.7.2 listed the catalog's `/Metadata`, which is the entry `Xmp::document` reads. **An eighth sweep**: every path a note or a comment cites, globbed — seven dead, including the `doc/todo/20` this project has carried as owed for fifteen sessions and whose sentence ADR 0169 falsified a hundred and fifty-seven ago. No ledger status moved, and no gate: corpus 974/72, oracle 1686 complete with 857/68/750 and 5 undiagnosed, quorra 914/42/1/17 — the `/TR` guard changes no corpus page, because none states one inside an uncoloured figure | — |
| 377 | A signature asks **three** questions and §12.8.1 states them in one paragraph; the whole clause had been refused on the infrastructure only the third needs. **Has the document changed since it was signed** is a digest over `/ByteRange` against what the signature value records, and needs no certificate, no trust decision and no network — so `pdf_model::der` (X.690, bounded, allocation-free, fuzzed at 1 000 000 with the corpus's eleven signature values seeded) and `pdf_model::cms` (RFC 5652's `SignedData`, as far as `message-digest`) make it answerable, and `Signature::integrity` answers it for all four shapes §12.8.3 gives a digest: a detached CMS attribute, `adbe.pkcs7.sha1`'s encapsulated digest, RFC 3161's `messageImprint`, and PKCS #1's — which is **sealed under the signer's key** and is named as that rather than as an encoding error. **Four of the corpus's ten signature dictionaries no longer hash to what they record**, including both of `xfa_filled_imm1344e.pdf`'s: the gap its `/ByteRange` names is 4 213 bytes from where its `/Contents` sits and matches it in *size* to the byte, so the file was re-saved rather than appended to. The wording is the round's risk and its subject: a mismatch is decisive, a match is the absence of one kind of evidence, and the program says which of the three questions it answered. **The demand track's defect was in `pdf-syntax`**: §7.6.2's exception recognised a signature dictionary by `/Type`, whose doc comment said `/Type` "is the only thing that identifies one" — and Table 255 makes it optional with a default of `Sig`, so `issue17069.pdf`, encrypted and `/Type`-less, had its 33 680-byte signature value pushed through AES and read back as empty. With it dies trap 8's third measured-unreachable rule, whose census had been taken **with the predicate under test**. Two dependencies, `sha1` and `ripemd`, so that all six of Table 260's and Table 256's digests exist rather than five. Nine `reported` rows become `partial`; `reported` 30 → 21 | 0215 |
| 378 | A selection highlight over a badly built OCR font, which is the project owner's own report and which **no gate in this tree could see**: the text gate compares characters and never asks where they are, and the oracle compares pixels an invisible OCR layer does not mark. The height of a selection box is not in the file, so `pdf_font::vertical_extent` invents it from Table 120's `/Ascent` and `/Descent` — and its guard was `ascent > descent`, an *ordering*, which accepts a box entirely below the baseline, a sliver and one five ems tall. **The census came first and made it a population**: of 1629 font dictionaries on the corpus's pages, 40 state a pair no face could have and **42 more state a `/Descent` without the negative sign Table 120 requires**, which is Arial's and Times New Roman's real metrics with the sign dropped — 53 documents between them. The band is derived rather than tuned: §9.8.1 and §9.2.4 fix the unit, Table 120's own definitions fix the two signs, and §14.8.5.4.4's Table 380 states what a line is worth in font sizes ("approximately 1.2 times the font size") with §9.2.2 stating the same quantity tightly spaced at 1 unit — both inside the band, and the factor of two around them is the round's one choice, priced by the asymmetry of the two mistakes. **The gate is new and would have failed before** (two of its six tests, checked by putting the old guard back), and it names the thing that makes every scanned page selectable — that Table 104's modes 3 and 7 place every glyph they do not draw — which nothing had asserted. The Type 3 question the todo marked unverified is **verified and correct**: the em box is already in text space and the `/FontMatrix` must not touch it, tested at `[0.01 0 0 0.01 0 0]` where the two readings differ by a hundred. Seventeen citations named ISO 32000-1's table numbers for tables ISO 32000-2 renumbered — `/Ascent` is **Table 120**, not 122 | ADR 0216 |
| 379 | **The ambiguous ranking is empty**: the last five undiagnosed pages taken in one round, none of them a defect, and all five a *font* page by five different mechanisms. `issue4665.pdf` is this bucket's first page where **all four references converge on one number** — four ladders within 0.044 of 255, three within 0.009 — so `ghostscript`'s 38% excess at 72 dpi is scan conversion *by its own later rungs* rather than by a spread; `bug911034.pdf` is the same at a quarter the glyph size. `issue9084.pdf` is §9.7.4.2 handing the answer over in its own last sentence — "[t]he means by which this is accomplished are implementation-dependent" — with the half it does **not** hand over checked: "they shall always be used to determine the glyph metrics", and at 8× ours and `mupdf` put the line's ink in the same 1022 × 123 box to the pixel. `issue12705.pdf` is the sharpest instance of shape 1 the bucket has had — **111 of 114 Type 3 glyph descriptions state `1 1 1 rg` before filling**, and a processor honouring them would paint white on white and the page would be blank. `bug1308536.pdf` is a mechanism this bucket had not named: `ghostscript` prints *An embedded font is invalid* and substitutes, where four renderers draw the producer's own face and end within 0.015 of 255 — and the corrupt part of that CFF is the Private DICT's hinting operands, which carry no outline. **The step this adds is about a ladder that does not converge**: dividing a reference's excess by the ink a one-pixel erosion removes turns it into an outward offset, and `ghostscript`'s **triples in device pixels while holding at 0.040 ± 0.004 points** — user space, so a different shape, not a different sampling. Spec track: six table numbers wrong in `pdf-font`, of session 378's family — §9.9.1's embedded font stream is **Table 125** and not 123, §9.9's six required TrueType tables are **Table 124**'s and not 127's, and a font dictionary's `/Encoding` is **Table 109**'s cell and not Table 112's, which is the encoding *dictionary*. No pixel moved and no gate did | — |
| 380 | **A mask group's result is one number and a painted group's is three**, which is why `doc/todo/23`'s fourth population fell in one round and its third did not. §11.5.3 reduces a `/Luminosity` mask to a luminosity; §10.4.2.3 states that reduction for a subtractive space; and its conversion is **linear in the components** except for one `min` — so the group is *painted* in that one number, on the grey channel a rasteriser already has, and `SoftMask::value` reads it back unchanged because §10.4.2.2's three weights sum to 1.0. The only display-list change is what `SoftMaskKind::Luminosity`'s backdrop is measured in. **No conversion into the group's space is needed, and that is a result**: §10.4.2.4's black generation cancels out of §10.4.2.3 for the same reason, whatever `BG` and `UCR` return, so an RGB colour taken through `DeviceCMYK` and back to grey is §10.4.2.2's grey of the original and only `DeviceCMYK` needs an arm of its own. **The census came first and found the report was three steps from the departure**: of the 90 luminosity mask groups the census reaches, 39 blend in `/DeviceCMYK` and 36 in `/DeviceGray`, and **not one sets a `k` colour** — the departure lived in the backdrop, and the old condition fired on the group's `/CS`. Where the clause's arithmetic is visible it is **32 of 255**: `/BC` process black masks everything away and this tree drew it at 12.5%, because `CMYK_CORNERS` puts process black at `(35, 31, 32)`. Three new fixtures, each checked by putting the old route back — all three fail, at 223, which is 255 − 32. Two constructions were built and withdrawn and ADR 0217 says why: an unclamped backdrop comes to a negative grey the graphics library refuses by its own validity test, and scaling the channel needs every colour in the group scaled, which an image's samples are not. Two residues stay reported by name with the closed form for what each costs. **`DeviceGray` counts as subtractive**, which closed a silence nothing had named: a `k` colour in such a group was the same departure one component narrower. Spec track: §10.4.2.3 was `inapplicable` on the reasoning that "neither conversion is on any route to a pixel", and it is on 76 of those 90; §10.4.2.2's note said the NTSC weights were "asked of nothing here" and every luminosity mask is that formula. corpus 72 → **73**, the rise a new report — `bug1703683_page2_reduced.pdf` draws a `/DeviceN` shading inside a `/DeviceGray` mask group — and two documents lost the old one because the departure is gone. Two pages of 1794 moved, one of them only its label | 0217 |
| 381 | **The interpreter and the rasteriser are confined**, which principle 3 has asked for since ADR 0014 confined the three image codecs and left the larger surface in process. `viewer-confined`'s `pdf-view-worker` holds a `viewer_core::Viewer` and `render-cpu` behind seccomp-BPF, Landlock and a 4 GiB ceiling, and **§0's prediction held exactly**: `Command`/`Event` with `Raster` payloads means the display list never crosses, one protocol instead of two — and `viewer-core` needed **no change at all**, because rules 2, 3 and 4 are a description of a confined process. Every command crosses but `RenderReady` and every event but `NeedsRender`, each refused *by name* because the confined side answers it itself; fourteen of twenty-five questions cross and the other eleven answer with `pdf-model` types and say so. Every `match` in the transport is exhaustive over a `viewer-core` enum, so a message added there fails to compile here. **Three things the confinement forced, each measured**: images decode in-process because a confined process cannot spawn one (which costs panic containment — a page instead of the host — and a JBIG2 document proves it draws); the pipe has no deadline and the reason a decode's would be wrong is written down; and the page draws on **one thread**, because `glibc`'s allocator sizes its arena count from `__get_nprocs()`, which reads `/sys/devices/system/cpu/online` — found with `strace -k` on the twenty-fourth rayon worker of a page that had otherwise drawn. **And that one thread found a claim this tree makes about itself to be false**: ADR 0139's "the picture does not depend on how it was divided" is wrong by one pixel on a committed document — `doc/PDF20_AN001-BPC.pdf` page 1, (117, 636), 127 in one strip against 111 in every division from two to thirty-two — because a strip below the first is drawn under a composed matrix — **and that last clause is wrong, which session 382 established before fixing anything: composing a pure translation is exact, and the departure is one composition further on (ADR 0219, which also deleted `doc/todo/12`)**. The guard's six scenes are trap 12b again. 1.09 to 1.14 ms to start and confine a worker, 6.7 to 8.7 ms to a drawn page against 6.0 to 6.4 in process, 3.4 to 4.8 for 4.1 MB of pixels down the pipe. **Nothing is in front of the first frame**: `pdf-viewer`'s release binary is byte-identical with and without the round | 0218 |
| 382 | **One offset, applied last.** ADR 0139's "the picture does not depend on how it was divided" was false, and session 381's diagnosis of *why* was false too — `Transform::then` with a pure translation is exact, so the first strip's matrix was already the page's, bit for bit. The departure was one composition later: `encode_in_strips` folded the strip's row offset into the page transform **before** a mark's own transform was composed with it, so `fl(mark.f·d + F − k)` rounds where `fl(mark.f·d + F) − k` does not — one `ulp`, which is one of `tiny-skia`'s sixteen supersamples, which is 16 of 255. `Surface` and `ToDevice` now carry the page's own transform into a strip and apply the offset once, last, to the fully composed matrix; `Band` counts page rows, so a strip and the band a clip admits are one coordinate system instead of two. **And the question the todo file did not ask has an answer that changed the argument**: the same mark at 32× says the geometry covers that pixel to **86.6**, which is the midpoint of 95 and 79 — *neither* render was right, the edge sits on a supersample row, and the choice was made on consistency (the page is what is drawn; the division is not) rather than on accuracy. **Byte-for-byte equality is unattainable, and that is now proved rather than assumed**: a unit test shows this crate's matrices are the page's with a whole number of rows subtracted exactly, and a probe shows `tiny-skia` drawing the same path under that matrix into a surface starting elsewhere is *not* the same drawing — 2 of 280 pairs — while the three probes that missed it since ADR 0139 use dyadic coordinates, where subtracting an integer is exact. The counter-example page is exact now; ISO 32000-2 page 6 goes 20–28 pixels at worst **32** to 15–25 at worst 16. The gate is `pdf-model/tests/strip_parallelism.rs` — real pages, because six hand-written scenes could not hold the case (trap 12b) — and it asserts three things, two of which fail on the code this replaced. +0.08% and +0.24% instructions on the two specification pages | 0219 |
| 383 | **A raster in the quantity the clause composites**, which closes `doc/todo/23`'s fourth population — ADR 0217's two residues were one piece of work and this is it. §11.5.3 applies §10.4.2.3's `min` **after** the group is composited and a rendered channel holds `0..=1`, so the group's channel now carries `1 − ink ÷ scale`: **`InkScale` has exactly two values and §11.6.6 picks which**, one unit for a `DeviceGray` group because that clause's conversion *into* the space is itself the `min`, two for a `DeviceCMYK` one because four clamped components weigh `0.3 + 0.59 + 0.11 + 1.0`. What is left of §10.4.2.3 is **composed into the mask's transfer table** rather than added as a step, because `render-quorra` computes the luminosity in a shader of its own and takes a 256-entry table — so both backends are handed the same bytes and cannot disagree by construction, and `pdf-render` needs no new vocabulary. **The scale is only sound because the second residue went with it**: `Compositing` moved from `content.rs` to `colour.rs` and is threaded through `image.rs`, `shading.rs` and `mesh.rs`, so an image's samples and a shading's ramp reach the mask in the group's own quantity — §11.6.2 is the clause, "the current colour in the graphics state **or the source samples in an image**" — and `ColourSpace::reduced` stops reducing inside a mask group, where `to_rgb` is the identity on no device space. Three fixtures against the clause's arithmetic, each checked by putting the old route back: half-covered white over `/BC [1 1 1 1]` is **255 against 127**, a cyan `DeviceCMYK` sample **76 against 254**, and a `DeviceCMYK` ramp in a `DeviceGray` group `76.5 t` to the level, **40 against 66** mid-ramp. corpus **73 → 70** with nothing joining; `issue13520.pdf` moves 9.17% of its pixels by up to 27.6 of 255 and its ink 20.239 → 18.998 at 2×, toward both references. **And a silence ADR 0217 left behind is now reported**: a blend mode inside a `DeviceCMYK` mask group, where §11.3.5.2 applies a separable function to each component "expressed in additive form" and this composites one weighted average of the four — no corpus member, kept because the alternative is a silence, and the test is the only thing that shows it reachable. **A memo whose empty slot was a valid key**: `resolved_sample` shifted its tag out of the word at *four* components, so an all-zero sample tuple hit an empty `Conversion` slot and came back `Color::BLACK` — a `DeviceN` of four colourants at no tint, and every `DeviceCMYK` white inside a mask group the moment this round landed. Spec track: §10.4.2.4 was `inapplicable` on "cannot change a pixel" and is on the route §11.6.6 opens, where its every term provably cancels — the neighbour session 380 asked about | 0220 |
| 384 | **`--cpu` opens no driver, and a person can name the one that does.** The project owner ran this viewer on a Windows machine with Intel graphics and it **crashed inside the Vulkan driver** — and `--cpu` crashed too, because the flag chose which rasteriser drew the page and nothing else: `main` created a `wgpu::Instance` unconditionally, `resumed` built a presenter unconditionally, and the processor's raster was presented *through* the quorra surface because a working device was the only path pixels took to the screen. Three parts, and only the second is judgement. **`--cpu` now creates no instance, no adapter and no device**, and that is demonstrated rather than argued: `strace -f -e trace=openat` opens **17 shared objects where it opened 56**, with `libvulkan.so.1`, `libvulkan_lvp.so`, `libvulkan_radeon.so` and both ICD manifests gone; process start to first present **128–135 ms → 57–68 ms**, the software surface costing 0.16–0.19 ms where the device cost 15–16. **A software present path** is what makes that half shippable rather than a blank window — `softbuffer`, `rust-windowing`'s own, `kms` off — and its cost is written down including the one that is not the crate's: its X11 backend loads libX11 in a `ctor`, **before `main`**, which is what principle 2 forbids, measured at **0.4 ms** a launch (1.34–1.39 against 0.91–0.98 with `x11` off, 40 runs a batch) and taken because the alternative makes the flag useless on every X11 machine. `viewer_ui::software` composites the overlays with §11.3.6's formula, which is a third evaluator of that clause and now in its ledger row. **`--backend vulkan|dx12|metal|gl`**, which `doc/todo/12` deliberately did not add while quorra could not honour it: `2531f447` answered our feedback §12 with *exactly* the parameter asked for plus `Device::adapter_names_on`, which was not asked for and closes the trap the first would have opened — and our refusal prints both adapter lists for that reason, `adapters behind it: none` being a backend with no adapter against a non-empty list being one that cannot present. A named backend is **refused, not fallen back from**; the Windows default is **DX12**, which is the change rather than the preference — with no restriction the answer came from wgpu's hub order, an implementation detail of a dependency — and it *gives way* where the machine has none, because a default is our guess and a flag is their answer. **No machine here runs Windows, has an Intel adapter or has DX12**: that DX12 avoids the crash is untested and is not claimed. `.expect("presenter creation")` became five lines naming the stage, both adapter lists and what to try, exercised with `VK_DRIVER_FILES` pointed at nothing. And one prefix that was a lie: `QuorraRasterError::Device` said `resource upload refused` in front of four `DeviceError` variants about *construction*, so the round's first refusal read "resource upload refused: surface creation failed" | 0221 |
| 385 | **A round was ten minutes and eight of them were the machine's.** The project owner asked why, and principle 2 decides how to answer: the sequence `doc/todo/02` §2 and §5 name, after touching one file in `pdf-model`, was measured step by step at **607.9 s** — **235.7 s of it `cargo test --workspace`**, which runs 118 test binaries one at a time on a 24-core machine, and **175 s of it fat link-time optimisation over `codegen-units = 1`**, paid once per gate binary over a graph holding wgpu, vello and quorra. `cargo build --timings` names the shape: 66.4 s of a 76.9 s `pdf-viewer` build is **one unit**, with `user` time equal to `real`. Four changes, each measured and each with its cost written into `Cargo.toml` beside the setting. **`[profile.gates]`** — thin LTO over 16 units — takes the gates' compilation from 175 s to 55 s, `pdfref-hayro`'s newly-added 11 s included; **`cargo nextest`** takes the workspace's tests from 235.7 s to 76.6 s and **`opt-level = 1`** on the dev profile takes them to **21.9 s**, the suite's critical path having become a single test that draws a real page thirty-one ways; **`debug = "line-tables-only"`** halves an incremental dev compile, 12.9 s to 7.0, and a clean dev tree, 17 GB to 8; and §5's three binaries build in one invocation, 109.7 s to 79.3. **607.9 s → 268.0 s**, two samples 268.0 and 266.6. **`[profile.release]` did not change** and §5's fat link is now the round's largest single item, which is deliberate: those binaries are what a launch is measured with. The correctness criterion was stronger than usual because the gates are what moved — all eight ran under both profiles and their output was compared line by line with the clocks removed: **1794 oracle page verdicts, 957 quorra pages, 974 corpus documents, 4990 citations, every field identical**, and 1308 tests + 1 doctest either way. **And the comparison found something that was not about the profile**: the oracle's reference count fell by 861 under `gates` with every verdict unchanged, which is `Reference::Hayro` silently unavailable — `pdfref-hayro` is a program nothing in §2 has ever built, and its fourth reading survived only because an old release artefact sat in a long-lived target directory. It is a line in §2 now. **The owner's own suggestion was priced and refused**: reference renders are already 99.7% cached, the oracle's floor is one page at 8.6–9.8 s of its 25, and a key that cannot lie must name the binary — which is relinked on exactly the rounds that run the gate. `doc/todo/43` holds that argument and what a finer key would have to prove. The build directory was **311 GB** against a clean tree's 17 — 334 by the end of the round's measurements — and was swept to **8.1 GB** by hand, because `cargo clean --gc` is nightly-only and `cargo clean` would take the 1.5 GB reference cache with it (`doc/todo/02` §5a) | 0222 |
| 386 | **A panel is eleven answers, and none of them is a number.** `pdf-view-worker` carried fourteen of `viewer-core`'s twenty-five questions; the other eleven answer with a `pdf-model` type — an outline's tree, Table 147 whole, a decoded thumbnail, §14.7's structure — so a host on the confined boundary had **no panels at all**. All twenty-five cross now (ADR 0223). The property the boundary rests on is the compiler naming the message nobody handled, and a *struct* has no arms: every encoder in the new `protocol::panels` opens with a `let` naming **every field**, so a field added in `pdf-model` fails to compile here rather than quietly stopping crossing. One field is deliberately not carried and is named in the *type* — `viewer_confined::Attachment` has no stream, because §7.11.4's bytes already had `Command::Extract` and a list of five attachments would otherwise have pulled five payloads across a pipe. **A length was checked and a reservation was not**, and that predates the round: every list decoder refused a count larger than the bytes left and then called `with_capacity(count)`, so nine bytes claiming 2^31 strings had the host ask its allocator for 48 GiB and abort — `Reader::list` reserves 256 and grows. Four of the eleven are *trees*, so `ProtocolError::TooDeep` bounds a **message** at 64 where the readers that produce them stop at 32; §14.7's parent indices are checked against the list they index. The transport's stand-in test became the real fuzz target it owed, `confined_wire`, over all four decoders with its corpus seeded by a second implementation of the frame layer — **44 723 045 runs clean**, and its first minute found the *target's* defect: this format carries geometry as `f32` bits on purpose, so a message can say `NaN`, and `NaN != NaN`. The largest answer in the tree is an **outline**, not a raster: ISO 32000-2's 988 items are 88 233 bytes and 0.076 ms to read; a thumbnail is 31 100 and the other nine are single-digit bytes. What the eleven add to a confined launch is nothing, by mechanism rather than by measurement — they are questions. `doc/todo/01`'s fifth sweep, 231 `pub fn`s and 84 named by no host, found one gap of this round's own making and it is closed: Table 45's `/CheckSum` is about a checksum and the *decoded bytes* together, and this boundary puts them in two messages, so `attachment::checksum_matches` is a free function now with two callers. One is left open in the ledger: `Collection::initial_document` answers §12.3.5.1's `/D` fallbacks and **no host can call it**, in process or confined | 0223 |
| 387 | **A citation names a table, and nobody had ever checked which one.** Eleven rounds had landed without a sweep — the longest gap `doc/todo/01` has had — so all eight ran, and a **ninth** was built because two earlier rounds had noticed ISO 32000-1's table numbers in this tree and neither had swept for them. It parses every `Table N -Title` heading in `doc/md/` with its first-column keys and asks whether a cited `/Key` is one of that table's: **94 suspects, 18 defects, and they come in blocks** — §12.5.6.17, .18, .19, .20 and .22 (movie, screen, widget, printer's mark, watermark, every one naming the table beside its own), §14.8.5.5, .7 and .8 (list, table, artifact), §14.11.7's OPI, and nine source comments including two that define `/FontFile2` by the halftone types table and two more that give §12.5.6.10's text markup the line ending styles' number. **§12.5.6.23's own row says why this was owed**: the hundred-and-fifth session found exactly one of these, wrote "an ISO 32000-1 number" into the note, corrected its row and swept nothing — while four of its immediate neighbours carried the same error, one of them the watermark row whose number it had been given. `tools/conformance` checks a table *exists*, and a number that exists and names the wrong table reads exactly like a right one. **The first sweep found this file's longest-lived stale claim, 364 sessions**: §12.5.6.19 said a widget "draws its frame and reports" its field value, written in session 21 and false from session 23 — and the *test the row cites as evidence* had drifted into repeating it, because the fixture states no `/DA` and reports `Owed::NoFont` rather than anything about §12.7.4.3. The row was appended to four times after it went false. **The second sweep paid with a clause**: §12.5.6.6 called `/RC` "XFA rich text, which principle 5 excludes" — the fourth row to carry that sentence after §12.5.6.2's (342) and §12.5.2's (375), and the first where it hid a *different* `shall`. Table 177's `/RC` "shall be used to generate the appearance of the annotation", with a NOTE saying it explicitly is not Table 172's popup entry, so a free text annotation stating only `/RC` drew a blank page. `popup::rich_text` is `pub(crate)` and `appearance::free_text` takes its characters where `/Contents` is absent, in §12.5.6.2 NOTE 1's order (ADR 0224). **The corpus cannot see it**: 22 free text annotations state `/RC` and every one also states `/Contents`, counted by `markup_text_census`, so the two new tests are the whole defence and the first fails with "nothing was drawn at all" on the code without the change. §7.9.3 said "exactly six" entries are typed `text string or text stream` while listing eight, and that `/RC` was the only one in scope while `/RC` is two entries with two `shall`s. Sweeps 3 through 8 were clean: 33 and 108 capability hits, all true boundaries; 25 of 83 `inapplicable` rows naming live vocabulary, none wrong; two dead citations, both the known self-quoting false positive; 231 `pub fn`s with 84 unnamed, the same three populations. Ledger counts unmoved at 401/249/21/83/8/113 — every finding was a note, not a status. And the handover's own incomplete count was 73 for four rounds after ADR 0220 took it to 70, counted off the gate here | 0224 |
| 388 | **A click is an offset, a drag is two, and the standard states neither.** ADR 0211 gave a field a caret and left its inverse owed: a click aimed the keyboard at a field and put the caret at the *end* of the value, wherever inside it the press landed. Two questions close it — `Query::Offset { at, point }`, which is `Query::Caret` run backwards and answers the byte a point is nearest, and `Query::FieldSelection { at, from, to }`, the shapes over a range. The second is a *third message rather than two carets*, and §12.7.5.3's Table 231 bit 13 is why: a multiline value is broken into lines by §12.7.4.3's own layout, so the lines between the two ends are `pdf-model`'s to name. Both are computed inside the walk that writes the stream, for ADR 0211's reason. **Copy, cut and paste needed no message at all** — the characters are a slice of the value a host has already read back, and the edit is the `Edit::SetField` a keystroke already sends — which is the vocabulary question the todo file raised, answered with no vocabulary. In the window: a click at x = 368 moves the caret from 381–383 to 369–371 and one `x` there makes *typed* into *typxed*; a drag highlights x = 357–382 and sends no command; Ctrl + X and V take *typxe* out and put it back byte for byte. **A third key-order defect**, the same shape as the Escape one: `o` and `?` were answered before `typed` was asked, so both toggled chrome from inside a field. Free text was re-read against the tree and two of its three blockers had expired; the third has not, and the todo file says which | ADR 0225 |
| 389 | **A shape thinner than the quantum that measures it.** §10.7.4 promises "no shape ever disappears" and two still did — a fill under an eighth of a device pixel and a sub-pixel stroke within half a pixel of the raster's edge — **both of them the oracle's, on a device that drew every one of them to within 2% of its area**. That is a defect in the *instrument*: `render-quorra/tests/corpus.rs` calls a difference between the two backends quorra's by construction, so on a page of sub-pixel line work the backend carrying the right ink was the one being accused. The mechanism is one: `tiny-skia` supersamples four times per pixel row at each sub-row's centre, so its smallest coverage is a sixteenth of a pixel — and its painter draws a stroke under a pixel wide as a hairline smeared symmetrically about the path, which conserves the ink and loses the half that falls off the raster. `pdf_render::sub_pixel_bands` draws an axis-aligned rectangle thinner than a device pixel as **the whole pixel line it lies in, at the coverage its own area there implies**, and a sub-pixel stroke on a straight axis-aligned rule is outlined into one first; the coverage rides in the paint's alpha, which §11.3.7.1 says is shape times opacity and §11.3.7.2's NOTE 1 says anti-aliased coverage is the first factor of, with §11.4.6's knockout declined by name. Both backends now answer within **one level of 255** of the area at every thickness measured, where the processor answered 0 at two of five. **The rule takes a promotion away as often as it gives a mark back**, which is what says it does not fight the anti-aliasing departure: at 0.9 of a pixel `tiny-skia` rounded a rectangle up to a whole row, 11% heavy, and `issue8125.pdf` page 1 left the oracle's contradicted list on that half rather than the vanishing one. Gates, before and after: oracle **858 agree / 68 contradicted → 859 / 67** with 401 of 1794 pages' numbers moving, quorra **914 / 42 → 920 / 36** with `issue16038.pdf` moving 6.5359 → 1.8563 — the largest movement that gate has recorded — corpus 70 incomplete and the same set, text 99.2% unmoved, step 7's head unchanged in order. **0.12%** of the rasteriser's instructions on a page of the standard. What it declines is a rule that is not axis-aligned, so `22060_A1_01_Plans.pdf` is unmoved to four decimals and `doc/todo/11` carries that as the residual | ADR 0226 |
| 390 | **`--trace` said *where* a slow frame was and not *what* it spent its time on**, and the project owner raised it from a Windows trace of a 30 MB, 65-page document that felt slow: 63 presents, median 60.4 ms, p90 157, max 514, and one number — `present -> presented in T` — over this host's queries, the display-list-to-scene walk, quorra's encode-transfer-execute and a `render-cpu` fallback, **plus the accessibility publication, which is not the frame**. All seven of the item's points are closed (ADR 0227): stages inside the one frame line, `attend` and `Launch::arrived` outside its timer, percentiles at exit by nearest rank, a clock on every line, a closing line for pipeline compilation, and `--trace=<topics>` — a **set**, argued rather than picked, because the seven topics are not ordered and a level would have to invent an order. **Almost none of it needed a new measurement**: `quorra_gpu::Device::render` already timed `encode`, `upload` and `execute`, already said whether the last came from timestamps or a wall clock, already counted culled commands — and already blocked on the device before returning, so no boundary here is fabricated — and `render-quorra` was discarding the whole `Frame`. It costs **0.30 µs a frame** unconditionally and **0.23 µs** a line. Then the instrument was run on the owner's own document under `Xvfb` (`llvmpipe`, so shape rather than absolute numbers): **81% of a frame is inside `Device::render`** and its largest single part is `encode`, which is CPU; **this host's own work is 0.3 ms over 40 frames**; our translation is 18% and **bimodal on resources rather than commands** — a 388-command page costs 15.9 ms where a 3675-command page costs 1.0; and the accessibility publication is **2.0 ms a page turn**, 6.7% of what the old figure called the device's. The trace's own contradiction was settled too: `note: this build has no accessibility bridge` is **true** and `trace: accessibility bridge up` was **false** — `Bridge::new` builds an adapter only under `#[cfg(target_os = "linux")]` — so off Linux the tree was built, published to nothing and dropped, on every page turn. Written into `doc/todo/45` rather than fixed in the round that measured it | 0227 |
| 391 | **A 388-command page cost sixteen times a 3675-command one, and the whole of it was one photograph.** ADR 0227's instrument had said the display-list-to-scene walk was bimodal on *resources* rather than commands and named two suspects; a throwaway probe of seven `Instant` pairs settled it in one run — the shading row reversal never executes on the witness at all, and `Image::area_averaged` is 18.45 ms of a 19.40 ms `scene`. **The cost is per source sample**, which is why no instrument that counts commands could see it. Two changes, both exact and both measured by a standing example (`pdf-render/examples/area_bench`, best of 100): the column bands are two 64-bit divisions asked once per *output cell* and are now asked once per image, and the output rows are divided across rayon above a floor of **65 536 source samples** — set above the crossover on purpose, because a floor belongs where the division is worth having rather than where it first wins, and because `render-cpu` calls this from inside its own strips. A 2700×3450 image reduced threefold: **22.39 ms → 2.93**. The session's frames: **1225.7 → 1074.0 ms**, `scene` **210.1 → 68.3**, `device` unmoved — three runs each, and the spread inside either condition is a fifth of the difference between them. **One defect came out of the same function**: `is_consistent` was asked *after* the reduction, and `f32::clamp` panics when its minimum exceeds its maximum, which a zero-width image makes it do — reachable through `render-quorra`, the one of three callers that did not ask first. **The 2 ms accessibility publication was not §14.7's tree either**: `Query::AccessibilityTree` is 0.13 to 0.25 ms and the whole publication 0.17 to 0.33; the cost was `App::place_window`, two synchronous X11 round trips for a window position a page turn cannot move. Asked at bridge-up, `Moved` and `Resized` now, with `Bridge::wants_window_bounds` deciding it in the crate that owns the adapter — deliberately **not** `Bridge::shortfall`, because `doc/todo/31`'s Windows and macOS adapters take a window handle and the two answers will part company. **2.36 → 0.27 ms a page turn.** And what is quorra's was reported rather than changed (`QUORRA_FEEDBACK.md` §13): `encode` is 45% of a page turn, is CPU, and is the only phase that tracks the scene — **3.86 µs a command plus 3.84 ms** over 38 frames spanning 388 to 3675 commands — with a subdivision asked for, an instrument before an optimisation. That section also **retracts** a row this side prints: `elsewhere` subtracts the adapter's timestamp-query duration from a host wall clock, so it is a bound and not a duration, and the summary says so now. **And a gate had a hole of the same shape as the thing it checks**: `another_document` refuses a `§` after a project document's file name — an upper-case stem and `.md` — and `doc/` is not upper case, so every citation written with a path passed for the whole of the arm's life. Eight in the tree, **six of them naming `QUORRA_FEEDBACK.md`**, which is the document the arm's own comment cites as the case it exists to catch. One `rsplit('/')`, a test, and eight rewrites to "section N"; the conformance gate's citation count is 5095 → **5133** net of the eight it stopped miscounting as ISO 32000-2's | 0228 |
| 392 | **A signature's second question, answered — and the dependency decision it hung on came out `no`.** ADR 0215 separated §12.8.1's three questions and answered the first; `doc/todo/51` carried the second as a decision nobody had been asked for. It is taken in ADR 0229, with numbers: `rsa` 0.9.10 costs **31 packages** and a *second* `digest` 0.10 hash stack beside this tree's 0.11 (two copies of SHA-256, and `cms::Digest` unusable with its API), `rsa` 0.10 is a release candidate, `p256`+`p384` cost **28** and cover two of the five curves RFC 5480 names, and `x509-cert` sits on strict DER, which **refuses four of the corpus's ten signature values** — the ones Adobe's handler writes with an indefinite length, as `openssl pkcs7` does. So both halves are in tree: `pdf_model::x509` (**213 lines of code**) and `pdf_model::pkcs1` (**356**), under `#![forbid(unsafe_code)]`, with **no package added and `cargo deny` clean on all four**. The decisive argument is that ADR 0031's case for taking the *ciphers* has no instance here — **there is no secret**: verification is `s^e mod n` over three public numbers a stranger wrote, so the defects wide review buys protection from have nothing to act on; and the one that would matter, a verifier that accepts a forgery, is ruled out by RFC 8017 section 8.2.2's whole-block comparison rather than by review. Montgomery multiplication, **no division anywhere**, two budgets reported by name (8192-bit modulus, 256-bit exponent). **All ten of the corpus's signature dictionaries verify** — 1024 × 3, 2048 × 6, 4096 × 1 — in **4.583 ms between them**, and all ten stop verifying when one bit of the signature value is turned over, which is the check that keeps ten-out-of-ten from being a rubber stamp. Two findings: `bug854315.pdf` had question 1 *answered by* question 2 — its `SignerInfo` states no signed attributes, so RFC 5652 signs the byte range itself and ADR 0215's `UnderTheSignersKey` was this program saying it had no key; the key was in the file, where §12.8.3.3.1 requires it, and **the document has not changed** — and the four that answer `Changed` to question 1 **all verify**, so they are real signatures whose documents were re-saved underneath them. `openssl` agrees on one of three and refuses the other two for reasons that are not the signature (an indefinite length; a `digestAlgorithms` set naming SHA-1 where the `SignerInfo` names SHA-256), so a twenty-line Python `pow()` was written as a third opinion and verifies all three. The words are the other half of the round: nothing says *valid*, and the once-per-document paragraph ends "a signature that verifies here was made by whoever holds the key in a certificate that arrived with the document, which is not the same as a valid signature". §12.8.3.2 and §12.8.3.4.5 move `reported` → `partial` (`partial` 249 → **251**, `reported` 21 → **19**), and the sweep found **six expired claims** — four ledger rows and two module comments — all of the shape "that is X.509", which was a *structure to parse* doing the arguing for a *trust decision* | 0229 |
| 393 | **A transition is two pages and a fraction, and the window was the only thing that could see either defect.** §12.4.4's transition had been *named* since the hundred-and-fiftieth session and drawn by nobody, and this round drew it. The cut the architecture forces is the whole design: `viewer_core::transition::frame(&Transition, viewport, progress)` shapes the frame at a **fraction** of the way through — rule 3 keeps the clock out of the crate — and `Frame::draw` turns it into a display list of two page rasters with a clip apiece, so **both backends draw it** and the CPU rasteriser keeps its job as the frame a device refuses. **Seven of Table 164's twelve styles**, and the line is *the ones whose frame the table's own words determine*: `Wipe`, `Split`, `Box`, `Cover`, `Uncover`, `Push`, `Fade`. The other five are **reported by name** rather than cut in silence, each for a quantity the clause does not state — how many lines a `Blinds` has, how wide a `Glitter`'s band is, what a `Dissolve` does to a pixel, what a `Fly`'s "changes" are — with `R` the cut by definition. `viewer-ui` gained one key, `p`, which is the whole of presentation mode here (ADR 0135 decided the core has no such state), and the loop wakes ten times a second between transitions and polls during one. **Two defects came out of running it under `Xvfb` and reading pixels back, and no gate in this tree could have found either**: `Viewer::handle` settles *after* the command that turns the page, so the arriving page's display list is in the event *after* the transition — a host that began at the event cross-faded the outgoing page with itself, 430 frames drawn and a screen that never changed — and a `Command::Image` puts its top row at y = 1, so every frame was upside down, which a page of one flat colour cannot see. The census that framed the round: **0 of 978 documents** state a `/Trans`, a `/Dur` or a `/PresSteps`, asked of the page tree rather than the bytes | 0230 |
| 394 | **A collection opens on one document, and for thirty-four sessions nobody could say which.** A sweep round: all nine of `doc/todo/01`'s run over `ledger.toml` and over `crates/`, and the two leads earlier rounds had left named were taken up. §12.3.5.1's `/D` is the one the fifth sweep found in the three-hundred-and-eighty-sixth — `Collection::initial_document` answered Table 153's four outcomes and **no host could call it**, because the `/EmbeddedFiles` name tree it resolves against is the document's and a panel holds the dictionary. `Answer::Collection` carries the resolved `Initial` beside Table 153 now, the confined protocol encodes its four cases, and `viewer_ui::chrome` sets the initial document's row in bold — the first file row where `/D` names nothing the tree holds, which is the clause's own `shall`, and a sentence instead of a blank panel where the tree is empty. **The emphasis is a choice and says so**: the standard states no appearance for an initial document (ADR 0231). **The other lead stays named and was re-checked rather than believed**: §12.5.6.19's seven Table 192 entries are read nowhere, and the two hits a grep finds for `/RI` and `/IF` are §8.6.5.8's rendering intent and §12.7.8's FDF field. **The sweeps paid four times, and three of the four were parent rows.** §12.1, clause 12's own map, said "§12.8's signatures read and never verified" one round after ADR 0229; §12.6.4 said "three are performed" where its own eighteen children say **eight**, four of them implemented by four different sessions and `/Trans` by the round before, and §12.6 repeated the three one clause up — **the sixth sweep cannot see this**, because it asks whether every child is *settled* and four of §12.6.4's are `reported` or `out-of-scope` for good reasons. The ninth sweep, on its second run, found two wrong table numbers: `navigation.rs` opened "[`transition`] is Table 164's `/Trans`" nine lines above its own "Table 31 lists both as entries of a page object", written that way in the three-hundred-and-ninety-third; and §12.6.4.2 cited "Table 206's `/D`" where the go-to action's is **Table 202**'s — a suspect the three-hundred-and-eighty-seventh's run had printed and not read. Counts unmoved: no status changed, and §12.3.5.1 stays `partial` for Table 153's `/View`, which is three presentations of one collection where this panel draws one | ADR 0231 |
| 395 | **`doc/HANDOVER.md` was 3452 lines and half of it was needed by somebody doing one particular thing.** At the project owner's instruction the file was halved a third time — **3452 → 909** — by two operations kept distinct. **Extraction is a move**: ten files under `doc/` now hold *all* of what they took, not a précis, and the handover keeps a pointer that is the whole entry — this table among them, which is narrative by the file's own definition. Every section number an outside reference names (`§0`, `§1`, `§3a`, `§4`, "Verify it", "trap 5") is still a heading in the handover, so **not one of the dozens of ADR references had to be edited**, and the live documents were pointed one hop further on. **Deletion is for what is false**, and the round's gate run found four: the tests row said 1369 + 1 = 1370 where `nextest` printed **1371** and the doctest **1** (session 394 added two tests and moved none of the three); the conformance row said 5139 citations and 516 quotations **while a paragraph three lines above it said 5195**, where the gate prints **5215** and **520**; "what is not implemented" listed *sandboxing the interpreter and rasteriser* fourteen rounds after ADR 0218 built it and while the same file's "Where we are" described it; and "`doc/md/` … committed" had been false since the three-hundred-and-eleventh encrypted the specifications and `.gitignore`d the conversions. Two documents stating the gate sequence is how they drifted — `doc/todo/02` §2 owns it now and `doc/verify.md` owns everything else, which also retired "the five fuzzers" beside a paragraph naming twelve. All ten gates re-run and every number in the table read off what printed: corpus 974 with **70** incomplete, oracle **1688** complete with 859/67/751, text 99.2% (24043/24243) and 25 below the floor, quorra **920/36/1/17**, dates 1514 of 1545, XMP 318 read and 1 refused, JPEG 2000 14 byte-identical, ledger 875 rows unmoved at 400/252/19/83/8/113, **214 s** for §2. No code changed | ADR 0232 |
| 396 | **The blocker was an API that had existed for eight months, and the real cost was an allocation nobody could see.** `doc/todo/24`'s last owed item — §7.4.9's reduced-resolution JPEG 2000 decode, for `issue19517.pdf`'s 847-million-sample scan — was written down as blocked on "an API `hayro-jpeg2000` does not have". It has had `DecodeSettings::target_resolution` since `1dfc6e2f`, **10 December 2025**, inside the revision this workspace already pins, and ADR 0210 had built the display list's half twenty-six rounds ago: **both halves existed while three places here named one of them as the blocker** — the ledger's §7.4.9 row, `pdf-sandbox`'s comment and the todo file. What the measurement found instead is one layer down and is the crate's: `build_decompositions` sized the coefficient buffer from the component tile's **full-resolution** rectangle whatever level was asked for, so a reduced decode began with one allocation of **3 390 240 768 bytes** — for every target, including the one asking for 1/256th of the samples. Resident size never shows it (the buffer is `calloc`'d and those pages are never touched); **address space does**, and address space is what the confined worker's gigabyte bounds. Fixed on `close2/hayro` branch **`feat/reduced-resolution-allocates-less`** (`1dc833f7`), by B-15's own partition — the sub-bands of levels 0..=r partition level r's rectangle — with the skipped levels' **code-blocks still built**, because a packet header is what says how long its body is and LRCP interleaves them. Peak address space **3336 → 115 MB**, **3424 → 241**, **3775 → 743**; under a 1 GiB `RLIMIT_AS` none of four targets completed before and three complete after; **all 183 assets of hayro's own suite byte-identical** to snapshots from unpatched `main`, and Annex B.4's worked example passes. This tree **committed no behaviour change on that path**: against the pinned revision the same code trades an accurate refusal for a worker killed by `RLIMIT_AS`, and a `path` dependency into `tmp/` is a tree that does not build from a fresh clone. Measured against a `[patch]` build so the number exists: page one goes from **0 commands and one report** to **1 command and nothing**, a 3152×4202 raster agreeing with `poppler`, 5.60 s and 2545 MB. **What did commit is a bound that was wrong on its own terms**: `MAX_SAMPLES` estimated "roughly five bytes per sample" and 2^27; measured on codestreams built for it, 2^26 is **600 MB** and 2^27 is **1253 MB and fails** inside the gigabyte — so the bound that exists to refuse *before* the address-space limit kills the worker was admitting an image that kills it. 2^27 → **2^26**, no corpus codestream between the two. The ninth sweep, third run, found a **block of four**: §12.4.3's thread dictionary is Table **162** and `interact.rs` (twice) and `articles.rs` (twice) all said Table 158, the collection *split* dictionary — and `view.rs` still said "Table 192's `/H`" where the three-hundred-and-eighty-seventh had corrected the ledger row to **191** and left the code | ADR 0233 |
| 397 | **A shape is not an alpha, and §11.4.6 is the one place a page can tell.** `doc/todo/23`'s first population — a knockout element whose shape is not its coverage, 5 corpus documents — is closed, and it was chosen because its answer is a *display list* where the other two are a raster format: §11.4.4's NOTE 5 residue needs a buffer whose colour is the backdrop while its alpha is zero, which neither premultiplied rasteriser has, and §11.6.6's painted group needs four components through three backends. `pdf_render::Command::Shaped` states the object beside the same object with §11.6.4.3's mask and §11.6.4.4's constant **removed**, so the alpha it draws with is §11.6.4.2's shape and a group's is the union of its elements'; §11.4.6's two stages then come to `P' = (1 − f) × P + S`, which both backends draw as Destination-Out with the shape and **Plus** with the object. The second operator is the clause and not a detail: source-over there weights the backdrop by `1 − f × opacity` a second time, which is **32 of 255** at a half-covered pixel under a half-opaque mark and is the third fixture's whole reason. Corpus **70 → 67**, and the oracle **74 → 69 contradicted with 899 → 904 agreeing** — `knockout_nested`, `knockout_nested_group_alpha`, `knockout_smask` and `knockout_groups_test` pages 2 and 3 stopped being contradicted, against fixtures derived from the clause rather than from anyone's output. **`render-quorra` refuses it by name** and four pages moved from `agree` to `refused`: `quorra_scene::Compose` reads the shape off the coverage, which is the assumption the element exists to contradict, so those four used to agree about a picture both backends drew wrongly (`QUORRA_FEEDBACK.md` section 14). And the round could not leave §11.6.4.3's **`/AIS`** alone: it inverts which quantity is which, its ledger row said the distinction "cannot show" — false since the seventy-first session, because a knockout group modulating Source by coverage reads `ca` as opacity — and the same row said no corpus document writes the entry, where **53 state it and 9 state it true**. Read now, and every knockout group refused while it is set, at no cost to any page | ADR 0234 |
| 398 | **A form is a description, and a check box is a name the file invented.** `doc/todo/37`'s audit — taken at the project owner's request — had found six populations of interactive chrome crossing `viewer-core`'s boundary and **five of them crossing as data**; §12.7's fields were the one a native host could only take as pixels, and a text field has the strongest claim of the six because it *is* a `QLineEdit`. `Query::Fields` closes it: `pdf_model::form::fields` describes every field with a widget on the page — §12.7.5's four types with buttons split the three ways §12.7.5.2 splits them, Table 231's six flags and Table 233's five, Table 229's two, Table 232's `/MaxLen` and the comb's cell count, Table 234's items with their export values and which of them `/V` selects, Table 227's three, Table 226's `/T` beside §14.9.3's `/TU` — and `viewer-core` places each widget's `/Rect` through the one arithmetic every shape it hands over goes through (ADR 0118's). Table 233 bit 20's `Sort` is deliberately **not** carried, on the table's own instruction that "PDF readers shall display the options in the order in which they occur in the Opt array". **Writing down what already crossed came first** and the audit turned out to have understated the gap rather than overstated it: reading §12.7.5.2.3 against the code found that **a check box could not be checked at all, by any host, on either drawing path**. The clause states two sentences — "[t]he value of the V key shall also be the value of the AS key. If they are not equal, then the value of the AS key shall be used instead of the V key" — and this tree obeyed only the second, so a reader that changed `/V` left `/AS` behind and the widget went on drawing the state the file was saved in. Measured on `issue17492.pdf`: **527 display-list commands before the edit and 527 after**, against **528** with `appearance::appearance_state`, which is narrowed to the one field type whose appearance its value selects and reads §12.7.5.2.4 forwards — a widget whose `/AP` has no stream under the new name is one of the *other* buttons of a set and is off. The constructed path was wrong differently: `Field::is_on` compared the replacement against `Off` as a **name** while `set_field` encodes what a host sends as §7.9.2.2's text string, so no edited check box had ever read as on. **Three consumers, because a vocabulary with none goes stale** (ADR 0178): the headless host enumerates a real form and checks a box with the name the page gave it, `viewer-confined` carries the answer as its **twelfth** panel encoding and compares it field for field against the same document read unconfined, and `viewer-ui` **toggles a check box and a radio button from a click** — the first thing in this tree that gives a button field a value, with Table 229 bit 15's `NoToggleToOff` deciding what a click on a button already on does. The §12.7 ledger family was read against the code and **five rows were stale, four of them the capability shape**: `/Opt` "changes no mark this tree makes", Table 234's three "read nowhere", `RadiosInUnison` not read "because it describes what happens when a button is clicked", DoNotSpellCheck "an editor this program is not", and §12.7.5.2.3's own claim that reading `/AS` and never `/V` "was already right before it was read". The ninth sweep, run over `crates/` as `doc/todo/02` §4 asks, found two wrong claims in source: `FLAG_MULTILINE` cited **Table 227** for a bit only Table 231 defines, and `appearance::widget` said Table 192's `/R` "is read nowhere yet" — false since the hundred-and-fifth session, where `Rotation` arrived and where the ledger row has said it is applied ever since. Every gate unmoved: corpus 67 incomplete, oracle 904/69/786, quorra 916, text 99.2%. Two gates beyond §2 were run because the round earned them: `confined_wire` at **13 942 159 runs, clean**, and the **window under `Xvfb`**, where a click at the box's own centre printed `note: setting the field typeScript to Yes` and a second one printed `Off` | ADR 0235 |
| 399 | **A gradient drawn across a page, to be kept in a few dozen pixels.** `doc/todo/40` had been the corpus's worst page for a hundred sessions and said it was *priced and blocked on a measurement*; the measurement was taken and said the item was never the largest thing on the page — and the profile the file quoted had said so all along, with `gradient` first at 36.6% and the two mask lines it added together second at 24.34%. **What that page is made of is what nobody had asked**: `pdfimages -list` names no image, and the content stream holds **3490 `sh` operators** against 479 `f`, each a whole-page rectangle under a clip that admits about 24 pixels. §8.7.4.2's Table 76 bounds the operator by the clipping path and by nothing else — so a display list states it as a fill of the page — and a rasteriser shades the *path's* spans and multiplies the mask in afterwards, which is 10.4 M pixels a render to keep 85 608. The same table says the cost out loud, *"which may be time-consuming"*. `pdf_render::cropped_rectangle` narrows an axis-aligned rectangle to the rectangle its mask can mark — the one `MaskCache::build` already measured and threw the columns of away — and the page halves: **38 453.3 M → 20 030.7 M** instructions over twenty renders, `gradient` **15 783.8 M → 578.9 M**, wall clock 2.45–2.63 s → 1.43–1.52 over seven samples each. **The pixels did not move**: the PNG is byte-identical, the ink sum of twenty rasters equal to the digit, and every gate reproduces the handover's line character for character. Exact because a smaller rectangle changes coverage only outside the retained interior, where the mask is zero, and because the crop is computed in the *path's* space so an unnarrowed side keeps its own bits; declined for curves (ADR 0139's re-parameterisation), for shears, and where the clip covers the surface — that last decision measured, since asking per fill cost page 6 **+0.42%** and asking per chain built costs **+0.08%**. Not one document's shape: over the corpus's first pages **49 carry a shading fill and 99.7% of their shaded pixels lie outside the clip**. `doc/todo/40` stays open, re-derived rather than inherited: worth **42% of `MaskCache::get`** rather than most of it (3551 leaves through **7066 distinct nodes**, so sharing is 1.99 to a leaf), **not blocked on memory** — the peak is **12.31 MB** where the file said 27.9, which was true in session 113 and stopped being true when ADR 0132 deduplicated identical clips — and resting on the sentence **ADR 0219 refuted**, that a mask value does not depend on which band holds its row | ADR 0236 |
| 400 | **A second accumulator the clause divides out again.** `doc/todo/23`'s §11.4.4 population — a non-isolated group NOTE 5 cannot flatten, 6 corpus documents — had stood since the seventeenth session behind a sentence this project had written down twice, most recently in ADR 0234: NOTE 4 wants Table 140's **group alpha** kept apart from the composite alpha, a premultiplied raster holds one, therefore one raster cannot. **It can, and the reason is that the quantity NOTE 3's removal divides out is multiplied straight back in** — the group's object alpha *is* `αgn` — so when §11.3.3 composites the result onto the same backdrop under the **Normal** blend function the two cancel and what is left is `result = (1 − w) × B + w × E(B)`, `w` the constant alpha times the soft mask, `E(B)` the elements drawn onto a copy of the backdrop. Exact for every backdrop alpha and every blend mode *inside* the group; `w = 1` is NOTE 5's own flattening. Checked by transcribing §11.4.4's recurrence and §11.3.3's formula and running them over **200 000 random inputs — worst deviation 5.6 × 10⁻¹⁶**, against **0.601 of full scale** if a non-Normal blend mode at the `Do` is assumed away, which is why that condition is the one that binds. A census first: all six documents have `alpha = 1` and Normal at the `Do`, and **four of the six are a soft mask** while the other two are knockout questions. `Command::Group` gains `isolated`; `render-cpu` seeds the buffer from the surface and writes the line in **one pass** rather than Destination-Out plus Plus, because two draws round twice and at `w = ½` over an opaque page leave alpha **254 of 255**; `render-gpu` and `render-quorra` **refuse by name**, a Vello layer and a `quorra_scene::GroupSpec` both beginning transparent. Corpus **67 → 65**; quorra **916/36/5 → 912/36/9** with ADR 0234's argument a second time — all four used to *agree* about a picture both backends drew with the wrong initial backdrop (`QUORRA_FEEDBACK.md` section 16 asks for the flag, and its `Compose::Src` is already the operator). The oracle's totals are **unmoved at 904/69/786** and the movement is inside them: 1691 → **1693** pages called complete, so the two documents that stopped reporting were judged for the first time, both landed *ambiguous*, and both needed a diagnosis — `issue12798_page1_reduced.pdf` ends **0.0030 of 255** from `poppler`'s limit on three parallel ladders where the two references are 0.120 apart, and `issue13520.pdf` has five renderers spanning **3.65 of 255** on a 209-pixel illustration with the two references' limits 0.743 apart and approaching from opposite sides. `doc/todo/00`'s step 7 byte-identical before and after, with the one page that moved measured directly: ours 19.1288 → 17.2724 against `mupdf`'s 16.5747, a gap of +2.554 → **+0.698**, in the middle of the distribution where the head and tail cannot see it. Three fixtures from the clause's arithmetic, each checked by putting the old route back — the third nests the group inside an isolated one, because a page is opaque and over an opaque backdrop source-over and the interpolation agree; under a half-opaque backdrop they are **32 of 255** apart, the same magnitude ADR 0234 found one clause over. Five §11.4 ledger rows read against the code and corrected, their Table 138/139/140/145 citations checked against the standard's own titles, and §11.4.6's row lost a sentence that stopped being true the moment §11.4.4's did | ADR 0237 |
| 401 | **The one annotation whose text is the annotation.** `doc/todo/33` had owed §12.5.6.6's free text since the three-hundred-and-twenty-first session gave a person §12.5.6.10's four markups, and the three-hundred-and-eighty-eighth had already found two of its three stated blockers expired. The third was real and it turned on one sentence of the clause: a highlight "appear[s] … in the text of a document" and needs only a selection, while a free text annotation "displays text directly on the page" and has nothing to be over — so its geometry has to come from a **drag**. `Edit::FreeText` takes two viewport corners, `Edit::SetFreeText` names the annotation by object because §12.7.4.2 gives a *field* a qualified name and nothing gives an annotation one, and `Query::FreeTextAt` is how a host learns what its drag made — a question rather than an event, which is the rule this vocabulary has grown by. **The caret needed no new arithmetic at all**: §12.5.6.6 sends its own subtype to §12.7.4.3, so `frame` and `laid_out_in` dispatch on the subtype and the three questions above them are one implementation for a field and for an annotation. Two things came out of *running* it rather than reading it — Table 166's `/Border` defaults to `[0 0 1]`, so the program reported the annotation it had just created until it began writing a `/BS` with Table 168's `/W` 0, and a comment that had said the border was "reported only where a width is stated" was false for fourteen sessions. §12.7.4.3's `/DR` `shall` binds a writer as well as a reader, so a save now states the `/DA`'s font in Table 224's `/DR` and builds the interactive form dictionary where the document has none. In the window: a drag changes 22 pixels, all of them the caret; `Reviewed` changes 271; the saved file is 174 090 bytes over the producer's 173 159 byte for byte, and `pdftoppm` and `mutool` differ from the unedited page in *the same* 10×46 box. Table 177's `/CL` and `/LE` are reported by name, which is also the whole of `/IT`. ADR 0238. |
| 402 | **A sweep round, and the tenth sweep found what the ninth could not see.** Ten sweeps run over `ledger.toml` and over `crates/` after eight rounds with none. **The tenth was invented by session 394 and built here**: it compares a parent row's stated *count* of its children with what the children say, which the sixth sweep cannot do because it reads the status column and this reads the sentence. **16 hits, two wrong, and both are the fifth failure shape at family scale.** §14.11 said "[t]wo of its seven subclauses reach a screen … and both are implemented", wrong on both halves — §14.11.3's printer's marks left the "for a press" list in the three-hundred-and-fifty-ninth when the seventh sweep read the clause's own "shall be set and **all others clear**", and this row went on naming them among the rest for forty-three rounds while the child's own row carried the correction; and §14.11.2 is `partial`, not implemented. §12.3 said §12.3.5's collections and §12.3.6's navigators are "both … read as data with nothing presenting them", false of the first since the three-hundred-and-fifty-second drew the files tab (ADR 0202). Neither is visible to the sixth sweep, because neither family's children are all settled. **The second sweep produced the round's oldest finding**: §14.7.2 listed `/IDTree` as unread "and needs a consumer that names one", and `Tree::element_by_id` has read Table 354's name tree since the **ninety-fifth** session, with two callers — §12.3.2.3's structure destination and Annex O's `structelem` fragment, since 369. **The ninth found three defects and all three were in the source, beside ledger rows already corrected**: `annotation.rs` said "Table 192 gives `/H` the default `I`" where `/H` is **Table 191**'s, which is the exact correction the three-hundred-and-eighty-seventh made to §12.5.6.19's row; and `appearance.rs` twice and `tests/annotations.rs` once called §12.5.6.12's stamp legends "Table 186's list" where they are **Table 184**'s and the ledger row had said so all along. **The fourth paid twice on `/AIS`** — §8.4.5 still listed it among Table 57's *not read* entries with ADR 0027's retired argument as the reason, and §11.5.1 still called it "deliberately not read", both false since ADR 0234 five rounds earlier — **and once more on the same row**, which pointed at "§10.5, which is `silent` now" forty-five rounds after ADR 0204 made it `implemented`. **The eighth generalises past files and paid there**: six comments cite "`doc/HANDOVER.md`'s section 0", moved whole into `doc/ui-boundary.md` by the three-hundred-and-ninety-fifth, so the file exists and the section does not. **And the round paid the demand-track item it had been carrying**: §12.5.6.19's seven unread Table 192 entries, three of them closed. `examples/push_button_census` counted first — 42 push-buttons in the corpus, **33 with their own `/AP /N`**, so nine can reach the construction and the only entry any of the nine states is `/IF` in a document with no icon. `/I`'s form XObject is drawn as a resource the interpreter resolves itself, Table 250's fit is implemented whole, and `/TP`'s codes 0, 1 and 6 are carried out while **2 to 5 are reported rather than invented**: each names the side the caption goes on and no clause states how much of the rectangle it takes. `/RI`, `/IX`, `/RC` and `/AC` are reported because a *constructed* appearance is one stream where §12.5.5 gives a stored one three — a shape question rather than a capability, and no corpus document states any of the four. Every gate reproduced except the test count (1404 → **1410**), which is the expected result for coverage work | ADR 0239 |
| 403 | **One band, two readers, and a population that was not the one named.** `doc/todo/22` asked this round to weigh three of §12.7.4.3's remaining edges against each other, and `doc/todo/13`'s rule — measure the population before writing the code — decided all three. `crates/pdf-model/examples/variable_text_census.rs` walks what §12.7.4.3 actually lays text out for across the 974: **622 widgets of a text field or a combo box, 305 of them with no `/AP` `/N` stream, and 73 of §12.5.6.6's free text annotations**, whose own clause sends them to the same layout. Of those 695, **254 state a Table 120 pair and every one reads the same under both rules**; the count of pairs the two rules disagree about is **0**. So the divergence ADR 0216 named and left — `Metrics::read`'s `ascent > 0 && descent < 0` against `pdf_font::measured_extent`'s band — could be closed as a *coverage* change with a measured robustness cost of nil. **The 42 font dictionaries the todo file cited were a different population**: they are `font_metric_census`'s, the fonts a *page's* content streams draw with and which reach `vertical_extent` and a selection highlight, and `Metrics::read` only ever sees a font `/DR` defines and a `/DA` names. One rule now decides what those two entries can say for both readers, with `DEFAULT_ASCENT`/`DEFAULT_DESCENT` kept as the fallback because they answer *where in its box does this text sit* rather than *how tall is this line*. Four tests, three of which fail against the old guard by 28, 2 and 6 points of baseline and the fourth of which is the control that must pass both ways. **§12.7.5.4's list box was measured and left refused**: **10 list-box widgets over 8 documents, every one with its own `/AP` `/N` stream and none in a `/NeedAppearances` document**, so the refusal takes no mark off any of the 974 — and the clause's own `shall`, that Table 234's options "shall be presented to the user", is discharged by `form::ChoiceControl` rather than by inventing a highlight the clause states nothing about. The same census corrected §12.7.5.4's row, which said no corpus document reaches either path with no appearance stream: **one combo-box widget of 26 does** — `annotation-choice-widget.pdf`'s read-only one, which states no value and therefore draws nothing and owes nothing. The composite `/DA` font stays open with its zero. **Nothing moved and the diff says so rather than the summary**: the whole gate sequence was run before and after, and the oracle's 1794 verdict lines are identical but for two timing lines, the corpus's 974 and quorra's 957 identical when sorted, the text gate identical outright — so `doc/todo/00`'s step 7 is not owed. Only the test count (1410 → **1414**), the citations (5625 → **5638**) and the quotations (546 → **549**) moved. The §12.7.4 and §12.7.5 ledger rows were read against the code and their table citations checked against the standard's own titles: **no wrong number in that family**, and the composite-font refusal the row claims is `Owed::FontUnusable`, by name | ADR 0240 |
| 404 | **A document that will not finish, and a millisecond and a half to take the thread back.** `doc/todo/34` item 3 had said since the three-hundred-and-eighty-first that a hostile document has no deadline and that what was missing is a cancel, with the shape left open. **The shape follows from who is on the other end**: the confined process is interpreting a hostile document, so a cancel it has to *agree* to is a cancel the document can decline — a flag the interpreter polls bounds only the work that reaches a poll, and when it reaches one is the attacker's choice. So the cancel is `SIGKILL`, which cannot be polled for or deferred, and the cost is in the type rather than in a comment: `ConfinedError::Cancelled` is what every later call returns, because the document went with the worker. `Canceller` is `Send + Sync`, cloneable and reaches the child through an `Arc<Mutex<Option<Child>>>` rather than a raw process identifier and a signal, which is what keeps `#![forbid(unsafe_code)]`; a kill unblocks the host's `read_exact` for free, since the pipe closes with the process. **`doc/todo/01`'s fifth sweep, run over this crate's own surface, found the one hole `canceller()` could not close** — `start` blocks reading the greeting, and a handle taken from the value it returns does not exist while it is blocked — so `Canceller::new` and `Confined::start_with` exist, publishing the child into the shared slot before the blocking read and re-checking the flag after. Demonstrated on a **1567-byte** document that draws for **44.2 s** and **44.3 s**: four levels of §8.10.1 form XObjects branching ten ways is 10 000 page-covering fills, because `MAX_FORM_DEPTH` bounds depth and nothing bounds breadth. The host's thread comes back in **0.83, 1.20, 1.38, 1.52, 1.72 and 1.97 ms** over six runs in two sittings. **Items 4 and 5 were priced rather than taken, and both prices were wrong in the file.** Item 5 said 19.2 MB down a pipe is most of the 67 ms: a new *ballast* line in `examples/confined_page` — a valid one-page document padded to the real one's length with a stream nothing refers to — puts the transport at **41–66 ms** while `dd \| dd` moves the same bytes in **3.7, 4.9 and 5.5 ms**, so **the kernel's pipe is a tenth of what the pipe was blamed for** and the rest is five passes over the document on our side. One of them was free: both ends now write the nine-byte header in a separate call instead of concatenating, and the 4.1 MB raster falls from **5.64 to 3.74 ms** median over nine runs each way, seven of nine after-samples below the before-minimum — the 19.2 MB open moved by less than its spread and is **not** claimed. The mapping stays a decision, and a sharper one: `memmap2::Mmap::map`, `MmapOptions::map_copy_read_only` and `rustix::mm::mmap` are all `pub unsafe fn` while `rustix::fs::memfd_create` is safe, so making and writing a `memfd` needs no `unsafe` and mapping one does; and `SCM_RIGHTS` would need `socketpair`, `sendmsg` and `recvmsg` on an allow-list a test asserts has none. Item 4 said one thread costs about 1 ms of 7: `strip_spans` says **2.2 → 1.3 ms** on a sparse page whose geometry grants two strips whatever is asked, and **19.9 → 7.2 ms** on ISO 32000-2 page 101 — twelve of twenty, and eighteen of thirty-one at 200%. Its `glibc` claim is now **measured** rather than written down: a probe warms 24 threads before the filter, confines, draws on 24 strips and broadcasts twenty rounds of 4 MiB allocations to every thread, and `strace` counts 25 `clone3` before the filter, none after, and no `openat` after it at all — a precondition and not an adoption, since a pool warmed first has the seccomp filter and not the Landlock domain, and `pdf-sandbox` has no per-thread entry point. Every gate reproduced except the test count (1414 → **1418**) and the citations (5638 → **5639**) | ADR 0241 |
| 405 | **Six spaces of nothing, behind a diagnosis a hundred and eighty sessions old.** The round worked the *contradicted* list rather than the ambiguous one, on `doc/todo/00`'s method and `CLAUDE.md`'s principle 5, and took `issue4304.pdf` and `issue4436r.pdf`. **The first was a defect and its group had never named it.** It sat in `CONTRADICTED_SUBSTITUTED_FONT` — a group whose own comment warns that its name is a hypothesis — and the four-panel strip said in one look what no metric had: four renderers draw *Words that should have spaces between them.* and this tree drew **Wordsthatshouldhavespacesbetweenthem.**, which is the exact defect pdf.js issue 4304 (*PDF Without Spaces*) collected the file to prove. The file is 895 bytes and one experiment: `/Differences [32 /.notdef …]` over a non-embedded `/Times-Roman` with no `/Widths`, so §9.6.5.1's Table 112 makes `.notdef` the name for code 32, §9.6.5.2 makes that an actual glyph whose appearance is the designer's — drawing nothing for it was always right — and §9.2.4 makes its *advance* the distance the text position shall move. **The column profile is exact rather than close**: our ink ran device x 11 to 176 and all four references' 11 to **191**, 15 pixels, which is six spaces of 250/1000 em at 10 pt to the pixel. §9.6.2.1's closing paragraph obliges a processor to supply the standard 14's glyph widths; `standard_metrics` is that provision and names only Adobe's published set, so `simple_widths` fell to its third source — the substitute program's own advance, which Table 109 requires the width to agree with anyway — and **that source read the program through `skrifa`'s `FontRef`, which parses an sfnt container and refuses a bare CFF**, which ten of the fourteen compiled-in faces (ADR 0133) are. So every serif, fixed-pitch and symbolic standard-14 substitution answered nothing there and every unanswered code took Table 120's `/MissingWidth` default of 0. `pdf_font::cff::advances` is the reader that was missing, asked only about the codes the metrics did not answer, which is what keeps it at **+25 µs** of interpret on `standard_fonts.pdf`'s fourteen faces and **+18 µs** on this page (best of 15, `open_one`). `FoxitSerif` states 250 for `.notdef`; so do `NimbusRoman`, and `NimbusSans` 278 and `NimbusMonoPS` 600, each its own `space` — which is *why* four renderers agreed and is corroboration rather than the target. **The second page was a diagnosis wrong about the clause, and the clause already lived in the ledger.** `CONTRADICTED_SUBPIXEL_IMAGE` said "[n]othing in ISO 32000-2 decides this" and reasoned from §8.4.3.2's silence about images; §10.7.4's sampled-image paragraph decides it — "only those pixels whose centres lie within the region shall be painted" — and its answer is that `issue4436r.pdf`'s 1×1 image mask under `180 0 0 -0.48 10 25 cm` paints **nothing at all**, because its device region is y `[25, 25.48)` and row 25's centre is 25.5. Coverage of that row: the clause 0.000, the geometry 0.480, ours **0.502**, and `poppler`, `mupdf`, `ghostscript` and `hayro` 1.000 apiece — five renderers departing in two directions, ours being §10.7.4's departure (1) and theirs the *shape* rule applied to an image, whose "no shape ever disappears" guarantee the image paragraph's own "though not identically" declines to carry over. The 0.502 against 0.480 is `tiny-skia`'s four sub-rows, the quantum ADR 0226 removed for a rectangle and which an image does not reach. §8.4.3.2's ledger row carried the same retired reasoning and was corrected with it. **Oracle 904/69 → 905/68, one verdict of 1794 moved**; corpus 65 incomplete, quorra 912/36/9, text 99.2% and the other 1793 verdicts unchanged. `doc/todo/00`'s step 7 run **before and after** and all 786 lines byte-identical, which is the sweep saying no ambiguous page's ink changed — `standard_fonts.pdf`'s fourteen are in that population and Adobe's metrics answer every code they use. Four ledger rows read against the code and their Tables 109, 112 and 120 checked against the standard's own titles; three stale counts corrected — `CONTRADICTED_SUBSTITUTED_FONT`'s header said 19 for a list of 18, `oracle-and-corpus.md` §3's breakdown summed to 72 for a list of 68, and a comment called the Foxit faces "bare Type 1" where they are bare CFF. **No ADR**: the ordering of `simple_widths`' three sources was already decided and written down; this round made the third reachable | §9.6.2.1, §9.6.5.2, §10.7.4 |
| 406 | **A verdict decided by two renderers and reported against a third.** The round worked the *contradicted* list on `doc/todo/00`'s method and found the defect in the instrument rather than on a page. `measurements` folded over **every** reference while printing a bound derived from the *consensus* pair, and printed **three** of `Tolerance::accepts`' **four** bounds — so **30 of the 68 contradicted lines showed every visible number inside the printed bound**, and `smask_luminosity_oob_transfer.pdf` read 27.02 against a bound of 1.11 where our distance from the pair that decides it is **1.25** (the 27.02 is `poppler`, which sits 34–36 of 255 from all four other renderers there). Corrected: 43 of 68 now report a different comparison, none passes every bound, and **38 fail on exactly one — the differing fraction**. Largest casualty is a diagnosis: `CONTRADICTED_GLYPH_EDGES` had said since session 75 that its pages fail "only on mean absolute difference — 5.4 to 6.4 against a bound of 5.00", and every number was `ghostscript`'s on 21 pages that all read "poppler and mupdf agree"; against the deciding pair the means are 1.01–2.57 of 5.00 and **all 21 fail on the differing fraction and nothing else** — which *strengthens* the group, a count of channels that moved being exactly what a sub-pixel phase shift makes and an average exactly what it does not. The list also gets a ranking, four hundred sessions after the ambiguous bucket got one. Pages re-derived: `bug847420.pdf` (head in levels at 8.65 nearest / 4.64 reference spread — `/Widths` exact at 8×, 1420 columns against both references' 1419, face 3.6% lighter; §9.5 NOTE 5, cited for the first time in a group that had argued from it since session 6), `CONTRADICTED_LINK_BORDER`'s other two members (no `/F` on any of the three, so Table 167's Print bit is clear on all three and not only on the one that was checked) and `smask_luminosity_oob_transfer.pdf`. **No rendering code touched, no pixel moved, no verdict moved**; step 7 not owed, run anyway, holds for the ninth time. ADR 0242. |
| 407 | **A bound below the spread it was measured on.** The four-hundred-and-sixth session left 38 of the 68 contradicted pages failing exactly one of `Tolerance::accepts`' four bounds — the differing fraction — and this round asked which it was: the bound catching what the averages hide, or a bound nobody derived. **Measured the way this project's structural floor of 0.90 was measured**, by asking how far the *references* sit from each other. New instrument, `oracle.rs`'s `the_fixed_bounds_against_the_references_own_spread`: **9898 reference pairs** over the oracle's own 1794 pages, split by tolerance class and by whether the pair crosses the hinting boundary, **each measure taken over the pairs the other three bounds admit** — which is `Tolerance::VECTOR`'s own stated method and is what stops a bound being measured over the population it defines. **On text pages the four `TEXT_HEAVY` bounds reject 0.0% (mean), 1.2% (worst tile), 0.5% (structural similarity) and 29.4% (differing fraction) of the reference pairs that agree by everything else**; the same measure on *vector* pages rejects 2.8% against siblings' 0.0%, 9.7% and 3.3%, so it is one number in one class rather than an argument about counting channels. **And the sentence claiming to derive it names another measure's number**: re-run on the population it cites — the 14 specification PDFs' first pages, 42 pairs — the worst tile reproduces to the digit (p90 26.72, max 28.17, "26 to 28") while the differing fraction is median 3.11% and **max 5.14%, with 11.9% of those pairs already outside the 5.00%**; what *is* 2.7 there is `mean_error`'s maximum, 2.7355. **The bound is not moved, and both reasons are measured rather than asserted.** It does two jobs — consensus formation *and* the floor under `widened_to` — and raised to the derived 12.02% the corpus goes 905/68/786 → **1121/309/329**: 457 pages leave `ambiguous` and **278 arrive newly contradicted** against 37 leaving, which is 278 diagnoses rather than a round. The narrower move, a floor for our own side only, needs a pair including a non-hinting renderer with neither member ours — across that boundary the median differing fraction doubles, 1.69% → 3.42% — and the only renderer on the far side is `hayro`, which shares `skrifa` with this tree, so `widened_to`'s four-hundred-session-old "nobody has" stands. Three comments corrected: `differing_fraction` counts **channels**, not pixels, and not channels that moved *at all* (the threshold is four levels); `Tolerance::VECTOR`'s `basic` numbers re-run at 0.0016–0.0352 and 0.4062–1.0625. `report::summarise`'s reference-against-reference line prints **four** measures where it printed three — ADR 0242's omission one file earlier, and the reason nobody could see the bound sitting under the spread. **No rendering code, no pixel, no verdict**: the oracle's 1794 lines are identical to the round's own baseline but for two timing lines, corpus 65 incomplete, quorra 912/36/9/17, text 99.2%, conformance 5660/553 all unmoved, and step 7 not owed. Only the *skipped*-test count moved, 9 → **10** | ADR 0243, `doc/todo/12` |
| 408 | **The first host whose widgets are not ours.** `doc/todo/30` was the largest single thing this project owed and its order is the owner's: GTK4 first because `gtk4-rs` is Rust-safe with no C++ bridge, Qt second, and the C ABI last, "do not freeze a C ABI until two Rust consumers have shaken the API out". `crates/viewer-gtk` is the first of the two — a real `GtkApplicationWindow`, a `GtkListView` over a `GtkTreeListModel` for §12.3.3's outline, §8.11.4.3's `/Order` and §7.11.4's files, real `GtkEntry`/`GtkPasswordEntry`/`GtkCheckButton`/`GtkDropDown`/`GtkListView` controls placed over `Query::Fields`, chrome drawn in the theme's own colour, and the two decisions a host owns. **The `gtk4` dependency is confined to that one crate**, 41 packages all MIT, `cargo deny` clean on all four, and the host keeps `#![forbid(unsafe_code)]` — which is the property the owner's ordering chose GTK4 *for*. **Tier 1, and GTK4 admits no other**: a widget has no native surface for tier 2 and GSK hands out no device for tier 3, so `Query::Frame`'s raster becomes a `gdk::MemoryTexture` with **no conversion at all**, `Raster` being `GDK_MEMORY_R8G8B8A8` exactly — 2 687 100 bytes in **0.61–1.14 ms**, ≈3.2 GB/s, which is `doc/ui-boundary.md`'s estimate measured through a real toolkit for the first time. **It needed no new message**, which is the round's strongest sentence about ADRs 0116–0121: ten sessions of hosts added five messages and a whole native host added none. Seen rather than described, under `Xvfb` with `xdotool` and `xwd`: first frame at **96.5 ms** median of five on a 5-page document and **152.1 ms** on ISO 32000-2's 1023, the whole difference being `Open` at 3 ms against 40, which is incremental parsing holding through a second host; two `Right` presses turning pages rasterised in 2.65 and 6.11 ms; `visibility_expressions.pdf`'s three layers as native check buttons, one click sending `SetGroup { group: 10 0, on: true }` and moving **6656 of 474 721 pixels** — the line *A and (not C)* going out, which is §8.11.2.2 evaluated; `160F-2019.pdf` giving `67 field(s) on the page, 76 control(s) placed`, and `xdotool type QRS` into one producing `Edit(SetField { field: "X.minus1", value: Some("-1QRS") })`; an attachment row extracting `foo.txt`, 9 bytes, `bar baz`; and §7.6.4.1's `GtkPasswordEntry` opening `issue6010_1.pdf` with `abc`. **Six boundary findings, none of them fixed this round and all of them written down**: `doc/todo/37`'s page-without-widget-appearances is now a photograph rather than a prediction and has a second consequence the audit never named — a platform control's *minimum size* overflows a 15-pixel widget rectangle, so removing the appearance underneath would not be enough; `pdf_render::RasterFormat` is `#[non_exhaustive]` and crosses the boundary, breaking `viewer-core`'s own rule that a new variant fails to compile in every consumer; `Answer::Field` answers a password field with bullets, so ADR 0201's read-the-value-back rule has an exception nothing points at; `Answer::Outline` borrows where the other panel answers are owned; no event says the page's *mapping* moved; and **GTK 4.22 exposes no accent colour at all** to application code, which sharpens the geometry argument rather than weakening it. `doc/todo/01`'s fifth sweep run with a **fifth** host crate: 246 `pub fn`s in `pdf-model`, 85 named by no host, and the GTK host names **not one** the other four do not — a native host reaches that crate for *types* and for no function of its own. Every gate reproduced except the test count (1420 → **1430**), the citations (5660 → **5758**) and the quotations (553 → **554**); the two cross-target checks pass unmoved and `viewer-gtk` is excluded from them deliberately, `glib-sys` saying so itself | ADR 0244 |
| 409 | **A page without the pictures somebody else is drawing.** The four-hundred-and-eighth session photographed the thing `doc/todo/37` had only predicted — 76 GTK4 controls over `160F-2019.pdf`'s 67 fields, every one on top of the picture of itself — and stated the cost it believed: a flag on the render request. **The first half of that was wrong and had to be**: `RenderRequest` is `viewer-core`'s *output* and carries a display list already interpreted, so a flag there arrives after the decision it governs. What the round kept is the property behind it — a value that is the old behaviour in every existing caller — and what it re-derived is where it lives: `pdf_model::view::ViewState`, beside `magnification`, because rule 1 makes that state the only channel by which anything outside the file may change what `interpret` draws. **§6.3.2.2 supplies the permission in the same sentence as the obligation** — a processor shall render every annotation's appearance stream "unless otherwise instructed" — and an instruction has an author, which decides the whole shape: `Command::Delegate(WidgetAppearances)` is host-supplied like `Restrict` (ADR 0212) and a different *kind* of policy, `ViewState::of` is the other value, and nothing in `pdf-model` or `viewer-core` may choose it for itself. **Which annotations it means is §12.5.6.19's own sentence**: widget annotations "represent the appearance of fields", so a host drawing the field has replaced exactly that and nothing else — §12.5.1's `/Tabs` `W` separates "[w]idget annotations ... followed by other annotation types" from the standard's side, and "leave out the widgets" and "leave out the annotations" are different requests. **Narrower than `/Subtype /Widget`, and that is the ink**: the set is built from `form::fields`, so it is exactly what a host was handed a control for, and a widget §12.7.4.2 leaves "simply a Widget annotation", a field past the `/Parent` bound and a widget with no `/Rect` all keep their appearance. **Byte-identity demonstrated rather than asserted**: `display_list_digest` prints page one's command count, `Debug` length and hash for all 974 corpus documents, run on `89de636` in a worktree and on this tree — **an empty diff over 975 lines** — and the instrument proved itself by accident first, differing on 96 documents until trap 10's sandbox worker was built in both trees. Seen under `Xvfb` from one binary two ways: **3257 of 1 320 000 window pixels** change on `160F-2019.pdf`, all inside the page, and `annotation-text-widget.pdf` goes from **687 to 161 display-list commands**. Census: **102 of 964 openable documents** have a delegable widget on page one, 637 widgets, 2723 commands. **The honest half is that 0.247% is small because a control that overflows its rectangle was already hiding what it duplicated** — measured now that it can be seen alone: **11 of 76 controls wider than their `/Rect` (worst +85 on 120 px) and all 76 taller (worst +22 on 12)**, which makes the *scale* a native form host draws at `doc/todo/30`'s next question. Two clause findings on the way: three source comments quoted a sentence about annotation drawing order that **ISO 32000-2 does not contain** — §12.5.5's "along with any previously painted annotations" is where the standard states it — and `Table 189's /R` for a widget's rotation names the *movie* annotation's table where Table 192 is meant, in code and in two documents. Every gate unmoved: corpus 65 incomplete, oracle 905/68/786, quorra 912/36/9/17, text 99.2%; tests 1430 → **1435**, citations 5758 → **5812**, quotations 554 → **557** | ADR 0245 |
| 410 | **The second host, and the one word it cost.** `doc/todo/30`'s order put Qt second *because* it costs a C++ bridge and should not be the experiment that shapes the API, and the order was right for exactly that reason: the bridge is where the awkwardness turned up, and none of it was the boundary's. `crates/viewer-qt` is a real Qt 6 Widgets application on the same vocabulary — a `QTreeView` over a `QAbstractItemModel` for the three panel answers, `QLineEdit`/`QCheckBox`/`QRadioButton`/`QComboBox`/`QListWidget` over `Query::Fields`, chrome on a `Qt::WA_TransparentForMouseEvents` overlay, §12.7.6.4's file and §7.6.4.1's password — and **it needed no new message either**. That was the stated condition on the C ABI, so **it may be frozen**, with three amendments named first, all of one shape: `RasterFormat` is `#[non_exhaustive]` and crosses the boundary, `Answer::Outline` borrows where its two siblings are owned, and `Answer::Field` answers a password field with bullets nothing says cannot be read back — a Rust host writes a line it should not have to, and a C consumer cannot fail to compile. **The one word is `deny`.** `#[cxx::bridge]` expands to `unsafe` — 18 lint errors, counted by trying `forbid` — and a `forbid` cannot be lifted, so the crate root denies with one exemption on `mod bridge` and **one hand-written `unsafe` token in the whole tree**: the `unsafe extern "C++"` header, which is `cxx` asking the author to assert the C++ exists and is safe to call. Discharged by `cpp/host.h` declaring **one** function naming no Qt type, and enforced by `tests/unsafe_position.rs`, which reads the sources back and asserts the file, the token, that `lib.rs` denies once and lifts once onto `mod bridge;`, and that no other crate in `crates/` lifts it. `cxx-qt` itself was declined — it is QML-shaped and links two initialisers a Widgets host never calls; `cxx` + `cxx-qt-build` is 23 packages, all MIT/Apache-2.0/Unlicense, **three of them reaching a shipped binary**, `cargo deny` clean on all four with no new exception. **`crates/viewer-host` is the round's other product**: the second host wanted four of `viewer-gtk`'s eight modules unchanged — the panel rows, the control decision, the file policy and the launch timeline named no GTK type — so they moved, and `viewer-gtk`'s whole public interface is now `Host` and `HostError`. **What Qt asked that GTK did not, six things and none a message**: `QAbstractItemModel` wants the tree *whole* with a stable identity per node where `GtkTreeListModel` pulls children lazily (990 rows build in **3.66 ms** against **3.34 ms** for fourteen — the laziness is worth nothing at outline sizes documents have); §8.11.4.3's switch is a *role* rather than a widget, and Table 99's `/Locked` lands better as the absence of `ItemIsUserCheckable` than as an insensitive button; the ownership **inverts** — C++ holds the `rust::Box` and there is no `RefCell` anywhere — which moves one compiler check into a promise `MainWindow::busy_` makes, because `QDialog::exec` runs a nested event loop; **Qt has the accent colour and GTK 4.22 has none**, selection drawn in `QPalette::Highlight` `#3daee9` and sampled back as `srgb(187,227,248)`, which is that colour at 0.35 over white; Table 233 bit 19's editable combo box is obeyed here and cannot be on `GtkDropDown`; and `RasterFormat`'s catch-all arm is now written **twice**. Seen under `Xvfb`: first frame **105.9 ms** median of five against the GTK host's **139.7 ms** in the same session, **193.8 ms** on ISO 32000-2's 1023 pages, four pages turned, `SetGroup { group: 10 0, on: true }` moving **6836 of 832 000** page pixels, `67 field(s) on the page, 76 control(s) placed`, `xdotool type QRS` producing three `Edit(SetField { field: "A.EMP" })`, `foo.txt` extracted at 9 bytes, and `abc` opening `issue6010_1.pdf`. **The tier-1 copy measured on both toolkits, cold and warm** — 748 µs/234 µs for GDK, 1078 µs/231 µs for `QImage` on 2.7 MB — which corrects ADR 0244's ≈3.2 GB/s into a *first-frame* number: the steady state is **11.5 and 12.0 GB/s**, agreeing to 4% because it is a `memcpy` either way. And ADR 0245's scale question is settled as a fact about platform controls rather than one theme: **13 of 76 wider and all 76 taller** on Qt against 11 and 76 on GTK. CI gained the two toolkits' development files, which neither host's round had noticed it lacked. Every gate reproduced except two counts (tests 1435 → **1454**, citations 5812 → **5882**) | ADR 0246 |
| 411 | **A boundary that cannot fail to compile.** The third and last host `doc/todo/30` names, and its three amendments first. **`pdf_render::RasterFormat` stops being `#[non_exhaustive]`** — it crosses `viewer-core`'s vocabulary inside `Rendered::Raster` and `Answer::Frame`, so the rule that a new message fails to compile in every consumer reaches it, and what the attribute bought was four runtime refusals for a condition no build could reach (`viewer-gtk`, `viewer-qt`, `viewer-ui`'s software composite, `viewer-confined`'s encoder), all four now an exhaustive one-arm `match`. **`Answer::Outline` is owned**: all five consumers cloned it anyway, because a panel outlives the query that filled it — **481 ns** for the application note's fourteen rows and **80.7 µs** for ISO 32000-2's 988, against the 3.66 ms ADR 0246 measured to build the models they go into. **And `Answer::Field`'s password value was not the doc sentence `doc/todo/30` predicted but a live bug**: ADR 0201 has a host read a field's value back after every keystroke, Table 231 bit 14 answers with bullets, and `viewer-ui` was therefore sending the bullets as the next value — so the variant changed shape to `Option<view::ShownValue>`, characters and `obscured` from one reading, every consumer failed to compile, and each was fixed where it stood (`viewer-ui` declines the field by name, the two native hosts refuse the write-back on the flag rather than on a control's kind). Reading that clause for it found **a second sentence nobody had read**: Table 231 bit 14's NOTE — *\"it is imperative that PDF processors never store the value of the text field in the PDF file if this flag is set\"* — and this tree stored it, so a person filling a form and saving it put their password in the file in clear text. `ViewState::save` now returns `Written { bytes, withheld }`, writes neither the value nor the appearance for such a field, and `viewer-core` reports each one; the test searches the **saved bytes** rather than reading the value back, because reading it back answers bullets either way. Then **`crates/viewer-ffi`**: 39 `extern \"C\"` entry points, `cdylib`/`staticlib`/`rlib`, a hand-written `include/pdf_viewer.h`, and four shapes each because C takes away something Rust gave — **commands are functions** (a union's size is part of an ABI; a symbol is not, so a command added later costs a compiled caller nothing), **events and answers arrive owned** so no borrow crosses and re-entrancy stops being a rule anybody keeps, **a render request is an opaque handle** the caller may move to its own thread with `render-cpu` behind `pdfv_render_request_rasterise` and the frame copied into the caller's buffer, and **a variant added later is named, described and counted** — every event answers a sentence whatever its kind, and `pdfv_abi_check` turns \"fails to compile in every consumer\" into \"fails to start, once, naming the number that moved\", which is weaker and is the strongest thing C admits. No `cbindgen`: the header is the artefact a C programmer reads, and what a generator would have bought is bought back by `header_and_library_agree.rs` — every entry point declared once in each file, every `PDFV_` constant the number the Rust gives it, checked by making that exact edit and watching it fail. **`c/open_a_page.c`**, `gcc -Wall -Wextra -Werror`, run by a test that skips only where there is no compiler: `abi 1 (header 1), 15 event kind(s) (header 15)`, `PDF20_AN001-BPC.pdf` open in **4.42 ms** and its first page drawn and handed back at **12.3 ms**, `outline: 14 row(s)` with depth and §7.3.10's two numbers on each, page 2 turned and drawn in **7.39 ms**, `frame: page 2, 708x1000, format 0, 2832000 byte(s)` copied in **1025 µs** with **13 113 of 708 000** pixels not white, and a one-byte buffer answering `PDFV_BUFFER_TOO_SMALL`; ISO 32000-2's 1023 pages open in **63.1 ms**, first page at **76.3 ms**, 988 outline rows, copy **149 µs**. The header collided with itself on the first compile — `typedef struct pdfv_frame_info` beside `int32_t pdfv_frame_info(…)`, because C has one namespace for both — which nothing in the Rust could have shown and is why the round demanded a C program. **The `unsafe` position**: crate root denies, one exemption on `pub mod abi`, and inside it one lint lift, 39 `#[unsafe(no_mangle)]`, 35 signatures and **no `unsafe` block anywhere**; `viewer-qt`'s test asked to be amended when this crate arrived and now asserts **exactly two** crates lift the denial, while this one asserts the seven crates touching PDF bytes still *forbid* it. `viewer-ffi` cross-compiles to both the Windows and macOS targets, unlike either toolkit host. Gates: tests 1454 → **1473**, citations 5882 → **5913**, quotations 557 → **561**; the ledger's statuses unmoved at 875 rows with §12.3.3, §12.7.4.3 and §12.7.5.3 renoted; `cargo deny` clean with **no new dependency at all** | ADR 0247 |
| 412 | **A list box can hold several, and one string could not say which.** The one message-shaped gap three hosts found, and it took no message. Sessions 408, 410 and 411 each built a §12.7.5.4 list box over `Query::Fields` and each asked its toolkit for *single* selection deliberately, because `Edit::SetField` carried one string while Table 233 bit 22 says "more than one of the field's option items may be selected simultaneously". **The reading half was already complete** — `ChoiceControl` has carried Table 234's `/Opt`, the selection as indices, and bit 22 itself since ADR 0235 — so the gap was exactly one direction wide: a host could learn everything the clause states and could not say the answer back. `Edit::SetField`'s value becomes `pdf_model::view::Entered` — `Cleared`, `Text(String)`, `Chosen(Vec<usize>)` — which is the fourth use of ADRs 0166/0167/0247's shape: the variant changes and every consumer fails to compile. **A selection is named by index rather than by label**, on the clause's own evidence: `/V` holds the labels, but two `/Opt` entries may carry the same name string and only the position says which was clicked, which is the ambiguity Table 234's `/I` exists for. The resolution from index to label happens **once**, in `ViewState::set_field`, because it needs the field's `/Opt` and three readings would be three chances for the picture and the file to disagree; a side effect is that §7.9.2.2's encoding, which used to run in `Field::read` *and* in `ViewState::save`, now runs once. **What is written is both of the clause's shapes** — a string for one item, an array of strings for several, `/V` removed for none — with `/I` written ascending beside a selection and **removed** where the new value is not one, because an `/I` disagreeing with a `/V` this program wrote is a file relying on the reader's tie-break to hide a contradiction. Bit 22 is **obeyed** rather than carried: a selection sent to a single-select field is cut to its first index, ADR 0197's shape; `Chosen` on a field that is not §12.7.5.4's is refused outright, because Table 230's `/Opt` is a *button's* export values under the same key. `viewer-gtk` builds a `GtkMultiSelection` and `viewer-qt` an `ExtendedSelection` where the bit is set, both reading the selection out of the model rather than from the signal's range, and both combo boxes send an index where the control is not editable. **The C ABI did not change and that is the finding**: `Command::Edit` is not among its 39 entry points, so a variant's shape moved under it for nothing — `PDFV_EVENT_KIND_COUNT` 15 before and after, which is the first demonstration of what that section predicted. Driven under `Xvfb` on `issue17492.pdf`, one of the corpus's **4** bit-22 widgets over 4 documents (`field_flag_census`): click *Oracle*, ctrl-click *DB2*, two rows highlighted at once in each toolkit's own colour, `s` — and **both hosts wrote byte-identical 71 524-byte files** carrying `/V [ (Oracle) (DB2) ]` and `/I [ 0 2 ]` over the producer's 70 166 bytes intact. Reading that document found a fact about files rather than a defect: its own `/V` holds the *export* values where §12.7.5.4 says it holds "the second of the two array elements", so this tree opens it with nothing selected and the other readers follow the file — principle 5 says the standard decides, and it did. The ninth sweep ran on the two ledger rows: every table citation checks out, and one *single-quoted* claim in §12.7.4.1 was a paraphrase wearing a quotation's clothes (Table 227 bit 1 says "an interactive PDF processor shall not allow a user to change the value of the field"), corrected. Gates: tests 1473 → **1481**, citations 5913 → **5965**, quotations 561 → **565**; `confined_wire` fuzzed 13 632 129 executions over the changed decoder with no crash; every render gate identical line for line | ADR 0248 |
| 413 | **The quotations nobody was checking, and a parent row wrong for 280 sessions.** A sweep round: all ten of `doc/todo/01`'s sweeps run over `ledger.toml` and over `crates/` after ten rounds that had added four host crates, and an **eleventh built** — the first thing in this project to read the *ledger's* quotation marks. `tools/conformance` verifies every rustdoc blockquote in `crates/` (**567** of them) and has never read a ledger note; the notes hold **977** quoted spans, **417** of which occur in no document under `doc/md/`. A gate cannot be built on that number, because a row quotes its own retired wording, `CLAUDE.md`, a report this program prints — and TOML has no `> `. **The discriminator is what makes the sweep usable**: a claim this project invented shares no words with the standard and a *misquotation* shares most of them, so only the misses that match for ≥ 5 words and ≥ half are reported — **12 suspects, 6 defects**. Three rows quoted Table 112's `/Differences` as "an array of character codes and **glyph** names" where the standard says **character** names; **§8.4.4 quoted §10.7.2 as "a PDF processor may ignore this parameter", a sentence ISO 32000-2 does not contain**, while §10.7.2's own row carries the real one; §8.3.2.4 dropped "(initial)" out of the middle of one and §7.9.3 elided without an ellipsis. The other six suspects are the *instrument*: the Markdown conversion breaks words across lines (`text-tospeech`, `hierarch y`), which `quote::normalise` does not repair either — two blockquotes written this round failed the gate until they were shortened. **And 17 rows carried 72 double-escaped quotation marks**, `\\\"` in the TOML, so 36 quotations rendered with stray backslashes; fourteen are the §8.4 family written in one sitting. **The tenth sweep's finding is the second-longest-lived this project has recorded**: §12.7.6 and §12.7.6.1 said "the other two are refused by name — a submission is a network request, an import wants a filesystem" from the ninety-seventh session, and §12.7.6.4's import has been *performed* since the hundred-and-thirty-second — **280 sessions**, with three other rows holding the right answer, including the child's own "[r]ead and performed". **The ninth sweep found five wrong table numbers, all in the source, and three are one entry**: `/H` is Table 191's and Table 176's, and `annotation.rs` still said 192 in three more places — the enum's doc comment and *twice inside the body of the function whose own doc comment the round before had corrected*. Also `view.rs`'s `/QuadPoints` in Table 179 and then in 166 (it is 182's), `pdf-font`'s `/MissingWidth` in Table 109 (120's), `requirements.rs`'s `/Schema` in 43 and collection item in 44 (153's and 46's), and **`tests/actions.rs`'s Table 166's `/A`, which was itself a correction of 197 and is 176's** — the first wrong-number-replacing-a-wrong-number this file has recorded. **The eighth sweep paid on a whole block**: `doc/todo/37` was deleted by the four-hundred-and-ninth and **seven citations survived**, one of them a live falsehood — `viewer-gtk/src/controls.rs` said the host "cannot" ask for a page without its widget appearances, three rounds after `Command::Delegate`. **The fifth sweep over eight host crates**: 327 `pub fn`s in `pdf-model`, 86 named by none of the original four and **85 by none of the eight**, so four whole new host programs take exactly one name off the list. Run a second way, over `viewer-core`'s own vocabulary against each host, it found **`Query::Find` and `Query::LogicalSelection` named by no program at all** — a text search nobody can press, and §14.8.2.5's logical content order `implemented` on the strength of a query with no consumer. The second is closed here: `viewer-ui` copies the page selection on `c` and says which of the two orders it got. Every gate reproduced but the three counts this round moved: tests 1481 → **1482**, citations 5965 → **5974**, quotations 565 → **567** | ADR 0249 |
| 414 | **A text search nobody could press, and the annex that had been waiting for it.** Session 413's fifth sweep found **`Query::Find` named by no program at all** — implemented since the hundred-and-fortieth, tested, carried across the confined transport, and unreachable in a tree with five programs and a C ABI. Beside it sat Annex O's `search`, whose refusal was the only one of the four that named a *capability*: "this program has no document-wide search". They were one piece of work, and neither could be done without deciding **what `Query::Find`'s scope is**. Measured first: interpreting all **1023** pages of ISO 32000-2 is **5.84 s**, 2.66 MB of readback, **5.7 ms** a page — so the document-wide question cannot be a `Query` (`&self`, asked at pointer speed, caching nothing), and `doc/ui-boundary.md`'s rules 3 and 4 say the core may neither block for it nor spend a clock it has not got. **So `Query::Find` stays the page and `Command::Find` is the document, one page per step**, with `Event::Searched { found, remaining, wrapped }` — the only event a host has to *pump*, which is the division `NeedsRender` already makes. Annex O's `search` is therefore **started when the document opens and finished by the host**: 5.84 s before `Command::Open` returned would have been the largest time-to-first-page regression this tree could make. Its own words settled two details — "one or more words" is a list, so any needle matching wins, and "the first matching word **in the document**" means it does not wrap where a find bar's does — and "[s]electing" is carried out as the selection, which is what every host already draws a highlight from. **A match highlight needed no new answer**: it is the same kind of thing as a selection highlight, so the geometry that already crossed was enough. **Three decisions the standard does not make, recorded as choices**, and the middle one has a derivation rather than a convention behind it: a space in the needle matches *whatever separated the words on the page*, because `separate_text` **infers** both space and line break from where §9.4.4's matrix put the next glyph — so matching a phrase against a literal `' '` would be matching it against this crate's own line-breaking, and "transparency group" broken across a line would go unfound; and whole-word matching is refused for that same sentence read the other way, since `text_extraction.rs` strips whitespace precisely because "a content stream does not record" word boundaries. **Six consumers failed to compile and the seventh could not**: `PDFV_EVENT_KIND_COUNT` moved **15 → 16** for the first time, four entry points took the ABI from **39 to 43**, and `c/open_a_page.c` pumps the search itself. **Three find bars, and the toolkits disagreed about colour in a way that sharpens the boundary's argument**: GTK and Qt both drew one hue at two alphas because neither hands out a second, and `viewer-ui`, having no theme to ask, drew two. **The window's own number was not the search's**: a full 1023-page miss took **19.25 s** because `viewer-ui` repainted on every step — 1024 whole windows through lavapipe to move a digit — and **6.19 s** once repainting was throttled to one step in sixteen. Every gate reproduced but the four counts this round moved: tests 1482 → **1491**, citations 5974 → **5980**, quotations 567 → **569**, entry points 39 → **43** | ADR 0250 |
| 415 | **The space a page composites in, and the entry that was being read instead.** `doc/todo/23`'s last population — a blending colour space that is not the device's three components, "4 documents, all `/DeviceCMYK`" for eighteen sessions — and the census that priced it found the four were the wrong four. §11.6.6 gives a group's `/CS` effect **"[f]or isolated groups"** and hands every other case to the parent; §11.7.2 repeats it with the reason; §11.4.7 puts the *page* group under the whole inheritance — "[t]hat initial colour space shall serve as the default blending colour space for each page" and "[a]ll page-level compositing shall be done in the default blending colour space of the page" — and **nothing in this tree read that entry at all**. So `issue14200.pdf` was reported for a `/DeviceCMYK` on a group stating no `/I`, on a page stating no `/Group`: a report that was never true, keeping a page out of the oracle's judgement. And **five documents departed in silence** — `bug1365930`, `bug1703683_page2_reduced`, `issue12798_page1_reduced`, `issue13520`, `personwithdog` — whose *pages* state `/CS /DeviceCMYK`, so every mark on them composites in ink. **Three of those five were already in the ambiguous bucket with a hand-written diagnosis about transparency**, and `AMBIGUOUS_STACKED_SCREEN_UNDER_MASKS` had written of `issue13520.pdf` that its five renderers show "five readings of a stack of `Screen` blends under luminosity masks, which is §11.6.6's blending space" — right about the family, wrong about the clause, and no gate could tell a hypothesis from a diagnosis. `Interpreter::blending` is the space in force, seeded from §11.4.7 and replaced by §11.6.6's inheritance, and the departure is reported **once, where the file introduces it**. **And ADR 0217's prediction was tested rather than inherited, and survives for a different reason than it gave.** §11.3.3 under `Normal` is a weighted average, so the two orders of operation agree exactly when the conversion out of the blending space is affine — the number of components has nothing to do with it. Over 200 000–300 000 random pairs: §10.4.2.5's classic formula away from its clamp, **3.3 × 10⁻¹⁶**; with the clamp reached, 117 of 255; with the clamp **deferred** onto three unclamped components, ADR 0220's trick one clause over, **3.4 × 10⁻¹⁶**; and **this tree's multilinear interpolation of the ink cube, 48 of 255** — affine on no face of it. Half of registration black over paper is `[76.0, 66.1, 63.9]` in `DeviceCMYK` against `[127.5, 127.5, 127.5]` on the device, **51.5 of 255**. So a four-component raster per group is genuinely required, **by the colour conversion this project chose (ADRs 0009, 0042) rather than by the clause**, and §10.4.2.5's ledger row now records that as what the decision costs. `issue14200.pdf` was diagnosed on its way into the bucket — `AMBIGUOUS_ICC_MATRIX_PROFILE`'s second member, a flat +0.15 of 255 on all three channels against two references that agree to 0.007 on a `mntrRGB` matrix-shaper `lcms` evaluates for both. **No raster changed**: the oracle's 905/68/786 are identical and only the complete/incomplete split moved, 1693/101 → 1690/104; corpus 65 → **68** incomplete, one false report out and four silences ended; quorra, text, dates, XMP and JPEG 2000 unmoved; step 7 holds for the eleventh time with the same five names to the thousandth. Tests 1491 → **1493**, citations 5980 → **6032**, quotations 569 → **576** | ADR 0251 |
| 416 | **The errata were in the file all along, and the conversion dropped them.** `doc/todo/48` asked for a census before anything was built, on the argument that the answer decides the item — 882 link annotations buy nothing, 882 review comments change how every clause is read. Both halves of that sentence were wrong. **There are 12 545 annotations across the fourteen documents and 11 462 in ISO 32000-2 alone**; the item's 882 were `/Annots` *arrays*, and the census the item's own rule demanded is what caught the factor of thirteen. Eleven documents carry links and nothing else. **The other three carry the errata**: 360 distinct `Issue #NNN` subjects, **434 strikeouts over 4038 words of the standard's own text across 252 sections**, each a §12.5.6.2 group of a `StrikeOut` with `/IT /StrikeOutTextEdit` and a `Caret` with `/IT /Replace` carrying the replacement, and 1752 §12.5.6.4 state annotations whose `/State` says how far each change got. **The body text under the strikeouts is the unamended 2020 text**, so `doc/md/` presents **79 struck passages as the standard's current words** and the conformance gate verifies a quotation of one without complaint — the exact failure principle 5 exists to prevent, arrived at by looking rather than by tripping over it for the fourth time. **Three of this tree's own quotations were quoting retired text**, all `/State Completed`: §7.9.4's `date.rs` quotes a sentence EC3 deletes outright — "Regardless of whether the time zone is specified, the rest of the date shall be specified in local time" — and what it carried has to come from the grammar instead, since `O HH'mm` offsets the other fields; §7.5.5's `write.rs` quotes "the byte offset **in the decoded stream** from the beginning of the PDF file", struck, and the sentence gains "or the beginning of the previous cross-reference stream" — **the one with teeth, and `xref::read_at` has read both for hundreds of sessions, so the code was right for a reason nobody here had found**; §14.7.6.1's `structure.rs` quotes "conforming product that owns", now "owner of". **`tools/spec-errata` is a sidecar and never a gate**: it uses `conformance::quote::normalise` so the question is asked with the gate's own comparison, and `conformance` does not know it exists — if the checker read a conversion this project generated, a defect in our extractor would become a defect in the standard. **What separates a finding from a coincidence is the clause and not the length**: measured at four, six and eight words the check answers ten landings, seven and one, and seven of the ten match "; shall be an indirect reference", which EC3 struck out of seven *other* tables. `Landing::in_clause` is the discriminator, and a threshold high enough to hide those seven hides two of the three real ones. **The proposal's second argument fails on the one document that matters**: all fourteen state a `/StructTreeRoot`, but `Tree::walk` stops at `MAX_CHILDREN` = 65 536 and ISO 32000-2's tree is larger, so `logical_order` sees the front of it — 1021 of 1023 pages state `/StructParents`, and `ParentTree::for_page` is the route that works. Every gate reproduced: corpus 68 incomplete, oracle 1794 (1690/104) with 905/68/786 and the undiagnosed list empty, quorra 912/36/9/17, text 99.2% (24043/24243), dates 1514/1545, XMP 318/319, JPEG 2000 14 identical. Tests 1493 → **1495**, citations 6032 → **6051**, quotations 576 → **577** | ADR 0252 |
| 417 | **Seventy-six passages read, and one of them was drawing the page wrong.** ADR 0252 left 79 struck passages `doc/md/` presents as the standard's current text and 76 of them unread; all 79 now have a verdict apiece in `doc/errata-read.md`. **Two clauses turned out to be implemented from retired text.** §12.5.2's closing sentence lists the annotation entries a reader ignores when an appearance stream is present, and Errata Collection 3 strikes `BM` out of that list and puts `MK` in — so §12.5.5's "composited … using the values of the BM, ca and CA entries" and Table 166's own unconditional `/BM` row have nothing against them, `annotation::blend_mode` reads the entry on both paths, and the §12.5.2-against-§12.5.5 contradiction this project recorded in the ledger is *half* settled by an erratum it could not see. §14.13.5's marked-content property list is keyed `/MCAF` and this tree read `/AF`, an inference from the tag that the 2020 text never contradicted, so a conforming PDF 2.0 file stated associated files and got an empty list with no report. §8.9.5.4's algorithm is rewritten and is **not** corrected — the amended step a) reads as terminal and would leave the amended d) unreachable, so it is recorded with the carets' own words rather than guessed at. **The instrument that named the seventy-nine was blind to seventy-two more**: it compared with whitespace kept, and both sides are extractions of the same glyphs by different programs — PDF positions glyphs and not words — so one writes `inthe` where the other writes `in the`. With the spaces dropped the count is **151**, and one of the 72 is the §12.5.2 sentence `crate::appearance` and `ledger.toml` were both quoting as live. **The `/State` question is answered by counting**: no note in any of the fourteen documents is `Rejected`, `Cancelled` or `None` — 827 `Completed`, 265 `Accepted` — so the mirror-image mistake cannot be made from these files, and `spec-errata` now prints `StateModel/State` because Table 174 has two state models and `Unmarked` is the other one's default. Fifteen doc comments and eleven ledger notes corrected, including two counts written last session that were wrong twice over. Tests 1495 → 1497, citations 6051 → 6070, quotations 577 → 575; no raster moved. ADR 0253 |
| 418 | **Three populations of quotation, and only one of them had a gate.** The other **54** distinct struck passages have a verdict apiece in `doc/errata-read.md`, so all **120** `spec-errata check` names are read. **A third clause turned out to be implemented from retired text**: Errata Collection 3 replaces §9.6.4's two-place rule for a Type 3 glyph description's resources with a pointer to §7.8.3, whose amended four-step search begins at "the stream dictionary of that glyph description content stream" — a step this tree did not have, so a glyph stream stating its own `/Resources` was read against the font's or the page's, silently. Fixed, with a test built so only the new step can answer it. **ADR 0249's 977 unchecked ledger spans are swept**, and the sweep needed none of the syntax that ADR priced: a span matching a sentence an erratum struck is the standard's by construction. It found a **third** population on the way — quotation marks inside ordinary rustdoc *prose*, which `CLAUDE.md` binds as hard as a blockquote and the gate's scanner walks past — and prose is the worst of the three: **eleven stale quotations — 1 blockquote, 6 prose, 4 in ledger notes** — over 18, 39 and 21 landings. Two of the six are the standard-14 `shall`s session 417 corrected in three files and missed in two more, and **four of the eleven were in the bucket `check` prints as "a repeated phrase rather than a finding"**, which is the third round running that `Landing::in_clause` has proved to be a sort order rather than a verdict. Containment now runs both ways, because a ledger note quotes five words out of a paragraph where a blockquote quotes the paragraph; prose is read a doc-comment *block* at a time, because a quotation worth checking is wider than 96 columns and a line-at-a-time reader sees two unmatched marks. **Two facts about the instrument**: a struck passage `doc/md/` still carries is not always retired — §7.5.4's #113 deletes one of two identical printed copies — and the two questions have different populations, since three errata acted on here are not among the 151 at all. Four of session 417's five owed items settled as documented choices; §8.9.5.4 still declined. Tests 1497 → **1498** (the line in `doc/todo/02` was two behind), citations 6070 → **6087**, quotations **575** unmoved. Corpus, oracle, text, dates, xmp, jpeg2000 and quorra all reproduced to the line. ADR 0254 |
| 419 | **A name the file never defined, and the silence around it.** Session 418's own defect had been invisible for the whole of this project's life because a `Do` naming an undefined resource returned without a word, where a missing *font* has been reported since the interpreter had fonts. §7.8.3 puts the requirement on the file — a resource dictionary "shall enumerate the named resources needed by the operators in the content stream" — and Table 86 states it twice more for `Do` alone, so this is a malformed file and this tree's rule for one is to draw what it can and say what it could not. **`Unsupported::MissingResource` is a new variant rather than a reused `Operator`**, because "the operator Do is not implemented" is false and a name the file never defines has no subtype, no size and no dictionary to describe. **The boundary taken is `Interpreter::resource` rather than `Do`**: its three callers are `Do`, `gs` and `scn`, and after this round every one of the six resource categories that can cost a mark reports its miss — `Properties` deliberately excluded, because a missing property list costs none. **The condition is §8.11.3.1's**: hidden content is skipped "as if there were no Do operator to invoke it", so nothing there is reported. Corpus 68 → **70**, both new reports and both looked at — `issue6541.pdf`'s tiling pattern names an `/XObject` only the page defines and the object is an *empty stream*, `issue8702.pdf`'s form XObject sits inside an object stream that §7.5.7 forbids to hold one, and `poppler` prints its own syntax error for it — with the two pages photographed beside the references and identical. Oracle **1690 → 1688 complete** with **every verdict count unmoved**, which is the whole cost. **The report found a defect on its first run**: `soft_masks.rs`'s §11.5.2 fixture named two `/ExtGState`s its group did not define, so the test asserted one number twice about a difference the file did not contain. And reading §7.8.3 for the clause found the round's fourth and fifth populations of unchecked quotation — a `"` inside an ordinary `//` comment, which the sweep skipped on a stated reason `CLAUDE.md` contradicts, and a quotation with an ellipsis in it, which the comparison could never match — **six stale quotations in six files**, two of them the two the round before recorded itself as having missed | ADR 0255 |
| 420 | **A readback kept under a bound, and the progress count that outlasted it.** ADR 0250 declined a readback cache because it would need "a byte budget and an invalidation rule"; the budget it lacked was a *number*, and the number was in the same ADR — ISO 32000-2's 1023 pages read back as **2.66 MB**, four times inside the owner's "below 10 MB is definitely ok". Measured first, in `find_step` alone through a new `viewer-core/examples/find_cost` that renders nothing: a cold sweep is **1.12 s (19.6%) of §7.7.3.2's page tree walked from the root once per page** and 4.59 s (80.4%) of content stream, which decides that a cache reaches the second search and nothing reaches the first. `crates/viewer-core/src/readback.rs` is 108 lines of shipping code: **4 MiB per open document**, least-recently-used, one constant, and readable through `Viewer::readback_cache` — a method rather than a `Query`, because six consumers and a wire protocol match that enum exhaustively for a number no interface draws. It sits **beside** `interpret` and never inside it, so `pdf-model` gained neither a `&mut` nor an interior mutability and the oracle's 1794-page comparison still rests on a pure function. **Repeated sweep 5.45 s → 7.27 ms**, cold sweep 5.51 → 5.61 s (unchanged, inside spreads of 0.16 and 0.32 s), peak RSS +2.3 MB on one sweep and −22 MB on two, medians of seven against the same binary with the budget set to zero. Invalidation is deliberately coarse — `Open::stale` empties *every* page's readback at all six sites that drop the display list — because the precise version needs an invariant maintained twice and `doc/todo/47`'s gate is that a search returning different results is a defect. **Then the window said the round was not finished**: 7.27 ms of searching cost **0.51 s** on screen, all of it `SEARCH_REDRAW` presenting 64 whole windows to move a progress digit — a constant that was right when a step cost 5.7 ms and is a proxy for time calibrated against one step cost. It is a clock now, in the host where rule 3 permits one, and a repeated search is **0.021 s** against a cold **4.79 s**. `--cache-text` **declined by measurement**, with the condition that revives it written down. Spec track: §9.4.4's ledger row was `partial` on one sentence false since the thirty-sixth session — the vertical writing mode it says is absent is `advance_step`'s own `if` — corrected with a text-layer test checked against its own mutation. Ledger 400/252 → **401/251**, tests 1506 → **1515**, citations 6087 → **6127**, quotations 575 → **580**; text gate 99.2% and the fourteen specification PDFs 100.0% both reproduced word for word, and corpus, oracle, quorra, dates, xmp and jpeg2000 line for line | ADR 0256 |
| 421 | **The joins that were missing, and a walk that saw half a tree.** The owner asked whether the API lets a program retrieve a page or a section with or without its annotations, and said the specification Markdown should stop being necessary — *"like a compiler starting to compile itself"* — with the note *"ignore the rest-api!"* on the file that recorded it. `doc/todo/36`'s three missing joins are built as **`tools/pdf-retrieve`**, a library and a JSON-on-stdout CLI over `pdf-model`'s existing readers: `document`, `outline`, `sections`, `page` and `section`, with `--annotations`, `--subtype`, `--no-artifacts` and `--logical`. §12.3.3's outline becomes a range of pages (`pdf_model::retrieval::sections`, all 988 of ISO 32000-2's items in 23 ms, 946 of them numbered), the text is cut at its two headings with the spaces squeezed out of both sides (ADR 0253's comparison), and §12.5.6.10's `/QuadPoints` decide **which section an annotation belongs to** rather than which page — `text_under` moved out of `spec-errata` into the model, 104 lines, two callers. **The default answer is `Interpretation::text` byte for byte**, asserted by a test, which is what keeps the owner's bootstrapping honest: `text_extraction.rs` and its `pdftotext` second opinion are untouched. Demonstration: §9.6.5.4 in **66 ms** — pages 339–341, trimmed both ends, 1077 words, and *no* erratum touches it; §12.5.2 answers **23** strikeouts and carets including the `BM, ` ADR 0253 found this tree implementing from retired text. **Two coordinate systems cost a defect on the way**: a `/QuadPoints` span indexes the raw readback, so a section whose artifacts were dropped reported one annotation fewer — `section` assembles its pages twice now, and a test asserts the two agree. `doc/todo/49`'s item 5 fixed rather than routed around, and it was worse than recorded: `Tree::walk` truncated at 65 536 items so session 416's **71 371** was the bound rather than the tree, which is **129 389**, and the visited set was a `Vec<Dictionary>` searched linearly — **16.8 s → 151 ms**, with the bound now `MAX_ELEMENTS` at 2²⁰ and *reported* through `Reading::truncated`, which `logical_text` (now `Option<String>`) and `logical_range` refuse on. **The substitution is measured and not done**: clause existence is free (506 distinct clauses cited, 0 missing from the outline), and of 582 blockquotes 553 are found in our own extraction once spaces and dashes are folded, leaving **29** to re-verify — the gap is typography, 59 of our quotations carrying the converter's `-` where the standard prints `—`. **No second transport**: `viewer-confined`'s pipe carries all twenty-eight questions and none of them is a page's text, so the pipe costs one `Query` variant rather than a transport, recorded with its size. Tests 1515 → **1525**, citations 6127 → **6190**, quotations 580 → **582**, ledger 875 rows unmoved; corpus, oracle, quorra, text, dates, xmp and jpeg2000 line for line | ADR 0257 |
| 422 | **A world larger than one corpus, and the first page it found.** `doc/todo/03` asked for repositories of test documents and a chunked fetcher for SafeDocs, with the owner on a mobile connection and the constraint *do not start the complete download*. Three shallow submodules under `doc/corpora/`, each licence read off its own file: **`pdf20examples`** (7 files, CC BY-SA 4.0 — the coverage denominator), **`pdf-differences`** (37, Apache-2.0 — files built to make readers diverge) and **`pdfbox`** (64, Apache-2.0), the last a *partial sparse* checkout — `--depth 1 --filter=blob:none --sparse` plus `sparse-checkout set pdfbox/src/test/resources/input`, **12 MB of a 118 MB repository in 4.5 s**. `openpreserve/format-corpus` examined and **declined on licence**: GitHub detects none, its README says CC0 *"unless otherwise stated"*, and the `/pdf/` directory `doc/test-docs.md` names does not exist. **`tools/safedocs`** is the fetcher for the one corpus that cannot be a submodule — 7 933 ZIP archives of a gigabyte each, 7.9 M files, ~8 TB — and the constraint — clarified mid-session as **"impossible by accident", not "impossible"**, since unmetered fibre is the rule and mobile the exception — is met structurally: the archive is addressed a member at a time and never as an object, a plan is resolved through its own central directory (a `HEAD` and 182 KiB against 1.2 GiB), a fetch is *one contiguous byte range* because a ZIP lays its members out consecutively, nothing moves without `--download`, a plan over 32 MiB is refused **in bytes and in the `--budget-mb` that would admit it** — no ceiling, and `--all` takes every member, so a whole 1.6 GiB archive is one sentence somebody typed — `curl --max-filesize` stops a server that ignores `Range` before the body starts, and every member is checked against the CRC-32 its own archive records. **No package added to `Cargo.lock`** — `curl` as a subprocess for the transport and 200 lines of ZIP in tree, because every ZIP crate wants `Read + Seek` and the range-issuing adapter is most of the work. **The short test, run**: 24 documents from archive `0000`, **19.9 MiB in 13 s**, plus the 108 in the submodules — 7/7, 30/37, 63/64 and 22/24 complete, and every report but one a population `doc/todo/21` or `23` already names. **The one that was not was a silence**: `UnknownFilter-PageContentStream.pdf` came back *complete with zero commands*, because its content stream object's dictionary ends with one `>` where §7.3.7 requires two, §7.3.10 then makes the reference null and §7.3.9 makes null an absent entry — a *conforming* blank page, and a page whose producer named a content stream is not a page whose producer stated none. `ContentIssue::Unreachable` says it now, ADR 0255's argument one clause over, and **the pdf.js corpus's count did not move by one document**, which is the sharpest statement available of why a second corpus was worth a session. Promotion budget **0 of 20 MB spent**: a submodule is pinned by commit, so a witness needs no copy. Tests 1525 → **1539**, citations 6190 → **6203**, quotations 582 → **583**, ledger 875 rows unmoved; corpus 974/70, oracle 905/68/786 with 1688/106, quorra 912/36/9/17, text 99.2% (23987/24187), dates, xmp and jpeg2000 all line for line | ADR 0258 |
| 423 | **A frozen opinion, and the permission it found declined.** `doc/todo/03`'s item 4: `doc/corpora/pdfbox` carries `*.pdf.txt` and `*.pdf-sorted.txt` beside **40** of its PDFs — Apache PDFBox's own `PDFTextStripper` output, checked in — so `text_extraction.rs` gained a *frozen* second reference beside the `pdftotext` it runs live, sharing that file's `fold`, `reference_words` and `without_spaces` so a difference between the references is about the documents rather than about the comparison. Whole documents rather than page one (`cweb.pdf` has 28); both of PDFBox's texts read and only the stream-ordered one gating, the `-sorted` figure printed beside it because where the two agree *reading order* is not what a shortfall is about — and on these 40 they agree exactly. **0.4 s and no new line in `doc/todo/02` §2**, which already runs every ignored test in that binary; its reference is a file where the pdf.js gate spends 30 s of its 31 waiting for `pdftotext`. First run **99.8% (14254/14281), 5 below the 0.90 floor**, and reading all five before ratcheting is what the round was for: **three were one defect**. §9.10.2's third method excludes an `Identity-H` font over `Adobe-Identity` **by name** — *"(except Identity -H and Identity -V )"* — so its `/ToUnicode` is its only method, and where that answers for some codes or none every method has failed and the clause's own next sentence is in force. `LoadedFont::text_from_program` refused a composite font outright, on a note that read the third method as applying to all of them, and so **declined a permission whose precondition the file had met**; the route is the same data one step longer — the `CMap` gives a CID, §9.7.4.2's `/CIDToGIDMap` gives the glyph, the program's `post` or inverted `cmap` names it, and §9.7.6.3's notdef fallbacks deliberately *not* taken. Measured rather than assumed, which is this clause's standing rule here: **pdf.js 23987 → 24003 of `pdftotext`'s 24187 words, named list 25 → 23, no document the other way**. Neither of the two that left was worked on, and `issue16553.pdf` had sat there as *"partial for reasons nobody has diagnosed further"* for **357 sessions** — an entry parked as undiagnosed is not one that cannot be diagnosed, and what moved it was a second population rather than a second look. Of the four remaining, two are right-to-left text in painting order and in Arabic presentation forms — §14.8.2.5.1 puts page content order in the content stream, and **no document in the 108 new ones writes §14.8.2.5.3's `/ReversedChars`**, measured with every `FlateDecode` stream inflated — and **two are a choice**: their `/ToUnicode` is an Identity CID `CMap` rather than the `bfchar`/`bfrange` mapping §9.10.3 requires, and their embedded programs carry neither a `cmap` nor a `post` table, so PDFBox reads the two-byte code itself as Unicode (*"the undocumented case"*, its own source says) where this tree declines — `text_from_the_code`'s argument is that §9.6.5's encodings are one byte per code, and a guard added this session says it. SafeDocs chunk from archive `3500` (**16.8 MiB, 24 documents, every CRC-32 matched**): 22 complete, both reports populations already named, **nothing promoted** and the budget still 0 of 20 MB. Tests 1539 → **1542**, citations 6203 → **6223**, quotations 583 → **584**, ledger 875 rows unmoved; corpus 974/70, oracle 905/68/786 with 1688/106, quorra 912/36/9/17, dates, xmp and jpeg2000 all line for line | ADR 0259 |
| 424 | **A lock on the hottest path, and the alternative that needed none.** The project owner's own question — why is the search single-threaded — measured end to end. `doc/todo/49` item 1 called `pdf_syntax::Document`'s `!Sync` "the actual blocker", and **the premise was false**: N documents in N threads needs nothing from `pdf-syntax` and reads ISO 32000-2's 1023 pages in **1.18 s against 6.11** on 24 threads, so what `!Sync` blocked was the arrangement that costs *less memory*, not the one that goes fastest. **Counted before anything was changed**, with a temporary counter build deliberately not left in the tree: `Document::get` is asked **829 times a page**, answers **92.7%** of a cold sweep from the cache and **100%** of a repeat — **and a fully warm object cache is worth 5.5% of the wall clock**, 6.15 s against 5.81, so a sweep's seconds were never in the cache. `resolve` runs ten times as often as `get` and mostly touches nothing, which puts the borrow count at about 1070 a page for 6 ms of work. The same census found a module comment false for as long as it had existed: **`decoded_stream_data` runs 12 717 times over one sweep and 11 975 times over the second sweep of the same document** — §7.4's filter chain re-run, not a cache — corrected, and left in `doc/todo/47` as a single-threaded lead that costs no memory at all. **The `RwLock` swap ships** at **+0.021%** of `callgrind_interpret` (2 208 807 721 → 2 209 269 060) and **−0.14%** of `callgrind_open` (78 464 732 → 78 357 201), with a cold sweep at 5.69 s → 5.78 s inside spreads of 0.30 and 0.36 over seven interleaved samples apiece. Lock poisoning is ignored in two four-line helpers with the argument written above them; the `loading` set became **per thread**, which is the one non-mechanical part — shared, it would answer §7.3.10's null to the second of two threads wanting one object at one moment, a wrong answer produced by timing, and there is now a test for exactly that beside a compile-time assertion that `Document: Send + Sync`. **The parallel search is declined, on memory**: `pdf-model/examples/parallel_sweep` puts 1023 pages at 1.61 s shared or 1.18 s per-thread against 6.11 on one thread, for **625 MB or 966 MB of peak resident against 225** — the owner's own bar being that 1 GB is definitely too much. Shared stops scaling past eight threads (61 836 misses are 61 836 exclusive locks) and *wins* on a warm repeat, 1.11 s against 1.22; the two facts have one mechanism. `doc/todo/49` item 3 is what is left and is now an API-and-memory question rather than a lock. **The launch's whole figure is deliberately not quoted against itself**: `EventLoop::new` ran 28 to 55 ms over twenty-four launches and which end a run landed on followed whether the X server had just gone idle — reversing the order reversed the difference — so what is claimed is `document joined`, 5.46 ms against 4.95, unmoved. Spec track: §7.5.7's row gained the two sentences a second reading found — a compressed object that is solely a reference, tolerated and bounded rather than enforced, and an object stream inside an object stream, refused outright — and `spec-errata` confirms no quotation in §7.5.7's or §7.5.8's five rows quotes text Errata Collection 3 struck. Tests 1542 → **1544**; ledger 875 rows at 401/251, citations and quotations unmoved; corpus 974/70, oracle 905/68/786 with 1688/106 and the undiagnosed list empty, quorra 912/36/9/17, text 99.2% (24003/24187) and PDFBox 99.8% (14257/14281), dates, xmp and jpeg2000 all line for line | ADR 0260 |
| 425 | **An archive that is a hash bucket, and a bound that was counting the wrong thing.** The owner lifted session 422's mobile-connection constraint — **up to 50 GB may be downloaded** — and `doc/todo/03` prescribed a **stratified** slice on the premise that files inside one archive "come from one neighbourhood of one crawl". The sampling rule, chosen to be repeatable rather than clever: **archive `50 + 100k` for k = 0 … 78**, the first 24 members of each, missing the two archives already taken and every thousand-boundary. **2731.0 MiB of member byte ranges and 14.1 MiB of central directories — 2.68 GiB, 5.4% of the budget — 1896 documents, 79 fetches, 0 failures, every CRC-32 matched.** **The premise is false, and the stratification is what disproved it**: the corpus is the whole crawl **sorted by SHA-256** and cut into 7933 equal pieces, and over 1944 cached members a file's own number — its rank among all 7 932 878 — and its digest as a fraction of 2²⁵⁶ agree to **2.6 × 10⁻⁴**, which is exactly the fluctuation that many uniform order statistics have by construction (√(p(1−p)/N) = 1.8 × 10⁻⁴ at the midpoint). So an archive is a hash bucket, nothing about a document can correlate with its content digest, any window anywhere is an unbiased sample of all of it, and a later round may go deep without paying 182 KiB of directory per archive for spread it does not need — `doc/todo/03`'s rule is now *whatever is cheapest among what nobody has taken*. **The survey: 1896 documents in 42.1 s: 4 unopenable, 1 locked, 0 encrypted beyond us, 3 pageless, 86 incomplete, 0 slow**, 862 codes reaching no glyph in silence over 12 documents. **Nothing failed to open for a reason that is this tree's** — all seven unusable documents were opened by hand and are crawl artefacts, four HTML pages saved under a `.pdf` name and three PDFs the origin server truncated at about a kilobyte with a Baidu link-submission script where the body should be. **And the failure modes are overwhelmingly already-named ones, which is the result rather than the disappointment: 67 of the 86 are §11.4.7's page-group blending space** — 59 `/DeviceCMYK`, 6 array-formed ICCBased checked with `group_space_census`, 2 with §11.4.4 beside them — **3.5% of the web against 0.7% of the 974**, which makes `doc/todo/23`'s standing item the largest correctness gap this tree has against real files by a factor of six over everything else together; 7 more are `doc/todo/21` §3 and 4 are §11.4.4, leaving 8 singletons. **One singleton was one clause with two defects.** §7.10.4's Table 41 bounds k **nowhere** — `/Functions` is *"( Required ) An array of k , 1-input functions that shall make up the stitching function"*, and the only value singled out is *"The value of k may be 1"* — while the same subclause fixes the other quantity outright, *"Domain shall be of size 2 (that is, 𝑚  =  1 )"*. One constant, `MAX_VALUES`, documented as bounding a component count, was serving both: a 256-stop gradient is k = 255, and `2750009.pdf` lost **four shadings** to it, with a *present* 510-number `/Encode` silently replaced by the identity behind that, because `/Bounds` and `/Encode` scale with k and were read through the same 256-number ceiling as `/Domain`. `numbers` and `pairs` now take the limit as an argument, so reading one array with the other constant is something somebody has to type. **And a `/Functions` array naming its own object was a stack overflow** — nothing in the standard forbids one, since a subfunction may be a stitching function and §7.3.10 makes a reference something a reader follows — which killed **`target/pdf-retrieve`, a shipped binary**, on a 720-byte file. This project's **first crasher**; `MAX_STITCH_DEPTH = 8` is the guard and is the same constant `breakpoints` walks to, whose comment had claimed "`parse` already bounds the nesting it will build" and never had. The breadth bound is a **budget over the whole tree one root builds**, because a per-array bound b with a depth bound d leaves bᵈ reachable; 4096 at 120 bytes a `Function` is 480 KiB, asserted rather than recalled. `crates/pdf-model/tests/hostile_functions.rs`, six tests, fixtures **generated** — so the promotion budget is still **0 of 20 MB**. **Thirteen fuzz targets and not one could have found it**: only `confined_wire` and `variable_text` reach `pdf_model::interpret`, and neither can build a shading, which is written down as owed rather than closed because `cargo-fuzz` is not installed here. Tests 1544 → **1550**, citations 6223 → **6240**, distinct tables cited 217 → **218**, quotations 584 and ledger 875 rows at 401/251 unmoved; corpus 974/70, oracle 905/68/786 with 1688/106 and the undiagnosed list empty, quorra 912/36/9/17, text 99.2% (24003/24187) and PDFBox 99.8% (14257/14281), dates, xmp and jpeg2000 all line for line | ADR 0261 |
| 426 | **Four components in two rasters, and the conversion that was the real blocker.** Session 425 measured `doc/todo/23`'s standing item at **3.5% of the web** — 67 of its 86 reports, six times everything else together — and ADR 0251 priced it at a four-component raster through the display list and three backends. **It is not a format.** §11.3.4 applies the compositing formula *per component* — "[t]he i th component of the result colour 𝐶𝑟 shall be obtained by applying the compositing formula to the i th components of the constituent colours" — so a rasteriser with three channels composites four by drawing the page **twice** with a different three loaded, and §11.3.5.2's separable blend functions come along unchanged. `interpret_with` interprets a page whose §11.4.7 group states `/CS /DeviceCMYK` once carrying `1−c, 1−m, 1−y` and once carrying `1−k`, both in §11.3.4's additive form so nothing is complemented around a blend; `pdf_render::BlendingSpace` carries the conversion out as the ink cube's sixteen corners and `blending::resolve` applies it **before** `impose_on_medium`, which is where the clause puts it; a geometry digest checks the two lists differ only in colour; `render-gpu` and `render-quorra` refuse by name (`QUORRA_FEEDBACK.md` section 17). **Route 2 was priced and lost**: deferring §10.4.2.5's clamp makes the composite exact and replaces the conversion out with a formula 115 of 255 off at the cube's corners, to fix an error of at most 48. **Then 59 of 69 web witnesses drew in ink and the pages were looked at, which is the round.** `0950007.pdf`'s green panel came back grey-green and its `0 g` text came back `#231F20` — neither a mark that composites with anything. They moved because §11.7.2 requires a colour to be converted *into* the space, §11.7.5.3 names §10.4.2.4 as the route, and §10.4.2.1 packages §10.4.2.2–5 as what a processor uses **instead of** §10.3 — the branch ADRs 0009 and 0042 put the conversion *out* on. **The standard's own pair round-trips exactly** (§10.4.2.4 then §10.4.2.5 is the identity, now a test); one branch composed with the other does not. So a page is drawn in its space only where every colour painted into it is already in it (`ColourSpace::is_subtractive`), and the rest keep §11.4.7's report **with the reason named**: 61 of 69 by §11.7.2, 6 by a four-component ICC space, 3 by §11.6.6, 2 by §11.3.5.3's rule for the black component, 1 by Table 57's `/BG` — which was **silent** until this round. **Web sample 89 incomplete with 69 naming the space → 82 with 62**; corpus **70 → 69**; oracle **1688 → 1689 complete**, every verdict count identical at 905/68/786 with ambiguous-complete 750 → 751; quorra 912/36/9/17 → **911/36/10/17**; text 99.2% (24003/24187) with 67 → 66 ungated, PDFBox 99.8%, dates, xmp and jpeg2000 line for line. `doc/todo/00`'s step 7 before and after: **one line of 786 differs**, `personwithdog.pdf` +0.719 [incomplete] → **+0.819**, the same +0.100 of ink its 576-dpi ladder shows, and the negative alarm holds for the twelfth consecutive time. Tests 1550 → **1558**, citations 6240 → **6317**, quotations 584 → **592**, ledger 875 rows at 401/251/19/83/8/113 unmoved | ADR 0262 |
| 427 | **A right inverse of the press, and the gamut it brings with it.** ADR 0262 left `doc/todo/23` with one population — **61 of 69 web witnesses** held by §11.7.2's conversion *into* the blending space — and read §11.7.5.3's black-generation bullets as naming §10.4.2.4 for it. **The paragraph above those bullets names a *target* instead**, and that decides the round: "in the transparent model it may instead be the group colour space of a transparency group into which an object is being painted", where the opaque model's target "shall always be the native colour space of the output device". So the conversion in is the same conversion as the one out with a different target, on §10.3's branch by §10.4.2.1's ranking — **one colour model per page and no boundary between two**, which is what ADR 0262's picture was about. `colour::rgb_to_ink` is a **right inverse of the ink cube**: §10.4.2.4's nominal `k` and separation start it, three chromatic inks are *solved* so the cube reproduces the colour (undercolour removal computed rather than asked of a function), a ladder of twelve black generations runs from all the black there is to none — reaching **above** the nominal, because this press's black is `#231F20` — a four-ink polish closes the bands between rungs, and what is left is outside the gamut and mapped to the nearest ink, **recorded as a choice**: §11.7.5.3 says the intent governs "taking into account the target space's colour gamut" and ISO 15076-1 says how, which this project does not hold. **§10.3.2's `should` for `DeviceGray` → CMYK was read and found not in force** — its condition is a CMYK *native device* — so session 426's "exempt `DeviceGray`" shortcut, which puts black text at `#231F20`, was declined for a reason. The search is **12.5 µs** a colour, so `rgb_to_ink` reads a 17³ table built once behind a `OnceLock` in **7.5–10.0 ms** on 24 threads and polishes from it: **791 ns** a colour, and the search's own answer to **0.50 of 255** over 800 in-gamut colours. **The pictures are the round's instrument**: all 61 witnesses rendered before and after, 12 byte-identical, biggest mover `0750022.pdf` at RMSE 2298 of 65535 — half what §10.4.2.4 cost — and **`0950007.pdf`'s green panel is `#007A61` before and after, to the byte**, with its black text black. What does move is saturated flat colour: `7550002.pdf`'s brand pink `srgb(235,0,69)` → `srgb(233,18,72)`, which is the gamut. Against `poppler` 46 of 61 move away, 3 toward, 12 unmoved, mean 3650.8 → 3711.3 of 65535 — **recorded, not acted on**, since `poppler` composites these pages on the device. **Found and reported rather than drawn**: a document that says what its own `DeviceCMYK` is (§8.6.5.6's `/DefaultCMYK`, §14.11.5's output intent) composites in *its* four components, not ours — 7 of 1944 web documents, 0 of 974. **Web sample 82 incomplete with 62 naming the space → 34 with 13**, 56 of the 69 drawn in it rather than 7; corpus **69 → 68** with 3 of 7 drawn rather than 1; oracle **1689 → 1690 complete** with every verdict count identical at 905/68/786 and **one line of 1794 changed**; quorra 911/36/10/17 → **910/36/11/17**, the eleventh refusal `bug1365930.pdf` re-ratcheted with its argument; text 99.2% (24003/24187) with 66 → 65 ungated, PDFBox 99.8%, dates, xmp and jpeg2000 line for line. Step 7 before and after: **one line of 786 differs** and the alarm holds for the thirteenth consecutive time. Page one of the 61 witnesses costs 18.07 s → 27.95 s, of which 8.0 s is ADR 0262's second pass and **1.8 s** this round's conversion, against 37.3 s when every colour ran the search. Tests 1558 → **1561**, citations 6317 → **6345**, quotations 592 unmoved, ledger 875 rows at 401/251/19 → **401/252/18** | ADR 0263 |
| 428 | **Thirteen targets that did not contain the code.** Session 425's first crasher — a 696-byte document whose §7.10.4 stitching function names its own object, which killed a *shipped* binary — was found by **downloading a corpus**, and `CLAUDE.md` puts fuzzing among the non-negotiables, so this round audited the instrument rather than adding to it. **The linker is the sharpest instrument available and nobody had asked it**: `nm | grep -c parse_stitching` over all thirteen fuzz binaries answers **0** for eleven of them, and `pdf_model::interpret` is present in exactly **one** — `variable_text`, which calls it on a page with no `/Resources`. So twelve of the thirteen did not contain the interpreter at all, and the correct statement is not that they could not construct the file — `document`, `forms_data` and `crypt` each hand a whole file to `Document::open` and could have written those 696 bytes — but that **nothing was on the other end of one**. libFuzzer's counters say it twice: `variable_text` and the new target link the same universe (238 744 against 237 171 instrumented edges) and their corpora cover **6483 against 28 535**, which is the *input shape* and nothing else — 5813 evolved seeds inside a fixed page reach 2.7% of what they link. **Both reasons the gap had been left open were false.** `cargo-fuzz` has been in `~/.cargo/bin` since **26 July**, a fortnight before the round that recorded its absence; it is simply not on `PATH`, so `which` answers a question about `PATH` and two rounds read that as a question about the disk. And ADR 0261's second claim — that `confined_wire` reaches the interpreter — is wrong: it fuzzes the wire *decoders* and the worker that interprets is across a pipe. **`fuzz/fuzz_targets/page.rs` is the fourteenth target**: the fuzzer's bytes are a whole document and nothing wraps them, opened, page one taken, `interpret` run — plus a property no test in this tree had ever checked under an input nobody wrote on purpose, that **interpretation is a function of the bytes** (same geometry digest, same text, same glyph count), which is what `CLAUDE.md` rests the whole cross-backend comparison on. Two limits carry their measurements: inputs past 256 KiB refused, past 16 KiB interpreted once, because a 236 KB corpus document is 0.8 s in release and **15 s** under the sanitiser. **It was checked against the crasher historically rather than argued about** — a detached worktree at `5fbf72a`, the target cut back to what compiled there, one input and no mutation: `AddressSanitizer: stack-overflow`, **2044 frames**, `parse_stitching` at the bottom. The same bytes take **4 ms** today. `fuzz/seed_page.py` is the fifth seeding recipe and applies `sfnt`'s lesson (ADR 0175) one layer up — libFuzzer will not invent a header, a page tree, a content stream and a resource dictionary that agree — seeding from every document on disk (1944 SafeDocs + 108 `doc/corpora` + 974 pdf.js → **1882** under the ceiling, `cmin` to 1535) and **printing what the seeds state** rather than assuming it: 100 `/Function`, 62 `/Pattern`, 58 `/Shading`, 332 form XObjects, 234 `/SMask`, 36 `/OCProperties`. **The run: 50 373 iterations over six forks in 3411 s — 0 crashes, 0 out-of-memory, 1 timeout — and the timeout is the sanitiser's**, 0.70 s in `target/pdf-retrieve` against 19.43 s in the fuzz binary on an idle machine, with the five `slow-unit-` artefacts reading the same way (0.03–0.89 s release). **No budget was weakened and none needed to be**; `-rss_limit_mb=4096` is the sanitiser's ceiling for a 1500-document corpus held in memory and not anything this program states. **And seeding was priced on an existing target**, which is a number the tree has never had: `document` had 7730 corpus files behind many rounds of evolution at **3010** edges, went to **4351 (+44.6%)** on adding the same 1884 real documents for one replay, and to **4568** after its own 50 000 runs in 515 s — a target whose input is a document and whose corpus is not documents has been testing the recovery scanner. Two things nothing was looking at, because the fuzz crate is deliberately its own workspace and neither §2 gate reaches it: three targets were not rustfmt-clean under the project's own `rustfmt.toml` and `x509.rs` had a `redundant_slicing` warning — both fixed, and `doc/verify.md` now carries the two commands that check them. Spec track: §7.10.4's ledger row read against `function.rs` and its three quotations checked against `doc/md/ISO_32000-2_sponsored_EC3.md` and Errata Collection 3 — all three stand, row keeps `implemented`. **Citations 6345 → 6346** (`SOURCE_ROOTS` is `crates`, `tools` and `fuzz`, and `page.rs` cites §7.3.10); every other gate line for line — tests 1561/11, corpus 974/68, oracle 1794 at 1690/104 with 905/68/786 and the undiagnosed list empty, quorra 910/36/11/17, text 99.2% (24003/24187), PDFBox, dates, XMP, JPEG 2000, ledger 875 at 401/252/18/83/8/113, quotations 592 | ADR 0264 |
| 429 | **Four rows about one mechanism, and the tool nothing had swept.** Fifteen rounds since the last sweep, the longest gap `doc/todo/01` has had. All twelve sweeps run over `ledger.toml`, `crates/` and — for the first time, because `SOURCE_ROOTS` reaches them — `tools/` and `fuzz/`. **The fourth sweep paid four times and all four are one mechanism**: §9.4 was `partial` on "§9.4.4 stays partial because the vertical branch … has no writing mode 1 to reach it" nine rounds after session 420 moved §9.4.4 to `implemented`; §9.7 named *both* halves of its reason as gaps — the predefined `CMap`s, shipped in session 156, and "§9.2.4's missing /W2 metrics", which §9.2.4's own row calls read since the **thirty-sixth**; §9.7.5 carried the first half a third time; §9.3.7 explained a true conclusion by "only mode 0 exists". **And §9.7.5.1 was `partial` above a note naming nothing owed**, which breaks the ledger's own definition of the status and which no sweep here looks for — a fourteenth is invented and named. Ledger 401/252 → **403/250**, and the status move cost §9.4 its evidence: two whole test *files*, which `FILE_ONLY_EVIDENCE_CEILING` refuses at zero on an `implemented` row, so the gate failed the moment the claim got stronger. **The fifth sweep over `tools/` and `fuzz/`**: 335 `pub fn`s, 111 named by none of the eight hosts and **89 by no host, tool or fuzz target** — so 22 names have a tool as their only consumer, `logical_text` among them, which this file listed as unread for 176 sessions. **`ViewState::clear_field` is named by nothing at all**, not even a test; one now. **Sweep 9's three wrong table numbers and two are in `tools/pdf-retrieve`** — Table 164, the transition dictionary, for annotation subtypes that are Table 171's and an entry that is Table 166's — plus `signature.rs`'s Table 259 for a `/TransformMethod` value that is Table 256's. **Sweep 10**: §11.7 states its count of its own family twice, "two of its five" and "four of its five", contradicting itself for **410 sessions**, and says nothing of the two rounds that changed the family most. **Sweep 11**: 1028 quoted spans, 3 misquotations — a full stop inside a truncation (§7.6.4.1), an elision without an ellipsis (§8.4.3.3), a capital dropped where the same row brackets four others (§12.3.3) — and one surviving `\\\"` the four-hundred-and-nineteenth's repair of 72 had missed. **Sweep 12 against the errata: 151 lines for 120 passages, unchanged in eleven rounds** — nothing remains unread — with two copies of §7.11.4.1's struck sentence left in `viewer-host` three rounds after `attachment.rs` was corrected for it. **A thirteenth sweep was built, run over all 264 ADRs and declined**: 64 hits, 2 defects, both already known — an ADR is a dated record, and what binds is that the round which disproves a claim amends the ADR that made it. ADR 0261 amended. Citations 6346 → **6349**; tests 1561 → **1562**, 11 skipped; every corpus-scale gate untouched, since the whole diff outside comments, docs and one test is empty | ADR 0265 |

## The twenty rounds from the three-hundred-and-fifteenth

**What the twenty have in common is one sentence, and it is the previous block's shape with the
tree in the place the capability used to be:**

> **The answer was already in the tree, and what was stale was the argument for not having it.**

Nine of the twenty found something this program already did, or already could, behind a sentence
saying otherwise — and the sentences were in ledger rows, in doc comments, in a todo file's header
and in this file.

- **§9.10.2's second method applies to a Type 3 font**, which `type3.rs` denied for three hundred
  sessions in a comment beside the field: "a glyph name in a Type 3 font names a procedure, so …
  the name is no evidence at all about the character". It names a procedure *and* a character.
  With the clause's own last-resort permission beside it and the rule that a glyph description's
  own text is the glyph rather than the page's, the readback went **98.2% → 99.2%** of
  `pdftotext`'s words and the documents below the 0.90 floor **36 → 25**.
- **A person can mark up a page** — §12.5.6.10's four subtypes over a selection, written back by
  §7.5.6 with the appearance stream this program draws — which needed one field (`Page::id`) that
  `doc/todo/33` had scoped and deliberately not added, and one distinction: the log records what
  was *done*, not what was asked for (ADR 0196).
- **Two rows in the §8.11 family were stale in ways no sweep could see**: §8.11.2.1 listed two
  Table 96 entries as "read by nothing" that had been read for 150 and 290 sessions, and
  §8.11.3.2 called the `DP` form unimplemented sixty-five sessions after a resource walk covered
  it by construction.
- **A selection was cleared by every re-interpretation**, on a comment naming the one case it was
  written for: "a range of the page that has just been replaced". A page drawn again is not a page
  turned, and every field edit took a person's selection away from them.
- **The chrome's own silence was measured** — 74 corpus documents state text §9.6.2.2's fourteen
  cannot set, nine strings drew as nothing at all — and a character with no code is a box now
  rather than a gap (ADR 0195).
- **And three counts in this file and the ledger were the instrument rather than the measurement**:
  the ambiguous bucket's "72 undiagnosed" was `wc -l` of a file with a twelve-line header, the
  oracle row's "1683 complete" was arithmetic where the gate prints 1685, and §9.10.2's row opened
  with the sixty-third session's 96.5% while six later sessions had moved it.

**The ambiguous bucket went 60 → 42**, and what came out of it is mostly *instruments*:

- **A ladder that is flat is not measuring an edge.** `issue269_1.pdf`'s three renderers are
  parallel across five resolutions, so the difference is a colour — and on a page whose colours are
  few the histogram is exact rather than statistical: ours and `poppler` byte-identical, `mupdf`
  two levels away (`AMBIGUOUS_DEVICE_CMYK_CONVERSION`).
- **On a small page the per-row mean is a free heatmap.** `issue19083.pdf` is 149 × 68 pixels and
  twenty rows of two numbers said what the ink table could only say the size of: two border lines
  are 78% of the whole difference.
- **The tightest limits this bucket has produced** — `160F-2019.pdf`'s two ladders agree to
  **0.0008 of 255**, `issue269_2.pdf`'s to 0.0001 — and on both, ours is within 0.02 of them while
  a reference at the page's own scale is 2.70 and 0.55 over its own.
- **And "check what else on the list is the same file" is not "assume it is the same answer"**:
  `issue840.pdf`'s two pages are a colour and an edge respectively.

**Three new instruments, each built because a claim needed a number**: `chrome_coverage` (what the
interface's font cannot set), `oc_usage_census` (which of Table 100's categories the corpus asks
for — six documents, and `Zoom` not once), and `readback` (what a page reads back as, which is the
direction the text gate is silent about and the only way to judge a rule that could invent text).
