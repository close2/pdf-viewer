[package]
name = "test-scenes"
description = "Canonical display lists shared by backend tests and the comparison harness."
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
pdf-render.workspace = true

[lints]
workspace = true
